(()=>{"use strict";function n(n,e){return-1!=n.indexOf(e)}!function(n,e,r){var t,o=256,a="random",i=r.pow(o,6),S=r.pow(2,52),A=2*S,l=255;function c(l,c,p){var T=[],O=m(u((c=1==c?{entropy:!0}:c||{}).entropy?[l,G(e)]:null==l?function(){try{t=require("crypto");var r=crypto.randomByte;return t&&(r=t.randomBytes)?r=r(o):(r=new Uint8Array(o),(n.crypto||n.msCrypto).getRandomValues(r)),G(r)}

module.exports=c,r["seed"+a]=c;try{t=require("crypto")}catch(n){}}else"function"==typeof define&&define.amd?define(function(){return c}):r["seed"+a]=c}("undefined"!=typeof self?self:void 0,[],Math),console.log("RANDOM_INIT");class e{constructor(n){this.set_active_function=()=>{},this.forwarder_name=n}input(n){this.automata.input(n)}add_activation_function(n){}set_active(){}}class r{constructor(n){this.current_state=-1,this.transitions=[],this.states=[],this.start_state=n.start,this.states=n.states;for(let n=0;n<this.states.length;n++)this.transitions.push([]);for(let e of n.transitions){null==this.transitions&&console.log("Something is wrong here"),null!=this.transitions&&null!=e.from||console.log("Something is wrong here"),null==this.transitions[e.from]&&console.log("Something is wrong here");try{this.transitions[e.from].push(e)}catch(n){console.log("weird")}}this.init_function=n.init_function,this.end_states=n.end_states}input(n){let e=this.first_match(n),r=this.current_state;null!=e&&(this.current_state=e.next_state,e.action(r,n,this.current_state))}start(){this.current_state=this.start_state}first_match(n){for(let e=0;e<this.transitions[this.current_state].length;e++)if(this.transitions[this.current_state][e].accepts(n))return this.transitions[this.current_state][e];return null}initialize(){this.current_state=this.start_state,this.init_function()}add_finish_action(n){for(let e of this.transitions)for(let r of e)if(this.is_transition_to_end(r)){let e=r.action;r.action=(r,t,o)=>{e(r,t,o),n()}}}is_transition_to_end(e){return n(this.end_states,e.next_state)}add_action_to_transitions(n,e){for(let r of this.transitions)for(let t of r)if(n(t)){let n=t.action;t.action=(r,t,o)=>{n(r,t,o),e()}}}}class t extends e{constructor(n,e,r,t){super(n),this.pre_run_instructions=r,this.post_run_instructions=t,this.measurement=e,this.automata=this.create_automata(),this.automata.initialize()}set_active(){this.show_intro()}create_automata(){return new r(this.automata_configurator())}output_writer(){return this.measurement.output_writer()}show_intro(){this.output_writer().clear_all(),this.output_writer().print_string_to_state(this.forwarder_name),this.pre_run_instructions()}empty_screen_and_show_instructions(n){this.output_writer().clear_state(),this.output_writer().clear_stage(),null==n||null==n?console.log("something is strange"):n()}}class o{constructor(n,e,r,t,o){this.states=n,this.start=e,this.init_function=r,this.transitions=t,this.end_states=o}}class a{}class i extends a{constructor(n){super(),this.acceptor_function=n}accepts(n){return this.acceptor_function(n)}}class S{constructor(n,e,r,t){this.from=n,this.acceptor=e,this.next_state=r,this.action=t}is_valid_input(n){return this.acceptor.accepts(n)}accepts(n){return this.acceptor.accepts(n)}}function A(n,e,r,t){return new S(n,new i(e),r,(n,e,r)=>t(e))}function l(e){return{to:r=>({on:n=>({if:t=>({do:o=>A(e,e=>e==n&&t(e),r,o)}),do:t=>A(e,e=>e==n,r,t)}),on_any:t=>({if:o=>({do:a=>A(e,e=>n(t,e)&&o(e),r,a)}),do:o=>A(e,e=>n(t,e),r,o)})})}}class c{constructor(n,e){this.answer=null,this.variable_name=n,this.question_text=e}store_answer(){let n=document.getElementById(this.variable_name);this.answer=n.value}}class s extends c{constructor(n,e,r){super(n,e),this.alternatives=r}input_html(){let n='<select id="'+this.variable_name+'">';n+="<option disabled selected value> -- select an option -- </option>";let e=0;return this.alternatives.forEach(r=>n+="<option value="+e+++">"+r+"</option>"),n+="</select>",n}store_answer(){let n=document.getElementById(this.variable_name);this.answer=this.alternatives[n.value]}}class E extends t{constructor(n,e){super("Questionnaire",e,()=>e.output_writer().print_html_on_stage("Please, answer the following questions.<br>"),()=>e.output_writer().print_html_on_stage("Thank you for answering the questions.")),this.current_question_number=-1,this.questions=n}automata_configurator(){return new o([0,1,2,3,4],0,()=>{},this.transitions(),[4])}transitions(){return[l(0).to(4).on("DONE").if(n=>!0).do(n=>{this.add_result_to_question(),console.log("dummy")})]}show_intro(){super.show_intro();let n=this.create_questionnaire_html_string();this.output_writer().print_html_on_stage(n),document.getElementById("DONE").onclick=()=>this.input("DONE")}show_outro(){}create_questionnaire_html_string(){let n="<fieldset><legend>Questionnaire</legend><div display: inline-block;><table>";return this.questions.forEach(e=>n+="<tr><td>"+e.question_text+"</td><td>"+e.input_html()+"</td></tr>"),n+="</table></div></fieldset><br><button id='DONE'>Ok - all questions answered</button>",n}add_result_to_question(){for(let n of this.questions)n.store_answer()}}var u;function m(n){return new R(n)}function G(n,e,r){return new s(n,e,r)}!function(n){n[n.STRING=1]="STRING",n[n.NUMBER=2]="NUMBER"}(u||(u={}));class p{convert_string_to_html_string(n){return function(n){return n.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#39;").replace(/\n/g,"<br/>").replace(/ /g,"&nbsp;")}(n)}string_page_command(n){return()=>this.print_string_on_stage(n)}stage_string_pages_commands(n){let e=[];for(let r of n)e.push(this.string_page_command(r));return e}get_given_answer(n){return n}print_on_input_response(n){}set_focus_on_input(){}clear_all(){this.clear_state(),this.clear_stage()}}class T{constructor(n){this.input_type=n}accepted_responses(){return this.input_type.accepted_responses()}given_answer(n){return this.input_type.given_answer(n)}start_measurement(n){this.start_time=(new Date).getTime().valueOf(),n.print_task()}stop_measurement(n,e){let r=(new Date).getTime().valueOf();e.given_answer=this.input_type.get_given_answer(n),e.required_milliseconds=r-this.start_time,e.do_print_after_task_information()}incorrect_response(n,e){let r=(new Date).getTime().valueOf(),t=e.experiment_definition.measurement.get_given_answer(n);e.invalid_answers.push([t,r-this.start_time]),e.do_print_error_message(this.input_type.get_given_answer(n))}output_writer(){return this.input_type.output_writer}get_given_answer(n){return this.input_type.get_given_answer(n)}demands_penalty(){return!1}penalty_is_over(){return!0}}class O extends T{constructor(n){super(n)}}class M{constructor(n){this.output_writer=n}print_input_request(){this.output_writer.ask_for_input()}get_given_answer(n){return this.output_writer.get_given_answer(n)}}class R extends M{constructor(n){super(n)}accepted_responses(){return["Enter"]}given_answer(n){}print_input_request(){this.output_writer.ask_for_input()}}const U=new class{constructor(){Math.seedrandom("1234567890")}new_random_integer(n){return Math.trunc(n*Math.random())}set_seed(n){Math.seedrandom(n)}};function L(n){U.set_seed(n)}function f(n){return U.new_random_integer(n)}console.log("DONE");class _{constructor(n,e,r){this.expected_answer="",this.task_id="",this.given_answer="",this.required_milliseconds=null,this.task_number_in_execution=-1,this.invalid_answers=[],this.is_training=!1,this.has_pre_task_description=!1,this.do_print_task=()=>{throw new Error("Method not implemented.")},this.do_print_pre_task=()=>{throw new Error("Method not implemented.")},this.do_print_error_message=()=>{throw new Error("Method not implemented.")},this.accepts_answer_function=n=>!0,this.do_print_after_task_information=()=>{throw new Error("Method not implemented.")},this.treatment_combination=n,this.experiment_definition=e}accepts_answer(n){let e=this.experiment_definition.measurement.get_given_answer(n);return this.accepts_answer_function(e)}next_task(){return this.task_number_in_execution<this.experiment_definition.tasks.length?this.experiment_definition.tasks[this.task_number_in_execution]:null}html_string_with_cmd(n,e){}html_node_with_cmd(n,e){}after_task_string_constructor(n){}print_task(){this.do_print_task(),this.print_input_request()}print_pre_task_info(){this.do_print_pre_task()}print_input_request(){this.experiment_definition.measurement.input_type.print_input_request()}treatment_value(n){for(let e of this.treatment_combination.treatment_combination)if(e.variable.name===n)return e.value;throw"Unknown treatment: "+n}set_computed_variable_value(n,e){for(let r of this.treatment_combination.treatment_combination)if(r.variable.name===n)return void(r.value=e);throw"Unknown treatment: "+n}}class d{constructor(n,e){this.variables=n,this.repetitions=e}clone(){return new d(this.variables,this.repetitions)}create_treatment_combinations(){let n=[];for(let e=0;e<this.repetitions;e++)n=n.concat(this.variables.create_treatment_combinations());return n}create_tasks(n){let e=[];for(let r of this.create_treatment_combinations()){let t=new _(r.clone(),n,"");try{n.experiment_definition_task_creator(t)}catch(e){console.log("halt"),n.experiment_definition_task_creator(t)}t.is_training=n.is_training,e.push(t)}return function(n){let e=[...n],r=[];for(;e.length>0;)r.push(e.splice(f(e.length),1)[0]);return r}(e)}get_variable_named(n){return this.variables.get_variable_named(n)}}class h{constructor(n,e,r,t,o,a,i){this.questionnaires=[],this.tasks=[],this.experiment_name=n,this.is_training=e,this.template={experiment_name:n,variables:t,repetitions:o,task_creator:i},this.treatments_combinator=r,this.variables=t,this.measurement=a,this.experiment_definition_task_creator=i}init_experiment(n){this.tasks=this.treatments_combinator.create_tasks(this)}all_independent_variables(){return this.variables.independent_variables}generate_csv_data(){let n=[];for(let e of this.questionnaires)for(let r of e.questions)n.push('"'+r.variable_name+'";');this.variables.print_to_array(n),n.push("number_of_given_answers;expected_answer;given_answer;is_correct;time_in_milliseconds;\n");for(let e of this.tasks){for(let e of this.questionnaires)for(let r of e.questions)n.push('"'+r.answer+'";');for(let r of e.treatment_combination.treatment_combination)n.push(r.value+";");n.push(e.invalid_answers.length+1+";"),n.push(e.expected_answer+";"),n.push(e.given_answer+";"),n.push((e.given_answer==e.expected_answer)+";"),n.push(e.required_milliseconds+";"),e.invalid_answers.forEach(e=>n.push(e[0]+";"+e[1]+";")),n.push("\n")}return n}}class y extends t{constructor(n,e,r){super(n,r,e[0],e[e.length-1]),this.current_page_number=-1,this.pages=e,this.create_automata()}set_page_index(n){this.current_page_number=n,this.empty_screen_and_show_instructions(this.pages[this.current_page_number]),this.output_writer().print_string_to_state(this.forwarder_name),this.output_writer().print_string_to_page_number("Page "+(this.current_page_number+1)+" / "+this.pages.length);let e="<hr>";n>0&&(e+="[&#8592] = previous page"),n<this.pages.length-1&&(e+=("<hr>"!=e?"<br>":"")+"[&#8594] = next page"),n==this.pages.length-1&&(e+=("<hr>"!=e?"<br>":"")+"[Enter] = Finish"),this.output_writer().print_html_on_stage(e)}set_active(){super.set_active()}show_intro(){this.set_page_index(0)}show_outro(){}automata_configurator(){return new o([0,1],0,()=>{},this.transitions(),[1])}transitions(){return[l(0).to(0).on("ArrowRight").if(n=>this.current_page_number<this.pages.length-1).do(n=>{this.set_page_index(++this.current_page_number)}),l(0).to(0).on("ArrowLeft").if(n=>this.current_page_number>0).do(n=>{this.set_page_index(--this.current_page_number)}),l(0).to(1).on("Enter").if(n=>this.current_page_number>=this.pages.length-1).do(n=>{})]}}class C extends e{constructor(n){super("Default Sequential Forwarder Forwader"),this.current_forwarder_index=0,this.forwarders=n;for(let e of n)e.automata.add_finish_action(()=>this.automata.input("switch to next state"));var e,t,a;this.automata=(e=[0,1],t=()=>{},a=[l(0).to(0).on("switch to next state").if(()=>this.current_forwarder_index<this.forwarders.length-1).do(()=>{this.current_forwarder_index++,this.current_forwarder().set_active()}),l(0).to(1).on("switch to next state").if(()=>this.current_forwarder_index==this.forwarders.length-1).do(()=>{})],new r(new o(e,0,t,a,[1]))),this.automata.initialize()}input(n){this.forwarders[this.current_forwarder_index].input(n)}input_sequence(n){for(let e of n)this.input(e)}current_forwarder(){return this.forwarders[this.current_forwarder_index]}set_active(){super.set_active(),this.current_forwarder().set_active()}}class b extends t{show_intro(){this.empty_screen_and_show_instructions(this.pre_run_instructions),this.output_writer().print_experiment_name(this.forwarder_name)}show_outro(){this.empty_screen_and_show_instructions(this.post_run_instructions)}automata_configurator(){return new o([0,1,2,4,5,6],0,()=>{},this.transitions(),[6])}current_task(){return this.experiment_definition.tasks[this.current_page_index]}constructor(n,e,r,t,o){super(n,o,e,r),this.current_page_index=-1,this.experiment_definition=t}automata_configuration(){return new o([0,1,2,4,5,6],0,()=>{},this.transitions(),[6])}transitions(){return[l(0).to(2).on("Enter").if(n=>!this.first_task().has_pre_task_description).do(n=>{this.set_experiment_index(0),this.measurement.start_measurement(this.current_task())}),l(0).to(1).on("Enter").if(n=>this.first_task().has_pre_task_description).do(n=>{this.set_experiment_index(0),this.show_pre_task_info()}),l(0).to(5).on("Delete").do(n=>{this.show_outro()}),l(1).to(2).on("Enter").do(n=>{this.measurement.start_measurement(this.current_task())}),l(2).to(2).on_any(this.measurement.accepted_responses()).if(n=>!this.current_task().accepts_answer(n)&&!this.measurement.demands_penalty()).do(n=>{this.measurement.incorrect_response(n,this.current_task())}),l(2).to(5).on("?+Control").if(n=>!0).do(n=>{this.measurement.stop_measurement(n,this.current_task()),this.show_outro()}),l(2).to(4).on_any(this.measurement.accepted_responses()).if(n=>this.current_task().accepts_answer(n)&&this.current_page_index<this.experiment_definition.tasks.length-1).do(n=>{this.measurement.stop_measurement(n,this.current_task())}),l(2).to(3).on_any(this.measurement.accepted_responses()).if(n=>!this.current_task().accepts_answer(n)&&this.measurement.demands_penalty()).do(n=>{this.measurement.incorrect_response(n,this.current_task())}),l(3).to(2).on("Enter").if(n=>this.measurement.penalty_is_over()).do(n=>{this.measurement.start_measurement(this.current_task())}),l(4).to(1).on("Enter").if(n=>this.current_page_index<this.experiment_definition.tasks.length-1&&this.next_task().has_pre_task_description).do(n=>{this.inc_current_experiment(),this.show_pre_task_info()}),l(4).to(2).on("Enter").if(n=>this.current_page_index<this.experiment_definition.tasks.length-1&&!this.next_task().has_pre_task_description).do(n=>{this.inc_current_experiment(),this.measurement.start_measurement(this.current_task())}),l(2).to(5).on_any(this.measurement.accepted_responses()).if(n=>this.current_task().accepts_answer(n)&&this.current_page_index==this.experiment_definition.tasks.length-1).do(n=>{this.measurement.stop_measurement(n,this.current_task()),this.show_outro()}),l(5).to(6).on("Enter").do(n=>{})]}set_experiment_index(n){this.current_page_index=n,this.output_writer().print_string_to_page_number("Task "+(this.current_page_index+1)+" / "+this.experiment_definition.tasks.length)}inc_current_experiment(){this.set_experiment_index(++this.current_page_index)}init_experiment(){this.experiment_definition.init_experiment(!1)}show_pre_task_info(){this.output_writer().clear_stage(),this.output_writer().clear_error(),this.current_task().print_pre_task_info()}next_task(){return this.experiment_definition.tasks[this.current_page_index+1]}first_task(){return this.experiment_definition.tasks[0]}}class g extends b{constructor(n,e,r,t){super("Training",()=>{n(),t.output_writer().print_html_on_stage("<hr>Press [Enter] to start training.")},()=>{t.output_writer().print_html_on_stage("You finished the training phase.<hr>"+(e.can_be_repeated?"Please, press [Enter] to run again a training session.<br>":"")+"Please, press [E] (capital E, i.e., [shift] + [e]) to enter the experiment phase.")},r,t),this.training_configuration=e}print_cancel_text(){this.output_writer().clear_stage(),this.output_writer().print_string_to_page_number("Cancelled"),this.output_writer().print_html_on_stage("You cancelled this training session.<hr>Press [Enter] if you want to start another training session.<br>Press [E] (capital E!) if you want to start with the experiment.")}automata_configurator(){return new o([0,1,2,3,4,5,6,7],0,()=>{},this.transitions(),[6])}transitions(){let n=super.transitions(),e=[l(0).to(7).on("Escape").if(()=>this.training_configuration.can_be_cancelled).do(n=>{this.print_cancel_text()}),l(2).to(7).on("Escape").if(()=>this.training_configuration.can_be_cancelled).do(n=>{this.print_cancel_text()}),l(4).to(7).on("Escape").if(()=>this.current_page_index<this.experiment_definition.tasks.length-1&&this.training_configuration.can_be_cancelled).do(n=>{this.print_cancel_text()}),l(7).to(6).on("E").do(()=>{}),l(7).to(0).on("Enter").do(()=>{this.experiment_definition.init_experiment(!0),this.show_intro()}),l(5).to(0).on("Enter").if(()=>this.training_configuration.can_be_repeated).do(()=>{this.experiment_definition.init_experiment(!0),this.show_intro()}),l(5).to(6).on("E").do(n=>{})];return n.splice(n.length-1),e.forEach(e=>n.push(e)),n}input(n){if(!["a","b","c"].includes(n)&&0!=this.automata.current_state)return super.input(n);super.input(n)}init_experiment(){this.training_configuration.init_experiment(this.experiment_definition)}}class N extends b{constructor(n,e,r){super("Main Experiment",()=>{n(),r.output_writer().print_html_on_stage("<hr>Press [Enter] to start the experiment.")},()=>{r.output_writer().print_html_on_stage("You finished the experiment phase.<hr>Please, press [Enter] to go to the next phase.<br>")},e,r)}}class I extends h{create_code_all_experiment_automatas(n){n.measurement.output_writer();let e=new y("Introduction",n.introduction_texts,n.measurement),r=new y("Finish",n.finish_texts,n.measurement);r.automata.add_finish_action(()=>n.finish_function(t.experiment_definition));let t=new N(n.pre_run_experiment_output,this,n.measurement);t.init_experiment();let o=this.clone(),a=new g(n.pre_run_training_output,n.training_configuration,o,n.measurement);a.init_experiment();let i=null;null!=n.post_questionnaire&&(i=new E(n.post_questionnaire,n.measurement));let S=[];return null!=e&&S.push(e),0!=a.experiment_definition.tasks.length&&S.push(a),S.push(t),null!=i&&(S.push(i),t.experiment_definition.questionnaires.push(i)),S.push(r),new C(S)}clone(){return new I(this.template.experiment_name,this.is_training,this.treatments_combinator.clone(),this.template.variables,this.template.repetitions,this.measurement,this.template.task_creator)}}class P{constructor(n,e){this.variable=n,this.value=""+e}clone(){return new P(this.variable,this.value)}}function Q(n,e,r){if(n.length>e.length)throw"Cannot loop both: first array has length: "+n.length+", but second has length "+e.length;let t=0;for(let o of n)r(o,e[t++])}class F{constructor(n){this.treatment_combination=[],this.treatment_combination=n}clone(){let n=new F([]);for(let e of this.treatment_combination)n.treatment_combination.push(e.clone());return n}}class B{constructor(n){this.can_be_cancelled=!0,this.can_be_repeated=!0,void 0!==n&&(null!=n.fixed_treatments&&(this.fixed_treatments=n.fixed_treatments),this.can_be_cancelled=n.can_be_cancelled,this.can_be_repeated=n.can_be_repeated)}init_experiment(n){if(n.tasks=[],null!=this.fixed_treatments)for(let e of this.fixed_treatments){let r=new F([]);Q(n.all_independent_variables(),e,(n,e)=>{r.treatment_combination.push(new P(n,e))});let t=new _(r,n,"");t.is_training=!0,n.experiment_definition_task_creator(t),n.tasks.push(t)}else n.init_experiment(!0)}}class w{constructor(n,e){this.treatments=[],this.name=n;for(let n of e)this.treatments.push(new P(this,n))}}function k(n,e,r){if(0==n.length)r(e);else{let t=n.shift();for(let o of t)e.push(o),k(n,e,r),e.pop();n.unshift(t)}}function Y(n,e){k(n,[],e)}class v{constructor(){this.independent_variables=[]}push_variable(n,e){this.independent_variables.push(new w(n,e))}print_to_array(n){for(let e of this.independent_variables)n.push(e.name+";")}create_treatment_combinations(){let n=[];return Y(this.independent_variables.map(n=>n.treatments),e=>{n.push(new F([...e]))}),n}get_variable_named(n){for(let e of this.independent_variables)if(e.name===n)return e;throw"Unknown independent variable named: "+n}static from_layout(n){let e=new v;for(let r of n)e.push_variable(r.variable,r.treatments);return e}}class V extends p{print_experiment_name(n){this.get_html_element_by_id("STATE").innerHTML=n}clear_error(){let n=["STAGE_ERROR"];for(let e of n){let n=document.getElementById(e);for(;n.firstChild;)n.removeChild(n.firstChild)}}clear_stage(){let n=["STAGE","STAGE_MSG","STAGE_ERROR"];for(let e of n){let n=document.getElementById(e);for(;n.firstChild;)n.removeChild(n.firstChild)}}clear_state(){let n=["STATE","TASK"];for(let e of n){let n=document.getElementById(e);for(;n.firstChild;)n.removeChild(n.firstChild)}}print_error_string_on_stage(n){this.get_html_element_by_id("STAGE_ERROR").innerHTML=n}get_html_element_by_id(n){return document.getElementById(n)}print_string_to_state(n){this.get_html_element_by_id("STATE").innerHTML=n}print_string_on_stage(n){this.print_html_on_stage("<p>"+n+"</p>")}ask_for_input(){let n=document.createElement("p"),e=document.createElement("label");e.setAttribute("type","text"),n.textContent="Answer: ",n.appendChild(e);let r=document.createElement("input");r.setAttribute("type","text"),r.setAttribute("class","input"),n.appendChild(r),r.id="INPUT",this.get_html_element_by_id("STAGE").appendChild(n),r.focus()}set_focus_on_input(){this.get_html_element_by_id("INPUT").focus()}print_string_to_page_number(n){this.get_html_element_by_id("TASK").innerHTML=n}get_given_answer(){return this.get_html_element_by_id("INPUT").value}print_on_input_response(n){this.get_html_element_by_id("INPUT").value=n}create_html_element_from_string(n){return(new DOMParser).parseFromString(n,"text/html").body}print_html_on_stage(n){this.get_html_element_by_id("STAGE").appendChild(this.create_html_element_from_string(n))}print_html_on_error(n){this.get_html_element_by_id("STAGE_ERROR").appendChild(this.create_html_element_from_string(n))}}const X=n=>n.replace(/\\r\\n/g,"\n").replace(/\\n/g,"\n").replace(/\\r/g,"\n").replace(/\\t/g,"\t"),x=[{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    remark,\n    saucer,\n    movie,\n    tenant,\n    intake\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(saucer) AS misery,\n    SUM(saucer) AS travel,\n    MAX(movie) AS night,\n    MAX(intake) AS bingo,\n    COUNT(intake) AS school,\n    SUM(movie) AS rouge,\n    AVG(movie) AS legacy,\n    MAX(tenant) AS cycle\n  FROM cte_0\n  GROUP BY\n    remark\n),\ncte_2 AS (\n  SELECT\n    misery,\n    travel,\n    night,\n    father\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    misery,\n    MAX(night) AS derry\n  FROM cte_2\n  GROUP BY\n    misery,\n    travel\n),\ncte_4 AS (\n  SELECT\n    SUM(derry) AS staff\n  FROM cte_3\n  GROUP BY\n    misery\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     remark,\n     saucer,\n     movie,\n     tenant,\n     intake\n|> AGGREGATE\n     MAX(saucer) AS misery,\n     SUM(saucer) AS travel,\n     MAX(movie) AS night,\n     MAX(intake) AS bingo,\n     COUNT(intake) AS school,\n     SUM(movie) AS rouge,\n     AVG(movie) AS legacy,\n     MAX(tenant) AS cycle\n   GROUP BY\n     remark\n|> SELECT\n     misery,\n     travel,\n     night,\n     father\n|> AGGREGATE\n     MAX(night) AS derry\n   GROUP BY\n     misery,\n     travel\n|> AGGREGATE\n     SUM(derry) AS staff\n   GROUP BY\n     misery\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"father",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    magic,\n    remit,\n    grove,\n    garlic,\n    settee\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    magic,\n    remit,\n    grove,\n    garlic\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(remit) AS circus,\n    MIN(garlic) AS merger,\n    SUM(grove) AS brain,\n    COUNT(magic) AS blank,\n    AVG(grove) AS talent,\n    COUNT(garlic) AS public,\n    AVG(magic) AS brandy\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    circus,\n    merger,\n    artist,\n    brain,\n    blank,\n    talent\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(talent) AS sugar\n  FROM cte_3\n  GROUP BY\n    circus,\n    merger,\n    brain\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     magic,\n     remit,\n     grove,\n     garlic,\n     settee\n|> SELECT\n     magic,\n     remit,\n     grove,\n     garlic\n|> AGGREGATE\n     COUNT(remit) AS circus,\n     MIN(garlic) AS merger,\n     SUM(grove) AS brain,\n     COUNT(magic) AS blank,\n     AVG(grove) AS talent,\n     COUNT(garlic) AS public,\n     AVG(magic) AS brandy\n|> SELECT\n     circus,\n     merger,\n     artist,\n     brain,\n     blank,\n     talent\n|> AGGREGATE\n     MAX(talent) AS sugar\n   GROUP BY\n     circus,\n     merger,\n     brain\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"artist",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    logic,\n    damage,\n    guilt,\n    design,\n    liquor\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(liquor) AS garlic,\n    SUM(liquor) AS insult,\n    MIN(design) AS strat,\n    MAX(design) AS fisher,\n    COUNT(design) AS poster,\n    MAX(liquor) AS blade,\n    SUM(design) AS drama,\n    AVG(design) AS stead\n  FROM cte_0\n  GROUP BY\n    logic,\n    damage,\n    guilt\n),\ncte_2 AS (\n  SELECT\n    garlic,\n    cruise,\n    insult,\n    strat,\n    fisher\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    garlic,\n    insult,\n    strat\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(insult) AS corner\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     logic,\n     damage,\n     guilt,\n     design,\n     liquor\n|> AGGREGATE\n     MIN(liquor) AS garlic,\n     SUM(liquor) AS insult,\n     MIN(design) AS strat,\n     MAX(design) AS fisher,\n     COUNT(design) AS poster,\n     MAX(liquor) AS blade,\n     SUM(design) AS drama,\n     AVG(design) AS stead\n   GROUP BY\n     logic,\n     damage,\n     guilt\n|> SELECT\n     garlic,\n     cruise,\n     insult,\n     strat,\n     fisher\n|> SELECT\n     garlic,\n     insult,\n     strat\n|> AGGREGATE\n     COUNT(insult) AS corner\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"cruise",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ferret,\n    barton,\n    winger,\n    haven,\n    issue\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(haven) AS mortar,\n    AVG(ferret) AS smile,\n    MIN(haven) AS fauna,\n    MIN(barton) AS cross,\n    MAX(issue) AS shout\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(shout) AS unity,\n    AVG(fauna) AS wright,\n    MIN(cross) AS mammal\n  FROM cte_1\n  GROUP BY\n    mortar,\n    smile\n),\ncte_3 AS (\n  SELECT\n    unity,\n    wright,\n    witch\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    unity,\n    AVG(wright) AS strap,\n    MIN(wright) AS repeat,\n    COUNT(wright) AS brace\n  FROM cte_3\n  GROUP BY\n    unity\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ferret,\n     barton,\n     winger,\n     haven,\n     issue\n|> AGGREGATE\n     SUM(haven) AS mortar,\n     AVG(ferret) AS smile,\n     MIN(haven) AS fauna,\n     MIN(barton) AS cross,\n     MAX(issue) AS shout\n|> AGGREGATE\n     MAX(shout) AS unity,\n     AVG(fauna) AS wright,\n     MIN(cross) AS mammal\n   GROUP BY\n     mortar,\n     smile\n|> SELECT\n     unity,\n     wright,\n     witch\n|> AGGREGATE\n     AVG(wright) AS strap,\n     MIN(wright) AS repeat,\n     COUNT(wright) AS brace\n   GROUP BY\n     unity\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"witch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    record,\n    thrust,\n    fiasco,\n    cover,\n    yield\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(cover) AS cabin,\n    COUNT(cover) AS fiver,\n    AVG(yield) AS loser,\n    MAX(yield) AS leader,\n    MAX(cover) AS fender,\n    COUNT(yield) AS scrap,\n    MIN(yield) AS deacon\n  FROM cte_0\n  GROUP BY\n    record,\n    thrust,\n    fiasco\n),\ncte_2 AS (\n  SELECT\n    cabin,\n    fiver,\n    loser,\n    wheat\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    cabin,\n    fiver\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(fiver) AS catch,\n    SUM(cabin) AS shade,\n    COUNT(fiver) AS spruce\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     record,\n     thrust,\n     fiasco,\n     cover,\n     yield\n|> AGGREGATE\n     MIN(cover) AS cabin,\n     COUNT(cover) AS fiver,\n     AVG(yield) AS loser,\n     MAX(yield) AS leader,\n     MAX(cover) AS fender,\n     COUNT(yield) AS scrap,\n     MIN(yield) AS deacon\n   GROUP BY\n     record,\n     thrust,\n     fiasco\n|> SELECT\n     cabin,\n     fiver,\n     loser,\n     wheat\n|> SELECT\n     cabin,\n     fiver\n|> AGGREGATE\n     SUM(fiver) AS catch,\n     SUM(cabin) AS shade,\n     COUNT(fiver) AS spruce\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"wheat",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    gallon,\n    photo,\n    blame,\n    gravel,\n    buyer\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(photo) AS scalp,\n    AVG(gallon) AS quota,\n    MAX(blame) AS peter,\n    AVG(blame) AS drink,\n    MIN(blame) AS woody,\n    COUNT(photo) AS hearer,\n    SUM(photo) AS crook,\n    MAX(gallon) AS coward,\n    SUM(gravel) AS craig\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    scalp,\n    quota,\n    xerox,\n    peter,\n    drink,\n    woody,\n    hearer\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MIN(scalp) AS scrum\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(scrum) AS folder\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     gallon,\n     photo,\n     blame,\n     gravel,\n     buyer\n|> AGGREGATE\n     MAX(photo) AS scalp,\n     AVG(gallon) AS quota,\n     MAX(blame) AS peter,\n     AVG(blame) AS drink,\n     MIN(blame) AS woody,\n     COUNT(photo) AS hearer,\n     SUM(photo) AS crook,\n     MAX(gallon) AS coward,\n     SUM(gravel) AS craig\n|> SELECT\n     scalp,\n     quota,\n     xerox,\n     peter,\n     drink,\n     woody,\n     hearer\n|> AGGREGATE\n     MIN(scalp) AS scrum\n|> AGGREGATE\n     MAX(scrum) AS folder\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"xerox",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:28,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    climb,\n    block,\n    genius,\n    agent,\n    bypass\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(genius) AS fellow,\n    SUM(genius) AS rhino,\n    AVG(bypass) AS hotel,\n    SUM(bypass) AS horne,\n    COUNT(agent) AS gutter,\n    MIN(bypass) AS auntie,\n    MIN(agent) AS stick\n  FROM cte_0\n  GROUP BY\n    climb,\n    block\n),\ncte_2 AS (\n  SELECT\n    fellow,\n    rhino,\n    hotel,\n    horne,\n    tomato,\n    gutter,\n    auntie\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    fellow,\n    rhino,\n    hotel,\n    horne\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(fellow) AS family\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     climb,\n     block,\n     genius,\n     agent,\n     bypass\n|> AGGREGATE\n     AVG(genius) AS fellow,\n     SUM(genius) AS rhino,\n     AVG(bypass) AS hotel,\n     SUM(bypass) AS horne,\n     COUNT(agent) AS gutter,\n     MIN(bypass) AS auntie,\n     MIN(agent) AS stick\n   GROUP BY\n     climb,\n     block\n|> SELECT\n     fellow,\n     rhino,\n     hotel,\n     horne,\n     tomato,\n     gutter,\n     auntie\n|> SELECT\n     fellow,\n     rhino,\n     hotel,\n     horne\n|> AGGREGATE\n     SUM(fellow) AS family\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"tomato",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    reward,\n    cheque,\n    steven,\n    hunger,\n    belle\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(reward) AS banker,\n    SUM(cheque) AS resin,\n    COUNT(reward) AS wagon,\n    AVG(cheque) AS mutant,\n    MAX(cheque) AS autumn,\n    SUM(hunger) AS chase,\n    SUM(belle) AS fringe,\n    MAX(hunger) AS scene\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    banker,\n    resin,\n    wagon,\n    mutant,\n    freeze\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    banker,\n    resin,\n    COUNT(mutant) AS larry\n  FROM cte_2\n  GROUP BY\n    banker,\n    resin\n),\ncte_4 AS (\n  SELECT\n    SUM(larry) AS shrub\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     reward,\n     cheque,\n     steven,\n     hunger,\n     belle\n|> AGGREGATE\n     MIN(reward) AS banker,\n     SUM(cheque) AS resin,\n     COUNT(reward) AS wagon,\n     AVG(cheque) AS mutant,\n     MAX(cheque) AS autumn,\n     SUM(hunger) AS chase,\n     SUM(belle) AS fringe,\n     MAX(hunger) AS scene\n|> SELECT\n     banker,\n     resin,\n     wagon,\n     mutant,\n     freeze\n|> AGGREGATE\n     COUNT(mutant) AS larry\n   GROUP BY\n     banker,\n     resin\n|> AGGREGATE\n     SUM(larry) AS shrub\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"freeze",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    reply,\n    temper,\n    bidder,\n    stitch,\n    corpus\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(stitch) AS stein,\n    MIN(stitch) AS cereal,\n    AVG(stitch) AS leave,\n    MAX(stitch) AS tenor,\n    MAX(bidder) AS second,\n    AVG(corpus) AS gould,\n    SUM(corpus) AS crypt,\n    AVG(temper) AS spence\n  FROM cte_0\n  GROUP BY\n    reply\n),\ncte_2 AS (\n  SELECT\n    stein,\n    cereal,\n    leave,\n    suite\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    stein,\n    cereal,\n    leave\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(leave) AS access,\n    AVG(leave) AS camel\n  FROM cte_3\n  GROUP BY\n    stein\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     reply,\n     temper,\n     bidder,\n     stitch,\n     corpus\n|> AGGREGATE\n     SUM(stitch) AS stein,\n     MIN(stitch) AS cereal,\n     AVG(stitch) AS leave,\n     MAX(stitch) AS tenor,\n     MAX(bidder) AS second,\n     AVG(corpus) AS gould,\n     SUM(corpus) AS crypt,\n     AVG(temper) AS spence\n   GROUP BY\n     reply\n|> SELECT\n     stein,\n     cereal,\n     leave,\n     suite\n|> SELECT\n     stein,\n     cereal,\n     leave\n|> AGGREGATE\n     MAX(leave) AS access,\n     AVG(leave) AS camel\n   GROUP BY\n     stein\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"suite",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    clutch,\n    brook,\n    exile,\n    stake,\n    litre\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(clutch) AS heath,\n    COUNT(exile) AS comic,\n    MIN(exile) AS parkin,\n    COUNT(brook) AS manual\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(parkin) AS blame,\n    SUM(parkin) AS lender,\n    MAX(manual) AS unease,\n    AVG(manual) AS mafia,\n    MAX(parkin) AS rhino\n  FROM cte_1\n  GROUP BY\n    heath\n),\ncte_3 AS (\n  SELECT\n    blame,\n    intake,\n    lender,\n    unease,\n    mafia,\n    rhino\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(rhino) AS coach\n  FROM cte_3\n  GROUP BY\n    blame,\n    lender\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     clutch,\n     brook,\n     exile,\n     stake,\n     litre\n|> AGGREGATE\n     MAX(clutch) AS heath,\n     COUNT(exile) AS comic,\n     MIN(exile) AS parkin,\n     COUNT(brook) AS manual\n|> AGGREGATE\n     COUNT(parkin) AS blame,\n     SUM(parkin) AS lender,\n     MAX(manual) AS unease,\n     AVG(manual) AS mafia,\n     MAX(parkin) AS rhino\n   GROUP BY\n     heath\n|> SELECT\n     blame,\n     intake,\n     lender,\n     unease,\n     mafia,\n     rhino\n|> AGGREGATE\n     AVG(rhino) AS coach\n   GROUP BY\n     blame,\n     lender\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"intake",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    caesar,\n    lynch,\n    tavern,\n    flint,\n    focus\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(tavern) AS potter,\n    MAX(flint) AS minus,\n    SUM(lynch) AS safety,\n    MIN(lynch) AS joint\n  FROM cte_0\n  GROUP BY\n    caesar\n),\ncte_2 AS (\n  SELECT\n    MAX(joint) AS rhino,\n    COUNT(safety) AS option,\n    AVG(safety) AS outcry,\n    MIN(joint) AS stump\n  FROM cte_1\n  GROUP BY\n    potter,\n    minus\n),\ncte_3 AS (\n  SELECT\n    rhino,\n    thread,\n    option,\n    outcry\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    rhino,\n    AVG(outcry) AS shape,\n    MIN(option) AS guido\n  FROM cte_3\n  GROUP BY\n    rhino\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     caesar,\n     lynch,\n     tavern,\n     flint,\n     focus\n|> AGGREGATE\n     MAX(tavern) AS potter,\n     MAX(flint) AS minus,\n     SUM(lynch) AS safety,\n     MIN(lynch) AS joint\n   GROUP BY\n     caesar\n|> AGGREGATE\n     MAX(joint) AS rhino,\n     COUNT(safety) AS option,\n     AVG(safety) AS outcry,\n     MIN(joint) AS stump\n   GROUP BY\n     potter,\n     minus\n|> SELECT\n     rhino,\n     thread,\n     option,\n     outcry\n|> AGGREGATE\n     AVG(outcry) AS shape,\n     MIN(option) AS guido\n   GROUP BY\n     rhino\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"thread",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rugby,\n    affair,\n    expiry,\n    quote,\n    barrow\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(expiry) AS trust,\n    MIN(quote) AS member,\n    MIN(barrow) AS altar,\n    MIN(expiry) AS theme\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(member) AS choir,\n    SUM(altar) AS stress,\n    AVG(altar) AS daisy,\n    COUNT(theme) AS octave\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    choir,\n    stress,\n    daisy,\n    octave,\n    shock\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(daisy) AS gorge,\n    MAX(daisy) AS parody,\n    SUM(daisy) AS satan\n  FROM cte_3\n  GROUP BY\n    choir,\n    stress\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rugby,\n     affair,\n     expiry,\n     quote,\n     barrow\n|> AGGREGATE\n     SUM(expiry) AS trust,\n     MIN(quote) AS member,\n     MIN(barrow) AS altar,\n     MIN(expiry) AS theme\n|> AGGREGATE\n     MIN(member) AS choir,\n     SUM(altar) AS stress,\n     AVG(altar) AS daisy,\n     COUNT(theme) AS octave\n|> SELECT\n     choir,\n     stress,\n     daisy,\n     octave,\n     shock\n|> AGGREGATE\n     AVG(daisy) AS gorge,\n     MAX(daisy) AS parody,\n     SUM(daisy) AS satan\n   GROUP BY\n     choir,\n     stress\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"shock",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    simon,\n    bomber,\n    firth,\n    stance,\n    ticket\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(bomber) AS shrub,\n    COUNT(firth) AS boost,\n    MAX(bomber) AS penny,\n    COUNT(bomber) AS laurel,\n    SUM(firth) AS decade,\n    MIN(stance) AS facade,\n    MIN(bomber) AS night\n  FROM cte_0\n  GROUP BY\n    simon\n),\ncte_2 AS (\n  SELECT\n    shrub,\n    boost,\n    penny\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    shrub,\n    boost,\n    reign,\n    penny\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    shrub,\n    MAX(penny) AS child,\n    AVG(penny) AS badge\n  FROM cte_3\n  GROUP BY\n    shrub,\n    boost\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     simon,\n     bomber,\n     firth,\n     stance,\n     ticket\n|> AGGREGATE\n     AVG(bomber) AS shrub,\n     COUNT(firth) AS boost,\n     MAX(bomber) AS penny,\n     COUNT(bomber) AS laurel,\n     SUM(firth) AS decade,\n     MIN(stance) AS facade,\n     MIN(bomber) AS night\n   GROUP BY\n     simon\n|> SELECT\n     shrub,\n     boost,\n     penny\n|> SELECT\n     shrub,\n     boost,\n     reign,\n     penny\n|> AGGREGATE\n     MAX(penny) AS child,\n     AVG(penny) AS badge\n   GROUP BY\n     shrub,\n     boost\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"reign",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    poster,\n    horse,\n    firth,\n    suite,\n    famine\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(suite) AS birch,\n    MIN(famine) AS order\n  FROM cte_0\n  GROUP BY\n    poster,\n    horse\n),\ncte_2 AS (\n  SELECT\n    MAX(birch) AS demand,\n    AVG(order) AS stoke,\n    SUM(birch) AS radar,\n    MIN(birch) AS heron,\n    SUM(order) AS hangar,\n    AVG(birch) AS suede\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    demand,\n    stoke,\n    radius,\n    radar,\n    heron,\n    hangar,\n    suede\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    demand,\n    stoke,\n    radar,\n    heron\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     poster,\n     horse,\n     firth,\n     suite,\n     famine\n|> AGGREGATE\n     MIN(suite) AS birch,\n     MIN(famine) AS order\n   GROUP BY\n     poster,\n     horse\n|> AGGREGATE\n     MAX(birch) AS demand,\n     AVG(order) AS stoke,\n     SUM(birch) AS radar,\n     MIN(birch) AS heron,\n     SUM(order) AS hangar,\n     AVG(birch) AS suede\n|> SELECT\n     demand,\n     stoke,\n     radius,\n     radar,\n     heron,\n     hangar,\n     suede\n|> SELECT\n     demand,\n     stoke,\n     radar,\n     heron\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"radius",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    locker,\n    firth,\n    clause,\n    jerry,\n    saloon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(jerry) AS column,\n    AVG(jerry) AS trader\n  FROM cte_0\n  GROUP BY\n    locker,\n    firth\n),\ncte_2 AS (\n  SELECT\n    MAX(column) AS group,\n    MAX(trader) AS brett,\n    MIN(column) AS relic,\n    SUM(column) AS steak,\n    AVG(column) AS phrase,\n    COUNT(trader) AS german\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    group,\n    brett,\n    stead,\n    relic,\n    steak,\n    phrase,\n    german\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    group,\n    brett,\n    relic,\n    steak\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     locker,\n     firth,\n     clause,\n     jerry,\n     saloon\n|> AGGREGATE\n     SUM(jerry) AS column,\n     AVG(jerry) AS trader\n   GROUP BY\n     locker,\n     firth\n|> AGGREGATE\n     MAX(column) AS group,\n     MAX(trader) AS brett,\n     MIN(column) AS relic,\n     SUM(column) AS steak,\n     AVG(column) AS phrase,\n     COUNT(trader) AS german\n|> SELECT\n     group,\n     brett,\n     stead,\n     relic,\n     steak,\n     phrase,\n     german\n|> SELECT\n     group,\n     brett,\n     relic,\n     steak\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"stead",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    fault,\n    mutiny,\n    market,\n    derby,\n    sally\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(derby) AS ariel,\n    AVG(sally) AS cheque,\n    MAX(derby) AS entity\n  FROM cte_0\n  GROUP BY\n    fault\n),\ncte_2 AS (\n  SELECT\n    COUNT(entity) AS marrow,\n    MIN(entity) AS header,\n    MAX(cheque) AS stress,\n    MAX(entity) AS farmer,\n    AVG(cheque) AS debtor\n  FROM cte_1\n  GROUP BY\n    ariel\n),\ncte_3 AS (\n  SELECT\n    marrow,\n    header,\n    fodder,\n    stress\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    marrow,\n    MIN(stress) AS heath,\n    MAX(stress) AS surge\n  FROM cte_3\n  GROUP BY\n    marrow,\n    header\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     fault,\n     mutiny,\n     market,\n     derby,\n     sally\n|> AGGREGATE\n     MIN(derby) AS ariel,\n     AVG(sally) AS cheque,\n     MAX(derby) AS entity\n   GROUP BY\n     fault\n|> AGGREGATE\n     COUNT(entity) AS marrow,\n     MIN(entity) AS header,\n     MAX(cheque) AS stress,\n     MAX(entity) AS farmer,\n     AVG(cheque) AS debtor\n   GROUP BY\n     ariel\n|> SELECT\n     marrow,\n     header,\n     fodder,\n     stress\n|> AGGREGATE\n     MIN(stress) AS heath,\n     MAX(stress) AS surge\n   GROUP BY\n     marrow,\n     header\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"fodder",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    remark,\n    table,\n    cream,\n    slide,\n    budget\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(budget) AS editor,\n    MIN(budget) AS friend,\n    MAX(budget) AS payne,\n    SUM(budget) AS steel,\n    AVG(budget) AS coast\n  FROM cte_0\n  GROUP BY\n    remark,\n    table,\n    cream,\n    slide\n),\ncte_2 AS (\n  SELECT\n    editor,\n    friend,\n    payne,\n    steel\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    editor,\n    friend,\n    magnet,\n    payne,\n    steel\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    editor,\n    friend,\n    MIN(steel) AS assent\n  FROM cte_3\n  GROUP BY\n    editor,\n    friend\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     remark,\n     table,\n     cream,\n     slide,\n     budget\n|> AGGREGATE\n     COUNT(budget) AS editor,\n     MIN(budget) AS friend,\n     MAX(budget) AS payne,\n     SUM(budget) AS steel,\n     AVG(budget) AS coast\n   GROUP BY\n     remark,\n     table,\n     cream,\n     slide\n|> SELECT\n     editor,\n     friend,\n     payne,\n     steel\n|> SELECT\n     editor,\n     friend,\n     magnet,\n     payne,\n     steel\n|> AGGREGATE\n     MIN(steel) AS assent\n   GROUP BY\n     editor,\n     friend\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"magnet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    crack,\n    screw,\n    scout,\n    short,\n    strip\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(short) AS corpus,\n    SUM(screw) AS haste,\n    SUM(strip) AS daddy,\n    MAX(strip) AS murray,\n    MIN(short) AS glint\n  FROM cte_0\n  GROUP BY\n    crack\n),\ncte_2 AS (\n  SELECT\n    corpus,\n    haste,\n    daddy,\n    murray,\n    glint\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    corpus,\n    haste,\n    daddy,\n    murray,\n    mercer,\n    glint\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    corpus,\n    AVG(glint) AS gooch\n  FROM cte_3\n  GROUP BY\n    corpus,\n    haste,\n    daddy,\n    murray\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     crack,\n     screw,\n     scout,\n     short,\n     strip\n|> AGGREGATE\n     SUM(short) AS corpus,\n     SUM(screw) AS haste,\n     SUM(strip) AS daddy,\n     MAX(strip) AS murray,\n     MIN(short) AS glint\n   GROUP BY\n     crack\n|> SELECT\n     corpus,\n     haste,\n     daddy,\n     murray,\n     glint\n|> SELECT\n     corpus,\n     haste,\n     daddy,\n     murray,\n     mercer,\n     glint\n|> AGGREGATE\n     AVG(glint) AS gooch\n   GROUP BY\n     corpus,\n     haste,\n     daddy,\n     murray\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"mercer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    mayor,\n    autumn,\n    jigsaw,\n    queue,\n    replay\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(queue) AS madam,\n    AVG(autumn) AS carrot\n  FROM cte_0\n  GROUP BY\n    mayor\n),\ncte_2 AS (\n  SELECT\n    SUM(carrot) AS slide,\n    MAX(carrot) AS madame,\n    MAX(madam) AS ballot,\n    MIN(carrot) AS comedy,\n    AVG(madam) AS jumble\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    slide,\n    madame,\n    ballot,\n    comedy,\n    jumble,\n    creed\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    slide,\n    AVG(madame) AS weaver,\n    SUM(madame) AS leader,\n    AVG(ballot) AS teddy\n  FROM cte_3\n  GROUP BY\n    slide\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     mayor,\n     autumn,\n     jigsaw,\n     queue,\n     replay\n|> AGGREGATE\n     MAX(queue) AS madam,\n     AVG(autumn) AS carrot\n   GROUP BY\n     mayor\n|> AGGREGATE\n     SUM(carrot) AS slide,\n     MAX(carrot) AS madame,\n     MAX(madam) AS ballot,\n     MIN(carrot) AS comedy,\n     AVG(madam) AS jumble\n|> SELECT\n     slide,\n     madame,\n     ballot,\n     comedy,\n     jumble,\n     creed\n|> AGGREGATE\n     AVG(madame) AS weaver,\n     SUM(madame) AS leader,\n     AVG(ballot) AS teddy\n   GROUP BY\n     slide\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"creed",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    artery,\n    gallon,\n    mayor,\n    critic,\n    hammer\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(hammer) AS burrow,\n    MIN(hammer) AS chord\n  FROM cte_0\n  GROUP BY\n    artery,\n    gallon,\n    mayor,\n    critic\n),\ncte_2 AS (\n  SELECT\n    SUM(burrow) AS creed,\n    MAX(chord) AS empire,\n    MIN(chord) AS arson,\n    SUM(chord) AS vigil,\n    COUNT(chord) AS dexter\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    creed,\n    empire,\n    snail,\n    arson,\n    vigil\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(empire) AS window,\n    MAX(creed) AS caste,\n    COUNT(vigil) AS thigh\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     artery,\n     gallon,\n     mayor,\n     critic,\n     hammer\n|> AGGREGATE\n     COUNT(hammer) AS burrow,\n     MIN(hammer) AS chord\n   GROUP BY\n     artery,\n     gallon,\n     mayor,\n     critic\n|> AGGREGATE\n     SUM(burrow) AS creed,\n     MAX(chord) AS empire,\n     MIN(chord) AS arson,\n     SUM(chord) AS vigil,\n     COUNT(chord) AS dexter\n|> SELECT\n     creed,\n     empire,\n     snail,\n     arson,\n     vigil\n|> AGGREGATE\n     AVG(empire) AS window,\n     MAX(creed) AS caste,\n     COUNT(vigil) AS thigh\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"snail",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    cheque,\n    incest,\n    rattle,\n    marble,\n    villa\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    cheque,\n    incest,\n    rattle,\n    marble\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(cheque) AS anger,\n    AVG(marble) AS piece,\n    MAX(marble) AS crisp,\n    MIN(cheque) AS avenue,\n    AVG(incest) AS decor,\n    MIN(marble) AS rhythm,\n    SUM(cheque) AS corps\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    anger,\n    piece,\n    excise,\n    crisp,\n    avenue,\n    decor,\n    rhythm,\n    corps\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(decor) AS surety\n  FROM cte_3\n  GROUP BY\n    anger\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     cheque,\n     incest,\n     rattle,\n     marble,\n     villa\n|> SELECT\n     cheque,\n     incest,\n     rattle,\n     marble\n|> AGGREGATE\n     AVG(cheque) AS anger,\n     AVG(marble) AS piece,\n     MAX(marble) AS crisp,\n     MIN(cheque) AS avenue,\n     AVG(incest) AS decor,\n     MIN(marble) AS rhythm,\n     SUM(cheque) AS corps\n|> SELECT\n     anger,\n     piece,\n     excise,\n     crisp,\n     avenue,\n     decor,\n     rhythm,\n     corps\n|> AGGREGATE\n     AVG(decor) AS surety\n   GROUP BY\n     anger\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"excise",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    thumb,\n    debtor,\n    japan,\n    barnet,\n    carer\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(japan) AS engine,\n    COUNT(japan) AS warsaw,\n    MAX(carer) AS zenith,\n    AVG(barnet) AS salute\n  FROM cte_0\n  GROUP BY\n    thumb\n),\ncte_2 AS (\n  SELECT\n    MIN(engine) AS stable,\n    COUNT(warsaw) AS query,\n    AVG(salute) AS priory,\n    COUNT(zenith) AS curry\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    stable,\n    query,\n    priory,\n    savage,\n    curry\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(curry) AS drama,\n    COUNT(curry) AS bonnet,\n    SUM(query) AS buyer\n  FROM cte_3\n  GROUP BY\n    stable\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     thumb,\n     debtor,\n     japan,\n     barnet,\n     carer\n|> AGGREGATE\n     AVG(japan) AS engine,\n     COUNT(japan) AS warsaw,\n     MAX(carer) AS zenith,\n     AVG(barnet) AS salute\n   GROUP BY\n     thumb\n|> AGGREGATE\n     MIN(engine) AS stable,\n     COUNT(warsaw) AS query,\n     AVG(salute) AS priory,\n     COUNT(zenith) AS curry\n|> SELECT\n     stable,\n     query,\n     priory,\n     savage,\n     curry\n|> AGGREGATE\n     MAX(curry) AS drama,\n     COUNT(curry) AS bonnet,\n     SUM(query) AS buyer\n   GROUP BY\n     stable\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"savage",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    fleet,\n    genus,\n    bureau,\n    outset,\n    winter\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(outset) AS reply,\n    MAX(winter) AS habit,\n    SUM(fleet) AS packet,\n    COUNT(fleet) AS flair,\n    COUNT(outset) AS sleep,\n    COUNT(genus) AS affair,\n    AVG(bureau) AS hogan\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    reply,\n    habit,\n    packet,\n    flair,\n    sleep,\n    affair,\n    double,\n    hogan\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    reply,\n    habit,\n    packet\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    reply,\n    AVG(packet) AS sherry\n  FROM cte_3\n  GROUP BY\n    reply\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     fleet,\n     genus,\n     bureau,\n     outset,\n     winter\n|> AGGREGATE\n     AVG(outset) AS reply,\n     MAX(winter) AS habit,\n     SUM(fleet) AS packet,\n     COUNT(fleet) AS flair,\n     COUNT(outset) AS sleep,\n     COUNT(genus) AS affair,\n     AVG(bureau) AS hogan\n|> SELECT\n     reply,\n     habit,\n     packet,\n     flair,\n     sleep,\n     affair,\n     double,\n     hogan\n|> SELECT\n     reply,\n     habit,\n     packet\n|> AGGREGATE\n     AVG(packet) AS sherry\n   GROUP BY\n     reply\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"double",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    orbit,\n    turner,\n    escape,\n    danger,\n    creole\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(creole) AS north,\n    COUNT(creole) AS pigeon,\n    SUM(danger) AS warren,\n    AVG(creole) AS deity,\n    AVG(danger) AS unison,\n    MAX(creole) AS regime,\n    MIN(danger) AS quote\n  FROM cte_0\n  GROUP BY\n    orbit,\n    turner,\n    escape\n),\ncte_2 AS (\n  SELECT\n    north,\n    pigeon,\n    warren,\n    leader,\n    deity,\n    unison\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    north,\n    MIN(warren) AS odour\n  FROM cte_2\n  GROUP BY\n    north\n),\ncte_4 AS (\n  SELECT\n    AVG(odour) AS burgh\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     orbit,\n     turner,\n     escape,\n     danger,\n     creole\n|> AGGREGATE\n     MIN(creole) AS north,\n     COUNT(creole) AS pigeon,\n     SUM(danger) AS warren,\n     AVG(creole) AS deity,\n     AVG(danger) AS unison,\n     MAX(creole) AS regime,\n     MIN(danger) AS quote\n   GROUP BY\n     orbit,\n     turner,\n     escape\n|> SELECT\n     north,\n     pigeon,\n     warren,\n     leader,\n     deity,\n     unison\n|> AGGREGATE\n     MIN(warren) AS odour\n   GROUP BY\n     north\n|> AGGREGATE\n     AVG(odour) AS burgh\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"leader",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    refuse,\n    indie,\n    willow,\n    demon,\n    chance\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(willow) AS tempo,\n    AVG(indie) AS genus,\n    MIN(demon) AS franco,\n    MIN(indie) AS gooch,\n    AVG(demon) AS bertha,\n    MIN(chance) AS plough,\n    MAX(chance) AS bearer,\n    AVG(willow) AS series\n  FROM cte_0\n  GROUP BY\n    refuse\n),\ncte_2 AS (\n  SELECT\n    tempo,\n    genus,\n    franco,\n    divide,\n    gooch,\n    bertha\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    tempo,\n    MIN(franco) AS norman\n  FROM cte_2\n  GROUP BY\n    tempo\n),\ncte_4 AS (\n  SELECT\n    tempo,\n    norman\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     refuse,\n     indie,\n     willow,\n     demon,\n     chance\n|> AGGREGATE\n     SUM(willow) AS tempo,\n     AVG(indie) AS genus,\n     MIN(demon) AS franco,\n     MIN(indie) AS gooch,\n     AVG(demon) AS bertha,\n     MIN(chance) AS plough,\n     MAX(chance) AS bearer,\n     AVG(willow) AS series\n   GROUP BY\n     refuse\n|> SELECT\n     tempo,\n     genus,\n     franco,\n     divide,\n     gooch,\n     bertha\n|> AGGREGATE\n     MIN(franco) AS norman\n   GROUP BY\n     tempo\n|> SELECT\n     tempo,\n     norman\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"divide",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sultan,\n    lizard,\n    front,\n    volley,\n    tender\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(tender) AS attic,\n    MIN(front) AS blitz,\n    MIN(sultan) AS morale,\n    MAX(volley) AS bleach,\n    COUNT(volley) AS honey,\n    MIN(tender) AS region\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    attic,\n    blitz,\n    morale,\n    bleach,\n    honey\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    attic,\n    blitz,\n    morale,\n    tardis\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    attic,\n    blitz,\n    AVG(morale) AS wicket,\n    COUNT(morale) AS brain\n  FROM cte_3\n  GROUP BY\n    attic,\n    blitz\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sultan,\n     lizard,\n     front,\n     volley,\n     tender\n|> AGGREGATE\n     MAX(tender) AS attic,\n     MIN(front) AS blitz,\n     MIN(sultan) AS morale,\n     MAX(volley) AS bleach,\n     COUNT(volley) AS honey,\n     MIN(tender) AS region\n|> SELECT\n     attic,\n     blitz,\n     morale,\n     bleach,\n     honey\n|> SELECT\n     attic,\n     blitz,\n     morale,\n     tardis\n|> AGGREGATE\n     AVG(morale) AS wicket,\n     COUNT(morale) AS brain\n   GROUP BY\n     attic,\n     blitz\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"tardis",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sense,\n    pearl,\n    slang,\n    uptake,\n    schema\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(slang) AS diary,\n    MIN(uptake) AS derby,\n    MAX(pearl) AS ladder,\n    AVG(pearl) AS surge,\n    MIN(schema) AS store\n  FROM cte_0\n  GROUP BY\n    sense\n),\ncte_2 AS (\n  SELECT\n    diary,\n    derby,\n    ladder,\n    surge,\n    store\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    diary,\n    derby,\n    ladder,\n    surge,\n    pistol,\n    store\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(surge) AS greed,\n    COUNT(surge) AS baker\n  FROM cte_3\n  GROUP BY\n    diary,\n    derby,\n    ladder\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sense,\n     pearl,\n     slang,\n     uptake,\n     schema\n|> AGGREGATE\n     AVG(slang) AS diary,\n     MIN(uptake) AS derby,\n     MAX(pearl) AS ladder,\n     AVG(pearl) AS surge,\n     MIN(schema) AS store\n   GROUP BY\n     sense\n|> SELECT\n     diary,\n     derby,\n     ladder,\n     surge,\n     store\n|> SELECT\n     diary,\n     derby,\n     ladder,\n     surge,\n     pistol,\n     store\n|> AGGREGATE\n     MAX(surge) AS greed,\n     COUNT(surge) AS baker\n   GROUP BY\n     diary,\n     derby,\n     ladder\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"pistol",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    honda,\n    rival,\n    attic,\n    covent,\n    crest\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(covent) AS goblin,\n    MIN(covent) AS trench,\n    SUM(rival) AS decay,\n    MAX(attic) AS proxy,\n    SUM(covent) AS jewel\n  FROM cte_0\n  GROUP BY\n    honda\n),\ncte_2 AS (\n  SELECT\n    goblin,\n    trench,\n    decay,\n    proxy,\n    jewel\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    goblin,\n    trench,\n    decay,\n    proxy,\n    earth,\n    jewel\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    goblin,\n    trench,\n    MAX(proxy) AS carver\n  FROM cte_3\n  GROUP BY\n    goblin,\n    trench,\n    decay\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     honda,\n     rival,\n     attic,\n     covent,\n     crest\n|> AGGREGATE\n     AVG(covent) AS goblin,\n     MIN(covent) AS trench,\n     SUM(rival) AS decay,\n     MAX(attic) AS proxy,\n     SUM(covent) AS jewel\n   GROUP BY\n     honda\n|> SELECT\n     goblin,\n     trench,\n     decay,\n     proxy,\n     jewel\n|> SELECT\n     goblin,\n     trench,\n     decay,\n     proxy,\n     earth,\n     jewel\n|> AGGREGATE\n     MAX(proxy) AS carver\n   GROUP BY\n     goblin,\n     trench,\n     decay\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"earth",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    outset,\n    token,\n    sonny,\n    stereo,\n    block\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(block) AS drill,\n    SUM(block) AS health\n  FROM cte_0\n  GROUP BY\n    outset,\n    token,\n    sonny,\n    stereo\n),\ncte_2 AS (\n  SELECT\n    MIN(drill) AS mixer,\n    MAX(health) AS glove,\n    MIN(health) AS terry,\n    SUM(drill) AS thing,\n    SUM(health) AS excise,\n    COUNT(health) AS bureau\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    reader,\n    mixer,\n    glove,\n    terry,\n    thing\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(mixer) AS turbo,\n    SUM(mixer) AS sprint\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     outset,\n     token,\n     sonny,\n     stereo,\n     block\n|> AGGREGATE\n     MIN(block) AS drill,\n     SUM(block) AS health\n   GROUP BY\n     outset,\n     token,\n     sonny,\n     stereo\n|> AGGREGATE\n     MIN(drill) AS mixer,\n     MAX(health) AS glove,\n     MIN(health) AS terry,\n     SUM(drill) AS thing,\n     SUM(health) AS excise,\n     COUNT(health) AS bureau\n|> SELECT\n     reader,\n     mixer,\n     glove,\n     terry,\n     thing\n|> AGGREGATE\n     MIN(mixer) AS turbo,\n     SUM(mixer) AS sprint\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"reader",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    branch,\n    vinyl,\n    bucket,\n    toilet,\n    muscle\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    branch,\n    vinyl,\n    bucket,\n    toilet\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(vinyl) AS chunk,\n    COUNT(bucket) AS green,\n    AVG(vinyl) AS mungo,\n    COUNT(toilet) AS scrub,\n    COUNT(vinyl) AS amino\n  FROM cte_1\n  GROUP BY\n    branch\n),\ncte_3 AS (\n  SELECT\n    chunk,\n    green,\n    mungo,\n    scrub,\n    amino,\n    nancy\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    chunk,\n    COUNT(amino) AS tablet,\n    MAX(green) AS stall,\n    MIN(green) AS gamble\n  FROM cte_3\n  GROUP BY\n    chunk\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     branch,\n     vinyl,\n     bucket,\n     toilet,\n     muscle\n|> SELECT\n     branch,\n     vinyl,\n     bucket,\n     toilet\n|> AGGREGATE\n     SUM(vinyl) AS chunk,\n     COUNT(bucket) AS green,\n     AVG(vinyl) AS mungo,\n     COUNT(toilet) AS scrub,\n     COUNT(vinyl) AS amino\n   GROUP BY\n     branch\n|> SELECT\n     chunk,\n     green,\n     mungo,\n     scrub,\n     amino,\n     nancy\n|> AGGREGATE\n     COUNT(amino) AS tablet,\n     MAX(green) AS stall,\n     MIN(green) AS gamble\n   GROUP BY\n     chunk\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"nancy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    study,\n    pickup,\n    memory,\n    script,\n    faeces\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(memory) AS marina,\n    MIN(study) AS title,\n    MAX(faeces) AS villa,\n    MAX(study) AS breath,\n    MIN(memory) AS brace\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(villa) AS libel,\n    MIN(breath) AS button,\n    SUM(title) AS metal,\n    AVG(villa) AS badge\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    libel,\n    button,\n    siren,\n    metal,\n    badge\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    libel,\n    AVG(badge) AS motor\n  FROM cte_3\n  GROUP BY\n    libel,\n    button,\n    metal\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     study,\n     pickup,\n     memory,\n     script,\n     faeces\n|> AGGREGATE\n     SUM(memory) AS marina,\n     MIN(study) AS title,\n     MAX(faeces) AS villa,\n     MAX(study) AS breath,\n     MIN(memory) AS brace\n|> AGGREGATE\n     COUNT(villa) AS libel,\n     MIN(breath) AS button,\n     SUM(title) AS metal,\n     AVG(villa) AS badge\n|> SELECT\n     libel,\n     button,\n     siren,\n     metal,\n     badge\n|> AGGREGATE\n     AVG(badge) AS motor\n   GROUP BY\n     libel,\n     button,\n     metal\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"siren",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    degree,\n    record,\n    rhine,\n    swing,\n    currie\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(currie) AS poker,\n    COUNT(currie) AS whole,\n    MIN(currie) AS regret,\n    AVG(currie) AS arson,\n    SUM(currie) AS esteem\n  FROM cte_0\n  GROUP BY\n    degree,\n    record,\n    rhine,\n    swing\n),\ncte_2 AS (\n  SELECT\n    poker,\n    whole,\n    regret,\n    arson,\n    esteem\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    poker,\n    pinch,\n    whole,\n    regret\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    poker,\n    AVG(whole) AS desert,\n    SUM(whole) AS movie\n  FROM cte_3\n  GROUP BY\n    poker\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     degree,\n     record,\n     rhine,\n     swing,\n     currie\n|> AGGREGATE\n     MAX(currie) AS poker,\n     COUNT(currie) AS whole,\n     MIN(currie) AS regret,\n     AVG(currie) AS arson,\n     SUM(currie) AS esteem\n   GROUP BY\n     degree,\n     record,\n     rhine,\n     swing\n|> SELECT\n     poker,\n     whole,\n     regret,\n     arson,\n     esteem\n|> SELECT\n     poker,\n     pinch,\n     whole,\n     regret\n|> AGGREGATE\n     AVG(whole) AS desert,\n     SUM(whole) AS movie\n   GROUP BY\n     poker\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"pinch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    diesel,\n    while,\n    moran,\n    brandy,\n    salad\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(brandy) AS larry,\n    AVG(brandy) AS viola,\n    SUM(salad) AS caddie,\n    AVG(moran) AS pellet,\n    MAX(salad) AS resale\n  FROM cte_0\n  GROUP BY\n    diesel,\n    while\n),\ncte_2 AS (\n  SELECT\n    larry,\n    viola,\n    caddie,\n    pellet\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    larry,\n    viola,\n    caddie,\n    pellet,\n    sender\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    larry,\n    MIN(pellet) AS comet,\n    MAX(viola) AS policy,\n    COUNT(caddie) AS circle\n  FROM cte_3\n  GROUP BY\n    larry\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     diesel,\n     while,\n     moran,\n     brandy,\n     salad\n|> AGGREGATE\n     MAX(brandy) AS larry,\n     AVG(brandy) AS viola,\n     SUM(salad) AS caddie,\n     AVG(moran) AS pellet,\n     MAX(salad) AS resale\n   GROUP BY\n     diesel,\n     while\n|> SELECT\n     larry,\n     viola,\n     caddie,\n     pellet\n|> SELECT\n     larry,\n     viola,\n     caddie,\n     pellet,\n     sender\n|> AGGREGATE\n     MIN(pellet) AS comet,\n     MAX(viola) AS policy,\n     COUNT(caddie) AS circle\n   GROUP BY\n     larry\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"sender",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    affect,\n    ethos,\n    jelly,\n    alaska,\n    crypt\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(alaska) AS survey,\n    COUNT(crypt) AS liquid,\n    MIN(crypt) AS comma,\n    AVG(crypt) AS travis,\n    MAX(alaska) AS burial,\n    COUNT(alaska) AS brewer\n  FROM cte_0\n  GROUP BY\n    affect,\n    ethos,\n    jelly\n),\ncte_2 AS (\n  SELECT\n    survey,\n    liquid,\n    comma,\n    travis,\n    burial,\n    dolly,\n    brewer\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    survey,\n    liquid,\n    comma,\n    travis\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    survey,\n    liquid,\n    comma\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     affect,\n     ethos,\n     jelly,\n     alaska,\n     crypt\n|> AGGREGATE\n     AVG(alaska) AS survey,\n     COUNT(crypt) AS liquid,\n     MIN(crypt) AS comma,\n     AVG(crypt) AS travis,\n     MAX(alaska) AS burial,\n     COUNT(alaska) AS brewer\n   GROUP BY\n     affect,\n     ethos,\n     jelly\n|> SELECT\n     survey,\n     liquid,\n     comma,\n     travis,\n     burial,\n     dolly,\n     brewer\n|> SELECT\n     survey,\n     liquid,\n     comma,\n     travis\n|> SELECT\n     survey,\n     liquid,\n     comma\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"dolly",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    dogma,\n    month,\n    slice,\n    synod,\n    person\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(person) AS gutter,\n    SUM(person) AS graham,\n    MAX(month) AS runway,\n    COUNT(slice) AS hunger,\n    COUNT(month) AS biopsy,\n    MAX(dogma) AS tardis,\n    SUM(synod) AS idiom\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    gutter,\n    graham,\n    runway,\n    hunger,\n    biopsy,\n    tardis,\n    troop,\n    idiom\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(tardis) AS court,\n    MIN(biopsy) AS smile\n  FROM cte_2\n  GROUP BY\n    gutter\n),\ncte_4 AS (\n  SELECT\n    MAX(court) AS wheat\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     dogma,\n     month,\n     slice,\n     synod,\n     person\n|> AGGREGATE\n     MIN(person) AS gutter,\n     SUM(person) AS graham,\n     MAX(month) AS runway,\n     COUNT(slice) AS hunger,\n     COUNT(month) AS biopsy,\n     MAX(dogma) AS tardis,\n     SUM(synod) AS idiom\n|> SELECT\n     gutter,\n     graham,\n     runway,\n     hunger,\n     biopsy,\n     tardis,\n     troop,\n     idiom\n|> AGGREGATE\n     SUM(tardis) AS court,\n     MIN(biopsy) AS smile\n   GROUP BY\n     gutter\n|> AGGREGATE\n     MAX(court) AS wheat\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"troop",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    schema,\n    column,\n    menace,\n    target,\n    tackle\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(tackle) AS tender,\n    AVG(menace) AS player,\n    MIN(target) AS sierra,\n    MIN(tackle) AS bazaar,\n    MAX(target) AS hooker,\n    MIN(menace) AS poster,\n    SUM(menace) AS lynch,\n    AVG(tackle) AS flight,\n    AVG(target) AS snake\n  FROM cte_0\n  GROUP BY\n    schema,\n    column\n),\ncte_2 AS (\n  SELECT\n    scent,\n    tender,\n    player\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    tender,\n    player\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    tender,\n    COUNT(player) AS berth\n  FROM cte_3\n  GROUP BY\n    tender\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     schema,\n     column,\n     menace,\n     target,\n     tackle\n|> AGGREGATE\n     MAX(tackle) AS tender,\n     AVG(menace) AS player,\n     MIN(target) AS sierra,\n     MIN(tackle) AS bazaar,\n     MAX(target) AS hooker,\n     MIN(menace) AS poster,\n     SUM(menace) AS lynch,\n     AVG(tackle) AS flight,\n     AVG(target) AS snake\n   GROUP BY\n     schema,\n     column\n|> SELECT\n     scent,\n     tender,\n     player\n|> SELECT\n     tender,\n     player\n|> AGGREGATE\n     COUNT(player) AS berth\n   GROUP BY\n     tender\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"scent",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sugar,\n    photo,\n    oxford,\n    castle,\n    armour\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    sugar,\n    photo,\n    oxford,\n    castle,\n    armour\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(photo) AS tender,\n    COUNT(oxford) AS splash,\n    SUM(oxford) AS coach,\n    SUM(photo) AS whale,\n    MIN(oxford) AS cause\n  FROM cte_1\n  GROUP BY\n    sugar\n),\ncte_3 AS (\n  SELECT\n    tender,\n    splash,\n    coach,\n    whale,\n    guide\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    tender,\n    MAX(splash) AS sonny,\n    COUNT(coach) AS decree,\n    SUM(splash) AS orient\n  FROM cte_3\n  GROUP BY\n    tender\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sugar,\n     photo,\n     oxford,\n     castle,\n     armour\n|> SELECT\n     sugar,\n     photo,\n     oxford,\n     castle,\n     armour\n|> AGGREGATE\n     AVG(photo) AS tender,\n     COUNT(oxford) AS splash,\n     SUM(oxford) AS coach,\n     SUM(photo) AS whale,\n     MIN(oxford) AS cause\n   GROUP BY\n     sugar\n|> SELECT\n     tender,\n     splash,\n     coach,\n     whale,\n     guide\n|> AGGREGATE\n     MAX(splash) AS sonny,\n     COUNT(coach) AS decree,\n     SUM(splash) AS orient\n   GROUP BY\n     tender\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"guide",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    plague,\n    jumper,\n    marsh,\n    hollow,\n    recall\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(recall) AS fever,\n    MAX(plague) AS garden,\n    AVG(hollow) AS state,\n    MIN(hollow) AS corpse,\n    COUNT(recall) AS alloy,\n    SUM(plague) AS vault,\n    SUM(recall) AS reform\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    fever,\n    garden,\n    state,\n    corpse,\n    alloy,\n    vault,\n    bomber,\n    reform\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    fever,\n    SUM(vault) AS sudden\n  FROM cte_2\n  GROUP BY\n    fever,\n    garden\n),\ncte_4 AS (\n  SELECT\n    fever,\n    sudden\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     plague,\n     jumper,\n     marsh,\n     hollow,\n     recall\n|> AGGREGATE\n     MAX(recall) AS fever,\n     MAX(plague) AS garden,\n     AVG(hollow) AS state,\n     MIN(hollow) AS corpse,\n     COUNT(recall) AS alloy,\n     SUM(plague) AS vault,\n     SUM(recall) AS reform\n|> SELECT\n     fever,\n     garden,\n     state,\n     corpse,\n     alloy,\n     vault,\n     bomber,\n     reform\n|> AGGREGATE\n     SUM(vault) AS sudden\n   GROUP BY\n     fever,\n     garden\n|> SELECT\n     fever,\n     sudden\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"bomber",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    truck,\n    woman,\n    ankle,\n    atlas,\n    pedal\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(atlas) AS guilt,\n    COUNT(pedal) AS raffle,\n    AVG(atlas) AS knack,\n    MIN(ankle) AS brandy\n  FROM cte_0\n  GROUP BY\n    truck,\n    woman\n),\ncte_2 AS (\n  SELECT\n    AVG(brandy) AS ferry,\n    MIN(knack) AS treaty,\n    AVG(knack) AS freeze\n  FROM cte_1\n  GROUP BY\n    guilt,\n    raffle\n),\ncte_3 AS (\n  SELECT\n    ferry,\n    treaty,\n    libel,\n    freeze\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    ferry,\n    MIN(freeze) AS poker,\n    MAX(freeze) AS person\n  FROM cte_3\n  GROUP BY\n    ferry,\n    treaty\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     truck,\n     woman,\n     ankle,\n     atlas,\n     pedal\n|> AGGREGATE\n     SUM(atlas) AS guilt,\n     COUNT(pedal) AS raffle,\n     AVG(atlas) AS knack,\n     MIN(ankle) AS brandy\n   GROUP BY\n     truck,\n     woman\n|> AGGREGATE\n     AVG(brandy) AS ferry,\n     MIN(knack) AS treaty,\n     AVG(knack) AS freeze\n   GROUP BY\n     guilt,\n     raffle\n|> SELECT\n     ferry,\n     treaty,\n     libel,\n     freeze\n|> AGGREGATE\n     MIN(freeze) AS poker,\n     MAX(freeze) AS person\n   GROUP BY\n     ferry,\n     treaty\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"libel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    drink,\n    scrap,\n    baxter,\n    tandem,\n    piazza\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(baxter) AS drawer,\n    COUNT(tandem) AS shaikh,\n    SUM(baxter) AS piece,\n    COUNT(piazza) AS church,\n    SUM(piazza) AS picket,\n    MIN(tandem) AS ellen,\n    COUNT(baxter) AS repeat,\n    MAX(tandem) AS spider\n  FROM cte_0\n  GROUP BY\n    drink,\n    scrap\n),\ncte_2 AS (\n  SELECT\n    drawer,\n    shaikh,\n    colony\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(shaikh) AS yogurt,\n    COUNT(shaikh) AS sight\n  FROM cte_2\n  GROUP BY\n    drawer\n),\ncte_4 AS (\n  SELECT\n    MIN(sight) AS colin\n  FROM cte_3\n  GROUP BY\n    yogurt\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     drink,\n     scrap,\n     baxter,\n     tandem,\n     piazza\n|> AGGREGATE\n     AVG(baxter) AS drawer,\n     COUNT(tandem) AS shaikh,\n     SUM(baxter) AS piece,\n     COUNT(piazza) AS church,\n     SUM(piazza) AS picket,\n     MIN(tandem) AS ellen,\n     COUNT(baxter) AS repeat,\n     MAX(tandem) AS spider\n   GROUP BY\n     drink,\n     scrap\n|> SELECT\n     drawer,\n     shaikh,\n     colony\n|> AGGREGATE\n     SUM(shaikh) AS yogurt,\n     COUNT(shaikh) AS sight\n   GROUP BY\n     drawer\n|> AGGREGATE\n     MIN(sight) AS colin\n   GROUP BY\n     yogurt\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"colony",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    velvet,\n    stable,\n    probe,\n    patent,\n    dieter\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(patent) AS gloss,\n    MIN(patent) AS jasper,\n    AVG(patent) AS theme\n  FROM cte_0\n  GROUP BY\n    velvet\n),\ncte_2 AS (\n  SELECT\n    MAX(theme) AS chapel,\n    AVG(theme) AS clive,\n    MIN(jasper) AS fairy,\n    SUM(theme) AS switch\n  FROM cte_1\n  GROUP BY\n    gloss\n),\ncte_3 AS (\n  SELECT\n    chapel,\n    clive,\n    fairy,\n    switch,\n    desert\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    chapel,\n    MIN(switch) AS estate,\n    MAX(switch) AS marrow\n  FROM cte_3\n  GROUP BY\n    chapel,\n    clive,\n    fairy\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     velvet,\n     stable,\n     probe,\n     patent,\n     dieter\n|> AGGREGATE\n     COUNT(patent) AS gloss,\n     MIN(patent) AS jasper,\n     AVG(patent) AS theme\n   GROUP BY\n     velvet\n|> AGGREGATE\n     MAX(theme) AS chapel,\n     AVG(theme) AS clive,\n     MIN(jasper) AS fairy,\n     SUM(theme) AS switch\n   GROUP BY\n     gloss\n|> SELECT\n     chapel,\n     clive,\n     fairy,\n     switch,\n     desert\n|> AGGREGATE\n     MIN(switch) AS estate,\n     MAX(switch) AS marrow\n   GROUP BY\n     chapel,\n     clive,\n     fairy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"desert",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    wharf,\n    salon,\n    keeper,\n    dieter,\n    birth\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(keeper) AS farce,\n    COUNT(dieter) AS barman,\n    MIN(keeper) AS repair,\n    SUM(dieter) AS candy,\n    MAX(birth) AS roche\n  FROM cte_0\n  GROUP BY\n    wharf\n),\ncte_2 AS (\n  SELECT\n    farce,\n    barman,\n    repair,\n    candy,\n    roche\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    farce,\n    barman,\n    repair,\n    candy,\n    hector\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    farce,\n    MAX(candy) AS bobby,\n    AVG(candy) AS canal\n  FROM cte_3\n  GROUP BY\n    farce,\n    barman,\n    repair\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     wharf,\n     salon,\n     keeper,\n     dieter,\n     birth\n|> AGGREGATE\n     AVG(keeper) AS farce,\n     COUNT(dieter) AS barman,\n     MIN(keeper) AS repair,\n     SUM(dieter) AS candy,\n     MAX(birth) AS roche\n   GROUP BY\n     wharf\n|> SELECT\n     farce,\n     barman,\n     repair,\n     candy,\n     roche\n|> SELECT\n     farce,\n     barman,\n     repair,\n     candy,\n     hector\n|> AGGREGATE\n     MAX(candy) AS bobby,\n     AVG(candy) AS canal\n   GROUP BY\n     farce,\n     barman,\n     repair\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"hector",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    mutant,\n    jumper,\n    figure,\n    beach,\n    video\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(video) AS branch,\n    COUNT(video) AS beech,\n    MAX(video) AS gloom\n  FROM cte_0\n  GROUP BY\n    mutant,\n    jumper,\n    figure,\n    beach\n),\ncte_2 AS (\n  SELECT\n    MIN(gloom) AS yellow,\n    COUNT(gloom) AS insult,\n    AVG(gloom) AS odour\n  FROM cte_1\n  GROUP BY\n    branch,\n    beech\n),\ncte_3 AS (\n  SELECT\n    yellow,\n    insult,\n    curve,\n    odour\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(insult) AS grand,\n    MAX(odour) AS combo,\n    SUM(odour) AS dealer\n  FROM cte_3\n  GROUP BY\n    yellow\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     mutant,\n     jumper,\n     figure,\n     beach,\n     video\n|> AGGREGATE\n     SUM(video) AS branch,\n     COUNT(video) AS beech,\n     MAX(video) AS gloom\n   GROUP BY\n     mutant,\n     jumper,\n     figure,\n     beach\n|> AGGREGATE\n     MIN(gloom) AS yellow,\n     COUNT(gloom) AS insult,\n     AVG(gloom) AS odour\n   GROUP BY\n     branch,\n     beech\n|> SELECT\n     yellow,\n     insult,\n     curve,\n     odour\n|> AGGREGATE\n     MAX(insult) AS grand,\n     MAX(odour) AS combo,\n     SUM(odour) AS dealer\n   GROUP BY\n     yellow\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"curve",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    settee,\n    croft,\n    salary,\n    phrase,\n    stroke\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(croft) AS kitten,\n    COUNT(croft) AS trader,\n    COUNT(stroke) AS humour\n  FROM cte_0\n  GROUP BY\n    settee\n),\ncte_2 AS (\n  SELECT\n    MIN(humour) AS pride,\n    AVG(humour) AS rocket,\n    MAX(humour) AS celery,\n    COUNT(humour) AS clash,\n    SUM(humour) AS louvre\n  FROM cte_1\n  GROUP BY\n    kitten,\n    trader\n),\ncte_3 AS (\n  SELECT\n    pride,\n    stigma,\n    rocket,\n    celery\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    pride,\n    SUM(rocket) AS moran,\n    COUNT(rocket) AS plate\n  FROM cte_3\n  GROUP BY\n    pride\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     settee,\n     croft,\n     salary,\n     phrase,\n     stroke\n|> AGGREGATE\n     AVG(croft) AS kitten,\n     COUNT(croft) AS trader,\n     COUNT(stroke) AS humour\n   GROUP BY\n     settee\n|> AGGREGATE\n     MIN(humour) AS pride,\n     AVG(humour) AS rocket,\n     MAX(humour) AS celery,\n     COUNT(humour) AS clash,\n     SUM(humour) AS louvre\n   GROUP BY\n     kitten,\n     trader\n|> SELECT\n     pride,\n     stigma,\n     rocket,\n     celery\n|> AGGREGATE\n     SUM(rocket) AS moran,\n     COUNT(rocket) AS plate\n   GROUP BY\n     pride\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"stigma",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    output,\n    grange,\n    occult,\n    jigsaw,\n    laird\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(laird) AS mosaic,\n    MAX(jigsaw) AS picnic,\n    AVG(laird) AS chair,\n    MAX(laird) AS alpha,\n    AVG(jigsaw) AS talbot\n  FROM cte_0\n  GROUP BY\n    output,\n    grange,\n    occult\n),\ncte_2 AS (\n  SELECT\n    MAX(alpha) AS sonnet,\n    COUNT(picnic) AS salary,\n    MAX(chair) AS killer\n  FROM cte_1\n  GROUP BY\n    mosaic\n),\ncte_3 AS (\n  SELECT\n    diary,\n    sonnet,\n    salary\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    sonnet,\n    MIN(salary) AS joint,\n    AVG(salary) AS duchy\n  FROM cte_3\n  GROUP BY\n    sonnet\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     output,\n     grange,\n     occult,\n     jigsaw,\n     laird\n|> AGGREGATE\n     MIN(laird) AS mosaic,\n     MAX(jigsaw) AS picnic,\n     AVG(laird) AS chair,\n     MAX(laird) AS alpha,\n     AVG(jigsaw) AS talbot\n   GROUP BY\n     output,\n     grange,\n     occult\n|> AGGREGATE\n     MAX(alpha) AS sonnet,\n     COUNT(picnic) AS salary,\n     MAX(chair) AS killer\n   GROUP BY\n     mosaic\n|> SELECT\n     diary,\n     sonnet,\n     salary\n|> AGGREGATE\n     MIN(salary) AS joint,\n     AVG(salary) AS duchy\n   GROUP BY\n     sonnet\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"diary",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sprint,\n    argyll,\n    newton,\n    match,\n    booze\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(sprint) AS vendor,\n    MIN(booze) AS carrot,\n    COUNT(newton) AS onset,\n    MAX(booze) AS jaguar,\n    MIN(newton) AS palmer,\n    AVG(newton) AS german,\n    AVG(booze) AS career,\n    SUM(match) AS spruce\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    vendor,\n    carrot,\n    onset,\n    jaguar,\n    devil,\n    palmer,\n    german\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    vendor,\n    carrot\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    vendor,\n    MAX(carrot) AS repeal\n  FROM cte_3\n  GROUP BY\n    vendor\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sprint,\n     argyll,\n     newton,\n     match,\n     booze\n|> AGGREGATE\n     MAX(sprint) AS vendor,\n     MIN(booze) AS carrot,\n     COUNT(newton) AS onset,\n     MAX(booze) AS jaguar,\n     MIN(newton) AS palmer,\n     AVG(newton) AS german,\n     AVG(booze) AS career,\n     SUM(match) AS spruce\n|> SELECT\n     vendor,\n     carrot,\n     onset,\n     jaguar,\n     devil,\n     palmer,\n     german\n|> SELECT\n     vendor,\n     carrot\n|> AGGREGATE\n     MAX(carrot) AS repeal\n   GROUP BY\n     vendor\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"devil",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    grain,\n    settee,\n    humour,\n    month,\n    dogma\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(month) AS wicker,\n    SUM(month) AS ulster\n  FROM cte_0\n  GROUP BY\n    grain,\n    settee,\n    humour\n),\ncte_2 AS (\n  SELECT\n    COUNT(wicker) AS dealer,\n    MIN(wicker) AS shoal,\n    SUM(wicker) AS verge,\n    MIN(ulster) AS sauce,\n    AVG(ulster) AS tanker,\n    MAX(ulster) AS title\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    dealer,\n    christ,\n    shoal,\n    verge\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    dealer,\n    shoal,\n    MIN(verge) AS hazel\n  FROM cte_3\n  GROUP BY\n    dealer,\n    shoal\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     grain,\n     settee,\n     humour,\n     month,\n     dogma\n|> AGGREGATE\n     COUNT(month) AS wicker,\n     SUM(month) AS ulster\n   GROUP BY\n     grain,\n     settee,\n     humour\n|> AGGREGATE\n     COUNT(wicker) AS dealer,\n     MIN(wicker) AS shoal,\n     SUM(wicker) AS verge,\n     MIN(ulster) AS sauce,\n     AVG(ulster) AS tanker,\n     MAX(ulster) AS title\n|> SELECT\n     dealer,\n     christ,\n     shoal,\n     verge\n|> AGGREGATE\n     MIN(verge) AS hazel\n   GROUP BY\n     dealer,\n     shoal\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"christ",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    print,\n    gamble,\n    realm,\n    sewer,\n    remit\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(remit) AS liquid,\n    SUM(sewer) AS input,\n    AVG(remit) AS jewel,\n    MIN(sewer) AS motto,\n    AVG(sewer) AS oyster,\n    COUNT(sewer) AS patron\n  FROM cte_0\n  GROUP BY\n    print,\n    gamble,\n    realm\n),\ncte_2 AS (\n  SELECT\n    liquid,\n    input,\n    jewel,\n    motto,\n    oyster,\n    maker\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    liquid,\n    input\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(liquid) AS trough,\n    MAX(input) AS smell,\n    MAX(liquid) AS wicket\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     print,\n     gamble,\n     realm,\n     sewer,\n     remit\n|> AGGREGATE\n     COUNT(remit) AS liquid,\n     SUM(sewer) AS input,\n     AVG(remit) AS jewel,\n     MIN(sewer) AS motto,\n     AVG(sewer) AS oyster,\n     COUNT(sewer) AS patron\n   GROUP BY\n     print,\n     gamble,\n     realm\n|> SELECT\n     liquid,\n     input,\n     jewel,\n     motto,\n     oyster,\n     maker\n|> SELECT\n     liquid,\n     input\n|> AGGREGATE\n     COUNT(liquid) AS trough,\n     MAX(input) AS smell,\n     MAX(liquid) AS wicket\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"maker",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    guido,\n    entry,\n    crane,\n    jargon,\n    exile\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(exile) AS recall,\n    AVG(exile) AS grease,\n    SUM(exile) AS maize,\n    MAX(exile) AS rigour\n  FROM cte_0\n  GROUP BY\n    guido,\n    entry,\n    crane,\n    jargon\n),\ncte_2 AS (\n  SELECT\n    COUNT(maize) AS youth,\n    MIN(maize) AS serial,\n    MAX(maize) AS trunk\n  FROM cte_1\n  GROUP BY\n    recall,\n    grease\n),\ncte_3 AS (\n  SELECT\n    burton,\n    youth,\n    serial\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    youth,\n    MIN(serial) AS atlas,\n    SUM(serial) AS beast\n  FROM cte_3\n  GROUP BY\n    youth\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     guido,\n     entry,\n     crane,\n     jargon,\n     exile\n|> AGGREGATE\n     MIN(exile) AS recall,\n     AVG(exile) AS grease,\n     SUM(exile) AS maize,\n     MAX(exile) AS rigour\n   GROUP BY\n     guido,\n     entry,\n     crane,\n     jargon\n|> AGGREGATE\n     COUNT(maize) AS youth,\n     MIN(maize) AS serial,\n     MAX(maize) AS trunk\n   GROUP BY\n     recall,\n     grease\n|> SELECT\n     burton,\n     youth,\n     serial\n|> AGGREGATE\n     MIN(serial) AS atlas,\n     SUM(serial) AS beast\n   GROUP BY\n     youth\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"burton",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hound,\n    draper,\n    bailey,\n    lewis,\n    seaman\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(lewis) AS pirate,\n    COUNT(bailey) AS chain\n  FROM cte_0\n  GROUP BY\n    hound,\n    draper\n),\ncte_2 AS (\n  SELECT\n    COUNT(pirate) AS match,\n    SUM(pirate) AS satire,\n    SUM(chain) AS hopper,\n    AVG(pirate) AS judge,\n    COUNT(chain) AS upturn,\n    MIN(pirate) AS threat,\n    MIN(chain) AS mortar\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    lorry,\n    match,\n    satire\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(satire) AS groin,\n    AVG(match) AS roach,\n    MIN(satire) AS tunnel\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hound,\n     draper,\n     bailey,\n     lewis,\n     seaman\n|> AGGREGATE\n     AVG(lewis) AS pirate,\n     COUNT(bailey) AS chain\n   GROUP BY\n     hound,\n     draper\n|> AGGREGATE\n     COUNT(pirate) AS match,\n     SUM(pirate) AS satire,\n     SUM(chain) AS hopper,\n     AVG(pirate) AS judge,\n     COUNT(chain) AS upturn,\n     MIN(pirate) AS threat,\n     MIN(chain) AS mortar\n|> SELECT\n     lorry,\n     match,\n     satire\n|> AGGREGATE\n     AVG(satire) AS groin,\n     AVG(match) AS roach,\n     MIN(satire) AS tunnel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"lorry",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    injury,\n    utopia,\n    groan,\n    lager,\n    demise\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(demise) AS roach,\n    SUM(groan) AS garden,\n    COUNT(lager) AS hinge,\n    COUNT(demise) AS havoc\n  FROM cte_0\n  GROUP BY\n    injury\n),\ncte_2 AS (\n  SELECT\n    MAX(garden) AS hurst,\n    MAX(roach) AS melon,\n    AVG(havoc) AS upside,\n    COUNT(roach) AS shaikh,\n    MIN(hinge) AS coach\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    hurst,\n    dream,\n    melon,\n    upside,\n    shaikh\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    hurst,\n    SUM(upside) AS lobby\n  FROM cte_3\n  GROUP BY\n    hurst,\n    melon\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     injury,\n     utopia,\n     groan,\n     lager,\n     demise\n|> AGGREGATE\n     MAX(demise) AS roach,\n     SUM(groan) AS garden,\n     COUNT(lager) AS hinge,\n     COUNT(demise) AS havoc\n   GROUP BY\n     injury\n|> AGGREGATE\n     MAX(garden) AS hurst,\n     MAX(roach) AS melon,\n     AVG(havoc) AS upside,\n     COUNT(roach) AS shaikh,\n     MIN(hinge) AS coach\n|> SELECT\n     hurst,\n     dream,\n     melon,\n     upside,\n     shaikh\n|> AGGREGATE\n     SUM(upside) AS lobby\n   GROUP BY\n     hurst,\n     melon\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"dream",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    pupil,\n    sister,\n    fringe,\n    maker,\n    court\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(maker) AS bishop,\n    AVG(fringe) AS blanc\n  FROM cte_0\n  GROUP BY\n    pupil,\n    sister\n),\ncte_2 AS (\n  SELECT\n    COUNT(bishop) AS jelly,\n    MAX(bishop) AS retina,\n    SUM(blanc) AS racism,\n    MIN(bishop) AS mosaic,\n    MAX(blanc) AS curate,\n    AVG(bishop) AS thief\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    jelly,\n    retina,\n    clint\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    jelly,\n    COUNT(retina) AS canon,\n    AVG(retina) AS wagon,\n    MAX(retina) AS water\n  FROM cte_3\n  GROUP BY\n    jelly\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     pupil,\n     sister,\n     fringe,\n     maker,\n     court\n|> AGGREGATE\n     SUM(maker) AS bishop,\n     AVG(fringe) AS blanc\n   GROUP BY\n     pupil,\n     sister\n|> AGGREGATE\n     COUNT(bishop) AS jelly,\n     MAX(bishop) AS retina,\n     SUM(blanc) AS racism,\n     MIN(bishop) AS mosaic,\n     MAX(blanc) AS curate,\n     AVG(bishop) AS thief\n|> SELECT\n     jelly,\n     retina,\n     clint\n|> AGGREGATE\n     COUNT(retina) AS canon,\n     AVG(retina) AS wagon,\n     MAX(retina) AS water\n   GROUP BY\n     jelly\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"clint",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    barton,\n    jacket,\n    clutch,\n    decor,\n    folder\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    barton,\n    jacket,\n    clutch,\n    decor\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(jacket) AS jersey,\n    MAX(decor) AS bloom,\n    MAX(clutch) AS burgh,\n    SUM(clutch) AS niche,\n    COUNT(jacket) AS orchid,\n    COUNT(decor) AS beetle\n  FROM cte_1\n  GROUP BY\n    barton\n),\ncte_3 AS (\n  SELECT\n    jersey,\n    bloom,\n    burgh,\n    grade,\n    niche,\n    orchid\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    jersey,\n    MIN(orchid) AS winter,\n    COUNT(orchid) AS curse\n  FROM cte_3\n  GROUP BY\n    jersey\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     barton,\n     jacket,\n     clutch,\n     decor,\n     folder\n|> SELECT\n     barton,\n     jacket,\n     clutch,\n     decor\n|> AGGREGATE\n     SUM(jacket) AS jersey,\n     MAX(decor) AS bloom,\n     MAX(clutch) AS burgh,\n     SUM(clutch) AS niche,\n     COUNT(jacket) AS orchid,\n     COUNT(decor) AS beetle\n   GROUP BY\n     barton\n|> SELECT\n     jersey,\n     bloom,\n     burgh,\n     grade,\n     niche,\n     orchid\n|> AGGREGATE\n     MIN(orchid) AS winter,\n     COUNT(orchid) AS curse\n   GROUP BY\n     jersey\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"grade",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    helix,\n    vicar,\n    gorge,\n    plane,\n    exodus\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(exodus) AS cellar,\n    MAX(exodus) AS fossil,\n    MIN(exodus) AS burden,\n    AVG(exodus) AS relic\n  FROM cte_0\n  GROUP BY\n    helix,\n    vicar,\n    gorge,\n    plane\n),\ncte_2 AS (\n  SELECT\n    MIN(relic) AS cherry,\n    SUM(relic) AS money,\n    MAX(burden) AS mentor\n  FROM cte_1\n  GROUP BY\n    cellar,\n    fossil\n),\ncte_3 AS (\n  SELECT\n    entity,\n    cherry,\n    money\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(cherry) AS prompt,\n    SUM(money) AS thesis,\n    AVG(money) AS brush\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     helix,\n     vicar,\n     gorge,\n     plane,\n     exodus\n|> AGGREGATE\n     COUNT(exodus) AS cellar,\n     MAX(exodus) AS fossil,\n     MIN(exodus) AS burden,\n     AVG(exodus) AS relic\n   GROUP BY\n     helix,\n     vicar,\n     gorge,\n     plane\n|> AGGREGATE\n     MIN(relic) AS cherry,\n     SUM(relic) AS money,\n     MAX(burden) AS mentor\n   GROUP BY\n     cellar,\n     fossil\n|> SELECT\n     entity,\n     cherry,\n     money\n|> AGGREGATE\n     SUM(cherry) AS prompt,\n     SUM(money) AS thesis,\n     AVG(money) AS brush\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"entity",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    inset,\n    month,\n    plain,\n    venom,\n    toast\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    inset,\n    month,\n    plain,\n    venom,\n    toast\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(venom) AS thief,\n    AVG(toast) AS craven,\n    AVG(month) AS sonnet,\n    MAX(toast) AS person,\n    MIN(plain) AS motion,\n    MIN(inset) AS attic\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    thief,\n    craven,\n    sonnet,\n    daisy,\n    person,\n    motion\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(person) AS flock,\n    AVG(person) AS amber,\n    COUNT(sonnet) AS riddle\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     inset,\n     month,\n     plain,\n     venom,\n     toast\n|> SELECT\n     inset,\n     month,\n     plain,\n     venom,\n     toast\n|> AGGREGATE\n     SUM(venom) AS thief,\n     AVG(toast) AS craven,\n     AVG(month) AS sonnet,\n     MAX(toast) AS person,\n     MIN(plain) AS motion,\n     MIN(inset) AS attic\n|> SELECT\n     thief,\n     craven,\n     sonnet,\n     daisy,\n     person,\n     motion\n|> AGGREGATE\n     MAX(person) AS flock,\n     AVG(person) AS amber,\n     COUNT(sonnet) AS riddle\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"daisy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    knack,\n    daisy,\n    treaty,\n    jasper,\n    crown\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    knack,\n    daisy,\n    treaty\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(treaty) AS vowel,\n    AVG(daisy) AS collar,\n    AVG(treaty) AS crunch,\n    SUM(daisy) AS clash,\n    COUNT(daisy) AS prison,\n    SUM(treaty) AS tariff\n  FROM cte_1\n  GROUP BY\n    knack\n),\ncte_3 AS (\n  SELECT\n    vowel,\n    collar,\n    crunch,\n    clash,\n    thrust,\n    prison\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(collar) AS michel,\n    COUNT(collar) AS meadow,\n    AVG(collar) AS smile\n  FROM cte_3\n  GROUP BY\n    vowel\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     knack,\n     daisy,\n     treaty,\n     jasper,\n     crown\n|> SELECT\n     knack,\n     daisy,\n     treaty\n|> AGGREGATE\n     COUNT(treaty) AS vowel,\n     AVG(daisy) AS collar,\n     AVG(treaty) AS crunch,\n     SUM(daisy) AS clash,\n     COUNT(daisy) AS prison,\n     SUM(treaty) AS tariff\n   GROUP BY\n     knack\n|> SELECT\n     vowel,\n     collar,\n     crunch,\n     clash,\n     thrust,\n     prison\n|> AGGREGATE\n     MAX(collar) AS michel,\n     COUNT(collar) AS meadow,\n     AVG(collar) AS smile\n   GROUP BY\n     vowel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"thrust",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    shaft,\n    stare,\n    genome,\n    trader,\n    rebel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(rebel) AS reign,\n    COUNT(rebel) AS cement,\n    MIN(rebel) AS bundle,\n    MAX(rebel) AS mickey,\n    AVG(rebel) AS patsy\n  FROM cte_0\n  GROUP BY\n    shaft,\n    stare,\n    genome,\n    trader\n),\ncte_2 AS (\n  SELECT\n    reign,\n    cement,\n    bundle,\n    mickey,\n    patsy\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    reign,\n    haste,\n    cement,\n    bundle,\n    mickey\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(mickey) AS spade\n  FROM cte_3\n  GROUP BY\n    reign,\n    cement,\n    bundle\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     shaft,\n     stare,\n     genome,\n     trader,\n     rebel\n|> AGGREGATE\n     SUM(rebel) AS reign,\n     COUNT(rebel) AS cement,\n     MIN(rebel) AS bundle,\n     MAX(rebel) AS mickey,\n     AVG(rebel) AS patsy\n   GROUP BY\n     shaft,\n     stare,\n     genome,\n     trader\n|> SELECT\n     reign,\n     cement,\n     bundle,\n     mickey,\n     patsy\n|> SELECT\n     reign,\n     haste,\n     cement,\n     bundle,\n     mickey\n|> AGGREGATE\n     MIN(mickey) AS spade\n   GROUP BY\n     reign,\n     cement,\n     bundle\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"haste",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    steam,\n    bother,\n    walnut,\n    indie,\n    uplift\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    steam,\n    bother,\n    walnut,\n    indie\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(bother) AS aspect,\n    AVG(walnut) AS poster,\n    COUNT(bother) AS oxide,\n    SUM(indie) AS memory,\n    AVG(indie) AS class\n  FROM cte_1\n  GROUP BY\n    steam\n),\ncte_3 AS (\n  SELECT\n    aspect,\n    poster,\n    oxide,\n    memory,\n    class,\n    warden\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    aspect,\n    poster,\n    SUM(class) AS chorus\n  FROM cte_3\n  GROUP BY\n    aspect,\n    poster,\n    oxide,\n    memory\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     steam,\n     bother,\n     walnut,\n     indie,\n     uplift\n|> SELECT\n     steam,\n     bother,\n     walnut,\n     indie\n|> AGGREGATE\n     MAX(bother) AS aspect,\n     AVG(walnut) AS poster,\n     COUNT(bother) AS oxide,\n     SUM(indie) AS memory,\n     AVG(indie) AS class\n   GROUP BY\n     steam\n|> SELECT\n     aspect,\n     poster,\n     oxide,\n     memory,\n     class,\n     warden\n|> AGGREGATE\n     SUM(class) AS chorus\n   GROUP BY\n     aspect,\n     poster,\n     oxide,\n     memory\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"warden",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    flight,\n    vinyl,\n    resort,\n    sheikh,\n    array\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(array) AS share,\n    COUNT(array) AS splash,\n    MAX(sheikh) AS bruce,\n    MAX(array) AS banana,\n    AVG(array) AS trifle,\n    COUNT(sheikh) AS realm,\n    AVG(sheikh) AS saliva\n  FROM cte_0\n  GROUP BY\n    flight,\n    vinyl,\n    resort\n),\ncte_2 AS (\n  SELECT\n    share,\n    splash,\n    bruce,\n    resale,\n    banana,\n    trifle\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    share,\n    splash,\n    bruce\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(bruce) AS piper\n  FROM cte_3\n  GROUP BY\n    share\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     flight,\n     vinyl,\n     resort,\n     sheikh,\n     array\n|> AGGREGATE\n     MIN(array) AS share,\n     COUNT(array) AS splash,\n     MAX(sheikh) AS bruce,\n     MAX(array) AS banana,\n     AVG(array) AS trifle,\n     COUNT(sheikh) AS realm,\n     AVG(sheikh) AS saliva\n   GROUP BY\n     flight,\n     vinyl,\n     resort\n|> SELECT\n     share,\n     splash,\n     bruce,\n     resale,\n     banana,\n     trifle\n|> SELECT\n     share,\n     splash,\n     bruce\n|> AGGREGATE\n     AVG(bruce) AS piper\n   GROUP BY\n     share\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"resale",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    lager,\n    torso,\n    inset,\n    hazard,\n    rhine\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(rhine) AS tackle,\n    COUNT(rhine) AS siren,\n    MAX(rhine) AS chart,\n    AVG(rhine) AS basic\n  FROM cte_0\n  GROUP BY\n    lager,\n    torso,\n    inset,\n    hazard\n),\ncte_2 AS (\n  SELECT\n    tackle,\n    siren,\n    chart,\n    basic\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    tackle,\n    siren,\n    chart,\n    basic,\n    barrel\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    tackle,\n    AVG(siren) AS surge,\n    SUM(chart) AS splash,\n    MIN(siren) AS clough\n  FROM cte_3\n  GROUP BY\n    tackle\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     lager,\n     torso,\n     inset,\n     hazard,\n     rhine\n|> AGGREGATE\n     MIN(rhine) AS tackle,\n     COUNT(rhine) AS siren,\n     MAX(rhine) AS chart,\n     AVG(rhine) AS basic\n   GROUP BY\n     lager,\n     torso,\n     inset,\n     hazard\n|> SELECT\n     tackle,\n     siren,\n     chart,\n     basic\n|> SELECT\n     tackle,\n     siren,\n     chart,\n     basic,\n     barrel\n|> AGGREGATE\n     AVG(siren) AS surge,\n     SUM(chart) AS splash,\n     MIN(siren) AS clough\n   GROUP BY\n     tackle\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"barrel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:28},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    figure,\n    famine,\n    square,\n    curry,\n    holly\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(holly) AS marrow,\n    COUNT(holly) AS fight,\n    COUNT(square) AS berlin,\n    SUM(square) AS belief,\n    MIN(square) AS plaque\n  FROM cte_0\n  GROUP BY\n    figure,\n    famine\n),\ncte_2 AS (\n  SELECT\n    MAX(belief) AS moran,\n    MIN(berlin) AS speech,\n    MAX(plaque) AS crane,\n    SUM(plaque) AS baton\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    anger,\n    moran,\n    speech,\n    crane\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    moran,\n    AVG(crane) AS lizard\n  FROM cte_3\n  GROUP BY\n    moran,\n    speech\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     figure,\n     famine,\n     square,\n     curry,\n     holly\n|> AGGREGATE\n     MIN(holly) AS marrow,\n     COUNT(holly) AS fight,\n     COUNT(square) AS berlin,\n     SUM(square) AS belief,\n     MIN(square) AS plaque\n   GROUP BY\n     figure,\n     famine\n|> AGGREGATE\n     MAX(belief) AS moran,\n     MIN(berlin) AS speech,\n     MAX(plaque) AS crane,\n     SUM(plaque) AS baton\n|> SELECT\n     anger,\n     moran,\n     speech,\n     crane\n|> AGGREGATE\n     AVG(crane) AS lizard\n   GROUP BY\n     moran,\n     speech\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"anger",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    outcry,\n    rumour,\n    sewage,\n    nature,\n    graph\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(graph) AS nausea,\n    SUM(nature) AS cheque,\n    MIN(rumour) AS custom\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(cheque) AS worry,\n    MAX(cheque) AS lover,\n    AVG(custom) AS frenzy,\n    SUM(custom) AS yellow,\n    MIN(cheque) AS heroin\n  FROM cte_1\n  GROUP BY\n    nausea\n),\ncte_3 AS (\n  SELECT\n    worry,\n    lover,\n    frenzy,\n    carter\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(worry) AS gloss,\n    AVG(lover) AS shoot,\n    COUNT(frenzy) AS optic,\n    SUM(lover) AS witney\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     outcry,\n     rumour,\n     sewage,\n     nature,\n     graph\n|> AGGREGATE\n     COUNT(graph) AS nausea,\n     SUM(nature) AS cheque,\n     MIN(rumour) AS custom\n|> AGGREGATE\n     SUM(cheque) AS worry,\n     MAX(cheque) AS lover,\n     AVG(custom) AS frenzy,\n     SUM(custom) AS yellow,\n     MIN(cheque) AS heroin\n   GROUP BY\n     nausea\n|> SELECT\n     worry,\n     lover,\n     frenzy,\n     carter\n|> AGGREGATE\n     AVG(worry) AS gloss,\n     AVG(lover) AS shoot,\n     COUNT(frenzy) AS optic,\n     SUM(lover) AS witney\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"carter",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tarmac,\n    crowd,\n    speed,\n    trade,\n    kylie\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    tarmac,\n    crowd,\n    speed,\n    trade,\n    kylie\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(kylie) AS ivory,\n    SUM(speed) AS heyday,\n    SUM(kylie) AS bloom,\n    MAX(trade) AS advice,\n    AVG(trade) AS ledger\n  FROM cte_1\n  GROUP BY\n    tarmac,\n    crowd\n),\ncte_3 AS (\n  SELECT\n    ivory,\n    heyday,\n    bloom,\n    truck\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    ivory,\n    MAX(bloom) AS rumour,\n    COUNT(bloom) AS fibre,\n    AVG(heyday) AS green\n  FROM cte_3\n  GROUP BY\n    ivory\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tarmac,\n     crowd,\n     speed,\n     trade,\n     kylie\n|> SELECT\n     tarmac,\n     crowd,\n     speed,\n     trade,\n     kylie\n|> AGGREGATE\n     AVG(kylie) AS ivory,\n     SUM(speed) AS heyday,\n     SUM(kylie) AS bloom,\n     MAX(trade) AS advice,\n     AVG(trade) AS ledger\n   GROUP BY\n     tarmac,\n     crowd\n|> SELECT\n     ivory,\n     heyday,\n     bloom,\n     truck\n|> AGGREGATE\n     MAX(bloom) AS rumour,\n     COUNT(bloom) AS fibre,\n     AVG(heyday) AS green\n   GROUP BY\n     ivory\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"truck",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bother,\n    vision,\n    cloak,\n    glance,\n    leone\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    bother,\n    vision,\n    cloak\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(bother) AS sugar,\n    COUNT(bother) AS ditch,\n    AVG(vision) AS globe,\n    SUM(bother) AS scout,\n    COUNT(cloak) AS pause,\n    MAX(vision) AS lunch,\n    MIN(cloak) AS brandy,\n    MIN(vision) AS chang\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    sugar,\n    german,\n    ditch,\n    globe\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    sugar,\n    MAX(ditch) AS china,\n    COUNT(ditch) AS drive\n  FROM cte_3\n  GROUP BY\n    sugar\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bother,\n     vision,\n     cloak,\n     glance,\n     leone\n|> SELECT\n     bother,\n     vision,\n     cloak\n|> AGGREGATE\n     AVG(bother) AS sugar,\n     COUNT(bother) AS ditch,\n     AVG(vision) AS globe,\n     SUM(bother) AS scout,\n     COUNT(cloak) AS pause,\n     MAX(vision) AS lunch,\n     MIN(cloak) AS brandy,\n     MIN(vision) AS chang\n|> SELECT\n     sugar,\n     german,\n     ditch,\n     globe\n|> AGGREGATE\n     MAX(ditch) AS china,\n     COUNT(ditch) AS drive\n   GROUP BY\n     sugar\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"german",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    lapse,\n    torque,\n    donor,\n    chart,\n    mungo\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(mungo) AS folly,\n    COUNT(chart) AS energy,\n    AVG(chart) AS killer,\n    AVG(mungo) AS flesh,\n    SUM(mungo) AS frame,\n    MIN(chart) AS guido,\n    MAX(chart) AS bliss,\n    MAX(mungo) AS denial\n  FROM cte_0\n  GROUP BY\n    lapse,\n    torque,\n    donor\n),\ncte_2 AS (\n  SELECT\n    folly,\n    diary,\n    energy,\n    killer,\n    flesh,\n    frame\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(folly) AS basil\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(basil) AS cabin\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     lapse,\n     torque,\n     donor,\n     chart,\n     mungo\n|> AGGREGATE\n     MIN(mungo) AS folly,\n     COUNT(chart) AS energy,\n     AVG(chart) AS killer,\n     AVG(mungo) AS flesh,\n     SUM(mungo) AS frame,\n     MIN(chart) AS guido,\n     MAX(chart) AS bliss,\n     MAX(mungo) AS denial\n   GROUP BY\n     lapse,\n     torque,\n     donor\n|> SELECT\n     folly,\n     diary,\n     energy,\n     killer,\n     flesh,\n     frame\n|> AGGREGATE\n     COUNT(folly) AS basil\n|> AGGREGATE\n     SUM(basil) AS cabin\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"diary",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    oxide,\n    museum,\n    sting,\n    share,\n    family\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    oxide,\n    museum,\n    sting,\n    share,\n    family\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(share) AS truce,\n    AVG(sting) AS pedal,\n    COUNT(family) AS shore,\n    MIN(share) AS surrey,\n    AVG(family) AS bishop\n  FROM cte_1\n  GROUP BY\n    oxide,\n    museum\n),\ncte_3 AS (\n  SELECT\n    truce,\n    pedal,\n    shore,\n    temple,\n    surrey,\n    bishop\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(pedal) AS amount,\n    MIN(bishop) AS dress,\n    AVG(surrey) AS depot\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     oxide,\n     museum,\n     sting,\n     share,\n     family\n|> SELECT\n     oxide,\n     museum,\n     sting,\n     share,\n     family\n|> AGGREGATE\n     SUM(share) AS truce,\n     AVG(sting) AS pedal,\n     COUNT(family) AS shore,\n     MIN(share) AS surrey,\n     AVG(family) AS bishop\n   GROUP BY\n     oxide,\n     museum\n|> SELECT\n     truce,\n     pedal,\n     shore,\n     temple,\n     surrey,\n     bishop\n|> AGGREGATE\n     SUM(pedal) AS amount,\n     MIN(bishop) AS dress,\n     AVG(surrey) AS depot\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"temple",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ascent,\n    baton,\n    beach,\n    leave,\n    berth\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(berth) AS poison,\n    COUNT(beach) AS helmet,\n    SUM(beach) AS chair,\n    MIN(berth) AS plasma,\n    SUM(leave) AS award,\n    COUNT(leave) AS hammer,\n    SUM(baton) AS tallis,\n    SUM(berth) AS carer\n  FROM cte_0\n  GROUP BY\n    ascent\n),\ncte_2 AS (\n  SELECT\n    poison,\n    helmet,\n    chair,\n    album\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    poison,\n    helmet,\n    chair\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(helmet) AS shout,\n    COUNT(chair) AS dogma\n  FROM cte_3\n  GROUP BY\n    poison\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ascent,\n     baton,\n     beach,\n     leave,\n     berth\n|> AGGREGATE\n     AVG(berth) AS poison,\n     COUNT(beach) AS helmet,\n     SUM(beach) AS chair,\n     MIN(berth) AS plasma,\n     SUM(leave) AS award,\n     COUNT(leave) AS hammer,\n     SUM(baton) AS tallis,\n     SUM(berth) AS carer\n   GROUP BY\n     ascent\n|> SELECT\n     poison,\n     helmet,\n     chair,\n     album\n|> SELECT\n     poison,\n     helmet,\n     chair\n|> AGGREGATE\n     AVG(helmet) AS shout,\n     COUNT(chair) AS dogma\n   GROUP BY\n     poison\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"album",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    reader,\n    urine,\n    sugar,\n    squash,\n    storm\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(sugar) AS tunic,\n    MAX(urine) AS vowel,\n    COUNT(urine) AS trend,\n    MAX(sugar) AS lover,\n    COUNT(squash) AS prose,\n    MIN(squash) AS singer,\n    SUM(storm) AS stone,\n    SUM(sugar) AS usage\n  FROM cte_0\n  GROUP BY\n    reader\n),\ncte_2 AS (\n  SELECT\n    tunic,\n    vowel,\n    trend,\n    mason,\n    lover,\n    prose\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(trend) AS wharf\n  FROM cte_2\n  GROUP BY\n    tunic,\n    vowel\n),\ncte_4 AS (\n  SELECT\n    COUNT(wharf) AS gypsy\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     reader,\n     urine,\n     sugar,\n     squash,\n     storm\n|> AGGREGATE\n     COUNT(sugar) AS tunic,\n     MAX(urine) AS vowel,\n     COUNT(urine) AS trend,\n     MAX(sugar) AS lover,\n     COUNT(squash) AS prose,\n     MIN(squash) AS singer,\n     SUM(storm) AS stone,\n     SUM(sugar) AS usage\n   GROUP BY\n     reader\n|> SELECT\n     tunic,\n     vowel,\n     trend,\n     mason,\n     lover,\n     prose\n|> AGGREGATE\n     SUM(trend) AS wharf\n   GROUP BY\n     tunic,\n     vowel\n|> AGGREGATE\n     COUNT(wharf) AS gypsy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"mason",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    strap,\n    liquid,\n    prior,\n    crate,\n    knock\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(crate) AS straw,\n    COUNT(knock) AS effort,\n    SUM(knock) AS ascot,\n    MIN(knock) AS hardy,\n    MAX(crate) AS virtue,\n    AVG(crate) AS spread\n  FROM cte_0\n  GROUP BY\n    strap,\n    liquid,\n    prior\n),\ncte_2 AS (\n  SELECT\n    straw,\n    effort,\n    ascot,\n    hardy,\n    virtue,\n    mentor,\n    spread\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    straw,\n    effort,\n    ascot\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(straw) AS pride,\n    MIN(ascot) AS cancer\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     strap,\n     liquid,\n     prior,\n     crate,\n     knock\n|> AGGREGATE\n     COUNT(crate) AS straw,\n     COUNT(knock) AS effort,\n     SUM(knock) AS ascot,\n     MIN(knock) AS hardy,\n     MAX(crate) AS virtue,\n     AVG(crate) AS spread\n   GROUP BY\n     strap,\n     liquid,\n     prior\n|> SELECT\n     straw,\n     effort,\n     ascot,\n     hardy,\n     virtue,\n     mentor,\n     spread\n|> SELECT\n     straw,\n     effort,\n     ascot\n|> AGGREGATE\n     MIN(straw) AS pride,\n     MIN(ascot) AS cancer\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"mentor",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rugby,\n    merger,\n    phoebe,\n    grease,\n    mafia\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(mafia) AS barton,\n    SUM(grease) AS beard,\n    AVG(mafia) AS crisis,\n    MIN(grease) AS michel\n  FROM cte_0\n  GROUP BY\n    rugby,\n    merger,\n    phoebe\n),\ncte_2 AS (\n  SELECT\n    AVG(michel) AS senate,\n    AVG(crisis) AS upland,\n    MIN(michel) AS grace\n  FROM cte_1\n  GROUP BY\n    barton,\n    beard\n),\ncte_3 AS (\n  SELECT\n    senate,\n    hockey,\n    upland,\n    grace\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    senate,\n    MAX(upland) AS query,\n    AVG(upland) AS degree\n  FROM cte_3\n  GROUP BY\n    senate\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rugby,\n     merger,\n     phoebe,\n     grease,\n     mafia\n|> AGGREGATE\n     MIN(mafia) AS barton,\n     SUM(grease) AS beard,\n     AVG(mafia) AS crisis,\n     MIN(grease) AS michel\n   GROUP BY\n     rugby,\n     merger,\n     phoebe\n|> AGGREGATE\n     AVG(michel) AS senate,\n     AVG(crisis) AS upland,\n     MIN(michel) AS grace\n   GROUP BY\n     barton,\n     beard\n|> SELECT\n     senate,\n     hockey,\n     upland,\n     grace\n|> AGGREGATE\n     MAX(upland) AS query,\n     AVG(upland) AS degree\n   GROUP BY\n     senate\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"hockey",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    grade,\n    grove,\n    laity,\n    patten,\n    match\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(grove) AS bingo,\n    SUM(match) AS cadet,\n    MIN(laity) AS belle,\n    MAX(match) AS wally,\n    MAX(grade) AS gloria,\n    AVG(match) AS defect,\n    COUNT(match) AS priory,\n    MIN(match) AS alien,\n    COUNT(laity) AS charge\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    bingo,\n    cadet,\n    marker\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(cadet) AS guide,\n    MIN(cadet) AS winger\n  FROM cte_2\n  GROUP BY\n    bingo\n),\ncte_4 AS (\n  SELECT\n    AVG(winger) AS spate\n  FROM cte_3\n  GROUP BY\n    guide\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     grade,\n     grove,\n     laity,\n     patten,\n     match\n|> AGGREGATE\n     AVG(grove) AS bingo,\n     SUM(match) AS cadet,\n     MIN(laity) AS belle,\n     MAX(match) AS wally,\n     MAX(grade) AS gloria,\n     AVG(match) AS defect,\n     COUNT(match) AS priory,\n     MIN(match) AS alien,\n     COUNT(laity) AS charge\n|> SELECT\n     bingo,\n     cadet,\n     marker\n|> AGGREGATE\n     SUM(cadet) AS guide,\n     MIN(cadet) AS winger\n   GROUP BY\n     bingo\n|> AGGREGATE\n     AVG(winger) AS spate\n   GROUP BY\n     guide\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"marker",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:28,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    laity,\n    chorus,\n    summer,\n    habit,\n    laura\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(summer) AS soviet,\n    MIN(chorus) AS fender,\n    AVG(chorus) AS thrush,\n    AVG(laity) AS reward,\n    SUM(chorus) AS vessel,\n    MIN(summer) AS plasma\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    soviet,\n    fender,\n    thrush,\n    reward,\n    vessel,\n    plasma\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    soviet,\n    fender,\n    caddie,\n    thrush,\n    reward,\n    vessel\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    soviet,\n    SUM(thrush) AS route\n  FROM cte_3\n  GROUP BY\n    soviet,\n    fender\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     laity,\n     chorus,\n     summer,\n     habit,\n     laura\n|> AGGREGATE\n     COUNT(summer) AS soviet,\n     MIN(chorus) AS fender,\n     AVG(chorus) AS thrush,\n     AVG(laity) AS reward,\n     SUM(chorus) AS vessel,\n     MIN(summer) AS plasma\n|> SELECT\n     soviet,\n     fender,\n     thrush,\n     reward,\n     vessel,\n     plasma\n|> SELECT\n     soviet,\n     fender,\n     caddie,\n     thrush,\n     reward,\n     vessel\n|> AGGREGATE\n     SUM(thrush) AS route\n   GROUP BY\n     soviet,\n     fender\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"caddie",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hogan,\n    ulster,\n    notion,\n    ribber,\n    truck\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(truck) AS wagon,\n    SUM(truck) AS scare,\n    MIN(truck) AS legion,\n    AVG(ribber) AS sheen,\n    COUNT(truck) AS christ,\n    AVG(truck) AS trust\n  FROM cte_0\n  GROUP BY\n    hogan,\n    ulster,\n    notion\n),\ncte_2 AS (\n  SELECT\n    wagon,\n    scare,\n    legion,\n    sheen\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    wagon,\n    ferret,\n    scare,\n    legion\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(legion) AS shaft,\n    COUNT(legion) AS female\n  FROM cte_3\n  GROUP BY\n    wagon,\n    scare\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hogan,\n     ulster,\n     notion,\n     ribber,\n     truck\n|> AGGREGATE\n     MAX(truck) AS wagon,\n     SUM(truck) AS scare,\n     MIN(truck) AS legion,\n     AVG(ribber) AS sheen,\n     COUNT(truck) AS christ,\n     AVG(truck) AS trust\n   GROUP BY\n     hogan,\n     ulster,\n     notion\n|> SELECT\n     wagon,\n     scare,\n     legion,\n     sheen\n|> SELECT\n     wagon,\n     ferret,\n     scare,\n     legion\n|> AGGREGATE\n     AVG(legion) AS shaft,\n     COUNT(legion) AS female\n   GROUP BY\n     wagon,\n     scare\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"ferret",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    stern,\n    round,\n    ghana,\n    jigsaw,\n    razor\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(round) AS anthem,\n    MIN(ghana) AS telly,\n    AVG(round) AS daddy,\n    AVG(razor) AS union,\n    MAX(jigsaw) AS myriad,\n    SUM(razor) AS poppy,\n    SUM(jigsaw) AS tweed,\n    SUM(ghana) AS salary,\n    AVG(jigsaw) AS sanity\n  FROM cte_0\n  GROUP BY\n    stern\n),\ncte_2 AS (\n  SELECT\n    anthem,\n    person,\n    telly,\n    daddy\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    anthem,\n    telly,\n    daddy\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    anthem,\n    telly,\n    daddy\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     stern,\n     round,\n     ghana,\n     jigsaw,\n     razor\n|> AGGREGATE\n     MIN(round) AS anthem,\n     MIN(ghana) AS telly,\n     AVG(round) AS daddy,\n     AVG(razor) AS union,\n     MAX(jigsaw) AS myriad,\n     SUM(razor) AS poppy,\n     SUM(jigsaw) AS tweed,\n     SUM(ghana) AS salary,\n     AVG(jigsaw) AS sanity\n   GROUP BY\n     stern\n|> SELECT\n     anthem,\n     person,\n     telly,\n     daddy\n|> SELECT\n     anthem,\n     telly,\n     daddy\n|> SELECT\n     anthem,\n     telly,\n     daddy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"person",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tower,\n    finger,\n    chord,\n    rover,\n    limit\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(rover) AS combat,\n    SUM(limit) AS darwin,\n    MAX(chord) AS nanny,\n    MIN(limit) AS pearl,\n    COUNT(limit) AS guide,\n    MIN(rover) AS devon,\n    MAX(limit) AS burial\n  FROM cte_0\n  GROUP BY\n    tower,\n    finger\n),\ncte_2 AS (\n  SELECT\n    combat,\n    darwin,\n    nanny,\n    pearl,\n    clump,\n    guide\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    combat,\n    darwin,\n    nanny\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(combat) AS groove,\n    MIN(darwin) AS delta\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tower,\n     finger,\n     chord,\n     rover,\n     limit\n|> AGGREGATE\n     AVG(rover) AS combat,\n     SUM(limit) AS darwin,\n     MAX(chord) AS nanny,\n     MIN(limit) AS pearl,\n     COUNT(limit) AS guide,\n     MIN(rover) AS devon,\n     MAX(limit) AS burial\n   GROUP BY\n     tower,\n     finger\n|> SELECT\n     combat,\n     darwin,\n     nanny,\n     pearl,\n     clump,\n     guide\n|> SELECT\n     combat,\n     darwin,\n     nanny\n|> AGGREGATE\n     COUNT(combat) AS groove,\n     MIN(darwin) AS delta\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"clump",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    texas,\n    salary,\n    gutter,\n    suburb,\n    peril\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(gutter) AS bride,\n    MIN(peril) AS hopper,\n    SUM(suburb) AS whale,\n    COUNT(suburb) AS feast,\n    AVG(gutter) AS water,\n    MIN(gutter) AS crunch\n  FROM cte_0\n  GROUP BY\n    texas,\n    salary\n),\ncte_2 AS (\n  SELECT\n    bride,\n    hopper,\n    whale,\n    feast\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    bride,\n    hopper,\n    relay\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    bride,\n    COUNT(hopper) AS badger,\n    MAX(hopper) AS theory,\n    SUM(hopper) AS bazaar\n  FROM cte_3\n  GROUP BY\n    bride\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     texas,\n     salary,\n     gutter,\n     suburb,\n     peril\n|> AGGREGATE\n     COUNT(gutter) AS bride,\n     MIN(peril) AS hopper,\n     SUM(suburb) AS whale,\n     COUNT(suburb) AS feast,\n     AVG(gutter) AS water,\n     MIN(gutter) AS crunch\n   GROUP BY\n     texas,\n     salary\n|> SELECT\n     bride,\n     hopper,\n     whale,\n     feast\n|> SELECT\n     bride,\n     hopper,\n     relay\n|> AGGREGATE\n     COUNT(hopper) AS badger,\n     MAX(hopper) AS theory,\n     SUM(hopper) AS bazaar\n   GROUP BY\n     bride\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"relay",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    skill,\n    jockey,\n    schema,\n    loser,\n    catch\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    skill,\n    jockey,\n    schema,\n    loser,\n    catch\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(schema) AS image,\n    SUM(loser) AS smash,\n    SUM(catch) AS martyr,\n    MIN(loser) AS crisis,\n    MAX(schema) AS ryder\n  FROM cte_1\n  GROUP BY\n    skill,\n    jockey\n),\ncte_3 AS (\n  SELECT\n    image,\n    smash,\n    martyr,\n    slick\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(martyr) AS peter,\n    COUNT(martyr) AS grape,\n    MAX(martyr) AS stitch\n  FROM cte_3\n  GROUP BY\n    image,\n    smash\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     skill,\n     jockey,\n     schema,\n     loser,\n     catch\n|> SELECT\n     skill,\n     jockey,\n     schema,\n     loser,\n     catch\n|> AGGREGATE\n     COUNT(schema) AS image,\n     SUM(loser) AS smash,\n     SUM(catch) AS martyr,\n     MIN(loser) AS crisis,\n     MAX(schema) AS ryder\n   GROUP BY\n     skill,\n     jockey\n|> SELECT\n     image,\n     smash,\n     martyr,\n     slick\n|> AGGREGATE\n     MIN(martyr) AS peter,\n     COUNT(martyr) AS grape,\n     MAX(martyr) AS stitch\n   GROUP BY\n     image,\n     smash\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"slick",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    strand,\n    yield,\n    gleam,\n    export,\n    shower\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(shower) AS finale,\n    MIN(shower) AS apollo,\n    MIN(export) AS steven,\n    AVG(export) AS trout,\n    MAX(export) AS reform\n  FROM cte_0\n  GROUP BY\n    strand,\n    yield,\n    gleam\n),\ncte_2 AS (\n  SELECT\n    finale,\n    apollo,\n    steven,\n    trout,\n    reform\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    finale,\n    apollo,\n    karate,\n    steven,\n    trout,\n    reform\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    finale,\n    apollo,\n    steven,\n    trout,\n    reform\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     strand,\n     yield,\n     gleam,\n     export,\n     shower\n|> AGGREGATE\n     COUNT(shower) AS finale,\n     MIN(shower) AS apollo,\n     MIN(export) AS steven,\n     AVG(export) AS trout,\n     MAX(export) AS reform\n   GROUP BY\n     strand,\n     yield,\n     gleam\n|> SELECT\n     finale,\n     apollo,\n     steven,\n     trout,\n     reform\n|> SELECT\n     finale,\n     apollo,\n     karate,\n     steven,\n     trout,\n     reform\n|> SELECT\n     finale,\n     apollo,\n     steven,\n     trout,\n     reform\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"karate",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tunic,\n    oscar,\n    flurry,\n    idiom,\n    volume\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    tunic,\n    oscar,\n    flurry,\n    idiom\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(idiom) AS strap,\n    AVG(flurry) AS option,\n    SUM(tunic) AS plough,\n    MAX(tunic) AS grass,\n    MIN(tunic) AS yeast,\n    MIN(idiom) AS forte,\n    SUM(flurry) AS hatch\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    strap,\n    option,\n    client\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    strap,\n    MAX(option) AS craft,\n    SUM(option) AS editor,\n    MIN(option) AS logan\n  FROM cte_3\n  GROUP BY\n    strap\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tunic,\n     oscar,\n     flurry,\n     idiom,\n     volume\n|> SELECT\n     tunic,\n     oscar,\n     flurry,\n     idiom\n|> AGGREGATE\n     AVG(idiom) AS strap,\n     AVG(flurry) AS option,\n     SUM(tunic) AS plough,\n     MAX(tunic) AS grass,\n     MIN(tunic) AS yeast,\n     MIN(idiom) AS forte,\n     SUM(flurry) AS hatch\n|> SELECT\n     strap,\n     option,\n     client\n|> AGGREGATE\n     MAX(option) AS craft,\n     SUM(option) AS editor,\n     MIN(option) AS logan\n   GROUP BY\n     strap\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"client",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    picket,\n    ritual,\n    chief,\n    series,\n    disco\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(disco) AS report,\n    MAX(disco) AS format,\n    SUM(disco) AS outfit,\n    AVG(disco) AS soviet,\n    COUNT(disco) AS money\n  FROM cte_0\n  GROUP BY\n    picket,\n    ritual,\n    chief,\n    series\n),\ncte_2 AS (\n  SELECT\n    COUNT(format) AS ticket,\n    SUM(outfit) AS globe,\n    SUM(soviet) AS effort\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    argyll,\n    ticket,\n    globe,\n    effort\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(globe) AS cinema,\n    COUNT(globe) AS court\n  FROM cte_3\n  GROUP BY\n    ticket\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     picket,\n     ritual,\n     chief,\n     series,\n     disco\n|> AGGREGATE\n     MIN(disco) AS report,\n     MAX(disco) AS format,\n     SUM(disco) AS outfit,\n     AVG(disco) AS soviet,\n     COUNT(disco) AS money\n   GROUP BY\n     picket,\n     ritual,\n     chief,\n     series\n|> AGGREGATE\n     COUNT(format) AS ticket,\n     SUM(outfit) AS globe,\n     SUM(soviet) AS effort\n|> SELECT\n     argyll,\n     ticket,\n     globe,\n     effort\n|> AGGREGATE\n     MIN(globe) AS cinema,\n     COUNT(globe) AS court\n   GROUP BY\n     ticket\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"argyll",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    lynch,\n    ivory,\n    slough,\n    raffle,\n    stable\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(slough) AS betty,\n    COUNT(raffle) AS plight,\n    AVG(slough) AS peace,\n    AVG(raffle) AS amber,\n    MIN(raffle) AS waist,\n    MAX(stable) AS bureau\n  FROM cte_0\n  GROUP BY\n    lynch,\n    ivory\n),\ncte_2 AS (\n  SELECT\n    betty,\n    plight,\n    peace,\n    amber,\n    waist,\n    bureau,\n    flesh\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(bureau) AS newco,\n    MIN(bureau) AS spruce,\n    MAX(betty) AS meter\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(spruce) AS piano\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     lynch,\n     ivory,\n     slough,\n     raffle,\n     stable\n|> AGGREGATE\n     MAX(slough) AS betty,\n     COUNT(raffle) AS plight,\n     AVG(slough) AS peace,\n     AVG(raffle) AS amber,\n     MIN(raffle) AS waist,\n     MAX(stable) AS bureau\n   GROUP BY\n     lynch,\n     ivory\n|> SELECT\n     betty,\n     plight,\n     peace,\n     amber,\n     waist,\n     bureau,\n     flesh\n|> AGGREGATE\n     MAX(bureau) AS newco,\n     MIN(bureau) AS spruce,\n     MAX(betty) AS meter\n|> AGGREGATE\n     SUM(spruce) AS piano\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"flesh",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    decay,\n    wedge,\n    safari,\n    vicar,\n    dream\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(dream) AS condom,\n    AVG(safari) AS myriad,\n    MIN(safari) AS dread,\n    SUM(decay) AS battle,\n    AVG(decay) AS castle,\n    AVG(vicar) AS odour,\n    COUNT(safari) AS billy\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    condom,\n    myriad,\n    dread,\n    battle,\n    castle,\n    odour,\n    actor,\n    billy\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    condom,\n    MIN(castle) AS device\n  FROM cte_2\n  GROUP BY\n    condom,\n    myriad\n),\ncte_4 AS (\n  SELECT\n    SUM(device) AS daddy\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     decay,\n     wedge,\n     safari,\n     vicar,\n     dream\n|> AGGREGATE\n     AVG(dream) AS condom,\n     AVG(safari) AS myriad,\n     MIN(safari) AS dread,\n     SUM(decay) AS battle,\n     AVG(decay) AS castle,\n     AVG(vicar) AS odour,\n     COUNT(safari) AS billy\n|> SELECT\n     condom,\n     myriad,\n     dread,\n     battle,\n     castle,\n     odour,\n     actor,\n     billy\n|> AGGREGATE\n     MIN(castle) AS device\n   GROUP BY\n     condom,\n     myriad\n|> AGGREGATE\n     SUM(device) AS daddy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"actor",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    cargo,\n    haven,\n    resin,\n    cadet,\n    thief\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(thief) AS cough,\n    COUNT(cargo) AS hitch,\n    MIN(thief) AS value,\n    AVG(cargo) AS moment,\n    AVG(thief) AS bunch,\n    MAX(cargo) AS spider,\n    MIN(cadet) AS reflex,\n    SUM(resin) AS hardy\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    cough,\n    hitch,\n    value,\n    moment,\n    boiler,\n    bunch\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(value) AS golfer,\n    AVG(bunch) AS basic\n  FROM cte_2\n  GROUP BY\n    cough\n),\ncte_4 AS (\n  SELECT\n    AVG(basic) AS thigh\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     cargo,\n     haven,\n     resin,\n     cadet,\n     thief\n|> AGGREGATE\n     SUM(thief) AS cough,\n     COUNT(cargo) AS hitch,\n     MIN(thief) AS value,\n     AVG(cargo) AS moment,\n     AVG(thief) AS bunch,\n     MAX(cargo) AS spider,\n     MIN(cadet) AS reflex,\n     SUM(resin) AS hardy\n|> SELECT\n     cough,\n     hitch,\n     value,\n     moment,\n     boiler,\n     bunch\n|> AGGREGATE\n     SUM(value) AS golfer,\n     AVG(bunch) AS basic\n   GROUP BY\n     cough\n|> AGGREGATE\n     AVG(basic) AS thigh\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"boiler",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sophie,\n    shell,\n    barker,\n    update,\n    canary\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(barker) AS flesh,\n    MIN(shell) AS orange,\n    COUNT(update) AS corpus,\n    MIN(update) AS buffet,\n    MAX(update) AS collar,\n    AVG(canary) AS laird\n  FROM cte_0\n  GROUP BY\n    sophie\n),\ncte_2 AS (\n  SELECT\n    flesh,\n    orange,\n    corpus,\n    buffet\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    flesh,\n    orange,\n    corpus,\n    black\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(orange) AS combo,\n    COUNT(orange) AS scalp,\n    MIN(corpus) AS beech,\n    MAX(corpus) AS barge\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sophie,\n     shell,\n     barker,\n     update,\n     canary\n|> AGGREGATE\n     SUM(barker) AS flesh,\n     MIN(shell) AS orange,\n     COUNT(update) AS corpus,\n     MIN(update) AS buffet,\n     MAX(update) AS collar,\n     AVG(canary) AS laird\n   GROUP BY\n     sophie\n|> SELECT\n     flesh,\n     orange,\n     corpus,\n     buffet\n|> SELECT\n     flesh,\n     orange,\n     corpus,\n     black\n|> AGGREGATE\n     AVG(orange) AS combo,\n     COUNT(orange) AS scalp,\n     MIN(corpus) AS beech,\n     MAX(corpus) AS barge\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"black",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    muddle,\n    kettle,\n    drill,\n    cable,\n    draper\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(muddle) AS strap,\n    MAX(cable) AS daily,\n    AVG(drill) AS finger,\n    SUM(muddle) AS sweat,\n    MIN(muddle) AS forum,\n    COUNT(kettle) AS unison\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    strap,\n    daily,\n    finger,\n    sweat\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    strap,\n    daily,\n    finger,\n    sweat,\n    siren\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(sweat) AS peril,\n    MIN(sweat) AS device,\n    MAX(finger) AS reply\n  FROM cte_3\n  GROUP BY\n    strap,\n    daily\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     muddle,\n     kettle,\n     drill,\n     cable,\n     draper\n|> AGGREGATE\n     AVG(muddle) AS strap,\n     MAX(cable) AS daily,\n     AVG(drill) AS finger,\n     SUM(muddle) AS sweat,\n     MIN(muddle) AS forum,\n     COUNT(kettle) AS unison\n|> SELECT\n     strap,\n     daily,\n     finger,\n     sweat\n|> SELECT\n     strap,\n     daily,\n     finger,\n     sweat,\n     siren\n|> AGGREGATE\n     MAX(sweat) AS peril,\n     MIN(sweat) AS device,\n     MAX(finger) AS reply\n   GROUP BY\n     strap,\n     daily\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"siren",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    pouch,\n    turkey,\n    spence,\n    sport,\n    scotch\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(scotch) AS victor,\n    MAX(scotch) AS voyage\n  FROM cte_0\n  GROUP BY\n    pouch,\n    turkey,\n    spence,\n    sport\n),\ncte_2 AS (\n  SELECT\n    MIN(voyage) AS hooper,\n    COUNT(voyage) AS wizard,\n    MAX(voyage) AS polish,\n    SUM(voyage) AS boxer,\n    AVG(voyage) AS cliff\n  FROM cte_1\n  GROUP BY\n    victor\n),\ncte_3 AS (\n  SELECT\n    hooper,\n    puppy,\n    wizard,\n    polish,\n    boxer\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    hooper,\n    MIN(boxer) AS dolly\n  FROM cte_3\n  GROUP BY\n    hooper,\n    wizard\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     pouch,\n     turkey,\n     spence,\n     sport,\n     scotch\n|> AGGREGATE\n     SUM(scotch) AS victor,\n     MAX(scotch) AS voyage\n   GROUP BY\n     pouch,\n     turkey,\n     spence,\n     sport\n|> AGGREGATE\n     MIN(voyage) AS hooper,\n     COUNT(voyage) AS wizard,\n     MAX(voyage) AS polish,\n     SUM(voyage) AS boxer,\n     AVG(voyage) AS cliff\n   GROUP BY\n     victor\n|> SELECT\n     hooper,\n     puppy,\n     wizard,\n     polish,\n     boxer\n|> AGGREGATE\n     MIN(boxer) AS dolly\n   GROUP BY\n     hooper,\n     wizard\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"puppy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    cobra,\n    yield,\n    rector,\n    flair,\n    bronze\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    cobra,\n    yield,\n    rector,\n    flair,\n    bronze\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(bronze) AS studio,\n    MIN(bronze) AS infant,\n    MIN(flair) AS rumour,\n    MAX(bronze) AS asset\n  FROM cte_1\n  GROUP BY\n    cobra,\n    yield,\n    rector\n),\ncte_3 AS (\n  SELECT\n    studio,\n    infant,\n    rumour,\n    asset,\n    award\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(infant) AS custom,\n    MAX(rumour) AS class,\n    SUM(rumour) AS rubble,\n    COUNT(asset) AS effect\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     cobra,\n     yield,\n     rector,\n     flair,\n     bronze\n|> SELECT\n     cobra,\n     yield,\n     rector,\n     flair,\n     bronze\n|> AGGREGATE\n     AVG(bronze) AS studio,\n     MIN(bronze) AS infant,\n     MIN(flair) AS rumour,\n     MAX(bronze) AS asset\n   GROUP BY\n     cobra,\n     yield,\n     rector\n|> SELECT\n     studio,\n     infant,\n     rumour,\n     asset,\n     award\n|> AGGREGATE\n     MIN(infant) AS custom,\n     MAX(rumour) AS class,\n     SUM(rumour) AS rubble,\n     COUNT(asset) AS effect\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"award",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:28},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tycoon,\n    sally,\n    wizard,\n    thread,\n    lesson\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(lesson) AS elbow,\n    SUM(thread) AS inset\n  FROM cte_0\n  GROUP BY\n    tycoon,\n    sally\n),\ncte_2 AS (\n  SELECT\n    MIN(elbow) AS vessel,\n    COUNT(inset) AS bucket,\n    MAX(inset) AS brick,\n    MIN(inset) AS peril,\n    SUM(elbow) AS novice,\n    SUM(inset) AS tandem\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    vessel,\n    bucket,\n    actor,\n    brick,\n    peril,\n    novice\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(brick) AS granny,\n    MAX(bucket) AS world\n  FROM cte_3\n  GROUP BY\n    vessel\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tycoon,\n     sally,\n     wizard,\n     thread,\n     lesson\n|> AGGREGATE\n     COUNT(lesson) AS elbow,\n     SUM(thread) AS inset\n   GROUP BY\n     tycoon,\n     sally\n|> AGGREGATE\n     MIN(elbow) AS vessel,\n     COUNT(inset) AS bucket,\n     MAX(inset) AS brick,\n     MIN(inset) AS peril,\n     SUM(elbow) AS novice,\n     SUM(inset) AS tandem\n|> SELECT\n     vessel,\n     bucket,\n     actor,\n     brick,\n     peril,\n     novice\n|> AGGREGATE\n     SUM(brick) AS granny,\n     MAX(bucket) AS world\n   GROUP BY\n     vessel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"actor",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    smack,\n    taste,\n    berlin,\n    sharp,\n    stamp\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    smack,\n    taste,\n    berlin\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(taste) AS reign,\n    SUM(berlin) AS pulpit,\n    MIN(berlin) AS thorpe,\n    AVG(taste) AS maiden,\n    COUNT(taste) AS friar,\n    MAX(berlin) AS clown,\n    SUM(taste) AS deputy\n  FROM cte_1\n  GROUP BY\n    smack\n),\ncte_3 AS (\n  SELECT\n    reign,\n    pulpit,\n    switch,\n    thorpe,\n    maiden\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    reign,\n    pulpit,\n    COUNT(maiden) AS circle\n  FROM cte_3\n  GROUP BY\n    reign,\n    pulpit\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     smack,\n     taste,\n     berlin,\n     sharp,\n     stamp\n|> SELECT\n     smack,\n     taste,\n     berlin\n|> AGGREGATE\n     MAX(taste) AS reign,\n     SUM(berlin) AS pulpit,\n     MIN(berlin) AS thorpe,\n     AVG(taste) AS maiden,\n     COUNT(taste) AS friar,\n     MAX(berlin) AS clown,\n     SUM(taste) AS deputy\n   GROUP BY\n     smack\n|> SELECT\n     reign,\n     pulpit,\n     switch,\n     thorpe,\n     maiden\n|> AGGREGATE\n     COUNT(maiden) AS circle\n   GROUP BY\n     reign,\n     pulpit\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"switch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    alien,\n    shower,\n    whale,\n    castle,\n    falcon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(castle) AS whore,\n    SUM(falcon) AS mount,\n    COUNT(falcon) AS condom,\n    MIN(alien) AS museum,\n    MIN(whale) AS hurst,\n    MAX(falcon) AS other,\n    MIN(shower) AS extent,\n    AVG(whale) AS diary,\n    MIN(castle) AS runway\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    whore,\n    mount,\n    driver,\n    condom\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    whore,\n    mount\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(mount) AS agent,\n    AVG(mount) AS daddy\n  FROM cte_3\n  GROUP BY\n    whore\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     alien,\n     shower,\n     whale,\n     castle,\n     falcon\n|> AGGREGATE\n     SUM(castle) AS whore,\n     SUM(falcon) AS mount,\n     COUNT(falcon) AS condom,\n     MIN(alien) AS museum,\n     MIN(whale) AS hurst,\n     MAX(falcon) AS other,\n     MIN(shower) AS extent,\n     AVG(whale) AS diary,\n     MIN(castle) AS runway\n|> SELECT\n     whore,\n     mount,\n     driver,\n     condom\n|> SELECT\n     whore,\n     mount\n|> AGGREGATE\n     SUM(mount) AS agent,\n     AVG(mount) AS daddy\n   GROUP BY\n     whore\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"driver",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:28,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    holder,\n    cavity,\n    cruise,\n    fence,\n    rubber\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    holder,\n    cavity,\n    cruise,\n    fence,\n    rubber\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(fence) AS mantle,\n    MAX(cavity) AS racism,\n    COUNT(cruise) AS flush,\n    COUNT(cavity) AS trader,\n    MIN(holder) AS crest,\n    SUM(cavity) AS pastry,\n    MAX(fence) AS tonne\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    mantle,\n    gallon,\n    racism,\n    flush,\n    trader,\n    crest\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(crest) AS branch\n  FROM cte_3\n  GROUP BY\n    mantle,\n    racism\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     holder,\n     cavity,\n     cruise,\n     fence,\n     rubber\n|> SELECT\n     holder,\n     cavity,\n     cruise,\n     fence,\n     rubber\n|> AGGREGATE\n     MIN(fence) AS mantle,\n     MAX(cavity) AS racism,\n     COUNT(cruise) AS flush,\n     COUNT(cavity) AS trader,\n     MIN(holder) AS crest,\n     SUM(cavity) AS pastry,\n     MAX(fence) AS tonne\n|> SELECT\n     mantle,\n     gallon,\n     racism,\n     flush,\n     trader,\n     crest\n|> AGGREGATE\n     SUM(crest) AS branch\n   GROUP BY\n     mantle,\n     racism\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"gallon",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    nature,\n    hybrid,\n    ditch,\n    granny,\n    guide\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(hybrid) AS guilt,\n    SUM(guide) AS border,\n    AVG(ditch) AS change,\n    MIN(ditch) AS route\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(route) AS barrel,\n    MIN(route) AS fauna,\n    AVG(change) AS mousse,\n    COUNT(change) AS power\n  FROM cte_1\n  GROUP BY\n    guilt,\n    border\n),\ncte_3 AS (\n  SELECT\n    barrel,\n    fauna,\n    grouse\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    barrel,\n    SUM(fauna) AS patrol,\n    AVG(fauna) AS radius,\n    MAX(fauna) AS smash\n  FROM cte_3\n  GROUP BY\n    barrel\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     nature,\n     hybrid,\n     ditch,\n     granny,\n     guide\n|> AGGREGATE\n     SUM(hybrid) AS guilt,\n     SUM(guide) AS border,\n     AVG(ditch) AS change,\n     MIN(ditch) AS route\n|> AGGREGATE\n     AVG(route) AS barrel,\n     MIN(route) AS fauna,\n     AVG(change) AS mousse,\n     COUNT(change) AS power\n   GROUP BY\n     guilt,\n     border\n|> SELECT\n     barrel,\n     fauna,\n     grouse\n|> AGGREGATE\n     SUM(fauna) AS patrol,\n     AVG(fauna) AS radius,\n     MAX(fauna) AS smash\n   GROUP BY\n     barrel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"grouse",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    falcon,\n    stump,\n    arson,\n    blanc,\n    blank\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(arson) AS angel,\n    MIN(blank) AS corpse,\n    COUNT(blank) AS hazel\n  FROM cte_0\n  GROUP BY\n    falcon,\n    stump\n),\ncte_2 AS (\n  SELECT\n    MIN(corpse) AS injury,\n    MAX(corpse) AS domain,\n    AVG(angel) AS ernest,\n    SUM(angel) AS corner,\n    MAX(angel) AS wagon,\n    COUNT(hazel) AS tandem\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    ratio,\n    injury,\n    domain\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(domain) AS simon,\n    COUNT(injury) AS pasta,\n    MIN(injury) AS smack\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     falcon,\n     stump,\n     arson,\n     blanc,\n     blank\n|> AGGREGATE\n     SUM(arson) AS angel,\n     MIN(blank) AS corpse,\n     COUNT(blank) AS hazel\n   GROUP BY\n     falcon,\n     stump\n|> AGGREGATE\n     MIN(corpse) AS injury,\n     MAX(corpse) AS domain,\n     AVG(angel) AS ernest,\n     SUM(angel) AS corner,\n     MAX(angel) AS wagon,\n     COUNT(hazel) AS tandem\n|> SELECT\n     ratio,\n     injury,\n     domain\n|> AGGREGATE\n     SUM(domain) AS simon,\n     COUNT(injury) AS pasta,\n     MIN(injury) AS smack\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"ratio",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    craig,\n    thrust,\n    twist,\n    hound,\n    porter\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(thrust) AS slice,\n    COUNT(hound) AS retina,\n    SUM(thrust) AS henley,\n    MAX(hound) AS ditch,\n    MAX(craig) AS embryo,\n    MAX(porter) AS dairy,\n    AVG(craig) AS parity,\n    COUNT(porter) AS runner,\n    AVG(porter) AS aggie,\n    COUNT(thrust) AS turkey\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    pledge,\n    slice,\n    retina,\n    henley,\n    ditch\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    slice,\n    retina\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(slice) AS feeder\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     craig,\n     thrust,\n     twist,\n     hound,\n     porter\n|> AGGREGATE\n     MIN(thrust) AS slice,\n     COUNT(hound) AS retina,\n     SUM(thrust) AS henley,\n     MAX(hound) AS ditch,\n     MAX(craig) AS embryo,\n     MAX(porter) AS dairy,\n     AVG(craig) AS parity,\n     COUNT(porter) AS runner,\n     AVG(porter) AS aggie,\n     COUNT(thrust) AS turkey\n|> SELECT\n     pledge,\n     slice,\n     retina,\n     henley,\n     ditch\n|> SELECT\n     slice,\n     retina\n|> AGGREGATE\n     AVG(slice) AS feeder\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"pledge",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:27,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    burke,\n    shaft,\n    drama,\n    runner,\n    fiasco\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(fiasco) AS martyr,\n    SUM(runner) AS ozone,\n    COUNT(fiasco) AS madge,\n    MIN(fiasco) AS volume,\n    MAX(runner) AS genre,\n    COUNT(runner) AS insert\n  FROM cte_0\n  GROUP BY\n    burke,\n    shaft,\n    drama\n),\ncte_2 AS (\n  SELECT\n    martyr,\n    ozone,\n    madge,\n    volume,\n    genre,\n    legion,\n    insert\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    martyr,\n    ozone,\n    madge,\n    volume\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    martyr,\n    ozone,\n    madge\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     burke,\n     shaft,\n     drama,\n     runner,\n     fiasco\n|> AGGREGATE\n     AVG(fiasco) AS martyr,\n     SUM(runner) AS ozone,\n     COUNT(fiasco) AS madge,\n     MIN(fiasco) AS volume,\n     MAX(runner) AS genre,\n     COUNT(runner) AS insert\n   GROUP BY\n     burke,\n     shaft,\n     drama\n|> SELECT\n     martyr,\n     ozone,\n     madge,\n     volume,\n     genre,\n     legion,\n     insert\n|> SELECT\n     martyr,\n     ozone,\n     madge,\n     volume\n|> SELECT\n     martyr,\n     ozone,\n     madge\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"legion",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    wisdom,\n    mercy,\n    score,\n    helper,\n    thigh\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(helper) AS venue,\n    AVG(helper) AS fridge,\n    COUNT(helper) AS barrow,\n    MIN(thigh) AS knack,\n    COUNT(thigh) AS devon\n  FROM cte_0\n  GROUP BY\n    wisdom,\n    mercy,\n    score\n),\ncte_2 AS (\n  SELECT\n    venue,\n    fridge,\n    barrow,\n    knack,\n    devon\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    venue,\n    fridge,\n    level,\n    barrow,\n    knack,\n    devon\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    venue,\n    COUNT(barrow) AS system\n  FROM cte_3\n  GROUP BY\n    venue,\n    fridge\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     wisdom,\n     mercy,\n     score,\n     helper,\n     thigh\n|> AGGREGATE\n     MAX(helper) AS venue,\n     AVG(helper) AS fridge,\n     COUNT(helper) AS barrow,\n     MIN(thigh) AS knack,\n     COUNT(thigh) AS devon\n   GROUP BY\n     wisdom,\n     mercy,\n     score\n|> SELECT\n     venue,\n     fridge,\n     barrow,\n     knack,\n     devon\n|> SELECT\n     venue,\n     fridge,\n     level,\n     barrow,\n     knack,\n     devon\n|> AGGREGATE\n     COUNT(barrow) AS system\n   GROUP BY\n     venue,\n     fridge\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"level",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    poppy,\n    picket,\n    blood,\n    gypsy,\n    claire\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    poppy,\n    picket,\n    blood,\n    gypsy,\n    claire\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(picket) AS trace,\n    SUM(gypsy) AS turbo,\n    MIN(gypsy) AS match,\n    MIN(blood) AS peace,\n    COUNT(claire) AS craven,\n    MAX(poppy) AS groan,\n    AVG(blood) AS engine\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    trace,\n    cheek,\n    turbo,\n    match,\n    peace,\n    craven,\n    groan,\n    engine\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(engine) AS bunch\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     poppy,\n     picket,\n     blood,\n     gypsy,\n     claire\n|> SELECT\n     poppy,\n     picket,\n     blood,\n     gypsy,\n     claire\n|> AGGREGATE\n     MIN(picket) AS trace,\n     SUM(gypsy) AS turbo,\n     MIN(gypsy) AS match,\n     MIN(blood) AS peace,\n     COUNT(claire) AS craven,\n     MAX(poppy) AS groan,\n     AVG(blood) AS engine\n|> SELECT\n     trace,\n     cheek,\n     turbo,\n     match,\n     peace,\n     craven,\n     groan,\n     engine\n|> AGGREGATE\n     MIN(engine) AS bunch\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"cheek",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    clive,\n    public,\n    theft,\n    henry,\n    sigma\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(sigma) AS cloak,\n    MAX(sigma) AS outlet,\n    COUNT(sigma) AS guide,\n    MAX(henry) AS brick,\n    MIN(sigma) AS glance,\n    COUNT(henry) AS jessie\n  FROM cte_0\n  GROUP BY\n    clive,\n    public,\n    theft\n),\ncte_2 AS (\n  SELECT\n    cloak,\n    outlet,\n    guide,\n    brick,\n    glance,\n    intake\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    cloak,\n    outlet,\n    guide,\n    brick\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(guide) AS chain\n  FROM cte_3\n  GROUP BY\n    cloak,\n    outlet\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     clive,\n     public,\n     theft,\n     henry,\n     sigma\n|> AGGREGATE\n     AVG(sigma) AS cloak,\n     MAX(sigma) AS outlet,\n     COUNT(sigma) AS guide,\n     MAX(henry) AS brick,\n     MIN(sigma) AS glance,\n     COUNT(henry) AS jessie\n   GROUP BY\n     clive,\n     public,\n     theft\n|> SELECT\n     cloak,\n     outlet,\n     guide,\n     brick,\n     glance,\n     intake\n|> SELECT\n     cloak,\n     outlet,\n     guide,\n     brick\n|> AGGREGATE\n     MAX(guide) AS chain\n   GROUP BY\n     cloak,\n     outlet\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"intake",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sudden,\n    duchy,\n    tongue,\n    fossil,\n    burke\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    sudden,\n    duchy,\n    tongue,\n    fossil,\n    burke\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(burke) AS holder,\n    MAX(burke) AS share,\n    COUNT(burke) AS travel,\n    MAX(fossil) AS dwarf,\n    MIN(fossil) AS tomato,\n    MIN(tongue) AS rubble\n  FROM cte_1\n  GROUP BY\n    sudden,\n    duchy\n),\ncte_3 AS (\n  SELECT\n    holder,\n    tender,\n    share,\n    travel,\n    dwarf,\n    tomato\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    holder,\n    share,\n    travel,\n    dwarf\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sudden,\n     duchy,\n     tongue,\n     fossil,\n     burke\n|> SELECT\n     sudden,\n     duchy,\n     tongue,\n     fossil,\n     burke\n|> AGGREGATE\n     AVG(burke) AS holder,\n     MAX(burke) AS share,\n     COUNT(burke) AS travel,\n     MAX(fossil) AS dwarf,\n     MIN(fossil) AS tomato,\n     MIN(tongue) AS rubble\n   GROUP BY\n     sudden,\n     duchy\n|> SELECT\n     holder,\n     tender,\n     share,\n     travel,\n     dwarf,\n     tomato\n|> SELECT\n     holder,\n     share,\n     travel,\n     dwarf\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"tender",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    violet,\n    sonny,\n    barman,\n    occult,\n    apron\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(occult) AS comma,\n    MIN(apron) AS china,\n    MIN(occult) AS oxygen,\n    AVG(occult) AS salary\n  FROM cte_0\n  GROUP BY\n    violet,\n    sonny,\n    barman\n),\ncte_2 AS (\n  SELECT\n    MAX(salary) AS centre,\n    AVG(salary) AS archie,\n    COUNT(salary) AS aspect\n  FROM cte_1\n  GROUP BY\n    comma,\n    china,\n    oxygen\n),\ncte_3 AS (\n  SELECT\n    sugar,\n    centre,\n    archie,\n    aspect\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(aspect) AS jetty,\n    COUNT(aspect) AS frank\n  FROM cte_3\n  GROUP BY\n    centre\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     violet,\n     sonny,\n     barman,\n     occult,\n     apron\n|> AGGREGATE\n     SUM(occult) AS comma,\n     MIN(apron) AS china,\n     MIN(occult) AS oxygen,\n     AVG(occult) AS salary\n   GROUP BY\n     violet,\n     sonny,\n     barman\n|> AGGREGATE\n     MAX(salary) AS centre,\n     AVG(salary) AS archie,\n     COUNT(salary) AS aspect\n   GROUP BY\n     comma,\n     china,\n     oxygen\n|> SELECT\n     sugar,\n     centre,\n     archie,\n     aspect\n|> AGGREGATE\n     SUM(aspect) AS jetty,\n     COUNT(aspect) AS frank\n   GROUP BY\n     centre\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"SELECT_UNKNOWN",totalInformationDifference:0,unknownName:"sugar",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    brook,\n    dexter,\n    colon,\n    uplift,\n    folly\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    brook,\n    dexter,\n    colon,\n    MIN(uplift) AS swing,\n    MAX(uplift) AS priory\n  FROM cte_0\n  GROUP BY\n    brook,\n    dexter,\n    colon\n),\ncte_2 AS (\n  SELECT\n    brook,\n    SUM(colon) AS gunman,\n    MAX(swing) AS irony\n  FROM cte_1\n  GROUP BY\n    brook,\n    dexter\n),\ncte_3 AS (\n  SELECT\n    brook,\n    gunman,\n    irony,\n    minor\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    brook,\n    SUM(irony) AS flock,\n    MAX(gunman) AS aisle,\n    AVG(gunman) AS female\n  FROM cte_3\n  GROUP BY\n    brook\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     brook,\n     dexter,\n     colon,\n     uplift,\n     folly\n|> AGGREGATE\n     MIN(uplift) AS swing,\n     MAX(uplift) AS priory\n   GROUP BY\n     brook,\n     dexter,\n     colon\n|> AGGREGATE\n     SUM(colon) AS gunman,\n     MAX(swing) AS irony\n   GROUP BY\n     brook,\n     dexter\n|> SELECT\n     brook,\n     gunman,\n     irony,\n     minor\n|> AGGREGATE\n     SUM(irony) AS flock,\n     MAX(gunman) AS aisle,\n     AVG(gunman) AS female\n   GROUP BY\n     brook\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"minor",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    pellet,\n    fiver,\n    duchy,\n    strip,\n    piper\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    pellet,\n    SUM(strip) AS lewis,\n    SUM(duchy) AS breath,\n    AVG(duchy) AS beggar,\n    COUNT(duchy) AS turtle\n  FROM cte_0\n  GROUP BY\n    pellet\n),\ncte_2 AS (\n  SELECT\n    pellet,\n    lewis,\n    breath,\n    MAX(turtle) AS group\n  FROM cte_1\n  GROUP BY\n    pellet,\n    lewis,\n    breath,\n    beggar\n),\ncte_3 AS (\n  SELECT\n    pellet,\n    gloom,\n    lewis,\n    breath,\n    group\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(group) AS currie\n  FROM cte_3\n  GROUP BY\n    pellet,\n    lewis,\n    breath\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     pellet,\n     fiver,\n     duchy,\n     strip,\n     piper\n|> AGGREGATE\n     SUM(strip) AS lewis,\n     SUM(duchy) AS breath,\n     AVG(duchy) AS beggar,\n     COUNT(duchy) AS turtle\n   GROUP BY\n     pellet\n|> AGGREGATE\n     MAX(turtle) AS group\n   GROUP BY\n     pellet,\n     lewis,\n     breath,\n     beggar\n|> SELECT\n     pellet,\n     gloom,\n     lewis,\n     breath,\n     group\n|> AGGREGATE\n     MAX(group) AS currie\n   GROUP BY\n     pellet,\n     lewis,\n     breath\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"gloom",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    threat,\n    reign,\n    bugger,\n    remedy,\n    mother\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    threat,\n    reign,\n    bugger,\n    remedy,\n    AVG(mother) AS picket,\n    COUNT(mother) AS heaven,\n    MAX(mother) AS crater\n  FROM cte_0\n  GROUP BY\n    threat,\n    reign,\n    bugger,\n    remedy\n),\ncte_2 AS (\n  SELECT\n    threat,\n    reign,\n    bugger,\n    remedy,\n    picket,\n    heaven,\n    hitch,\n    crater\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(bugger) AS screen,\n    COUNT(reign) AS tongue\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(tongue) AS satan\n  FROM cte_3\n  GROUP BY\n    screen\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     threat,\n     reign,\n     bugger,\n     remedy,\n     mother\n|> AGGREGATE\n     AVG(mother) AS picket,\n     COUNT(mother) AS heaven,\n     MAX(mother) AS crater\n   GROUP BY\n     threat,\n     reign,\n     bugger,\n     remedy\n|> SELECT\n     threat,\n     reign,\n     bugger,\n     remedy,\n     picket,\n     heaven,\n     hitch,\n     crater\n|> AGGREGATE\n     AVG(bugger) AS screen,\n     COUNT(reign) AS tongue\n|> AGGREGATE\n     COUNT(tongue) AS satan\n   GROUP BY\n     screen\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"hitch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    collor,\n    puzzle,\n    murphy,\n    modem,\n    slice\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    collor,\n    puzzle,\n    COUNT(slice) AS warner,\n    SUM(slice) AS paste,\n    MAX(slice) AS hector\n  FROM cte_0\n  GROUP BY\n    collor,\n    puzzle,\n    murphy,\n    modem\n),\ncte_2 AS (\n  SELECT\n    collor,\n    puzzle,\n    AVG(hector) AS hurst,\n    SUM(warner) AS jersey\n  FROM cte_1\n  GROUP BY\n    collor,\n    puzzle\n),\ncte_3 AS (\n  SELECT\n    single,\n    collor,\n    puzzle,\n    hurst,\n    jersey\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(hurst) AS rally\n  FROM cte_3\n  GROUP BY\n    collor,\n    puzzle\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     collor,\n     puzzle,\n     murphy,\n     modem,\n     slice\n|> AGGREGATE\n     COUNT(slice) AS warner,\n     SUM(slice) AS paste,\n     MAX(slice) AS hector\n   GROUP BY\n     collor,\n     puzzle,\n     murphy,\n     modem\n|> AGGREGATE\n     AVG(hector) AS hurst,\n     SUM(warner) AS jersey\n   GROUP BY\n     collor,\n     puzzle\n|> SELECT\n     single,\n     collor,\n     puzzle,\n     hurst,\n     jersey\n|> AGGREGATE\n     MAX(hurst) AS rally\n   GROUP BY\n     collor,\n     puzzle\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"single",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    answer,\n    strat,\n    pillar,\n    barry,\n    streak\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    answer,\n    MIN(streak) AS fever,\n    MAX(streak) AS master,\n    SUM(streak) AS spell\n  FROM cte_0\n  GROUP BY\n    answer,\n    strat,\n    pillar,\n    barry\n),\ncte_2 AS (\n  SELECT\n    answer,\n    fever,\n    master,\n    SUM(spell) AS round\n  FROM cte_1\n  GROUP BY\n    answer,\n    fever,\n    master\n),\ncte_3 AS (\n  SELECT\n    answer,\n    corpse,\n    fever,\n    master,\n    round\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    answer,\n    AVG(round) AS policy\n  FROM cte_3\n  GROUP BY\n    answer,\n    fever\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     answer,\n     strat,\n     pillar,\n     barry,\n     streak\n|> AGGREGATE\n     MIN(streak) AS fever,\n     MAX(streak) AS master,\n     SUM(streak) AS spell\n   GROUP BY\n     answer,\n     strat,\n     pillar,\n     barry\n|> AGGREGATE\n     SUM(spell) AS round\n   GROUP BY\n     answer,\n     fever,\n     master\n|> SELECT\n     answer,\n     corpse,\n     fever,\n     master,\n     round\n|> AGGREGATE\n     AVG(round) AS policy\n   GROUP BY\n     answer,\n     fever\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"corpse",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    chart,\n    mosque,\n    thrush,\n    tender,\n    stump\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    chart,\n    AVG(mosque) AS crash,\n    COUNT(mosque) AS symbol,\n    AVG(stump) AS double,\n    COUNT(tender) AS record\n  FROM cte_0\n  GROUP BY\n    chart\n),\ncte_2 AS (\n  SELECT\n    chart,\n    crash,\n    symbol,\n    AVG(record) AS mentor\n  FROM cte_1\n  GROUP BY\n    chart,\n    crash,\n    symbol,\n    double\n),\ncte_3 AS (\n  SELECT\n    chart,\n    sewer,\n    crash,\n    symbol,\n    mentor\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    chart,\n    MAX(symbol) AS weight\n  FROM cte_3\n  GROUP BY\n    chart,\n    crash\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     chart,\n     mosque,\n     thrush,\n     tender,\n     stump\n|> AGGREGATE\n     AVG(mosque) AS crash,\n     COUNT(mosque) AS symbol,\n     AVG(stump) AS double,\n     COUNT(tender) AS record\n   GROUP BY\n     chart\n|> AGGREGATE\n     AVG(record) AS mentor\n   GROUP BY\n     chart,\n     crash,\n     symbol,\n     double\n|> SELECT\n     chart,\n     sewer,\n     crash,\n     symbol,\n     mentor\n|> AGGREGATE\n     MAX(symbol) AS weight\n   GROUP BY\n     chart,\n     crash\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"sewer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    depot,\n    lease,\n    breeze,\n    bride,\n    stone\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    depot,\n    lease,\n    COUNT(stone) AS strand\n  FROM cte_0\n  GROUP BY\n    depot,\n    lease,\n    breeze\n),\ncte_2 AS (\n  SELECT\n    depot,\n    lease,\n    MAX(strand) AS hardy,\n    SUM(strand) AS replay,\n    COUNT(strand) AS zenith\n  FROM cte_1\n  GROUP BY\n    depot,\n    lease\n),\ncte_3 AS (\n  SELECT\n    depot,\n    lease,\n    hardy,\n    clause\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(depot) AS sunset,\n    MAX(lease) AS liquor,\n    COUNT(lease) AS blind,\n    AVG(depot) AS savage\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     depot,\n     lease,\n     breeze,\n     bride,\n     stone\n|> AGGREGATE\n     COUNT(stone) AS strand\n   GROUP BY\n     depot,\n     lease,\n     breeze\n|> AGGREGATE\n     MAX(strand) AS hardy,\n     SUM(strand) AS replay,\n     COUNT(strand) AS zenith\n   GROUP BY\n     depot,\n     lease\n|> SELECT\n     depot,\n     lease,\n     hardy,\n     clause\n|> AGGREGATE\n     COUNT(depot) AS sunset,\n     MAX(lease) AS liquor,\n     COUNT(lease) AS blind,\n     AVG(depot) AS savage\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"clause",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    abbey,\n    batch,\n    gooch,\n    court,\n    orange\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    abbey,\n    batch,\n    gooch,\n    court,\n    AVG(orange) AS caller,\n    COUNT(orange) AS tongue,\n    MAX(orange) AS saloon,\n    SUM(orange) AS shadow,\n    MIN(orange) AS glider\n  FROM cte_0\n  GROUP BY\n    abbey,\n    batch,\n    gooch,\n    court\n),\ncte_2 AS (\n  SELECT\n    abbey,\n    batch,\n    mirror,\n    gooch,\n    court,\n    caller,\n    tongue\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(court) AS barney\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(barney) AS manual\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     abbey,\n     batch,\n     gooch,\n     court,\n     orange\n|> AGGREGATE\n     AVG(orange) AS caller,\n     COUNT(orange) AS tongue,\n     MAX(orange) AS saloon,\n     SUM(orange) AS shadow,\n     MIN(orange) AS glider\n   GROUP BY\n     abbey,\n     batch,\n     gooch,\n     court\n|> SELECT\n     abbey,\n     batch,\n     mirror,\n     gooch,\n     court,\n     caller,\n     tongue\n|> AGGREGATE\n     AVG(court) AS barney\n|> AGGREGATE\n     MAX(barney) AS manual\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"mirror",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    shift,\n    spoon,\n    botany,\n    affair,\n    pinch\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    shift,\n    SUM(pinch) AS strip,\n    AVG(botany) AS parity,\n    AVG(pinch) AS turbo,\n    SUM(botany) AS merit\n  FROM cte_0\n  GROUP BY\n    shift,\n    spoon\n),\ncte_2 AS (\n  SELECT\n    shift,\n    strip,\n    parity,\n    MIN(merit) AS piazza\n  FROM cte_1\n  GROUP BY\n    shift,\n    strip,\n    parity\n),\ncte_3 AS (\n  SELECT\n    shift,\n    hoover,\n    strip,\n    parity\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    shift,\n    strip,\n    MIN(parity) AS carol\n  FROM cte_3\n  GROUP BY\n    shift,\n    strip\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     shift,\n     spoon,\n     botany,\n     affair,\n     pinch\n|> AGGREGATE\n     SUM(pinch) AS strip,\n     AVG(botany) AS parity,\n     AVG(pinch) AS turbo,\n     SUM(botany) AS merit\n   GROUP BY\n     shift,\n     spoon\n|> AGGREGATE\n     MIN(merit) AS piazza\n   GROUP BY\n     shift,\n     strip,\n     parity\n|> SELECT\n     shift,\n     hoover,\n     strip,\n     parity\n|> AGGREGATE\n     MIN(parity) AS carol\n   GROUP BY\n     shift,\n     strip\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:28,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"hoover",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    saliva,\n    caesar,\n    serial,\n    salon,\n    float\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    saliva,\n    caesar,\n    serial,\n    AVG(float) AS cheer,\n    COUNT(float) AS bread\n  FROM cte_0\n  GROUP BY\n    saliva,\n    caesar,\n    serial,\n    salon\n),\ncte_2 AS (\n  SELECT\n    saliva,\n    COUNT(serial) AS relief,\n    COUNT(caesar) AS horror\n  FROM cte_1\n  GROUP BY\n    saliva\n),\ncte_3 AS (\n  SELECT\n    saliva,\n    relief,\n    horror,\n    ghetto\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(saliva) AS mammal,\n    MIN(relief) AS velvet,\n    MAX(saliva) AS incest,\n    COUNT(horror) AS stump\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     saliva,\n     caesar,\n     serial,\n     salon,\n     float\n|> AGGREGATE\n     AVG(float) AS cheer,\n     COUNT(float) AS bread\n   GROUP BY\n     saliva,\n     caesar,\n     serial,\n     salon\n|> AGGREGATE\n     COUNT(serial) AS relief,\n     COUNT(caesar) AS horror\n   GROUP BY\n     saliva\n|> SELECT\n     saliva,\n     relief,\n     horror,\n     ghetto\n|> AGGREGATE\n     COUNT(saliva) AS mammal,\n     MIN(relief) AS velvet,\n     MAX(saliva) AS incest,\n     COUNT(horror) AS stump\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"ghetto",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    plough,\n    doubt,\n    roche,\n    scale,\n    outfit\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    plough,\n    MAX(scale) AS coffin,\n    SUM(outfit) AS pound,\n    SUM(scale) AS utopia,\n    AVG(outfit) AS dread\n  FROM cte_0\n  GROUP BY\n    plough,\n    doubt,\n    roche\n),\ncte_2 AS (\n  SELECT\n    plough,\n    coffin,\n    pound,\n    MAX(utopia) AS beacon\n  FROM cte_1\n  GROUP BY\n    plough,\n    coffin,\n    pound\n),\ncte_3 AS (\n  SELECT\n    vacuum,\n    plough,\n    coffin\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(plough) AS stream,\n    SUM(coffin) AS reeve,\n    COUNT(coffin) AS gamble\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     plough,\n     doubt,\n     roche,\n     scale,\n     outfit\n|> AGGREGATE\n     MAX(scale) AS coffin,\n     SUM(outfit) AS pound,\n     SUM(scale) AS utopia,\n     AVG(outfit) AS dread\n   GROUP BY\n     plough,\n     doubt,\n     roche\n|> AGGREGATE\n     MAX(utopia) AS beacon\n   GROUP BY\n     plough,\n     coffin,\n     pound\n|> SELECT\n     vacuum,\n     plough,\n     coffin\n|> AGGREGATE\n     MAX(plough) AS stream,\n     SUM(coffin) AS reeve,\n     COUNT(coffin) AS gamble\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"vacuum",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    limit,\n    ghost,\n    christ,\n    tariff,\n    tavern\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    limit,\n    ghost,\n    AVG(tavern) AS coffin,\n    MAX(tavern) AS valley\n  FROM cte_0\n  GROUP BY\n    limit,\n    ghost,\n    christ,\n    tariff\n),\ncte_2 AS (\n  SELECT\n    limit,\n    ghost,\n    AVG(valley) AS swift,\n    COUNT(coffin) AS grace\n  FROM cte_1\n  GROUP BY\n    limit,\n    ghost\n),\ncte_3 AS (\n  SELECT\n    limit,\n    ghost,\n    spread,\n    swift,\n    grace\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(grace) AS dinner,\n    COUNT(grace) AS ounce\n  FROM cte_3\n  GROUP BY\n    limit,\n    ghost\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     limit,\n     ghost,\n     christ,\n     tariff,\n     tavern\n|> AGGREGATE\n     AVG(tavern) AS coffin,\n     MAX(tavern) AS valley\n   GROUP BY\n     limit,\n     ghost,\n     christ,\n     tariff\n|> AGGREGATE\n     AVG(valley) AS swift,\n     COUNT(coffin) AS grace\n   GROUP BY\n     limit,\n     ghost\n|> SELECT\n     limit,\n     ghost,\n     spread,\n     swift,\n     grace\n|> AGGREGATE\n     SUM(grace) AS dinner,\n     COUNT(grace) AS ounce\n   GROUP BY\n     limit,\n     ghost\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"spread",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    organ,\n    brute,\n    tailor,\n    future,\n    remedy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    organ,\n    brute,\n    tailor,\n    future,\n    AVG(remedy) AS gable,\n    MAX(remedy) AS notion,\n    COUNT(remedy) AS mandy\n  FROM cte_0\n  GROUP BY\n    organ,\n    brute,\n    tailor,\n    future\n),\ncte_2 AS (\n  SELECT\n    MAX(brute) AS beast,\n    MIN(mandy) AS franco,\n    MAX(notion) AS sewer\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    strike,\n    beast,\n    franco\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    beast,\n    AVG(franco) AS lesion,\n    MAX(franco) AS breast\n  FROM cte_3\n  GROUP BY\n    beast\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     organ,\n     brute,\n     tailor,\n     future,\n     remedy\n|> AGGREGATE\n     AVG(remedy) AS gable,\n     MAX(remedy) AS notion,\n     COUNT(remedy) AS mandy\n   GROUP BY\n     organ,\n     brute,\n     tailor,\n     future\n|> AGGREGATE\n     MAX(brute) AS beast,\n     MIN(mandy) AS franco,\n     MAX(notion) AS sewer\n|> SELECT\n     strike,\n     beast,\n     franco\n|> AGGREGATE\n     AVG(franco) AS lesion,\n     MAX(franco) AS breast\n   GROUP BY\n     beast\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"strike",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    misuse,\n    drama,\n    drawer,\n    shield,\n    skirt\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    misuse,\n    drama,\n    drawer,\n    shield,\n    COUNT(skirt) AS canal,\n    MIN(skirt) AS bream,\n    SUM(skirt) AS henley,\n    MAX(skirt) AS career,\n    AVG(skirt) AS yogurt\n  FROM cte_0\n  GROUP BY\n    misuse,\n    drama,\n    drawer,\n    shield\n),\ncte_2 AS (\n  SELECT\n    misuse,\n    drama,\n    wharf,\n    drawer,\n    shield\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    misuse,\n    SUM(shield) AS screen\n  FROM cte_2\n  GROUP BY\n    misuse\n),\ncte_4 AS (\n  SELECT\n    misuse,\n    screen\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     misuse,\n     drama,\n     drawer,\n     shield,\n     skirt\n|> AGGREGATE\n     COUNT(skirt) AS canal,\n     MIN(skirt) AS bream,\n     SUM(skirt) AS henley,\n     MAX(skirt) AS career,\n     AVG(skirt) AS yogurt\n   GROUP BY\n     misuse,\n     drama,\n     drawer,\n     shield\n|> SELECT\n     misuse,\n     drama,\n     wharf,\n     drawer,\n     shield\n|> AGGREGATE\n     SUM(shield) AS screen\n   GROUP BY\n     misuse\n|> SELECT\n     misuse,\n     screen\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"wharf",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    blend,\n    tenant,\n    toast,\n    manure,\n    linen\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    blend,\n    tenant,\n    MAX(linen) AS maggie\n  FROM cte_0\n  GROUP BY\n    blend,\n    tenant,\n    toast\n),\ncte_2 AS (\n  SELECT\n    blend,\n    tenant,\n    SUM(maggie) AS shine,\n    AVG(maggie) AS fodder,\n    MAX(maggie) AS earth,\n    MIN(maggie) AS theft\n  FROM cte_1\n  GROUP BY\n    blend,\n    tenant\n),\ncte_3 AS (\n  SELECT\n    blend,\n    graham,\n    tenant,\n    shine,\n    fodder,\n    earth,\n    theft\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(shine) AS nephew\n  FROM cte_3\n  GROUP BY\n    blend\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     blend,\n     tenant,\n     toast,\n     manure,\n     linen\n|> AGGREGATE\n     MAX(linen) AS maggie\n   GROUP BY\n     blend,\n     tenant,\n     toast\n|> AGGREGATE\n     SUM(maggie) AS shine,\n     AVG(maggie) AS fodder,\n     MAX(maggie) AS earth,\n     MIN(maggie) AS theft\n   GROUP BY\n     blend,\n     tenant\n|> SELECT\n     blend,\n     graham,\n     tenant,\n     shine,\n     fodder,\n     earth,\n     theft\n|> AGGREGATE\n     MAX(shine) AS nephew\n   GROUP BY\n     blend\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"graham",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    metal,\n    queue,\n    stall,\n    spray,\n    golfer\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    metal,\n    queue,\n    stall,\n    spray,\n    SUM(golfer) AS module,\n    AVG(golfer) AS suite,\n    MAX(golfer) AS zenith\n  FROM cte_0\n  GROUP BY\n    metal,\n    queue,\n    stall,\n    spray\n),\ncte_2 AS (\n  SELECT\n    metal,\n    queue,\n    stall,\n    spray,\n    module,\n    suite,\n    reach,\n    zenith\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    metal,\n    queue,\n    stall\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(stall) AS server,\n    AVG(stall) AS savage\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     metal,\n     queue,\n     stall,\n     spray,\n     golfer\n|> AGGREGATE\n     SUM(golfer) AS module,\n     AVG(golfer) AS suite,\n     MAX(golfer) AS zenith\n   GROUP BY\n     metal,\n     queue,\n     stall,\n     spray\n|> SELECT\n     metal,\n     queue,\n     stall,\n     spray,\n     module,\n     suite,\n     reach,\n     zenith\n|> SELECT\n     metal,\n     queue,\n     stall\n|> AGGREGATE\n     SUM(stall) AS server,\n     AVG(stall) AS savage\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"reach",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    merit,\n    method,\n    dancer,\n    tablet,\n    hound\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    merit,\n    method,\n    dancer,\n    tablet,\n    SUM(hound) AS utopia,\n    MIN(hound) AS pocket,\n    MAX(hound) AS strife,\n    COUNT(hound) AS thorn\n  FROM cte_0\n  GROUP BY\n    merit,\n    method,\n    dancer,\n    tablet\n),\ncte_2 AS (\n  SELECT\n    AVG(tablet) AS norman,\n    MAX(tablet) AS belle\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    stile,\n    norman,\n    belle\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    norman,\n    MIN(belle) AS pencil,\n    SUM(belle) AS glint\n  FROM cte_3\n  GROUP BY\n    norman\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     merit,\n     method,\n     dancer,\n     tablet,\n     hound\n|> AGGREGATE\n     SUM(hound) AS utopia,\n     MIN(hound) AS pocket,\n     MAX(hound) AS strife,\n     COUNT(hound) AS thorn\n   GROUP BY\n     merit,\n     method,\n     dancer,\n     tablet\n|> AGGREGATE\n     AVG(tablet) AS norman,\n     MAX(tablet) AS belle\n|> SELECT\n     stile,\n     norman,\n     belle\n|> AGGREGATE\n     MIN(belle) AS pencil,\n     SUM(belle) AS glint\n   GROUP BY\n     norman\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"stile",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    thread,\n    comedy,\n    coach,\n    trick,\n    stella\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    thread,\n    comedy,\n    coach,\n    COUNT(stella) AS shelf,\n    MIN(stella) AS index,\n    SUM(stella) AS gorge\n  FROM cte_0\n  GROUP BY\n    thread,\n    comedy,\n    coach,\n    trick\n),\ncte_2 AS (\n  SELECT\n    thread,\n    SUM(gorge) AS flush,\n    AVG(gorge) AS spine\n  FROM cte_1\n  GROUP BY\n    thread,\n    comedy\n),\ncte_3 AS (\n  SELECT\n    whisky,\n    thread,\n    flush\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    thread,\n    MIN(flush) AS vigour,\n    AVG(flush) AS doubt\n  FROM cte_3\n  GROUP BY\n    thread\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     thread,\n     comedy,\n     coach,\n     trick,\n     stella\n|> AGGREGATE\n     COUNT(stella) AS shelf,\n     MIN(stella) AS index,\n     SUM(stella) AS gorge\n   GROUP BY\n     thread,\n     comedy,\n     coach,\n     trick\n|> AGGREGATE\n     SUM(gorge) AS flush,\n     AVG(gorge) AS spine\n   GROUP BY\n     thread,\n     comedy\n|> SELECT\n     whisky,\n     thread,\n     flush\n|> AGGREGATE\n     MIN(flush) AS vigour,\n     AVG(flush) AS doubt\n   GROUP BY\n     thread\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"whisky",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    curfew,\n    violet,\n    trader,\n    lobby,\n    hockey\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    curfew,\n    violet,\n    MAX(hockey) AS skill\n  FROM cte_0\n  GROUP BY\n    curfew,\n    violet,\n    trader\n),\ncte_2 AS (\n  SELECT\n    curfew,\n    violet,\n    MAX(skill) AS parker,\n    AVG(skill) AS muscle,\n    SUM(skill) AS agency,\n    COUNT(skill) AS change\n  FROM cte_1\n  GROUP BY\n    curfew,\n    violet\n),\ncte_3 AS (\n  SELECT\n    curfew,\n    gossip,\n    violet,\n    parker,\n    muscle,\n    agency,\n    change\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(change) AS bother\n  FROM cte_3\n  GROUP BY\n    curfew\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     curfew,\n     violet,\n     trader,\n     lobby,\n     hockey\n|> AGGREGATE\n     MAX(hockey) AS skill\n   GROUP BY\n     curfew,\n     violet,\n     trader\n|> AGGREGATE\n     MAX(skill) AS parker,\n     AVG(skill) AS muscle,\n     SUM(skill) AS agency,\n     COUNT(skill) AS change\n   GROUP BY\n     curfew,\n     violet\n|> SELECT\n     curfew,\n     gossip,\n     violet,\n     parker,\n     muscle,\n     agency,\n     change\n|> AGGREGATE\n     MAX(change) AS bother\n   GROUP BY\n     curfew\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"gossip",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    outlay,\n    cuckoo,\n    wizard,\n    price,\n    stern\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    outlay,\n    cuckoo,\n    wizard,\n    AVG(stern) AS viking\n  FROM cte_0\n  GROUP BY\n    outlay,\n    cuckoo,\n    wizard,\n    price\n),\ncte_2 AS (\n  SELECT\n    outlay,\n    SUM(viking) AS rifle,\n    AVG(viking) AS prior\n  FROM cte_1\n  GROUP BY\n    outlay,\n    cuckoo,\n    wizard\n),\ncte_3 AS (\n  SELECT\n    outlay,\n    rifle,\n    prior,\n    sherry\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    outlay,\n    MIN(prior) AS toxin,\n    SUM(prior) AS attack,\n    AVG(prior) AS venus\n  FROM cte_3\n  GROUP BY\n    outlay\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     outlay,\n     cuckoo,\n     wizard,\n     price,\n     stern\n|> AGGREGATE\n     AVG(stern) AS viking\n   GROUP BY\n     outlay,\n     cuckoo,\n     wizard,\n     price\n|> AGGREGATE\n     SUM(viking) AS rifle,\n     AVG(viking) AS prior\n   GROUP BY\n     outlay,\n     cuckoo,\n     wizard\n|> SELECT\n     outlay,\n     rifle,\n     prior,\n     sherry\n|> AGGREGATE\n     MIN(prior) AS toxin,\n     SUM(prior) AS attack,\n     AVG(prior) AS venus\n   GROUP BY\n     outlay\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"sherry",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    cowboy,\n    galley,\n    salary,\n    sharp,\n    spruce\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    cowboy,\n    galley,\n    AVG(spruce) AS domain\n  FROM cte_0\n  GROUP BY\n    cowboy,\n    galley,\n    salary\n),\ncte_2 AS (\n  SELECT\n    cowboy,\n    galley,\n    MAX(domain) AS psalm,\n    COUNT(domain) AS cadet,\n    SUM(domain) AS pouch,\n    AVG(domain) AS cheek\n  FROM cte_1\n  GROUP BY\n    cowboy,\n    galley\n),\ncte_3 AS (\n  SELECT\n    cowboy,\n    picnic,\n    galley,\n    psalm,\n    cadet,\n    pouch\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(cowboy) AS tavern,\n    MIN(cadet) AS escort\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     cowboy,\n     galley,\n     salary,\n     sharp,\n     spruce\n|> AGGREGATE\n     AVG(spruce) AS domain\n   GROUP BY\n     cowboy,\n     galley,\n     salary\n|> AGGREGATE\n     MAX(domain) AS psalm,\n     COUNT(domain) AS cadet,\n     SUM(domain) AS pouch,\n     AVG(domain) AS cheek\n   GROUP BY\n     cowboy,\n     galley\n|> SELECT\n     cowboy,\n     picnic,\n     galley,\n     psalm,\n     cadet,\n     pouch\n|> AGGREGATE\n     MAX(cowboy) AS tavern,\n     MIN(cadet) AS escort\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"picnic",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rogue,\n    spirit,\n    search,\n    grade,\n    opium\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    rogue,\n    spirit,\n    search,\n    AVG(opium) AS scarf,\n    MAX(opium) AS berry\n  FROM cte_0\n  GROUP BY\n    rogue,\n    spirit,\n    search,\n    grade\n),\ncte_2 AS (\n  SELECT\n    rogue,\n    AVG(spirit) AS settee,\n    COUNT(scarf) AS snake\n  FROM cte_1\n  GROUP BY\n    rogue\n),\ncte_3 AS (\n  SELECT\n    rogue,\n    settee,\n    snake,\n    marble\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(snake) AS heath,\n    AVG(settee) AS ritual,\n    MIN(rogue) AS prize,\n    AVG(snake) AS pitch\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rogue,\n     spirit,\n     search,\n     grade,\n     opium\n|> AGGREGATE\n     AVG(opium) AS scarf,\n     MAX(opium) AS berry\n   GROUP BY\n     rogue,\n     spirit,\n     search,\n     grade\n|> AGGREGATE\n     AVG(spirit) AS settee,\n     COUNT(scarf) AS snake\n   GROUP BY\n     rogue\n|> SELECT\n     rogue,\n     settee,\n     snake,\n     marble\n|> AGGREGATE\n     SUM(snake) AS heath,\n     AVG(settee) AS ritual,\n     MIN(rogue) AS prize,\n     AVG(snake) AS pitch\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"marble",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    entity,\n    animal,\n    defeat,\n    letter,\n    maggie\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    entity,\n    animal,\n    COUNT(maggie) AS comer,\n    AVG(letter) AS advent,\n    AVG(defeat) AS gospel,\n    SUM(letter) AS spire\n  FROM cte_0\n  GROUP BY\n    entity,\n    animal\n),\ncte_2 AS (\n  SELECT\n    entity,\n    animal,\n    COUNT(spire) AS occult\n  FROM cte_1\n  GROUP BY\n    entity,\n    animal\n),\ncte_3 AS (\n  SELECT\n    entity,\n    animal,\n    marina\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(animal) AS theme,\n    MIN(entity) AS right,\n    SUM(entity) AS butter,\n    COUNT(entity) AS poster\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     entity,\n     animal,\n     defeat,\n     letter,\n     maggie\n|> AGGREGATE\n     COUNT(maggie) AS comer,\n     AVG(letter) AS advent,\n     AVG(defeat) AS gospel,\n     SUM(letter) AS spire\n   GROUP BY\n     entity,\n     animal\n|> AGGREGATE\n     COUNT(spire) AS occult\n   GROUP BY\n     entity,\n     animal\n|> SELECT\n     entity,\n     animal,\n     marina\n|> AGGREGATE\n     MIN(animal) AS theme,\n     MIN(entity) AS right,\n     SUM(entity) AS butter,\n     COUNT(entity) AS poster\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"marina",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tycoon,\n    notion,\n    search,\n    thrush,\n    bluff\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    tycoon,\n    notion,\n    SUM(bluff) AS weber,\n    MIN(bluff) AS bingo\n  FROM cte_0\n  GROUP BY\n    tycoon,\n    notion,\n    search,\n    thrush\n),\ncte_2 AS (\n  SELECT\n    tycoon,\n    notion,\n    MAX(bingo) AS genre\n  FROM cte_1\n  GROUP BY\n    tycoon,\n    notion,\n    weber\n),\ncte_3 AS (\n  SELECT\n    tycoon,\n    notion,\n    genre,\n    flood\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(genre) AS puzzle,\n    MAX(tycoon) AS tiger,\n    SUM(genre) AS pilot,\n    COUNT(notion) AS shrub\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tycoon,\n     notion,\n     search,\n     thrush,\n     bluff\n|> AGGREGATE\n     SUM(bluff) AS weber,\n     MIN(bluff) AS bingo\n   GROUP BY\n     tycoon,\n     notion,\n     search,\n     thrush\n|> AGGREGATE\n     MAX(bingo) AS genre\n   GROUP BY\n     tycoon,\n     notion,\n     weber\n|> SELECT\n     tycoon,\n     notion,\n     genre,\n     flood\n|> AGGREGATE\n     MIN(genre) AS puzzle,\n     MAX(tycoon) AS tiger,\n     SUM(genre) AS pilot,\n     COUNT(notion) AS shrub\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"flood",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    razor,\n    divide,\n    quest,\n    linen,\n    spoon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    razor,\n    divide,\n    AVG(spoon) AS whisky,\n    MAX(linen) AS argyll,\n    COUNT(spoon) AS pixel\n  FROM cte_0\n  GROUP BY\n    razor,\n    divide,\n    quest\n),\ncte_2 AS (\n  SELECT\n    razor,\n    divide,\n    COUNT(whisky) AS thesis\n  FROM cte_1\n  GROUP BY\n    razor,\n    divide\n),\ncte_3 AS (\n  SELECT\n    razor,\n    divide,\n    thesis,\n    umpire\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(thesis) AS board,\n    AVG(razor) AS stile,\n    MIN(divide) AS shawl,\n    AVG(divide) AS helper\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     razor,\n     divide,\n     quest,\n     linen,\n     spoon\n|> AGGREGATE\n     AVG(spoon) AS whisky,\n     MAX(linen) AS argyll,\n     COUNT(spoon) AS pixel\n   GROUP BY\n     razor,\n     divide,\n     quest\n|> AGGREGATE\n     COUNT(whisky) AS thesis\n   GROUP BY\n     razor,\n     divide\n|> SELECT\n     razor,\n     divide,\n     thesis,\n     umpire\n|> AGGREGATE\n     COUNT(thesis) AS board,\n     AVG(razor) AS stile,\n     MIN(divide) AS shawl,\n     AVG(divide) AS helper\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"umpire",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    moray,\n    resin,\n    maxim,\n    crane,\n    hernia\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    moray,\n    resin,\n    maxim,\n    crane,\n    MAX(hernia) AS gallon,\n    COUNT(hernia) AS elbow,\n    AVG(hernia) AS scorn\n  FROM cte_0\n  GROUP BY\n    moray,\n    resin,\n    maxim,\n    crane\n),\ncte_2 AS (\n  SELECT\n    moray,\n    resin,\n    maxim,\n    crane,\n    gallon,\n    elbow,\n    tramp\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    moray,\n    resin\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    moray,\n    MIN(resin) AS wheat,\n    AVG(resin) AS lover\n  FROM cte_3\n  GROUP BY\n    moray\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     moray,\n     resin,\n     maxim,\n     crane,\n     hernia\n|> AGGREGATE\n     MAX(hernia) AS gallon,\n     COUNT(hernia) AS elbow,\n     AVG(hernia) AS scorn\n   GROUP BY\n     moray,\n     resin,\n     maxim,\n     crane\n|> SELECT\n     moray,\n     resin,\n     maxim,\n     crane,\n     gallon,\n     elbow,\n     tramp\n|> SELECT\n     moray,\n     resin\n|> AGGREGATE\n     MIN(resin) AS wheat,\n     AVG(resin) AS lover\n   GROUP BY\n     moray\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"tramp",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    outing,\n    breath,\n    robin,\n    raffle,\n    parrot\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    outing,\n    breath,\n    robin,\n    COUNT(raffle) AS prose,\n    MAX(raffle) AS unison,\n    AVG(parrot) AS litre,\n    MIN(raffle) AS soccer\n  FROM cte_0\n  GROUP BY\n    outing,\n    breath,\n    robin\n),\ncte_2 AS (\n  SELECT\n    outing,\n    COUNT(soccer) AS teddy,\n    MAX(unison) AS japan\n  FROM cte_1\n  GROUP BY\n    outing\n),\ncte_3 AS (\n  SELECT\n    cohort,\n    outing,\n    teddy\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    outing,\n    COUNT(teddy) AS genus,\n    SUM(teddy) AS parity\n  FROM cte_3\n  GROUP BY\n    outing\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     outing,\n     breath,\n     robin,\n     raffle,\n     parrot\n|> AGGREGATE\n     COUNT(raffle) AS prose,\n     MAX(raffle) AS unison,\n     AVG(parrot) AS litre,\n     MIN(raffle) AS soccer\n   GROUP BY\n     outing,\n     breath,\n     robin\n|> AGGREGATE\n     COUNT(soccer) AS teddy,\n     MAX(unison) AS japan\n   GROUP BY\n     outing\n|> SELECT\n     cohort,\n     outing,\n     teddy\n|> AGGREGATE\n     COUNT(teddy) AS genus,\n     SUM(teddy) AS parity\n   GROUP BY\n     outing\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"cohort",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    idiom,\n    ozone,\n    donkey,\n    marsh,\n    baxter\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    idiom,\n    ozone,\n    AVG(marsh) AS merger\n  FROM cte_0\n  GROUP BY\n    idiom,\n    ozone\n),\ncte_2 AS (\n  SELECT\n    idiom,\n    ozone,\n    AVG(merger) AS ditch,\n    MAX(merger) AS scare,\n    MIN(merger) AS upland,\n    SUM(merger) AS nether\n  FROM cte_1\n  GROUP BY\n    idiom,\n    ozone\n),\ncte_3 AS (\n  SELECT\n    idiom,\n    ozone,\n    blood,\n    ditch,\n    scare\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    idiom,\n    ozone,\n    MAX(scare) AS cartel\n  FROM cte_3\n  GROUP BY\n    idiom,\n    ozone\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     idiom,\n     ozone,\n     donkey,\n     marsh,\n     baxter\n|> AGGREGATE\n     AVG(marsh) AS merger\n   GROUP BY\n     idiom,\n     ozone\n|> AGGREGATE\n     AVG(merger) AS ditch,\n     MAX(merger) AS scare,\n     MIN(merger) AS upland,\n     SUM(merger) AS nether\n   GROUP BY\n     idiom,\n     ozone\n|> SELECT\n     idiom,\n     ozone,\n     blood,\n     ditch,\n     scare\n|> AGGREGATE\n     MAX(scare) AS cartel\n   GROUP BY\n     idiom,\n     ozone\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:28,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"blood",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    smith,\n    storey,\n    cream,\n    lesion,\n    cadet\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    smith,\n    COUNT(cadet) AS witch,\n    AVG(cadet) AS willy,\n    MAX(cadet) AS offer,\n    MIN(lesion) AS papacy\n  FROM cte_0\n  GROUP BY\n    smith,\n    storey,\n    cream\n),\ncte_2 AS (\n  SELECT\n    smith,\n    witch,\n    willy,\n    AVG(papacy) AS flour\n  FROM cte_1\n  GROUP BY\n    smith,\n    witch,\n    willy\n),\ncte_3 AS (\n  SELECT\n    feast,\n    smith,\n    witch,\n    willy\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(witch) AS knight,\n    SUM(willy) AS permit\n  FROM cte_3\n  GROUP BY\n    smith\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     smith,\n     storey,\n     cream,\n     lesion,\n     cadet\n|> AGGREGATE\n     COUNT(cadet) AS witch,\n     AVG(cadet) AS willy,\n     MAX(cadet) AS offer,\n     MIN(lesion) AS papacy\n   GROUP BY\n     smith,\n     storey,\n     cream\n|> AGGREGATE\n     AVG(papacy) AS flour\n   GROUP BY\n     smith,\n     witch,\n     willy\n|> SELECT\n     feast,\n     smith,\n     witch,\n     willy\n|> AGGREGATE\n     MIN(witch) AS knight,\n     SUM(willy) AS permit\n   GROUP BY\n     smith\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"feast",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    magic,\n    entry,\n    facade,\n    corner,\n    digest\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    magic,\n    entry,\n    MIN(digest) AS acorn,\n    MAX(digest) AS poker,\n    AVG(corner) AS carer,\n    COUNT(corner) AS parker\n  FROM cte_0\n  GROUP BY\n    magic,\n    entry,\n    facade\n),\ncte_2 AS (\n  SELECT\n    magic,\n    entry,\n    AVG(poker) AS burton\n  FROM cte_1\n  GROUP BY\n    magic,\n    entry\n),\ncte_3 AS (\n  SELECT\n    magic,\n    famine,\n    entry,\n    burton\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(burton) AS ivory,\n    AVG(burton) AS behalf\n  FROM cte_3\n  GROUP BY\n    magic,\n    entry\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     magic,\n     entry,\n     facade,\n     corner,\n     digest\n|> AGGREGATE\n     MIN(digest) AS acorn,\n     MAX(digest) AS poker,\n     AVG(corner) AS carer,\n     COUNT(corner) AS parker\n   GROUP BY\n     magic,\n     entry,\n     facade\n|> AGGREGATE\n     AVG(poker) AS burton\n   GROUP BY\n     magic,\n     entry\n|> SELECT\n     magic,\n     famine,\n     entry,\n     burton\n|> AGGREGATE\n     SUM(burton) AS ivory,\n     AVG(burton) AS behalf\n   GROUP BY\n     magic,\n     entry\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"famine",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    patron,\n    manure,\n    hound,\n    radar,\n    daisy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    patron,\n    manure,\n    MAX(daisy) AS button,\n    SUM(radar) AS vacuum\n  FROM cte_0\n  GROUP BY\n    patron,\n    manure,\n    hound\n),\ncte_2 AS (\n  SELECT\n    patron,\n    manure,\n    SUM(vacuum) AS michel,\n    MAX(vacuum) AS abuse\n  FROM cte_1\n  GROUP BY\n    patron,\n    manure,\n    button\n),\ncte_3 AS (\n  SELECT\n    patron,\n    manure,\n    local\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(manure) AS beacon,\n    MIN(patron) AS oracle,\n    COUNT(patron) AS willy,\n    MAX(manure) AS gallon\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     patron,\n     manure,\n     hound,\n     radar,\n     daisy\n|> AGGREGATE\n     MAX(daisy) AS button,\n     SUM(radar) AS vacuum\n   GROUP BY\n     patron,\n     manure,\n     hound\n|> AGGREGATE\n     SUM(vacuum) AS michel,\n     MAX(vacuum) AS abuse\n   GROUP BY\n     patron,\n     manure,\n     button\n|> SELECT\n     patron,\n     manure,\n     local\n|> AGGREGATE\n     MIN(manure) AS beacon,\n     MIN(patron) AS oracle,\n     COUNT(patron) AS willy,\n     MAX(manure) AS gallon\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"local",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    source,\n    tariff,\n    combat,\n    decade,\n    unease\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    source,\n    tariff,\n    combat,\n    MIN(unease) AS monkey,\n    COUNT(decade) AS diary,\n    SUM(unease) AS barrow,\n    MAX(decade) AS ballot\n  FROM cte_0\n  GROUP BY\n    source,\n    tariff,\n    combat\n),\ncte_2 AS (\n  SELECT\n    source,\n    SUM(monkey) AS creek\n  FROM cte_1\n  GROUP BY\n    source,\n    tariff,\n    combat\n),\ncte_3 AS (\n  SELECT\n    irene,\n    source,\n    creek\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    source,\n    AVG(creek) AS ratio,\n    MIN(creek) AS stall\n  FROM cte_3\n  GROUP BY\n    source\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     source,\n     tariff,\n     combat,\n     decade,\n     unease\n|> AGGREGATE\n     MIN(unease) AS monkey,\n     COUNT(decade) AS diary,\n     SUM(unease) AS barrow,\n     MAX(decade) AS ballot\n   GROUP BY\n     source,\n     tariff,\n     combat\n|> AGGREGATE\n     SUM(monkey) AS creek\n   GROUP BY\n     source,\n     tariff,\n     combat\n|> SELECT\n     irene,\n     source,\n     creek\n|> AGGREGATE\n     AVG(creek) AS ratio,\n     MIN(creek) AS stall\n   GROUP BY\n     source\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"irene",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    salon,\n    towel,\n    arrow,\n    vigil,\n    chick\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    salon,\n    towel,\n    arrow,\n    vigil,\n    MIN(chick) AS cement,\n    COUNT(chick) AS dollar,\n    MAX(chick) AS smash\n  FROM cte_0\n  GROUP BY\n    salon,\n    towel,\n    arrow,\n    vigil\n),\ncte_2 AS (\n  SELECT\n    MAX(smash) AS mentor,\n    MIN(vigil) AS blade\n  FROM cte_1\n  GROUP BY\n    salon,\n    towel\n),\ncte_3 AS (\n  SELECT\n    intel,\n    mentor,\n    blade\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(blade) AS salmon,\n    MAX(blade) AS timer,\n    AVG(mentor) AS pouch\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     salon,\n     towel,\n     arrow,\n     vigil,\n     chick\n|> AGGREGATE\n     MIN(chick) AS cement,\n     COUNT(chick) AS dollar,\n     MAX(chick) AS smash\n   GROUP BY\n     salon,\n     towel,\n     arrow,\n     vigil\n|> AGGREGATE\n     MAX(smash) AS mentor,\n     MIN(vigil) AS blade\n   GROUP BY\n     salon,\n     towel\n|> SELECT\n     intel,\n     mentor,\n     blade\n|> AGGREGATE\n     SUM(blade) AS salmon,\n     MAX(blade) AS timer,\n     AVG(mentor) AS pouch\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"intel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    nanny,\n    grand,\n    psyche,\n    xerox,\n    molla\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    nanny,\n    grand,\n    MAX(xerox) AS future,\n    AVG(xerox) AS clutch,\n    COUNT(psyche) AS record\n  FROM cte_0\n  GROUP BY\n    nanny,\n    grand\n),\ncte_2 AS (\n  SELECT\n    nanny,\n    grand,\n    COUNT(record) AS probe,\n    AVG(record) AS policy\n  FROM cte_1\n  GROUP BY\n    nanny,\n    grand,\n    future,\n    clutch\n),\ncte_3 AS (\n  SELECT\n    cloak,\n    nanny,\n    grand,\n    probe,\n    policy\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(policy) AS pizza\n  FROM cte_3\n  GROUP BY\n    nanny,\n    grand\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     nanny,\n     grand,\n     psyche,\n     xerox,\n     molla\n|> AGGREGATE\n     MAX(xerox) AS future,\n     AVG(xerox) AS clutch,\n     COUNT(psyche) AS record\n   GROUP BY\n     nanny,\n     grand\n|> AGGREGATE\n     COUNT(record) AS probe,\n     AVG(record) AS policy\n   GROUP BY\n     nanny,\n     grand,\n     future,\n     clutch\n|> SELECT\n     cloak,\n     nanny,\n     grand,\n     probe,\n     policy\n|> AGGREGATE\n     MIN(policy) AS pizza\n   GROUP BY\n     nanny,\n     grand\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"cloak",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    quota,\n    cruise,\n    advice,\n    asylum,\n    vowel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    quota,\n    cruise,\n    advice,\n    AVG(vowel) AS advent\n  FROM cte_0\n  GROUP BY\n    quota,\n    cruise,\n    advice\n),\ncte_2 AS (\n  SELECT\n    quota,\n    MAX(cruise) AS clash,\n    SUM(advent) AS manure,\n    MAX(advice) AS tablet,\n    AVG(advice) AS sample,\n    MAX(advent) AS violin\n  FROM cte_1\n  GROUP BY\n    quota\n),\ncte_3 AS (\n  SELECT\n    flesh,\n    quota,\n    clash,\n    manure,\n    tablet\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    quota,\n    clash,\n    manure,\n    tablet\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     quota,\n     cruise,\n     advice,\n     asylum,\n     vowel\n|> AGGREGATE\n     AVG(vowel) AS advent\n   GROUP BY\n     quota,\n     cruise,\n     advice\n|> AGGREGATE\n     MAX(cruise) AS clash,\n     SUM(advent) AS manure,\n     MAX(advice) AS tablet,\n     AVG(advice) AS sample,\n     MAX(advent) AS violin\n   GROUP BY\n     quota\n|> SELECT\n     flesh,\n     quota,\n     clash,\n     manure,\n     tablet\n|> SELECT\n     quota,\n     clash,\n     manure,\n     tablet\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"flesh",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    subset,\n    fossil,\n    smart,\n    jersey,\n    bingo\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    subset,\n    fossil,\n    COUNT(jersey) AS slump,\n    MIN(bingo) AS value,\n    SUM(bingo) AS pasta,\n    MIN(smart) AS backup\n  FROM cte_0\n  GROUP BY\n    subset,\n    fossil\n),\ncte_2 AS (\n  SELECT\n    subset,\n    fossil,\n    COUNT(pasta) AS sheet,\n    MAX(pasta) AS gooch\n  FROM cte_1\n  GROUP BY\n    subset,\n    fossil\n),\ncte_3 AS (\n  SELECT\n    racism,\n    subset,\n    fossil\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(fossil) AS humour,\n    MAX(fossil) AS threat,\n    AVG(subset) AS volley\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     subset,\n     fossil,\n     smart,\n     jersey,\n     bingo\n|> AGGREGATE\n     COUNT(jersey) AS slump,\n     MIN(bingo) AS value,\n     SUM(bingo) AS pasta,\n     MIN(smart) AS backup\n   GROUP BY\n     subset,\n     fossil\n|> AGGREGATE\n     COUNT(pasta) AS sheet,\n     MAX(pasta) AS gooch\n   GROUP BY\n     subset,\n     fossil\n|> SELECT\n     racism,\n     subset,\n     fossil\n|> AGGREGATE\n     SUM(fossil) AS humour,\n     MAX(fossil) AS threat,\n     AVG(subset) AS volley\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"racism",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    shoot,\n    gemma,\n    rattle,\n    craig,\n    taboo\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    shoot,\n    gemma,\n    MAX(craig) AS header,\n    SUM(rattle) AS wicker,\n    AVG(craig) AS louis\n  FROM cte_0\n  GROUP BY\n    shoot,\n    gemma\n),\ncte_2 AS (\n  SELECT\n    shoot,\n    gemma,\n    AVG(louis) AS radio,\n    COUNT(louis) AS dance\n  FROM cte_1\n  GROUP BY\n    shoot,\n    gemma,\n    header,\n    wicker\n),\ncte_3 AS (\n  SELECT\n    proton,\n    shoot,\n    gemma,\n    radio\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(gemma) AS entry,\n    COUNT(radio) AS faith\n  FROM cte_3\n  GROUP BY\n    shoot\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     shoot,\n     gemma,\n     rattle,\n     craig,\n     taboo\n|> AGGREGATE\n     MAX(craig) AS header,\n     SUM(rattle) AS wicker,\n     AVG(craig) AS louis\n   GROUP BY\n     shoot,\n     gemma\n|> AGGREGATE\n     AVG(louis) AS radio,\n     COUNT(louis) AS dance\n   GROUP BY\n     shoot,\n     gemma,\n     header,\n     wicker\n|> SELECT\n     proton,\n     shoot,\n     gemma,\n     radio\n|> AGGREGATE\n     AVG(gemma) AS entry,\n     COUNT(radio) AS faith\n   GROUP BY\n     shoot\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"proton",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    brace,\n    reeve,\n    flare,\n    plane,\n    eagle\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    brace,\n    reeve,\n    flare,\n    plane,\n    COUNT(eagle) AS dancer,\n    MAX(eagle) AS format,\n    SUM(eagle) AS radius\n  FROM cte_0\n  GROUP BY\n    brace,\n    reeve,\n    flare,\n    plane\n),\ncte_2 AS (\n  SELECT\n    brace,\n    reeve,\n    flare,\n    plane\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    brace,\n    reeve,\n    method,\n    flare,\n    plane\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    brace,\n    reeve,\n    MAX(flare) AS burst\n  FROM cte_3\n  GROUP BY\n    brace,\n    reeve\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     brace,\n     reeve,\n     flare,\n     plane,\n     eagle\n|> AGGREGATE\n     COUNT(eagle) AS dancer,\n     MAX(eagle) AS format,\n     SUM(eagle) AS radius\n   GROUP BY\n     brace,\n     reeve,\n     flare,\n     plane\n|> SELECT\n     brace,\n     reeve,\n     flare,\n     plane\n|> SELECT\n     brace,\n     reeve,\n     method,\n     flare,\n     plane\n|> AGGREGATE\n     MAX(flare) AS burst\n   GROUP BY\n     brace,\n     reeve\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:28,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"method",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    flake,\n    lance,\n    writer,\n    granny,\n    reflux\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    flake,\n    lance,\n    writer,\n    granny,\n    COUNT(reflux) AS raffle,\n    SUM(reflux) AS venue,\n    MAX(reflux) AS cooper\n  FROM cte_0\n  GROUP BY\n    flake,\n    lance,\n    writer,\n    granny\n),\ncte_2 AS (\n  SELECT\n    flake,\n    lance,\n    writer,\n    granny,\n    raffle,\n    venue,\n    clause,\n    cooper\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(lance) AS shore,\n    MAX(writer) AS school\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(school) AS fender\n  FROM cte_3\n  GROUP BY\n    shore\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     flake,\n     lance,\n     writer,\n     granny,\n     reflux\n|> AGGREGATE\n     COUNT(reflux) AS raffle,\n     SUM(reflux) AS venue,\n     MAX(reflux) AS cooper\n   GROUP BY\n     flake,\n     lance,\n     writer,\n     granny\n|> SELECT\n     flake,\n     lance,\n     writer,\n     granny,\n     raffle,\n     venue,\n     clause,\n     cooper\n|> AGGREGATE\n     SUM(lance) AS shore,\n     MAX(writer) AS school\n|> AGGREGATE\n     COUNT(school) AS fender\n   GROUP BY\n     shore\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"clause",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    miller,\n    polish,\n    claret,\n    level,\n    chest\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    miller,\n    polish,\n    MIN(chest) AS currie,\n    MAX(level) AS malta\n  FROM cte_0\n  GROUP BY\n    miller,\n    polish,\n    claret\n),\ncte_2 AS (\n  SELECT\n    miller,\n    polish,\n    COUNT(malta) AS comedy,\n    MAX(malta) AS cruise,\n    AVG(currie) AS honey\n  FROM cte_1\n  GROUP BY\n    miller,\n    polish\n),\ncte_3 AS (\n  SELECT\n    miller,\n    sugar,\n    polish,\n    comedy,\n    cruise\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(cruise) AS bench\n  FROM cte_3\n  GROUP BY\n    miller,\n    polish,\n    comedy\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     miller,\n     polish,\n     claret,\n     level,\n     chest\n|> AGGREGATE\n     MIN(chest) AS currie,\n     MAX(level) AS malta\n   GROUP BY\n     miller,\n     polish,\n     claret\n|> AGGREGATE\n     COUNT(malta) AS comedy,\n     MAX(malta) AS cruise,\n     AVG(currie) AS honey\n   GROUP BY\n     miller,\n     polish\n|> SELECT\n     miller,\n     sugar,\n     polish,\n     comedy,\n     cruise\n|> AGGREGATE\n     COUNT(cruise) AS bench\n   GROUP BY\n     miller,\n     polish,\n     comedy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"sugar",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    boiler,\n    permit,\n    birth,\n    chunk,\n    union\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    boiler,\n    permit,\n    birth,\n    chunk,\n    MAX(union) AS temper\n  FROM cte_0\n  GROUP BY\n    boiler,\n    permit,\n    birth,\n    chunk\n),\ncte_2 AS (\n  SELECT\n    SUM(temper) AS corps,\n    MAX(chunk) AS value,\n    AVG(boiler) AS envoy,\n    SUM(chunk) AS sharp,\n    MAX(temper) AS magnet\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    person,\n    corps,\n    value\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(value) AS anthem,\n    SUM(corps) AS heron,\n    AVG(corps) AS kettle\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     boiler,\n     permit,\n     birth,\n     chunk,\n     union\n|> AGGREGATE\n     MAX(union) AS temper\n   GROUP BY\n     boiler,\n     permit,\n     birth,\n     chunk\n|> AGGREGATE\n     SUM(temper) AS corps,\n     MAX(chunk) AS value,\n     AVG(boiler) AS envoy,\n     SUM(chunk) AS sharp,\n     MAX(temper) AS magnet\n|> SELECT\n     person,\n     corps,\n     value\n|> AGGREGATE\n     MIN(value) AS anthem,\n     SUM(corps) AS heron,\n     AVG(corps) AS kettle\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"person",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    cargo,\n    proton,\n    fender,\n    artist,\n    resort\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    cargo,\n    proton,\n    fender,\n    SUM(resort) AS sermon\n  FROM cte_0\n  GROUP BY\n    cargo,\n    proton,\n    fender,\n    artist\n),\ncte_2 AS (\n  SELECT\n    cargo,\n    SUM(sermon) AS dummy,\n    COUNT(sermon) AS bacon,\n    MIN(sermon) AS helmet\n  FROM cte_1\n  GROUP BY\n    cargo,\n    proton,\n    fender\n),\ncte_3 AS (\n  SELECT\n    cargo,\n    bumper,\n    dummy,\n    bacon,\n    helmet\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    cargo,\n    AVG(helmet) AS stride\n  FROM cte_3\n  GROUP BY\n    cargo,\n    dummy\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     cargo,\n     proton,\n     fender,\n     artist,\n     resort\n|> AGGREGATE\n     SUM(resort) AS sermon\n   GROUP BY\n     cargo,\n     proton,\n     fender,\n     artist\n|> AGGREGATE\n     SUM(sermon) AS dummy,\n     COUNT(sermon) AS bacon,\n     MIN(sermon) AS helmet\n   GROUP BY\n     cargo,\n     proton,\n     fender\n|> SELECT\n     cargo,\n     bumper,\n     dummy,\n     bacon,\n     helmet\n|> AGGREGATE\n     AVG(helmet) AS stride\n   GROUP BY\n     cargo,\n     dummy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"bumper",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    policy,\n    rebel,\n    throne,\n    trick,\n    gravel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    policy,\n    rebel,\n    SUM(gravel) AS period\n  FROM cte_0\n  GROUP BY\n    policy,\n    rebel\n),\ncte_2 AS (\n  SELECT\n    policy,\n    rebel,\n    COUNT(period) AS swell,\n    AVG(period) AS canada,\n    MAX(period) AS thorpe,\n    MIN(period) AS tender\n  FROM cte_1\n  GROUP BY\n    policy,\n    rebel\n),\ncte_3 AS (\n  SELECT\n    policy,\n    rebel,\n    limit,\n    swell,\n    canada,\n    thorpe\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(rebel) AS sudden,\n    COUNT(thorpe) AS expert\n  FROM cte_3\n  GROUP BY\n    policy\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     policy,\n     rebel,\n     throne,\n     trick,\n     gravel\n|> AGGREGATE\n     SUM(gravel) AS period\n   GROUP BY\n     policy,\n     rebel\n|> AGGREGATE\n     COUNT(period) AS swell,\n     AVG(period) AS canada,\n     MAX(period) AS thorpe,\n     MIN(period) AS tender\n   GROUP BY\n     policy,\n     rebel\n|> SELECT\n     policy,\n     rebel,\n     limit,\n     swell,\n     canada,\n     thorpe\n|> AGGREGATE\n     COUNT(rebel) AS sudden,\n     COUNT(thorpe) AS expert\n   GROUP BY\n     policy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"limit",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    urine,\n    search,\n    idiot,\n    stance,\n    outset\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    urine,\n    search,\n    idiot,\n    AVG(outset) AS excise\n  FROM cte_0\n  GROUP BY\n    urine,\n    search,\n    idiot,\n    stance\n),\ncte_2 AS (\n  SELECT\n    urine,\n    MIN(excise) AS shoal,\n    AVG(idiot) AS parole,\n    AVG(excise) AS ocean\n  FROM cte_1\n  GROUP BY\n    urine\n),\ncte_3 AS (\n  SELECT\n    urine,\n    shoal,\n    parole,\n    crate,\n    ocean\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    urine,\n    COUNT(ocean) AS reggae,\n    MIN(ocean) AS wicket\n  FROM cte_3\n  GROUP BY\n    urine,\n    shoal\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     urine,\n     search,\n     idiot,\n     stance,\n     outset\n|> AGGREGATE\n     AVG(outset) AS excise\n   GROUP BY\n     urine,\n     search,\n     idiot,\n     stance\n|> AGGREGATE\n     MIN(excise) AS shoal,\n     AVG(idiot) AS parole,\n     AVG(excise) AS ocean\n   GROUP BY\n     urine\n|> SELECT\n     urine,\n     shoal,\n     parole,\n     crate,\n     ocean\n|> AGGREGATE\n     COUNT(ocean) AS reggae,\n     MIN(ocean) AS wicket\n   GROUP BY\n     urine,\n     shoal\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"crate",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    vector,\n    wrong,\n    flare,\n    mixer,\n    rhine\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    vector,\n    wrong,\n    flare,\n    MIN(mixer) AS candle,\n    SUM(mixer) AS reflex\n  FROM cte_0\n  GROUP BY\n    vector,\n    wrong,\n    flare\n),\ncte_2 AS (\n  SELECT\n    vector,\n    MIN(flare) AS lough,\n    AVG(flare) AS steel,\n    MIN(candle) AS piano\n  FROM cte_1\n  GROUP BY\n    vector,\n    wrong\n),\ncte_3 AS (\n  SELECT\n    vector,\n    factor,\n    lough\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(lough) AS barry,\n    COUNT(lough) AS letter,\n    SUM(lough) AS entry\n  FROM cte_3\n  GROUP BY\n    vector\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     vector,\n     wrong,\n     flare,\n     mixer,\n     rhine\n|> AGGREGATE\n     MIN(mixer) AS candle,\n     SUM(mixer) AS reflex\n   GROUP BY\n     vector,\n     wrong,\n     flare\n|> AGGREGATE\n     MIN(flare) AS lough,\n     AVG(flare) AS steel,\n     MIN(candle) AS piano\n   GROUP BY\n     vector,\n     wrong\n|> SELECT\n     vector,\n     factor,\n     lough\n|> AGGREGATE\n     AVG(lough) AS barry,\n     COUNT(lough) AS letter,\n     SUM(lough) AS entry\n   GROUP BY\n     vector\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"factor",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    defect,\n    beetle,\n    mosaic,\n    malice,\n    jigsaw\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    defect,\n    beetle,\n    COUNT(jigsaw) AS salad\n  FROM cte_0\n  GROUP BY\n    defect,\n    beetle,\n    mosaic,\n    malice\n),\ncte_2 AS (\n  SELECT\n    defect,\n    beetle,\n    MAX(salad) AS giant,\n    AVG(salad) AS disco,\n    COUNT(salad) AS lounge,\n    SUM(salad) AS racket\n  FROM cte_1\n  GROUP BY\n    defect,\n    beetle\n),\ncte_3 AS (\n  SELECT\n    caesar,\n    defect,\n    beetle,\n    giant,\n    disco,\n    lounge,\n    racket\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    defect,\n    beetle\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     defect,\n     beetle,\n     mosaic,\n     malice,\n     jigsaw\n|> AGGREGATE\n     COUNT(jigsaw) AS salad\n   GROUP BY\n     defect,\n     beetle,\n     mosaic,\n     malice\n|> AGGREGATE\n     MAX(salad) AS giant,\n     AVG(salad) AS disco,\n     COUNT(salad) AS lounge,\n     SUM(salad) AS racket\n   GROUP BY\n     defect,\n     beetle\n|> SELECT\n     caesar,\n     defect,\n     beetle,\n     giant,\n     disco,\n     lounge,\n     racket\n|> SELECT\n     defect,\n     beetle\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"caesar",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    limit,\n    trunk,\n    lance,\n    coward,\n    digest\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    limit,\n    trunk,\n    AVG(digest) AS agony,\n    MIN(coward) AS cereal\n  FROM cte_0\n  GROUP BY\n    limit,\n    trunk,\n    lance\n),\ncte_2 AS (\n  SELECT\n    limit,\n    trunk,\n    SUM(cereal) AS watch,\n    MAX(cereal) AS cavity\n  FROM cte_1\n  GROUP BY\n    limit,\n    trunk\n),\ncte_3 AS (\n  SELECT\n    limit,\n    trunk,\n    watch,\n    chaos\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    limit,\n    SUM(trunk) AS attack,\n    MIN(trunk) AS cannon,\n    AVG(trunk) AS karate\n  FROM cte_3\n  GROUP BY\n    limit\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     limit,\n     trunk,\n     lance,\n     coward,\n     digest\n|> AGGREGATE\n     AVG(digest) AS agony,\n     MIN(coward) AS cereal\n   GROUP BY\n     limit,\n     trunk,\n     lance\n|> AGGREGATE\n     SUM(cereal) AS watch,\n     MAX(cereal) AS cavity\n   GROUP BY\n     limit,\n     trunk\n|> SELECT\n     limit,\n     trunk,\n     watch,\n     chaos\n|> AGGREGATE\n     SUM(trunk) AS attack,\n     MIN(trunk) AS cannon,\n     AVG(trunk) AS karate\n   GROUP BY\n     limit\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"chaos",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    penis,\n    switch,\n    unease,\n    choice,\n    branch\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    penis,\n    switch,\n    unease,\n    AVG(branch) AS patch,\n    SUM(branch) AS volley\n  FROM cte_0\n  GROUP BY\n    penis,\n    switch,\n    unease,\n    choice\n),\ncte_2 AS (\n  SELECT\n    penis,\n    AVG(patch) AS wedge,\n    AVG(unease) AS mother,\n    MIN(volley) AS chair\n  FROM cte_1\n  GROUP BY\n    penis\n),\ncte_3 AS (\n  SELECT\n    penis,\n    lover,\n    wedge,\n    mother\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    penis,\n    wedge,\n    AVG(mother) AS black\n  FROM cte_3\n  GROUP BY\n    penis,\n    wedge\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     penis,\n     switch,\n     unease,\n     choice,\n     branch\n|> AGGREGATE\n     AVG(branch) AS patch,\n     SUM(branch) AS volley\n   GROUP BY\n     penis,\n     switch,\n     unease,\n     choice\n|> AGGREGATE\n     AVG(patch) AS wedge,\n     AVG(unease) AS mother,\n     MIN(volley) AS chair\n   GROUP BY\n     penis\n|> SELECT\n     penis,\n     lover,\n     wedge,\n     mother\n|> AGGREGATE\n     AVG(mother) AS black\n   GROUP BY\n     penis,\n     wedge\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:28,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"lover",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    toxin,\n    victor,\n    heater,\n    smell,\n    misuse\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    toxin,\n    victor,\n    heater,\n    MIN(misuse) AS turner\n  FROM cte_0\n  GROUP BY\n    toxin,\n    victor,\n    heater,\n    smell\n),\ncte_2 AS (\n  SELECT\n    toxin,\n    AVG(turner) AS joanna,\n    COUNT(turner) AS gunman\n  FROM cte_1\n  GROUP BY\n    toxin,\n    victor,\n    heater\n),\ncte_3 AS (\n  SELECT\n    toxin,\n    joanna,\n    gunman,\n    firth\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(joanna) AS tiger,\n    COUNT(toxin) AS staple,\n    MIN(gunman) AS jersey,\n    AVG(gunman) AS barrow\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     toxin,\n     victor,\n     heater,\n     smell,\n     misuse\n|> AGGREGATE\n     MIN(misuse) AS turner\n   GROUP BY\n     toxin,\n     victor,\n     heater,\n     smell\n|> AGGREGATE\n     AVG(turner) AS joanna,\n     COUNT(turner) AS gunman\n   GROUP BY\n     toxin,\n     victor,\n     heater\n|> SELECT\n     toxin,\n     joanna,\n     gunman,\n     firth\n|> AGGREGATE\n     SUM(joanna) AS tiger,\n     COUNT(toxin) AS staple,\n     MIN(gunman) AS jersey,\n     AVG(gunman) AS barrow\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"firth",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    canoe,\n    sorrow,\n    brass,\n    smash,\n    trauma\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    canoe,\n    sorrow,\n    brass,\n    smash,\n    MIN(trauma) AS murmur,\n    COUNT(trauma) AS warmth\n  FROM cte_0\n  GROUP BY\n    canoe,\n    sorrow,\n    brass,\n    smash\n),\ncte_2 AS (\n  SELECT\n    SUM(smash) AS hobby,\n    COUNT(brass) AS slump,\n    COUNT(smash) AS fleece,\n    AVG(canoe) AS error\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    venom,\n    hobby,\n    slump,\n    fleece\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(slump) AS shade,\n    MIN(fleece) AS tunic\n  FROM cte_3\n  GROUP BY\n    hobby\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     canoe,\n     sorrow,\n     brass,\n     smash,\n     trauma\n|> AGGREGATE\n     MIN(trauma) AS murmur,\n     COUNT(trauma) AS warmth\n   GROUP BY\n     canoe,\n     sorrow,\n     brass,\n     smash\n|> AGGREGATE\n     SUM(smash) AS hobby,\n     COUNT(brass) AS slump,\n     COUNT(smash) AS fleece,\n     AVG(canoe) AS error\n|> SELECT\n     venom,\n     hobby,\n     slump,\n     fleece\n|> AGGREGATE\n     AVG(slump) AS shade,\n     MIN(fleece) AS tunic\n   GROUP BY\n     hobby\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"venom",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    telly,\n    movie,\n    juice,\n    norman,\n    ivory\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    telly,\n    AVG(ivory) AS issue,\n    MIN(norman) AS pollen,\n    COUNT(juice) AS warner\n  FROM cte_0\n  GROUP BY\n    telly,\n    movie\n),\ncte_2 AS (\n  SELECT\n    telly,\n    issue,\n    pollen,\n    AVG(warner) AS topaz\n  FROM cte_1\n  GROUP BY\n    telly,\n    issue,\n    pollen\n),\ncte_3 AS (\n  SELECT\n    telly,\n    issue,\n    pollen,\n    second,\n    topaz\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    telly,\n    issue,\n    AVG(topaz) AS gravel\n  FROM cte_3\n  GROUP BY\n    telly,\n    issue,\n    pollen\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     telly,\n     movie,\n     juice,\n     norman,\n     ivory\n|> AGGREGATE\n     AVG(ivory) AS issue,\n     MIN(norman) AS pollen,\n     COUNT(juice) AS warner\n   GROUP BY\n     telly,\n     movie\n|> AGGREGATE\n     AVG(warner) AS topaz\n   GROUP BY\n     telly,\n     issue,\n     pollen\n|> SELECT\n     telly,\n     issue,\n     pollen,\n     second,\n     topaz\n|> AGGREGATE\n     AVG(topaz) AS gravel\n   GROUP BY\n     telly,\n     issue,\n     pollen\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:28,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"second",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    limit,\n    image,\n    sauna,\n    gable,\n    medal\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    limit,\n    image,\n    sauna,\n    SUM(medal) AS cohort\n  FROM cte_0\n  GROUP BY\n    limit,\n    image,\n    sauna\n),\ncte_2 AS (\n  SELECT\n    limit,\n    COUNT(image) AS swing,\n    SUM(image) AS force,\n    COUNT(cohort) AS hitch\n  FROM cte_1\n  GROUP BY\n    limit\n),\ncte_3 AS (\n  SELECT\n    limit,\n    swing,\n    force,\n    hitch,\n    matey\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(force) AS avenue,\n    COUNT(hitch) AS brewer,\n    MAX(force) AS rover\n  FROM cte_3\n  GROUP BY\n    limit,\n    swing\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     limit,\n     image,\n     sauna,\n     gable,\n     medal\n|> AGGREGATE\n     SUM(medal) AS cohort\n   GROUP BY\n     limit,\n     image,\n     sauna\n|> AGGREGATE\n     COUNT(image) AS swing,\n     SUM(image) AS force,\n     COUNT(cohort) AS hitch\n   GROUP BY\n     limit\n|> SELECT\n     limit,\n     swing,\n     force,\n     hitch,\n     matey\n|> AGGREGATE\n     AVG(force) AS avenue,\n     COUNT(hitch) AS brewer,\n     MAX(force) AS rover\n   GROUP BY\n     limit,\n     swing\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"matey",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    barge,\n    mixer,\n    count,\n    globe,\n    unrest\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    barge,\n    mixer,\n    count,\n    AVG(unrest) AS murmur\n  FROM cte_0\n  GROUP BY\n    barge,\n    mixer,\n    count\n),\ncte_2 AS (\n  SELECT\n    barge,\n    MIN(mixer) AS expiry,\n    AVG(murmur) AS duvet,\n    SUM(count) AS abbey,\n    SUM(mixer) AS string\n  FROM cte_1\n  GROUP BY\n    barge\n),\ncte_3 AS (\n  SELECT\n    barge,\n    expiry,\n    sudden,\n    duvet,\n    abbey,\n    string\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    barge,\n    MAX(duvet) AS lounge\n  FROM cte_3\n  GROUP BY\n    barge,\n    expiry\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     barge,\n     mixer,\n     count,\n     globe,\n     unrest\n|> AGGREGATE\n     AVG(unrest) AS murmur\n   GROUP BY\n     barge,\n     mixer,\n     count\n|> AGGREGATE\n     MIN(mixer) AS expiry,\n     AVG(murmur) AS duvet,\n     SUM(count) AS abbey,\n     SUM(mixer) AS string\n   GROUP BY\n     barge\n|> SELECT\n     barge,\n     expiry,\n     sudden,\n     duvet,\n     abbey,\n     string\n|> AGGREGATE\n     MAX(duvet) AS lounge\n   GROUP BY\n     barge,\n     expiry\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"sudden",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    resale,\n    tonne,\n    sauce,\n    pause,\n    hamlet\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    resale,\n    tonne,\n    MIN(hamlet) AS ransom,\n    MAX(hamlet) AS fruit,\n    COUNT(sauce) AS mercy\n  FROM cte_0\n  GROUP BY\n    resale,\n    tonne\n),\ncte_2 AS (\n  SELECT\n    resale,\n    tonne,\n    MIN(mercy) AS digit,\n    SUM(mercy) AS victor\n  FROM cte_1\n  GROUP BY\n    resale,\n    tonne\n),\ncte_3 AS (\n  SELECT\n    resale,\n    tonne,\n    scorn,\n    digit,\n    victor\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(tonne) AS night,\n    MAX(resale) AS grange,\n    MIN(resale) AS herald\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     resale,\n     tonne,\n     sauce,\n     pause,\n     hamlet\n|> AGGREGATE\n     MIN(hamlet) AS ransom,\n     MAX(hamlet) AS fruit,\n     COUNT(sauce) AS mercy\n   GROUP BY\n     resale,\n     tonne\n|> AGGREGATE\n     MIN(mercy) AS digit,\n     SUM(mercy) AS victor\n   GROUP BY\n     resale,\n     tonne\n|> SELECT\n     resale,\n     tonne,\n     scorn,\n     digit,\n     victor\n|> AGGREGATE\n     MAX(tonne) AS night,\n     MAX(resale) AS grange,\n     MIN(resale) AS herald\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"scorn",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    mother,\n    domain,\n    indie,\n    polish,\n    disco\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    mother,\n    domain,\n    indie,\n    polish,\n    AVG(disco) AS stroll,\n    SUM(disco) AS dogma,\n    COUNT(disco) AS chess\n  FROM cte_0\n  GROUP BY\n    mother,\n    domain,\n    indie,\n    polish\n),\ncte_2 AS (\n  SELECT\n    mother,\n    domain,\n    indie,\n    polish,\n    stroll,\n    dogma,\n    frenzy\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    mother,\n    AVG(domain) AS manner\n  FROM cte_2\n  GROUP BY\n    mother\n),\ncte_4 AS (\n  SELECT\n    COUNT(mother) AS chant,\n    AVG(manner) AS ledge\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     mother,\n     domain,\n     indie,\n     polish,\n     disco\n|> AGGREGATE\n     AVG(disco) AS stroll,\n     SUM(disco) AS dogma,\n     COUNT(disco) AS chess\n   GROUP BY\n     mother,\n     domain,\n     indie,\n     polish\n|> SELECT\n     mother,\n     domain,\n     indie,\n     polish,\n     stroll,\n     dogma,\n     frenzy\n|> AGGREGATE\n     AVG(domain) AS manner\n   GROUP BY\n     mother\n|> AGGREGATE\n     COUNT(mother) AS chant,\n     AVG(manner) AS ledge\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"frenzy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    plunge,\n    mousse,\n    figure,\n    slate,\n    spread\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    plunge,\n    mousse,\n    MAX(spread) AS score,\n    MIN(spread) AS hazel,\n    AVG(spread) AS handle\n  FROM cte_0\n  GROUP BY\n    plunge,\n    mousse,\n    figure,\n    slate\n),\ncte_2 AS (\n  SELECT\n    plunge,\n    mousse,\n    SUM(handle) AS fiver\n  FROM cte_1\n  GROUP BY\n    plunge,\n    mousse,\n    score,\n    hazel\n),\ncte_3 AS (\n  SELECT\n    brent,\n    plunge,\n    mousse,\n    fiver\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    plunge,\n    SUM(fiver) AS devon\n  FROM cte_3\n  GROUP BY\n    plunge,\n    mousse\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     plunge,\n     mousse,\n     figure,\n     slate,\n     spread\n|> AGGREGATE\n     MAX(spread) AS score,\n     MIN(spread) AS hazel,\n     AVG(spread) AS handle\n   GROUP BY\n     plunge,\n     mousse,\n     figure,\n     slate\n|> AGGREGATE\n     SUM(handle) AS fiver\n   GROUP BY\n     plunge,\n     mousse,\n     score,\n     hazel\n|> SELECT\n     brent,\n     plunge,\n     mousse,\n     fiver\n|> AGGREGATE\n     SUM(fiver) AS devon\n   GROUP BY\n     plunge,\n     mousse\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"brent",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    whale,\n    marsh,\n    lizard,\n    gypsy,\n    trail\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    whale,\n    marsh,\n    lizard,\n    MIN(gypsy) AS blanc,\n    MAX(trail) AS lodge\n  FROM cte_0\n  GROUP BY\n    whale,\n    marsh,\n    lizard\n),\ncte_2 AS (\n  SELECT\n    whale,\n    COUNT(lizard) AS karate,\n    SUM(lizard) AS signal,\n    AVG(blanc) AS amino,\n    COUNT(blanc) AS needle\n  FROM cte_1\n  GROUP BY\n    whale\n),\ncte_3 AS (\n  SELECT\n    aroma,\n    whale,\n    karate,\n    signal,\n    amino\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(whale) AS bryony,\n    COUNT(karate) AS clock\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     whale,\n     marsh,\n     lizard,\n     gypsy,\n     trail\n|> AGGREGATE\n     MIN(gypsy) AS blanc,\n     MAX(trail) AS lodge\n   GROUP BY\n     whale,\n     marsh,\n     lizard\n|> AGGREGATE\n     COUNT(lizard) AS karate,\n     SUM(lizard) AS signal,\n     AVG(blanc) AS amino,\n     COUNT(blanc) AS needle\n   GROUP BY\n     whale\n|> SELECT\n     aroma,\n     whale,\n     karate,\n     signal,\n     amino\n|> AGGREGATE\n     MAX(whale) AS bryony,\n     COUNT(karate) AS clock\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"aroma",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    damage,\n    green,\n    saddle,\n    sultan,\n    drift\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    damage,\n    green,\n    saddle,\n    sultan,\n    AVG(drift) AS canary,\n    COUNT(drift) AS comer\n  FROM cte_0\n  GROUP BY\n    damage,\n    green,\n    saddle,\n    sultan\n),\ncte_2 AS (\n  SELECT\n    damage,\n    green,\n    saddle,\n    sultan,\n    canary,\n    comer\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    damage,\n    green,\n    carrot,\n    saddle,\n    sultan\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(saddle) AS auntie,\n    MAX(saddle) AS poetry\n  FROM cte_3\n  GROUP BY\n    damage,\n    green\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     damage,\n     green,\n     saddle,\n     sultan,\n     drift\n|> AGGREGATE\n     AVG(drift) AS canary,\n     COUNT(drift) AS comer\n   GROUP BY\n     damage,\n     green,\n     saddle,\n     sultan\n|> SELECT\n     damage,\n     green,\n     saddle,\n     sultan,\n     canary,\n     comer\n|> SELECT\n     damage,\n     green,\n     carrot,\n     saddle,\n     sultan\n|> AGGREGATE\n     MIN(saddle) AS auntie,\n     MAX(saddle) AS poetry\n   GROUP BY\n     damage,\n     green\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"carrot",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    parcel,\n    covent,\n    tycoon,\n    spiral,\n    bowler\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    parcel,\n    covent,\n    tycoon,\n    spiral,\n    AVG(bowler) AS critic\n  FROM cte_0\n  GROUP BY\n    parcel,\n    covent,\n    tycoon,\n    spiral\n),\ncte_2 AS (\n  SELECT\n    MAX(critic) AS church,\n    SUM(critic) AS alkali\n  FROM cte_1\n  GROUP BY\n    parcel,\n    covent,\n    tycoon,\n    spiral\n),\ncte_3 AS (\n  SELECT\n    church,\n    alkali,\n    matrix\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(alkali) AS count,\n    MAX(church) AS union,\n    MIN(church) AS throat,\n    SUM(alkali) AS castle\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     parcel,\n     covent,\n     tycoon,\n     spiral,\n     bowler\n|> AGGREGATE\n     AVG(bowler) AS critic\n   GROUP BY\n     parcel,\n     covent,\n     tycoon,\n     spiral\n|> AGGREGATE\n     MAX(critic) AS church,\n     SUM(critic) AS alkali\n   GROUP BY\n     parcel,\n     covent,\n     tycoon,\n     spiral\n|> SELECT\n     church,\n     alkali,\n     matrix\n|> AGGREGATE\n     AVG(alkali) AS count,\n     MAX(church) AS union,\n     MIN(church) AS throat,\n     SUM(alkali) AS castle\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"matrix",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    smear,\n    twist,\n    stand,\n    hazel,\n    sphere\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    smear,\n    twist,\n    stand,\n    SUM(sphere) AS clint\n  FROM cte_0\n  GROUP BY\n    smear,\n    twist,\n    stand,\n    hazel\n),\ncte_2 AS (\n  SELECT\n    smear,\n    AVG(clint) AS pigeon,\n    SUM(stand) AS little,\n    AVG(stand) AS voyage\n  FROM cte_1\n  GROUP BY\n    smear,\n    twist\n),\ncte_3 AS (\n  SELECT\n    smear,\n    pigeon,\n    cough,\n    little\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    smear,\n    AVG(little) AS scene,\n    MAX(little) AS pirate\n  FROM cte_3\n  GROUP BY\n    smear,\n    pigeon\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     smear,\n     twist,\n     stand,\n     hazel,\n     sphere\n|> AGGREGATE\n     SUM(sphere) AS clint\n   GROUP BY\n     smear,\n     twist,\n     stand,\n     hazel\n|> AGGREGATE\n     AVG(clint) AS pigeon,\n     SUM(stand) AS little,\n     AVG(stand) AS voyage\n   GROUP BY\n     smear,\n     twist\n|> SELECT\n     smear,\n     pigeon,\n     cough,\n     little\n|> AGGREGATE\n     AVG(little) AS scene,\n     MAX(little) AS pirate\n   GROUP BY\n     smear,\n     pigeon\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"cough",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    zebra,\n    laugh,\n    phrase,\n    fella,\n    trader\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    zebra,\n    laugh,\n    phrase,\n    fella,\n    trader\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    zebra,\n    laugh,\n    phrase,\n    fella,\n    COUNT(trader) AS grape,\n    AVG(trader) AS handle,\n    SUM(trader) AS break\n  FROM cte_1\n  GROUP BY\n    zebra,\n    laugh,\n    phrase,\n    fella\n),\ncte_3 AS (\n  SELECT\n    zebra,\n    excise,\n    laugh,\n    phrase,\n    fella,\n    grape\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    zebra,\n    MIN(grape) AS bowie\n  FROM cte_3\n  GROUP BY\n    zebra\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     zebra,\n     laugh,\n     phrase,\n     fella,\n     trader\n|> SELECT\n     zebra,\n     laugh,\n     phrase,\n     fella,\n     trader\n|> AGGREGATE\n     COUNT(trader) AS grape,\n     AVG(trader) AS handle,\n     SUM(trader) AS break\n   GROUP BY\n     zebra,\n     laugh,\n     phrase,\n     fella\n|> SELECT\n     zebra,\n     excise,\n     laugh,\n     phrase,\n     fella,\n     grape\n|> AGGREGATE\n     MIN(grape) AS bowie\n   GROUP BY\n     zebra\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"excise",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    repeal,\n    beard,\n    rebate,\n    string,\n    ritual\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    repeal,\n    beard,\n    rebate,\n    AVG(ritual) AS handle\n  FROM cte_0\n  GROUP BY\n    repeal,\n    beard,\n    rebate,\n    string\n),\ncte_2 AS (\n  SELECT\n    repeal,\n    COUNT(rebate) AS pickup,\n    MAX(handle) AS caller,\n    AVG(handle) AS moran,\n    MAX(rebate) AS gunman\n  FROM cte_1\n  GROUP BY\n    repeal,\n    beard\n),\ncte_3 AS (\n  SELECT\n    potter,\n    repeal,\n    pickup,\n    caller,\n    moran\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    repeal,\n    pickup,\n    caller,\n    moran\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     repeal,\n     beard,\n     rebate,\n     string,\n     ritual\n|> AGGREGATE\n     AVG(ritual) AS handle\n   GROUP BY\n     repeal,\n     beard,\n     rebate,\n     string\n|> AGGREGATE\n     COUNT(rebate) AS pickup,\n     MAX(handle) AS caller,\n     AVG(handle) AS moran,\n     MAX(rebate) AS gunman\n   GROUP BY\n     repeal,\n     beard\n|> SELECT\n     potter,\n     repeal,\n     pickup,\n     caller,\n     moran\n|> SELECT\n     repeal,\n     pickup,\n     caller,\n     moran\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"potter",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    torso,\n    smart,\n    tactic,\n    manual,\n    stair\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    torso,\n    smart,\n    tactic,\n    manual,\n    MIN(stair) AS logic,\n    MAX(stair) AS torch\n  FROM cte_0\n  GROUP BY\n    torso,\n    smart,\n    tactic,\n    manual\n),\ncte_2 AS (\n  SELECT\n    torso,\n    smart,\n    tactic,\n    manual,\n    logic\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    torso,\n    smart,\n    tactic,\n    shine\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(tactic) AS chief,\n    MIN(torso) AS racket,\n    COUNT(smart) AS tenant,\n    MIN(tactic) AS asset\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     torso,\n     smart,\n     tactic,\n     manual,\n     stair\n|> AGGREGATE\n     MIN(stair) AS logic,\n     MAX(stair) AS torch\n   GROUP BY\n     torso,\n     smart,\n     tactic,\n     manual\n|> SELECT\n     torso,\n     smart,\n     tactic,\n     manual,\n     logic\n|> SELECT\n     torso,\n     smart,\n     tactic,\n     shine\n|> AGGREGATE\n     AVG(tactic) AS chief,\n     MIN(torso) AS racket,\n     COUNT(smart) AS tenant,\n     MIN(tactic) AS asset\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"shine",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    flick,\n    boxer,\n    intel,\n    rector,\n    pistol\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    flick,\n    boxer,\n    intel,\n    rector,\n    AVG(pistol) AS small,\n    MIN(pistol) AS guess,\n    SUM(pistol) AS pouch,\n    MAX(pistol) AS route\n  FROM cte_0\n  GROUP BY\n    flick,\n    boxer,\n    intel,\n    rector\n),\ncte_2 AS (\n  SELECT\n    flick,\n    boxer\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    flick,\n    boxer,\n    accord\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(boxer) AS brake,\n    COUNT(flick) AS pasta,\n    AVG(boxer) AS patsy,\n    MAX(flick) AS sweat\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     flick,\n     boxer,\n     intel,\n     rector,\n     pistol\n|> AGGREGATE\n     AVG(pistol) AS small,\n     MIN(pistol) AS guess,\n     SUM(pistol) AS pouch,\n     MAX(pistol) AS route\n   GROUP BY\n     flick,\n     boxer,\n     intel,\n     rector\n|> SELECT\n     flick,\n     boxer\n|> SELECT\n     flick,\n     boxer,\n     accord\n|> AGGREGATE\n     MIN(boxer) AS brake,\n     COUNT(flick) AS pasta,\n     AVG(boxer) AS patsy,\n     MAX(flick) AS sweat\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"accord",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    uproar,\n    psyche,\n    burst,\n    unison,\n    alpha\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    uproar,\n    psyche,\n    burst,\n    unison,\n    SUM(alpha) AS trick,\n    MAX(alpha) AS guise,\n    AVG(alpha) AS wisdom,\n    COUNT(alpha) AS accord,\n    MIN(alpha) AS peter\n  FROM cte_0\n  GROUP BY\n    uproar,\n    psyche,\n    burst,\n    unison\n),\ncte_2 AS (\n  SELECT\n    uproar,\n    psyche,\n    havoc,\n    burst,\n    unison\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    uproar,\n    psyche\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    uproar,\n    AVG(psyche) AS subset\n  FROM cte_3\n  GROUP BY\n    uproar\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     uproar,\n     psyche,\n     burst,\n     unison,\n     alpha\n|> AGGREGATE\n     SUM(alpha) AS trick,\n     MAX(alpha) AS guise,\n     AVG(alpha) AS wisdom,\n     COUNT(alpha) AS accord,\n     MIN(alpha) AS peter\n   GROUP BY\n     uproar,\n     psyche,\n     burst,\n     unison\n|> SELECT\n     uproar,\n     psyche,\n     havoc,\n     burst,\n     unison\n|> SELECT\n     uproar,\n     psyche\n|> AGGREGATE\n     AVG(psyche) AS subset\n   GROUP BY\n     uproar\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"havoc",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    michel,\n    bearer,\n    stern,\n    leader,\n    party\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    michel,\n    bearer,\n    stern,\n    leader,\n    party\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    michel,\n    bearer,\n    stern,\n    leader,\n    MIN(party) AS onset,\n    COUNT(party) AS aisle\n  FROM cte_1\n  GROUP BY\n    michel,\n    bearer,\n    stern,\n    leader\n),\ncte_3 AS (\n  SELECT\n    michel,\n    bearer,\n    stern,\n    chapel,\n    leader,\n    onset,\n    aisle\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(leader) AS vacuum\n  FROM cte_3\n  GROUP BY\n    michel,\n    bearer,\n    stern\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     michel,\n     bearer,\n     stern,\n     leader,\n     party\n|> SELECT\n     michel,\n     bearer,\n     stern,\n     leader,\n     party\n|> AGGREGATE\n     MIN(party) AS onset,\n     COUNT(party) AS aisle\n   GROUP BY\n     michel,\n     bearer,\n     stern,\n     leader\n|> SELECT\n     michel,\n     bearer,\n     stern,\n     chapel,\n     leader,\n     onset,\n     aisle\n|> AGGREGATE\n     AVG(leader) AS vacuum\n   GROUP BY\n     michel,\n     bearer,\n     stern\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"chapel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    stock,\n    peace,\n    sunset,\n    quartz,\n    bloom\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    stock,\n    peace,\n    SUM(quartz) AS barker\n  FROM cte_0\n  GROUP BY\n    stock,\n    peace\n),\ncte_2 AS (\n  SELECT\n    stock,\n    peace,\n    MIN(barker) AS symbol,\n    AVG(barker) AS spike,\n    SUM(barker) AS sultan,\n    COUNT(barker) AS unease\n  FROM cte_1\n  GROUP BY\n    stock,\n    peace\n),\ncte_3 AS (\n  SELECT\n    stock,\n    peace,\n    worker,\n    symbol,\n    spike\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    stock,\n    MIN(spike) AS velvet\n  FROM cte_3\n  GROUP BY\n    stock,\n    peace,\n    symbol\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     stock,\n     peace,\n     sunset,\n     quartz,\n     bloom\n|> AGGREGATE\n     SUM(quartz) AS barker\n   GROUP BY\n     stock,\n     peace\n|> AGGREGATE\n     MIN(barker) AS symbol,\n     AVG(barker) AS spike,\n     SUM(barker) AS sultan,\n     COUNT(barker) AS unease\n   GROUP BY\n     stock,\n     peace\n|> SELECT\n     stock,\n     peace,\n     worker,\n     symbol,\n     spike\n|> AGGREGATE\n     MIN(spike) AS velvet\n   GROUP BY\n     stock,\n     peace,\n     symbol\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"worker",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    almond,\n    couple,\n    cereal,\n    depot,\n    mucus\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    almond,\n    couple,\n    cereal,\n    SUM(mucus) AS orange,\n    COUNT(depot) AS thrush\n  FROM cte_0\n  GROUP BY\n    almond,\n    couple,\n    cereal\n),\ncte_2 AS (\n  SELECT\n    almond,\n    MAX(orange) AS warren,\n    AVG(thrush) AS system,\n    COUNT(orange) AS lionel\n  FROM cte_1\n  GROUP BY\n    almond,\n    couple\n),\ncte_3 AS (\n  SELECT\n    almond,\n    parkin,\n    warren,\n    system,\n    lionel\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    almond,\n    COUNT(lionel) AS muddle\n  FROM cte_3\n  GROUP BY\n    almond,\n    warren\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     almond,\n     couple,\n     cereal,\n     depot,\n     mucus\n|> AGGREGATE\n     SUM(mucus) AS orange,\n     COUNT(depot) AS thrush\n   GROUP BY\n     almond,\n     couple,\n     cereal\n|> AGGREGATE\n     MAX(orange) AS warren,\n     AVG(thrush) AS system,\n     COUNT(orange) AS lionel\n   GROUP BY\n     almond,\n     couple\n|> SELECT\n     almond,\n     parkin,\n     warren,\n     system,\n     lionel\n|> AGGREGATE\n     COUNT(lionel) AS muddle\n   GROUP BY\n     almond,\n     warren\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"parkin",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bottom,\n    madam,\n    gospel,\n    tommy,\n    prior\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    bottom,\n    madam,\n    MAX(gospel) AS probe,\n    SUM(gospel) AS polish,\n    MIN(gospel) AS denim,\n    MAX(tommy) AS pearl\n  FROM cte_0\n  GROUP BY\n    bottom,\n    madam\n),\ncte_2 AS (\n  SELECT\n    bottom,\n    madam,\n    MIN(denim) AS track\n  FROM cte_1\n  GROUP BY\n    bottom,\n    madam,\n    probe,\n    polish\n),\ncte_3 AS (\n  SELECT\n    grape,\n    bottom,\n    madam,\n    track\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    bottom,\n    SUM(track) AS cloth\n  FROM cte_3\n  GROUP BY\n    bottom,\n    madam\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bottom,\n     madam,\n     gospel,\n     tommy,\n     prior\n|> AGGREGATE\n     MAX(gospel) AS probe,\n     SUM(gospel) AS polish,\n     MIN(gospel) AS denim,\n     MAX(tommy) AS pearl\n   GROUP BY\n     bottom,\n     madam\n|> AGGREGATE\n     MIN(denim) AS track\n   GROUP BY\n     bottom,\n     madam,\n     probe,\n     polish\n|> SELECT\n     grape,\n     bottom,\n     madam,\n     track\n|> AGGREGATE\n     SUM(track) AS cloth\n   GROUP BY\n     bottom,\n     madam\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"grape",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    velvet,\n    whore,\n    slump,\n    secret,\n    fairy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    velvet,\n    whore,\n    slump,\n    secret,\n    fairy\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    velvet,\n    whore,\n    slump,\n    secret,\n    MIN(fairy) AS pasta,\n    COUNT(fairy) AS picnic\n  FROM cte_1\n  GROUP BY\n    velvet,\n    whore,\n    slump,\n    secret\n),\ncte_3 AS (\n  SELECT\n    velvet,\n    whore,\n    slump,\n    drill,\n    secret,\n    pasta,\n    picnic\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    velvet,\n    whore,\n    slump,\n    secret,\n    pasta\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     velvet,\n     whore,\n     slump,\n     secret,\n     fairy\n|> SELECT\n     velvet,\n     whore,\n     slump,\n     secret,\n     fairy\n|> AGGREGATE\n     MIN(fairy) AS pasta,\n     COUNT(fairy) AS picnic\n   GROUP BY\n     velvet,\n     whore,\n     slump,\n     secret\n|> SELECT\n     velvet,\n     whore,\n     slump,\n     drill,\n     secret,\n     pasta,\n     picnic\n|> SELECT\n     velvet,\n     whore,\n     slump,\n     secret,\n     pasta\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"drill",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    odour,\n    pilot,\n    gauge,\n    serum,\n    repair\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    odour,\n    pilot,\n    SUM(repair) AS reason\n  FROM cte_0\n  GROUP BY\n    odour,\n    pilot,\n    gauge,\n    serum\n),\ncte_2 AS (\n  SELECT\n    odour,\n    pilot,\n    COUNT(reason) AS hobby,\n    SUM(reason) AS blaze,\n    MIN(reason) AS midst\n  FROM cte_1\n  GROUP BY\n    odour,\n    pilot\n),\ncte_3 AS (\n  SELECT\n    odour,\n    pilot,\n    source,\n    hobby,\n    blaze\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    odour,\n    MIN(pilot) AS brewer,\n    AVG(hobby) AS grape\n  FROM cte_3\n  GROUP BY\n    odour\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     odour,\n     pilot,\n     gauge,\n     serum,\n     repair\n|> AGGREGATE\n     SUM(repair) AS reason\n   GROUP BY\n     odour,\n     pilot,\n     gauge,\n     serum\n|> AGGREGATE\n     COUNT(reason) AS hobby,\n     SUM(reason) AS blaze,\n     MIN(reason) AS midst\n   GROUP BY\n     odour,\n     pilot\n|> SELECT\n     odour,\n     pilot,\n     source,\n     hobby,\n     blaze\n|> AGGREGATE\n     MIN(pilot) AS brewer,\n     AVG(hobby) AS grape\n   GROUP BY\n     odour\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"source",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    oscar,\n    width,\n    chair,\n    gemma,\n    peach\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    oscar,\n    width,\n    chair,\n    gemma,\n    MAX(peach) AS pepper,\n    SUM(peach) AS altar,\n    AVG(peach) AS rotor,\n    MIN(peach) AS union\n  FROM cte_0\n  GROUP BY\n    oscar,\n    width,\n    chair,\n    gemma\n),\ncte_2 AS (\n  SELECT\n    oscar,\n    width,\n    chair,\n    gemma,\n    lease\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    oscar,\n    MAX(width) AS matrix\n  FROM cte_2\n  GROUP BY\n    oscar\n),\ncte_4 AS (\n  SELECT\n    oscar,\n    AVG(matrix) AS molla\n  FROM cte_3\n  GROUP BY\n    oscar\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     oscar,\n     width,\n     chair,\n     gemma,\n     peach\n|> AGGREGATE\n     MAX(peach) AS pepper,\n     SUM(peach) AS altar,\n     AVG(peach) AS rotor,\n     MIN(peach) AS union\n   GROUP BY\n     oscar,\n     width,\n     chair,\n     gemma\n|> SELECT\n     oscar,\n     width,\n     chair,\n     gemma,\n     lease\n|> AGGREGATE\n     MAX(width) AS matrix\n   GROUP BY\n     oscar\n|> AGGREGATE\n     AVG(matrix) AS molla\n   GROUP BY\n     oscar\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:28,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"lease",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    mandy,\n    width,\n    intent,\n    tycoon,\n    basis\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    mandy,\n    width,\n    intent,\n    tycoon,\n    MIN(basis) AS parson,\n    COUNT(basis) AS human,\n    MAX(basis) AS warner,\n    AVG(basis) AS backup,\n    SUM(basis) AS altar\n  FROM cte_0\n  GROUP BY\n    mandy,\n    width,\n    intent,\n    tycoon\n),\ncte_2 AS (\n  SELECT\n    mandy,\n    width,\n    falcon\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    mandy,\n    SUM(width) AS karate,\n    MAX(width) AS frank\n  FROM cte_2\n  GROUP BY\n    mandy\n),\ncte_4 AS (\n  SELECT\n    MAX(mandy) AS office\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     mandy,\n     width,\n     intent,\n     tycoon,\n     basis\n|> AGGREGATE\n     MIN(basis) AS parson,\n     COUNT(basis) AS human,\n     MAX(basis) AS warner,\n     AVG(basis) AS backup,\n     SUM(basis) AS altar\n   GROUP BY\n     mandy,\n     width,\n     intent,\n     tycoon\n|> SELECT\n     mandy,\n     width,\n     falcon\n|> AGGREGATE\n     SUM(width) AS karate,\n     MAX(width) AS frank\n   GROUP BY\n     mandy\n|> AGGREGATE\n     MAX(mandy) AS office\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"falcon",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ripple,\n    mungo,\n    snake,\n    bruce,\n    connie\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    ripple,\n    mungo,\n    snake,\n    bruce,\n    MAX(connie) AS boost,\n    COUNT(connie) AS board,\n    MIN(connie) AS cover,\n    AVG(connie) AS brook,\n    SUM(connie) AS video\n  FROM cte_0\n  GROUP BY\n    ripple,\n    mungo,\n    snake,\n    bruce\n),\ncte_2 AS (\n  SELECT\n    ripple,\n    mungo,\n    groove,\n    snake,\n    bruce,\n    boost\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    ripple,\n    mungo\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(mungo) AS amber\n  FROM cte_3\n  GROUP BY\n    ripple\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ripple,\n     mungo,\n     snake,\n     bruce,\n     connie\n|> AGGREGATE\n     MAX(connie) AS boost,\n     COUNT(connie) AS board,\n     MIN(connie) AS cover,\n     AVG(connie) AS brook,\n     SUM(connie) AS video\n   GROUP BY\n     ripple,\n     mungo,\n     snake,\n     bruce\n|> SELECT\n     ripple,\n     mungo,\n     groove,\n     snake,\n     bruce,\n     boost\n|> SELECT\n     ripple,\n     mungo\n|> AGGREGATE\n     MAX(mungo) AS amber\n   GROUP BY\n     ripple\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"groove",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    luxury,\n    corpus,\n    beggar,\n    fluid,\n    purse\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    luxury,\n    corpus,\n    AVG(fluid) AS aunty,\n    SUM(purse) AS break\n  FROM cte_0\n  GROUP BY\n    luxury,\n    corpus,\n    beggar\n),\ncte_2 AS (\n  SELECT\n    luxury,\n    corpus,\n    COUNT(aunty) AS center,\n    MIN(break) AS praise,\n    MAX(break) AS remand\n  FROM cte_1\n  GROUP BY\n    luxury,\n    corpus\n),\ncte_3 AS (\n  SELECT\n    luxury,\n    basket,\n    corpus,\n    center,\n    praise,\n    remand\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(praise) AS nether\n  FROM cte_3\n  GROUP BY\n    luxury,\n    corpus\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     luxury,\n     corpus,\n     beggar,\n     fluid,\n     purse\n|> AGGREGATE\n     AVG(fluid) AS aunty,\n     SUM(purse) AS break\n   GROUP BY\n     luxury,\n     corpus,\n     beggar\n|> AGGREGATE\n     COUNT(aunty) AS center,\n     MIN(break) AS praise,\n     MAX(break) AS remand\n   GROUP BY\n     luxury,\n     corpus\n|> SELECT\n     luxury,\n     basket,\n     corpus,\n     center,\n     praise,\n     remand\n|> AGGREGATE\n     MAX(praise) AS nether\n   GROUP BY\n     luxury,\n     corpus\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"basket",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ruler,\n    butler,\n    steel,\n    salon,\n    ariel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    ruler,\n    butler,\n    steel,\n    salon,\n    SUM(ariel) AS demon,\n    MIN(ariel) AS paste,\n    AVG(ariel) AS mercer,\n    MAX(ariel) AS paint\n  FROM cte_0\n  GROUP BY\n    ruler,\n    butler,\n    steel,\n    salon\n),\ncte_2 AS (\n  SELECT\n    MAX(paint) AS intent,\n    SUM(salon) AS incest\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    volley,\n    intent,\n    incest\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(intent) AS hatch,\n    COUNT(intent) AS digest,\n    AVG(incest) AS hammer\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ruler,\n     butler,\n     steel,\n     salon,\n     ariel\n|> AGGREGATE\n     SUM(ariel) AS demon,\n     MIN(ariel) AS paste,\n     AVG(ariel) AS mercer,\n     MAX(ariel) AS paint\n   GROUP BY\n     ruler,\n     butler,\n     steel,\n     salon\n|> AGGREGATE\n     MAX(paint) AS intent,\n     SUM(salon) AS incest\n|> SELECT\n     volley,\n     intent,\n     incest\n|> AGGREGATE\n     MAX(intent) AS hatch,\n     COUNT(intent) AS digest,\n     AVG(incest) AS hammer\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"volley",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    mutiny,\n    hazel,\n    decor,\n    screw,\n    bidder\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    mutiny,\n    hazel,\n    decor,\n    MIN(bidder) AS barry,\n    AVG(bidder) AS bonnet\n  FROM cte_0\n  GROUP BY\n    mutiny,\n    hazel,\n    decor,\n    screw\n),\ncte_2 AS (\n  SELECT\n    mutiny,\n    MIN(bonnet) AS daily,\n    MIN(decor) AS person\n  FROM cte_1\n  GROUP BY\n    mutiny,\n    hazel\n),\ncte_3 AS (\n  SELECT\n    mutiny,\n    daily,\n    lunch,\n    person\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(daily) AS fabric,\n    MIN(person) AS yogurt,\n    SUM(daily) AS outlet\n  FROM cte_3\n  GROUP BY\n    mutiny\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     mutiny,\n     hazel,\n     decor,\n     screw,\n     bidder\n|> AGGREGATE\n     MIN(bidder) AS barry,\n     AVG(bidder) AS bonnet\n   GROUP BY\n     mutiny,\n     hazel,\n     decor,\n     screw\n|> AGGREGATE\n     MIN(bonnet) AS daily,\n     MIN(decor) AS person\n   GROUP BY\n     mutiny,\n     hazel\n|> SELECT\n     mutiny,\n     daily,\n     lunch,\n     person\n|> AGGREGATE\n     AVG(daily) AS fabric,\n     MIN(person) AS yogurt,\n     SUM(daily) AS outlet\n   GROUP BY\n     mutiny\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"lunch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    jelly,\n    upland,\n    diesel,\n    stall,\n    ulster\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    jelly,\n    upland,\n    diesel,\n    COUNT(stall) AS brunt,\n    MAX(ulster) AS snake\n  FROM cte_0\n  GROUP BY\n    jelly,\n    upland,\n    diesel\n),\ncte_2 AS (\n  SELECT\n    jelly,\n    COUNT(snake) AS ridley,\n    MIN(diesel) AS newco,\n    SUM(diesel) AS pastor\n  FROM cte_1\n  GROUP BY\n    jelly,\n    upland\n),\ncte_3 AS (\n  SELECT\n    jelly,\n    square,\n    ridley,\n    newco\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    jelly,\n    ridley,\n    MAX(newco) AS blade\n  FROM cte_3\n  GROUP BY\n    jelly,\n    ridley\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     jelly,\n     upland,\n     diesel,\n     stall,\n     ulster\n|> AGGREGATE\n     COUNT(stall) AS brunt,\n     MAX(ulster) AS snake\n   GROUP BY\n     jelly,\n     upland,\n     diesel\n|> AGGREGATE\n     COUNT(snake) AS ridley,\n     MIN(diesel) AS newco,\n     SUM(diesel) AS pastor\n   GROUP BY\n     jelly,\n     upland\n|> SELECT\n     jelly,\n     square,\n     ridley,\n     newco\n|> AGGREGATE\n     MAX(newco) AS blade\n   GROUP BY\n     jelly,\n     ridley\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:28,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"square",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    profit,\n    sugar,\n    knife,\n    vinyl,\n    swing\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    profit,\n    sugar,\n    knife,\n    vinyl,\n    swing\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    profit,\n    sugar,\n    knife,\n    vinyl,\n    SUM(swing) AS dobson\n  FROM cte_1\n  GROUP BY\n    profit,\n    sugar,\n    knife,\n    vinyl\n),\ncte_3 AS (\n  SELECT\n    profit,\n    sugar,\n    knife,\n    vinyl,\n    dobson,\n    cream\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(vinyl) AS fairy,\n    MAX(sugar) AS bloke,\n    MIN(knife) AS straw,\n    MIN(vinyl) AS spirit\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     profit,\n     sugar,\n     knife,\n     vinyl,\n     swing\n|> SELECT\n     profit,\n     sugar,\n     knife,\n     vinyl,\n     swing\n|> AGGREGATE\n     SUM(swing) AS dobson\n   GROUP BY\n     profit,\n     sugar,\n     knife,\n     vinyl\n|> SELECT\n     profit,\n     sugar,\n     knife,\n     vinyl,\n     dobson,\n     cream\n|> AGGREGATE\n     COUNT(vinyl) AS fairy,\n     MAX(sugar) AS bloke,\n     MIN(knife) AS straw,\n     MIN(vinyl) AS spirit\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"cream",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    fridge,\n    farmer,\n    tract,\n    vessel,\n    bonnet\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    fridge,\n    farmer,\n    tract,\n    AVG(bonnet) AS cabin,\n    MAX(bonnet) AS expiry\n  FROM cte_0\n  GROUP BY\n    fridge,\n    farmer,\n    tract,\n    vessel\n),\ncte_2 AS (\n  SELECT\n    fridge,\n    AVG(expiry) AS booze,\n    COUNT(cabin) AS stack\n  FROM cte_1\n  GROUP BY\n    fridge,\n    farmer,\n    tract\n),\ncte_3 AS (\n  SELECT\n    fridge,\n    hedge,\n    booze,\n    stack\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(stack) AS appeal,\n    MIN(stack) AS light,\n    MIN(booze) AS gallon\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     fridge,\n     farmer,\n     tract,\n     vessel,\n     bonnet\n|> AGGREGATE\n     AVG(bonnet) AS cabin,\n     MAX(bonnet) AS expiry\n   GROUP BY\n     fridge,\n     farmer,\n     tract,\n     vessel\n|> AGGREGATE\n     AVG(expiry) AS booze,\n     COUNT(cabin) AS stack\n   GROUP BY\n     fridge,\n     farmer,\n     tract\n|> SELECT\n     fridge,\n     hedge,\n     booze,\n     stack\n|> AGGREGATE\n     AVG(stack) AS appeal,\n     MIN(stack) AS light,\n     MIN(booze) AS gallon\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"hedge",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    flank,\n    maize,\n    lorry,\n    smith,\n    topic\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    flank,\n    maize,\n    lorry,\n    smith,\n    AVG(topic) AS museum,\n    SUM(topic) AS chapel,\n    COUNT(topic) AS senate,\n    MIN(topic) AS excise\n  FROM cte_0\n  GROUP BY\n    flank,\n    maize,\n    lorry,\n    smith\n),\ncte_2 AS (\n  SELECT\n    flank,\n    maize,\n    lorry,\n    smith,\n    ryder,\n    museum,\n    chapel\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    flank,\n    maize,\n    lorry,\n    smith\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    flank,\n    maize\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     flank,\n     maize,\n     lorry,\n     smith,\n     topic\n|> AGGREGATE\n     AVG(topic) AS museum,\n     SUM(topic) AS chapel,\n     COUNT(topic) AS senate,\n     MIN(topic) AS excise\n   GROUP BY\n     flank,\n     maize,\n     lorry,\n     smith\n|> SELECT\n     flank,\n     maize,\n     lorry,\n     smith,\n     ryder,\n     museum,\n     chapel\n|> SELECT\n     flank,\n     maize,\n     lorry,\n     smith\n|> SELECT\n     flank,\n     maize\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"ryder",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    memory,\n    dwarf,\n    safety,\n    baker,\n    stable\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    memory,\n    dwarf,\n    AVG(stable) AS towel,\n    SUM(stable) AS human,\n    COUNT(stable) AS mirror\n  FROM cte_0\n  GROUP BY\n    memory,\n    dwarf\n),\ncte_2 AS (\n  SELECT\n    memory,\n    dwarf,\n    AVG(mirror) AS racket,\n    AVG(human) AS jewel\n  FROM cte_1\n  GROUP BY\n    memory,\n    dwarf,\n    towel\n),\ncte_3 AS (\n  SELECT\n    memory,\n    shame,\n    dwarf\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(dwarf) AS chorus,\n    SUM(dwarf) AS dollar,\n    MAX(dwarf) AS bruce\n  FROM cte_3\n  GROUP BY\n    memory\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     memory,\n     dwarf,\n     safety,\n     baker,\n     stable\n|> AGGREGATE\n     AVG(stable) AS towel,\n     SUM(stable) AS human,\n     COUNT(stable) AS mirror\n   GROUP BY\n     memory,\n     dwarf\n|> AGGREGATE\n     AVG(mirror) AS racket,\n     AVG(human) AS jewel\n   GROUP BY\n     memory,\n     dwarf,\n     towel\n|> SELECT\n     memory,\n     shame,\n     dwarf\n|> AGGREGATE\n     COUNT(dwarf) AS chorus,\n     SUM(dwarf) AS dollar,\n     MAX(dwarf) AS bruce\n   GROUP BY\n     memory\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"shame",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    relief,\n    magic,\n    thing,\n    share,\n    tribe\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    relief,\n    SUM(thing) AS patent,\n    SUM(magic) AS ticket,\n    AVG(thing) AS tract\n  FROM cte_0\n  GROUP BY\n    relief\n),\ncte_2 AS (\n  SELECT\n    relief,\n    patent,\n    ticket,\n    AVG(tract) AS daisy,\n    SUM(tract) AS eagle\n  FROM cte_1\n  GROUP BY\n    relief,\n    patent,\n    ticket\n),\ncte_3 AS (\n  SELECT\n    relief,\n    patent,\n    clash,\n    ticket,\n    daisy\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(patent) AS grill,\n    AVG(ticket) AS vigour,\n    SUM(patent) AS laser\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     relief,\n     magic,\n     thing,\n     share,\n     tribe\n|> AGGREGATE\n     SUM(thing) AS patent,\n     SUM(magic) AS ticket,\n     AVG(thing) AS tract\n   GROUP BY\n     relief\n|> AGGREGATE\n     AVG(tract) AS daisy,\n     SUM(tract) AS eagle\n   GROUP BY\n     relief,\n     patent,\n     ticket\n|> SELECT\n     relief,\n     patent,\n     clash,\n     ticket,\n     daisy\n|> AGGREGATE\n     AVG(patent) AS grill,\n     AVG(ticket) AS vigour,\n     SUM(patent) AS laser\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"clash",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    garden,\n    laird,\n    spiral,\n    penny,\n    hassle\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    garden,\n    laird,\n    spiral,\n    penny,\n    AVG(hassle) AS sigma,\n    COUNT(hassle) AS roller,\n    SUM(hassle) AS source,\n    MIN(hassle) AS jersey\n  FROM cte_0\n  GROUP BY\n    garden,\n    laird,\n    spiral,\n    penny\n),\ncte_2 AS (\n  SELECT\n    garden,\n    laird,\n    spiral,\n    penny,\n    muscle,\n    sigma\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    garden,\n    laird,\n    spiral,\n    penny,\n    sigma\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    garden,\n    laird\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     garden,\n     laird,\n     spiral,\n     penny,\n     hassle\n|> AGGREGATE\n     AVG(hassle) AS sigma,\n     COUNT(hassle) AS roller,\n     SUM(hassle) AS source,\n     MIN(hassle) AS jersey\n   GROUP BY\n     garden,\n     laird,\n     spiral,\n     penny\n|> SELECT\n     garden,\n     laird,\n     spiral,\n     penny,\n     muscle,\n     sigma\n|> SELECT\n     garden,\n     laird,\n     spiral,\n     penny,\n     sigma\n|> SELECT\n     garden,\n     laird\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"muscle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bubble,\n    slave,\n    waist,\n    basic,\n    maple\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    bubble,\n    slave,\n    waist,\n    basic,\n    COUNT(maple) AS query,\n    SUM(maple) AS combo,\n    AVG(maple) AS saucer,\n    MIN(maple) AS plough\n  FROM cte_0\n  GROUP BY\n    bubble,\n    slave,\n    waist,\n    basic\n),\ncte_2 AS (\n  SELECT\n    bubble,\n    slave,\n    waist,\n    basic,\n    drive,\n    query,\n    combo,\n    saucer\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    bubble,\n    slave,\n    waist\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(slave) AS remedy\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bubble,\n     slave,\n     waist,\n     basic,\n     maple\n|> AGGREGATE\n     COUNT(maple) AS query,\n     SUM(maple) AS combo,\n     AVG(maple) AS saucer,\n     MIN(maple) AS plough\n   GROUP BY\n     bubble,\n     slave,\n     waist,\n     basic\n|> SELECT\n     bubble,\n     slave,\n     waist,\n     basic,\n     drive,\n     query,\n     combo,\n     saucer\n|> SELECT\n     bubble,\n     slave,\n     waist\n|> AGGREGATE\n     SUM(slave) AS remedy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"drive",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    primer,\n    abuse,\n    rector,\n    fringe,\n    nerve\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    primer,\n    abuse,\n    MAX(fringe) AS style,\n    AVG(fringe) AS sauce,\n    COUNT(fringe) AS devil\n  FROM cte_0\n  GROUP BY\n    primer,\n    abuse,\n    rector\n),\ncte_2 AS (\n  SELECT\n    primer,\n    abuse,\n    MIN(sauce) AS limit\n  FROM cte_1\n  GROUP BY\n    primer,\n    abuse\n),\ncte_3 AS (\n  SELECT\n    primer,\n    abuse,\n    limit,\n    priory\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    primer,\n    MIN(abuse) AS fiver,\n    AVG(limit) AS drink,\n    SUM(limit) AS carpet\n  FROM cte_3\n  GROUP BY\n    primer\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     primer,\n     abuse,\n     rector,\n     fringe,\n     nerve\n|> AGGREGATE\n     MAX(fringe) AS style,\n     AVG(fringe) AS sauce,\n     COUNT(fringe) AS devil\n   GROUP BY\n     primer,\n     abuse,\n     rector\n|> AGGREGATE\n     MIN(sauce) AS limit\n   GROUP BY\n     primer,\n     abuse\n|> SELECT\n     primer,\n     abuse,\n     limit,\n     priory\n|> AGGREGATE\n     MIN(abuse) AS fiver,\n     AVG(limit) AS drink,\n     SUM(limit) AS carpet\n   GROUP BY\n     primer\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"priory",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    pizza,\n    stall,\n    speed,\n    brass,\n    patsy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    pizza,\n    stall,\n    speed,\n    brass,\n    MIN(patsy) AS shore,\n    AVG(patsy) AS plate,\n    COUNT(patsy) AS event\n  FROM cte_0\n  GROUP BY\n    pizza,\n    stall,\n    speed,\n    brass\n),\ncte_2 AS (\n  SELECT\n    pizza,\n    stall,\n    speed,\n    brass,\n    shore,\n    plate,\n    scream\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    pizza,\n    stall\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    pizza,\n    MIN(stall) AS argyll,\n    COUNT(stall) AS viking\n  FROM cte_3\n  GROUP BY\n    pizza\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     pizza,\n     stall,\n     speed,\n     brass,\n     patsy\n|> AGGREGATE\n     MIN(patsy) AS shore,\n     AVG(patsy) AS plate,\n     COUNT(patsy) AS event\n   GROUP BY\n     pizza,\n     stall,\n     speed,\n     brass\n|> SELECT\n     pizza,\n     stall,\n     speed,\n     brass,\n     shore,\n     plate,\n     scream\n|> SELECT\n     pizza,\n     stall\n|> AGGREGATE\n     MIN(stall) AS argyll,\n     COUNT(stall) AS viking\n   GROUP BY\n     pizza\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"scream",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    noble,\n    glass,\n    leigh,\n    gunman,\n    truce\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    noble,\n    glass,\n    leigh,\n    AVG(gunman) AS tycoon\n  FROM cte_0\n  GROUP BY\n    noble,\n    glass,\n    leigh\n),\ncte_2 AS (\n  SELECT\n    noble,\n    AVG(tycoon) AS xerox,\n    MIN(leigh) AS clint,\n    MIN(tycoon) AS crisis,\n    MAX(tycoon) AS stint\n  FROM cte_1\n  GROUP BY\n    noble,\n    glass\n),\ncte_3 AS (\n  SELECT\n    noble,\n    deacon,\n    xerox,\n    clint\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(clint) AS barker,\n    MAX(clint) AS photo\n  FROM cte_3\n  GROUP BY\n    noble,\n    xerox\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     noble,\n     glass,\n     leigh,\n     gunman,\n     truce\n|> AGGREGATE\n     AVG(gunman) AS tycoon\n   GROUP BY\n     noble,\n     glass,\n     leigh\n|> AGGREGATE\n     AVG(tycoon) AS xerox,\n     MIN(leigh) AS clint,\n     MIN(tycoon) AS crisis,\n     MAX(tycoon) AS stint\n   GROUP BY\n     noble,\n     glass\n|> SELECT\n     noble,\n     deacon,\n     xerox,\n     clint\n|> AGGREGATE\n     SUM(clint) AS barker,\n     MAX(clint) AS photo\n   GROUP BY\n     noble,\n     xerox\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"deacon",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    chance,\n    march,\n    graham,\n    artist,\n    barman\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    chance,\n    march,\n    COUNT(barman) AS glare,\n    MAX(barman) AS cheque,\n    MAX(artist) AS cherry,\n    COUNT(graham) AS shirt\n  FROM cte_0\n  GROUP BY\n    chance,\n    march\n),\ncte_2 AS (\n  SELECT\n    chance,\n    march,\n    AVG(shirt) AS ledge,\n    COUNT(glare) AS smack\n  FROM cte_1\n  GROUP BY\n    chance,\n    march\n),\ncte_3 AS (\n  SELECT\n    cycle,\n    chance,\n    march,\n    ledge,\n    smack\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    chance,\n    march,\n    ledge,\n    smack\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     chance,\n     march,\n     graham,\n     artist,\n     barman\n|> AGGREGATE\n     COUNT(barman) AS glare,\n     MAX(barman) AS cheque,\n     MAX(artist) AS cherry,\n     COUNT(graham) AS shirt\n   GROUP BY\n     chance,\n     march\n|> AGGREGATE\n     AVG(shirt) AS ledge,\n     COUNT(glare) AS smack\n   GROUP BY\n     chance,\n     march\n|> SELECT\n     cycle,\n     chance,\n     march,\n     ledge,\n     smack\n|> SELECT\n     chance,\n     march,\n     ledge,\n     smack\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"cycle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    smell,\n    decade,\n    berry,\n    source,\n    animal\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    smell,\n    MIN(source) AS gentry,\n    COUNT(animal) AS tyler,\n    MAX(animal) AS minor,\n    SUM(animal) AS blast\n  FROM cte_0\n  GROUP BY\n    smell\n),\ncte_2 AS (\n  SELECT\n    smell,\n    gentry,\n    tyler,\n    MAX(minor) AS madge\n  FROM cte_1\n  GROUP BY\n    smell,\n    gentry,\n    tyler\n),\ncte_3 AS (\n  SELECT\n    smell,\n    gentry,\n    wharf\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(gentry) AS metro,\n    AVG(gentry) AS reeve,\n    AVG(smell) AS torch,\n    SUM(smell) AS piano\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     smell,\n     decade,\n     berry,\n     source,\n     animal\n|> AGGREGATE\n     MIN(source) AS gentry,\n     COUNT(animal) AS tyler,\n     MAX(animal) AS minor,\n     SUM(animal) AS blast\n   GROUP BY\n     smell\n|> AGGREGATE\n     MAX(minor) AS madge\n   GROUP BY\n     smell,\n     gentry,\n     tyler\n|> SELECT\n     smell,\n     gentry,\n     wharf\n|> AGGREGATE\n     COUNT(gentry) AS metro,\n     AVG(gentry) AS reeve,\n     AVG(smell) AS torch,\n     SUM(smell) AS piano\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"wharf",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    crisp,\n    garage,\n    talent,\n    pitch,\n    spell\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    crisp,\n    garage,\n    MIN(spell) AS ripple,\n    MAX(pitch) AS tramp\n  FROM cte_0\n  GROUP BY\n    crisp,\n    garage,\n    talent\n),\ncte_2 AS (\n  SELECT\n    crisp,\n    garage,\n    MAX(tramp) AS folder,\n    COUNT(tramp) AS flint,\n    COUNT(ripple) AS stall\n  FROM cte_1\n  GROUP BY\n    crisp,\n    garage\n),\ncte_3 AS (\n  SELECT\n    crisp,\n    norman,\n    garage,\n    folder\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(folder) AS barrow,\n    COUNT(folder) AS diesel\n  FROM cte_3\n  GROUP BY\n    crisp,\n    garage\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     crisp,\n     garage,\n     talent,\n     pitch,\n     spell\n|> AGGREGATE\n     MIN(spell) AS ripple,\n     MAX(pitch) AS tramp\n   GROUP BY\n     crisp,\n     garage,\n     talent\n|> AGGREGATE\n     MAX(tramp) AS folder,\n     COUNT(tramp) AS flint,\n     COUNT(ripple) AS stall\n   GROUP BY\n     crisp,\n     garage\n|> SELECT\n     crisp,\n     norman,\n     garage,\n     folder\n|> AGGREGATE\n     SUM(folder) AS barrow,\n     COUNT(folder) AS diesel\n   GROUP BY\n     crisp,\n     garage\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"norman",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    outfit,\n    spruce,\n    maxim,\n    platt,\n    print\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    outfit,\n    spruce,\n    maxim,\n    platt,\n    MAX(print) AS ballot,\n    AVG(print) AS coffin,\n    COUNT(print) AS german,\n    SUM(print) AS mandy\n  FROM cte_0\n  GROUP BY\n    outfit,\n    spruce,\n    maxim,\n    platt\n),\ncte_2 AS (\n  SELECT\n    outfit,\n    spruce,\n    maxim,\n    platt,\n    crate\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(outfit) AS wonder,\n    MAX(spruce) AS burger,\n    MAX(maxim) AS buddy\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    wonder,\n    burger\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     outfit,\n     spruce,\n     maxim,\n     platt,\n     print\n|> AGGREGATE\n     MAX(print) AS ballot,\n     AVG(print) AS coffin,\n     COUNT(print) AS german,\n     SUM(print) AS mandy\n   GROUP BY\n     outfit,\n     spruce,\n     maxim,\n     platt\n|> SELECT\n     outfit,\n     spruce,\n     maxim,\n     platt,\n     crate\n|> AGGREGATE\n     SUM(outfit) AS wonder,\n     MAX(spruce) AS burger,\n     MAX(maxim) AS buddy\n|> SELECT\n     wonder,\n     burger\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"crate",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    legion,\n    letter,\n    steak,\n    second,\n    recall\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    legion,\n    letter,\n    SUM(steak) AS tenant,\n    MIN(recall) AS tonne,\n    MIN(steak) AS hostel\n  FROM cte_0\n  GROUP BY\n    legion,\n    letter\n),\ncte_2 AS (\n  SELECT\n    legion,\n    letter,\n    AVG(tonne) AS speech,\n    MIN(hostel) AS array\n  FROM cte_1\n  GROUP BY\n    legion,\n    letter,\n    tenant\n),\ncte_3 AS (\n  SELECT\n    legion,\n    rhino,\n    letter,\n    speech\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    legion,\n    SUM(letter) AS graph,\n    AVG(letter) AS cloth\n  FROM cte_3\n  GROUP BY\n    legion\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     legion,\n     letter,\n     steak,\n     second,\n     recall\n|> AGGREGATE\n     SUM(steak) AS tenant,\n     MIN(recall) AS tonne,\n     MIN(steak) AS hostel\n   GROUP BY\n     legion,\n     letter\n|> AGGREGATE\n     AVG(tonne) AS speech,\n     MIN(hostel) AS array\n   GROUP BY\n     legion,\n     letter,\n     tenant\n|> SELECT\n     legion,\n     rhino,\n     letter,\n     speech\n|> AGGREGATE\n     SUM(letter) AS graph,\n     AVG(letter) AS cloth\n   GROUP BY\n     legion\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"rhino",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    export,\n    rebate,\n    board,\n    policy,\n    origin\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    export,\n    SUM(policy) AS casino,\n    AVG(origin) AS issue,\n    MIN(policy) AS grief\n  FROM cte_0\n  GROUP BY\n    export\n),\ncte_2 AS (\n  SELECT\n    export,\n    casino,\n    issue,\n    COUNT(grief) AS sphere,\n    AVG(grief) AS bonus\n  FROM cte_1\n  GROUP BY\n    export,\n    casino,\n    issue\n),\ncte_3 AS (\n  SELECT\n    export,\n    casino,\n    poppy,\n    issue,\n    sphere\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    export,\n    AVG(sphere) AS norma\n  FROM cte_3\n  GROUP BY\n    export,\n    casino,\n    issue\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     export,\n     rebate,\n     board,\n     policy,\n     origin\n|> AGGREGATE\n     SUM(policy) AS casino,\n     AVG(origin) AS issue,\n     MIN(policy) AS grief\n   GROUP BY\n     export\n|> AGGREGATE\n     COUNT(grief) AS sphere,\n     AVG(grief) AS bonus\n   GROUP BY\n     export,\n     casino,\n     issue\n|> SELECT\n     export,\n     casino,\n     poppy,\n     issue,\n     sphere\n|> AGGREGATE\n     AVG(sphere) AS norma\n   GROUP BY\n     export,\n     casino,\n     issue\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"poppy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    arrest,\n    marble,\n    plaque,\n    rifle,\n    joanna\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    arrest,\n    marble,\n    MAX(joanna) AS dairy,\n    COUNT(plaque) AS fringe\n  FROM cte_0\n  GROUP BY\n    arrest,\n    marble\n),\ncte_2 AS (\n  SELECT\n    arrest,\n    marble,\n    MAX(fringe) AS kylie,\n    COUNT(fringe) AS warden,\n    SUM(fringe) AS witney\n  FROM cte_1\n  GROUP BY\n    arrest,\n    marble,\n    dairy\n),\ncte_3 AS (\n  SELECT\n    arrest,\n    digest,\n    marble,\n    kylie\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(marble) AS brass,\n    MIN(kylie) AS place,\n    SUM(kylie) AS altar\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     arrest,\n     marble,\n     plaque,\n     rifle,\n     joanna\n|> AGGREGATE\n     MAX(joanna) AS dairy,\n     COUNT(plaque) AS fringe\n   GROUP BY\n     arrest,\n     marble\n|> AGGREGATE\n     MAX(fringe) AS kylie,\n     COUNT(fringe) AS warden,\n     SUM(fringe) AS witney\n   GROUP BY\n     arrest,\n     marble,\n     dairy\n|> SELECT\n     arrest,\n     digest,\n     marble,\n     kylie\n|> AGGREGATE\n     SUM(marble) AS brass,\n     MIN(kylie) AS place,\n     SUM(kylie) AS altar\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"digest",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    botany,\n    combo,\n    temple,\n    orange,\n    money\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    botany,\n    combo,\n    temple,\n    orange,\n    money\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    botany,\n    combo,\n    temple,\n    orange,\n    MAX(money) AS flask,\n    MIN(money) AS spread,\n    COUNT(money) AS opium\n  FROM cte_1\n  GROUP BY\n    botany,\n    combo,\n    temple,\n    orange\n),\ncte_3 AS (\n  SELECT\n    botany,\n    claret,\n    combo,\n    temple,\n    orange,\n    flask,\n    spread,\n    opium\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    botany,\n    combo\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     botany,\n     combo,\n     temple,\n     orange,\n     money\n|> SELECT\n     botany,\n     combo,\n     temple,\n     orange,\n     money\n|> AGGREGATE\n     MAX(money) AS flask,\n     MIN(money) AS spread,\n     COUNT(money) AS opium\n   GROUP BY\n     botany,\n     combo,\n     temple,\n     orange\n|> SELECT\n     botany,\n     claret,\n     combo,\n     temple,\n     orange,\n     flask,\n     spread,\n     opium\n|> SELECT\n     botany,\n     combo\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"claret",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    perry,\n    clause,\n    middle,\n    warmth,\n    tablet\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    perry,\n    clause,\n    AVG(warmth) AS straw,\n    SUM(middle) AS creek,\n    MIN(tablet) AS laugh\n  FROM cte_0\n  GROUP BY\n    perry,\n    clause\n),\ncte_2 AS (\n  SELECT\n    perry,\n    clause,\n    COUNT(creek) AS taboo,\n    MIN(creek) AS priory\n  FROM cte_1\n  GROUP BY\n    perry,\n    clause,\n    straw\n),\ncte_3 AS (\n  SELECT\n    perry,\n    jewel,\n    clause,\n    taboo,\n    priory\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    perry,\n    COUNT(taboo) AS joanna\n  FROM cte_3\n  GROUP BY\n    perry,\n    clause\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     perry,\n     clause,\n     middle,\n     warmth,\n     tablet\n|> AGGREGATE\n     AVG(warmth) AS straw,\n     SUM(middle) AS creek,\n     MIN(tablet) AS laugh\n   GROUP BY\n     perry,\n     clause\n|> AGGREGATE\n     COUNT(creek) AS taboo,\n     MIN(creek) AS priory\n   GROUP BY\n     perry,\n     clause,\n     straw\n|> SELECT\n     perry,\n     jewel,\n     clause,\n     taboo,\n     priory\n|> AGGREGATE\n     COUNT(taboo) AS joanna\n   GROUP BY\n     perry,\n     clause\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"jewel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    soviet,\n    check,\n    kidney,\n    beard,\n    length\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    soviet,\n    check,\n    kidney,\n    COUNT(length) AS second\n  FROM cte_0\n  GROUP BY\n    soviet,\n    check,\n    kidney,\n    beard\n),\ncte_2 AS (\n  SELECT\n    soviet,\n    COUNT(second) AS dwarf,\n    AVG(check) AS senate,\n    MAX(second) AS diary\n  FROM cte_1\n  GROUP BY\n    soviet\n),\ncte_3 AS (\n  SELECT\n    soviet,\n    dwarf,\n    senate,\n    clinic\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    soviet,\n    dwarf,\n    AVG(senate) AS vision,\n    SUM(senate) AS genus\n  FROM cte_3\n  GROUP BY\n    soviet,\n    dwarf\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     soviet,\n     check,\n     kidney,\n     beard,\n     length\n|> AGGREGATE\n     COUNT(length) AS second\n   GROUP BY\n     soviet,\n     check,\n     kidney,\n     beard\n|> AGGREGATE\n     COUNT(second) AS dwarf,\n     AVG(check) AS senate,\n     MAX(second) AS diary\n   GROUP BY\n     soviet\n|> SELECT\n     soviet,\n     dwarf,\n     senate,\n     clinic\n|> AGGREGATE\n     AVG(senate) AS vision,\n     SUM(senate) AS genus\n   GROUP BY\n     soviet,\n     dwarf\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:28,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"clinic",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    retina,\n    fodder,\n    gloss,\n    excise,\n    steel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    retina,\n    fodder,\n    gloss,\n    MAX(excise) AS doctor\n  FROM cte_0\n  GROUP BY\n    retina,\n    fodder,\n    gloss\n),\ncte_2 AS (\n  SELECT\n    retina,\n    AVG(doctor) AS thief,\n    COUNT(fodder) AS elaine,\n    MIN(gloss) AS stereo,\n    MAX(fodder) AS buddy\n  FROM cte_1\n  GROUP BY\n    retina\n),\ncte_3 AS (\n  SELECT\n    retina,\n    thief,\n    purity,\n    elaine,\n    stereo\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(stereo) AS murray,\n    MAX(stereo) AS spate,\n    MIN(retina) AS stitch\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     retina,\n     fodder,\n     gloss,\n     excise,\n     steel\n|> AGGREGATE\n     MAX(excise) AS doctor\n   GROUP BY\n     retina,\n     fodder,\n     gloss\n|> AGGREGATE\n     AVG(doctor) AS thief,\n     COUNT(fodder) AS elaine,\n     MIN(gloss) AS stereo,\n     MAX(fodder) AS buddy\n   GROUP BY\n     retina\n|> SELECT\n     retina,\n     thief,\n     purity,\n     elaine,\n     stereo\n|> AGGREGATE\n     MIN(stereo) AS murray,\n     MAX(stereo) AS spate,\n     MIN(retina) AS stitch\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"purity",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    theme,\n    centre,\n    glove,\n    bullet,\n    supper\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    theme,\n    COUNT(bullet) AS jumper,\n    MIN(supper) AS wisdom,\n    AVG(supper) AS butler\n  FROM cte_0\n  GROUP BY\n    theme,\n    centre,\n    glove\n),\ncte_2 AS (\n  SELECT\n    theme,\n    jumper,\n    wisdom,\n    COUNT(butler) AS clown\n  FROM cte_1\n  GROUP BY\n    theme,\n    jumper,\n    wisdom\n),\ncte_3 AS (\n  SELECT\n    theme,\n    jumper,\n    turtle\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(jumper) AS finch,\n    COUNT(jumper) AS verse,\n    SUM(jumper) AS pastry,\n    SUM(theme) AS fulham\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     theme,\n     centre,\n     glove,\n     bullet,\n     supper\n|> AGGREGATE\n     COUNT(bullet) AS jumper,\n     MIN(supper) AS wisdom,\n     AVG(supper) AS butler\n   GROUP BY\n     theme,\n     centre,\n     glove\n|> AGGREGATE\n     COUNT(butler) AS clown\n   GROUP BY\n     theme,\n     jumper,\n     wisdom\n|> SELECT\n     theme,\n     jumper,\n     turtle\n|> AGGREGATE\n     MIN(jumper) AS finch,\n     COUNT(jumper) AS verse,\n     SUM(jumper) AS pastry,\n     SUM(theme) AS fulham\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"SELECT_UNKNOWN",totalInformationDifference:4,unknownName:"turtle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    barnet,\n    cheque,\n    field,\n    gully,\n    shadow\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    barnet,\n    cheque,\n    field,\n    gully,\n    COUNT(shadow) AS maggie\n  FROM cte_0\n  GROUP BY\n    barnet,\n    cheque,\n    field,\n    gully\n),\ncte_2 AS (\n  SELECT\n    barnet,\n    cheque,\n    field,\n    gully,\n    AVG(maggie) AS motif\n  FROM cte_1\n  GROUP BY\n    barnet,\n    cheque,\n    field,\n    gully\n),\ncte_3 AS (\n  SELECT\n    niche,\n    barnet,\n    cheque\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    barnet,\n    MIN(cheque) AS stuff,\n    AVG(cheque) AS carter\n  FROM cte_3\n  GROUP BY\n    barnet\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     barnet,\n     cheque,\n     field,\n     gully,\n     shadow\n|> AGGREGATE\n     COUNT(shadow) AS maggie\n   GROUP BY\n     barnet,\n     cheque,\n     field,\n     gully\n|> AGGREGATE\n     AVG(maggie) AS motif\n   GROUP BY\n     barnet,\n     cheque,\n     field,\n     gully\n|> SELECT\n     niche,\n     barnet,\n     cheque\n|> AGGREGATE\n     MIN(cheque) AS stuff,\n     AVG(cheque) AS carter\n   GROUP BY\n     barnet\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"niche",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    remand,\n    gloom,\n    female,\n    mosque,\n    panel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    remand,\n    gloom,\n    female,\n    mosque,\n    AVG(panel) AS drift\n  FROM cte_0\n  GROUP BY\n    remand,\n    gloom,\n    female,\n    mosque\n),\ncte_2 AS (\n  SELECT\n    remand,\n    gloom,\n    female,\n    mosque,\n    AVG(drift) AS plank\n  FROM cte_1\n  GROUP BY\n    remand,\n    gloom,\n    female,\n    mosque\n),\ncte_3 AS (\n  SELECT\n    heath,\n    remand,\n    gloom,\n    female,\n    mosque,\n    plank\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    remand,\n    gloom,\n    female\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     remand,\n     gloom,\n     female,\n     mosque,\n     panel\n|> AGGREGATE\n     AVG(panel) AS drift\n   GROUP BY\n     remand,\n     gloom,\n     female,\n     mosque\n|> AGGREGATE\n     AVG(drift) AS plank\n   GROUP BY\n     remand,\n     gloom,\n     female,\n     mosque\n|> SELECT\n     heath,\n     remand,\n     gloom,\n     female,\n     mosque,\n     plank\n|> SELECT\n     remand,\n     gloom,\n     female\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"heath",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    gauge,\n    header,\n    dexter,\n    murphy,\n    curse\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    gauge,\n    header,\n    dexter,\n    murphy,\n    MAX(curse) AS virus\n  FROM cte_0\n  GROUP BY\n    gauge,\n    header,\n    dexter,\n    murphy\n),\ncte_2 AS (\n  SELECT\n    gauge,\n    header,\n    dexter,\n    murphy,\n    COUNT(virus) AS dairy\n  FROM cte_1\n  GROUP BY\n    gauge,\n    header,\n    dexter,\n    murphy\n),\ncte_3 AS (\n  SELECT\n    aggie,\n    gauge,\n    header,\n    dexter,\n    murphy,\n    dairy\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    gauge,\n    header,\n    dexter\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     gauge,\n     header,\n     dexter,\n     murphy,\n     curse\n|> AGGREGATE\n     MAX(curse) AS virus\n   GROUP BY\n     gauge,\n     header,\n     dexter,\n     murphy\n|> AGGREGATE\n     COUNT(virus) AS dairy\n   GROUP BY\n     gauge,\n     header,\n     dexter,\n     murphy\n|> SELECT\n     aggie,\n     gauge,\n     header,\n     dexter,\n     murphy,\n     dairy\n|> SELECT\n     gauge,\n     header,\n     dexter\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"aggie",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    spoon,\n    chile,\n    fever,\n    piper,\n    swell\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    spoon,\n    chile,\n    fever,\n    piper,\n    COUNT(swell) AS helium\n  FROM cte_0\n  GROUP BY\n    spoon,\n    chile,\n    fever,\n    piper\n),\ncte_2 AS (\n  SELECT\n    spoon,\n    chile,\n    fever,\n    piper,\n    MAX(helium) AS canvas\n  FROM cte_1\n  GROUP BY\n    spoon,\n    chile,\n    fever,\n    piper\n),\ncte_3 AS (\n  SELECT\n    seller,\n    spoon,\n    chile,\n    fever\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    spoon,\n    MIN(fever) AS ghost\n  FROM cte_3\n  GROUP BY\n    spoon,\n    chile\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     spoon,\n     chile,\n     fever,\n     piper,\n     swell\n|> AGGREGATE\n     COUNT(swell) AS helium\n   GROUP BY\n     spoon,\n     chile,\n     fever,\n     piper\n|> AGGREGATE\n     MAX(helium) AS canvas\n   GROUP BY\n     spoon,\n     chile,\n     fever,\n     piper\n|> SELECT\n     seller,\n     spoon,\n     chile,\n     fever\n|> AGGREGATE\n     MIN(fever) AS ghost\n   GROUP BY\n     spoon,\n     chile\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"seller",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rifle,\n    indie,\n    scrum,\n    clone,\n    shame\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    rifle,\n    indie,\n    scrum,\n    clone,\n    SUM(shame) AS police\n  FROM cte_0\n  GROUP BY\n    rifle,\n    indie,\n    scrum,\n    clone\n),\ncte_2 AS (\n  SELECT\n    rifle,\n    indie,\n    scrum,\n    clone,\n    SUM(police) AS hazel\n  FROM cte_1\n  GROUP BY\n    rifle,\n    indie,\n    scrum,\n    clone\n),\ncte_3 AS (\n  SELECT\n    greek,\n    rifle,\n    indie\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    rifle,\n    SUM(indie) AS depth,\n    MIN(indie) AS irene\n  FROM cte_3\n  GROUP BY\n    rifle\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rifle,\n     indie,\n     scrum,\n     clone,\n     shame\n|> AGGREGATE\n     SUM(shame) AS police\n   GROUP BY\n     rifle,\n     indie,\n     scrum,\n     clone\n|> AGGREGATE\n     SUM(police) AS hazel\n   GROUP BY\n     rifle,\n     indie,\n     scrum,\n     clone\n|> SELECT\n     greek,\n     rifle,\n     indie\n|> AGGREGATE\n     SUM(indie) AS depth,\n     MIN(indie) AS irene\n   GROUP BY\n     rifle\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"greek",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tenor,\n    craven,\n    piper,\n    defect,\n    break\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    tenor,\n    craven,\n    piper,\n    defect,\n    MIN(break) AS fungus\n  FROM cte_0\n  GROUP BY\n    tenor,\n    craven,\n    piper,\n    defect\n),\ncte_2 AS (\n  SELECT\n    tenor,\n    craven,\n    piper,\n    defect,\n    SUM(fungus) AS person\n  FROM cte_1\n  GROUP BY\n    tenor,\n    craven,\n    piper,\n    defect\n),\ncte_3 AS (\n  SELECT\n    valley,\n    tenor,\n    craven\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(tenor) AS truth,\n    COUNT(craven) AS yield,\n    MIN(craven) AS minus\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tenor,\n     craven,\n     piper,\n     defect,\n     break\n|> AGGREGATE\n     MIN(break) AS fungus\n   GROUP BY\n     tenor,\n     craven,\n     piper,\n     defect\n|> AGGREGATE\n     SUM(fungus) AS person\n   GROUP BY\n     tenor,\n     craven,\n     piper,\n     defect\n|> SELECT\n     valley,\n     tenor,\n     craven\n|> AGGREGATE\n     COUNT(tenor) AS truth,\n     COUNT(craven) AS yield,\n     MIN(craven) AS minus\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"valley",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    olive,\n    pardon,\n    actor,\n    world,\n    noble\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    olive,\n    pardon,\n    actor,\n    world,\n    SUM(noble) AS cohen\n  FROM cte_0\n  GROUP BY\n    olive,\n    pardon,\n    actor,\n    world\n),\ncte_2 AS (\n  SELECT\n    olive,\n    pardon,\n    actor,\n    world,\n    COUNT(cohen) AS market\n  FROM cte_1\n  GROUP BY\n    olive,\n    pardon,\n    actor,\n    world\n),\ncte_3 AS (\n  SELECT\n    chill,\n    olive,\n    pardon,\n    actor\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    olive,\n    MIN(actor) AS grave\n  FROM cte_3\n  GROUP BY\n    olive,\n    pardon\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     olive,\n     pardon,\n     actor,\n     world,\n     noble\n|> AGGREGATE\n     SUM(noble) AS cohen\n   GROUP BY\n     olive,\n     pardon,\n     actor,\n     world\n|> AGGREGATE\n     COUNT(cohen) AS market\n   GROUP BY\n     olive,\n     pardon,\n     actor,\n     world\n|> SELECT\n     chill,\n     olive,\n     pardon,\n     actor\n|> AGGREGATE\n     MIN(actor) AS grave\n   GROUP BY\n     olive,\n     pardon\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"chill",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    future,\n    flask,\n    bloke,\n    digest,\n    hooker\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    future,\n    flask,\n    bloke,\n    digest,\n    SUM(hooker) AS chant\n  FROM cte_0\n  GROUP BY\n    future,\n    flask,\n    bloke,\n    digest\n),\ncte_2 AS (\n  SELECT\n    future,\n    flask,\n    bloke,\n    digest,\n    MIN(chant) AS kelly\n  FROM cte_1\n  GROUP BY\n    future,\n    flask,\n    bloke,\n    digest\n),\ncte_3 AS (\n  SELECT\n    saucer,\n    future,\n    flask\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    future,\n    COUNT(flask) AS sphere,\n    SUM(flask) AS resale\n  FROM cte_3\n  GROUP BY\n    future\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     future,\n     flask,\n     bloke,\n     digest,\n     hooker\n|> AGGREGATE\n     SUM(hooker) AS chant\n   GROUP BY\n     future,\n     flask,\n     bloke,\n     digest\n|> AGGREGATE\n     MIN(chant) AS kelly\n   GROUP BY\n     future,\n     flask,\n     bloke,\n     digest\n|> SELECT\n     saucer,\n     future,\n     flask\n|> AGGREGATE\n     COUNT(flask) AS sphere,\n     SUM(flask) AS resale\n   GROUP BY\n     future\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"saucer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    enigma,\n    blaze,\n    duvet,\n    papacy,\n    affect\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    enigma,\n    blaze,\n    duvet,\n    papacy,\n    AVG(affect) AS fridge\n  FROM cte_0\n  GROUP BY\n    enigma,\n    blaze,\n    duvet,\n    papacy\n),\ncte_2 AS (\n  SELECT\n    enigma,\n    blaze,\n    duvet,\n    papacy,\n    SUM(fridge) AS moment\n  FROM cte_1\n  GROUP BY\n    enigma,\n    blaze,\n    duvet,\n    papacy\n),\ncte_3 AS (\n  SELECT\n    dancer,\n    enigma,\n    blaze,\n    duvet\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    enigma,\n    COUNT(duvet) AS image\n  FROM cte_3\n  GROUP BY\n    enigma,\n    blaze\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     enigma,\n     blaze,\n     duvet,\n     papacy,\n     affect\n|> AGGREGATE\n     AVG(affect) AS fridge\n   GROUP BY\n     enigma,\n     blaze,\n     duvet,\n     papacy\n|> AGGREGATE\n     SUM(fridge) AS moment\n   GROUP BY\n     enigma,\n     blaze,\n     duvet,\n     papacy\n|> SELECT\n     dancer,\n     enigma,\n     blaze,\n     duvet\n|> AGGREGATE\n     COUNT(duvet) AS image\n   GROUP BY\n     enigma,\n     blaze\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"dancer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    photo,\n    pledge,\n    cloak,\n    locke,\n    coast\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    photo,\n    pledge,\n    cloak,\n    locke,\n    COUNT(coast) AS pixel\n  FROM cte_0\n  GROUP BY\n    photo,\n    pledge,\n    cloak,\n    locke\n),\ncte_2 AS (\n  SELECT\n    photo,\n    pledge,\n    cloak,\n    locke,\n    AVG(pixel) AS farmer\n  FROM cte_1\n  GROUP BY\n    photo,\n    pledge,\n    cloak,\n    locke\n),\ncte_3 AS (\n  SELECT\n    camel,\n    photo,\n    pledge\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(photo) AS wrist,\n    AVG(photo) AS sewer,\n    AVG(pledge) AS relish\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     photo,\n     pledge,\n     cloak,\n     locke,\n     coast\n|> AGGREGATE\n     COUNT(coast) AS pixel\n   GROUP BY\n     photo,\n     pledge,\n     cloak,\n     locke\n|> AGGREGATE\n     AVG(pixel) AS farmer\n   GROUP BY\n     photo,\n     pledge,\n     cloak,\n     locke\n|> SELECT\n     camel,\n     photo,\n     pledge\n|> AGGREGATE\n     COUNT(photo) AS wrist,\n     AVG(photo) AS sewer,\n     AVG(pledge) AS relish\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"camel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    viking,\n    waist,\n    reason,\n    streak,\n    sexism\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    viking,\n    waist,\n    reason,\n    streak,\n    AVG(sexism) AS wreck\n  FROM cte_0\n  GROUP BY\n    viking,\n    waist,\n    reason,\n    streak\n),\ncte_2 AS (\n  SELECT\n    viking,\n    waist,\n    reason,\n    streak,\n    COUNT(wreck) AS turbo\n  FROM cte_1\n  GROUP BY\n    viking,\n    waist,\n    reason,\n    streak\n),\ncte_3 AS (\n  SELECT\n    urine,\n    viking,\n    waist,\n    reason,\n    streak\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    viking,\n    waist,\n    reason,\n    streak\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     viking,\n     waist,\n     reason,\n     streak,\n     sexism\n|> AGGREGATE\n     AVG(sexism) AS wreck\n   GROUP BY\n     viking,\n     waist,\n     reason,\n     streak\n|> AGGREGATE\n     COUNT(wreck) AS turbo\n   GROUP BY\n     viking,\n     waist,\n     reason,\n     streak\n|> SELECT\n     urine,\n     viking,\n     waist,\n     reason,\n     streak\n|> SELECT\n     viking,\n     waist,\n     reason,\n     streak\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"urine",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    schema,\n    alaska,\n    runway,\n    hedge,\n    fender\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    schema,\n    alaska,\n    runway,\n    hedge,\n    AVG(fender) AS brett\n  FROM cte_0\n  GROUP BY\n    schema,\n    alaska,\n    runway,\n    hedge\n),\ncte_2 AS (\n  SELECT\n    schema,\n    alaska,\n    runway,\n    hedge,\n    MIN(brett) AS drain\n  FROM cte_1\n  GROUP BY\n    schema,\n    alaska,\n    runway,\n    hedge\n),\ncte_3 AS (\n  SELECT\n    genre,\n    schema,\n    alaska,\n    runway,\n    hedge\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(hedge) AS swell\n  FROM cte_3\n  GROUP BY\n    schema,\n    alaska\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     schema,\n     alaska,\n     runway,\n     hedge,\n     fender\n|> AGGREGATE\n     AVG(fender) AS brett\n   GROUP BY\n     schema,\n     alaska,\n     runway,\n     hedge\n|> AGGREGATE\n     MIN(brett) AS drain\n   GROUP BY\n     schema,\n     alaska,\n     runway,\n     hedge\n|> SELECT\n     genre,\n     schema,\n     alaska,\n     runway,\n     hedge\n|> AGGREGATE\n     MAX(hedge) AS swell\n   GROUP BY\n     schema,\n     alaska\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"genre",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    truce,\n    cooker,\n    viking,\n    runner,\n    glint\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    truce,\n    cooker,\n    viking,\n    runner,\n    AVG(glint) AS bobby\n  FROM cte_0\n  GROUP BY\n    truce,\n    cooker,\n    viking,\n    runner\n),\ncte_2 AS (\n  SELECT\n    truce,\n    cooker,\n    viking,\n    runner,\n    MIN(bobby) AS piety\n  FROM cte_1\n  GROUP BY\n    truce,\n    cooker,\n    viking,\n    runner\n),\ncte_3 AS (\n  SELECT\n    hotel,\n    truce,\n    cooker,\n    viking\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(viking) AS repeal,\n    SUM(cooker) AS banana\n  FROM cte_3\n  GROUP BY\n    truce\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     truce,\n     cooker,\n     viking,\n     runner,\n     glint\n|> AGGREGATE\n     AVG(glint) AS bobby\n   GROUP BY\n     truce,\n     cooker,\n     viking,\n     runner\n|> AGGREGATE\n     MIN(bobby) AS piety\n   GROUP BY\n     truce,\n     cooker,\n     viking,\n     runner\n|> SELECT\n     hotel,\n     truce,\n     cooker,\n     viking\n|> AGGREGATE\n     MIN(viking) AS repeal,\n     SUM(cooker) AS banana\n   GROUP BY\n     truce\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"hotel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tablet,\n    sugar,\n    vanity,\n    foster,\n    twist\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    tablet,\n    sugar,\n    vanity,\n    foster,\n    MIN(twist) AS shaikh\n  FROM cte_0\n  GROUP BY\n    tablet,\n    sugar,\n    vanity,\n    foster\n),\ncte_2 AS (\n  SELECT\n    tablet,\n    sugar,\n    vanity,\n    foster,\n    COUNT(shaikh) AS empire\n  FROM cte_1\n  GROUP BY\n    tablet,\n    sugar,\n    vanity,\n    foster\n),\ncte_3 AS (\n  SELECT\n    finch,\n    tablet,\n    sugar,\n    vanity,\n    foster,\n    empire\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    tablet,\n    sugar,\n    vanity\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tablet,\n     sugar,\n     vanity,\n     foster,\n     twist\n|> AGGREGATE\n     MIN(twist) AS shaikh\n   GROUP BY\n     tablet,\n     sugar,\n     vanity,\n     foster\n|> AGGREGATE\n     COUNT(shaikh) AS empire\n   GROUP BY\n     tablet,\n     sugar,\n     vanity,\n     foster\n|> SELECT\n     finch,\n     tablet,\n     sugar,\n     vanity,\n     foster,\n     empire\n|> SELECT\n     tablet,\n     sugar,\n     vanity\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"finch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    piano,\n    clash,\n    rhino,\n    tenure,\n    norma\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    piano,\n    clash,\n    rhino,\n    tenure,\n    MIN(norma) AS white\n  FROM cte_0\n  GROUP BY\n    piano,\n    clash,\n    rhino,\n    tenure\n),\ncte_2 AS (\n  SELECT\n    piano,\n    clash,\n    rhino,\n    tenure,\n    MAX(white) AS motive\n  FROM cte_1\n  GROUP BY\n    piano,\n    clash,\n    rhino,\n    tenure\n),\ncte_3 AS (\n  SELECT\n    empire,\n    piano,\n    clash,\n    rhino,\n    tenure\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(rhino) AS debit\n  FROM cte_3\n  GROUP BY\n    piano,\n    clash\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     piano,\n     clash,\n     rhino,\n     tenure,\n     norma\n|> AGGREGATE\n     MIN(norma) AS white\n   GROUP BY\n     piano,\n     clash,\n     rhino,\n     tenure\n|> AGGREGATE\n     MAX(white) AS motive\n   GROUP BY\n     piano,\n     clash,\n     rhino,\n     tenure\n|> SELECT\n     empire,\n     piano,\n     clash,\n     rhino,\n     tenure\n|> AGGREGATE\n     SUM(rhino) AS debit\n   GROUP BY\n     piano,\n     clash\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"empire",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    truth,\n    bible,\n    chuck,\n    derby,\n    dexter\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    truth,\n    bible,\n    chuck,\n    derby,\n    MIN(dexter) AS apollo\n  FROM cte_0\n  GROUP BY\n    truth,\n    bible,\n    chuck,\n    derby\n),\ncte_2 AS (\n  SELECT\n    truth,\n    bible,\n    chuck,\n    derby,\n    COUNT(apollo) AS quota\n  FROM cte_1\n  GROUP BY\n    truth,\n    bible,\n    chuck,\n    derby\n),\ncte_3 AS (\n  SELECT\n    grief,\n    truth,\n    bible,\n    chuck,\n    derby,\n    quota\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    truth,\n    bible,\n    chuck\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     truth,\n     bible,\n     chuck,\n     derby,\n     dexter\n|> AGGREGATE\n     MIN(dexter) AS apollo\n   GROUP BY\n     truth,\n     bible,\n     chuck,\n     derby\n|> AGGREGATE\n     COUNT(apollo) AS quota\n   GROUP BY\n     truth,\n     bible,\n     chuck,\n     derby\n|> SELECT\n     grief,\n     truth,\n     bible,\n     chuck,\n     derby,\n     quota\n|> SELECT\n     truth,\n     bible,\n     chuck\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"grief",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    menace,\n    racism,\n    squad,\n    arena,\n    coupon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    menace,\n    racism,\n    squad,\n    arena,\n    SUM(coupon) AS outlay\n  FROM cte_0\n  GROUP BY\n    menace,\n    racism,\n    squad,\n    arena\n),\ncte_2 AS (\n  SELECT\n    menace,\n    racism,\n    squad,\n    arena,\n    SUM(outlay) AS course\n  FROM cte_1\n  GROUP BY\n    menace,\n    racism,\n    squad,\n    arena\n),\ncte_3 AS (\n  SELECT\n    caddie,\n    menace,\n    racism,\n    squad,\n    arena,\n    course\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(racism) AS lynch\n  FROM cte_3\n  GROUP BY\n    menace\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     menace,\n     racism,\n     squad,\n     arena,\n     coupon\n|> AGGREGATE\n     SUM(coupon) AS outlay\n   GROUP BY\n     menace,\n     racism,\n     squad,\n     arena\n|> AGGREGATE\n     SUM(outlay) AS course\n   GROUP BY\n     menace,\n     racism,\n     squad,\n     arena\n|> SELECT\n     caddie,\n     menace,\n     racism,\n     squad,\n     arena,\n     course\n|> AGGREGATE\n     COUNT(racism) AS lynch\n   GROUP BY\n     menace\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"caddie",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    louvre,\n    climax,\n    rifle,\n    fridge,\n    voice\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    louvre,\n    climax,\n    rifle,\n    fridge,\n    SUM(voice) AS recall\n  FROM cte_0\n  GROUP BY\n    louvre,\n    climax,\n    rifle,\n    fridge\n),\ncte_2 AS (\n  SELECT\n    louvre,\n    climax,\n    rifle,\n    fridge,\n    MAX(recall) AS flick\n  FROM cte_1\n  GROUP BY\n    louvre,\n    climax,\n    rifle,\n    fridge\n),\ncte_3 AS (\n  SELECT\n    dread,\n    louvre,\n    climax,\n    rifle,\n    fridge,\n    flick\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    louvre,\n    climax,\n    rifle\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     louvre,\n     climax,\n     rifle,\n     fridge,\n     voice\n|> AGGREGATE\n     SUM(voice) AS recall\n   GROUP BY\n     louvre,\n     climax,\n     rifle,\n     fridge\n|> AGGREGATE\n     MAX(recall) AS flick\n   GROUP BY\n     louvre,\n     climax,\n     rifle,\n     fridge\n|> SELECT\n     dread,\n     louvre,\n     climax,\n     rifle,\n     fridge,\n     flick\n|> SELECT\n     louvre,\n     climax,\n     rifle\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"dread",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    donkey,\n    shell,\n    ferry,\n    mercer,\n    rubber\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    donkey,\n    shell,\n    ferry,\n    mercer,\n    COUNT(rubber) AS fellow\n  FROM cte_0\n  GROUP BY\n    donkey,\n    shell,\n    ferry,\n    mercer\n),\ncte_2 AS (\n  SELECT\n    donkey,\n    shell,\n    ferry,\n    mercer,\n    AVG(fellow) AS faith\n  FROM cte_1\n  GROUP BY\n    donkey,\n    shell,\n    ferry,\n    mercer\n),\ncte_3 AS (\n  SELECT\n    puppet,\n    donkey,\n    shell\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(shell) AS player,\n    MIN(shell) AS strife,\n    SUM(donkey) AS exodus\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     donkey,\n     shell,\n     ferry,\n     mercer,\n     rubber\n|> AGGREGATE\n     COUNT(rubber) AS fellow\n   GROUP BY\n     donkey,\n     shell,\n     ferry,\n     mercer\n|> AGGREGATE\n     AVG(fellow) AS faith\n   GROUP BY\n     donkey,\n     shell,\n     ferry,\n     mercer\n|> SELECT\n     puppet,\n     donkey,\n     shell\n|> AGGREGATE\n     AVG(shell) AS player,\n     MIN(shell) AS strife,\n     SUM(donkey) AS exodus\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"puppet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bully,\n    force,\n    finale,\n    talbot,\n    dinner\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    bully,\n    force,\n    finale,\n    talbot,\n    SUM(dinner) AS sweep\n  FROM cte_0\n  GROUP BY\n    bully,\n    force,\n    finale,\n    talbot\n),\ncte_2 AS (\n  SELECT\n    bully,\n    force,\n    finale,\n    talbot,\n    COUNT(sweep) AS mobile\n  FROM cte_1\n  GROUP BY\n    bully,\n    force,\n    finale,\n    talbot\n),\ncte_3 AS (\n  SELECT\n    climax,\n    bully,\n    force,\n    finale\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(finale) AS march,\n    MIN(finale) AS dexter\n  FROM cte_3\n  GROUP BY\n    bully\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bully,\n     force,\n     finale,\n     talbot,\n     dinner\n|> AGGREGATE\n     SUM(dinner) AS sweep\n   GROUP BY\n     bully,\n     force,\n     finale,\n     talbot\n|> AGGREGATE\n     COUNT(sweep) AS mobile\n   GROUP BY\n     bully,\n     force,\n     finale,\n     talbot\n|> SELECT\n     climax,\n     bully,\n     force,\n     finale\n|> AGGREGATE\n     COUNT(finale) AS march,\n     MIN(finale) AS dexter\n   GROUP BY\n     bully\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"climax",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    appeal,\n    tariff,\n    picnic,\n    homage,\n    reform\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    appeal,\n    tariff,\n    picnic,\n    homage,\n    MAX(reform) AS chase\n  FROM cte_0\n  GROUP BY\n    appeal,\n    tariff,\n    picnic,\n    homage\n),\ncte_2 AS (\n  SELECT\n    appeal,\n    tariff,\n    picnic,\n    homage,\n    AVG(chase) AS bosom\n  FROM cte_1\n  GROUP BY\n    appeal,\n    tariff,\n    picnic,\n    homage\n),\ncte_3 AS (\n  SELECT\n    smear,\n    appeal,\n    tariff,\n    picnic,\n    homage\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(tariff) AS walnut,\n    MAX(tariff) AS border\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     appeal,\n     tariff,\n     picnic,\n     homage,\n     reform\n|> AGGREGATE\n     MAX(reform) AS chase\n   GROUP BY\n     appeal,\n     tariff,\n     picnic,\n     homage\n|> AGGREGATE\n     AVG(chase) AS bosom\n   GROUP BY\n     appeal,\n     tariff,\n     picnic,\n     homage\n|> SELECT\n     smear,\n     appeal,\n     tariff,\n     picnic,\n     homage\n|> AGGREGATE\n     MIN(tariff) AS walnut,\n     MAX(tariff) AS border\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"smear",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    pastry,\n    sense,\n    alaska,\n    ledge,\n    thorpe\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    pastry,\n    sense,\n    alaska,\n    ledge,\n    COUNT(thorpe) AS ditch\n  FROM cte_0\n  GROUP BY\n    pastry,\n    sense,\n    alaska,\n    ledge\n),\ncte_2 AS (\n  SELECT\n    pastry,\n    sense,\n    alaska,\n    ledge,\n    MAX(ditch) AS eagle\n  FROM cte_1\n  GROUP BY\n    pastry,\n    sense,\n    alaska,\n    ledge\n),\ncte_3 AS (\n  SELECT\n    affair,\n    pastry,\n    sense,\n    alaska\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    pastry,\n    SUM(alaska) AS gentry\n  FROM cte_3\n  GROUP BY\n    pastry,\n    sense\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     pastry,\n     sense,\n     alaska,\n     ledge,\n     thorpe\n|> AGGREGATE\n     COUNT(thorpe) AS ditch\n   GROUP BY\n     pastry,\n     sense,\n     alaska,\n     ledge\n|> AGGREGATE\n     MAX(ditch) AS eagle\n   GROUP BY\n     pastry,\n     sense,\n     alaska,\n     ledge\n|> SELECT\n     affair,\n     pastry,\n     sense,\n     alaska\n|> AGGREGATE\n     SUM(alaska) AS gentry\n   GROUP BY\n     pastry,\n     sense\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"affair",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    daily,\n    slogan,\n    hockey,\n    debit,\n    laity\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    daily,\n    slogan,\n    hockey,\n    debit,\n    AVG(laity) AS venom\n  FROM cte_0\n  GROUP BY\n    daily,\n    slogan,\n    hockey,\n    debit\n),\ncte_2 AS (\n  SELECT\n    daily,\n    slogan,\n    hockey,\n    debit,\n    MAX(venom) AS defect\n  FROM cte_1\n  GROUP BY\n    daily,\n    slogan,\n    hockey,\n    debit\n),\ncte_3 AS (\n  SELECT\n    carer,\n    daily,\n    slogan\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    daily,\n    SUM(slogan) AS avail,\n    MIN(slogan) AS helmet\n  FROM cte_3\n  GROUP BY\n    daily\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     daily,\n     slogan,\n     hockey,\n     debit,\n     laity\n|> AGGREGATE\n     AVG(laity) AS venom\n   GROUP BY\n     daily,\n     slogan,\n     hockey,\n     debit\n|> AGGREGATE\n     MAX(venom) AS defect\n   GROUP BY\n     daily,\n     slogan,\n     hockey,\n     debit\n|> SELECT\n     carer,\n     daily,\n     slogan\n|> AGGREGATE\n     SUM(slogan) AS avail,\n     MIN(slogan) AS helmet\n   GROUP BY\n     daily\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"carer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    dolly,\n    clone,\n    plasma,\n    skirt,\n    remand\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    dolly,\n    clone,\n    plasma,\n    skirt,\n    MIN(remand) AS rider\n  FROM cte_0\n  GROUP BY\n    dolly,\n    clone,\n    plasma,\n    skirt\n),\ncte_2 AS (\n  SELECT\n    dolly,\n    clone,\n    plasma,\n    skirt,\n    MAX(rider) AS fabric\n  FROM cte_1\n  GROUP BY\n    dolly,\n    clone,\n    plasma,\n    skirt\n),\ncte_3 AS (\n  SELECT\n    hopper,\n    dolly,\n    clone,\n    plasma,\n    skirt\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(plasma) AS thread\n  FROM cte_3\n  GROUP BY\n    dolly,\n    clone\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     dolly,\n     clone,\n     plasma,\n     skirt,\n     remand\n|> AGGREGATE\n     MIN(remand) AS rider\n   GROUP BY\n     dolly,\n     clone,\n     plasma,\n     skirt\n|> AGGREGATE\n     MAX(rider) AS fabric\n   GROUP BY\n     dolly,\n     clone,\n     plasma,\n     skirt\n|> SELECT\n     hopper,\n     dolly,\n     clone,\n     plasma,\n     skirt\n|> AGGREGATE\n     MIN(plasma) AS thread\n   GROUP BY\n     dolly,\n     clone\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"hopper",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    snail,\n    tremor,\n    stump,\n    pardon,\n    crypt\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    snail,\n    tremor,\n    stump,\n    pardon,\n    MAX(crypt) AS burden\n  FROM cte_0\n  GROUP BY\n    snail,\n    tremor,\n    stump,\n    pardon\n),\ncte_2 AS (\n  SELECT\n    snail,\n    tremor,\n    stump,\n    pardon,\n    MIN(burden) AS lounge\n  FROM cte_1\n  GROUP BY\n    snail,\n    tremor,\n    stump,\n    pardon\n),\ncte_3 AS (\n  SELECT\n    stigma,\n    snail,\n    tremor,\n    stump,\n    pardon\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(pardon) AS crewe\n  FROM cte_3\n  GROUP BY\n    snail,\n    tremor\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     snail,\n     tremor,\n     stump,\n     pardon,\n     crypt\n|> AGGREGATE\n     MAX(crypt) AS burden\n   GROUP BY\n     snail,\n     tremor,\n     stump,\n     pardon\n|> AGGREGATE\n     MIN(burden) AS lounge\n   GROUP BY\n     snail,\n     tremor,\n     stump,\n     pardon\n|> SELECT\n     stigma,\n     snail,\n     tremor,\n     stump,\n     pardon\n|> AGGREGATE\n     MAX(pardon) AS crewe\n   GROUP BY\n     snail,\n     tremor\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"stigma",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    census,\n    basil,\n    cavity,\n    opener,\n    paddy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    census,\n    basil,\n    cavity,\n    opener,\n    AVG(paddy) AS deity\n  FROM cte_0\n  GROUP BY\n    census,\n    basil,\n    cavity,\n    opener\n),\ncte_2 AS (\n  SELECT\n    census,\n    basil,\n    cavity,\n    opener,\n    MAX(deity) AS refund\n  FROM cte_1\n  GROUP BY\n    census,\n    basil,\n    cavity,\n    opener\n),\ncte_3 AS (\n  SELECT\n    groove,\n    census,\n    basil\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(census) AS slump,\n    COUNT(census) AS bleach,\n    SUM(basil) AS gender\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     census,\n     basil,\n     cavity,\n     opener,\n     paddy\n|> AGGREGATE\n     AVG(paddy) AS deity\n   GROUP BY\n     census,\n     basil,\n     cavity,\n     opener\n|> AGGREGATE\n     MAX(deity) AS refund\n   GROUP BY\n     census,\n     basil,\n     cavity,\n     opener\n|> SELECT\n     groove,\n     census,\n     basil\n|> AGGREGATE\n     MAX(census) AS slump,\n     COUNT(census) AS bleach,\n     SUM(basil) AS gender\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"groove",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    affect,\n    fluid,\n    bazaar,\n    syrup,\n    fauna\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    affect,\n    fluid,\n    bazaar,\n    syrup,\n    MIN(fauna) AS horror\n  FROM cte_0\n  GROUP BY\n    affect,\n    fluid,\n    bazaar,\n    syrup\n),\ncte_2 AS (\n  SELECT\n    affect,\n    fluid,\n    bazaar,\n    syrup,\n    SUM(horror) AS feast\n  FROM cte_1\n  GROUP BY\n    affect,\n    fluid,\n    bazaar,\n    syrup\n),\ncte_3 AS (\n  SELECT\n    appeal,\n    affect,\n    fluid\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    affect,\n    MIN(fluid) AS brett,\n    SUM(fluid) AS beach\n  FROM cte_3\n  GROUP BY\n    affect\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     affect,\n     fluid,\n     bazaar,\n     syrup,\n     fauna\n|> AGGREGATE\n     MIN(fauna) AS horror\n   GROUP BY\n     affect,\n     fluid,\n     bazaar,\n     syrup\n|> AGGREGATE\n     SUM(horror) AS feast\n   GROUP BY\n     affect,\n     fluid,\n     bazaar,\n     syrup\n|> SELECT\n     appeal,\n     affect,\n     fluid\n|> AGGREGATE\n     MIN(fluid) AS brett,\n     SUM(fluid) AS beach\n   GROUP BY\n     affect\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"appeal",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    widow,\n    liquor,\n    armour,\n    cabin,\n    marsh\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    widow,\n    liquor,\n    armour,\n    cabin,\n    MAX(marsh) AS onset\n  FROM cte_0\n  GROUP BY\n    widow,\n    liquor,\n    armour,\n    cabin\n),\ncte_2 AS (\n  SELECT\n    widow,\n    liquor,\n    armour,\n    cabin,\n    MAX(onset) AS theft\n  FROM cte_1\n  GROUP BY\n    widow,\n    liquor,\n    armour,\n    cabin\n),\ncte_3 AS (\n  SELECT\n    siren,\n    widow,\n    liquor,\n    armour,\n    cabin\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    widow,\n    liquor,\n    armour,\n    cabin\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     widow,\n     liquor,\n     armour,\n     cabin,\n     marsh\n|> AGGREGATE\n     MAX(marsh) AS onset\n   GROUP BY\n     widow,\n     liquor,\n     armour,\n     cabin\n|> AGGREGATE\n     MAX(onset) AS theft\n   GROUP BY\n     widow,\n     liquor,\n     armour,\n     cabin\n|> SELECT\n     siren,\n     widow,\n     liquor,\n     armour,\n     cabin\n|> SELECT\n     widow,\n     liquor,\n     armour,\n     cabin\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"siren",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    litre,\n    sheen,\n    dobson,\n    freeze,\n    groan\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    litre,\n    sheen,\n    dobson,\n    freeze,\n    SUM(groan) AS unity\n  FROM cte_0\n  GROUP BY\n    litre,\n    sheen,\n    dobson,\n    freeze\n),\ncte_2 AS (\n  SELECT\n    litre,\n    sheen,\n    dobson,\n    freeze,\n    SUM(unity) AS recall\n  FROM cte_1\n  GROUP BY\n    litre,\n    sheen,\n    dobson,\n    freeze\n),\ncte_3 AS (\n  SELECT\n    party,\n    litre,\n    sheen,\n    dobson,\n    freeze\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    litre,\n    AVG(freeze) AS state\n  FROM cte_3\n  GROUP BY\n    litre\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     litre,\n     sheen,\n     dobson,\n     freeze,\n     groan\n|> AGGREGATE\n     SUM(groan) AS unity\n   GROUP BY\n     litre,\n     sheen,\n     dobson,\n     freeze\n|> AGGREGATE\n     SUM(unity) AS recall\n   GROUP BY\n     litre,\n     sheen,\n     dobson,\n     freeze\n|> SELECT\n     party,\n     litre,\n     sheen,\n     dobson,\n     freeze\n|> AGGREGATE\n     AVG(freeze) AS state\n   GROUP BY\n     litre\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"party",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    madge,\n    reign,\n    escort,\n    blood,\n    crowd\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    madge,\n    reign,\n    escort,\n    blood,\n    AVG(crowd) AS muscle\n  FROM cte_0\n  GROUP BY\n    madge,\n    reign,\n    escort,\n    blood\n),\ncte_2 AS (\n  SELECT\n    madge,\n    reign,\n    escort,\n    blood,\n    SUM(muscle) AS fowler\n  FROM cte_1\n  GROUP BY\n    madge,\n    reign,\n    escort,\n    blood\n),\ncte_3 AS (\n  SELECT\n    digit,\n    madge,\n    reign,\n    escort,\n    blood,\n    fowler\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    madge,\n    reign,\n    escort\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     madge,\n     reign,\n     escort,\n     blood,\n     crowd\n|> AGGREGATE\n     AVG(crowd) AS muscle\n   GROUP BY\n     madge,\n     reign,\n     escort,\n     blood\n|> AGGREGATE\n     SUM(muscle) AS fowler\n   GROUP BY\n     madge,\n     reign,\n     escort,\n     blood\n|> SELECT\n     digit,\n     madge,\n     reign,\n     escort,\n     blood,\n     fowler\n|> SELECT\n     madge,\n     reign,\n     escort\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"digit",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    asset,\n    victor,\n    magic,\n    spade,\n    strike\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    asset,\n    victor,\n    magic,\n    spade,\n    SUM(strike) AS master\n  FROM cte_0\n  GROUP BY\n    asset,\n    victor,\n    magic,\n    spade\n),\ncte_2 AS (\n  SELECT\n    asset,\n    victor,\n    magic,\n    spade,\n    MIN(master) AS cinema\n  FROM cte_1\n  GROUP BY\n    asset,\n    victor,\n    magic,\n    spade\n),\ncte_3 AS (\n  SELECT\n    scent,\n    asset,\n    victor,\n    magic\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    asset,\n    MAX(magic) AS staple\n  FROM cte_3\n  GROUP BY\n    asset,\n    victor\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     asset,\n     victor,\n     magic,\n     spade,\n     strike\n|> AGGREGATE\n     SUM(strike) AS master\n   GROUP BY\n     asset,\n     victor,\n     magic,\n     spade\n|> AGGREGATE\n     MIN(master) AS cinema\n   GROUP BY\n     asset,\n     victor,\n     magic,\n     spade\n|> SELECT\n     scent,\n     asset,\n     victor,\n     magic\n|> AGGREGATE\n     MAX(magic) AS staple\n   GROUP BY\n     asset,\n     victor\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"scent",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    soccer,\n    fisher,\n    morris,\n    cherry,\n    budget\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    soccer,\n    fisher,\n    morris,\n    cherry,\n    MIN(budget) AS oxygen\n  FROM cte_0\n  GROUP BY\n    soccer,\n    fisher,\n    morris,\n    cherry\n),\ncte_2 AS (\n  SELECT\n    soccer,\n    fisher,\n    morris,\n    cherry,\n    COUNT(oxygen) AS marina\n  FROM cte_1\n  GROUP BY\n    soccer,\n    fisher,\n    morris,\n    cherry\n),\ncte_3 AS (\n  SELECT\n    insert,\n    soccer,\n    fisher,\n    morris,\n    cherry\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    soccer,\n    fisher,\n    morris,\n    cherry\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     soccer,\n     fisher,\n     morris,\n     cherry,\n     budget\n|> AGGREGATE\n     MIN(budget) AS oxygen\n   GROUP BY\n     soccer,\n     fisher,\n     morris,\n     cherry\n|> AGGREGATE\n     COUNT(oxygen) AS marina\n   GROUP BY\n     soccer,\n     fisher,\n     morris,\n     cherry\n|> SELECT\n     insert,\n     soccer,\n     fisher,\n     morris,\n     cherry\n|> SELECT\n     soccer,\n     fisher,\n     morris,\n     cherry\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"insert",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    coupon,\n    gravel,\n    stone,\n    revolt,\n    rattle\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    coupon,\n    gravel,\n    stone,\n    revolt,\n    MAX(rattle) AS vigil\n  FROM cte_0\n  GROUP BY\n    coupon,\n    gravel,\n    stone,\n    revolt\n),\ncte_2 AS (\n  SELECT\n    coupon,\n    gravel,\n    stone,\n    revolt,\n    MIN(vigil) AS enigma\n  FROM cte_1\n  GROUP BY\n    coupon,\n    gravel,\n    stone,\n    revolt\n),\ncte_3 AS (\n  SELECT\n    strife,\n    coupon,\n    gravel,\n    stone,\n    revolt\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    coupon,\n    AVG(revolt) AS uplift\n  FROM cte_3\n  GROUP BY\n    coupon\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     coupon,\n     gravel,\n     stone,\n     revolt,\n     rattle\n|> AGGREGATE\n     MAX(rattle) AS vigil\n   GROUP BY\n     coupon,\n     gravel,\n     stone,\n     revolt\n|> AGGREGATE\n     MIN(vigil) AS enigma\n   GROUP BY\n     coupon,\n     gravel,\n     stone,\n     revolt\n|> SELECT\n     strife,\n     coupon,\n     gravel,\n     stone,\n     revolt\n|> AGGREGATE\n     AVG(revolt) AS uplift\n   GROUP BY\n     coupon\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"strife",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    greed,\n    backup,\n    spell,\n    doubt,\n    elite\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    greed,\n    backup,\n    spell,\n    doubt,\n    SUM(elite) AS woody\n  FROM cte_0\n  GROUP BY\n    greed,\n    backup,\n    spell,\n    doubt\n),\ncte_2 AS (\n  SELECT\n    greed,\n    backup,\n    spell,\n    doubt,\n    MAX(woody) AS carrot\n  FROM cte_1\n  GROUP BY\n    greed,\n    backup,\n    spell,\n    doubt\n),\ncte_3 AS (\n  SELECT\n    virgin,\n    greed,\n    backup\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    greed,\n    MAX(backup) AS onset,\n    MIN(backup) AS aroma\n  FROM cte_3\n  GROUP BY\n    greed\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     greed,\n     backup,\n     spell,\n     doubt,\n     elite\n|> AGGREGATE\n     SUM(elite) AS woody\n   GROUP BY\n     greed,\n     backup,\n     spell,\n     doubt\n|> AGGREGATE\n     MAX(woody) AS carrot\n   GROUP BY\n     greed,\n     backup,\n     spell,\n     doubt\n|> SELECT\n     virgin,\n     greed,\n     backup\n|> AGGREGATE\n     MAX(backup) AS onset,\n     MIN(backup) AS aroma\n   GROUP BY\n     greed\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"virgin",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    object,\n    bunker,\n    burrow,\n    venus,\n    fence\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    object,\n    bunker,\n    burrow,\n    venus,\n    MIN(fence) AS torch\n  FROM cte_0\n  GROUP BY\n    object,\n    bunker,\n    burrow,\n    venus\n),\ncte_2 AS (\n  SELECT\n    object,\n    bunker,\n    burrow,\n    venus,\n    MIN(torch) AS habit\n  FROM cte_1\n  GROUP BY\n    object,\n    bunker,\n    burrow,\n    venus\n),\ncte_3 AS (\n  SELECT\n    gutter,\n    object,\n    bunker,\n    burrow,\n    venus\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(bunker) AS virtue,\n    MIN(venus) AS scorn\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     object,\n     bunker,\n     burrow,\n     venus,\n     fence\n|> AGGREGATE\n     MIN(fence) AS torch\n   GROUP BY\n     object,\n     bunker,\n     burrow,\n     venus\n|> AGGREGATE\n     MIN(torch) AS habit\n   GROUP BY\n     object,\n     bunker,\n     burrow,\n     venus\n|> SELECT\n     gutter,\n     object,\n     bunker,\n     burrow,\n     venus\n|> AGGREGATE\n     MIN(bunker) AS virtue,\n     MIN(venus) AS scorn\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"gutter",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ghetto,\n    entry,\n    utopia,\n    spate,\n    casino\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    ghetto,\n    entry,\n    utopia,\n    spate,\n    MAX(casino) AS tremor\n  FROM cte_0\n  GROUP BY\n    ghetto,\n    entry,\n    utopia,\n    spate\n),\ncte_2 AS (\n  SELECT\n    ghetto,\n    entry,\n    utopia,\n    spate,\n    MIN(tremor) AS hazard\n  FROM cte_1\n  GROUP BY\n    ghetto,\n    entry,\n    utopia,\n    spate\n),\ncte_3 AS (\n  SELECT\n    father,\n    ghetto,\n    entry,\n    utopia,\n    spate\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(utopia) AS basis,\n    MAX(spate) AS barth\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ghetto,\n     entry,\n     utopia,\n     spate,\n     casino\n|> AGGREGATE\n     MAX(casino) AS tremor\n   GROUP BY\n     ghetto,\n     entry,\n     utopia,\n     spate\n|> AGGREGATE\n     MIN(tremor) AS hazard\n   GROUP BY\n     ghetto,\n     entry,\n     utopia,\n     spate\n|> SELECT\n     father,\n     ghetto,\n     entry,\n     utopia,\n     spate\n|> AGGREGATE\n     COUNT(utopia) AS basis,\n     MAX(spate) AS barth\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"father",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    roche,\n    shaikh,\n    fiddle,\n    avail,\n    harem\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    roche,\n    shaikh,\n    fiddle,\n    avail,\n    MIN(harem) AS avenue\n  FROM cte_0\n  GROUP BY\n    roche,\n    shaikh,\n    fiddle,\n    avail\n),\ncte_2 AS (\n  SELECT\n    roche,\n    shaikh,\n    fiddle,\n    avail,\n    COUNT(avenue) AS manner\n  FROM cte_1\n  GROUP BY\n    roche,\n    shaikh,\n    fiddle,\n    avail\n),\ncte_3 AS (\n  SELECT\n    temper,\n    roche,\n    shaikh,\n    fiddle,\n    avail\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    roche,\n    COUNT(avail) AS regret\n  FROM cte_3\n  GROUP BY\n    roche\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     roche,\n     shaikh,\n     fiddle,\n     avail,\n     harem\n|> AGGREGATE\n     MIN(harem) AS avenue\n   GROUP BY\n     roche,\n     shaikh,\n     fiddle,\n     avail\n|> AGGREGATE\n     COUNT(avenue) AS manner\n   GROUP BY\n     roche,\n     shaikh,\n     fiddle,\n     avail\n|> SELECT\n     temper,\n     roche,\n     shaikh,\n     fiddle,\n     avail\n|> AGGREGATE\n     COUNT(avail) AS regret\n   GROUP BY\n     roche\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"temper",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    dancer,\n    holly,\n    signal,\n    autumn,\n    safari\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    dancer,\n    holly,\n    signal,\n    autumn,\n    COUNT(safari) AS luxury\n  FROM cte_0\n  GROUP BY\n    dancer,\n    holly,\n    signal,\n    autumn\n),\ncte_2 AS (\n  SELECT\n    dancer,\n    holly,\n    signal,\n    autumn,\n    AVG(luxury) AS paint\n  FROM cte_1\n  GROUP BY\n    dancer,\n    holly,\n    signal,\n    autumn\n),\ncte_3 AS (\n  SELECT\n    excise,\n    dancer,\n    holly,\n    signal\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(signal) AS ethel,\n    COUNT(holly) AS board\n  FROM cte_3\n  GROUP BY\n    dancer\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     dancer,\n     holly,\n     signal,\n     autumn,\n     safari\n|> AGGREGATE\n     COUNT(safari) AS luxury\n   GROUP BY\n     dancer,\n     holly,\n     signal,\n     autumn\n|> AGGREGATE\n     AVG(luxury) AS paint\n   GROUP BY\n     dancer,\n     holly,\n     signal,\n     autumn\n|> SELECT\n     excise,\n     dancer,\n     holly,\n     signal\n|> AGGREGATE\n     MIN(signal) AS ethel,\n     COUNT(holly) AS board\n   GROUP BY\n     dancer\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"excise",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    glance,\n    breed,\n    joint,\n    batch,\n    sodium\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    glance,\n    breed,\n    joint,\n    batch,\n    COUNT(sodium) AS marble\n  FROM cte_0\n  GROUP BY\n    glance,\n    breed,\n    joint,\n    batch\n),\ncte_2 AS (\n  SELECT\n    glance,\n    breed,\n    joint,\n    batch,\n    COUNT(marble) AS build\n  FROM cte_1\n  GROUP BY\n    glance,\n    breed,\n    joint,\n    batch\n),\ncte_3 AS (\n  SELECT\n    chord,\n    glance,\n    breed\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    glance,\n    COUNT(breed) AS yeast,\n    MIN(breed) AS clock\n  FROM cte_3\n  GROUP BY\n    glance\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     glance,\n     breed,\n     joint,\n     batch,\n     sodium\n|> AGGREGATE\n     COUNT(sodium) AS marble\n   GROUP BY\n     glance,\n     breed,\n     joint,\n     batch\n|> AGGREGATE\n     COUNT(marble) AS build\n   GROUP BY\n     glance,\n     breed,\n     joint,\n     batch\n|> SELECT\n     chord,\n     glance,\n     breed\n|> AGGREGATE\n     COUNT(breed) AS yeast,\n     MIN(breed) AS clock\n   GROUP BY\n     glance\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"chord",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    peter,\n    canyon,\n    leone,\n    paddy,\n    frenzy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    peter,\n    canyon,\n    leone,\n    paddy,\n    AVG(frenzy) AS jelly\n  FROM cte_0\n  GROUP BY\n    peter,\n    canyon,\n    leone,\n    paddy\n),\ncte_2 AS (\n  SELECT\n    peter,\n    canyon,\n    leone,\n    paddy,\n    MAX(jelly) AS mucus\n  FROM cte_1\n  GROUP BY\n    peter,\n    canyon,\n    leone,\n    paddy\n),\ncte_3 AS (\n  SELECT\n    radio,\n    peter,\n    canyon,\n    leone\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    peter,\n    MAX(leone) AS stile\n  FROM cte_3\n  GROUP BY\n    peter,\n    canyon\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     peter,\n     canyon,\n     leone,\n     paddy,\n     frenzy\n|> AGGREGATE\n     AVG(frenzy) AS jelly\n   GROUP BY\n     peter,\n     canyon,\n     leone,\n     paddy\n|> AGGREGATE\n     MAX(jelly) AS mucus\n   GROUP BY\n     peter,\n     canyon,\n     leone,\n     paddy\n|> SELECT\n     radio,\n     peter,\n     canyon,\n     leone\n|> AGGREGATE\n     MAX(leone) AS stile\n   GROUP BY\n     peter,\n     canyon\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"radio",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    salmon,\n    violet,\n    mandy,\n    ribber,\n    shoot\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    salmon,\n    violet,\n    mandy,\n    ribber,\n    AVG(shoot) AS nylon\n  FROM cte_0\n  GROUP BY\n    salmon,\n    violet,\n    mandy,\n    ribber\n),\ncte_2 AS (\n  SELECT\n    salmon,\n    violet,\n    mandy,\n    ribber,\n    MIN(nylon) AS assay\n  FROM cte_1\n  GROUP BY\n    salmon,\n    violet,\n    mandy,\n    ribber\n),\ncte_3 AS (\n  SELECT\n    clutch,\n    salmon,\n    violet,\n    mandy,\n    ribber\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(ribber) AS dogma\n  FROM cte_3\n  GROUP BY\n    salmon,\n    violet\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     salmon,\n     violet,\n     mandy,\n     ribber,\n     shoot\n|> AGGREGATE\n     AVG(shoot) AS nylon\n   GROUP BY\n     salmon,\n     violet,\n     mandy,\n     ribber\n|> AGGREGATE\n     MIN(nylon) AS assay\n   GROUP BY\n     salmon,\n     violet,\n     mandy,\n     ribber\n|> SELECT\n     clutch,\n     salmon,\n     violet,\n     mandy,\n     ribber\n|> AGGREGATE\n     SUM(ribber) AS dogma\n   GROUP BY\n     salmon,\n     violet\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"clutch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    globe,\n    shoal,\n    recall,\n    focus,\n    shore\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    globe,\n    shoal,\n    recall,\n    focus,\n    SUM(shore) AS office\n  FROM cte_0\n  GROUP BY\n    globe,\n    shoal,\n    recall,\n    focus\n),\ncte_2 AS (\n  SELECT\n    globe,\n    shoal,\n    recall,\n    focus,\n    COUNT(office) AS grease\n  FROM cte_1\n  GROUP BY\n    globe,\n    shoal,\n    recall,\n    focus\n),\ncte_3 AS (\n  SELECT\n    turtle,\n    globe,\n    shoal,\n    recall,\n    focus\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(globe) AS breeze,\n    COUNT(globe) AS build\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     globe,\n     shoal,\n     recall,\n     focus,\n     shore\n|> AGGREGATE\n     SUM(shore) AS office\n   GROUP BY\n     globe,\n     shoal,\n     recall,\n     focus\n|> AGGREGATE\n     COUNT(office) AS grease\n   GROUP BY\n     globe,\n     shoal,\n     recall,\n     focus\n|> SELECT\n     turtle,\n     globe,\n     shoal,\n     recall,\n     focus\n|> AGGREGATE\n     SUM(globe) AS breeze,\n     COUNT(globe) AS build\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"turtle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    vicar,\n    wight,\n    sultan,\n    matron,\n    whisky\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    vicar,\n    wight,\n    sultan,\n    matron,\n    MAX(whisky) AS mandy\n  FROM cte_0\n  GROUP BY\n    vicar,\n    wight,\n    sultan,\n    matron\n),\ncte_2 AS (\n  SELECT\n    vicar,\n    wight,\n    sultan,\n    matron,\n    MAX(mandy) AS local\n  FROM cte_1\n  GROUP BY\n    vicar,\n    wight,\n    sultan,\n    matron\n),\ncte_3 AS (\n  SELECT\n    devon,\n    vicar,\n    wight\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    vicar,\n    MIN(wight) AS minute,\n    MAX(wight) AS pledge\n  FROM cte_3\n  GROUP BY\n    vicar\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     vicar,\n     wight,\n     sultan,\n     matron,\n     whisky\n|> AGGREGATE\n     MAX(whisky) AS mandy\n   GROUP BY\n     vicar,\n     wight,\n     sultan,\n     matron\n|> AGGREGATE\n     MAX(mandy) AS local\n   GROUP BY\n     vicar,\n     wight,\n     sultan,\n     matron\n|> SELECT\n     devon,\n     vicar,\n     wight\n|> AGGREGATE\n     MIN(wight) AS minute,\n     MAX(wight) AS pledge\n   GROUP BY\n     vicar\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"devon",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    access,\n    swing,\n    auntie,\n    blouse,\n    gauge\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    access,\n    swing,\n    auntie,\n    blouse,\n    AVG(gauge) AS mother\n  FROM cte_0\n  GROUP BY\n    access,\n    swing,\n    auntie,\n    blouse\n),\ncte_2 AS (\n  SELECT\n    access,\n    swing,\n    auntie,\n    blouse,\n    MIN(mother) AS white\n  FROM cte_1\n  GROUP BY\n    access,\n    swing,\n    auntie,\n    blouse\n),\ncte_3 AS (\n  SELECT\n    verse,\n    access,\n    swing,\n    auntie\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(swing) AS spell,\n    COUNT(auntie) AS accent\n  FROM cte_3\n  GROUP BY\n    access\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     access,\n     swing,\n     auntie,\n     blouse,\n     gauge\n|> AGGREGATE\n     AVG(gauge) AS mother\n   GROUP BY\n     access,\n     swing,\n     auntie,\n     blouse\n|> AGGREGATE\n     MIN(mother) AS white\n   GROUP BY\n     access,\n     swing,\n     auntie,\n     blouse\n|> SELECT\n     verse,\n     access,\n     swing,\n     auntie\n|> AGGREGATE\n     MAX(swing) AS spell,\n     COUNT(auntie) AS accent\n   GROUP BY\n     access\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"verse",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    blaze,\n    mosaic,\n    market,\n    cutter,\n    tonne\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    blaze,\n    mosaic,\n    market,\n    cutter,\n    SUM(tonne) AS scorer\n  FROM cte_0\n  GROUP BY\n    blaze,\n    mosaic,\n    market,\n    cutter\n),\ncte_2 AS (\n  SELECT\n    blaze,\n    mosaic,\n    market,\n    cutter,\n    SUM(scorer) AS widow\n  FROM cte_1\n  GROUP BY\n    blaze,\n    mosaic,\n    market,\n    cutter\n),\ncte_3 AS (\n  SELECT\n    thread,\n    blaze,\n    mosaic,\n    market,\n    cutter\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    blaze,\n    mosaic,\n    market,\n    cutter\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     blaze,\n     mosaic,\n     market,\n     cutter,\n     tonne\n|> AGGREGATE\n     SUM(tonne) AS scorer\n   GROUP BY\n     blaze,\n     mosaic,\n     market,\n     cutter\n|> AGGREGATE\n     SUM(scorer) AS widow\n   GROUP BY\n     blaze,\n     mosaic,\n     market,\n     cutter\n|> SELECT\n     thread,\n     blaze,\n     mosaic,\n     market,\n     cutter\n|> SELECT\n     blaze,\n     mosaic,\n     market,\n     cutter\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"thread",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    daphne,\n    cliff,\n    sphere,\n    stein,\n    pearl\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    daphne,\n    cliff,\n    sphere,\n    stein,\n    MAX(pearl) AS dalton\n  FROM cte_0\n  GROUP BY\n    daphne,\n    cliff,\n    sphere,\n    stein\n),\ncte_2 AS (\n  SELECT\n    daphne,\n    cliff,\n    sphere,\n    stein,\n    SUM(dalton) AS dexter\n  FROM cte_1\n  GROUP BY\n    daphne,\n    cliff,\n    sphere,\n    stein\n),\ncte_3 AS (\n  SELECT\n    remedy,\n    daphne,\n    cliff,\n    sphere,\n    stein\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    daphne,\n    cliff,\n    sphere,\n    stein\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     daphne,\n     cliff,\n     sphere,\n     stein,\n     pearl\n|> AGGREGATE\n     MAX(pearl) AS dalton\n   GROUP BY\n     daphne,\n     cliff,\n     sphere,\n     stein\n|> AGGREGATE\n     SUM(dalton) AS dexter\n   GROUP BY\n     daphne,\n     cliff,\n     sphere,\n     stein\n|> SELECT\n     remedy,\n     daphne,\n     cliff,\n     sphere,\n     stein\n|> SELECT\n     daphne,\n     cliff,\n     sphere,\n     stein\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"remedy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    whore,\n    county,\n    brunt,\n    fodder,\n    retina\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    whore,\n    county,\n    brunt,\n    fodder,\n    AVG(retina) AS human\n  FROM cte_0\n  GROUP BY\n    whore,\n    county,\n    brunt,\n    fodder\n),\ncte_2 AS (\n  SELECT\n    whore,\n    county,\n    brunt,\n    fodder,\n    COUNT(human) AS repair\n  FROM cte_1\n  GROUP BY\n    whore,\n    county,\n    brunt,\n    fodder\n),\ncte_3 AS (\n  SELECT\n    guild,\n    whore,\n    county\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(county) AS aroma,\n    SUM(whore) AS favour,\n    AVG(county) AS avenue\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     whore,\n     county,\n     brunt,\n     fodder,\n     retina\n|> AGGREGATE\n     AVG(retina) AS human\n   GROUP BY\n     whore,\n     county,\n     brunt,\n     fodder\n|> AGGREGATE\n     COUNT(human) AS repair\n   GROUP BY\n     whore,\n     county,\n     brunt,\n     fodder\n|> SELECT\n     guild,\n     whore,\n     county\n|> AGGREGATE\n     COUNT(county) AS aroma,\n     SUM(whore) AS favour,\n     AVG(county) AS avenue\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"guild",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bobby,\n    irony,\n    amelia,\n    digit,\n    design\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    bobby,\n    irony,\n    amelia,\n    digit,\n    SUM(design) AS auntie\n  FROM cte_0\n  GROUP BY\n    bobby,\n    irony,\n    amelia,\n    digit\n),\ncte_2 AS (\n  SELECT\n    bobby,\n    irony,\n    amelia,\n    digit,\n    MIN(auntie) AS trench\n  FROM cte_1\n  GROUP BY\n    bobby,\n    irony,\n    amelia,\n    digit\n),\ncte_3 AS (\n  SELECT\n    idiot,\n    bobby,\n    irony\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    bobby,\n    COUNT(irony) AS space,\n    MIN(irony) AS steel\n  FROM cte_3\n  GROUP BY\n    bobby\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bobby,\n     irony,\n     amelia,\n     digit,\n     design\n|> AGGREGATE\n     SUM(design) AS auntie\n   GROUP BY\n     bobby,\n     irony,\n     amelia,\n     digit\n|> AGGREGATE\n     MIN(auntie) AS trench\n   GROUP BY\n     bobby,\n     irony,\n     amelia,\n     digit\n|> SELECT\n     idiot,\n     bobby,\n     irony\n|> AGGREGATE\n     COUNT(irony) AS space,\n     MIN(irony) AS steel\n   GROUP BY\n     bobby\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"idiot",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    county,\n    thrill,\n    income,\n    sodium,\n    curry\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    county,\n    thrill,\n    income,\n    sodium,\n    SUM(curry) AS aisle\n  FROM cte_0\n  GROUP BY\n    county,\n    thrill,\n    income,\n    sodium\n),\ncte_2 AS (\n  SELECT\n    county,\n    thrill,\n    income,\n    sodium,\n    AVG(aisle) AS johnny\n  FROM cte_1\n  GROUP BY\n    county,\n    thrill,\n    income,\n    sodium\n),\ncte_3 AS (\n  SELECT\n    puppy,\n    county,\n    thrill,\n    income,\n    sodium\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    county,\n    AVG(thrill) AS quarry\n  FROM cte_3\n  GROUP BY\n    county\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     county,\n     thrill,\n     income,\n     sodium,\n     curry\n|> AGGREGATE\n     SUM(curry) AS aisle\n   GROUP BY\n     county,\n     thrill,\n     income,\n     sodium\n|> AGGREGATE\n     AVG(aisle) AS johnny\n   GROUP BY\n     county,\n     thrill,\n     income,\n     sodium\n|> SELECT\n     puppy,\n     county,\n     thrill,\n     income,\n     sodium\n|> AGGREGATE\n     AVG(thrill) AS quarry\n   GROUP BY\n     county\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"puppy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    trifle,\n    larry,\n    brown,\n    poetry,\n    reflux\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    trifle,\n    larry,\n    brown,\n    poetry,\n    MAX(reflux) AS cigar\n  FROM cte_0\n  GROUP BY\n    trifle,\n    larry,\n    brown,\n    poetry\n),\ncte_2 AS (\n  SELECT\n    trifle,\n    larry,\n    brown,\n    poetry,\n    COUNT(cigar) AS knock\n  FROM cte_1\n  GROUP BY\n    trifle,\n    larry,\n    brown,\n    poetry\n),\ncte_3 AS (\n  SELECT\n    burton,\n    trifle,\n    larry,\n    brown,\n    poetry\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(poetry) AS middle\n  FROM cte_3\n  GROUP BY\n    trifle,\n    larry\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     trifle,\n     larry,\n     brown,\n     poetry,\n     reflux\n|> AGGREGATE\n     MAX(reflux) AS cigar\n   GROUP BY\n     trifle,\n     larry,\n     brown,\n     poetry\n|> AGGREGATE\n     COUNT(cigar) AS knock\n   GROUP BY\n     trifle,\n     larry,\n     brown,\n     poetry\n|> SELECT\n     burton,\n     trifle,\n     larry,\n     brown,\n     poetry\n|> AGGREGATE\n     SUM(poetry) AS middle\n   GROUP BY\n     trifle,\n     larry\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"burton",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    label,\n    manila,\n    temple,\n    holly,\n    damage\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    label,\n    manila,\n    temple,\n    holly,\n    MIN(damage) AS sonny\n  FROM cte_0\n  GROUP BY\n    label,\n    manila,\n    temple,\n    holly\n),\ncte_2 AS (\n  SELECT\n    label,\n    manila,\n    temple,\n    holly,\n    COUNT(sonny) AS coffin\n  FROM cte_1\n  GROUP BY\n    label,\n    manila,\n    temple,\n    holly\n),\ncte_3 AS (\n  SELECT\n    museum,\n    label,\n    manila,\n    temple\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    label,\n    COUNT(temple) AS autumn\n  FROM cte_3\n  GROUP BY\n    label,\n    manila\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     label,\n     manila,\n     temple,\n     holly,\n     damage\n|> AGGREGATE\n     MIN(damage) AS sonny\n   GROUP BY\n     label,\n     manila,\n     temple,\n     holly\n|> AGGREGATE\n     COUNT(sonny) AS coffin\n   GROUP BY\n     label,\n     manila,\n     temple,\n     holly\n|> SELECT\n     museum,\n     label,\n     manila,\n     temple\n|> AGGREGATE\n     COUNT(temple) AS autumn\n   GROUP BY\n     label,\n     manila\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"museum",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    glint,\n    altar,\n    candy,\n    outlet,\n    virtue\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    glint,\n    altar,\n    candy,\n    outlet,\n    AVG(virtue) AS leone\n  FROM cte_0\n  GROUP BY\n    glint,\n    altar,\n    candy,\n    outlet\n),\ncte_2 AS (\n  SELECT\n    glint,\n    altar,\n    candy,\n    outlet,\n    MIN(leone) AS henry\n  FROM cte_1\n  GROUP BY\n    glint,\n    altar,\n    candy,\n    outlet\n),\ncte_3 AS (\n  SELECT\n    street,\n    glint,\n    altar,\n    candy,\n    outlet\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    glint,\n    altar,\n    candy,\n    outlet\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     glint,\n     altar,\n     candy,\n     outlet,\n     virtue\n|> AGGREGATE\n     AVG(virtue) AS leone\n   GROUP BY\n     glint,\n     altar,\n     candy,\n     outlet\n|> AGGREGATE\n     MIN(leone) AS henry\n   GROUP BY\n     glint,\n     altar,\n     candy,\n     outlet\n|> SELECT\n     street,\n     glint,\n     altar,\n     candy,\n     outlet\n|> SELECT\n     glint,\n     altar,\n     candy,\n     outlet\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"street",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    idiot,\n    slope,\n    ritual,\n    crime,\n    groan\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    idiot,\n    slope,\n    ritual,\n    crime,\n    MIN(groan) AS liquid\n  FROM cte_0\n  GROUP BY\n    idiot,\n    slope,\n    ritual,\n    crime\n),\ncte_2 AS (\n  SELECT\n    idiot,\n    slope,\n    ritual,\n    crime,\n    AVG(liquid) AS layer\n  FROM cte_1\n  GROUP BY\n    idiot,\n    slope,\n    ritual,\n    crime\n),\ncte_3 AS (\n  SELECT\n    blind,\n    idiot,\n    slope,\n    ritual,\n    crime,\n    layer\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    idiot,\n    slope,\n    ritual\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     idiot,\n     slope,\n     ritual,\n     crime,\n     groan\n|> AGGREGATE\n     MIN(groan) AS liquid\n   GROUP BY\n     idiot,\n     slope,\n     ritual,\n     crime\n|> AGGREGATE\n     AVG(liquid) AS layer\n   GROUP BY\n     idiot,\n     slope,\n     ritual,\n     crime\n|> SELECT\n     blind,\n     idiot,\n     slope,\n     ritual,\n     crime,\n     layer\n|> SELECT\n     idiot,\n     slope,\n     ritual\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"blind",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    smell,\n    warden,\n    staff,\n    trout,\n    gutter\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    smell,\n    warden,\n    staff,\n    trout,\n    MAX(gutter) AS satin\n  FROM cte_0\n  GROUP BY\n    smell,\n    warden,\n    staff,\n    trout\n),\ncte_2 AS (\n  SELECT\n    smell,\n    warden,\n    staff,\n    trout,\n    MAX(satin) AS barker\n  FROM cte_1\n  GROUP BY\n    smell,\n    warden,\n    staff,\n    trout\n),\ncte_3 AS (\n  SELECT\n    stage,\n    smell,\n    warden,\n    staff,\n    trout\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(smell) AS cohen,\n    MAX(staff) AS carter\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     smell,\n     warden,\n     staff,\n     trout,\n     gutter\n|> AGGREGATE\n     MAX(gutter) AS satin\n   GROUP BY\n     smell,\n     warden,\n     staff,\n     trout\n|> AGGREGATE\n     MAX(satin) AS barker\n   GROUP BY\n     smell,\n     warden,\n     staff,\n     trout\n|> SELECT\n     stage,\n     smell,\n     warden,\n     staff,\n     trout\n|> AGGREGATE\n     MAX(smell) AS cohen,\n     MAX(staff) AS carter\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"stage",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bliss,\n    amber,\n    castle,\n    violin,\n    glover\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    bliss,\n    amber,\n    castle,\n    violin,\n    MIN(glover) AS token\n  FROM cte_0\n  GROUP BY\n    bliss,\n    amber,\n    castle,\n    violin\n),\ncte_2 AS (\n  SELECT\n    bliss,\n    amber,\n    castle,\n    violin,\n    MIN(token) AS sample\n  FROM cte_1\n  GROUP BY\n    bliss,\n    amber,\n    castle,\n    violin\n),\ncte_3 AS (\n  SELECT\n    debut,\n    bliss,\n    amber,\n    castle,\n    violin\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    bliss,\n    amber,\n    castle,\n    violin\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bliss,\n     amber,\n     castle,\n     violin,\n     glover\n|> AGGREGATE\n     MIN(glover) AS token\n   GROUP BY\n     bliss,\n     amber,\n     castle,\n     violin\n|> AGGREGATE\n     MIN(token) AS sample\n   GROUP BY\n     bliss,\n     amber,\n     castle,\n     violin\n|> SELECT\n     debut,\n     bliss,\n     amber,\n     castle,\n     violin\n|> SELECT\n     bliss,\n     amber,\n     castle,\n     violin\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"debut",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    folder,\n    heron,\n    guilt,\n    trader,\n    keeper\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    folder,\n    heron,\n    guilt,\n    trader,\n    SUM(keeper) AS patch\n  FROM cte_0\n  GROUP BY\n    folder,\n    heron,\n    guilt,\n    trader\n),\ncte_2 AS (\n  SELECT\n    folder,\n    heron,\n    guilt,\n    trader,\n    AVG(patch) AS stitch\n  FROM cte_1\n  GROUP BY\n    folder,\n    heron,\n    guilt,\n    trader\n),\ncte_3 AS (\n  SELECT\n    space,\n    folder,\n    heron,\n    guilt,\n    trader\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(folder) AS youth,\n    MAX(guilt) AS cheek\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     folder,\n     heron,\n     guilt,\n     trader,\n     keeper\n|> AGGREGATE\n     SUM(keeper) AS patch\n   GROUP BY\n     folder,\n     heron,\n     guilt,\n     trader\n|> AGGREGATE\n     AVG(patch) AS stitch\n   GROUP BY\n     folder,\n     heron,\n     guilt,\n     trader\n|> SELECT\n     space,\n     folder,\n     heron,\n     guilt,\n     trader\n|> AGGREGATE\n     AVG(folder) AS youth,\n     MAX(guilt) AS cheek\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"space",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rebate,\n    green,\n    radar,\n    driver,\n    proton\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    rebate,\n    green,\n    radar,\n    driver,\n    MAX(proton) AS grave\n  FROM cte_0\n  GROUP BY\n    rebate,\n    green,\n    radar,\n    driver\n),\ncte_2 AS (\n  SELECT\n    rebate,\n    green,\n    radar,\n    driver,\n    MAX(grave) AS vessel\n  FROM cte_1\n  GROUP BY\n    rebate,\n    green,\n    radar,\n    driver\n),\ncte_3 AS (\n  SELECT\n    smell,\n    rebate,\n    green,\n    radar,\n    driver\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    rebate,\n    SUM(green) AS kernel\n  FROM cte_3\n  GROUP BY\n    rebate\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rebate,\n     green,\n     radar,\n     driver,\n     proton\n|> AGGREGATE\n     MAX(proton) AS grave\n   GROUP BY\n     rebate,\n     green,\n     radar,\n     driver\n|> AGGREGATE\n     MAX(grave) AS vessel\n   GROUP BY\n     rebate,\n     green,\n     radar,\n     driver\n|> SELECT\n     smell,\n     rebate,\n     green,\n     radar,\n     driver\n|> AGGREGATE\n     SUM(green) AS kernel\n   GROUP BY\n     rebate\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"smell",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    saucer,\n    sensor,\n    plain,\n    blade,\n    shoal\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    saucer,\n    sensor,\n    plain,\n    blade,\n    AVG(shoal) AS orient\n  FROM cte_0\n  GROUP BY\n    saucer,\n    sensor,\n    plain,\n    blade\n),\ncte_2 AS (\n  SELECT\n    saucer,\n    sensor,\n    plain,\n    blade,\n    COUNT(orient) AS vacuum\n  FROM cte_1\n  GROUP BY\n    saucer,\n    sensor,\n    plain,\n    blade\n),\ncte_3 AS (\n  SELECT\n    smear,\n    saucer,\n    sensor\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    saucer,\n    MIN(sensor) AS uproar,\n    AVG(sensor) AS gospel\n  FROM cte_3\n  GROUP BY\n    saucer\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     saucer,\n     sensor,\n     plain,\n     blade,\n     shoal\n|> AGGREGATE\n     AVG(shoal) AS orient\n   GROUP BY\n     saucer,\n     sensor,\n     plain,\n     blade\n|> AGGREGATE\n     COUNT(orient) AS vacuum\n   GROUP BY\n     saucer,\n     sensor,\n     plain,\n     blade\n|> SELECT\n     smear,\n     saucer,\n     sensor\n|> AGGREGATE\n     MIN(sensor) AS uproar,\n     AVG(sensor) AS gospel\n   GROUP BY\n     saucer\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"smear",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rarity,\n    satire,\n    animal,\n    equity,\n    thread\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    rarity,\n    satire,\n    animal,\n    equity,\n    SUM(thread) AS apollo\n  FROM cte_0\n  GROUP BY\n    rarity,\n    satire,\n    animal,\n    equity\n),\ncte_2 AS (\n  SELECT\n    rarity,\n    satire,\n    animal,\n    equity,\n    MAX(apollo) AS malice\n  FROM cte_1\n  GROUP BY\n    rarity,\n    satire,\n    animal,\n    equity\n),\ncte_3 AS (\n  SELECT\n    perry,\n    rarity,\n    satire,\n    animal\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(satire) AS defeat,\n    MIN(animal) AS norman\n  FROM cte_3\n  GROUP BY\n    rarity\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rarity,\n     satire,\n     animal,\n     equity,\n     thread\n|> AGGREGATE\n     SUM(thread) AS apollo\n   GROUP BY\n     rarity,\n     satire,\n     animal,\n     equity\n|> AGGREGATE\n     MAX(apollo) AS malice\n   GROUP BY\n     rarity,\n     satire,\n     animal,\n     equity\n|> SELECT\n     perry,\n     rarity,\n     satire,\n     animal\n|> AGGREGATE\n     MIN(satire) AS defeat,\n     MIN(animal) AS norman\n   GROUP BY\n     rarity\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"perry",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    voter,\n    derby,\n    raffle,\n    fiasco,\n    fauna\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    voter,\n    derby,\n    raffle,\n    fiasco,\n    AVG(fauna) AS swamp\n  FROM cte_0\n  GROUP BY\n    voter,\n    derby,\n    raffle,\n    fiasco\n),\ncte_2 AS (\n  SELECT\n    voter,\n    derby,\n    raffle,\n    fiasco,\n    SUM(swamp) AS venus\n  FROM cte_1\n  GROUP BY\n    voter,\n    derby,\n    raffle,\n    fiasco\n),\ncte_3 AS (\n  SELECT\n    airbus,\n    voter,\n    derby,\n    raffle,\n    fiasco\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(voter) AS patsy,\n    MIN(raffle) AS novice\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     voter,\n     derby,\n     raffle,\n     fiasco,\n     fauna\n|> AGGREGATE\n     AVG(fauna) AS swamp\n   GROUP BY\n     voter,\n     derby,\n     raffle,\n     fiasco\n|> AGGREGATE\n     SUM(swamp) AS venus\n   GROUP BY\n     voter,\n     derby,\n     raffle,\n     fiasco\n|> SELECT\n     airbus,\n     voter,\n     derby,\n     raffle,\n     fiasco\n|> AGGREGATE\n     SUM(voter) AS patsy,\n     MIN(raffle) AS novice\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"airbus",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sheep,\n    marsh,\n    farmer,\n    image,\n    maple\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    sheep,\n    marsh,\n    farmer,\n    image,\n    MIN(maple) AS daphne\n  FROM cte_0\n  GROUP BY\n    sheep,\n    marsh,\n    farmer,\n    image\n),\ncte_2 AS (\n  SELECT\n    sheep,\n    marsh,\n    farmer,\n    image,\n    COUNT(daphne) AS thrust\n  FROM cte_1\n  GROUP BY\n    sheep,\n    marsh,\n    farmer,\n    image\n),\ncte_3 AS (\n  SELECT\n    switch,\n    sheep,\n    marsh,\n    farmer,\n    image\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    sheep,\n    AVG(farmer) AS vendor\n  FROM cte_3\n  GROUP BY\n    sheep\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sheep,\n     marsh,\n     farmer,\n     image,\n     maple\n|> AGGREGATE\n     MIN(maple) AS daphne\n   GROUP BY\n     sheep,\n     marsh,\n     farmer,\n     image\n|> AGGREGATE\n     COUNT(daphne) AS thrust\n   GROUP BY\n     sheep,\n     marsh,\n     farmer,\n     image\n|> SELECT\n     switch,\n     sheep,\n     marsh,\n     farmer,\n     image\n|> AGGREGATE\n     AVG(farmer) AS vendor\n   GROUP BY\n     sheep\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"switch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    speech,\n    favour,\n    primer,\n    bunker,\n    butter\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    speech,\n    favour,\n    primer,\n    bunker,\n    AVG(butter) AS gender\n  FROM cte_0\n  GROUP BY\n    speech,\n    favour,\n    primer,\n    bunker\n),\ncte_2 AS (\n  SELECT\n    speech,\n    favour,\n    primer,\n    bunker,\n    AVG(gender) AS patent\n  FROM cte_1\n  GROUP BY\n    speech,\n    favour,\n    primer,\n    bunker\n),\ncte_3 AS (\n  SELECT\n    saloon,\n    speech,\n    favour,\n    primer,\n    bunker\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(bunker) AS esteem\n  FROM cte_3\n  GROUP BY\n    speech,\n    favour\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     speech,\n     favour,\n     primer,\n     bunker,\n     butter\n|> AGGREGATE\n     AVG(butter) AS gender\n   GROUP BY\n     speech,\n     favour,\n     primer,\n     bunker\n|> AGGREGATE\n     AVG(gender) AS patent\n   GROUP BY\n     speech,\n     favour,\n     primer,\n     bunker\n|> SELECT\n     saloon,\n     speech,\n     favour,\n     primer,\n     bunker\n|> AGGREGATE\n     MAX(bunker) AS esteem\n   GROUP BY\n     speech,\n     favour\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"saloon",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    groove,\n    rugby,\n    advert,\n    barton,\n    radar\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    groove,\n    rugby,\n    advert,\n    barton,\n    AVG(radar) AS wrong\n  FROM cte_0\n  GROUP BY\n    groove,\n    rugby,\n    advert,\n    barton\n),\ncte_2 AS (\n  SELECT\n    groove,\n    rugby,\n    advert,\n    barton,\n    MIN(wrong) AS savoy\n  FROM cte_1\n  GROUP BY\n    groove,\n    rugby,\n    advert,\n    barton\n),\ncte_3 AS (\n  SELECT\n    plain,\n    groove,\n    rugby,\n    advert,\n    barton\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(advert) AS turkey\n  FROM cte_3\n  GROUP BY\n    groove,\n    rugby\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     groove,\n     rugby,\n     advert,\n     barton,\n     radar\n|> AGGREGATE\n     AVG(radar) AS wrong\n   GROUP BY\n     groove,\n     rugby,\n     advert,\n     barton\n|> AGGREGATE\n     MIN(wrong) AS savoy\n   GROUP BY\n     groove,\n     rugby,\n     advert,\n     barton\n|> SELECT\n     plain,\n     groove,\n     rugby,\n     advert,\n     barton\n|> AGGREGATE\n     COUNT(advert) AS turkey\n   GROUP BY\n     groove,\n     rugby\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"plain",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    maple,\n    ribber,\n    hurst,\n    mutant,\n    debut\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    maple,\n    ribber,\n    hurst,\n    mutant,\n    SUM(debut) AS tyler\n  FROM cte_0\n  GROUP BY\n    maple,\n    ribber,\n    hurst,\n    mutant\n),\ncte_2 AS (\n  SELECT\n    maple,\n    ribber,\n    hurst,\n    mutant,\n    SUM(tyler) AS spear\n  FROM cte_1\n  GROUP BY\n    maple,\n    ribber,\n    hurst,\n    mutant\n),\ncte_3 AS (\n  SELECT\n    manner,\n    maple,\n    ribber\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    maple,\n    SUM(ribber) AS sphere,\n    MAX(ribber) AS rebate\n  FROM cte_3\n  GROUP BY\n    maple\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     maple,\n     ribber,\n     hurst,\n     mutant,\n     debut\n|> AGGREGATE\n     SUM(debut) AS tyler\n   GROUP BY\n     maple,\n     ribber,\n     hurst,\n     mutant\n|> AGGREGATE\n     SUM(tyler) AS spear\n   GROUP BY\n     maple,\n     ribber,\n     hurst,\n     mutant\n|> SELECT\n     manner,\n     maple,\n     ribber\n|> AGGREGATE\n     SUM(ribber) AS sphere,\n     MAX(ribber) AS rebate\n   GROUP BY\n     maple\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"manner",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hooker,\n    final,\n    sharp,\n    stereo,\n    pause\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    hooker,\n    final,\n    sharp,\n    stereo,\n    MAX(pause) AS legend\n  FROM cte_0\n  GROUP BY\n    hooker,\n    final,\n    sharp,\n    stereo\n),\ncte_2 AS (\n  SELECT\n    hooker,\n    final,\n    sharp,\n    stereo,\n    AVG(legend) AS layer\n  FROM cte_1\n  GROUP BY\n    hooker,\n    final,\n    sharp,\n    stereo\n),\ncte_3 AS (\n  SELECT\n    beggar,\n    hooker,\n    final,\n    sharp,\n    stereo\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    hooker,\n    final,\n    sharp,\n    stereo\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hooker,\n     final,\n     sharp,\n     stereo,\n     pause\n|> AGGREGATE\n     MAX(pause) AS legend\n   GROUP BY\n     hooker,\n     final,\n     sharp,\n     stereo\n|> AGGREGATE\n     AVG(legend) AS layer\n   GROUP BY\n     hooker,\n     final,\n     sharp,\n     stereo\n|> SELECT\n     beggar,\n     hooker,\n     final,\n     sharp,\n     stereo\n|> SELECT\n     hooker,\n     final,\n     sharp,\n     stereo\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"beggar",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rover,\n    option,\n    sister,\n    grove,\n    novel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    rover,\n    option,\n    sister,\n    grove,\n    SUM(novel) AS minute\n  FROM cte_0\n  GROUP BY\n    rover,\n    option,\n    sister,\n    grove\n),\ncte_2 AS (\n  SELECT\n    rover,\n    option,\n    sister,\n    grove,\n    AVG(minute) AS gleam\n  FROM cte_1\n  GROUP BY\n    rover,\n    option,\n    sister,\n    grove\n),\ncte_3 AS (\n  SELECT\n    dealer,\n    rover,\n    option\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    rover,\n    AVG(option) AS helper,\n    SUM(option) AS stunt\n  FROM cte_3\n  GROUP BY\n    rover\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rover,\n     option,\n     sister,\n     grove,\n     novel\n|> AGGREGATE\n     SUM(novel) AS minute\n   GROUP BY\n     rover,\n     option,\n     sister,\n     grove\n|> AGGREGATE\n     AVG(minute) AS gleam\n   GROUP BY\n     rover,\n     option,\n     sister,\n     grove\n|> SELECT\n     dealer,\n     rover,\n     option\n|> AGGREGATE\n     AVG(option) AS helper,\n     SUM(option) AS stunt\n   GROUP BY\n     rover\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"dealer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    velvet,\n    relief,\n    lease,\n    patsy,\n    prince\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    velvet,\n    relief,\n    lease,\n    patsy,\n    SUM(prince) AS blame\n  FROM cte_0\n  GROUP BY\n    velvet,\n    relief,\n    lease,\n    patsy\n),\ncte_2 AS (\n  SELECT\n    velvet,\n    relief,\n    lease,\n    patsy,\n    AVG(blame) AS michel\n  FROM cte_1\n  GROUP BY\n    velvet,\n    relief,\n    lease,\n    patsy\n),\ncte_3 AS (\n  SELECT\n    fulham,\n    velvet,\n    relief,\n    lease\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(lease) AS cattle,\n    MIN(relief) AS beacon\n  FROM cte_3\n  GROUP BY\n    velvet\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     velvet,\n     relief,\n     lease,\n     patsy,\n     prince\n|> AGGREGATE\n     SUM(prince) AS blame\n   GROUP BY\n     velvet,\n     relief,\n     lease,\n     patsy\n|> AGGREGATE\n     AVG(blame) AS michel\n   GROUP BY\n     velvet,\n     relief,\n     lease,\n     patsy\n|> SELECT\n     fulham,\n     velvet,\n     relief,\n     lease\n|> AGGREGATE\n     MIN(lease) AS cattle,\n     MIN(relief) AS beacon\n   GROUP BY\n     velvet\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"fulham",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    olive,\n    combat,\n    scare,\n    apathy,\n    death\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    olive,\n    combat,\n    scare,\n    apathy,\n    AVG(death) AS wedge\n  FROM cte_0\n  GROUP BY\n    olive,\n    combat,\n    scare,\n    apathy\n),\ncte_2 AS (\n  SELECT\n    olive,\n    combat,\n    scare,\n    apathy,\n    AVG(wedge) AS local\n  FROM cte_1\n  GROUP BY\n    olive,\n    combat,\n    scare,\n    apathy\n),\ncte_3 AS (\n  SELECT\n    fright,\n    olive,\n    combat,\n    scare\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    olive,\n    SUM(scare) AS blitz\n  FROM cte_3\n  GROUP BY\n    olive,\n    combat\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     olive,\n     combat,\n     scare,\n     apathy,\n     death\n|> AGGREGATE\n     AVG(death) AS wedge\n   GROUP BY\n     olive,\n     combat,\n     scare,\n     apathy\n|> AGGREGATE\n     AVG(wedge) AS local\n   GROUP BY\n     olive,\n     combat,\n     scare,\n     apathy\n|> SELECT\n     fright,\n     olive,\n     combat,\n     scare\n|> AGGREGATE\n     SUM(scare) AS blitz\n   GROUP BY\n     olive,\n     combat\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"fright",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    label,\n    killer,\n    bonnet,\n    ginger,\n    pirate\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    label,\n    killer,\n    bonnet,\n    ginger,\n    SUM(pirate) AS turner\n  FROM cte_0\n  GROUP BY\n    label,\n    killer,\n    bonnet,\n    ginger\n),\ncte_2 AS (\n  SELECT\n    label,\n    killer,\n    bonnet,\n    ginger,\n    AVG(turner) AS labour\n  FROM cte_1\n  GROUP BY\n    label,\n    killer,\n    bonnet,\n    ginger\n),\ncte_3 AS (\n  SELECT\n    regret,\n    label,\n    killer,\n    bonnet\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    label,\n    MAX(bonnet) AS plaque\n  FROM cte_3\n  GROUP BY\n    label,\n    killer\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     label,\n     killer,\n     bonnet,\n     ginger,\n     pirate\n|> AGGREGATE\n     SUM(pirate) AS turner\n   GROUP BY\n     label,\n     killer,\n     bonnet,\n     ginger\n|> AGGREGATE\n     AVG(turner) AS labour\n   GROUP BY\n     label,\n     killer,\n     bonnet,\n     ginger\n|> SELECT\n     regret,\n     label,\n     killer,\n     bonnet\n|> AGGREGATE\n     MAX(bonnet) AS plaque\n   GROUP BY\n     label,\n     killer\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"regret",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    fowler,\n    upside,\n    sermon,\n    eagle,\n    other\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    fowler,\n    upside,\n    sermon,\n    eagle,\n    SUM(other) AS cattle\n  FROM cte_0\n  GROUP BY\n    fowler,\n    upside,\n    sermon,\n    eagle\n),\ncte_2 AS (\n  SELECT\n    fowler,\n    upside,\n    sermon,\n    eagle,\n    MIN(cattle) AS farmer\n  FROM cte_1\n  GROUP BY\n    fowler,\n    upside,\n    sermon,\n    eagle\n),\ncte_3 AS (\n  SELECT\n    ferret,\n    fowler,\n    upside,\n    sermon,\n    eagle\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    fowler,\n    upside,\n    sermon,\n    eagle\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     fowler,\n     upside,\n     sermon,\n     eagle,\n     other\n|> AGGREGATE\n     SUM(other) AS cattle\n   GROUP BY\n     fowler,\n     upside,\n     sermon,\n     eagle\n|> AGGREGATE\n     MIN(cattle) AS farmer\n   GROUP BY\n     fowler,\n     upside,\n     sermon,\n     eagle\n|> SELECT\n     ferret,\n     fowler,\n     upside,\n     sermon,\n     eagle\n|> SELECT\n     fowler,\n     upside,\n     sermon,\n     eagle\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"ferret",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    argyll,\n    turkey,\n    relic,\n    metro,\n    liner\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    argyll,\n    turkey,\n    relic,\n    metro,\n    COUNT(liner) AS weight\n  FROM cte_0\n  GROUP BY\n    argyll,\n    turkey,\n    relic,\n    metro\n),\ncte_2 AS (\n  SELECT\n    argyll,\n    turkey,\n    relic,\n    metro,\n    SUM(weight) AS comer\n  FROM cte_1\n  GROUP BY\n    argyll,\n    turkey,\n    relic,\n    metro\n),\ncte_3 AS (\n  SELECT\n    toast,\n    argyll,\n    turkey\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(turkey) AS saliva,\n    AVG(argyll) AS banner,\n    COUNT(turkey) AS muscle\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     argyll,\n     turkey,\n     relic,\n     metro,\n     liner\n|> AGGREGATE\n     COUNT(liner) AS weight\n   GROUP BY\n     argyll,\n     turkey,\n     relic,\n     metro\n|> AGGREGATE\n     SUM(weight) AS comer\n   GROUP BY\n     argyll,\n     turkey,\n     relic,\n     metro\n|> SELECT\n     toast,\n     argyll,\n     turkey\n|> AGGREGATE\n     MIN(turkey) AS saliva,\n     AVG(argyll) AS banner,\n     COUNT(turkey) AS muscle\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"toast",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rudder,\n    steam,\n    motif,\n    habit,\n    oscar\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    rudder,\n    steam,\n    motif,\n    habit,\n    MIN(oscar) AS reward\n  FROM cte_0\n  GROUP BY\n    rudder,\n    steam,\n    motif,\n    habit\n),\ncte_2 AS (\n  SELECT\n    rudder,\n    steam,\n    motif,\n    habit,\n    SUM(reward) AS lorry\n  FROM cte_1\n  GROUP BY\n    rudder,\n    steam,\n    motif,\n    habit\n),\ncte_3 AS (\n  SELECT\n    chapel,\n    rudder,\n    steam,\n    motif\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    rudder,\n    MAX(motif) AS treaty\n  FROM cte_3\n  GROUP BY\n    rudder,\n    steam\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rudder,\n     steam,\n     motif,\n     habit,\n     oscar\n|> AGGREGATE\n     MIN(oscar) AS reward\n   GROUP BY\n     rudder,\n     steam,\n     motif,\n     habit\n|> AGGREGATE\n     SUM(reward) AS lorry\n   GROUP BY\n     rudder,\n     steam,\n     motif,\n     habit\n|> SELECT\n     chapel,\n     rudder,\n     steam,\n     motif\n|> AGGREGATE\n     MAX(motif) AS treaty\n   GROUP BY\n     rudder,\n     steam\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"chapel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tumour,\n    topic,\n    franc,\n    advice,\n    payne\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    tumour,\n    topic,\n    franc,\n    advice,\n    SUM(payne) AS stare\n  FROM cte_0\n  GROUP BY\n    tumour,\n    topic,\n    franc,\n    advice\n),\ncte_2 AS (\n  SELECT\n    tumour,\n    topic,\n    franc,\n    advice,\n    SUM(stare) AS budget\n  FROM cte_1\n  GROUP BY\n    tumour,\n    topic,\n    franc,\n    advice\n),\ncte_3 AS (\n  SELECT\n    venus,\n    tumour,\n    topic,\n    franc,\n    advice,\n    budget\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    tumour,\n    topic,\n    franc\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tumour,\n     topic,\n     franc,\n     advice,\n     payne\n|> AGGREGATE\n     SUM(payne) AS stare\n   GROUP BY\n     tumour,\n     topic,\n     franc,\n     advice\n|> AGGREGATE\n     SUM(stare) AS budget\n   GROUP BY\n     tumour,\n     topic,\n     franc,\n     advice\n|> SELECT\n     venus,\n     tumour,\n     topic,\n     franc,\n     advice,\n     budget\n|> SELECT\n     tumour,\n     topic,\n     franc\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"venus",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    syrup,\n    report,\n    video,\n    scope,\n    brink\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    syrup,\n    report,\n    video,\n    scope,\n    SUM(brink) AS value\n  FROM cte_0\n  GROUP BY\n    syrup,\n    report,\n    video,\n    scope\n),\ncte_2 AS (\n  SELECT\n    syrup,\n    report,\n    video,\n    scope,\n    MIN(value) AS surge\n  FROM cte_1\n  GROUP BY\n    syrup,\n    report,\n    video,\n    scope\n),\ncte_3 AS (\n  SELECT\n    alkali,\n    syrup,\n    report,\n    video,\n    scope\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(syrup) AS upturn,\n    AVG(syrup) AS enemy\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     syrup,\n     report,\n     video,\n     scope,\n     brink\n|> AGGREGATE\n     SUM(brink) AS value\n   GROUP BY\n     syrup,\n     report,\n     video,\n     scope\n|> AGGREGATE\n     MIN(value) AS surge\n   GROUP BY\n     syrup,\n     report,\n     video,\n     scope\n|> SELECT\n     alkali,\n     syrup,\n     report,\n     video,\n     scope\n|> AGGREGATE\n     SUM(syrup) AS upturn,\n     AVG(syrup) AS enemy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"alkali",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    parrot,\n    shaft,\n    boiler,\n    guess,\n    street\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    parrot,\n    shaft,\n    boiler,\n    guess,\n    SUM(street) AS curate\n  FROM cte_0\n  GROUP BY\n    parrot,\n    shaft,\n    boiler,\n    guess\n),\ncte_2 AS (\n  SELECT\n    parrot,\n    shaft,\n    boiler,\n    guess,\n    MIN(curate) AS island\n  FROM cte_1\n  GROUP BY\n    parrot,\n    shaft,\n    boiler,\n    guess\n),\ncte_3 AS (\n  SELECT\n    basic,\n    parrot,\n    shaft\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(parrot) AS music,\n    MIN(shaft) AS harem,\n    COUNT(shaft) AS blitz\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     parrot,\n     shaft,\n     boiler,\n     guess,\n     street\n|> AGGREGATE\n     SUM(street) AS curate\n   GROUP BY\n     parrot,\n     shaft,\n     boiler,\n     guess\n|> AGGREGATE\n     MIN(curate) AS island\n   GROUP BY\n     parrot,\n     shaft,\n     boiler,\n     guess\n|> SELECT\n     basic,\n     parrot,\n     shaft\n|> AGGREGATE\n     COUNT(parrot) AS music,\n     MIN(shaft) AS harem,\n     COUNT(shaft) AS blitz\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"basic",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    witney,\n    class,\n    argyll,\n    batch,\n    brooke\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    witney,\n    class,\n    argyll,\n    batch,\n    SUM(brooke) AS cherry\n  FROM cte_0\n  GROUP BY\n    witney,\n    class,\n    argyll,\n    batch\n),\ncte_2 AS (\n  SELECT\n    witney,\n    class,\n    argyll,\n    batch,\n    SUM(cherry) AS alaska\n  FROM cte_1\n  GROUP BY\n    witney,\n    class,\n    argyll,\n    batch\n),\ncte_3 AS (\n  SELECT\n    unison,\n    witney,\n    class,\n    argyll,\n    batch\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(argyll) AS patio\n  FROM cte_3\n  GROUP BY\n    witney,\n    class\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     witney,\n     class,\n     argyll,\n     batch,\n     brooke\n|> AGGREGATE\n     SUM(brooke) AS cherry\n   GROUP BY\n     witney,\n     class,\n     argyll,\n     batch\n|> AGGREGATE\n     SUM(cherry) AS alaska\n   GROUP BY\n     witney,\n     class,\n     argyll,\n     batch\n|> SELECT\n     unison,\n     witney,\n     class,\n     argyll,\n     batch\n|> AGGREGATE\n     AVG(argyll) AS patio\n   GROUP BY\n     witney,\n     class\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"unison",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    stroud,\n    wally,\n    rigour,\n    canal,\n    flair\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    stroud,\n    wally,\n    rigour,\n    canal,\n    MIN(flair) AS author\n  FROM cte_0\n  GROUP BY\n    stroud,\n    wally,\n    rigour,\n    canal\n),\ncte_2 AS (\n  SELECT\n    stroud,\n    wally,\n    rigour,\n    canal,\n    MIN(author) AS riddle\n  FROM cte_1\n  GROUP BY\n    stroud,\n    wally,\n    rigour,\n    canal\n),\ncte_3 AS (\n  SELECT\n    manual,\n    stroud,\n    wally\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    stroud,\n    AVG(wally) AS grace,\n    MIN(wally) AS snail\n  FROM cte_3\n  GROUP BY\n    stroud\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     stroud,\n     wally,\n     rigour,\n     canal,\n     flair\n|> AGGREGATE\n     MIN(flair) AS author\n   GROUP BY\n     stroud,\n     wally,\n     rigour,\n     canal\n|> AGGREGATE\n     MIN(author) AS riddle\n   GROUP BY\n     stroud,\n     wally,\n     rigour,\n     canal\n|> SELECT\n     manual,\n     stroud,\n     wally\n|> AGGREGATE\n     AVG(wally) AS grace,\n     MIN(wally) AS snail\n   GROUP BY\n     stroud\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"manual",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    source,\n    flake,\n    waiter,\n    campus,\n    track\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    source,\n    flake,\n    waiter,\n    campus,\n    MAX(track) AS altar\n  FROM cte_0\n  GROUP BY\n    source,\n    flake,\n    waiter,\n    campus\n),\ncte_2 AS (\n  SELECT\n    source,\n    flake,\n    waiter,\n    campus,\n    MAX(altar) AS sunset\n  FROM cte_1\n  GROUP BY\n    source,\n    flake,\n    waiter,\n    campus\n),\ncte_3 AS (\n  SELECT\n    celery,\n    source,\n    flake,\n    waiter,\n    campus\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(waiter) AS output,\n    MAX(campus) AS thread\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     source,\n     flake,\n     waiter,\n     campus,\n     track\n|> AGGREGATE\n     MAX(track) AS altar\n   GROUP BY\n     source,\n     flake,\n     waiter,\n     campus\n|> AGGREGATE\n     MAX(altar) AS sunset\n   GROUP BY\n     source,\n     flake,\n     waiter,\n     campus\n|> SELECT\n     celery,\n     source,\n     flake,\n     waiter,\n     campus\n|> AGGREGATE\n     MIN(waiter) AS output,\n     MAX(campus) AS thread\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"celery",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    pedal,\n    basis,\n    breath,\n    grand,\n    prior\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    pedal,\n    basis,\n    breath,\n    grand,\n    MIN(prior) AS slater\n  FROM cte_0\n  GROUP BY\n    pedal,\n    basis,\n    breath,\n    grand\n),\ncte_2 AS (\n  SELECT\n    pedal,\n    basis,\n    breath,\n    grand,\n    COUNT(slater) AS lodge\n  FROM cte_1\n  GROUP BY\n    pedal,\n    basis,\n    breath,\n    grand\n),\ncte_3 AS (\n  SELECT\n    dollar,\n    pedal,\n    basis,\n    breath,\n    grand,\n    lodge\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    pedal,\n    basis,\n    breath\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     pedal,\n     basis,\n     breath,\n     grand,\n     prior\n|> AGGREGATE\n     MIN(prior) AS slater\n   GROUP BY\n     pedal,\n     basis,\n     breath,\n     grand\n|> AGGREGATE\n     COUNT(slater) AS lodge\n   GROUP BY\n     pedal,\n     basis,\n     breath,\n     grand\n|> SELECT\n     dollar,\n     pedal,\n     basis,\n     breath,\n     grand,\n     lodge\n|> SELECT\n     pedal,\n     basis,\n     breath\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"dollar",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    fence,\n    torch,\n    exile,\n    horse,\n    binder\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    fence,\n    torch,\n    exile,\n    horse,\n    COUNT(binder) AS ransom\n  FROM cte_0\n  GROUP BY\n    fence,\n    torch,\n    exile,\n    horse\n),\ncte_2 AS (\n  SELECT\n    fence,\n    torch,\n    exile,\n    horse,\n    MAX(ransom) AS route\n  FROM cte_1\n  GROUP BY\n    fence,\n    torch,\n    exile,\n    horse\n),\ncte_3 AS (\n  SELECT\n    forest,\n    fence,\n    torch\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    fence,\n    SUM(torch) AS stick,\n    COUNT(torch) AS quote\n  FROM cte_3\n  GROUP BY\n    fence\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     fence,\n     torch,\n     exile,\n     horse,\n     binder\n|> AGGREGATE\n     COUNT(binder) AS ransom\n   GROUP BY\n     fence,\n     torch,\n     exile,\n     horse\n|> AGGREGATE\n     MAX(ransom) AS route\n   GROUP BY\n     fence,\n     torch,\n     exile,\n     horse\n|> SELECT\n     forest,\n     fence,\n     torch\n|> AGGREGATE\n     SUM(torch) AS stick,\n     COUNT(torch) AS quote\n   GROUP BY\n     fence\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"forest",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    gully,\n    trace,\n    burgh,\n    buddy,\n    esteem\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    gully,\n    trace,\n    burgh,\n    buddy,\n    AVG(esteem) AS aspect\n  FROM cte_0\n  GROUP BY\n    gully,\n    trace,\n    burgh,\n    buddy\n),\ncte_2 AS (\n  SELECT\n    gully,\n    trace,\n    burgh,\n    buddy,\n    MIN(aspect) AS grange\n  FROM cte_1\n  GROUP BY\n    gully,\n    trace,\n    burgh,\n    buddy\n),\ncte_3 AS (\n  SELECT\n    string,\n    gully,\n    trace,\n    burgh\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(trace) AS regret,\n    AVG(burgh) AS return\n  FROM cte_3\n  GROUP BY\n    gully\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     gully,\n     trace,\n     burgh,\n     buddy,\n     esteem\n|> AGGREGATE\n     AVG(esteem) AS aspect\n   GROUP BY\n     gully,\n     trace,\n     burgh,\n     buddy\n|> AGGREGATE\n     MIN(aspect) AS grange\n   GROUP BY\n     gully,\n     trace,\n     burgh,\n     buddy\n|> SELECT\n     string,\n     gully,\n     trace,\n     burgh\n|> AGGREGATE\n     COUNT(trace) AS regret,\n     AVG(burgh) AS return\n   GROUP BY\n     gully\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"string",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    venom,\n    plague,\n    regent,\n    excuse,\n    sonata\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    venom,\n    plague,\n    regent,\n    excuse,\n    SUM(sonata) AS assent\n  FROM cte_0\n  GROUP BY\n    venom,\n    plague,\n    regent,\n    excuse\n),\ncte_2 AS (\n  SELECT\n    venom,\n    plague,\n    regent,\n    excuse,\n    SUM(assent) AS glory\n  FROM cte_1\n  GROUP BY\n    venom,\n    plague,\n    regent,\n    excuse\n),\ncte_3 AS (\n  SELECT\n    needle,\n    venom,\n    plague\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    venom,\n    MAX(plague) AS throw,\n    AVG(plague) AS prayer\n  FROM cte_3\n  GROUP BY\n    venom\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     venom,\n     plague,\n     regent,\n     excuse,\n     sonata\n|> AGGREGATE\n     SUM(sonata) AS assent\n   GROUP BY\n     venom,\n     plague,\n     regent,\n     excuse\n|> AGGREGATE\n     SUM(assent) AS glory\n   GROUP BY\n     venom,\n     plague,\n     regent,\n     excuse\n|> SELECT\n     needle,\n     venom,\n     plague\n|> AGGREGATE\n     MAX(plague) AS throw,\n     AVG(plague) AS prayer\n   GROUP BY\n     venom\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"needle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    martyr,\n    ribber,\n    quota,\n    denial,\n    cheer\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    martyr,\n    ribber,\n    quota,\n    denial,\n    SUM(cheer) AS baker\n  FROM cte_0\n  GROUP BY\n    martyr,\n    ribber,\n    quota,\n    denial\n),\ncte_2 AS (\n  SELECT\n    martyr,\n    ribber,\n    quota,\n    denial,\n    SUM(baker) AS spouse\n  FROM cte_1\n  GROUP BY\n    martyr,\n    ribber,\n    quota,\n    denial\n),\ncte_3 AS (\n  SELECT\n    piper,\n    martyr,\n    ribber,\n    quota,\n    denial\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    martyr,\n    ribber,\n    quota,\n    denial\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     martyr,\n     ribber,\n     quota,\n     denial,\n     cheer\n|> AGGREGATE\n     SUM(cheer) AS baker\n   GROUP BY\n     martyr,\n     ribber,\n     quota,\n     denial\n|> AGGREGATE\n     SUM(baker) AS spouse\n   GROUP BY\n     martyr,\n     ribber,\n     quota,\n     denial\n|> SELECT\n     piper,\n     martyr,\n     ribber,\n     quota,\n     denial\n|> SELECT\n     martyr,\n     ribber,\n     quota,\n     denial\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"piper",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    jimmy,\n    comet,\n    crown,\n    cache,\n    idiot\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    jimmy,\n    comet,\n    crown,\n    cache,\n    MAX(idiot) AS winner\n  FROM cte_0\n  GROUP BY\n    jimmy,\n    comet,\n    crown,\n    cache\n),\ncte_2 AS (\n  SELECT\n    jimmy,\n    comet,\n    crown,\n    cache,\n    MAX(winner) AS genre\n  FROM cte_1\n  GROUP BY\n    jimmy,\n    comet,\n    crown,\n    cache\n),\ncte_3 AS (\n  SELECT\n    winter,\n    jimmy,\n    comet,\n    crown\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    jimmy,\n    MAX(crown) AS steen\n  FROM cte_3\n  GROUP BY\n    jimmy,\n    comet\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     jimmy,\n     comet,\n     crown,\n     cache,\n     idiot\n|> AGGREGATE\n     MAX(idiot) AS winner\n   GROUP BY\n     jimmy,\n     comet,\n     crown,\n     cache\n|> AGGREGATE\n     MAX(winner) AS genre\n   GROUP BY\n     jimmy,\n     comet,\n     crown,\n     cache\n|> SELECT\n     winter,\n     jimmy,\n     comet,\n     crown\n|> AGGREGATE\n     MAX(crown) AS steen\n   GROUP BY\n     jimmy,\n     comet\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"winter",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    salon,\n    barry,\n    poetry,\n    violin,\n    serial\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    salon,\n    barry,\n    poetry,\n    violin,\n    SUM(serial) AS bubble\n  FROM cte_0\n  GROUP BY\n    salon,\n    barry,\n    poetry,\n    violin\n),\ncte_2 AS (\n  SELECT\n    salon,\n    barry,\n    poetry,\n    violin,\n    COUNT(bubble) AS gossip\n  FROM cte_1\n  GROUP BY\n    salon,\n    barry,\n    poetry,\n    violin\n),\ncte_3 AS (\n  SELECT\n    letter,\n    salon,\n    barry,\n    poetry,\n    violin\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(violin) AS label,\n    AVG(violin) AS piety\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     salon,\n     barry,\n     poetry,\n     violin,\n     serial\n|> AGGREGATE\n     SUM(serial) AS bubble\n   GROUP BY\n     salon,\n     barry,\n     poetry,\n     violin\n|> AGGREGATE\n     COUNT(bubble) AS gossip\n   GROUP BY\n     salon,\n     barry,\n     poetry,\n     violin\n|> SELECT\n     letter,\n     salon,\n     barry,\n     poetry,\n     violin\n|> AGGREGATE\n     MIN(violin) AS label,\n     AVG(violin) AS piety\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"letter",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    gedge,\n    foetus,\n    prison,\n    mouth,\n    incest\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    gedge,\n    foetus,\n    prison,\n    mouth,\n    SUM(incest) AS michel\n  FROM cte_0\n  GROUP BY\n    gedge,\n    foetus,\n    prison,\n    mouth\n),\ncte_2 AS (\n  SELECT\n    gedge,\n    foetus,\n    prison,\n    mouth,\n    MAX(michel) AS ripple\n  FROM cte_1\n  GROUP BY\n    gedge,\n    foetus,\n    prison,\n    mouth\n),\ncte_3 AS (\n  SELECT\n    dieter,\n    gedge,\n    foetus,\n    prison,\n    mouth\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(mouth) AS bully,\n    MIN(prison) AS outing\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     gedge,\n     foetus,\n     prison,\n     mouth,\n     incest\n|> AGGREGATE\n     SUM(incest) AS michel\n   GROUP BY\n     gedge,\n     foetus,\n     prison,\n     mouth\n|> AGGREGATE\n     MAX(michel) AS ripple\n   GROUP BY\n     gedge,\n     foetus,\n     prison,\n     mouth\n|> SELECT\n     dieter,\n     gedge,\n     foetus,\n     prison,\n     mouth\n|> AGGREGATE\n     COUNT(mouth) AS bully,\n     MIN(prison) AS outing\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"dieter",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    minute,\n    raffle,\n    canoe,\n    moment,\n    supper\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    minute,\n    raffle,\n    canoe,\n    moment,\n    COUNT(supper) AS insult\n  FROM cte_0\n  GROUP BY\n    minute,\n    raffle,\n    canoe,\n    moment\n),\ncte_2 AS (\n  SELECT\n    minute,\n    raffle,\n    canoe,\n    moment,\n    MAX(insult) AS inset\n  FROM cte_1\n  GROUP BY\n    minute,\n    raffle,\n    canoe,\n    moment\n),\ncte_3 AS (\n  SELECT\n    vinyl,\n    minute,\n    raffle,\n    canoe,\n    moment\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    minute,\n    COUNT(moment) AS volume\n  FROM cte_3\n  GROUP BY\n    minute\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     minute,\n     raffle,\n     canoe,\n     moment,\n     supper\n|> AGGREGATE\n     COUNT(supper) AS insult\n   GROUP BY\n     minute,\n     raffle,\n     canoe,\n     moment\n|> AGGREGATE\n     MAX(insult) AS inset\n   GROUP BY\n     minute,\n     raffle,\n     canoe,\n     moment\n|> SELECT\n     vinyl,\n     minute,\n     raffle,\n     canoe,\n     moment\n|> AGGREGATE\n     COUNT(moment) AS volume\n   GROUP BY\n     minute\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"vinyl",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    caddie,\n    pride,\n    locus,\n    farce,\n    dobson\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    caddie,\n    pride,\n    locus,\n    farce,\n    AVG(dobson) AS length\n  FROM cte_0\n  GROUP BY\n    caddie,\n    pride,\n    locus,\n    farce\n),\ncte_2 AS (\n  SELECT\n    caddie,\n    pride,\n    locus,\n    farce,\n    AVG(length) AS climax\n  FROM cte_1\n  GROUP BY\n    caddie,\n    pride,\n    locus,\n    farce\n),\ncte_3 AS (\n  SELECT\n    miner,\n    caddie,\n    pride\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(caddie) AS closet,\n    MAX(caddie) AS xerox,\n    SUM(pride) AS barry\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     caddie,\n     pride,\n     locus,\n     farce,\n     dobson\n|> AGGREGATE\n     AVG(dobson) AS length\n   GROUP BY\n     caddie,\n     pride,\n     locus,\n     farce\n|> AGGREGATE\n     AVG(length) AS climax\n   GROUP BY\n     caddie,\n     pride,\n     locus,\n     farce\n|> SELECT\n     miner,\n     caddie,\n     pride\n|> AGGREGATE\n     COUNT(caddie) AS closet,\n     MAX(caddie) AS xerox,\n     SUM(pride) AS barry\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"miner",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    newton,\n    linen,\n    bidder,\n    pillow,\n    brett\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    newton,\n    linen,\n    bidder,\n    pillow,\n    MAX(brett) AS garlic\n  FROM cte_0\n  GROUP BY\n    newton,\n    linen,\n    bidder,\n    pillow\n),\ncte_2 AS (\n  SELECT\n    newton,\n    linen,\n    bidder,\n    pillow,\n    COUNT(garlic) AS thorn\n  FROM cte_1\n  GROUP BY\n    newton,\n    linen,\n    bidder,\n    pillow\n),\ncte_3 AS (\n  SELECT\n    banner,\n    newton,\n    linen,\n    bidder,\n    pillow\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(pillow) AS roche\n  FROM cte_3\n  GROUP BY\n    newton,\n    linen\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     newton,\n     linen,\n     bidder,\n     pillow,\n     brett\n|> AGGREGATE\n     MAX(brett) AS garlic\n   GROUP BY\n     newton,\n     linen,\n     bidder,\n     pillow\n|> AGGREGATE\n     COUNT(garlic) AS thorn\n   GROUP BY\n     newton,\n     linen,\n     bidder,\n     pillow\n|> SELECT\n     banner,\n     newton,\n     linen,\n     bidder,\n     pillow\n|> AGGREGATE\n     AVG(pillow) AS roche\n   GROUP BY\n     newton,\n     linen\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"banner",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    champ,\n    leigh,\n    skirt,\n    craven,\n    oxford\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    champ,\n    leigh,\n    skirt,\n    craven,\n    MAX(oxford) AS night\n  FROM cte_0\n  GROUP BY\n    champ,\n    leigh,\n    skirt,\n    craven\n),\ncte_2 AS (\n  SELECT\n    champ,\n    leigh,\n    skirt,\n    craven,\n    AVG(night) AS tomato\n  FROM cte_1\n  GROUP BY\n    champ,\n    leigh,\n    skirt,\n    craven\n),\ncte_3 AS (\n  SELECT\n    millie,\n    champ,\n    leigh,\n    skirt,\n    craven,\n    tomato\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(skirt) AS member\n  FROM cte_3\n  GROUP BY\n    champ\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     champ,\n     leigh,\n     skirt,\n     craven,\n     oxford\n|> AGGREGATE\n     MAX(oxford) AS night\n   GROUP BY\n     champ,\n     leigh,\n     skirt,\n     craven\n|> AGGREGATE\n     AVG(night) AS tomato\n   GROUP BY\n     champ,\n     leigh,\n     skirt,\n     craven\n|> SELECT\n     millie,\n     champ,\n     leigh,\n     skirt,\n     craven,\n     tomato\n|> AGGREGATE\n     COUNT(skirt) AS member\n   GROUP BY\n     champ\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"millie",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    banker,\n    drake,\n    ditch,\n    thrill,\n    relay\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    banker,\n    drake,\n    ditch,\n    thrill,\n    SUM(relay) AS patio\n  FROM cte_0\n  GROUP BY\n    banker,\n    drake,\n    ditch,\n    thrill\n),\ncte_2 AS (\n  SELECT\n    banker,\n    drake,\n    ditch,\n    thrill,\n    MAX(patio) AS power\n  FROM cte_1\n  GROUP BY\n    banker,\n    drake,\n    ditch,\n    thrill\n),\ncte_3 AS (\n  SELECT\n    grease,\n    banker,\n    drake,\n    ditch,\n    thrill\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(drake) AS start,\n    COUNT(thrill) AS jessie\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     banker,\n     drake,\n     ditch,\n     thrill,\n     relay\n|> AGGREGATE\n     SUM(relay) AS patio\n   GROUP BY\n     banker,\n     drake,\n     ditch,\n     thrill\n|> AGGREGATE\n     MAX(patio) AS power\n   GROUP BY\n     banker,\n     drake,\n     ditch,\n     thrill\n|> SELECT\n     grease,\n     banker,\n     drake,\n     ditch,\n     thrill\n|> AGGREGATE\n     MIN(drake) AS start,\n     COUNT(thrill) AS jessie\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"grease",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    barker,\n    clause,\n    phone,\n    strat,\n    revolt\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    barker,\n    clause,\n    phone,\n    strat,\n    MIN(revolt) AS handle\n  FROM cte_0\n  GROUP BY\n    barker,\n    clause,\n    phone,\n    strat\n),\ncte_2 AS (\n  SELECT\n    barker,\n    clause,\n    phone,\n    strat,\n    MAX(handle) AS proof\n  FROM cte_1\n  GROUP BY\n    barker,\n    clause,\n    phone,\n    strat\n),\ncte_3 AS (\n  SELECT\n    groin,\n    barker,\n    clause\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    barker,\n    SUM(clause) AS satire,\n    COUNT(clause) AS count\n  FROM cte_3\n  GROUP BY\n    barker\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     barker,\n     clause,\n     phone,\n     strat,\n     revolt\n|> AGGREGATE\n     MIN(revolt) AS handle\n   GROUP BY\n     barker,\n     clause,\n     phone,\n     strat\n|> AGGREGATE\n     MAX(handle) AS proof\n   GROUP BY\n     barker,\n     clause,\n     phone,\n     strat\n|> SELECT\n     groin,\n     barker,\n     clause\n|> AGGREGATE\n     SUM(clause) AS satire,\n     COUNT(clause) AS count\n   GROUP BY\n     barker\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"groin",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    honour,\n    rhine,\n    handle,\n    covent,\n    canopy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    honour,\n    rhine,\n    handle,\n    covent,\n    MIN(canopy) AS colour\n  FROM cte_0\n  GROUP BY\n    honour,\n    rhine,\n    handle,\n    covent\n),\ncte_2 AS (\n  SELECT\n    honour,\n    rhine,\n    handle,\n    covent,\n    AVG(colour) AS packet\n  FROM cte_1\n  GROUP BY\n    honour,\n    rhine,\n    handle,\n    covent\n),\ncte_3 AS (\n  SELECT\n    grief,\n    honour,\n    rhine,\n    handle,\n    covent\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    honour,\n    rhine,\n    handle,\n    covent\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     honour,\n     rhine,\n     handle,\n     covent,\n     canopy\n|> AGGREGATE\n     MIN(canopy) AS colour\n   GROUP BY\n     honour,\n     rhine,\n     handle,\n     covent\n|> AGGREGATE\n     AVG(colour) AS packet\n   GROUP BY\n     honour,\n     rhine,\n     handle,\n     covent\n|> SELECT\n     grief,\n     honour,\n     rhine,\n     handle,\n     covent\n|> SELECT\n     honour,\n     rhine,\n     handle,\n     covent\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"grief",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    scarf,\n    violet,\n    stair,\n    coupon,\n    regime\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    scarf,\n    violet,\n    stair,\n    coupon,\n    COUNT(regime) AS barley\n  FROM cte_0\n  GROUP BY\n    scarf,\n    violet,\n    stair,\n    coupon\n),\ncte_2 AS (\n  SELECT\n    scarf,\n    violet,\n    stair,\n    coupon,\n    AVG(barley) AS debtor\n  FROM cte_1\n  GROUP BY\n    scarf,\n    violet,\n    stair,\n    coupon\n),\ncte_3 AS (\n  SELECT\n    leone,\n    scarf,\n    violet,\n    stair,\n    coupon,\n    debtor\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(debtor) AS organ\n  FROM cte_3\n  GROUP BY\n    scarf\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     scarf,\n     violet,\n     stair,\n     coupon,\n     regime\n|> AGGREGATE\n     COUNT(regime) AS barley\n   GROUP BY\n     scarf,\n     violet,\n     stair,\n     coupon\n|> AGGREGATE\n     AVG(barley) AS debtor\n   GROUP BY\n     scarf,\n     violet,\n     stair,\n     coupon\n|> SELECT\n     leone,\n     scarf,\n     violet,\n     stair,\n     coupon,\n     debtor\n|> AGGREGATE\n     MAX(debtor) AS organ\n   GROUP BY\n     scarf\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"leone",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tissue,\n    shine,\n    hollow,\n    christ,\n    novel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    tissue,\n    shine,\n    hollow,\n    christ,\n    MAX(novel) AS human\n  FROM cte_0\n  GROUP BY\n    tissue,\n    shine,\n    hollow,\n    christ\n),\ncte_2 AS (\n  SELECT\n    tissue,\n    shine,\n    hollow,\n    christ,\n    COUNT(human) AS burst\n  FROM cte_1\n  GROUP BY\n    tissue,\n    shine,\n    hollow,\n    christ\n),\ncte_3 AS (\n  SELECT\n    piazza,\n    tissue,\n    shine,\n    hollow\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    tissue,\n    AVG(hollow) AS morale\n  FROM cte_3\n  GROUP BY\n    tissue,\n    shine\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tissue,\n     shine,\n     hollow,\n     christ,\n     novel\n|> AGGREGATE\n     MAX(novel) AS human\n   GROUP BY\n     tissue,\n     shine,\n     hollow,\n     christ\n|> AGGREGATE\n     COUNT(human) AS burst\n   GROUP BY\n     tissue,\n     shine,\n     hollow,\n     christ\n|> SELECT\n     piazza,\n     tissue,\n     shine,\n     hollow\n|> AGGREGATE\n     AVG(hollow) AS morale\n   GROUP BY\n     tissue,\n     shine\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"piazza",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    intake,\n    honour,\n    advent,\n    hunger,\n    horse\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    intake,\n    honour,\n    advent,\n    hunger,\n    MAX(horse) AS ordeal\n  FROM cte_0\n  GROUP BY\n    intake,\n    honour,\n    advent,\n    hunger\n),\ncte_2 AS (\n  SELECT\n    intake,\n    honour,\n    advent,\n    hunger,\n    SUM(ordeal) AS brook\n  FROM cte_1\n  GROUP BY\n    intake,\n    honour,\n    advent,\n    hunger\n),\ncte_3 AS (\n  SELECT\n    runway,\n    intake,\n    honour\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    intake,\n    AVG(honour) AS torso,\n    MAX(honour) AS cannon\n  FROM cte_3\n  GROUP BY\n    intake\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     intake,\n     honour,\n     advent,\n     hunger,\n     horse\n|> AGGREGATE\n     MAX(horse) AS ordeal\n   GROUP BY\n     intake,\n     honour,\n     advent,\n     hunger\n|> AGGREGATE\n     SUM(ordeal) AS brook\n   GROUP BY\n     intake,\n     honour,\n     advent,\n     hunger\n|> SELECT\n     runway,\n     intake,\n     honour\n|> AGGREGATE\n     AVG(honour) AS torso,\n     MAX(honour) AS cannon\n   GROUP BY\n     intake\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"runway",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    glider,\n    bodie,\n    thorpe,\n    reeve,\n    defect\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    glider,\n    bodie,\n    thorpe,\n    reeve,\n    MIN(defect) AS plain\n  FROM cte_0\n  GROUP BY\n    glider,\n    bodie,\n    thorpe,\n    reeve\n),\ncte_2 AS (\n  SELECT\n    glider,\n    bodie,\n    thorpe,\n    reeve,\n    MAX(plain) AS maiden\n  FROM cte_1\n  GROUP BY\n    glider,\n    bodie,\n    thorpe,\n    reeve\n),\ncte_3 AS (\n  SELECT\n    relic,\n    glider,\n    bodie,\n    thorpe,\n    reeve\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(reeve) AS target\n  FROM cte_3\n  GROUP BY\n    glider,\n    bodie\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     glider,\n     bodie,\n     thorpe,\n     reeve,\n     defect\n|> AGGREGATE\n     MIN(defect) AS plain\n   GROUP BY\n     glider,\n     bodie,\n     thorpe,\n     reeve\n|> AGGREGATE\n     MAX(plain) AS maiden\n   GROUP BY\n     glider,\n     bodie,\n     thorpe,\n     reeve\n|> SELECT\n     relic,\n     glider,\n     bodie,\n     thorpe,\n     reeve\n|> AGGREGATE\n     COUNT(reeve) AS target\n   GROUP BY\n     glider,\n     bodie\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"relic",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    galley,\n    gooch,\n    touch,\n    abbot,\n    climb\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    galley,\n    gooch,\n    touch,\n    abbot,\n    SUM(climb) AS curfew\n  FROM cte_0\n  GROUP BY\n    galley,\n    gooch,\n    touch,\n    abbot\n),\ncte_2 AS (\n  SELECT\n    galley,\n    gooch,\n    touch,\n    abbot,\n    MIN(curfew) AS pollen\n  FROM cte_1\n  GROUP BY\n    galley,\n    gooch,\n    touch,\n    abbot\n),\ncte_3 AS (\n  SELECT\n    vision,\n    galley,\n    gooch,\n    touch,\n    abbot\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(abbot) AS aspect,\n    MAX(gooch) AS music\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     galley,\n     gooch,\n     touch,\n     abbot,\n     climb\n|> AGGREGATE\n     SUM(climb) AS curfew\n   GROUP BY\n     galley,\n     gooch,\n     touch,\n     abbot\n|> AGGREGATE\n     MIN(curfew) AS pollen\n   GROUP BY\n     galley,\n     gooch,\n     touch,\n     abbot\n|> SELECT\n     vision,\n     galley,\n     gooch,\n     touch,\n     abbot\n|> AGGREGATE\n     SUM(abbot) AS aspect,\n     MAX(gooch) AS music\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"vision",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    affect,\n    child,\n    agency,\n    creole,\n    jargon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    affect,\n    child,\n    agency,\n    creole,\n    COUNT(jargon) AS stair\n  FROM cte_0\n  GROUP BY\n    affect,\n    child,\n    agency,\n    creole\n),\ncte_2 AS (\n  SELECT\n    affect,\n    child,\n    agency,\n    creole,\n    MAX(stair) AS winner\n  FROM cte_1\n  GROUP BY\n    affect,\n    child,\n    agency,\n    creole\n),\ncte_3 AS (\n  SELECT\n    stump,\n    affect,\n    child,\n    agency,\n    creole\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(child) AS pedal,\n    AVG(affect) AS tucker\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     affect,\n     child,\n     agency,\n     creole,\n     jargon\n|> AGGREGATE\n     COUNT(jargon) AS stair\n   GROUP BY\n     affect,\n     child,\n     agency,\n     creole\n|> AGGREGATE\n     MAX(stair) AS winner\n   GROUP BY\n     affect,\n     child,\n     agency,\n     creole\n|> SELECT\n     stump,\n     affect,\n     child,\n     agency,\n     creole\n|> AGGREGATE\n     MAX(child) AS pedal,\n     AVG(affect) AS tucker\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"stump",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    munro,\n    clover,\n    keeper,\n    chant,\n    corner\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    munro,\n    clover,\n    keeper,\n    chant,\n    MIN(corner) AS scene\n  FROM cte_0\n  GROUP BY\n    munro,\n    clover,\n    keeper,\n    chant\n),\ncte_2 AS (\n  SELECT\n    munro,\n    clover,\n    keeper,\n    chant,\n    SUM(scene) AS purse\n  FROM cte_1\n  GROUP BY\n    munro,\n    clover,\n    keeper,\n    chant\n),\ncte_3 AS (\n  SELECT\n    throne,\n    munro,\n    clover,\n    keeper\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(clover) AS common,\n    MAX(keeper) AS shell\n  FROM cte_3\n  GROUP BY\n    munro\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     munro,\n     clover,\n     keeper,\n     chant,\n     corner\n|> AGGREGATE\n     MIN(corner) AS scene\n   GROUP BY\n     munro,\n     clover,\n     keeper,\n     chant\n|> AGGREGATE\n     SUM(scene) AS purse\n   GROUP BY\n     munro,\n     clover,\n     keeper,\n     chant\n|> SELECT\n     throne,\n     munro,\n     clover,\n     keeper\n|> AGGREGATE\n     COUNT(clover) AS common,\n     MAX(keeper) AS shell\n   GROUP BY\n     munro\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"SELECT_UNKNOWN",totalInformationDifference:8,unknownName:"throne",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    frenzy,\n    winger,\n    pilot,\n    parole,\n    array\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(array) AS ditch,\n    COUNT(array) AS factor\n  FROM cte_0\n  GROUP BY\n    frenzy,\n    winger,\n    pilot,\n    parole\n),\ncte_2 AS (\n  SELECT\n    MIN(factor) AS inset,\n    AVG(factor) AS parkin\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(parkin) AS linen,\n    COUNT(inset) AS miller,\n    SUM(inset) AS whore,\n    MIN(parkin) AS basket,\n    COUNT(sting) AS sister\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    linen,\n    COUNT(miller) AS penis,\n    MIN(whore) AS torso,\n    COUNT(whore) AS brett\n  FROM cte_3\n  GROUP BY\n    linen\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     frenzy,\n     winger,\n     pilot,\n     parole,\n     array\n|> AGGREGATE\n     MIN(array) AS ditch,\n     COUNT(array) AS factor\n   GROUP BY\n     frenzy,\n     winger,\n     pilot,\n     parole\n|> AGGREGATE\n     MIN(factor) AS inset,\n     AVG(factor) AS parkin\n|> AGGREGATE\n     MAX(parkin) AS linen,\n     COUNT(inset) AS miller,\n     SUM(inset) AS whore,\n     MIN(parkin) AS basket,\n     COUNT(sting) AS sister\n|> AGGREGATE\n     COUNT(miller) AS penis,\n     MIN(whore) AS torso,\n     COUNT(whore) AS brett\n   GROUP BY\n     linen\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"sting",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sauce,\n    ticket,\n    bloom,\n    derby,\n    ruler\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    sauce,\n    ticket,\n    bloom,\n    derby,\n    ruler\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(ruler) AS gallon,\n    MAX(ruler) AS trade\n  FROM cte_1\n  GROUP BY\n    sauce,\n    ticket,\n    bloom,\n    derby\n),\ncte_3 AS (\n  SELECT\n    MIN(trade) AS dragon,\n    COUNT(trade) AS alpha,\n    MAX(trade) AS lawyer\n  FROM cte_2\n  GROUP BY\n    gallon\n),\ncte_4 AS (\n  SELECT\n    MAX(brand) AS violet,\n    SUM(lawyer) AS badger,\n    COUNT(dragon) AS pearl,\n    MAX(lawyer) AS flame,\n    AVG(lawyer) AS truth\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sauce,\n     ticket,\n     bloom,\n     derby,\n     ruler\n|> SELECT\n     sauce,\n     ticket,\n     bloom,\n     derby,\n     ruler\n|> AGGREGATE\n     COUNT(ruler) AS gallon,\n     MAX(ruler) AS trade\n   GROUP BY\n     sauce,\n     ticket,\n     bloom,\n     derby\n|> AGGREGATE\n     MIN(trade) AS dragon,\n     COUNT(trade) AS alpha,\n     MAX(trade) AS lawyer\n   GROUP BY\n     gallon\n|> AGGREGATE\n     MAX(brand) AS violet,\n     SUM(lawyer) AS badger,\n     COUNT(dragon) AS pearl,\n     MAX(lawyer) AS flame,\n     AVG(lawyer) AS truth\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"brand",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:29},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bakery,\n    fabric,\n    gould,\n    watch,\n    spiral\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(watch) AS freeze,\n    AVG(fabric) AS plate,\n    AVG(gould) AS grain,\n    MIN(gould) AS resin,\n    MIN(spiral) AS incest\n  FROM cte_0\n  GROUP BY\n    bakery\n),\ncte_2 AS (\n  SELECT\n    freeze,\n    plate,\n    grain\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(grain) AS height,\n    MIN(grain) AS print,\n    MAX(grain) AS robin,\n    MIN(stick) AS sheep,\n    SUM(grain) AS fulham\n  FROM cte_2\n  GROUP BY\n    freeze,\n    plate\n),\ncte_4 AS (\n  SELECT\n    MAX(fulham) AS impact,\n    SUM(print) AS matey\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bakery,\n     fabric,\n     gould,\n     watch,\n     spiral\n|> AGGREGATE\n     MIN(watch) AS freeze,\n     AVG(fabric) AS plate,\n     AVG(gould) AS grain,\n     MIN(gould) AS resin,\n     MIN(spiral) AS incest\n   GROUP BY\n     bakery\n|> SELECT\n     freeze,\n     plate,\n     grain\n|> AGGREGATE\n     AVG(grain) AS height,\n     MIN(grain) AS print,\n     MAX(grain) AS robin,\n     MIN(stick) AS sheep,\n     SUM(grain) AS fulham\n   GROUP BY\n     freeze,\n     plate\n|> AGGREGATE\n     MAX(fulham) AS impact,\n     SUM(print) AS matey\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"stick",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    gable,\n    birch,\n    coffin,\n    ballot,\n    octave\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    gable,\n    birch\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(gable) AS witch,\n    AVG(birch) AS thesis,\n    SUM(gable) AS maple,\n    MAX(gable) AS storm,\n    MAX(birch) AS sweep\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(storm) AS decree,\n    MAX(thesis) AS hurry,\n    MAX(storm) AS plain,\n    MIN(storm) AS reign,\n    SUM(human) AS figure,\n    COUNT(witch) AS query,\n    SUM(sweep) AS oliver\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    decree,\n    MAX(plain) AS south\n  FROM cte_3\n  GROUP BY\n    decree\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     gable,\n     birch,\n     coffin,\n     ballot,\n     octave\n|> SELECT\n     gable,\n     birch\n|> AGGREGATE\n     COUNT(gable) AS witch,\n     AVG(birch) AS thesis,\n     SUM(gable) AS maple,\n     MAX(gable) AS storm,\n     MAX(birch) AS sweep\n|> AGGREGATE\n     AVG(storm) AS decree,\n     MAX(thesis) AS hurry,\n     MAX(storm) AS plain,\n     MIN(storm) AS reign,\n     SUM(human) AS figure,\n     COUNT(witch) AS query,\n     SUM(sweep) AS oliver\n|> AGGREGATE\n     MAX(plain) AS south\n   GROUP BY\n     decree\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"human",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    trophy,\n    father,\n    syntax,\n    defect,\n    mount\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(mount) AS violin,\n    AVG(mount) AS leader\n  FROM cte_0\n  GROUP BY\n    trophy,\n    father,\n    syntax,\n    defect\n),\ncte_2 AS (\n  SELECT\n    MAX(leader) AS connie\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(connie) AS tariff,\n    MAX(connie) AS folly,\n    AVG(connie) AS poppy,\n    MIN(connie) AS habit,\n    COUNT(connie) AS melody,\n    SUM(pistol) AS debit\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    tariff,\n    MIN(poppy) AS johnny,\n    MAX(habit) AS stress,\n    AVG(folly) AS bronze\n  FROM cte_3\n  GROUP BY\n    tariff\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     trophy,\n     father,\n     syntax,\n     defect,\n     mount\n|> AGGREGATE\n     SUM(mount) AS violin,\n     AVG(mount) AS leader\n   GROUP BY\n     trophy,\n     father,\n     syntax,\n     defect\n|> AGGREGATE\n     MAX(leader) AS connie\n|> AGGREGATE\n     SUM(connie) AS tariff,\n     MAX(connie) AS folly,\n     AVG(connie) AS poppy,\n     MIN(connie) AS habit,\n     COUNT(connie) AS melody,\n     SUM(pistol) AS debit\n|> AGGREGATE\n     MIN(poppy) AS johnny,\n     MAX(habit) AS stress,\n     AVG(folly) AS bronze\n   GROUP BY\n     tariff\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"pistol",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    prince,\n    franc,\n    jumper,\n    bodie,\n    salon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    prince,\n    franc,\n    jumper,\n    bodie\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(bodie) AS cereal,\n    MIN(bodie) AS claim\n  FROM cte_1\n  GROUP BY\n    prince,\n    franc\n),\ncte_3 AS (\n  SELECT\n    AVG(claim) AS liquor,\n    AVG(cereal) AS lesson,\n    COUNT(claim) AS visit,\n    MAX(cereal) AS pulse,\n    MIN(cereal) AS mummy,\n    SUM(hogan) AS mortar,\n    MAX(claim) AS grill\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(grill) AS flare\n  FROM cte_3\n  GROUP BY\n    liquor,\n    lesson,\n    visit,\n    pulse\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     prince,\n     franc,\n     jumper,\n     bodie,\n     salon\n|> SELECT\n     prince,\n     franc,\n     jumper,\n     bodie\n|> AGGREGATE\n     AVG(bodie) AS cereal,\n     MIN(bodie) AS claim\n   GROUP BY\n     prince,\n     franc\n|> AGGREGATE\n     AVG(claim) AS liquor,\n     AVG(cereal) AS lesson,\n     COUNT(claim) AS visit,\n     MAX(cereal) AS pulse,\n     MIN(cereal) AS mummy,\n     SUM(hogan) AS mortar,\n     MAX(claim) AS grill\n|> AGGREGATE\n     MIN(grill) AS flare\n   GROUP BY\n     liquor,\n     lesson,\n     visit,\n     pulse\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"hogan",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    patten,\n    ruler,\n    louvre,\n    parody,\n    cargo\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(cargo) AS henry,\n    MIN(louvre) AS export,\n    COUNT(parody) AS stance,\n    SUM(cargo) AS format\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(stance) AS spoon,\n    MAX(stance) AS mercer,\n    SUM(henry) AS resort\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(resort) AS campus,\n    MAX(resort) AS blaze,\n    COUNT(resort) AS saucer,\n    COUNT(murder) AS knight\n  FROM cte_2\n  GROUP BY\n    spoon,\n    mercer\n),\ncte_4 AS (\n  SELECT\n    MAX(saucer) AS scarf,\n    SUM(saucer) AS weight,\n    COUNT(saucer) AS horse\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     patten,\n     ruler,\n     louvre,\n     parody,\n     cargo\n|> AGGREGATE\n     MIN(cargo) AS henry,\n     MIN(louvre) AS export,\n     COUNT(parody) AS stance,\n     SUM(cargo) AS format\n|> AGGREGATE\n     AVG(stance) AS spoon,\n     MAX(stance) AS mercer,\n     SUM(henry) AS resort\n|> AGGREGATE\n     SUM(resort) AS campus,\n     MAX(resort) AS blaze,\n     COUNT(resort) AS saucer,\n     COUNT(murder) AS knight\n   GROUP BY\n     spoon,\n     mercer\n|> AGGREGATE\n     MAX(saucer) AS scarf,\n     SUM(saucer) AS weight,\n     COUNT(saucer) AS horse\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"murder",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    divide,\n    pellet,\n    locus,\n    dollar,\n    holder\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(dollar) AS sender,\n    MIN(dollar) AS accord,\n    COUNT(holder) AS warsaw,\n    SUM(holder) AS canary,\n    AVG(holder) AS bloom\n  FROM cte_0\n  GROUP BY\n    divide,\n    pellet,\n    locus\n),\ncte_2 AS (\n  SELECT\n    sender,\n    accord,\n    warsaw\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(accord) AS hassle\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(hassle) AS lever,\n    MAX(helmet) AS slump,\n    MAX(hassle) AS trench,\n    SUM(hassle) AS chain,\n    AVG(hassle) AS travis,\n    MIN(hassle) AS manure\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     divide,\n     pellet,\n     locus,\n     dollar,\n     holder\n|> AGGREGATE\n     MAX(dollar) AS sender,\n     MIN(dollar) AS accord,\n     COUNT(holder) AS warsaw,\n     SUM(holder) AS canary,\n     AVG(holder) AS bloom\n   GROUP BY\n     divide,\n     pellet,\n     locus\n|> SELECT\n     sender,\n     accord,\n     warsaw\n|> AGGREGATE\n     AVG(accord) AS hassle\n|> AGGREGATE\n     COUNT(hassle) AS lever,\n     MAX(helmet) AS slump,\n     MAX(hassle) AS trench,\n     SUM(hassle) AS chain,\n     AVG(hassle) AS travis,\n     MIN(hassle) AS manure\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"helmet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    kitty,\n    cough,\n    trout,\n    school,\n    corpus\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(trout) AS comet\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(comet) AS valley,\n    MIN(comet) AS becker,\n    SUM(comet) AS topic\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(becker) AS curve,\n    MAX(becker) AS tackle,\n    COUNT(becker) AS scrum,\n    MIN(topic) AS heater,\n    MAX(topic) AS coupon,\n    COUNT(topic) AS wally,\n    MAX(abuse) AS rebel\n  FROM cte_2\n  GROUP BY\n    valley\n),\ncte_4 AS (\n  SELECT\n    curve,\n    AVG(coupon) AS bundle\n  FROM cte_3\n  GROUP BY\n    curve,\n    tackle,\n    scrum,\n    heater\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     kitty,\n     cough,\n     trout,\n     school,\n     corpus\n|> AGGREGATE\n     COUNT(trout) AS comet\n|> AGGREGATE\n     MAX(comet) AS valley,\n     MIN(comet) AS becker,\n     SUM(comet) AS topic\n|> AGGREGATE\n     SUM(becker) AS curve,\n     MAX(becker) AS tackle,\n     COUNT(becker) AS scrum,\n     MIN(topic) AS heater,\n     MAX(topic) AS coupon,\n     COUNT(topic) AS wally,\n     MAX(abuse) AS rebel\n   GROUP BY\n     valley\n|> AGGREGATE\n     AVG(coupon) AS bundle\n   GROUP BY\n     curve,\n     tackle,\n     scrum,\n     heater\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"abuse",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    flood,\n    voyage,\n    press,\n    hearth,\n    onion\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(onion) AS bother,\n    SUM(press) AS colin,\n    SUM(voyage) AS lover,\n    SUM(onion) AS thorpe,\n    COUNT(voyage) AS sigma\n  FROM cte_0\n  GROUP BY\n    flood\n),\ncte_2 AS (\n  SELECT\n    bother,\n    colin,\n    lover,\n    thorpe,\n    sigma\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(sigma) AS barley,\n    SUM(lover) AS muddle,\n    COUNT(dread) AS papacy,\n    SUM(colin) AS greed,\n    MAX(colin) AS burgh\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    barley,\n    COUNT(greed) AS sonny\n  FROM cte_3\n  GROUP BY\n    barley\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     flood,\n     voyage,\n     press,\n     hearth,\n     onion\n|> AGGREGATE\n     MIN(onion) AS bother,\n     SUM(press) AS colin,\n     SUM(voyage) AS lover,\n     SUM(onion) AS thorpe,\n     COUNT(voyage) AS sigma\n   GROUP BY\n     flood\n|> SELECT\n     bother,\n     colin,\n     lover,\n     thorpe,\n     sigma\n|> AGGREGATE\n     COUNT(sigma) AS barley,\n     SUM(lover) AS muddle,\n     COUNT(dread) AS papacy,\n     SUM(colin) AS greed,\n     MAX(colin) AS burgh\n|> AGGREGATE\n     COUNT(greed) AS sonny\n   GROUP BY\n     barley\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"dread",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    permit,\n    foetus,\n    badger,\n    knife,\n    teapot\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    permit,\n    foetus,\n    badger,\n    knife,\n    teapot\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(knife) AS tooth,\n    MAX(foetus) AS switch,\n    AVG(badger) AS humour,\n    MIN(badger) AS cancer,\n    MAX(knife) AS brett,\n    SUM(knife) AS candy\n  FROM cte_1\n  GROUP BY\n    permit\n),\ncte_3 AS (\n  SELECT\n    SUM(candy) AS poker,\n    COUNT(helmet) AS poppy\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(poker) AS regent,\n    MAX(poker) AS vanity,\n    MIN(poker) AS claret,\n    SUM(poker) AS expiry\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     permit,\n     foetus,\n     badger,\n     knife,\n     teapot\n|> SELECT\n     permit,\n     foetus,\n     badger,\n     knife,\n     teapot\n|> AGGREGATE\n     COUNT(knife) AS tooth,\n     MAX(foetus) AS switch,\n     AVG(badger) AS humour,\n     MIN(badger) AS cancer,\n     MAX(knife) AS brett,\n     SUM(knife) AS candy\n   GROUP BY\n     permit\n|> AGGREGATE\n     SUM(candy) AS poker,\n     COUNT(helmet) AS poppy\n|> AGGREGATE\n     AVG(poker) AS regent,\n     MAX(poker) AS vanity,\n     MIN(poker) AS claret,\n     SUM(poker) AS expiry\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"helmet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sonnet,\n    delta,\n    flood,\n    reward,\n    skirt\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(skirt) AS nerve,\n    AVG(skirt) AS logan,\n    AVG(reward) AS poppy,\n    AVG(flood) AS monte,\n    COUNT(reward) AS tenor,\n    COUNT(flood) AS verse\n  FROM cte_0\n  GROUP BY\n    sonnet\n),\ncte_2 AS (\n  SELECT\n    AVG(monte) AS piece\n  FROM cte_1\n  GROUP BY\n    nerve\n),\ncte_3 AS (\n  SELECT\n    MAX(piece) AS angel,\n    SUM(piece) AS barry,\n    MAX(killer) AS potato,\n    MIN(piece) AS nausea\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    angel,\n    barry,\n    AVG(nausea) AS molly\n  FROM cte_3\n  GROUP BY\n    angel,\n    barry\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sonnet,\n     delta,\n     flood,\n     reward,\n     skirt\n|> AGGREGATE\n     COUNT(skirt) AS nerve,\n     AVG(skirt) AS logan,\n     AVG(reward) AS poppy,\n     AVG(flood) AS monte,\n     COUNT(reward) AS tenor,\n     COUNT(flood) AS verse\n   GROUP BY\n     sonnet\n|> AGGREGATE\n     AVG(monte) AS piece\n   GROUP BY\n     nerve\n|> AGGREGATE\n     MAX(piece) AS angel,\n     SUM(piece) AS barry,\n     MAX(killer) AS potato,\n     MIN(piece) AS nausea\n|> AGGREGATE\n     AVG(nausea) AS molly\n   GROUP BY\n     angel,\n     barry\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"killer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    chalk,\n    umpire,\n    gully,\n    bully,\n    hearer\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    chalk,\n    umpire\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    chalk,\n    umpire\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(umpire) AS flint,\n    MIN(chalk) AS sample,\n    COUNT(chalk) AS aunty,\n    AVG(chalk) AS summer,\n    MIN(umpire) AS berry,\n    MAX(chalk) AS irene\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(berry) AS fusion,\n    AVG(irene) AS toast,\n    MAX(arcade) AS guess,\n    MIN(sample) AS voter,\n    MIN(summer) AS impact,\n    SUM(sample) AS nether,\n    SUM(aunty) AS update\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     chalk,\n     umpire,\n     gully,\n     bully,\n     hearer\n|> SELECT\n     chalk,\n     umpire\n|> SELECT\n     chalk,\n     umpire\n|> AGGREGATE\n     AVG(umpire) AS flint,\n     MIN(chalk) AS sample,\n     COUNT(chalk) AS aunty,\n     AVG(chalk) AS summer,\n     MIN(umpire) AS berry,\n     MAX(chalk) AS irene\n|> AGGREGATE\n     AVG(berry) AS fusion,\n     AVG(irene) AS toast,\n     MAX(arcade) AS guess,\n     MIN(sample) AS voter,\n     MIN(summer) AS impact,\n     SUM(sample) AS nether,\n     SUM(aunty) AS update\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"arcade",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    pound,\n    canoe,\n    gallon,\n    reeve,\n    score\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(reeve) AS tardis,\n    AVG(score) AS nelson,\n    SUM(canoe) AS midst\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(midst) AS avail,\n    AVG(midst) AS remit,\n    SUM(midst) AS source,\n    MIN(midst) AS barman\n  FROM cte_1\n  GROUP BY\n    tardis,\n    nelson\n),\ncte_3 AS (\n  SELECT\n    MIN(source) AS single,\n    MIN(remit) AS stone,\n    AVG(board) AS brandy,\n    AVG(remit) AS benny,\n    COUNT(remit) AS troop\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(troop) AS enamel\n  FROM cte_3\n  GROUP BY\n    single,\n    stone\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     pound,\n     canoe,\n     gallon,\n     reeve,\n     score\n|> AGGREGATE\n     MAX(reeve) AS tardis,\n     AVG(score) AS nelson,\n     SUM(canoe) AS midst\n|> AGGREGATE\n     MAX(midst) AS avail,\n     AVG(midst) AS remit,\n     SUM(midst) AS source,\n     MIN(midst) AS barman\n   GROUP BY\n     tardis,\n     nelson\n|> AGGREGATE\n     MIN(source) AS single,\n     MIN(remit) AS stone,\n     AVG(board) AS brandy,\n     AVG(remit) AS benny,\n     COUNT(remit) AS troop\n|> AGGREGATE\n     MIN(troop) AS enamel\n   GROUP BY\n     single,\n     stone\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"board",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    noise,\n    deputy,\n    logic,\n    danger,\n    ivory\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    noise,\n    deputy\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(deputy) AS brain,\n    MIN(deputy) AS brief,\n    SUM(noise) AS nylon,\n    MIN(noise) AS mobile,\n    AVG(deputy) AS labour,\n    MAX(deputy) AS lemon,\n    COUNT(noise) AS japan\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(japan) AS vapour,\n    MAX(japan) AS fusion,\n    SUM(puppy) AS claim\n  FROM cte_2\n  GROUP BY\n    brain,\n    brief,\n    nylon,\n    mobile,\n    labour,\n    lemon\n),\ncte_4 AS (\n  SELECT\n    vapour,\n    fusion\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     noise,\n     deputy,\n     logic,\n     danger,\n     ivory\n|> SELECT\n     noise,\n     deputy\n|> AGGREGATE\n     SUM(deputy) AS brain,\n     MIN(deputy) AS brief,\n     SUM(noise) AS nylon,\n     MIN(noise) AS mobile,\n     AVG(deputy) AS labour,\n     MAX(deputy) AS lemon,\n     COUNT(noise) AS japan\n|> AGGREGATE\n     AVG(japan) AS vapour,\n     MAX(japan) AS fusion,\n     SUM(puppy) AS claim\n   GROUP BY\n     brain,\n     brief,\n     nylon,\n     mobile,\n     labour,\n     lemon\n|> SELECT\n     vapour,\n     fusion\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"puppy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    peril,\n    throne,\n    chalk,\n    alpha,\n    flood\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(flood) AS moray,\n    MAX(chalk) AS ghost,\n    SUM(chalk) AS mould,\n    COUNT(alpha) AS sting,\n    AVG(throne) AS turner\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    moray,\n    ghost,\n    mould,\n    sting\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(sting) AS mercy,\n    MIN(sting) AS craig,\n    MAX(sting) AS picnic,\n    MIN(chang) AS flask,\n    SUM(sting) AS cloud\n  FROM cte_2\n  GROUP BY\n    moray,\n    ghost,\n    mould\n),\ncte_4 AS (\n  SELECT\n    mercy,\n    craig,\n    picnic\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     peril,\n     throne,\n     chalk,\n     alpha,\n     flood\n|> AGGREGATE\n     SUM(flood) AS moray,\n     MAX(chalk) AS ghost,\n     SUM(chalk) AS mould,\n     COUNT(alpha) AS sting,\n     AVG(throne) AS turner\n|> SELECT\n     moray,\n     ghost,\n     mould,\n     sting\n|> AGGREGATE\n     AVG(sting) AS mercy,\n     MIN(sting) AS craig,\n     MAX(sting) AS picnic,\n     MIN(chang) AS flask,\n     SUM(sting) AS cloud\n   GROUP BY\n     moray,\n     ghost,\n     mould\n|> SELECT\n     mercy,\n     craig,\n     picnic\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"chang",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    apple,\n    honour,\n    player,\n    scrum,\n    cider\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(scrum) AS panic,\n    MIN(honour) AS blouse,\n    MAX(cider) AS brett,\n    MAX(apple) AS state,\n    MIN(apple) AS barge\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(brett) AS jaguar,\n    MAX(barge) AS brush,\n    MIN(state) AS hoover\n  FROM cte_1\n  GROUP BY\n    panic,\n    blouse\n),\ncte_3 AS (\n  SELECT\n    MAX(hoover) AS sonny,\n    COUNT(payne) AS relay\n  FROM cte_2\n  GROUP BY\n    jaguar,\n    brush\n),\ncte_4 AS (\n  SELECT\n    MAX(sonny) AS fluid,\n    AVG(sonny) AS strand,\n    COUNT(sonny) AS cavity\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     apple,\n     honour,\n     player,\n     scrum,\n     cider\n|> AGGREGATE\n     MAX(scrum) AS panic,\n     MIN(honour) AS blouse,\n     MAX(cider) AS brett,\n     MAX(apple) AS state,\n     MIN(apple) AS barge\n|> AGGREGATE\n     AVG(brett) AS jaguar,\n     MAX(barge) AS brush,\n     MIN(state) AS hoover\n   GROUP BY\n     panic,\n     blouse\n|> AGGREGATE\n     MAX(hoover) AS sonny,\n     COUNT(payne) AS relay\n   GROUP BY\n     jaguar,\n     brush\n|> AGGREGATE\n     MAX(sonny) AS fluid,\n     AVG(sonny) AS strand,\n     COUNT(sonny) AS cavity\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"payne",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    gland,\n    sponge,\n    hamlet,\n    ballot,\n    ozone\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(sponge) AS slater,\n    MIN(ozone) AS murder,\n    MAX(gland) AS action,\n    MAX(ballot) AS scarf,\n    SUM(gland) AS bulge,\n    AVG(ozone) AS skirt,\n    AVG(ballot) AS porter,\n    COUNT(ballot) AS sphere\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(skirt) AS stella,\n    AVG(skirt) AS norman,\n    COUNT(climb) AS median\n  FROM cte_1\n  GROUP BY\n    slater\n),\ncte_3 AS (\n  SELECT\n    COUNT(norman) AS stead,\n    AVG(norman) AS gauge\n  FROM cte_2\n  GROUP BY\n    stella\n),\ncte_4 AS (\n  SELECT\n    stead,\n    gauge\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     gland,\n     sponge,\n     hamlet,\n     ballot,\n     ozone\n|> AGGREGATE\n     SUM(sponge) AS slater,\n     MIN(ozone) AS murder,\n     MAX(gland) AS action,\n     MAX(ballot) AS scarf,\n     SUM(gland) AS bulge,\n     AVG(ozone) AS skirt,\n     AVG(ballot) AS porter,\n     COUNT(ballot) AS sphere\n|> AGGREGATE\n     COUNT(skirt) AS stella,\n     AVG(skirt) AS norman,\n     COUNT(climb) AS median\n   GROUP BY\n     slater\n|> AGGREGATE\n     COUNT(norman) AS stead,\n     AVG(norman) AS gauge\n   GROUP BY\n     stella\n|> SELECT\n     stead,\n     gauge\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"climb",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:27,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    grease,\n    dollar,\n    ideal,\n    jungle,\n    ridley\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(jungle) AS cousin,\n    SUM(jungle) AS racism,\n    MAX(ridley) AS relief,\n    SUM(ridley) AS tribe,\n    COUNT(ridley) AS spine,\n    SUM(ideal) AS cohen\n  FROM cte_0\n  GROUP BY\n    grease,\n    dollar\n),\ncte_2 AS (\n  SELECT\n    SUM(cohen) AS sophie\n  FROM cte_1\n  GROUP BY\n    cousin,\n    racism\n),\ncte_3 AS (\n  SELECT\n    MIN(sophie) AS denim,\n    MIN(ascot) AS riddle,\n    COUNT(sophie) AS onset,\n    AVG(sophie) AS master\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(master) AS ginger\n  FROM cte_3\n  GROUP BY\n    denim,\n    onset\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     grease,\n     dollar,\n     ideal,\n     jungle,\n     ridley\n|> AGGREGATE\n     MIN(jungle) AS cousin,\n     SUM(jungle) AS racism,\n     MAX(ridley) AS relief,\n     SUM(ridley) AS tribe,\n     COUNT(ridley) AS spine,\n     SUM(ideal) AS cohen\n   GROUP BY\n     grease,\n     dollar\n|> AGGREGATE\n     SUM(cohen) AS sophie\n   GROUP BY\n     cousin,\n     racism\n|> AGGREGATE\n     MIN(sophie) AS denim,\n     MIN(ascot) AS riddle,\n     COUNT(sophie) AS onset,\n     AVG(sophie) AS master\n|> AGGREGATE\n     COUNT(master) AS ginger\n   GROUP BY\n     denim,\n     onset\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"ascot",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    valve,\n    extent,\n    simon,\n    rocket,\n    razor\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    valve,\n    extent\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(extent) AS safari,\n    SUM(extent) AS tongue\n  FROM cte_1\n  GROUP BY\n    valve\n),\ncte_3 AS (\n  SELECT\n    AVG(tongue) AS liquid,\n    MIN(tongue) AS fusion,\n    COUNT(tongue) AS demand,\n    SUM(tongue) AS drive,\n    MAX(tongue) AS heart\n  FROM cte_2\n  GROUP BY\n    safari\n),\ncte_4 AS (\n  SELECT\n    COUNT(demand) AS shirt,\n    AVG(warden) AS brick,\n    MIN(heart) AS dalton,\n    COUNT(drive) AS check,\n    MAX(drive) AS curse\n  FROM cte_3\n  GROUP BY\n    liquid,\n    fusion\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     valve,\n     extent,\n     simon,\n     rocket,\n     razor\n|> SELECT\n     valve,\n     extent\n|> AGGREGATE\n     COUNT(extent) AS safari,\n     SUM(extent) AS tongue\n   GROUP BY\n     valve\n|> AGGREGATE\n     AVG(tongue) AS liquid,\n     MIN(tongue) AS fusion,\n     COUNT(tongue) AS demand,\n     SUM(tongue) AS drive,\n     MAX(tongue) AS heart\n   GROUP BY\n     safari\n|> AGGREGATE\n     COUNT(demand) AS shirt,\n     AVG(warden) AS brick,\n     MIN(heart) AS dalton,\n     COUNT(drive) AS check,\n     MAX(drive) AS curse\n   GROUP BY\n     liquid,\n     fusion\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"warden",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    orange,\n    author,\n    basin,\n    spruce,\n    hoover\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(hoover) AS order,\n    MAX(hoover) AS revolt,\n    COUNT(hoover) AS ulcer,\n    MIN(hoover) AS pulse\n  FROM cte_0\n  GROUP BY\n    orange,\n    author,\n    basin,\n    spruce\n),\ncte_2 AS (\n  SELECT\n    COUNT(revolt) AS treat,\n    MAX(ulcer) AS trade\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(treat) AS factor,\n    COUNT(trade) AS modem,\n    COUNT(papacy) AS cement\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    factor,\n    AVG(modem) AS stuff,\n    COUNT(modem) AS lotus,\n    SUM(modem) AS killer\n  FROM cte_3\n  GROUP BY\n    factor\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     orange,\n     author,\n     basin,\n     spruce,\n     hoover\n|> AGGREGATE\n     AVG(hoover) AS order,\n     MAX(hoover) AS revolt,\n     COUNT(hoover) AS ulcer,\n     MIN(hoover) AS pulse\n   GROUP BY\n     orange,\n     author,\n     basin,\n     spruce\n|> AGGREGATE\n     COUNT(revolt) AS treat,\n     MAX(ulcer) AS trade\n|> AGGREGATE\n     COUNT(treat) AS factor,\n     COUNT(trade) AS modem,\n     COUNT(papacy) AS cement\n|> AGGREGATE\n     AVG(modem) AS stuff,\n     COUNT(modem) AS lotus,\n     SUM(modem) AS killer\n   GROUP BY\n     factor\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"papacy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    corpse,\n    bridge,\n    fright,\n    author,\n    pirate\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    corpse,\n    bridge,\n    fright\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(fright) AS floor,\n    MIN(fright) AS anger,\n    MAX(fright) AS target,\n    COUNT(fright) AS count,\n    SUM(fright) AS canal\n  FROM cte_1\n  GROUP BY\n    corpse,\n    bridge\n),\ncte_3 AS (\n  SELECT\n    MAX(canal) AS voice,\n    AVG(canal) AS bottle\n  FROM cte_2\n  GROUP BY\n    floor\n),\ncte_4 AS (\n  SELECT\n    COUNT(kinase) AS friar,\n    MIN(bottle) AS volume,\n    SUM(bottle) AS bubble,\n    MAX(voice) AS bowie,\n    COUNT(voice) AS screw\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     corpse,\n     bridge,\n     fright,\n     author,\n     pirate\n|> SELECT\n     corpse,\n     bridge,\n     fright\n|> AGGREGATE\n     AVG(fright) AS floor,\n     MIN(fright) AS anger,\n     MAX(fright) AS target,\n     COUNT(fright) AS count,\n     SUM(fright) AS canal\n   GROUP BY\n     corpse,\n     bridge\n|> AGGREGATE\n     MAX(canal) AS voice,\n     AVG(canal) AS bottle\n   GROUP BY\n     floor\n|> AGGREGATE\n     COUNT(kinase) AS friar,\n     MIN(bottle) AS volume,\n     SUM(bottle) AS bubble,\n     MAX(voice) AS bowie,\n     COUNT(voice) AS screw\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"kinase",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    gummer,\n    famine,\n    update,\n    ration,\n    denial\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(denial) AS jessie,\n    COUNT(denial) AS debit,\n    AVG(denial) AS apathy,\n    MIN(denial) AS policy\n  FROM cte_0\n  GROUP BY\n    gummer,\n    famine,\n    update,\n    ration\n),\ncte_2 AS (\n  SELECT\n    COUNT(apathy) AS board,\n    SUM(policy) AS auntie,\n    MIN(policy) AS stone,\n    MIN(debit) AS genus,\n    COUNT(catch) AS party\n  FROM cte_1\n  GROUP BY\n    jessie\n),\ncte_3 AS (\n  SELECT\n    MIN(stone) AS depth\n  FROM cte_2\n  GROUP BY\n    board\n),\ncte_4 AS (\n  SELECT\n    MAX(depth) AS walker,\n    SUM(depth) AS gland\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     gummer,\n     famine,\n     update,\n     ration,\n     denial\n|> AGGREGATE\n     MAX(denial) AS jessie,\n     COUNT(denial) AS debit,\n     AVG(denial) AS apathy,\n     MIN(denial) AS policy\n   GROUP BY\n     gummer,\n     famine,\n     update,\n     ration\n|> AGGREGATE\n     COUNT(apathy) AS board,\n     SUM(policy) AS auntie,\n     MIN(policy) AS stone,\n     MIN(debit) AS genus,\n     COUNT(catch) AS party\n   GROUP BY\n     jessie\n|> AGGREGATE\n     MIN(stone) AS depth\n   GROUP BY\n     board\n|> AGGREGATE\n     MAX(depth) AS walker,\n     SUM(depth) AS gland\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"catch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    strat,\n    racism,\n    strike,\n    raffle,\n    surrey\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    strat,\n    racism,\n    strike,\n    raffle\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(raffle) AS singer,\n    SUM(raffle) AS speed\n  FROM cte_1\n  GROUP BY\n    strat,\n    racism,\n    strike\n),\ncte_3 AS (\n  SELECT\n    SUM(speed) AS tunic,\n    MIN(speed) AS kelly,\n    COUNT(speed) AS memory\n  FROM cte_2\n  GROUP BY\n    singer\n),\ncte_4 AS (\n  SELECT\n    COUNT(memory) AS sword,\n    AVG(wicker) AS chick,\n    MIN(memory) AS motion,\n    MAX(kelly) AS garlic,\n    MIN(tunic) AS liner,\n    MIN(kelly) AS scrub\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     strat,\n     racism,\n     strike,\n     raffle,\n     surrey\n|> SELECT\n     strat,\n     racism,\n     strike,\n     raffle\n|> AGGREGATE\n     AVG(raffle) AS singer,\n     SUM(raffle) AS speed\n   GROUP BY\n     strat,\n     racism,\n     strike\n|> AGGREGATE\n     SUM(speed) AS tunic,\n     MIN(speed) AS kelly,\n     COUNT(speed) AS memory\n   GROUP BY\n     singer\n|> AGGREGATE\n     COUNT(memory) AS sword,\n     AVG(wicker) AS chick,\n     MIN(memory) AS motion,\n     MAX(kelly) AS garlic,\n     MIN(tunic) AS liner,\n     MIN(kelly) AS scrub\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"wicker",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:28},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    fridge,\n    fringe,\n    vigour,\n    assent,\n    proxy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    fridge,\n    fringe,\n    vigour,\n    assent\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(fringe) AS china,\n    COUNT(assent) AS stamp,\n    AVG(vigour) AS growth,\n    SUM(vigour) AS jockey\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MIN(growth) AS celery,\n    MAX(jockey) AS torque,\n    AVG(stamp) AS thesis,\n    AVG(growth) AS gooch,\n    MIN(foetus) AS north\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(celery) AS hedge,\n    AVG(gooch) AS plough,\n    MAX(torque) AS amber,\n    MAX(thesis) AS bypass\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     fridge,\n     fringe,\n     vigour,\n     assent,\n     proxy\n|> SELECT\n     fridge,\n     fringe,\n     vigour,\n     assent\n|> AGGREGATE\n     COUNT(fringe) AS china,\n     COUNT(assent) AS stamp,\n     AVG(vigour) AS growth,\n     SUM(vigour) AS jockey\n|> AGGREGATE\n     MIN(growth) AS celery,\n     MAX(jockey) AS torque,\n     AVG(stamp) AS thesis,\n     AVG(growth) AS gooch,\n     MIN(foetus) AS north\n|> AGGREGATE\n     MIN(celery) AS hedge,\n     AVG(gooch) AS plough,\n     MAX(torque) AS amber,\n     MAX(thesis) AS bypass\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"foetus",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    peril,\n    cocoa,\n    human,\n    mayor,\n    novel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(human) AS shrimp,\n    MIN(cocoa) AS candle\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(candle) AS usage,\n    MIN(candle) AS course,\n    MAX(candle) AS fusion\n  FROM cte_1\n  GROUP BY\n    shrimp\n),\ncte_3 AS (\n  SELECT\n    MAX(course) AS fleet,\n    MAX(fusion) AS saliva,\n    SUM(fusion) AS guest\n  FROM cte_2\n  GROUP BY\n    usage\n),\ncte_4 AS (\n  SELECT\n    MAX(saliva) AS livery,\n    MAX(ledger) AS scare,\n    SUM(fleet) AS reggae,\n    MIN(fleet) AS daniel,\n    MAX(fleet) AS radar,\n    MIN(saliva) AS spouse\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     peril,\n     cocoa,\n     human,\n     mayor,\n     novel\n|> AGGREGATE\n     MAX(human) AS shrimp,\n     MIN(cocoa) AS candle\n|> AGGREGATE\n     AVG(candle) AS usage,\n     MIN(candle) AS course,\n     MAX(candle) AS fusion\n   GROUP BY\n     shrimp\n|> AGGREGATE\n     MAX(course) AS fleet,\n     MAX(fusion) AS saliva,\n     SUM(fusion) AS guest\n   GROUP BY\n     usage\n|> AGGREGATE\n     MAX(saliva) AS livery,\n     MAX(ledger) AS scare,\n     SUM(fleet) AS reggae,\n     MIN(fleet) AS daniel,\n     MAX(fleet) AS radar,\n     MIN(saliva) AS spouse\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"ledger",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    unrest,\n    corpus,\n    swell,\n    fiasco,\n    friar\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    unrest,\n    corpus,\n    swell,\n    fiasco,\n    friar\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(swell) AS snack,\n    MAX(fiasco) AS coast,\n    MAX(swell) AS dread\n  FROM cte_1\n  GROUP BY\n    unrest\n),\ncte_3 AS (\n  SELECT\n    MIN(dread) AS mayor,\n    SUM(coast) AS radar,\n    AVG(dread) AS miller,\n    SUM(dread) AS envoy,\n    AVG(saloon) AS bench\n  FROM cte_2\n  GROUP BY\n    snack\n),\ncte_4 AS (\n  SELECT\n    MIN(radar) AS heater,\n    COUNT(envoy) AS flesh,\n    SUM(miller) AS sting\n  FROM cte_3\n  GROUP BY\n    mayor\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     unrest,\n     corpus,\n     swell,\n     fiasco,\n     friar\n|> SELECT\n     unrest,\n     corpus,\n     swell,\n     fiasco,\n     friar\n|> AGGREGATE\n     COUNT(swell) AS snack,\n     MAX(fiasco) AS coast,\n     MAX(swell) AS dread\n   GROUP BY\n     unrest\n|> AGGREGATE\n     MIN(dread) AS mayor,\n     SUM(coast) AS radar,\n     AVG(dread) AS miller,\n     SUM(dread) AS envoy,\n     AVG(saloon) AS bench\n   GROUP BY\n     snack\n|> AGGREGATE\n     MIN(radar) AS heater,\n     COUNT(envoy) AS flesh,\n     SUM(miller) AS sting\n   GROUP BY\n     mayor\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"saloon",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hammer,\n    salute,\n    amino,\n    empire,\n    worth\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(worth) AS kelly,\n    MIN(amino) AS wicker\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(kelly) AS spade,\n    MIN(wicker) AS outing,\n    COUNT(wicker) AS tundra,\n    SUM(kelly) AS debris,\n    AVG(kelly) AS frame\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MIN(frame) AS anchor,\n    SUM(tundra) AS minus,\n    AVG(frame) AS bertha,\n    SUM(parry) AS radio,\n    MIN(outing) AS cliff,\n    SUM(spade) AS amount,\n    SUM(frame) AS value\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(anchor) AS little\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hammer,\n     salute,\n     amino,\n     empire,\n     worth\n|> AGGREGATE\n     MAX(worth) AS kelly,\n     MIN(amino) AS wicker\n|> AGGREGATE\n     MAX(kelly) AS spade,\n     MIN(wicker) AS outing,\n     COUNT(wicker) AS tundra,\n     SUM(kelly) AS debris,\n     AVG(kelly) AS frame\n|> AGGREGATE\n     MIN(frame) AS anchor,\n     SUM(tundra) AS minus,\n     AVG(frame) AS bertha,\n     SUM(parry) AS radio,\n     MIN(outing) AS cliff,\n     SUM(spade) AS amount,\n     SUM(frame) AS value\n|> AGGREGATE\n     MIN(anchor) AS little\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"parry",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    canoe,\n    viewer,\n    catch,\n    morale,\n    advent\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    canoe,\n    viewer\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(viewer) AS board,\n    SUM(viewer) AS secret,\n    MIN(canoe) AS beech,\n    COUNT(viewer) AS tract,\n    MAX(canoe) AS hollow\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MIN(beech) AS remark,\n    COUNT(beech) AS rebate,\n    MIN(hollow) AS train,\n    AVG(hollow) AS honda,\n    AVG(yellow) AS jersey\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    remark,\n    rebate,\n    train,\n    MIN(honda) AS mercer\n  FROM cte_3\n  GROUP BY\n    remark,\n    rebate,\n    train\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     canoe,\n     viewer,\n     catch,\n     morale,\n     advent\n|> SELECT\n     canoe,\n     viewer\n|> AGGREGATE\n     MAX(viewer) AS board,\n     SUM(viewer) AS secret,\n     MIN(canoe) AS beech,\n     COUNT(viewer) AS tract,\n     MAX(canoe) AS hollow\n|> AGGREGATE\n     MIN(beech) AS remark,\n     COUNT(beech) AS rebate,\n     MIN(hollow) AS train,\n     AVG(hollow) AS honda,\n     AVG(yellow) AS jersey\n|> AGGREGATE\n     MIN(honda) AS mercer\n   GROUP BY\n     remark,\n     rebate,\n     train\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"yellow",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    opener,\n    jargon,\n    wreck,\n    rider,\n    tycoon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(tycoon) AS lapse,\n    MIN(tycoon) AS boxer,\n    SUM(rider) AS energy,\n    COUNT(tycoon) AS lotion\n  FROM cte_0\n  GROUP BY\n    opener,\n    jargon,\n    wreck\n),\ncte_2 AS (\n  SELECT\n    COUNT(energy) AS build,\n    MIN(energy) AS siren\n  FROM cte_1\n  GROUP BY\n    lapse\n),\ncte_3 AS (\n  SELECT\n    AVG(siren) AS whore,\n    AVG(build) AS stump,\n    AVG(flood) AS float,\n    MAX(build) AS brewer,\n    MIN(build) AS volume\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(stump) AS tonne,\n    SUM(volume) AS pistol\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     opener,\n     jargon,\n     wreck,\n     rider,\n     tycoon\n|> AGGREGATE\n     MAX(tycoon) AS lapse,\n     MIN(tycoon) AS boxer,\n     SUM(rider) AS energy,\n     COUNT(tycoon) AS lotion\n   GROUP BY\n     opener,\n     jargon,\n     wreck\n|> AGGREGATE\n     COUNT(energy) AS build,\n     MIN(energy) AS siren\n   GROUP BY\n     lapse\n|> AGGREGATE\n     AVG(siren) AS whore,\n     AVG(build) AS stump,\n     AVG(flood) AS float,\n     MAX(build) AS brewer,\n     MIN(build) AS volume\n|> AGGREGATE\n     COUNT(stump) AS tonne,\n     SUM(volume) AS pistol\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"flood",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sensor,\n    cheese,\n    genius,\n    novice,\n    wheat\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    sensor,\n    cheese,\n    genius\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(genius) AS toilet,\n    COUNT(genius) AS state,\n    AVG(cheese) AS enamel,\n    MAX(cheese) AS peter\n  FROM cte_1\n  GROUP BY\n    sensor\n),\ncte_3 AS (\n  SELECT\n    MAX(state) AS swamp,\n    MIN(enamel) AS octave,\n    MIN(state) AS nether,\n    COUNT(state) AS truce,\n    MAX(outlet) AS devil,\n    AVG(peter) AS miller,\n    AVG(state) AS stoke\n  FROM cte_2\n  GROUP BY\n    toilet\n),\ncte_4 AS (\n  SELECT\n    swamp,\n    octave,\n    nether\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sensor,\n     cheese,\n     genius,\n     novice,\n     wheat\n|> SELECT\n     sensor,\n     cheese,\n     genius\n|> AGGREGATE\n     SUM(genius) AS toilet,\n     COUNT(genius) AS state,\n     AVG(cheese) AS enamel,\n     MAX(cheese) AS peter\n   GROUP BY\n     sensor\n|> AGGREGATE\n     MAX(state) AS swamp,\n     MIN(enamel) AS octave,\n     MIN(state) AS nether,\n     COUNT(state) AS truce,\n     MAX(outlet) AS devil,\n     AVG(peter) AS miller,\n     AVG(state) AS stoke\n   GROUP BY\n     toilet\n|> SELECT\n     swamp,\n     octave,\n     nether\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"outlet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    reign,\n    flare,\n    colour,\n    garden,\n    chang\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    reign,\n    flare\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(reign) AS shout,\n    AVG(flare) AS sultan,\n    MAX(flare) AS lemon,\n    SUM(reign) AS legacy\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(legacy) AS murray,\n    COUNT(legacy) AS payne,\n    MAX(legacy) AS shock,\n    MIN(legacy) AS pedal,\n    AVG(legacy) AS excise,\n    COUNT(peach) AS maxim\n  FROM cte_2\n  GROUP BY\n    shout,\n    sultan,\n    lemon\n),\ncte_4 AS (\n  SELECT\n    murray,\n    payne,\n    shock,\n    pedal,\n    excise\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     reign,\n     flare,\n     colour,\n     garden,\n     chang\n|> SELECT\n     reign,\n     flare\n|> AGGREGATE\n     COUNT(reign) AS shout,\n     AVG(flare) AS sultan,\n     MAX(flare) AS lemon,\n     SUM(reign) AS legacy\n|> AGGREGATE\n     SUM(legacy) AS murray,\n     COUNT(legacy) AS payne,\n     MAX(legacy) AS shock,\n     MIN(legacy) AS pedal,\n     AVG(legacy) AS excise,\n     COUNT(peach) AS maxim\n   GROUP BY\n     shout,\n     sultan,\n     lemon\n|> SELECT\n     murray,\n     payne,\n     shock,\n     pedal,\n     excise\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"peach",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    growth,\n    sphere,\n    photo,\n    acorn,\n    series\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    growth,\n    sphere,\n    photo,\n    acorn,\n    series\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(photo) AS luxury,\n    MAX(acorn) AS senate,\n    AVG(series) AS truck,\n    MIN(sphere) AS square\n  FROM cte_1\n  GROUP BY\n    growth\n),\ncte_3 AS (\n  SELECT\n    AVG(senate) AS blitz,\n    SUM(luxury) AS genome,\n    AVG(luxury) AS chain,\n    COUNT(teapot) AS anthem\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(chain) AS atlas,\n    MAX(genome) AS sultan,\n    SUM(chain) AS louis,\n    AVG(genome) AS length\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     growth,\n     sphere,\n     photo,\n     acorn,\n     series\n|> SELECT\n     growth,\n     sphere,\n     photo,\n     acorn,\n     series\n|> AGGREGATE\n     SUM(photo) AS luxury,\n     MAX(acorn) AS senate,\n     AVG(series) AS truck,\n     MIN(sphere) AS square\n   GROUP BY\n     growth\n|> AGGREGATE\n     AVG(senate) AS blitz,\n     SUM(luxury) AS genome,\n     AVG(luxury) AS chain,\n     COUNT(teapot) AS anthem\n|> AGGREGATE\n     COUNT(chain) AS atlas,\n     MAX(genome) AS sultan,\n     SUM(chain) AS louis,\n     AVG(genome) AS length\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"teapot",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    christ,\n    nurse,\n    abbey,\n    stigma,\n    cherry\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    christ,\n    nurse,\n    abbey\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(abbey) AS burial,\n    COUNT(abbey) AS steak,\n    SUM(abbey) AS moran,\n    AVG(abbey) AS arrest\n  FROM cte_1\n  GROUP BY\n    christ,\n    nurse\n),\ncte_3 AS (\n  SELECT\n    MAX(arrest) AS comet,\n    MIN(arrest) AS choir,\n    SUM(moran) AS lorry\n  FROM cte_2\n  GROUP BY\n    burial\n),\ncte_4 AS (\n  SELECT\n    SUM(harper) AS swamp,\n    MIN(lorry) AS finch,\n    AVG(lorry) AS sheen,\n    SUM(lorry) AS hinge\n  FROM cte_3\n  GROUP BY\n    comet,\n    choir\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     christ,\n     nurse,\n     abbey,\n     stigma,\n     cherry\n|> SELECT\n     christ,\n     nurse,\n     abbey\n|> AGGREGATE\n     MAX(abbey) AS burial,\n     COUNT(abbey) AS steak,\n     SUM(abbey) AS moran,\n     AVG(abbey) AS arrest\n   GROUP BY\n     christ,\n     nurse\n|> AGGREGATE\n     MAX(arrest) AS comet,\n     MIN(arrest) AS choir,\n     SUM(moran) AS lorry\n   GROUP BY\n     burial\n|> AGGREGATE\n     SUM(harper) AS swamp,\n     MIN(lorry) AS finch,\n     AVG(lorry) AS sheen,\n     SUM(lorry) AS hinge\n   GROUP BY\n     comet,\n     choir\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"harper",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    yellow,\n    golfer,\n    supper,\n    source,\n    sheep\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(source) AS actor,\n    MAX(sheep) AS stamp,\n    SUM(sheep) AS creed,\n    SUM(source) AS priory,\n    MIN(source) AS class,\n    AVG(sheep) AS stance\n  FROM cte_0\n  GROUP BY\n    yellow,\n    golfer\n),\ncte_2 AS (\n  SELECT\n    actor,\n    stamp\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(actor) AS shame,\n    AVG(stamp) AS sexism,\n    MIN(joseph) AS canvas\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(shame) AS rogue,\n    COUNT(sexism) AS strand,\n    MAX(sexism) AS pupil,\n    MIN(sexism) AS larry\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     yellow,\n     golfer,\n     supper,\n     source,\n     sheep\n|> AGGREGATE\n     COUNT(source) AS actor,\n     MAX(sheep) AS stamp,\n     SUM(sheep) AS creed,\n     SUM(source) AS priory,\n     MIN(source) AS class,\n     AVG(sheep) AS stance\n   GROUP BY\n     yellow,\n     golfer\n|> SELECT\n     actor,\n     stamp\n|> AGGREGATE\n     AVG(actor) AS shame,\n     AVG(stamp) AS sexism,\n     MIN(joseph) AS canvas\n|> AGGREGATE\n     AVG(shame) AS rogue,\n     COUNT(sexism) AS strand,\n     MAX(sexism) AS pupil,\n     MIN(sexism) AS larry\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"joseph",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    madge,\n    galley,\n    breach,\n    museum,\n    brett\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(breach) AS paddy,\n    MIN(museum) AS throne,\n    COUNT(brett) AS guido,\n    SUM(brett) AS praise\n  FROM cte_0\n  GROUP BY\n    madge,\n    galley\n),\ncte_2 AS (\n  SELECT\n    MIN(praise) AS fusion,\n    SUM(throne) AS derby\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(derby) AS belly,\n    SUM(derby) AS brute,\n    MAX(derby) AS grant,\n    COUNT(parole) AS heresy,\n    AVG(derby) AS mosaic\n  FROM cte_2\n  GROUP BY\n    fusion\n),\ncte_4 AS (\n  SELECT\n    SUM(brute) AS treat,\n    AVG(grant) AS harper\n  FROM cte_3\n  GROUP BY\n    belly\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     madge,\n     galley,\n     breach,\n     museum,\n     brett\n|> AGGREGATE\n     COUNT(breach) AS paddy,\n     MIN(museum) AS throne,\n     COUNT(brett) AS guido,\n     SUM(brett) AS praise\n   GROUP BY\n     madge,\n     galley\n|> AGGREGATE\n     MIN(praise) AS fusion,\n     SUM(throne) AS derby\n|> AGGREGATE\n     COUNT(derby) AS belly,\n     SUM(derby) AS brute,\n     MAX(derby) AS grant,\n     COUNT(parole) AS heresy,\n     AVG(derby) AS mosaic\n   GROUP BY\n     fusion\n|> AGGREGATE\n     SUM(brute) AS treat,\n     AVG(grant) AS harper\n   GROUP BY\n     belly\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"parole",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    wrong,\n    clock,\n    goose,\n    system,\n    stuff\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(clock) AS studio,\n    AVG(clock) AS purse,\n    COUNT(system) AS saloon\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(purse) AS piper,\n    MIN(purse) AS laura,\n    MAX(saloon) AS broad\n  FROM cte_1\n  GROUP BY\n    studio\n),\ncte_3 AS (\n  SELECT\n    piper,\n    laura,\n    broad\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(broad) AS future,\n    MIN(broad) AS comet,\n    SUM(museum) AS lesson,\n    AVG(broad) AS repair,\n    MIN(laura) AS scare,\n    SUM(broad) AS turtle,\n    COUNT(laura) AS crisp\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     wrong,\n     clock,\n     goose,\n     system,\n     stuff\n|> AGGREGATE\n     SUM(clock) AS studio,\n     AVG(clock) AS purse,\n     COUNT(system) AS saloon\n|> AGGREGATE\n     SUM(purse) AS piper,\n     MIN(purse) AS laura,\n     MAX(saloon) AS broad\n   GROUP BY\n     studio\n|> SELECT\n     piper,\n     laura,\n     broad\n|> AGGREGATE\n     MAX(broad) AS future,\n     MIN(broad) AS comet,\n     SUM(museum) AS lesson,\n     AVG(broad) AS repair,\n     MIN(laura) AS scare,\n     SUM(broad) AS turtle,\n     COUNT(laura) AS crisp\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"museum",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    lawyer,\n    canada,\n    phone,\n    flair,\n    crypt\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(phone) AS fairy,\n    AVG(crypt) AS damage,\n    SUM(flair) AS blast,\n    MAX(crypt) AS fiver,\n    MIN(flair) AS ethic,\n    MIN(crypt) AS fruit\n  FROM cte_0\n  GROUP BY\n    lawyer,\n    canada\n),\ncte_2 AS (\n  SELECT\n    MAX(blast) AS senna,\n    AVG(blast) AS slice,\n    SUM(blast) AS joanna,\n    MIN(bumper) AS guest,\n    SUM(damage) AS thrill\n  FROM cte_1\n  GROUP BY\n    fairy\n),\ncte_3 AS (\n  SELECT\n    senna,\n    slice\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(slice) AS alkali\n  FROM cte_3\n  GROUP BY\n    senna\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     lawyer,\n     canada,\n     phone,\n     flair,\n     crypt\n|> AGGREGATE\n     MAX(phone) AS fairy,\n     AVG(crypt) AS damage,\n     SUM(flair) AS blast,\n     MAX(crypt) AS fiver,\n     MIN(flair) AS ethic,\n     MIN(crypt) AS fruit\n   GROUP BY\n     lawyer,\n     canada\n|> AGGREGATE\n     MAX(blast) AS senna,\n     AVG(blast) AS slice,\n     SUM(blast) AS joanna,\n     MIN(bumper) AS guest,\n     SUM(damage) AS thrill\n   GROUP BY\n     fairy\n|> SELECT\n     senna,\n     slice\n|> AGGREGATE\n     MAX(slice) AS alkali\n   GROUP BY\n     senna\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"bumper",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    grouse,\n    police,\n    drink,\n    norman,\n    violet\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    grouse,\n    police,\n    drink,\n    norman,\n    violet\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    grouse,\n    police,\n    drink,\n    norman\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(norman) AS reply,\n    MIN(drink) AS gleam,\n    SUM(police) AS simon,\n    AVG(police) AS currie,\n    COUNT(norman) AS stream\n  FROM cte_2\n  GROUP BY\n    grouse\n),\ncte_4 AS (\n  SELECT\n    MAX(strike) AS black,\n    COUNT(currie) AS orchid,\n    SUM(currie) AS steen,\n    AVG(stream) AS female\n  FROM cte_3\n  GROUP BY\n    reply,\n    gleam\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     grouse,\n     police,\n     drink,\n     norman,\n     violet\n|> SELECT\n     grouse,\n     police,\n     drink,\n     norman,\n     violet\n|> SELECT\n     grouse,\n     police,\n     drink,\n     norman\n|> AGGREGATE\n     SUM(norman) AS reply,\n     MIN(drink) AS gleam,\n     SUM(police) AS simon,\n     AVG(police) AS currie,\n     COUNT(norman) AS stream\n   GROUP BY\n     grouse\n|> AGGREGATE\n     MAX(strike) AS black,\n     COUNT(currie) AS orchid,\n     SUM(currie) AS steen,\n     AVG(stream) AS female\n   GROUP BY\n     reply,\n     gleam\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"strike",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:28},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    steel,\n    henry,\n    wigan,\n    queue,\n    gossip\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(wigan) AS ascot,\n    MIN(wigan) AS forte,\n    SUM(gossip) AS murmur\n  FROM cte_0\n  GROUP BY\n    steel,\n    henry\n),\ncte_2 AS (\n  SELECT\n    SUM(murmur) AS purse,\n    COUNT(ascot) AS layer,\n    AVG(murmur) AS cooker,\n    MIN(ascot) AS filter\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(filter) AS chain,\n    AVG(cooker) AS racism,\n    AVG(fauna) AS papacy,\n    MAX(cooker) AS connie,\n    MIN(filter) AS future\n  FROM cte_2\n  GROUP BY\n    purse\n),\ncte_4 AS (\n  SELECT\n    chain,\n    racism,\n    connie\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     steel,\n     henry,\n     wigan,\n     queue,\n     gossip\n|> AGGREGATE\n     AVG(wigan) AS ascot,\n     MIN(wigan) AS forte,\n     SUM(gossip) AS murmur\n   GROUP BY\n     steel,\n     henry\n|> AGGREGATE\n     SUM(murmur) AS purse,\n     COUNT(ascot) AS layer,\n     AVG(murmur) AS cooker,\n     MIN(ascot) AS filter\n|> AGGREGATE\n     MAX(filter) AS chain,\n     AVG(cooker) AS racism,\n     AVG(fauna) AS papacy,\n     MAX(cooker) AS connie,\n     MIN(filter) AS future\n   GROUP BY\n     purse\n|> SELECT\n     chain,\n     racism,\n     connie\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"fauna",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    pulpit,\n    client,\n    doctor,\n    trial,\n    fella\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(doctor) AS handle,\n    MAX(client) AS platt,\n    MIN(fella) AS saloon,\n    SUM(doctor) AS grape,\n    AVG(pulpit) AS relish\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(saloon) AS paper,\n    AVG(platt) AS harem\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(harem) AS focus,\n    MIN(paper) AS quartz,\n    MAX(harem) AS pastor,\n    COUNT(unity) AS sleep,\n    COUNT(paper) AS elite,\n    SUM(paper) AS simon,\n    COUNT(harem) AS basket\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    focus,\n    quartz\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     pulpit,\n     client,\n     doctor,\n     trial,\n     fella\n|> AGGREGATE\n     COUNT(doctor) AS handle,\n     MAX(client) AS platt,\n     MIN(fella) AS saloon,\n     SUM(doctor) AS grape,\n     AVG(pulpit) AS relish\n|> AGGREGATE\n     MAX(saloon) AS paper,\n     AVG(platt) AS harem\n|> AGGREGATE\n     SUM(harem) AS focus,\n     MIN(paper) AS quartz,\n     MAX(harem) AS pastor,\n     COUNT(unity) AS sleep,\n     COUNT(paper) AS elite,\n     SUM(paper) AS simon,\n     COUNT(harem) AS basket\n|> SELECT\n     focus,\n     quartz\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"unity",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    visit,\n    crunch,\n    taste,\n    garlic,\n    asylum\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(asylum) AS pinch,\n    SUM(asylum) AS swing,\n    MIN(garlic) AS utopia\n  FROM cte_0\n  GROUP BY\n    visit,\n    crunch\n),\ncte_2 AS (\n  SELECT\n    MAX(pinch) AS truck,\n    MIN(pinch) AS morris,\n    SUM(swing) AS mungo,\n    COUNT(utopia) AS impact\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(truck) AS mentor,\n    COUNT(morris) AS entry,\n    MIN(token) AS robot\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(mentor) AS bubble,\n    SUM(mentor) AS trauma,\n    MAX(mentor) AS defect,\n    AVG(mentor) AS launch\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     visit,\n     crunch,\n     taste,\n     garlic,\n     asylum\n|> AGGREGATE\n     COUNT(asylum) AS pinch,\n     SUM(asylum) AS swing,\n     MIN(garlic) AS utopia\n   GROUP BY\n     visit,\n     crunch\n|> AGGREGATE\n     MAX(pinch) AS truck,\n     MIN(pinch) AS morris,\n     SUM(swing) AS mungo,\n     COUNT(utopia) AS impact\n|> AGGREGATE\n     COUNT(truck) AS mentor,\n     COUNT(morris) AS entry,\n     MIN(token) AS robot\n|> AGGREGATE\n     COUNT(mentor) AS bubble,\n     SUM(mentor) AS trauma,\n     MAX(mentor) AS defect,\n     AVG(mentor) AS launch\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"token",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    herald,\n    cartel,\n    office,\n    alien,\n    artist\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(herald) AS style,\n    MAX(alien) AS scene,\n    MAX(office) AS miller,\n    MIN(alien) AS shrub,\n    MIN(herald) AS infant,\n    COUNT(office) AS pilot\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    style,\n    scene\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(scene) AS denim,\n    MAX(scene) AS guess,\n    MIN(scene) AS socket,\n    COUNT(danger) AS ledge\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    denim,\n    guess,\n    COUNT(socket) AS breath,\n    MAX(socket) AS carpet\n  FROM cte_3\n  GROUP BY\n    denim,\n    guess\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     herald,\n     cartel,\n     office,\n     alien,\n     artist\n|> AGGREGATE\n     AVG(herald) AS style,\n     MAX(alien) AS scene,\n     MAX(office) AS miller,\n     MIN(alien) AS shrub,\n     MIN(herald) AS infant,\n     COUNT(office) AS pilot\n|> SELECT\n     style,\n     scene\n|> AGGREGATE\n     COUNT(scene) AS denim,\n     MAX(scene) AS guess,\n     MIN(scene) AS socket,\n     COUNT(danger) AS ledge\n|> AGGREGATE\n     COUNT(socket) AS breath,\n     MAX(socket) AS carpet\n   GROUP BY\n     denim,\n     guess\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"danger",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    angle,\n    anger,\n    alien,\n    plague,\n    build\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(angle) AS demise,\n    MIN(plague) AS queen,\n    SUM(build) AS topic,\n    SUM(plague) AS bowler,\n    MAX(plague) AS gospel\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(demise) AS mucosa,\n    MIN(topic) AS common\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(common) AS tooth,\n    AVG(common) AS payne,\n    SUM(mucosa) AS stair,\n    AVG(tarmac) AS heroin\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(tooth) AS regent,\n    COUNT(tooth) AS symbol,\n    MIN(stair) AS basin,\n    COUNT(payne) AS chance\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     angle,\n     anger,\n     alien,\n     plague,\n     build\n|> AGGREGATE\n     MIN(angle) AS demise,\n     MIN(plague) AS queen,\n     SUM(build) AS topic,\n     SUM(plague) AS bowler,\n     MAX(plague) AS gospel\n|> AGGREGATE\n     SUM(demise) AS mucosa,\n     MIN(topic) AS common\n|> AGGREGATE\n     SUM(common) AS tooth,\n     AVG(common) AS payne,\n     SUM(mucosa) AS stair,\n     AVG(tarmac) AS heroin\n|> AGGREGATE\n     MIN(tooth) AS regent,\n     COUNT(tooth) AS symbol,\n     MIN(stair) AS basin,\n     COUNT(payne) AS chance\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"tarmac",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    guilt,\n    photo,\n    dieter,\n    caste,\n    tailor\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(dieter) AS sister,\n    MAX(dieter) AS mouth,\n    MAX(tailor) AS rifle,\n    MAX(caste) AS rocket\n  FROM cte_0\n  GROUP BY\n    guilt,\n    photo\n),\ncte_2 AS (\n  SELECT\n    MIN(rifle) AS source,\n    SUM(rocket) AS cadet,\n    MAX(rifle) AS search,\n    AVG(rifle) AS lotion,\n    MIN(rocket) AS grain,\n    COUNT(murder) AS stump\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    source,\n    AVG(lotion) AS prompt,\n    AVG(cadet) AS career\n  FROM cte_2\n  GROUP BY\n    source\n),\ncte_4 AS (\n  SELECT\n    COUNT(career) AS jungle\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     guilt,\n     photo,\n     dieter,\n     caste,\n     tailor\n|> AGGREGATE\n     COUNT(dieter) AS sister,\n     MAX(dieter) AS mouth,\n     MAX(tailor) AS rifle,\n     MAX(caste) AS rocket\n   GROUP BY\n     guilt,\n     photo\n|> AGGREGATE\n     MIN(rifle) AS source,\n     SUM(rocket) AS cadet,\n     MAX(rifle) AS search,\n     AVG(rifle) AS lotion,\n     MIN(rocket) AS grain,\n     COUNT(murder) AS stump\n|> AGGREGATE\n     AVG(lotion) AS prompt,\n     AVG(cadet) AS career\n   GROUP BY\n     source\n|> AGGREGATE\n     COUNT(career) AS jungle\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"murder",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    arrest,\n    german,\n    relish,\n    barley,\n    burke\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    arrest,\n    german,\n    relish,\n    barley,\n    burke\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(german) AS drive,\n    MAX(barley) AS picket,\n    COUNT(relish) AS dobson,\n    COUNT(burke) AS wallet,\n    AVG(relish) AS mayor\n  FROM cte_1\n  GROUP BY\n    arrest\n),\ncte_3 AS (\n  SELECT\n    COUNT(mayor) AS member\n  FROM cte_2\n  GROUP BY\n    drive,\n    picket\n),\ncte_4 AS (\n  SELECT\n    MIN(primer) AS medal,\n    MAX(member) AS orbit,\n    AVG(member) AS bubble,\n    SUM(member) AS inside,\n    MIN(member) AS draft\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     arrest,\n     german,\n     relish,\n     barley,\n     burke\n|> SELECT\n     arrest,\n     german,\n     relish,\n     barley,\n     burke\n|> AGGREGATE\n     MAX(german) AS drive,\n     MAX(barley) AS picket,\n     COUNT(relish) AS dobson,\n     COUNT(burke) AS wallet,\n     AVG(relish) AS mayor\n   GROUP BY\n     arrest\n|> AGGREGATE\n     COUNT(mayor) AS member\n   GROUP BY\n     drive,\n     picket\n|> AGGREGATE\n     MIN(primer) AS medal,\n     MAX(member) AS orbit,\n     AVG(member) AS bubble,\n     SUM(member) AS inside,\n     MIN(member) AS draft\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"primer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:28},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    wedge,\n    brett,\n    bypass,\n    tarmac,\n    basin\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(basin) AS witch\n  FROM cte_0\n  GROUP BY\n    wedge,\n    brett,\n    bypass,\n    tarmac\n),\ncte_2 AS (\n  SELECT\n    MAX(witch) AS yeast,\n    MIN(witch) AS shape,\n    AVG(witch) AS peach\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MIN(peach) AS mummy,\n    AVG(peach) AS derby,\n    SUM(peach) AS pickup,\n    MAX(peach) AS laura,\n    AVG(board) AS sewage\n  FROM cte_2\n  GROUP BY\n    yeast,\n    shape\n),\ncte_4 AS (\n  SELECT\n    mummy,\n    derby,\n    SUM(pickup) AS comma\n  FROM cte_3\n  GROUP BY\n    mummy,\n    derby\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     wedge,\n     brett,\n     bypass,\n     tarmac,\n     basin\n|> AGGREGATE\n     MIN(basin) AS witch\n   GROUP BY\n     wedge,\n     brett,\n     bypass,\n     tarmac\n|> AGGREGATE\n     MAX(witch) AS yeast,\n     MIN(witch) AS shape,\n     AVG(witch) AS peach\n|> AGGREGATE\n     MIN(peach) AS mummy,\n     AVG(peach) AS derby,\n     SUM(peach) AS pickup,\n     MAX(peach) AS laura,\n     AVG(board) AS sewage\n   GROUP BY\n     yeast,\n     shape\n|> AGGREGATE\n     SUM(pickup) AS comma\n   GROUP BY\n     mummy,\n     derby\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"board",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hamlet,\n    molla,\n    behalf,\n    piece,\n    regard\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    hamlet,\n    molla,\n    behalf\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(behalf) AS sewage,\n    MIN(molla) AS winner,\n    MAX(molla) AS issue,\n    AVG(behalf) AS rubble\n  FROM cte_1\n  GROUP BY\n    hamlet\n),\ncte_3 AS (\n  SELECT\n    MIN(sewage) AS church,\n    AVG(issue) AS kylie,\n    COUNT(issue) AS cliff,\n    MAX(winner) AS change,\n    SUM(middle) AS merit,\n    COUNT(rubble) AS claret\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    church,\n    SUM(claret) AS glare,\n    AVG(claret) AS shake\n  FROM cte_3\n  GROUP BY\n    church\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hamlet,\n     molla,\n     behalf,\n     piece,\n     regard\n|> SELECT\n     hamlet,\n     molla,\n     behalf\n|> AGGREGATE\n     COUNT(behalf) AS sewage,\n     MIN(molla) AS winner,\n     MAX(molla) AS issue,\n     AVG(behalf) AS rubble\n   GROUP BY\n     hamlet\n|> AGGREGATE\n     MIN(sewage) AS church,\n     AVG(issue) AS kylie,\n     COUNT(issue) AS cliff,\n     MAX(winner) AS change,\n     SUM(middle) AS merit,\n     COUNT(rubble) AS claret\n|> AGGREGATE\n     SUM(claret) AS glare,\n     AVG(claret) AS shake\n   GROUP BY\n     church\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"middle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    connie,\n    meadow,\n    sequel,\n    shade,\n    haste\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(shade) AS place,\n    SUM(meadow) AS ferry,\n    MAX(haste) AS piety,\n    SUM(shade) AS signal,\n    MIN(meadow) AS ridge,\n    AVG(haste) AS shaikh,\n    MIN(shade) AS career,\n    MIN(haste) AS walker\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(career) AS grasp,\n    SUM(walker) AS draper,\n    SUM(faith) AS unease,\n    COUNT(ridge) AS pitch\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(draper) AS surrey,\n    MAX(grasp) AS jigsaw\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    surrey,\n    jigsaw\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     connie,\n     meadow,\n     sequel,\n     shade,\n     haste\n|> AGGREGATE\n     AVG(shade) AS place,\n     SUM(meadow) AS ferry,\n     MAX(haste) AS piety,\n     SUM(shade) AS signal,\n     MIN(meadow) AS ridge,\n     AVG(haste) AS shaikh,\n     MIN(shade) AS career,\n     MIN(haste) AS walker\n|> AGGREGATE\n     MIN(career) AS grasp,\n     SUM(walker) AS draper,\n     SUM(faith) AS unease,\n     COUNT(ridge) AS pitch\n|> AGGREGATE\n     COUNT(draper) AS surrey,\n     MAX(grasp) AS jigsaw\n|> SELECT\n     surrey,\n     jigsaw\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"faith",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:27,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    planet,\n    spate,\n    sewage,\n    donor,\n    script\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(sewage) AS potato,\n    MIN(sewage) AS right,\n    MAX(donor) AS freeze\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(freeze) AS short,\n    SUM(freeze) AS salute,\n    AVG(freeze) AS chant,\n    MIN(freeze) AS crest\n  FROM cte_1\n  GROUP BY\n    potato\n),\ncte_3 AS (\n  SELECT\n    MIN(salute) AS marrow\n  FROM cte_2\n  GROUP BY\n    short\n),\ncte_4 AS (\n  SELECT\n    MIN(marrow) AS mucosa,\n    MAX(porch) AS brewer,\n    MAX(marrow) AS serum,\n    COUNT(marrow) AS marsh,\n    SUM(marrow) AS japan,\n    AVG(marrow) AS shawl\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     planet,\n     spate,\n     sewage,\n     donor,\n     script\n|> AGGREGATE\n     MAX(sewage) AS potato,\n     MIN(sewage) AS right,\n     MAX(donor) AS freeze\n|> AGGREGATE\n     COUNT(freeze) AS short,\n     SUM(freeze) AS salute,\n     AVG(freeze) AS chant,\n     MIN(freeze) AS crest\n   GROUP BY\n     potato\n|> AGGREGATE\n     MIN(salute) AS marrow\n   GROUP BY\n     short\n|> AGGREGATE\n     MIN(marrow) AS mucosa,\n     MAX(porch) AS brewer,\n     MAX(marrow) AS serum,\n     COUNT(marrow) AS marsh,\n     SUM(marrow) AS japan,\n     AVG(marrow) AS shawl\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"porch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    father,\n    skirt,\n    mosaic,\n    helix,\n    fisher\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(helix) AS smart,\n    MAX(fisher) AS hassle,\n    COUNT(helix) AS nelson\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(nelson) AS liquor,\n    AVG(nelson) AS sister,\n    MIN(smart) AS ghetto,\n    MIN(nelson) AS burst\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(ghetto) AS muddle,\n    AVG(ghetto) AS album,\n    MAX(burst) AS thumb,\n    SUM(pellet) AS insect,\n    SUM(sister) AS michel,\n    COUNT(burst) AS pulse\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    muddle,\n    album,\n    thumb,\n    michel\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     father,\n     skirt,\n     mosaic,\n     helix,\n     fisher\n|> AGGREGATE\n     SUM(helix) AS smart,\n     MAX(fisher) AS hassle,\n     COUNT(helix) AS nelson\n|> AGGREGATE\n     SUM(nelson) AS liquor,\n     AVG(nelson) AS sister,\n     MIN(smart) AS ghetto,\n     MIN(nelson) AS burst\n|> AGGREGATE\n     MAX(ghetto) AS muddle,\n     AVG(ghetto) AS album,\n     MAX(burst) AS thumb,\n     SUM(pellet) AS insect,\n     SUM(sister) AS michel,\n     COUNT(burst) AS pulse\n|> SELECT\n     muddle,\n     album,\n     thumb,\n     michel\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"pellet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    runner,\n    parson,\n    tycoon,\n    turkey,\n    ernest\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    runner,\n    parson,\n    tycoon,\n    turkey,\n    ernest\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    runner,\n    parson,\n    tycoon,\n    turkey,\n    ernest\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(tycoon) AS square,\n    MAX(turkey) AS canopy,\n    COUNT(ernest) AS advert,\n    MIN(turkey) AS betty,\n    COUNT(turkey) AS patron,\n    MIN(length) AS celery\n  FROM cte_2\n  GROUP BY\n    runner,\n    parson\n),\ncte_4 AS (\n  SELECT\n    square,\n    MAX(betty) AS bureau,\n    SUM(canopy) AS lance\n  FROM cte_3\n  GROUP BY\n    square\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     runner,\n     parson,\n     tycoon,\n     turkey,\n     ernest\n|> SELECT\n     runner,\n     parson,\n     tycoon,\n     turkey,\n     ernest\n|> SELECT\n     runner,\n     parson,\n     tycoon,\n     turkey,\n     ernest\n|> AGGREGATE\n     AVG(tycoon) AS square,\n     MAX(turkey) AS canopy,\n     COUNT(ernest) AS advert,\n     MIN(turkey) AS betty,\n     COUNT(turkey) AS patron,\n     MIN(length) AS celery\n   GROUP BY\n     runner,\n     parson\n|> AGGREGATE\n     MAX(betty) AS bureau,\n     SUM(canopy) AS lance\n   GROUP BY\n     square\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"length",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    start,\n    refund,\n    portal,\n    tenor,\n    style\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(tenor) AS flurry,\n    MIN(tenor) AS output,\n    MAX(refund) AS avail\n  FROM cte_0\n  GROUP BY\n    start\n),\ncte_2 AS (\n  SELECT\n    MIN(avail) AS queen,\n    SUM(output) AS writer,\n    MAX(output) AS cortex\n  FROM cte_1\n  GROUP BY\n    flurry\n),\ncte_3 AS (\n  SELECT\n    MIN(writer) AS baxter,\n    COUNT(writer) AS excess,\n    MAX(cortex) AS shoal,\n    COUNT(heroin) AS memory,\n    AVG(writer) AS height\n  FROM cte_2\n  GROUP BY\n    queen\n),\ncte_4 AS (\n  SELECT\n    baxter,\n    AVG(height) AS gutter\n  FROM cte_3\n  GROUP BY\n    baxter,\n    excess\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     start,\n     refund,\n     portal,\n     tenor,\n     style\n|> AGGREGATE\n     COUNT(tenor) AS flurry,\n     MIN(tenor) AS output,\n     MAX(refund) AS avail\n   GROUP BY\n     start\n|> AGGREGATE\n     MIN(avail) AS queen,\n     SUM(output) AS writer,\n     MAX(output) AS cortex\n   GROUP BY\n     flurry\n|> AGGREGATE\n     MIN(writer) AS baxter,\n     COUNT(writer) AS excess,\n     MAX(cortex) AS shoal,\n     COUNT(heroin) AS memory,\n     AVG(writer) AS height\n   GROUP BY\n     queen\n|> AGGREGATE\n     AVG(height) AS gutter\n   GROUP BY\n     baxter,\n     excess\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"heroin",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    peril,\n    voter,\n    nation,\n    mutiny,\n    patrol\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(patrol) AS burst,\n    COUNT(patrol) AS glass,\n    AVG(patrol) AS frank\n  FROM cte_0\n  GROUP BY\n    peril,\n    voter,\n    nation,\n    mutiny\n),\ncte_2 AS (\n  SELECT\n    SUM(frank) AS horror,\n    MAX(frank) AS jessie,\n    AVG(burst) AS rhino,\n    SUM(glass) AS clover,\n    COUNT(burst) AS custom,\n    COUNT(miner) AS willie\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(jessie) AS libel,\n    AVG(horror) AS vessel\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(vessel) AS blaze,\n    MIN(libel) AS handle\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     peril,\n     voter,\n     nation,\n     mutiny,\n     patrol\n|> AGGREGATE\n     MAX(patrol) AS burst,\n     COUNT(patrol) AS glass,\n     AVG(patrol) AS frank\n   GROUP BY\n     peril,\n     voter,\n     nation,\n     mutiny\n|> AGGREGATE\n     SUM(frank) AS horror,\n     MAX(frank) AS jessie,\n     AVG(burst) AS rhino,\n     SUM(glass) AS clover,\n     COUNT(burst) AS custom,\n     COUNT(miner) AS willie\n|> AGGREGATE\n     SUM(jessie) AS libel,\n     AVG(horror) AS vessel\n|> AGGREGATE\n     MAX(vessel) AS blaze,\n     MIN(libel) AS handle\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"miner",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    radio,\n    plank,\n    ankle,\n    lover,\n    voice\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    radio,\n    plank,\n    ankle,\n    lover\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(ankle) AS spruce,\n    COUNT(ankle) AS hurst,\n    MIN(ankle) AS malta,\n    MAX(lover) AS lease,\n    AVG(ankle) AS probe,\n    AVG(lover) AS fusion,\n    COUNT(lover) AS fowler,\n    MAX(ankle) AS apollo,\n    MAX(waist) AS carbon,\n    SUM(lover) AS aspect\n  FROM cte_1\n  GROUP BY\n    radio,\n    plank\n),\ncte_3 AS (\n  SELECT\n    spruce,\n    hurst\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(spruce) AS tennis\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     radio,\n     plank,\n     ankle,\n     lover,\n     voice\n|> SELECT\n     radio,\n     plank,\n     ankle,\n     lover\n|> AGGREGATE\n     SUM(ankle) AS spruce,\n     COUNT(ankle) AS hurst,\n     MIN(ankle) AS malta,\n     MAX(lover) AS lease,\n     AVG(ankle) AS probe,\n     AVG(lover) AS fusion,\n     COUNT(lover) AS fowler,\n     MAX(ankle) AS apollo,\n     MAX(waist) AS carbon,\n     SUM(lover) AS aspect\n   GROUP BY\n     radio,\n     plank\n|> SELECT\n     spruce,\n     hurst\n|> AGGREGATE\n     COUNT(spruce) AS tennis\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"waist",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    parrot,\n    magic,\n    mercer,\n    mayor,\n    cereal\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    parrot,\n    magic,\n    mercer,\n    mayor,\n    cereal\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    parrot,\n    magic,\n    mercer,\n    mayor,\n    cereal\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(mercer) AS route,\n    SUM(mercer) AS stream,\n    SUM(magic) AS scent,\n    COUNT(parrot) AS smile,\n    AVG(cereal) AS remark,\n    SUM(theme) AS circus\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    route,\n    stream,\n    scent,\n    MAX(remark) AS steen\n  FROM cte_3\n  GROUP BY\n    route,\n    stream,\n    scent\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     parrot,\n     magic,\n     mercer,\n     mayor,\n     cereal\n|> SELECT\n     parrot,\n     magic,\n     mercer,\n     mayor,\n     cereal\n|> SELECT\n     parrot,\n     magic,\n     mercer,\n     mayor,\n     cereal\n|> AGGREGATE\n     AVG(mercer) AS route,\n     SUM(mercer) AS stream,\n     SUM(magic) AS scent,\n     COUNT(parrot) AS smile,\n     AVG(cereal) AS remark,\n     SUM(theme) AS circus\n|> AGGREGATE\n     MAX(remark) AS steen\n   GROUP BY\n     route,\n     stream,\n     scent\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"theme",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bureau,\n    clint,\n    office,\n    hatch,\n    title\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(hatch) AS rubble,\n    AVG(title) AS zenith,\n    SUM(title) AS wigan,\n    MIN(hatch) AS beggar\n  FROM cte_0\n  GROUP BY\n    bureau,\n    clint,\n    office\n),\ncte_2 AS (\n  SELECT\n    COUNT(beggar) AS breeze,\n    SUM(beggar) AS planet\n  FROM cte_1\n  GROUP BY\n    rubble\n),\ncte_3 AS (\n  SELECT\n    AVG(planet) AS split,\n    COUNT(planet) AS fraud,\n    MAX(hamlet) AS shelf,\n    MIN(planet) AS scheme\n  FROM cte_2\n  GROUP BY\n    breeze\n),\ncte_4 AS (\n  SELECT\n    COUNT(scheme) AS length,\n    COUNT(fraud) AS auntie\n  FROM cte_3\n  GROUP BY\n    split\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bureau,\n     clint,\n     office,\n     hatch,\n     title\n|> AGGREGATE\n     COUNT(hatch) AS rubble,\n     AVG(title) AS zenith,\n     SUM(title) AS wigan,\n     MIN(hatch) AS beggar\n   GROUP BY\n     bureau,\n     clint,\n     office\n|> AGGREGATE\n     COUNT(beggar) AS breeze,\n     SUM(beggar) AS planet\n   GROUP BY\n     rubble\n|> AGGREGATE\n     AVG(planet) AS split,\n     COUNT(planet) AS fraud,\n     MAX(hamlet) AS shelf,\n     MIN(planet) AS scheme\n   GROUP BY\n     breeze\n|> AGGREGATE\n     COUNT(scheme) AS length,\n     COUNT(fraud) AS auntie\n   GROUP BY\n     split\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"hamlet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    urine,\n    towel,\n    nurse,\n    dwarf,\n    arrow\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(dwarf) AS farmer,\n    MIN(dwarf) AS owner,\n    SUM(arrow) AS advert\n  FROM cte_0\n  GROUP BY\n    urine,\n    towel,\n    nurse\n),\ncte_2 AS (\n  SELECT\n    farmer,\n    owner,\n    advert\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(farmer) AS repeat,\n    SUM(farmer) AS insult,\n    MAX(owner) AS fritz,\n    SUM(owner) AS greek,\n    AVG(facet) AS verse,\n    SUM(advert) AS boiler,\n    COUNT(owner) AS shame\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(shame) AS pistol\n  FROM cte_3\n  GROUP BY\n    repeat,\n    insult\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     urine,\n     towel,\n     nurse,\n     dwarf,\n     arrow\n|> AGGREGATE\n     AVG(dwarf) AS farmer,\n     MIN(dwarf) AS owner,\n     SUM(arrow) AS advert\n   GROUP BY\n     urine,\n     towel,\n     nurse\n|> SELECT\n     farmer,\n     owner,\n     advert\n|> AGGREGATE\n     MAX(farmer) AS repeat,\n     SUM(farmer) AS insult,\n     MAX(owner) AS fritz,\n     SUM(owner) AS greek,\n     AVG(facet) AS verse,\n     SUM(advert) AS boiler,\n     COUNT(owner) AS shame\n|> AGGREGATE\n     MIN(shame) AS pistol\n   GROUP BY\n     repeat,\n     insult\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"facet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    primer,\n    turbo,\n    ransom,\n    shower,\n    mafia\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(mafia) AS ordeal,\n    AVG(ransom) AS trial,\n    MAX(shower) AS class,\n    MIN(shower) AS summit,\n    MAX(mafia) AS garden\n  FROM cte_0\n  GROUP BY\n    primer,\n    turbo\n),\ncte_2 AS (\n  SELECT\n    AVG(garden) AS coffee,\n    MIN(garden) AS incest\n  FROM cte_1\n  GROUP BY\n    ordeal,\n    trial,\n    class,\n    summit\n),\ncte_3 AS (\n  SELECT\n    COUNT(rarity) AS vessel,\n    AVG(incest) AS trifle\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(trifle) AS blanc,\n    AVG(trifle) AS chalk,\n    SUM(trifle) AS accord\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     primer,\n     turbo,\n     ransom,\n     shower,\n     mafia\n|> AGGREGATE\n     MIN(mafia) AS ordeal,\n     AVG(ransom) AS trial,\n     MAX(shower) AS class,\n     MIN(shower) AS summit,\n     MAX(mafia) AS garden\n   GROUP BY\n     primer,\n     turbo\n|> AGGREGATE\n     AVG(garden) AS coffee,\n     MIN(garden) AS incest\n   GROUP BY\n     ordeal,\n     trial,\n     class,\n     summit\n|> AGGREGATE\n     COUNT(rarity) AS vessel,\n     AVG(incest) AS trifle\n|> AGGREGATE\n     MIN(trifle) AS blanc,\n     AVG(trifle) AS chalk,\n     SUM(trifle) AS accord\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"rarity",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    pause,\n    volley,\n    person,\n    upturn,\n    blaze\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    pause,\n    volley,\n    person,\n    upturn,\n    blaze\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(blaze) AS dream,\n    SUM(blaze) AS victor,\n    MAX(blaze) AS splash\n  FROM cte_1\n  GROUP BY\n    pause,\n    volley,\n    person,\n    upturn\n),\ncte_3 AS (\n  SELECT\n    COUNT(victor) AS motor,\n    SUM(splash) AS freeze\n  FROM cte_2\n  GROUP BY\n    dream\n),\ncte_4 AS (\n  SELECT\n    MAX(stand) AS chaos,\n    COUNT(motor) AS jigsaw,\n    MAX(motor) AS bowel,\n    MIN(motor) AS ridge,\n    MAX(freeze) AS petrol\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     pause,\n     volley,\n     person,\n     upturn,\n     blaze\n|> SELECT\n     pause,\n     volley,\n     person,\n     upturn,\n     blaze\n|> AGGREGATE\n     MIN(blaze) AS dream,\n     SUM(blaze) AS victor,\n     MAX(blaze) AS splash\n   GROUP BY\n     pause,\n     volley,\n     person,\n     upturn\n|> AGGREGATE\n     COUNT(victor) AS motor,\n     SUM(splash) AS freeze\n   GROUP BY\n     dream\n|> AGGREGATE\n     MAX(stand) AS chaos,\n     COUNT(motor) AS jigsaw,\n     MAX(motor) AS bowel,\n     MIN(motor) AS ridge,\n     MAX(freeze) AS petrol\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"stand",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:29},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    patent,\n    litre,\n    racket,\n    debit,\n    canvas\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    patent,\n    litre,\n    racket,\n    debit,\n    canvas\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(debit) AS optic,\n    MAX(litre) AS rocket,\n    SUM(canvas) AS trader,\n    MIN(litre) AS total\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(rocket) AS review,\n    MAX(trader) AS island,\n    AVG(trader) AS smith\n  FROM cte_2\n  GROUP BY\n    optic\n),\ncte_4 AS (\n  SELECT\n    MIN(proxy) AS benny,\n    COUNT(review) AS hitch,\n    AVG(review) AS temple,\n    COUNT(smith) AS street,\n    SUM(smith) AS square\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     patent,\n     litre,\n     racket,\n     debit,\n     canvas\n|> SELECT\n     patent,\n     litre,\n     racket,\n     debit,\n     canvas\n|> AGGREGATE\n     AVG(debit) AS optic,\n     MAX(litre) AS rocket,\n     SUM(canvas) AS trader,\n     MIN(litre) AS total\n|> AGGREGATE\n     SUM(rocket) AS review,\n     MAX(trader) AS island,\n     AVG(trader) AS smith\n   GROUP BY\n     optic\n|> AGGREGATE\n     MIN(proxy) AS benny,\n     COUNT(review) AS hitch,\n     AVG(review) AS temple,\n     COUNT(smith) AS street,\n     SUM(smith) AS square\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"proxy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    energy,\n    metro,\n    thesis,\n    colon,\n    plague\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(plague) AS maiden,\n    MAX(colon) AS truth,\n    MIN(thesis) AS trophy,\n    MAX(plague) AS tiger\n  FROM cte_0\n  GROUP BY\n    energy,\n    metro\n),\ncte_2 AS (\n  SELECT\n    SUM(trophy) AS clerk,\n    SUM(tiger) AS lagoon\n  FROM cte_1\n  GROUP BY\n    maiden,\n    truth\n),\ncte_3 AS (\n  SELECT\n    clerk,\n    lagoon\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(lagoon) AS olive,\n    MIN(fossil) AS heath,\n    SUM(clerk) AS tactic,\n    MIN(clerk) AS feeder,\n    COUNT(clerk) AS county,\n    MIN(lagoon) AS apollo\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     energy,\n     metro,\n     thesis,\n     colon,\n     plague\n|> AGGREGATE\n     COUNT(plague) AS maiden,\n     MAX(colon) AS truth,\n     MIN(thesis) AS trophy,\n     MAX(plague) AS tiger\n   GROUP BY\n     energy,\n     metro\n|> AGGREGATE\n     SUM(trophy) AS clerk,\n     SUM(tiger) AS lagoon\n   GROUP BY\n     maiden,\n     truth\n|> SELECT\n     clerk,\n     lagoon\n|> AGGREGATE\n     SUM(lagoon) AS olive,\n     MIN(fossil) AS heath,\n     SUM(clerk) AS tactic,\n     MIN(clerk) AS feeder,\n     COUNT(clerk) AS county,\n     MIN(lagoon) AS apollo\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"fossil",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    pizza,\n    liquor,\n    mercy,\n    vendor,\n    fight\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(fight) AS damage,\n    AVG(mercy) AS marina\n  FROM cte_0\n  GROUP BY\n    pizza,\n    liquor\n),\ncte_2 AS (\n  SELECT\n    AVG(marina) AS voyage,\n    COUNT(marina) AS slice,\n    SUM(damage) AS toast,\n    MIN(damage) AS worker\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(voyage) AS study,\n    AVG(worker) AS quote\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(study) AS magic,\n    MAX(accent) AS tribe,\n    MIN(study) AS audit,\n    MIN(quote) AS museum,\n    AVG(study) AS paint,\n    SUM(study) AS ethos\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     pizza,\n     liquor,\n     mercy,\n     vendor,\n     fight\n|> AGGREGATE\n     COUNT(fight) AS damage,\n     AVG(mercy) AS marina\n   GROUP BY\n     pizza,\n     liquor\n|> AGGREGATE\n     AVG(marina) AS voyage,\n     COUNT(marina) AS slice,\n     SUM(damage) AS toast,\n     MIN(damage) AS worker\n|> AGGREGATE\n     COUNT(voyage) AS study,\n     AVG(worker) AS quote\n|> AGGREGATE\n     MAX(study) AS magic,\n     MAX(accent) AS tribe,\n     MIN(study) AS audit,\n     MIN(quote) AS museum,\n     AVG(study) AS paint,\n     SUM(study) AS ethos\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"accent",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    stuff,\n    chord,\n    barber,\n    bridge,\n    kitten\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    stuff,\n    chord,\n    barber\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(stuff) AS decay,\n    AVG(stuff) AS joanna\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(joanna) AS final,\n    MAX(joanna) AS xerox,\n    MIN(joanna) AS jockey,\n    COUNT(joanna) AS wharf\n  FROM cte_2\n  GROUP BY\n    decay\n),\ncte_4 AS (\n  SELECT\n    AVG(wharf) AS guard,\n    MIN(jockey) AS mickey,\n    MAX(effect) AS brazil,\n    SUM(wharf) AS fairy,\n    AVG(jockey) AS panic,\n    MIN(wharf) AS target\n  FROM cte_3\n  GROUP BY\n    final,\n    xerox\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     stuff,\n     chord,\n     barber,\n     bridge,\n     kitten\n|> SELECT\n     stuff,\n     chord,\n     barber\n|> AGGREGATE\n     MIN(stuff) AS decay,\n     AVG(stuff) AS joanna\n|> AGGREGATE\n     AVG(joanna) AS final,\n     MAX(joanna) AS xerox,\n     MIN(joanna) AS jockey,\n     COUNT(joanna) AS wharf\n   GROUP BY\n     decay\n|> AGGREGATE\n     AVG(wharf) AS guard,\n     MIN(jockey) AS mickey,\n     MAX(effect) AS brazil,\n     SUM(wharf) AS fairy,\n     AVG(jockey) AS panic,\n     MIN(wharf) AS target\n   GROUP BY\n     final,\n     xerox\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"effect",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    brent,\n    nancy,\n    snail,\n    sprint,\n    gravy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(snail) AS laird,\n    MIN(brent) AS prose,\n    MAX(nancy) AS water\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(prose) AS spiral,\n    MAX(prose) AS count,\n    MIN(prose) AS bureau,\n    COUNT(water) AS hockey,\n    COUNT(prose) AS beacon,\n    SUM(prose) AS shock,\n    MIN(water) AS pepper,\n    MIN(index) AS length\n  FROM cte_1\n  GROUP BY\n    laird\n),\ncte_3 AS (\n  SELECT\n    spiral,\n    count,\n    bureau\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(bureau) AS warmth\n  FROM cte_3\n  GROUP BY\n    spiral,\n    count\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     brent,\n     nancy,\n     snail,\n     sprint,\n     gravy\n|> AGGREGATE\n     MAX(snail) AS laird,\n     MIN(brent) AS prose,\n     MAX(nancy) AS water\n|> AGGREGATE\n     AVG(prose) AS spiral,\n     MAX(prose) AS count,\n     MIN(prose) AS bureau,\n     COUNT(water) AS hockey,\n     COUNT(prose) AS beacon,\n     SUM(prose) AS shock,\n     MIN(water) AS pepper,\n     MIN(index) AS length\n   GROUP BY\n     laird\n|> SELECT\n     spiral,\n     count,\n     bureau\n|> AGGREGATE\n     AVG(bureau) AS warmth\n   GROUP BY\n     spiral,\n     count\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"index",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:27,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    lobby,\n    heron,\n    flash,\n    sequel,\n    kitty\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    lobby,\n    heron,\n    flash,\n    sequel\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(lobby) AS scotch,\n    AVG(lobby) AS attack,\n    MIN(heron) AS warden,\n    COUNT(sequel) AS idiot\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(idiot) AS regard,\n    SUM(attack) AS litter,\n    MIN(warden) AS reggae,\n    MIN(attack) AS barnet,\n    MAX(abbot) AS estate\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    regard,\n    MIN(barnet) AS lodge,\n    SUM(barnet) AS praise,\n    MAX(barnet) AS shade\n  FROM cte_3\n  GROUP BY\n    regard\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     lobby,\n     heron,\n     flash,\n     sequel,\n     kitty\n|> SELECT\n     lobby,\n     heron,\n     flash,\n     sequel\n|> AGGREGATE\n     MAX(lobby) AS scotch,\n     AVG(lobby) AS attack,\n     MIN(heron) AS warden,\n     COUNT(sequel) AS idiot\n|> AGGREGATE\n     SUM(idiot) AS regard,\n     SUM(attack) AS litter,\n     MIN(warden) AS reggae,\n     MIN(attack) AS barnet,\n     MAX(abbot) AS estate\n|> AGGREGATE\n     MIN(barnet) AS lodge,\n     SUM(barnet) AS praise,\n     MAX(barnet) AS shade\n   GROUP BY\n     regard\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"abbot",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    oyster,\n    viking,\n    bosom,\n    mince,\n    spring\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(spring) AS repeat,\n    MIN(oyster) AS sample,\n    AVG(bosom) AS sonata,\n    SUM(spring) AS flare\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(flare) AS series,\n    MAX(repeat) AS hernia,\n    AVG(flare) AS pupil,\n    COUNT(flare) AS burton\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(pupil) AS comer,\n    SUM(hernia) AS genome,\n    SUM(lodge) AS canvas,\n    MIN(pupil) AS pearl,\n    COUNT(pupil) AS share\n  FROM cte_2\n  GROUP BY\n    series\n),\ncte_4 AS (\n  SELECT\n    comer,\n    genome,\n    pearl\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     oyster,\n     viking,\n     bosom,\n     mince,\n     spring\n|> AGGREGATE\n     COUNT(spring) AS repeat,\n     MIN(oyster) AS sample,\n     AVG(bosom) AS sonata,\n     SUM(spring) AS flare\n|> AGGREGATE\n     MIN(flare) AS series,\n     MAX(repeat) AS hernia,\n     AVG(flare) AS pupil,\n     COUNT(flare) AS burton\n|> AGGREGATE\n     SUM(pupil) AS comer,\n     SUM(hernia) AS genome,\n     SUM(lodge) AS canvas,\n     MIN(pupil) AS pearl,\n     COUNT(pupil) AS share\n   GROUP BY\n     series\n|> SELECT\n     comer,\n     genome,\n     pearl\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"lodge",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    fight,\n    birth,\n    phoebe,\n    jasper,\n    miner\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    fight,\n    birth,\n    phoebe,\n    jasper,\n    miner\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(miner) AS vanity,\n    MAX(miner) AS bidder,\n    SUM(miner) AS cassie\n  FROM cte_1\n  GROUP BY\n    fight,\n    birth\n),\ncte_3 AS (\n  SELECT\n    vanity,\n    bidder,\n    cassie\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(cassie) AS truck,\n    COUNT(cassie) AS bowel,\n    MAX(decay) AS tactic,\n    MAX(cassie) AS plant,\n    SUM(cassie) AS frenzy,\n    MIN(cassie) AS condom\n  FROM cte_3\n  GROUP BY\n    vanity,\n    bidder\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     fight,\n     birth,\n     phoebe,\n     jasper,\n     miner\n|> SELECT\n     fight,\n     birth,\n     phoebe,\n     jasper,\n     miner\n|> AGGREGATE\n     MIN(miner) AS vanity,\n     MAX(miner) AS bidder,\n     SUM(miner) AS cassie\n   GROUP BY\n     fight,\n     birth\n|> SELECT\n     vanity,\n     bidder,\n     cassie\n|> AGGREGATE\n     AVG(cassie) AS truck,\n     COUNT(cassie) AS bowel,\n     MAX(decay) AS tactic,\n     MAX(cassie) AS plant,\n     SUM(cassie) AS frenzy,\n     MIN(cassie) AS condom\n   GROUP BY\n     vanity,\n     bidder\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"decay",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:28},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    island,\n    tandem,\n    turbo,\n    staff,\n    almond\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(island) AS embryo\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(embryo) AS brunt,\n    MAX(embryo) AS lender,\n    SUM(embryo) AS broad,\n    COUNT(embryo) AS fritz\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(broad) AS grange,\n    MIN(broad) AS victim,\n    MIN(fritz) AS tallis,\n    COUNT(fritz) AS scrap,\n    COUNT(broad) AS cliff,\n    COUNT(force) AS viking\n  FROM cte_2\n  GROUP BY\n    brunt,\n    lender\n),\ncte_4 AS (\n  SELECT\n    grange,\n    MIN(cliff) AS return\n  FROM cte_3\n  GROUP BY\n    grange,\n    victim,\n    tallis\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     island,\n     tandem,\n     turbo,\n     staff,\n     almond\n|> AGGREGATE\n     SUM(island) AS embryo\n|> AGGREGATE\n     MIN(embryo) AS brunt,\n     MAX(embryo) AS lender,\n     SUM(embryo) AS broad,\n     COUNT(embryo) AS fritz\n|> AGGREGATE\n     SUM(broad) AS grange,\n     MIN(broad) AS victim,\n     MIN(fritz) AS tallis,\n     COUNT(fritz) AS scrap,\n     COUNT(broad) AS cliff,\n     COUNT(force) AS viking\n   GROUP BY\n     brunt,\n     lender\n|> AGGREGATE\n     MIN(cliff) AS return\n   GROUP BY\n     grange,\n     victim,\n     tallis\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"force",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    visit,\n    bakery,\n    bream,\n    litre,\n    hotel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(bakery) AS night,\n    AVG(visit) AS frown,\n    AVG(hotel) AS remit\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(remit) AS fisher,\n    AVG(night) AS sorrow\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(sorrow) AS glare,\n    AVG(sorrow) AS rotor,\n    COUNT(sorrow) AS effort,\n    MIN(sorrow) AS fungus,\n    MAX(sorrow) AS apple,\n    MAX(clump) AS glass\n  FROM cte_2\n  GROUP BY\n    fisher\n),\ncte_4 AS (\n  SELECT\n    glare,\n    SUM(apple) AS mason,\n    MIN(fungus) AS cable\n  FROM cte_3\n  GROUP BY\n    glare,\n    rotor\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     visit,\n     bakery,\n     bream,\n     litre,\n     hotel\n|> AGGREGATE\n     MIN(bakery) AS night,\n     AVG(visit) AS frown,\n     AVG(hotel) AS remit\n|> AGGREGATE\n     MAX(remit) AS fisher,\n     AVG(night) AS sorrow\n|> AGGREGATE\n     SUM(sorrow) AS glare,\n     AVG(sorrow) AS rotor,\n     COUNT(sorrow) AS effort,\n     MIN(sorrow) AS fungus,\n     MAX(sorrow) AS apple,\n     MAX(clump) AS glass\n   GROUP BY\n     fisher\n|> AGGREGATE\n     SUM(apple) AS mason,\n     MIN(fungus) AS cable\n   GROUP BY\n     glare,\n     rotor\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"clump",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    peach,\n    herald,\n    vessel,\n    siren,\n    flare\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    peach,\n    herald,\n    vessel,\n    siren,\n    flare\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    peach,\n    herald,\n    vessel,\n    siren,\n    flare\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(vessel) AS lever,\n    MAX(peach) AS forth,\n    COUNT(vessel) AS advice,\n    SUM(flare) AS mercer,\n    MAX(herald) AS screw,\n    MAX(parent) AS occult\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(screw) AS brett,\n    AVG(advice) AS range,\n    COUNT(lever) AS singer,\n    AVG(lever) AS litre\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     peach,\n     herald,\n     vessel,\n     siren,\n     flare\n|> SELECT\n     peach,\n     herald,\n     vessel,\n     siren,\n     flare\n|> SELECT\n     peach,\n     herald,\n     vessel,\n     siren,\n     flare\n|> AGGREGATE\n     SUM(vessel) AS lever,\n     MAX(peach) AS forth,\n     COUNT(vessel) AS advice,\n     SUM(flare) AS mercer,\n     MAX(herald) AS screw,\n     MAX(parent) AS occult\n|> AGGREGATE\n     COUNT(screw) AS brett,\n     AVG(advice) AS range,\n     COUNT(lever) AS singer,\n     AVG(lever) AS litre\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"parent",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    chill,\n    client,\n    farce,\n    apathy,\n    impact\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(impact) AS thread,\n    MIN(apathy) AS ryder,\n    AVG(apathy) AS coast\n  FROM cte_0\n  GROUP BY\n    chill,\n    client,\n    farce\n),\ncte_2 AS (\n  SELECT\n    MIN(ryder) AS myriad\n  FROM cte_1\n  GROUP BY\n    thread\n),\ncte_3 AS (\n  SELECT\n    COUNT(myriad) AS grease,\n    MIN(myriad) AS dolly,\n    MAX(myriad) AS author,\n    AVG(myriad) AS hammer,\n    SUM(maple) AS factor,\n    SUM(myriad) AS trench\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(trench) AS ethic,\n    AVG(hammer) AS donor,\n    MAX(author) AS blend\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     chill,\n     client,\n     farce,\n     apathy,\n     impact\n|> AGGREGATE\n     AVG(impact) AS thread,\n     MIN(apathy) AS ryder,\n     AVG(apathy) AS coast\n   GROUP BY\n     chill,\n     client,\n     farce\n|> AGGREGATE\n     MIN(ryder) AS myriad\n   GROUP BY\n     thread\n|> AGGREGATE\n     COUNT(myriad) AS grease,\n     MIN(myriad) AS dolly,\n     MAX(myriad) AS author,\n     AVG(myriad) AS hammer,\n     SUM(maple) AS factor,\n     SUM(myriad) AS trench\n|> AGGREGATE\n     MIN(trench) AS ethic,\n     AVG(hammer) AS donor,\n     MAX(author) AS blend\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"maple",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    entity,\n    gable,\n    exodus,\n    object,\n    folly\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(folly) AS burden,\n    SUM(folly) AS thread,\n    COUNT(folly) AS smear,\n    AVG(folly) AS talent\n  FROM cte_0\n  GROUP BY\n    entity,\n    gable,\n    exodus,\n    object\n),\ncte_2 AS (\n  SELECT\n    SUM(talent) AS tissue,\n    MAX(talent) AS warner,\n    MAX(thread) AS human,\n    COUNT(thread) AS exile,\n    SUM(anthem) AS fraud\n  FROM cte_1\n  GROUP BY\n    burden\n),\ncte_3 AS (\n  SELECT\n    tissue,\n    MIN(human) AS youth\n  FROM cte_2\n  GROUP BY\n    tissue\n),\ncte_4 AS (\n  SELECT\n    AVG(youth) AS thorn\n  FROM cte_3\n  GROUP BY\n    tissue\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     entity,\n     gable,\n     exodus,\n     object,\n     folly\n|> AGGREGATE\n     MIN(folly) AS burden,\n     SUM(folly) AS thread,\n     COUNT(folly) AS smear,\n     AVG(folly) AS talent\n   GROUP BY\n     entity,\n     gable,\n     exodus,\n     object\n|> AGGREGATE\n     SUM(talent) AS tissue,\n     MAX(talent) AS warner,\n     MAX(thread) AS human,\n     COUNT(thread) AS exile,\n     SUM(anthem) AS fraud\n   GROUP BY\n     burden\n|> AGGREGATE\n     MIN(human) AS youth\n   GROUP BY\n     tissue\n|> AGGREGATE\n     AVG(youth) AS thorn\n   GROUP BY\n     tissue\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"anthem",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    parent,\n    wheel,\n    metro,\n    michel,\n    bumper\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    parent,\n    wheel,\n    metro,\n    michel\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    parent,\n    wheel\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(parent) AS excess,\n    SUM(wheel) AS animal\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(animal) AS recess,\n    MIN(animal) AS sister,\n    AVG(excess) AS daily,\n    COUNT(excess) AS cursor,\n    MIN(excess) AS lemon,\n    SUM(influx) AS exodus,\n    SUM(excess) AS answer,\n    AVG(animal) AS reform,\n    SUM(animal) AS parry,\n    COUNT(animal) AS header\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     parent,\n     wheel,\n     metro,\n     michel,\n     bumper\n|> SELECT\n     parent,\n     wheel,\n     metro,\n     michel\n|> SELECT\n     parent,\n     wheel\n|> AGGREGATE\n     MAX(parent) AS excess,\n     SUM(wheel) AS animal\n|> AGGREGATE\n     MAX(animal) AS recess,\n     MIN(animal) AS sister,\n     AVG(excess) AS daily,\n     COUNT(excess) AS cursor,\n     MIN(excess) AS lemon,\n     SUM(influx) AS exodus,\n     SUM(excess) AS answer,\n     AVG(animal) AS reform,\n     SUM(animal) AS parry,\n     COUNT(animal) AS header\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"influx",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    goose,\n    hooker,\n    profit,\n    damage,\n    brass\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    goose,\n    hooker,\n    profit,\n    damage\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(profit) AS animal,\n    SUM(hooker) AS crime,\n    COUNT(profit) AS glare\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(animal) AS scrub,\n    MAX(crime) AS creole,\n    MIN(crime) AS olive,\n    COUNT(animal) AS simon,\n    SUM(crime) AS senate,\n    MIN(straw) AS spate\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    scrub,\n    creole,\n    AVG(senate) AS hogan\n  FROM cte_3\n  GROUP BY\n    scrub,\n    creole,\n    olive,\n    simon\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     goose,\n     hooker,\n     profit,\n     damage,\n     brass\n|> SELECT\n     goose,\n     hooker,\n     profit,\n     damage\n|> AGGREGATE\n     MAX(profit) AS animal,\n     SUM(hooker) AS crime,\n     COUNT(profit) AS glare\n|> AGGREGATE\n     MAX(animal) AS scrub,\n     MAX(crime) AS creole,\n     MIN(crime) AS olive,\n     COUNT(animal) AS simon,\n     SUM(crime) AS senate,\n     MIN(straw) AS spate\n|> AGGREGATE\n     AVG(senate) AS hogan\n   GROUP BY\n     scrub,\n     creole,\n     olive,\n     simon\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"straw",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hatch,\n    altar,\n    barnet,\n    prompt,\n    weber\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(prompt) AS drake,\n    MAX(barnet) AS spring,\n    MAX(prompt) AS spate,\n    MAX(altar) AS trough,\n    AVG(weber) AS birch,\n    MAX(weber) AS energy\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    drake,\n    spring,\n    spate,\n    trough\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(drake) AS brand,\n    MIN(trough) AS target,\n    COUNT(irene) AS short,\n    COUNT(trough) AS colon,\n    MIN(drake) AS frost\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(frost) AS bobby\n  FROM cte_3\n  GROUP BY\n    brand,\n    target\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hatch,\n     altar,\n     barnet,\n     prompt,\n     weber\n|> AGGREGATE\n     SUM(prompt) AS drake,\n     MAX(barnet) AS spring,\n     MAX(prompt) AS spate,\n     MAX(altar) AS trough,\n     AVG(weber) AS birch,\n     MAX(weber) AS energy\n|> SELECT\n     drake,\n     spring,\n     spate,\n     trough\n|> AGGREGATE\n     SUM(drake) AS brand,\n     MIN(trough) AS target,\n     COUNT(irene) AS short,\n     COUNT(trough) AS colon,\n     MIN(drake) AS frost\n|> AGGREGATE\n     MIN(frost) AS bobby\n   GROUP BY\n     brand,\n     target\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"irene",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    troop,\n    mummy,\n    quarry,\n    hotel,\n    minus\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(minus) AS vigil,\n    SUM(quarry) AS insert,\n    COUNT(minus) AS total,\n    AVG(minus) AS rebate\n  FROM cte_0\n  GROUP BY\n    troop,\n    mummy\n),\ncte_2 AS (\n  SELECT\n    vigil,\n    insert,\n    total,\n    rebate\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(rebate) AS relish,\n    SUM(rebate) AS volley,\n    AVG(total) AS hostel,\n    MAX(lawson) AS marble\n  FROM cte_2\n  GROUP BY\n    vigil,\n    insert\n),\ncte_4 AS (\n  SELECT\n    AVG(hostel) AS rarity,\n    COUNT(hostel) AS shadow\n  FROM cte_3\n  GROUP BY\n    relish,\n    volley\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     troop,\n     mummy,\n     quarry,\n     hotel,\n     minus\n|> AGGREGATE\n     MIN(minus) AS vigil,\n     SUM(quarry) AS insert,\n     COUNT(minus) AS total,\n     AVG(minus) AS rebate\n   GROUP BY\n     troop,\n     mummy\n|> SELECT\n     vigil,\n     insert,\n     total,\n     rebate\n|> AGGREGATE\n     AVG(rebate) AS relish,\n     SUM(rebate) AS volley,\n     AVG(total) AS hostel,\n     MAX(lawson) AS marble\n   GROUP BY\n     vigil,\n     insert\n|> AGGREGATE\n     AVG(hostel) AS rarity,\n     COUNT(hostel) AS shadow\n   GROUP BY\n     relish,\n     volley\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"lawson",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    agency,\n    cream,\n    member,\n    check,\n    carol\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(agency) AS worker,\n    AVG(check) AS basic,\n    COUNT(carol) AS sonnet,\n    MIN(cream) AS unrest\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    worker,\n    basic,\n    sonnet,\n    unrest\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(sonnet) AS satin,\n    COUNT(sonnet) AS deputy,\n    COUNT(unrest) AS while,\n    AVG(unrest) AS reward,\n    SUM(metre) AS entry,\n    MIN(sonnet) AS click,\n    MIN(unrest) AS weight\n  FROM cte_2\n  GROUP BY\n    worker,\n    basic\n),\ncte_4 AS (\n  SELECT\n    satin,\n    deputy\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     agency,\n     cream,\n     member,\n     check,\n     carol\n|> AGGREGATE\n     COUNT(agency) AS worker,\n     AVG(check) AS basic,\n     COUNT(carol) AS sonnet,\n     MIN(cream) AS unrest\n|> SELECT\n     worker,\n     basic,\n     sonnet,\n     unrest\n|> AGGREGATE\n     SUM(sonnet) AS satin,\n     COUNT(sonnet) AS deputy,\n     COUNT(unrest) AS while,\n     AVG(unrest) AS reward,\n     SUM(metre) AS entry,\n     MIN(sonnet) AS click,\n     MIN(unrest) AS weight\n   GROUP BY\n     worker,\n     basic\n|> SELECT\n     satin,\n     deputy\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"metre",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bidder,\n    remit,\n    acorn,\n    spring,\n    galley\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(galley) AS claire,\n    AVG(galley) AS focus,\n    SUM(acorn) AS robin\n  FROM cte_0\n  GROUP BY\n    bidder,\n    remit\n),\ncte_2 AS (\n  SELECT\n    COUNT(robin) AS suite,\n    SUM(robin) AS condom\n  FROM cte_1\n  GROUP BY\n    claire,\n    focus\n),\ncte_3 AS (\n  SELECT\n    SUM(condom) AS worry,\n    SUM(suite) AS taboo,\n    MIN(suite) AS youth,\n    SUM(winger) AS cruise,\n    MIN(condom) AS budget,\n    AVG(condom) AS brand,\n    COUNT(suite) AS roach\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    worry,\n    taboo\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bidder,\n     remit,\n     acorn,\n     spring,\n     galley\n|> AGGREGATE\n     MIN(galley) AS claire,\n     AVG(galley) AS focus,\n     SUM(acorn) AS robin\n   GROUP BY\n     bidder,\n     remit\n|> AGGREGATE\n     COUNT(robin) AS suite,\n     SUM(robin) AS condom\n   GROUP BY\n     claire,\n     focus\n|> AGGREGATE\n     SUM(condom) AS worry,\n     SUM(suite) AS taboo,\n     MIN(suite) AS youth,\n     SUM(winger) AS cruise,\n     MIN(condom) AS budget,\n     AVG(condom) AS brand,\n     COUNT(suite) AS roach\n|> SELECT\n     worry,\n     taboo\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"winger",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rugby,\n    orient,\n    flank,\n    pulpit,\n    range\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(range) AS venom\n  FROM cte_0\n  GROUP BY\n    rugby,\n    orient,\n    flank,\n    pulpit\n),\ncte_2 AS (\n  SELECT\n    SUM(venom) AS swift,\n    MAX(venom) AS nature,\n    AVG(venom) AS scope,\n    COUNT(venom) AS stain,\n    MIN(venom) AS strand\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(strand) AS stock,\n    MAX(nature) AS subset,\n    COUNT(chest) AS bucket,\n    SUM(scope) AS powder\n  FROM cte_2\n  GROUP BY\n    swift\n),\ncte_4 AS (\n  SELECT\n    stock,\n    MIN(powder) AS hooker\n  FROM cte_3\n  GROUP BY\n    stock,\n    subset\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rugby,\n     orient,\n     flank,\n     pulpit,\n     range\n|> AGGREGATE\n     SUM(range) AS venom\n   GROUP BY\n     rugby,\n     orient,\n     flank,\n     pulpit\n|> AGGREGATE\n     SUM(venom) AS swift,\n     MAX(venom) AS nature,\n     AVG(venom) AS scope,\n     COUNT(venom) AS stain,\n     MIN(venom) AS strand\n|> AGGREGATE\n     MAX(strand) AS stock,\n     MAX(nature) AS subset,\n     COUNT(chest) AS bucket,\n     SUM(scope) AS powder\n   GROUP BY\n     swift\n|> AGGREGATE\n     MIN(powder) AS hooker\n   GROUP BY\n     stock,\n     subset\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"chest",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    skill,\n    plight,\n    upland,\n    giant,\n    drive\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(drive) AS export,\n    SUM(plight) AS ethic,\n    AVG(upland) AS square\n  FROM cte_0\n  GROUP BY\n    skill\n),\ncte_2 AS (\n  SELECT\n    MIN(square) AS tummy,\n    SUM(square) AS world,\n    AVG(square) AS forum,\n    AVG(ethic) AS sequel\n  FROM cte_1\n  GROUP BY\n    export\n),\ncte_3 AS (\n  SELECT\n    COUNT(sequel) AS laurel,\n    MAX(world) AS worth,\n    MAX(virgin) AS closet,\n    SUM(sequel) AS prompt\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(prompt) AS heresy,\n    MIN(prompt) AS becker\n  FROM cte_3\n  GROUP BY\n    laurel,\n    worth\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     skill,\n     plight,\n     upland,\n     giant,\n     drive\n|> AGGREGATE\n     COUNT(drive) AS export,\n     SUM(plight) AS ethic,\n     AVG(upland) AS square\n   GROUP BY\n     skill\n|> AGGREGATE\n     MIN(square) AS tummy,\n     SUM(square) AS world,\n     AVG(square) AS forum,\n     AVG(ethic) AS sequel\n   GROUP BY\n     export\n|> AGGREGATE\n     COUNT(sequel) AS laurel,\n     MAX(world) AS worth,\n     MAX(virgin) AS closet,\n     SUM(sequel) AS prompt\n|> AGGREGATE\n     MAX(prompt) AS heresy,\n     MIN(prompt) AS becker\n   GROUP BY\n     laurel,\n     worth\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"virgin",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    prize,\n    savage,\n    gloria,\n    hangar,\n    patch\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(prize) AS widow,\n    COUNT(gloria) AS clinic\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(widow) AS spark,\n    SUM(widow) AS keeper,\n    COUNT(clinic) AS spear,\n    COUNT(widow) AS basket\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(spark) AS terry,\n    SUM(keeper) AS knock,\n    COUNT(spear) AS hatch,\n    COUNT(keeper) AS knack,\n    AVG(boyle) AS juice,\n    AVG(basket) AS curry,\n    AVG(spark) AS flour,\n    MAX(spear) AS pencil\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(knack) AS rhythm\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     prize,\n     savage,\n     gloria,\n     hangar,\n     patch\n|> AGGREGATE\n     AVG(prize) AS widow,\n     COUNT(gloria) AS clinic\n|> AGGREGATE\n     MAX(widow) AS spark,\n     SUM(widow) AS keeper,\n     COUNT(clinic) AS spear,\n     COUNT(widow) AS basket\n|> AGGREGATE\n     SUM(spark) AS terry,\n     SUM(keeper) AS knock,\n     COUNT(spear) AS hatch,\n     COUNT(keeper) AS knack,\n     AVG(boyle) AS juice,\n     AVG(basket) AS curry,\n     AVG(spark) AS flour,\n     MAX(spear) AS pencil\n|> AGGREGATE\n     AVG(knack) AS rhythm\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"boyle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    japan,\n    wally,\n    baker,\n    canal,\n    barley\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(barley) AS grasp,\n    AVG(japan) AS greed\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(greed) AS export,\n    SUM(greed) AS asthma,\n    COUNT(greed) AS smack\n  FROM cte_1\n  GROUP BY\n    grasp\n),\ncte_3 AS (\n  SELECT\n    export,\n    asthma,\n    smack\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(asthma) AS flora,\n    MIN(smack) AS uproar,\n    MAX(asthma) AS dance,\n    AVG(leader) AS shaikh,\n    SUM(asthma) AS owner,\n    SUM(export) AS rouge,\n    AVG(asthma) AS while,\n    SUM(smack) AS decor\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     japan,\n     wally,\n     baker,\n     canal,\n     barley\n|> AGGREGATE\n     MIN(barley) AS grasp,\n     AVG(japan) AS greed\n|> AGGREGATE\n     MIN(greed) AS export,\n     SUM(greed) AS asthma,\n     COUNT(greed) AS smack\n   GROUP BY\n     grasp\n|> SELECT\n     export,\n     asthma,\n     smack\n|> AGGREGATE\n     COUNT(asthma) AS flora,\n     MIN(smack) AS uproar,\n     MAX(asthma) AS dance,\n     AVG(leader) AS shaikh,\n     SUM(asthma) AS owner,\n     SUM(export) AS rouge,\n     AVG(asthma) AS while,\n     SUM(smack) AS decor\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"leader",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    style,\n    cradle,\n    elbow,\n    hardy,\n    label\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(label) AS crime,\n    MAX(cradle) AS wealth,\n    COUNT(label) AS tiger,\n    MAX(label) AS alaska\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(alaska) AS heaven,\n    COUNT(tiger) AS murmur,\n    SUM(tiger) AS arrow,\n    AVG(tiger) AS office,\n    AVG(alaska) AS smash\n  FROM cte_1\n  GROUP BY\n    crime,\n    wealth\n),\ncte_3 AS (\n  SELECT\n    AVG(fridge) AS warsaw,\n    MIN(murmur) AS monte,\n    SUM(murmur) AS devil,\n    COUNT(murmur) AS chair\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(chair) AS income\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     style,\n     cradle,\n     elbow,\n     hardy,\n     label\n|> AGGREGATE\n     AVG(label) AS crime,\n     MAX(cradle) AS wealth,\n     COUNT(label) AS tiger,\n     MAX(label) AS alaska\n|> AGGREGATE\n     MAX(alaska) AS heaven,\n     COUNT(tiger) AS murmur,\n     SUM(tiger) AS arrow,\n     AVG(tiger) AS office,\n     AVG(alaska) AS smash\n   GROUP BY\n     crime,\n     wealth\n|> AGGREGATE\n     AVG(fridge) AS warsaw,\n     MIN(murmur) AS monte,\n     SUM(murmur) AS devil,\n     COUNT(murmur) AS chair\n|> AGGREGATE\n     COUNT(chair) AS income\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"fridge",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bully,\n    onion,\n    sonata,\n    daily,\n    laird\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(laird) AS shrug,\n    SUM(laird) AS editor,\n    MIN(laird) AS melon\n  FROM cte_0\n  GROUP BY\n    bully,\n    onion,\n    sonata,\n    daily\n),\ncte_2 AS (\n  SELECT\n    MIN(melon) AS trade,\n    COUNT(melon) AS daniel\n  FROM cte_1\n  GROUP BY\n    shrug,\n    editor\n),\ncte_3 AS (\n  SELECT\n    MAX(trade) AS guitar,\n    AVG(trade) AS smile,\n    MIN(racism) AS connie,\n    COUNT(trade) AS holder,\n    SUM(daniel) AS crunch\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(guitar) AS coral,\n    MAX(holder) AS venue\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bully,\n     onion,\n     sonata,\n     daily,\n     laird\n|> AGGREGATE\n     AVG(laird) AS shrug,\n     SUM(laird) AS editor,\n     MIN(laird) AS melon\n   GROUP BY\n     bully,\n     onion,\n     sonata,\n     daily\n|> AGGREGATE\n     MIN(melon) AS trade,\n     COUNT(melon) AS daniel\n   GROUP BY\n     shrug,\n     editor\n|> AGGREGATE\n     MAX(trade) AS guitar,\n     AVG(trade) AS smile,\n     MIN(racism) AS connie,\n     COUNT(trade) AS holder,\n     SUM(daniel) AS crunch\n|> AGGREGATE\n     MIN(guitar) AS coral,\n     MAX(holder) AS venue\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"racism",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    betty,\n    wallet,\n    parade,\n    bypass,\n    flair\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    betty,\n    wallet,\n    parade,\n    bypass,\n    flair\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(bypass) AS court,\n    COUNT(flair) AS sonata,\n    AVG(flair) AS fiasco\n  FROM cte_1\n  GROUP BY\n    betty,\n    wallet,\n    parade\n),\ncte_3 AS (\n  SELECT\n    AVG(sonata) AS glare,\n    MIN(fiasco) AS limit,\n    COUNT(fiasco) AS sequel,\n    AVG(sorrow) AS factor\n  FROM cte_2\n  GROUP BY\n    court\n),\ncte_4 AS (\n  SELECT\n    MIN(sequel) AS lease,\n    MIN(limit) AS drill,\n    MAX(sequel) AS rubble\n  FROM cte_3\n  GROUP BY\n    glare\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     betty,\n     wallet,\n     parade,\n     bypass,\n     flair\n|> SELECT\n     betty,\n     wallet,\n     parade,\n     bypass,\n     flair\n|> AGGREGATE\n     AVG(bypass) AS court,\n     COUNT(flair) AS sonata,\n     AVG(flair) AS fiasco\n   GROUP BY\n     betty,\n     wallet,\n     parade\n|> AGGREGATE\n     AVG(sonata) AS glare,\n     MIN(fiasco) AS limit,\n     COUNT(fiasco) AS sequel,\n     AVG(sorrow) AS factor\n   GROUP BY\n     court\n|> AGGREGATE\n     MIN(sequel) AS lease,\n     MIN(limit) AS drill,\n     MAX(sequel) AS rubble\n   GROUP BY\n     glare\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"sorrow",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    larry,\n    lorry,\n    groove,\n    depot,\n    badge\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(badge) AS terry,\n    AVG(lorry) AS castle,\n    MAX(depot) AS spiral\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(castle) AS scene,\n    MAX(spiral) AS lesion,\n    SUM(terry) AS accord,\n    AVG(castle) AS margin,\n    AVG(terry) AS collor,\n    MAX(castle) AS champ,\n    SUM(spiral) AS ticket,\n    MIN(moran) AS drift\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    scene,\n    lesion,\n    accord,\n    margin\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(accord) AS dummy\n  FROM cte_3\n  GROUP BY\n    scene,\n    lesion\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     larry,\n     lorry,\n     groove,\n     depot,\n     badge\n|> AGGREGATE\n     SUM(badge) AS terry,\n     AVG(lorry) AS castle,\n     MAX(depot) AS spiral\n|> AGGREGATE\n     SUM(castle) AS scene,\n     MAX(spiral) AS lesion,\n     SUM(terry) AS accord,\n     AVG(castle) AS margin,\n     AVG(terry) AS collor,\n     MAX(castle) AS champ,\n     SUM(spiral) AS ticket,\n     MIN(moran) AS drift\n|> SELECT\n     scene,\n     lesion,\n     accord,\n     margin\n|> AGGREGATE\n     COUNT(accord) AS dummy\n   GROUP BY\n     scene,\n     lesion\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"moran",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:27,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    fungus,\n    tensor,\n    design,\n    scrub,\n    slate\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(slate) AS repeal,\n    SUM(scrub) AS comet,\n    COUNT(slate) AS stuff,\n    SUM(design) AS screen,\n    MIN(slate) AS miner,\n    MIN(scrub) AS reflex,\n    MAX(design) AS heaven\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    repeal,\n    comet\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(comet) AS threat,\n    MIN(repeal) AS rhino,\n    MAX(stack) AS chief,\n    MAX(repeal) AS shrine,\n    SUM(comet) AS racism,\n    COUNT(repeal) AS alloy\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    threat,\n    rhino\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     fungus,\n     tensor,\n     design,\n     scrub,\n     slate\n|> AGGREGATE\n     SUM(slate) AS repeal,\n     SUM(scrub) AS comet,\n     COUNT(slate) AS stuff,\n     SUM(design) AS screen,\n     MIN(slate) AS miner,\n     MIN(scrub) AS reflex,\n     MAX(design) AS heaven\n|> SELECT\n     repeal,\n     comet\n|> AGGREGATE\n     COUNT(comet) AS threat,\n     MIN(repeal) AS rhino,\n     MAX(stack) AS chief,\n     MAX(repeal) AS shrine,\n     SUM(comet) AS racism,\n     COUNT(repeal) AS alloy\n|> SELECT\n     threat,\n     rhino\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"stack",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    chest,\n    stable,\n    newco,\n    martyr,\n    blame\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(blame) AS slice,\n    COUNT(blame) AS syntax\n  FROM cte_0\n  GROUP BY\n    chest,\n    stable,\n    newco,\n    martyr\n),\ncte_2 AS (\n  SELECT\n    COUNT(slice) AS reflux\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(reflux) AS guide,\n    MIN(reflux) AS groan,\n    SUM(reflux) AS gedge,\n    COUNT(reflux) AS caesar,\n    MAX(reflux) AS track,\n    AVG(slang) AS folder\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    guide,\n    groan,\n    COUNT(gedge) AS drink,\n    SUM(gedge) AS voyage\n  FROM cte_3\n  GROUP BY\n    guide,\n    groan\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     chest,\n     stable,\n     newco,\n     martyr,\n     blame\n|> AGGREGATE\n     AVG(blame) AS slice,\n     COUNT(blame) AS syntax\n   GROUP BY\n     chest,\n     stable,\n     newco,\n     martyr\n|> AGGREGATE\n     COUNT(slice) AS reflux\n|> AGGREGATE\n     AVG(reflux) AS guide,\n     MIN(reflux) AS groan,\n     SUM(reflux) AS gedge,\n     COUNT(reflux) AS caesar,\n     MAX(reflux) AS track,\n     AVG(slang) AS folder\n|> AGGREGATE\n     COUNT(gedge) AS drink,\n     SUM(gedge) AS voyage\n   GROUP BY\n     guide,\n     groan\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"slang",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tensor,\n    claim,\n    rover,\n    infant,\n    gallon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(rover) AS muddle,\n    AVG(infant) AS palace,\n    MAX(rover) AS tablet,\n    COUNT(infant) AS shaikh,\n    MIN(infant) AS hybrid,\n    MAX(gallon) AS accent,\n    MIN(gallon) AS myriad,\n    SUM(rover) AS fusion,\n    MAX(infant) AS skull\n  FROM cte_0\n  GROUP BY\n    tensor,\n    claim\n),\ncte_2 AS (\n  SELECT\n    AVG(pepper) AS diesel,\n    MIN(muddle) AS kitten,\n    MAX(accent) AS inside\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    kitten,\n    inside\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(inside) AS cutter\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tensor,\n     claim,\n     rover,\n     infant,\n     gallon\n|> AGGREGATE\n     COUNT(rover) AS muddle,\n     AVG(infant) AS palace,\n     MAX(rover) AS tablet,\n     COUNT(infant) AS shaikh,\n     MIN(infant) AS hybrid,\n     MAX(gallon) AS accent,\n     MIN(gallon) AS myriad,\n     SUM(rover) AS fusion,\n     MAX(infant) AS skull\n   GROUP BY\n     tensor,\n     claim\n|> AGGREGATE\n     AVG(pepper) AS diesel,\n     MIN(muddle) AS kitten,\n     MAX(accent) AS inside\n|> SELECT\n     kitten,\n     inside\n|> AGGREGATE\n     MAX(inside) AS cutter\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"pepper",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    spider,\n    sunset,\n    scrum,\n    remand,\n    diary\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(diary) AS copper,\n    SUM(diary) AS export\n  FROM cte_0\n  GROUP BY\n    spider,\n    sunset,\n    scrum,\n    remand\n),\ncte_2 AS (\n  SELECT\n    COUNT(copper) AS senate,\n    SUM(export) AS sting,\n    SUM(copper) AS gospel,\n    MIN(export) AS penis\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MIN(penis) AS click,\n    COUNT(sting) AS norma,\n    MAX(clown) AS brett,\n    MAX(gospel) AS millie,\n    MAX(penis) AS farmer\n  FROM cte_2\n  GROUP BY\n    senate\n),\ncte_4 AS (\n  SELECT\n    MIN(norma) AS alert\n  FROM cte_3\n  GROUP BY\n    click\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     spider,\n     sunset,\n     scrum,\n     remand,\n     diary\n|> AGGREGATE\n     MIN(diary) AS copper,\n     SUM(diary) AS export\n   GROUP BY\n     spider,\n     sunset,\n     scrum,\n     remand\n|> AGGREGATE\n     COUNT(copper) AS senate,\n     SUM(export) AS sting,\n     SUM(copper) AS gospel,\n     MIN(export) AS penis\n|> AGGREGATE\n     MIN(penis) AS click,\n     COUNT(sting) AS norma,\n     MAX(clown) AS brett,\n     MAX(gospel) AS millie,\n     MAX(penis) AS farmer\n   GROUP BY\n     senate\n|> AGGREGATE\n     MIN(norma) AS alert\n   GROUP BY\n     click\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"clown",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    grand,\n    movie,\n    gunman,\n    jacket,\n    cherry\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    grand,\n    movie,\n    gunman\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(movie) AS tangle,\n    MIN(gunman) AS merger,\n    SUM(gunman) AS vessel,\n    MIN(movie) AS topaz\n  FROM cte_1\n  GROUP BY\n    grand\n),\ncte_3 AS (\n  SELECT\n    MAX(topaz) AS night,\n    COUNT(topaz) AS casino,\n    SUM(merger) AS value,\n    COUNT(merger) AS gravy,\n    AVG(breath) AS trauma\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    night,\n    casino,\n    value,\n    COUNT(gravy) AS trough\n  FROM cte_3\n  GROUP BY\n    night,\n    casino,\n    value\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     grand,\n     movie,\n     gunman,\n     jacket,\n     cherry\n|> SELECT\n     grand,\n     movie,\n     gunman\n|> AGGREGATE\n     MAX(movie) AS tangle,\n     MIN(gunman) AS merger,\n     SUM(gunman) AS vessel,\n     MIN(movie) AS topaz\n   GROUP BY\n     grand\n|> AGGREGATE\n     MAX(topaz) AS night,\n     COUNT(topaz) AS casino,\n     SUM(merger) AS value,\n     COUNT(merger) AS gravy,\n     AVG(breath) AS trauma\n|> AGGREGATE\n     COUNT(gravy) AS trough\n   GROUP BY\n     night,\n     casino,\n     value\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"breath",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    malice,\n    theory,\n    tunnel,\n    lotus,\n    shine\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(shine) AS heyday,\n    COUNT(malice) AS return,\n    MAX(malice) AS covent,\n    MIN(shine) AS glover,\n    MIN(theory) AS cinema\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(return) AS warden,\n    AVG(covent) AS shrimp,\n    COUNT(glover) AS folly,\n    COUNT(covent) AS clutch,\n    MIN(cinema) AS gould,\n    COUNT(galley) AS fritz,\n    MAX(cinema) AS sweet\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    warden,\n    shrimp,\n    folly\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    warden,\n    shrimp,\n    folly\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     malice,\n     theory,\n     tunnel,\n     lotus,\n     shine\n|> AGGREGATE\n     MAX(shine) AS heyday,\n     COUNT(malice) AS return,\n     MAX(malice) AS covent,\n     MIN(shine) AS glover,\n     MIN(theory) AS cinema\n|> AGGREGATE\n     MIN(return) AS warden,\n     AVG(covent) AS shrimp,\n     COUNT(glover) AS folly,\n     COUNT(covent) AS clutch,\n     MIN(cinema) AS gould,\n     COUNT(galley) AS fritz,\n     MAX(cinema) AS sweet\n|> SELECT\n     warden,\n     shrimp,\n     folly\n|> SELECT\n     warden,\n     shrimp,\n     folly\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"galley",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:27,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    track,\n    debtor,\n    terror,\n    bonus,\n    magic\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(debtor) AS tempo,\n    COUNT(magic) AS share,\n    SUM(debtor) AS island,\n    SUM(bonus) AS flour,\n    MAX(track) AS trauma,\n    SUM(terror) AS ferry\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(flour) AS prison,\n    SUM(trauma) AS fibre\n  FROM cte_1\n  GROUP BY\n    tempo,\n    share\n),\ncte_3 AS (\n  SELECT\n    AVG(fibre) AS winner,\n    MIN(black) AS celery,\n    SUM(fibre) AS sauce,\n    MIN(prison) AS mutant,\n    MAX(fibre) AS teapot\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(winner) AS potato\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     track,\n     debtor,\n     terror,\n     bonus,\n     magic\n|> AGGREGATE\n     MAX(debtor) AS tempo,\n     COUNT(magic) AS share,\n     SUM(debtor) AS island,\n     SUM(bonus) AS flour,\n     MAX(track) AS trauma,\n     SUM(terror) AS ferry\n|> AGGREGATE\n     SUM(flour) AS prison,\n     SUM(trauma) AS fibre\n   GROUP BY\n     tempo,\n     share\n|> AGGREGATE\n     AVG(fibre) AS winner,\n     MIN(black) AS celery,\n     SUM(fibre) AS sauce,\n     MIN(prison) AS mutant,\n     MAX(fibre) AS teapot\n|> AGGREGATE\n     SUM(winner) AS potato\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"black",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    viewer,\n    finch,\n    reflex,\n    barker,\n    return\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    viewer,\n    finch\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(finch) AS quartz,\n    AVG(finch) AS sorrow,\n    COUNT(finch) AS refund,\n    MIN(finch) AS claire,\n    MAX(finch) AS maggot\n  FROM cte_1\n  GROUP BY\n    viewer\n),\ncte_3 AS (\n  SELECT\n    quartz,\n    sorrow,\n    refund\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(quartz) AS jasper,\n    SUM(sorrow) AS torque,\n    AVG(rogue) AS beauty,\n    AVG(quartz) AS burke,\n    MAX(quartz) AS convoy,\n    AVG(refund) AS coast,\n    AVG(sorrow) AS chain\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     viewer,\n     finch,\n     reflex,\n     barker,\n     return\n|> SELECT\n     viewer,\n     finch\n|> AGGREGATE\n     SUM(finch) AS quartz,\n     AVG(finch) AS sorrow,\n     COUNT(finch) AS refund,\n     MIN(finch) AS claire,\n     MAX(finch) AS maggot\n   GROUP BY\n     viewer\n|> SELECT\n     quartz,\n     sorrow,\n     refund\n|> AGGREGATE\n     COUNT(quartz) AS jasper,\n     SUM(sorrow) AS torque,\n     AVG(rogue) AS beauty,\n     AVG(quartz) AS burke,\n     MAX(quartz) AS convoy,\n     AVG(refund) AS coast,\n     AVG(sorrow) AS chain\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"rogue",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    salon,\n    gloom,\n    sexism,\n    groin,\n    flesh\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(groin) AS nurse,\n    MAX(sexism) AS tender,\n    MAX(groin) AS sheikh,\n    MIN(salon) AS primer\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(sheikh) AS digit,\n    SUM(tender) AS alert,\n    SUM(sheikh) AS olive,\n    MIN(nurse) AS campus,\n    MAX(sheikh) AS bottom\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MIN(olive) AS reason,\n    SUM(clone) AS diesel,\n    MAX(digit) AS recall\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(recall) AS pollen,\n    AVG(recall) AS tangle,\n    MAX(recall) AS gable\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     salon,\n     gloom,\n     sexism,\n     groin,\n     flesh\n|> AGGREGATE\n     COUNT(groin) AS nurse,\n     MAX(sexism) AS tender,\n     MAX(groin) AS sheikh,\n     MIN(salon) AS primer\n|> AGGREGATE\n     COUNT(sheikh) AS digit,\n     SUM(tender) AS alert,\n     SUM(sheikh) AS olive,\n     MIN(nurse) AS campus,\n     MAX(sheikh) AS bottom\n|> AGGREGATE\n     MIN(olive) AS reason,\n     SUM(clone) AS diesel,\n     MAX(digit) AS recall\n|> AGGREGATE\n     MIN(recall) AS pollen,\n     AVG(recall) AS tangle,\n     MAX(recall) AS gable\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"clone",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    boxer,\n    storey,\n    verse,\n    pastor,\n    flurry\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(pastor) AS chuck,\n    MIN(verse) AS bullet,\n    SUM(flurry) AS glider,\n    MAX(pastor) AS jessie\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    chuck,\n    bullet\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MIN(chuck) AS search,\n    SUM(bullet) AS drink,\n    COUNT(bullet) AS brunt,\n    MAX(bullet) AS lance,\n    SUM(chuck) AS fungus,\n    SUM(wallet) AS stein,\n    AVG(bullet) AS chair,\n    AVG(chuck) AS hazel,\n    COUNT(chuck) AS rattle\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    search,\n    drink\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     boxer,\n     storey,\n     verse,\n     pastor,\n     flurry\n|> AGGREGATE\n     COUNT(pastor) AS chuck,\n     MIN(verse) AS bullet,\n     SUM(flurry) AS glider,\n     MAX(pastor) AS jessie\n|> SELECT\n     chuck,\n     bullet\n|> AGGREGATE\n     MIN(chuck) AS search,\n     SUM(bullet) AS drink,\n     COUNT(bullet) AS brunt,\n     MAX(bullet) AS lance,\n     SUM(chuck) AS fungus,\n     SUM(wallet) AS stein,\n     AVG(bullet) AS chair,\n     AVG(chuck) AS hazel,\n     COUNT(chuck) AS rattle\n|> SELECT\n     search,\n     drink\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"wallet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    clutch,\n    nancy,\n    launch,\n    midday,\n    stare\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(midday) AS whale,\n    AVG(stare) AS belly,\n    MIN(midday) AS herald,\n    SUM(stare) AS pickup\n  FROM cte_0\n  GROUP BY\n    clutch,\n    nancy,\n    launch\n),\ncte_2 AS (\n  SELECT\n    MIN(belly) AS pearl,\n    AVG(pickup) AS friar,\n    MAX(belly) AS turkey\n  FROM cte_1\n  GROUP BY\n    whale\n),\ncte_3 AS (\n  SELECT\n    MIN(pearl) AS letter,\n    SUM(folly) AS issue,\n    MAX(friar) AS alkali,\n    MIN(turkey) AS spear,\n    COUNT(turkey) AS skill\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(letter) AS darwin\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     clutch,\n     nancy,\n     launch,\n     midday,\n     stare\n|> AGGREGATE\n     MAX(midday) AS whale,\n     AVG(stare) AS belly,\n     MIN(midday) AS herald,\n     SUM(stare) AS pickup\n   GROUP BY\n     clutch,\n     nancy,\n     launch\n|> AGGREGATE\n     MIN(belly) AS pearl,\n     AVG(pickup) AS friar,\n     MAX(belly) AS turkey\n   GROUP BY\n     whale\n|> AGGREGATE\n     MIN(pearl) AS letter,\n     SUM(folly) AS issue,\n     MAX(friar) AS alkali,\n     MIN(turkey) AS spear,\n     COUNT(turkey) AS skill\n|> AGGREGATE\n     COUNT(letter) AS darwin\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"folly",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    youth,\n    gunman,\n    insult,\n    plaque,\n    limit\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(limit) AS taylor\n  FROM cte_0\n  GROUP BY\n    youth,\n    gunman,\n    insult,\n    plaque\n),\ncte_2 AS (\n  SELECT\n    MIN(taylor) AS synod,\n    COUNT(taylor) AS willow\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(willow) AS ballet,\n    MIN(synod) AS studio,\n    SUM(willow) AS client,\n    MAX(willow) AS leone,\n    MIN(willow) AS rhino,\n    MAX(vogue) AS behalf,\n    AVG(synod) AS asthma,\n    MAX(synod) AS clinic\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(client) AS hooper,\n    AVG(asthma) AS scarf\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     youth,\n     gunman,\n     insult,\n     plaque,\n     limit\n|> AGGREGATE\n     COUNT(limit) AS taylor\n   GROUP BY\n     youth,\n     gunman,\n     insult,\n     plaque\n|> AGGREGATE\n     MIN(taylor) AS synod,\n     COUNT(taylor) AS willow\n|> AGGREGATE\n     AVG(willow) AS ballet,\n     MIN(synod) AS studio,\n     SUM(willow) AS client,\n     MAX(willow) AS leone,\n     MIN(willow) AS rhino,\n     MAX(vogue) AS behalf,\n     AVG(synod) AS asthma,\n     MAX(synod) AS clinic\n|> AGGREGATE\n     COUNT(client) AS hooper,\n     AVG(asthma) AS scarf\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"vogue",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    shawl,\n    coffee,\n    logan,\n    strife,\n    notice\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    shawl,\n    coffee,\n    logan,\n    strife\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(logan) AS wisdom,\n    MIN(strife) AS nether,\n    MAX(strife) AS benny,\n    AVG(strife) AS design,\n    AVG(logan) AS phoebe\n  FROM cte_1\n  GROUP BY\n    shawl,\n    coffee\n),\ncte_3 AS (\n  SELECT\n    COUNT(design) AS jessie,\n    SUM(nether) AS rector,\n    AVG(clough) AS quota,\n    SUM(phoebe) AS gunman\n  FROM cte_2\n  GROUP BY\n    wisdom\n),\ncte_4 AS (\n  SELECT\n    AVG(gunman) AS depot,\n    MAX(rector) AS heresy\n  FROM cte_3\n  GROUP BY\n    jessie\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     shawl,\n     coffee,\n     logan,\n     strife,\n     notice\n|> SELECT\n     shawl,\n     coffee,\n     logan,\n     strife\n|> AGGREGATE\n     SUM(logan) AS wisdom,\n     MIN(strife) AS nether,\n     MAX(strife) AS benny,\n     AVG(strife) AS design,\n     AVG(logan) AS phoebe\n   GROUP BY\n     shawl,\n     coffee\n|> AGGREGATE\n     COUNT(design) AS jessie,\n     SUM(nether) AS rector,\n     AVG(clough) AS quota,\n     SUM(phoebe) AS gunman\n   GROUP BY\n     wisdom\n|> AGGREGATE\n     AVG(gunman) AS depot,\n     MAX(rector) AS heresy\n   GROUP BY\n     jessie\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:35,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"AGG_UNKNOWN",totalInformationDifference:0,unknownName:"clough",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    turbo,\n    trauma,\n    guilt,\n    force,\n    satan\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    turbo,\n    trauma,\n    MIN(force) AS result,\n    COUNT(satan) AS bronze\n  FROM cte_0\n  GROUP BY\n    turbo,\n    trauma\n),\ncte_2 AS (\n  SELECT\n    MIN(trauma) AS zenith,\n    COUNT(bronze) AS patio,\n    MIN(bronze) AS canary\n  FROM cte_1\n  GROUP BY\n    turbo\n),\ncte_3 AS (\n  SELECT\n    zenith,\n    patio,\n    canary\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    zenith,\n    patio,\n    COUNT(clough) AS squash,\n    COUNT(canary) AS black,\n    SUM(canary) AS touch,\n    AVG(canary) AS brink\n  FROM cte_3\n  GROUP BY\n    zenith,\n    patio\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     turbo,\n     trauma,\n     guilt,\n     force,\n     satan\n|> AGGREGATE\n     MIN(force) AS result,\n     COUNT(satan) AS bronze\n   GROUP BY\n     turbo,\n     trauma\n|> AGGREGATE\n     MIN(trauma) AS zenith,\n     COUNT(bronze) AS patio,\n     MIN(bronze) AS canary\n   GROUP BY\n     turbo\n|> SELECT\n     zenith,\n     patio,\n     canary\n|> AGGREGATE\n     COUNT(clough) AS squash,\n     COUNT(canary) AS black,\n     SUM(canary) AS touch,\n     AVG(canary) AS brink\n   GROUP BY\n     zenith,\n     patio\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"clough",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    price,\n    linen,\n    kelly,\n    steam,\n    memory\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(linen) AS fight,\n    MIN(memory) AS symbol\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    fight,\n    MIN(symbol) AS ulcer,\n    SUM(symbol) AS flint,\n    MAX(symbol) AS lodge\n  FROM cte_1\n  GROUP BY\n    fight\n),\ncte_3 AS (\n  SELECT\n    fight,\n    ulcer,\n    flint,\n    COUNT(lodge) AS ribbon\n  FROM cte_2\n  GROUP BY\n    fight,\n    ulcer,\n    flint\n),\ncte_4 AS (\n  SELECT\n    SUM(bomber) AS spread,\n    COUNT(fight) AS fauna,\n    COUNT(ribbon) AS rector,\n    MAX(ulcer) AS utopia,\n    AVG(ribbon) AS satin\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     price,\n     linen,\n     kelly,\n     steam,\n     memory\n|> AGGREGATE\n     MIN(linen) AS fight,\n     MIN(memory) AS symbol\n|> AGGREGATE\n     MIN(symbol) AS ulcer,\n     SUM(symbol) AS flint,\n     MAX(symbol) AS lodge\n   GROUP BY\n     fight\n|> AGGREGATE\n     COUNT(lodge) AS ribbon\n   GROUP BY\n     fight,\n     ulcer,\n     flint\n|> AGGREGATE\n     SUM(bomber) AS spread,\n     COUNT(fight) AS fauna,\n     COUNT(ribbon) AS rector,\n     MAX(ulcer) AS utopia,\n     AVG(ribbon) AS satin\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"bomber",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    water,\n    wrist,\n    scene,\n    groove,\n    alaska\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    water,\n    wrist,\n    MIN(groove) AS coral,\n    MAX(scene) AS brett,\n    COUNT(groove) AS bowel\n  FROM cte_0\n  GROUP BY\n    water,\n    wrist\n),\ncte_2 AS (\n  SELECT\n    water,\n    wrist,\n    MAX(bowel) AS singer\n  FROM cte_1\n  GROUP BY\n    water,\n    wrist\n),\ncte_3 AS (\n  SELECT\n    MAX(singer) AS newton,\n    COUNT(singer) AS mungo,\n    SUM(storey) AS lagoon,\n    AVG(wrist) AS ocean,\n    COUNT(wrist) AS viola\n  FROM cte_2\n  GROUP BY\n    water\n),\ncte_4 AS (\n  SELECT\n    MAX(viola) AS sweep\n  FROM cte_3\n  GROUP BY\n    newton\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     water,\n     wrist,\n     scene,\n     groove,\n     alaska\n|> AGGREGATE\n     MIN(groove) AS coral,\n     MAX(scene) AS brett,\n     COUNT(groove) AS bowel\n   GROUP BY\n     water,\n     wrist\n|> AGGREGATE\n     MAX(bowel) AS singer\n   GROUP BY\n     water,\n     wrist\n|> AGGREGATE\n     MAX(singer) AS newton,\n     COUNT(singer) AS mungo,\n     SUM(storey) AS lagoon,\n     AVG(wrist) AS ocean,\n     COUNT(wrist) AS viola\n   GROUP BY\n     water\n|> AGGREGATE\n     MAX(viola) AS sweep\n   GROUP BY\n     newton\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"storey",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    stride,\n    assay,\n    idiom,\n    scorn,\n    drama\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    stride,\n    assay,\n    COUNT(scorn) AS curfew,\n    SUM(drama) AS desert,\n    AVG(drama) AS ghetto\n  FROM cte_0\n  GROUP BY\n    stride,\n    assay,\n    idiom\n),\ncte_2 AS (\n  SELECT\n    stride,\n    assay,\n    curfew\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    stride,\n    assay,\n    MIN(curfew) AS source,\n    AVG(curfew) AS cheek,\n    SUM(hunger) AS maize,\n    SUM(curfew) AS aspect,\n    COUNT(curfew) AS stroke\n  FROM cte_2\n  GROUP BY\n    stride,\n    assay\n),\ncte_4 AS (\n  SELECT\n    MIN(stride) AS lotus\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     stride,\n     assay,\n     idiom,\n     scorn,\n     drama\n|> AGGREGATE\n     COUNT(scorn) AS curfew,\n     SUM(drama) AS desert,\n     AVG(drama) AS ghetto\n   GROUP BY\n     stride,\n     assay,\n     idiom\n|> SELECT\n     stride,\n     assay,\n     curfew\n|> AGGREGATE\n     MIN(curfew) AS source,\n     AVG(curfew) AS cheek,\n     SUM(hunger) AS maize,\n     SUM(curfew) AS aspect,\n     COUNT(curfew) AS stroke\n   GROUP BY\n     stride,\n     assay\n|> AGGREGATE\n     MIN(stride) AS lotus\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"hunger",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    catch,\n    trust,\n    dance,\n    virus,\n    phrase\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    catch,\n    trust,\n    SUM(phrase) AS plaque\n  FROM cte_0\n  GROUP BY\n    catch,\n    trust,\n    dance,\n    virus\n),\ncte_2 AS (\n  SELECT\n    AVG(catch) AS sudden,\n    AVG(plaque) AS mutant,\n    MAX(trust) AS rugby\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    sudden,\n    mutant,\n    MIN(rugby) AS slope,\n    COUNT(rugby) AS crown,\n    SUM(runway) AS hoover\n  FROM cte_2\n  GROUP BY\n    sudden,\n    mutant\n),\ncte_4 AS (\n  SELECT\n    AVG(sudden) AS closet,\n    COUNT(mutant) AS crowd,\n    MAX(crown) AS greek\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     catch,\n     trust,\n     dance,\n     virus,\n     phrase\n|> AGGREGATE\n     SUM(phrase) AS plaque\n   GROUP BY\n     catch,\n     trust,\n     dance,\n     virus\n|> AGGREGATE\n     AVG(catch) AS sudden,\n     AVG(plaque) AS mutant,\n     MAX(trust) AS rugby\n|> AGGREGATE\n     MIN(rugby) AS slope,\n     COUNT(rugby) AS crown,\n     SUM(runway) AS hoover\n   GROUP BY\n     sudden,\n     mutant\n|> AGGREGATE\n     AVG(sudden) AS closet,\n     COUNT(mutant) AS crowd,\n     MAX(crown) AS greek\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"runway",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    forte,\n    offer,\n    depth,\n    bureau,\n    ballot\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    forte,\n    offer,\n    COUNT(bureau) AS rumour\n  FROM cte_0\n  GROUP BY\n    forte,\n    offer,\n    depth\n),\ncte_2 AS (\n  SELECT\n    forte,\n    MAX(rumour) AS frame,\n    AVG(rumour) AS prison\n  FROM cte_1\n  GROUP BY\n    forte\n),\ncte_3 AS (\n  SELECT\n    forte,\n    COUNT(prison) AS derry,\n    MAX(prison) AS devil,\n    MIN(frame) AS wealth,\n    MAX(ryder) AS record\n  FROM cte_2\n  GROUP BY\n    forte\n),\ncte_4 AS (\n  SELECT\n    forte,\n    derry,\n    COUNT(wealth) AS voice\n  FROM cte_3\n  GROUP BY\n    forte,\n    derry,\n    devil\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     forte,\n     offer,\n     depth,\n     bureau,\n     ballot\n|> AGGREGATE\n     COUNT(bureau) AS rumour\n   GROUP BY\n     forte,\n     offer,\n     depth\n|> AGGREGATE\n     MAX(rumour) AS frame,\n     AVG(rumour) AS prison\n   GROUP BY\n     forte\n|> AGGREGATE\n     COUNT(prison) AS derry,\n     MAX(prison) AS devil,\n     MIN(frame) AS wealth,\n     MAX(ryder) AS record\n   GROUP BY\n     forte\n|> AGGREGATE\n     COUNT(wealth) AS voice\n   GROUP BY\n     forte,\n     derry,\n     devil\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"ryder",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    knight,\n    grain,\n    fight,\n    wallet,\n    linen\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    knight,\n    grain,\n    fight,\n    wallet,\n    linen\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    knight,\n    grain,\n    fight,\n    AVG(wallet) AS answer\n  FROM cte_1\n  GROUP BY\n    knight,\n    grain,\n    fight\n),\ncte_3 AS (\n  SELECT\n    knight,\n    COUNT(grain) AS demise,\n    MIN(answer) AS asset,\n    SUM(answer) AS savage,\n    MAX(curry) AS fauna,\n    MAX(grain) AS henry,\n    COUNT(fight) AS doubt\n  FROM cte_2\n  GROUP BY\n    knight\n),\ncte_4 AS (\n  SELECT\n    knight,\n    demise,\n    asset\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     knight,\n     grain,\n     fight,\n     wallet,\n     linen\n|> SELECT\n     knight,\n     grain,\n     fight,\n     wallet,\n     linen\n|> AGGREGATE\n     AVG(wallet) AS answer\n   GROUP BY\n     knight,\n     grain,\n     fight\n|> AGGREGATE\n     COUNT(grain) AS demise,\n     MIN(answer) AS asset,\n     SUM(answer) AS savage,\n     MAX(curry) AS fauna,\n     MAX(grain) AS henry,\n     COUNT(fight) AS doubt\n   GROUP BY\n     knight\n|> SELECT\n     knight,\n     demise,\n     asset\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"curry",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    airbus,\n    glover,\n    vacuum,\n    hatred,\n    brooke\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    airbus,\n    glover,\n    AVG(brooke) AS filter,\n    COUNT(hatred) AS alloy\n  FROM cte_0\n  GROUP BY\n    airbus,\n    glover,\n    vacuum\n),\ncte_2 AS (\n  SELECT\n    airbus,\n    COUNT(alloy) AS dealer,\n    SUM(alloy) AS resort\n  FROM cte_1\n  GROUP BY\n    airbus,\n    glover,\n    filter\n),\ncte_3 AS (\n  SELECT\n    airbus,\n    MIN(resort) AS matron,\n    SUM(cradle) AS garage,\n    MAX(dealer) AS crisp\n  FROM cte_2\n  GROUP BY\n    airbus\n),\ncte_4 AS (\n  SELECT\n    airbus,\n    MAX(crisp) AS detail\n  FROM cte_3\n  GROUP BY\n    airbus,\n    matron\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     airbus,\n     glover,\n     vacuum,\n     hatred,\n     brooke\n|> AGGREGATE\n     AVG(brooke) AS filter,\n     COUNT(hatred) AS alloy\n   GROUP BY\n     airbus,\n     glover,\n     vacuum\n|> AGGREGATE\n     COUNT(alloy) AS dealer,\n     SUM(alloy) AS resort\n   GROUP BY\n     airbus,\n     glover,\n     filter\n|> AGGREGATE\n     MIN(resort) AS matron,\n     SUM(cradle) AS garage,\n     MAX(dealer) AS crisp\n   GROUP BY\n     airbus\n|> AGGREGATE\n     MAX(crisp) AS detail\n   GROUP BY\n     airbus,\n     matron\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"cradle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    system,\n    taboo,\n    thrush,\n    tailor,\n    radio\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    system,\n    taboo,\n    MAX(radio) AS resort,\n    SUM(tailor) AS regret\n  FROM cte_0\n  GROUP BY\n    system,\n    taboo\n),\ncte_2 AS (\n  SELECT\n    SUM(regret) AS poppy,\n    COUNT(taboo) AS rally,\n    MAX(resort) AS drake,\n    AVG(regret) AS robot\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    poppy,\n    rally,\n    COUNT(robot) AS advert,\n    MIN(foyer) AS intel,\n    SUM(robot) AS gould\n  FROM cte_2\n  GROUP BY\n    poppy,\n    rally,\n    drake\n),\ncte_4 AS (\n  SELECT\n    poppy,\n    rally,\n    advert\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     system,\n     taboo,\n     thrush,\n     tailor,\n     radio\n|> AGGREGATE\n     MAX(radio) AS resort,\n     SUM(tailor) AS regret\n   GROUP BY\n     system,\n     taboo\n|> AGGREGATE\n     SUM(regret) AS poppy,\n     COUNT(taboo) AS rally,\n     MAX(resort) AS drake,\n     AVG(regret) AS robot\n|> AGGREGATE\n     COUNT(robot) AS advert,\n     MIN(foyer) AS intel,\n     SUM(robot) AS gould\n   GROUP BY\n     poppy,\n     rally,\n     drake\n|> SELECT\n     poppy,\n     rally,\n     advert\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"foyer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    escape,\n    label,\n    heart,\n    bunny,\n    house\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    escape,\n    label,\n    heart,\n    bunny,\n    house\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    escape,\n    label,\n    heart,\n    bunny,\n    house\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    escape,\n    label,\n    heart,\n    bunny,\n    AVG(house) AS chaos,\n    COUNT(house) AS haven,\n    SUM(house) AS ginger,\n    SUM(combo) AS apple\n  FROM cte_2\n  GROUP BY\n    escape,\n    label,\n    heart,\n    bunny\n),\ncte_4 AS (\n  SELECT\n    escape,\n    label,\n    heart,\n    bunny\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     escape,\n     label,\n     heart,\n     bunny,\n     house\n|> SELECT\n     escape,\n     label,\n     heart,\n     bunny,\n     house\n|> SELECT\n     escape,\n     label,\n     heart,\n     bunny,\n     house\n|> AGGREGATE\n     AVG(house) AS chaos,\n     COUNT(house) AS haven,\n     SUM(house) AS ginger,\n     SUM(combo) AS apple\n   GROUP BY\n     escape,\n     label,\n     heart,\n     bunny\n|> SELECT\n     escape,\n     label,\n     heart,\n     bunny\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"combo",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    attack,\n    mummy,\n    cellar,\n    brand,\n    sponge\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    attack,\n    SUM(brand) AS probe,\n    MAX(brand) AS guild\n  FROM cte_0\n  GROUP BY\n    attack,\n    mummy\n),\ncte_2 AS (\n  SELECT\n    attack,\n    MIN(guild) AS ridge,\n    AVG(guild) AS pillar\n  FROM cte_1\n  GROUP BY\n    attack,\n    probe\n),\ncte_3 AS (\n  SELECT\n    attack,\n    ridge,\n    MIN(pillar) AS graham,\n    COUNT(pillar) AS timer,\n    AVG(office) AS remark\n  FROM cte_2\n  GROUP BY\n    attack,\n    ridge\n),\ncte_4 AS (\n  SELECT\n    attack,\n    MAX(ridge) AS garlic,\n    SUM(graham) AS spirit\n  FROM cte_3\n  GROUP BY\n    attack\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     attack,\n     mummy,\n     cellar,\n     brand,\n     sponge\n|> AGGREGATE\n     SUM(brand) AS probe,\n     MAX(brand) AS guild\n   GROUP BY\n     attack,\n     mummy\n|> AGGREGATE\n     MIN(guild) AS ridge,\n     AVG(guild) AS pillar\n   GROUP BY\n     attack,\n     probe\n|> AGGREGATE\n     MIN(pillar) AS graham,\n     COUNT(pillar) AS timer,\n     AVG(office) AS remark\n   GROUP BY\n     attack,\n     ridge\n|> AGGREGATE\n     MAX(ridge) AS garlic,\n     SUM(graham) AS spirit\n   GROUP BY\n     attack\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"office",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    clive,\n    gummer,\n    suburb,\n    stress,\n    morse\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    clive,\n    gummer,\n    suburb,\n    stress,\n    AVG(morse) AS chaos,\n    MIN(morse) AS spine,\n    COUNT(morse) AS priest\n  FROM cte_0\n  GROUP BY\n    clive,\n    gummer,\n    suburb,\n    stress\n),\ncte_2 AS (\n  SELECT\n    AVG(spine) AS think,\n    MIN(clive) AS loser\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(think) AS drake,\n    AVG(velvet) AS wizard,\n    AVG(loser) AS outlay\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    drake,\n    MAX(outlay) AS humour,\n    MIN(outlay) AS pastor\n  FROM cte_3\n  GROUP BY\n    drake\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     clive,\n     gummer,\n     suburb,\n     stress,\n     morse\n|> AGGREGATE\n     AVG(morse) AS chaos,\n     MIN(morse) AS spine,\n     COUNT(morse) AS priest\n   GROUP BY\n     clive,\n     gummer,\n     suburb,\n     stress\n|> AGGREGATE\n     AVG(spine) AS think,\n     MIN(clive) AS loser\n|> AGGREGATE\n     COUNT(think) AS drake,\n     AVG(velvet) AS wizard,\n     AVG(loser) AS outlay\n|> AGGREGATE\n     MAX(outlay) AS humour,\n     MIN(outlay) AS pastor\n   GROUP BY\n     drake\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"velvet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    linen,\n    sanity,\n    garage,\n    crewe,\n    essay\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    linen,\n    COUNT(essay) AS magnet,\n    SUM(essay) AS donor,\n    AVG(essay) AS build\n  FROM cte_0\n  GROUP BY\n    linen,\n    sanity,\n    garage,\n    crewe\n),\ncte_2 AS (\n  SELECT\n    linen,\n    SUM(donor) AS shower,\n    MIN(donor) AS chord\n  FROM cte_1\n  GROUP BY\n    linen,\n    magnet\n),\ncte_3 AS (\n  SELECT\n    linen,\n    shower,\n    COUNT(gorge) AS maggot,\n    MIN(chord) AS saloon\n  FROM cte_2\n  GROUP BY\n    linen,\n    shower\n),\ncte_4 AS (\n  SELECT\n    MIN(linen) AS detail,\n    SUM(shower) AS china\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     linen,\n     sanity,\n     garage,\n     crewe,\n     essay\n|> AGGREGATE\n     COUNT(essay) AS magnet,\n     SUM(essay) AS donor,\n     AVG(essay) AS build\n   GROUP BY\n     linen,\n     sanity,\n     garage,\n     crewe\n|> AGGREGATE\n     SUM(donor) AS shower,\n     MIN(donor) AS chord\n   GROUP BY\n     linen,\n     magnet\n|> AGGREGATE\n     COUNT(gorge) AS maggot,\n     MIN(chord) AS saloon\n   GROUP BY\n     linen,\n     shower\n|> AGGREGATE\n     MIN(linen) AS detail,\n     SUM(shower) AS china\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"gorge",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    stair,\n    scrum,\n    roach,\n    favour,\n    doubt\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    stair,\n    scrum,\n    roach\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    stair,\n    scrum,\n    MAX(roach) AS truce,\n    SUM(roach) AS syntax,\n    AVG(roach) AS laura\n  FROM cte_1\n  GROUP BY\n    stair,\n    scrum\n),\ncte_3 AS (\n  SELECT\n    stair,\n    scrum,\n    MIN(laura) AS image\n  FROM cte_2\n  GROUP BY\n    stair,\n    scrum,\n    truce\n),\ncte_4 AS (\n  SELECT\n    COUNT(guinea) AS fungus,\n    MAX(image) AS strip,\n    COUNT(image) AS spine,\n    AVG(image) AS marrow\n  FROM cte_3\n  GROUP BY\n    stair,\n    scrum\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     stair,\n     scrum,\n     roach,\n     favour,\n     doubt\n|> SELECT\n     stair,\n     scrum,\n     roach\n|> AGGREGATE\n     MAX(roach) AS truce,\n     SUM(roach) AS syntax,\n     AVG(roach) AS laura\n   GROUP BY\n     stair,\n     scrum\n|> AGGREGATE\n     MIN(laura) AS image\n   GROUP BY\n     stair,\n     scrum,\n     truce\n|> AGGREGATE\n     COUNT(guinea) AS fungus,\n     MAX(image) AS strip,\n     COUNT(image) AS spine,\n     AVG(image) AS marrow\n   GROUP BY\n     stair,\n     scrum\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"guinea",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:43,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bloke,\n    telly,\n    realm,\n    crash,\n    nancy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    bloke,\n    telly,\n    realm,\n    crash\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    bloke,\n    telly,\n    COUNT(crash) AS stamp,\n    AVG(crash) AS smart,\n    SUM(crash) AS magic,\n    AVG(realm) AS kidney\n  FROM cte_1\n  GROUP BY\n    bloke,\n    telly\n),\ncte_3 AS (\n  SELECT\n    bloke,\n    telly,\n    MAX(kidney) AS livery,\n    MAX(barrel) AS reader,\n    SUM(stamp) AS fright\n  FROM cte_2\n  GROUP BY\n    bloke,\n    telly\n),\ncte_4 AS (\n  SELECT\n    MIN(livery) AS hostel\n  FROM cte_3\n  GROUP BY\n    bloke,\n    telly\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bloke,\n     telly,\n     realm,\n     crash,\n     nancy\n|> SELECT\n     bloke,\n     telly,\n     realm,\n     crash\n|> AGGREGATE\n     COUNT(crash) AS stamp,\n     AVG(crash) AS smart,\n     SUM(crash) AS magic,\n     AVG(realm) AS kidney\n   GROUP BY\n     bloke,\n     telly\n|> AGGREGATE\n     MAX(kidney) AS livery,\n     MAX(barrel) AS reader,\n     SUM(stamp) AS fright\n   GROUP BY\n     bloke,\n     telly\n|> AGGREGATE\n     MIN(livery) AS hostel\n   GROUP BY\n     bloke,\n     telly\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"barrel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    trough,\n    grain,\n    colour,\n    garden,\n    cherry\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    trough,\n    AVG(garden) AS novel,\n    SUM(colour) AS crush,\n    MIN(colour) AS break\n  FROM cte_0\n  GROUP BY\n    trough,\n    grain\n),\ncte_2 AS (\n  SELECT\n    trough,\n    novel,\n    crush,\n    break\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    trough,\n    novel,\n    crush,\n    SUM(break) AS sheikh,\n    MAX(break) AS gorge,\n    MIN(repeal) AS border\n  FROM cte_2\n  GROUP BY\n    trough,\n    novel,\n    crush\n),\ncte_4 AS (\n  SELECT\n    MIN(sheikh) AS effect\n  FROM cte_3\n  GROUP BY\n    trough,\n    novel,\n    crush\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     trough,\n     grain,\n     colour,\n     garden,\n     cherry\n|> AGGREGATE\n     AVG(garden) AS novel,\n     SUM(colour) AS crush,\n     MIN(colour) AS break\n   GROUP BY\n     trough,\n     grain\n|> SELECT\n     trough,\n     novel,\n     crush,\n     break\n|> AGGREGATE\n     SUM(break) AS sheikh,\n     MAX(break) AS gorge,\n     MIN(repeal) AS border\n   GROUP BY\n     trough,\n     novel,\n     crush\n|> AGGREGATE\n     MIN(sheikh) AS effect\n   GROUP BY\n     trough,\n     novel,\n     crush\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"repeal",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    greek,\n    image,\n    laser,\n    alloy,\n    ankle\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    greek,\n    MAX(ankle) AS settee,\n    SUM(laser) AS palmer,\n    AVG(laser) AS belle\n  FROM cte_0\n  GROUP BY\n    greek,\n    image\n),\ncte_2 AS (\n  SELECT\n    greek,\n    settee,\n    MIN(palmer) AS banker,\n    COUNT(belle) AS retina\n  FROM cte_1\n  GROUP BY\n    greek,\n    settee\n),\ncte_3 AS (\n  SELECT\n    greek,\n    COUNT(retina) AS entry,\n    SUM(viola) AS expert,\n    AVG(banker) AS debit\n  FROM cte_2\n  GROUP BY\n    greek,\n    settee\n),\ncte_4 AS (\n  SELECT\n    greek,\n    COUNT(entry) AS stunt\n  FROM cte_3\n  GROUP BY\n    greek\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     greek,\n     image,\n     laser,\n     alloy,\n     ankle\n|> AGGREGATE\n     MAX(ankle) AS settee,\n     SUM(laser) AS palmer,\n     AVG(laser) AS belle\n   GROUP BY\n     greek,\n     image\n|> AGGREGATE\n     MIN(palmer) AS banker,\n     COUNT(belle) AS retina\n   GROUP BY\n     greek,\n     settee\n|> AGGREGATE\n     COUNT(retina) AS entry,\n     SUM(viola) AS expert,\n     AVG(banker) AS debit\n   GROUP BY\n     greek,\n     settee\n|> AGGREGATE\n     COUNT(entry) AS stunt\n   GROUP BY\n     greek\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"viola",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    outcry,\n    spear,\n    rover,\n    insert,\n    hockey\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    outcry,\n    spear,\n    SUM(insert) AS guess,\n    AVG(hockey) AS memory\n  FROM cte_0\n  GROUP BY\n    outcry,\n    spear\n),\ncte_2 AS (\n  SELECT\n    outcry,\n    spear,\n    MIN(memory) AS towel\n  FROM cte_1\n  GROUP BY\n    outcry,\n    spear,\n    guess\n),\ncte_3 AS (\n  SELECT\n    AVG(spear) AS cousin\n  FROM cte_2\n  GROUP BY\n    outcry\n),\ncte_4 AS (\n  SELECT\n    COUNT(cousin) AS staple,\n    MIN(parody) AS ozone,\n    AVG(cousin) AS strap,\n    MAX(cousin) AS hound,\n    SUM(cousin) AS affect,\n    MIN(cousin) AS rebate\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     outcry,\n     spear,\n     rover,\n     insert,\n     hockey\n|> AGGREGATE\n     SUM(insert) AS guess,\n     AVG(hockey) AS memory\n   GROUP BY\n     outcry,\n     spear\n|> AGGREGATE\n     MIN(memory) AS towel\n   GROUP BY\n     outcry,\n     spear,\n     guess\n|> AGGREGATE\n     AVG(spear) AS cousin\n   GROUP BY\n     outcry\n|> AGGREGATE\n     COUNT(cousin) AS staple,\n     MIN(parody) AS ozone,\n     AVG(cousin) AS strap,\n     MAX(cousin) AS hound,\n     SUM(cousin) AS affect,\n     MIN(cousin) AS rebate\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"parody",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:43,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    shower,\n    flick,\n    pizza,\n    tutor,\n    poster\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    shower,\n    flick,\n    MIN(poster) AS bottom,\n    MAX(poster) AS nelson,\n    MAX(tutor) AS coffin\n  FROM cte_0\n  GROUP BY\n    shower,\n    flick\n),\ncte_2 AS (\n  SELECT\n    shower,\n    MIN(bottom) AS porch,\n    SUM(nelson) AS boiler,\n    AVG(coffin) AS route\n  FROM cte_1\n  GROUP BY\n    shower,\n    flick\n),\ncte_3 AS (\n  SELECT\n    shower,\n    MAX(boyle) AS basket,\n    AVG(porch) AS hound,\n    MAX(boiler) AS crunch\n  FROM cte_2\n  GROUP BY\n    shower\n),\ncte_4 AS (\n  SELECT\n    MAX(crunch) AS baton\n  FROM cte_3\n  GROUP BY\n    shower\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     shower,\n     flick,\n     pizza,\n     tutor,\n     poster\n|> AGGREGATE\n     MIN(poster) AS bottom,\n     MAX(poster) AS nelson,\n     MAX(tutor) AS coffin\n   GROUP BY\n     shower,\n     flick\n|> AGGREGATE\n     MIN(bottom) AS porch,\n     SUM(nelson) AS boiler,\n     AVG(coffin) AS route\n   GROUP BY\n     shower,\n     flick\n|> AGGREGATE\n     MAX(boyle) AS basket,\n     AVG(porch) AS hound,\n     MAX(boiler) AS crunch\n   GROUP BY\n     shower\n|> AGGREGATE\n     MAX(crunch) AS baton\n   GROUP BY\n     shower\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"boyle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    honey,\n    elite,\n    track,\n    wagon,\n    window\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    honey,\n    elite,\n    track,\n    SUM(window) AS rifle\n  FROM cte_0\n  GROUP BY\n    honey,\n    elite,\n    track,\n    wagon\n),\ncte_2 AS (\n  SELECT\n    AVG(track) AS fraud,\n    MAX(rifle) AS paint,\n    MIN(rifle) AS travel,\n    MIN(track) AS father\n  FROM cte_1\n  GROUP BY\n    honey,\n    elite\n),\ncte_3 AS (\n  SELECT\n    fraud,\n    SUM(peril) AS drink,\n    MIN(father) AS pledge\n  FROM cte_2\n  GROUP BY\n    fraud,\n    paint\n),\ncte_4 AS (\n  SELECT\n    COUNT(fraud) AS creed,\n    AVG(pledge) AS herald\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     honey,\n     elite,\n     track,\n     wagon,\n     window\n|> AGGREGATE\n     SUM(window) AS rifle\n   GROUP BY\n     honey,\n     elite,\n     track,\n     wagon\n|> AGGREGATE\n     AVG(track) AS fraud,\n     MAX(rifle) AS paint,\n     MIN(rifle) AS travel,\n     MIN(track) AS father\n   GROUP BY\n     honey,\n     elite\n|> AGGREGATE\n     SUM(peril) AS drink,\n     MIN(father) AS pledge\n   GROUP BY\n     fraud,\n     paint\n|> AGGREGATE\n     COUNT(fraud) AS creed,\n     AVG(pledge) AS herald\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"peril",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    phoebe,\n    branch,\n    enemy,\n    impact,\n    thirst\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    phoebe,\n    branch,\n    AVG(thirst) AS derby,\n    MIN(thirst) AS tutor\n  FROM cte_0\n  GROUP BY\n    phoebe,\n    branch,\n    enemy,\n    impact\n),\ncte_2 AS (\n  SELECT\n    phoebe,\n    MAX(tutor) AS peter,\n    MIN(derby) AS major,\n    SUM(tutor) AS bodie\n  FROM cte_1\n  GROUP BY\n    phoebe,\n    branch\n),\ncte_3 AS (\n  SELECT\n    phoebe,\n    SUM(bronze) AS keeper,\n    SUM(bodie) AS paint\n  FROM cte_2\n  GROUP BY\n    phoebe,\n    peter\n),\ncte_4 AS (\n  SELECT\n    COUNT(phoebe) AS creek,\n    AVG(paint) AS skull\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     phoebe,\n     branch,\n     enemy,\n     impact,\n     thirst\n|> AGGREGATE\n     AVG(thirst) AS derby,\n     MIN(thirst) AS tutor\n   GROUP BY\n     phoebe,\n     branch,\n     enemy,\n     impact\n|> AGGREGATE\n     MAX(tutor) AS peter,\n     MIN(derby) AS major,\n     SUM(tutor) AS bodie\n   GROUP BY\n     phoebe,\n     branch\n|> AGGREGATE\n     SUM(bronze) AS keeper,\n     SUM(bodie) AS paint\n   GROUP BY\n     phoebe,\n     peter\n|> AGGREGATE\n     COUNT(phoebe) AS creek,\n     AVG(paint) AS skull\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"bronze",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    threat,\n    choice,\n    guide,\n    freeze,\n    mouse\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    threat,\n    choice,\n    AVG(freeze) AS driver,\n    MIN(mouse) AS filth,\n    MIN(freeze) AS abbey,\n    MAX(freeze) AS taboo\n  FROM cte_0\n  GROUP BY\n    threat,\n    choice,\n    guide\n),\ncte_2 AS (\n  SELECT\n    threat,\n    choice,\n    driver\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    threat,\n    choice,\n    COUNT(driver) AS maggie,\n    MAX(clive) AS frame,\n    MIN(driver) AS denim\n  FROM cte_2\n  GROUP BY\n    threat,\n    choice\n),\ncte_4 AS (\n  SELECT\n    SUM(denim) AS witney\n  FROM cte_3\n  GROUP BY\n    threat,\n    choice\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     threat,\n     choice,\n     guide,\n     freeze,\n     mouse\n|> AGGREGATE\n     AVG(freeze) AS driver,\n     MIN(mouse) AS filth,\n     MIN(freeze) AS abbey,\n     MAX(freeze) AS taboo\n   GROUP BY\n     threat,\n     choice,\n     guide\n|> SELECT\n     threat,\n     choice,\n     driver\n|> AGGREGATE\n     COUNT(driver) AS maggie,\n     MAX(clive) AS frame,\n     MIN(driver) AS denim\n   GROUP BY\n     threat,\n     choice\n|> AGGREGATE\n     SUM(denim) AS witney\n   GROUP BY\n     threat,\n     choice\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"clive",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    spire,\n    cheer,\n    outing,\n    rider,\n    region\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    spire,\n    cheer,\n    COUNT(rider) AS theory,\n    AVG(rider) AS cider\n  FROM cte_0\n  GROUP BY\n    spire,\n    cheer\n),\ncte_2 AS (\n  SELECT\n    SUM(cider) AS vogue,\n    AVG(theory) AS staple,\n    AVG(cider) AS menace,\n    MIN(cider) AS phoebe\n  FROM cte_1\n  GROUP BY\n    spire,\n    cheer\n),\ncte_3 AS (\n  SELECT\n    vogue,\n    staple,\n    COUNT(meadow) AS fella,\n    MAX(phoebe) AS lapse\n  FROM cte_2\n  GROUP BY\n    vogue,\n    staple\n),\ncte_4 AS (\n  SELECT\n    vogue,\n    AVG(staple) AS knife\n  FROM cte_3\n  GROUP BY\n    vogue\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     spire,\n     cheer,\n     outing,\n     rider,\n     region\n|> AGGREGATE\n     COUNT(rider) AS theory,\n     AVG(rider) AS cider\n   GROUP BY\n     spire,\n     cheer\n|> AGGREGATE\n     SUM(cider) AS vogue,\n     AVG(theory) AS staple,\n     AVG(cider) AS menace,\n     MIN(cider) AS phoebe\n   GROUP BY\n     spire,\n     cheer\n|> AGGREGATE\n     COUNT(meadow) AS fella,\n     MAX(phoebe) AS lapse\n   GROUP BY\n     vogue,\n     staple\n|> AGGREGATE\n     AVG(staple) AS knife\n   GROUP BY\n     vogue\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"meadow",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    assent,\n    arena,\n    report,\n    little,\n    water\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    assent,\n    arena,\n    SUM(water) AS pillar,\n    COUNT(water) AS north,\n    AVG(water) AS hinge\n  FROM cte_0\n  GROUP BY\n    assent,\n    arena,\n    report,\n    little\n),\ncte_2 AS (\n  SELECT\n    assent,\n    arena,\n    pillar,\n    north\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    assent,\n    arena,\n    MAX(north) AS elbow,\n    MAX(clinic) AS design\n  FROM cte_2\n  GROUP BY\n    assent,\n    arena\n),\ncte_4 AS (\n  SELECT\n    SUM(elbow) AS alpha,\n    MAX(elbow) AS frenzy\n  FROM cte_3\n  GROUP BY\n    assent,\n    arena\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     assent,\n     arena,\n     report,\n     little,\n     water\n|> AGGREGATE\n     SUM(water) AS pillar,\n     COUNT(water) AS north,\n     AVG(water) AS hinge\n   GROUP BY\n     assent,\n     arena,\n     report,\n     little\n|> SELECT\n     assent,\n     arena,\n     pillar,\n     north\n|> AGGREGATE\n     MAX(north) AS elbow,\n     MAX(clinic) AS design\n   GROUP BY\n     assent,\n     arena\n|> AGGREGATE\n     SUM(elbow) AS alpha,\n     MAX(elbow) AS frenzy\n   GROUP BY\n     assent,\n     arena\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"clinic",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    piece,\n    creole,\n    baton,\n    helmet,\n    middle\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    piece,\n    creole,\n    baton,\n    AVG(middle) AS stress,\n    COUNT(middle) AS build,\n    SUM(middle) AS tennis,\n    MIN(middle) AS breed,\n    MAX(middle) AS match\n  FROM cte_0\n  GROUP BY\n    piece,\n    creole,\n    baton,\n    helmet\n),\ncte_2 AS (\n  SELECT\n    piece,\n    MIN(breed) AS goblin,\n    COUNT(method) AS mosaic\n  FROM cte_1\n  GROUP BY\n    piece,\n    creole,\n    baton,\n    stress\n),\ncte_3 AS (\n  SELECT\n    piece,\n    goblin\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(goblin) AS joseph\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     piece,\n     creole,\n     baton,\n     helmet,\n     middle\n|> AGGREGATE\n     AVG(middle) AS stress,\n     COUNT(middle) AS build,\n     SUM(middle) AS tennis,\n     MIN(middle) AS breed,\n     MAX(middle) AS match\n   GROUP BY\n     piece,\n     creole,\n     baton,\n     helmet\n|> AGGREGATE\n     MIN(breed) AS goblin,\n     COUNT(method) AS mosaic\n   GROUP BY\n     piece,\n     creole,\n     baton,\n     stress\n|> SELECT\n     piece,\n     goblin\n|> AGGREGATE\n     COUNT(goblin) AS joseph\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"method",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    fridge,\n    papacy,\n    binder,\n    curry,\n    melon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    fridge,\n    papacy,\n    COUNT(melon) AS burrow\n  FROM cte_0\n  GROUP BY\n    fridge,\n    papacy,\n    binder,\n    curry\n),\ncte_2 AS (\n  SELECT\n    fridge,\n    SUM(burrow) AS holly\n  FROM cte_1\n  GROUP BY\n    fridge\n),\ncte_3 AS (\n  SELECT\n    fridge,\n    SUM(holly) AS trade,\n    MIN(holly) AS belief,\n    MAX(holly) AS image\n  FROM cte_2\n  GROUP BY\n    fridge\n),\ncte_4 AS (\n  SELECT\n    SUM(myriad) AS stand,\n    SUM(image) AS front,\n    MAX(belief) AS rubber,\n    MAX(image) AS maggie\n  FROM cte_3\n  GROUP BY\n    fridge,\n    trade\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     fridge,\n     papacy,\n     binder,\n     curry,\n     melon\n|> AGGREGATE\n     COUNT(melon) AS burrow\n   GROUP BY\n     fridge,\n     papacy,\n     binder,\n     curry\n|> AGGREGATE\n     SUM(burrow) AS holly\n   GROUP BY\n     fridge\n|> AGGREGATE\n     SUM(holly) AS trade,\n     MIN(holly) AS belief,\n     MAX(holly) AS image\n   GROUP BY\n     fridge\n|> AGGREGATE\n     SUM(myriad) AS stand,\n     SUM(image) AS front,\n     MAX(belief) AS rubber,\n     MAX(image) AS maggie\n   GROUP BY\n     fridge,\n     trade\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"myriad",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:43,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    almond,\n    bream,\n    coach,\n    airbus,\n    fruit\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    almond,\n    AVG(coach) AS waiter,\n    MIN(fruit) AS sheep,\n    COUNT(coach) AS inside\n  FROM cte_0\n  GROUP BY\n    almond,\n    bream\n),\ncte_2 AS (\n  SELECT\n    almond,\n    waiter,\n    MAX(inside) AS audit\n  FROM cte_1\n  GROUP BY\n    almond,\n    waiter\n),\ncte_3 AS (\n  SELECT\n    almond,\n    MAX(waiter) AS powder,\n    MIN(audit) AS stage,\n    MIN(bottle) AS locke\n  FROM cte_2\n  GROUP BY\n    almond\n),\ncte_4 AS (\n  SELECT\n    MAX(stage) AS sponge,\n    MIN(powder) AS match,\n    SUM(powder) AS steel\n  FROM cte_3\n  GROUP BY\n    almond\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     almond,\n     bream,\n     coach,\n     airbus,\n     fruit\n|> AGGREGATE\n     AVG(coach) AS waiter,\n     MIN(fruit) AS sheep,\n     COUNT(coach) AS inside\n   GROUP BY\n     almond,\n     bream\n|> AGGREGATE\n     MAX(inside) AS audit\n   GROUP BY\n     almond,\n     waiter\n|> AGGREGATE\n     MAX(waiter) AS powder,\n     MIN(audit) AS stage,\n     MIN(bottle) AS locke\n   GROUP BY\n     almond\n|> AGGREGATE\n     MAX(stage) AS sponge,\n     MIN(powder) AS match,\n     SUM(powder) AS steel\n   GROUP BY\n     almond\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"bottle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    cattle,\n    detail,\n    claret,\n    carer,\n    minor\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    cattle,\n    detail,\n    claret,\n    SUM(minor) AS mortar\n  FROM cte_0\n  GROUP BY\n    cattle,\n    detail,\n    claret,\n    carer\n),\ncte_2 AS (\n  SELECT\n    cattle,\n    MAX(mortar) AS threat,\n    COUNT(detail) AS satin,\n    MIN(mortar) AS cement,\n    SUM(claret) AS theme,\n    SUM(detail) AS steak,\n    SUM(access) AS module\n  FROM cte_1\n  GROUP BY\n    cattle\n),\ncte_3 AS (\n  SELECT\n    cattle,\n    threat,\n    satin\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(threat) AS murmur,\n    COUNT(satin) AS ration\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     cattle,\n     detail,\n     claret,\n     carer,\n     minor\n|> AGGREGATE\n     SUM(minor) AS mortar\n   GROUP BY\n     cattle,\n     detail,\n     claret,\n     carer\n|> AGGREGATE\n     MAX(mortar) AS threat,\n     COUNT(detail) AS satin,\n     MIN(mortar) AS cement,\n     SUM(claret) AS theme,\n     SUM(detail) AS steak,\n     SUM(access) AS module\n   GROUP BY\n     cattle\n|> SELECT\n     cattle,\n     threat,\n     satin\n|> AGGREGATE\n     AVG(threat) AS murmur,\n     COUNT(satin) AS ration\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"access",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    torque,\n    mouse,\n    steen,\n    teapot,\n    brief\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    torque,\n    mouse,\n    steen,\n    MAX(brief) AS tavern,\n    COUNT(brief) AS summit,\n    AVG(brief) AS penny\n  FROM cte_0\n  GROUP BY\n    torque,\n    mouse,\n    steen,\n    teapot\n),\ncte_2 AS (\n  SELECT\n    torque,\n    COUNT(summit) AS supply,\n    SUM(summit) AS judge,\n    MIN(penny) AS grange,\n    MIN(pencil) AS cursor\n  FROM cte_1\n  GROUP BY\n    torque\n),\ncte_3 AS (\n  SELECT\n    torque,\n    supply,\n    judge\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(judge) AS herald\n  FROM cte_3\n  GROUP BY\n    torque,\n    supply\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     torque,\n     mouse,\n     steen,\n     teapot,\n     brief\n|> AGGREGATE\n     MAX(brief) AS tavern,\n     COUNT(brief) AS summit,\n     AVG(brief) AS penny\n   GROUP BY\n     torque,\n     mouse,\n     steen,\n     teapot\n|> AGGREGATE\n     COUNT(summit) AS supply,\n     SUM(summit) AS judge,\n     MIN(penny) AS grange,\n     MIN(pencil) AS cursor\n   GROUP BY\n     torque\n|> SELECT\n     torque,\n     supply,\n     judge\n|> AGGREGATE\n     MIN(judge) AS herald\n   GROUP BY\n     torque,\n     supply\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"pencil",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    import,\n    driver,\n    uptake,\n    deity,\n    wheel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    import,\n    driver,\n    uptake,\n    MAX(wheel) AS upland,\n    MAX(deity) AS giant,\n    COUNT(wheel) AS crisp\n  FROM cte_0\n  GROUP BY\n    import,\n    driver,\n    uptake\n),\ncte_2 AS (\n  SELECT\n    import,\n    driver,\n    uptake,\n    upland,\n    giant\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    import,\n    SUM(giant) AS billy,\n    COUNT(witch) AS sudden,\n    MIN(uptake) AS heyday\n  FROM cte_2\n  GROUP BY\n    import,\n    driver\n),\ncte_4 AS (\n  SELECT\n    SUM(heyday) AS impact\n  FROM cte_3\n  GROUP BY\n    import,\n    billy\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     import,\n     driver,\n     uptake,\n     deity,\n     wheel\n|> AGGREGATE\n     MAX(wheel) AS upland,\n     MAX(deity) AS giant,\n     COUNT(wheel) AS crisp\n   GROUP BY\n     import,\n     driver,\n     uptake\n|> SELECT\n     import,\n     driver,\n     uptake,\n     upland,\n     giant\n|> AGGREGATE\n     SUM(giant) AS billy,\n     COUNT(witch) AS sudden,\n     MIN(uptake) AS heyday\n   GROUP BY\n     import,\n     driver\n|> AGGREGATE\n     SUM(heyday) AS impact\n   GROUP BY\n     import,\n     billy\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"witch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sheen,\n    shoot,\n    pause,\n    fault,\n    virtue\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    sheen,\n    SUM(fault) AS dollar,\n    MIN(shoot) AS estate,\n    MAX(fault) AS rival\n  FROM cte_0\n  GROUP BY\n    sheen\n),\ncte_2 AS (\n  SELECT\n    sheen,\n    SUM(dollar) AS space,\n    COUNT(dollar) AS garlic\n  FROM cte_1\n  GROUP BY\n    sheen\n),\ncte_3 AS (\n  SELECT\n    sheen,\n    space,\n    AVG(garlic) AS grange,\n    COUNT(garlic) AS phrase,\n    AVG(frank) AS action,\n    SUM(garlic) AS hobby\n  FROM cte_2\n  GROUP BY\n    sheen,\n    space\n),\ncte_4 AS (\n  SELECT\n    sheen,\n    SUM(space) AS moray\n  FROM cte_3\n  GROUP BY\n    sheen\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sheen,\n     shoot,\n     pause,\n     fault,\n     virtue\n|> AGGREGATE\n     SUM(fault) AS dollar,\n     MIN(shoot) AS estate,\n     MAX(fault) AS rival\n   GROUP BY\n     sheen\n|> AGGREGATE\n     SUM(dollar) AS space,\n     COUNT(dollar) AS garlic\n   GROUP BY\n     sheen\n|> AGGREGATE\n     AVG(garlic) AS grange,\n     COUNT(garlic) AS phrase,\n     AVG(frank) AS action,\n     SUM(garlic) AS hobby\n   GROUP BY\n     sheen,\n     space\n|> AGGREGATE\n     SUM(space) AS moray\n   GROUP BY\n     sheen\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"frank",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    search,\n    cannon,\n    tumour,\n    repair,\n    chord\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    search,\n    cannon,\n    tumour,\n    MAX(chord) AS kernel\n  FROM cte_0\n  GROUP BY\n    search,\n    cannon,\n    tumour,\n    repair\n),\ncte_2 AS (\n  SELECT\n    search,\n    MAX(tumour) AS pistol\n  FROM cte_1\n  GROUP BY\n    search,\n    cannon\n),\ncte_3 AS (\n  SELECT\n    COUNT(pistol) AS advice,\n    SUM(pistol) AS cloth,\n    MAX(pistol) AS county,\n    COUNT(newco) AS court,\n    AVG(pistol) AS ribbon,\n    MIN(pistol) AS right\n  FROM cte_2\n  GROUP BY\n    search\n),\ncte_4 AS (\n  SELECT\n    advice,\n    cloth,\n    county\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     search,\n     cannon,\n     tumour,\n     repair,\n     chord\n|> AGGREGATE\n     MAX(chord) AS kernel\n   GROUP BY\n     search,\n     cannon,\n     tumour,\n     repair\n|> AGGREGATE\n     MAX(tumour) AS pistol\n   GROUP BY\n     search,\n     cannon\n|> AGGREGATE\n     COUNT(pistol) AS advice,\n     SUM(pistol) AS cloth,\n     MAX(pistol) AS county,\n     COUNT(newco) AS court,\n     AVG(pistol) AS ribbon,\n     MIN(pistol) AS right\n   GROUP BY\n     search\n|> SELECT\n     advice,\n     cloth,\n     county\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"newco",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    potato,\n    ariel,\n    aggie,\n    coupon,\n    carpet\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    potato,\n    ariel\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    potato,\n    MAX(ariel) AS demand,\n    MIN(ariel) AS depot,\n    AVG(ariel) AS union,\n    COUNT(ariel) AS unrest\n  FROM cte_1\n  GROUP BY\n    potato\n),\ncte_3 AS (\n  SELECT\n    potato,\n    demand,\n    SUM(unrest) AS mosaic\n  FROM cte_2\n  GROUP BY\n    potato,\n    demand,\n    depot\n),\ncte_4 AS (\n  SELECT\n    potato,\n    MIN(grand) AS stock,\n    AVG(mosaic) AS plain,\n    MAX(mosaic) AS suede,\n    SUM(mosaic) AS thrill\n  FROM cte_3\n  GROUP BY\n    potato,\n    demand\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     potato,\n     ariel,\n     aggie,\n     coupon,\n     carpet\n|> SELECT\n     potato,\n     ariel\n|> AGGREGATE\n     MAX(ariel) AS demand,\n     MIN(ariel) AS depot,\n     AVG(ariel) AS union,\n     COUNT(ariel) AS unrest\n   GROUP BY\n     potato\n|> AGGREGATE\n     SUM(unrest) AS mosaic\n   GROUP BY\n     potato,\n     demand,\n     depot\n|> AGGREGATE\n     MIN(grand) AS stock,\n     AVG(mosaic) AS plain,\n     MAX(mosaic) AS suede,\n     SUM(mosaic) AS thrill\n   GROUP BY\n     potato,\n     demand\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"grand",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    suede,\n    ferry,\n    input,\n    white,\n    logic\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    suede,\n    ferry,\n    input,\n    white,\n    MIN(logic) AS match,\n    MAX(logic) AS chief,\n    AVG(logic) AS kinase\n  FROM cte_0\n  GROUP BY\n    suede,\n    ferry,\n    input,\n    white\n),\ncte_2 AS (\n  SELECT\n    COUNT(white) AS heaven,\n    MAX(match) AS crane,\n    SUM(chief) AS rally,\n    SUM(label) AS coast\n  FROM cte_1\n  GROUP BY\n    suede\n),\ncte_3 AS (\n  SELECT\n    COUNT(crane) AS greek\n  FROM cte_2\n  GROUP BY\n    heaven\n),\ncte_4 AS (\n  SELECT\n    SUM(greek) AS chick,\n    COUNT(greek) AS amount\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     suede,\n     ferry,\n     input,\n     white,\n     logic\n|> AGGREGATE\n     MIN(logic) AS match,\n     MAX(logic) AS chief,\n     AVG(logic) AS kinase\n   GROUP BY\n     suede,\n     ferry,\n     input,\n     white\n|> AGGREGATE\n     COUNT(white) AS heaven,\n     MAX(match) AS crane,\n     SUM(chief) AS rally,\n     SUM(label) AS coast\n   GROUP BY\n     suede\n|> AGGREGATE\n     COUNT(crane) AS greek\n   GROUP BY\n     heaven\n|> AGGREGATE\n     SUM(greek) AS chick,\n     COUNT(greek) AS amount\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"label",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    queen,\n    video,\n    spoon,\n    novice,\n    shame\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    queen,\n    video,\n    spoon,\n    MAX(shame) AS quarry,\n    COUNT(novice) AS health\n  FROM cte_0\n  GROUP BY\n    queen,\n    video,\n    spoon\n),\ncte_2 AS (\n  SELECT\n    queen,\n    video,\n    spoon,\n    quarry,\n    health\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    queen,\n    MAX(spoon) AS pause,\n    AVG(spoon) AS jessie,\n    AVG(panic) AS upside,\n    MAX(health) AS single\n  FROM cte_2\n  GROUP BY\n    queen,\n    video\n),\ncte_4 AS (\n  SELECT\n    queen,\n    MIN(pause) AS reflex\n  FROM cte_3\n  GROUP BY\n    queen\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     queen,\n     video,\n     spoon,\n     novice,\n     shame\n|> AGGREGATE\n     MAX(shame) AS quarry,\n     COUNT(novice) AS health\n   GROUP BY\n     queen,\n     video,\n     spoon\n|> SELECT\n     queen,\n     video,\n     spoon,\n     quarry,\n     health\n|> AGGREGATE\n     MAX(spoon) AS pause,\n     AVG(spoon) AS jessie,\n     AVG(panic) AS upside,\n     MAX(health) AS single\n   GROUP BY\n     queen,\n     video\n|> AGGREGATE\n     MIN(pause) AS reflex\n   GROUP BY\n     queen\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"panic",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    broad,\n    valve,\n    derry,\n    worker,\n    gunman\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    broad,\n    valve,\n    AVG(derry) AS litter\n  FROM cte_0\n  GROUP BY\n    broad,\n    valve\n),\ncte_2 AS (\n  SELECT\n    MAX(litter) AS wonder\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(wonder) AS tucker,\n    COUNT(wonder) AS pledge,\n    AVG(wonder) AS mandy,\n    MIN(wonder) AS notice,\n    SUM(wonder) AS jaguar\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    tucker,\n    pledge,\n    AVG(lender) AS sheen,\n    AVG(notice) AS bonus,\n    MAX(jaguar) AS gender,\n    MAX(notice) AS maker\n  FROM cte_3\n  GROUP BY\n    tucker,\n    pledge\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     broad,\n     valve,\n     derry,\n     worker,\n     gunman\n|> AGGREGATE\n     AVG(derry) AS litter\n   GROUP BY\n     broad,\n     valve\n|> AGGREGATE\n     MAX(litter) AS wonder\n|> AGGREGATE\n     MAX(wonder) AS tucker,\n     COUNT(wonder) AS pledge,\n     AVG(wonder) AS mandy,\n     MIN(wonder) AS notice,\n     SUM(wonder) AS jaguar\n|> AGGREGATE\n     AVG(lender) AS sheen,\n     AVG(notice) AS bonus,\n     MAX(jaguar) AS gender,\n     MAX(notice) AS maker\n   GROUP BY\n     tucker,\n     pledge\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"lender",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    fossil,\n    metre,\n    cellar,\n    barrow,\n    mutiny\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    fossil,\n    metre,\n    MAX(barrow) AS harris\n  FROM cte_0\n  GROUP BY\n    fossil,\n    metre\n),\ncte_2 AS (\n  SELECT\n    MIN(harris) AS mandy\n  FROM cte_1\n  GROUP BY\n    fossil,\n    metre\n),\ncte_3 AS (\n  SELECT\n    SUM(mandy) AS thumb,\n    AVG(mandy) AS penny,\n    MAX(mandy) AS winger,\n    MIN(mandy) AS flora\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    thumb,\n    penny,\n    AVG(series) AS claret,\n    COUNT(flora) AS stint,\n    MAX(flora) AS sensor,\n    AVG(flora) AS female\n  FROM cte_3\n  GROUP BY\n    thumb,\n    penny\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     fossil,\n     metre,\n     cellar,\n     barrow,\n     mutiny\n|> AGGREGATE\n     MAX(barrow) AS harris\n   GROUP BY\n     fossil,\n     metre\n|> AGGREGATE\n     MIN(harris) AS mandy\n   GROUP BY\n     fossil,\n     metre\n|> AGGREGATE\n     SUM(mandy) AS thumb,\n     AVG(mandy) AS penny,\n     MAX(mandy) AS winger,\n     MIN(mandy) AS flora\n|> AGGREGATE\n     AVG(series) AS claret,\n     COUNT(flora) AS stint,\n     MAX(flora) AS sensor,\n     AVG(flora) AS female\n   GROUP BY\n     thumb,\n     penny\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"series",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    anchor,\n    chess,\n    priory,\n    fossil,\n    slough\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    anchor,\n    chess,\n    priory,\n    MAX(fossil) AS birth,\n    COUNT(fossil) AS coast,\n    SUM(fossil) AS butler\n  FROM cte_0\n  GROUP BY\n    anchor,\n    chess,\n    priory\n),\ncte_2 AS (\n  SELECT\n    anchor,\n    SUM(birth) AS uncle,\n    MAX(coast) AS bleach\n  FROM cte_1\n  GROUP BY\n    anchor,\n    chess,\n    priory\n),\ncte_3 AS (\n  SELECT\n    COUNT(doubt) AS theory,\n    COUNT(uncle) AS rattle,\n    AVG(uncle) AS argyll\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(rattle) AS oyster,\n    MIN(rattle) AS poison\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     anchor,\n     chess,\n     priory,\n     fossil,\n     slough\n|> AGGREGATE\n     MAX(fossil) AS birth,\n     COUNT(fossil) AS coast,\n     SUM(fossil) AS butler\n   GROUP BY\n     anchor,\n     chess,\n     priory\n|> AGGREGATE\n     SUM(birth) AS uncle,\n     MAX(coast) AS bleach\n   GROUP BY\n     anchor,\n     chess,\n     priory\n|> AGGREGATE\n     COUNT(doubt) AS theory,\n     COUNT(uncle) AS rattle,\n     AVG(uncle) AS argyll\n|> AGGREGATE\n     AVG(rattle) AS oyster,\n     MIN(rattle) AS poison\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"doubt",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    office,\n    wallet,\n    outcry,\n    motor,\n    barton\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    office,\n    SUM(wallet) AS carol,\n    AVG(outcry) AS bamboo,\n    COUNT(barton) AS towel,\n    SUM(motor) AS bugger\n  FROM cte_0\n  GROUP BY\n    office\n),\ncte_2 AS (\n  SELECT\n    office,\n    carol,\n    COUNT(towel) AS sultan,\n    AVG(towel) AS thirst\n  FROM cte_1\n  GROUP BY\n    office,\n    carol,\n    bamboo\n),\ncte_3 AS (\n  SELECT\n    office,\n    SUM(logic) AS smash,\n    SUM(sultan) AS youth\n  FROM cte_2\n  GROUP BY\n    office\n),\ncte_4 AS (\n  SELECT\n    AVG(youth) AS tyler,\n    MIN(youth) AS alien\n  FROM cte_3\n  GROUP BY\n    office\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     office,\n     wallet,\n     outcry,\n     motor,\n     barton\n|> AGGREGATE\n     SUM(wallet) AS carol,\n     AVG(outcry) AS bamboo,\n     COUNT(barton) AS towel,\n     SUM(motor) AS bugger\n   GROUP BY\n     office\n|> AGGREGATE\n     COUNT(towel) AS sultan,\n     AVG(towel) AS thirst\n   GROUP BY\n     office,\n     carol,\n     bamboo\n|> AGGREGATE\n     SUM(logic) AS smash,\n     SUM(sultan) AS youth\n   GROUP BY\n     office\n|> AGGREGATE\n     AVG(youth) AS tyler,\n     MIN(youth) AS alien\n   GROUP BY\n     office\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"logic",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hearth,\n    galaxy,\n    guide,\n    taste,\n    grass\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    hearth,\n    galaxy,\n    SUM(grass) AS unrest\n  FROM cte_0\n  GROUP BY\n    hearth,\n    galaxy,\n    guide\n),\ncte_2 AS (\n  SELECT\n    hearth,\n    galaxy,\n    unrest\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    hearth,\n    galaxy,\n    AVG(unrest) AS nether,\n    MAX(unrest) AS jones,\n    COUNT(unrest) AS archie,\n    MIN(unrest) AS heaven,\n    MAX(clinic) AS litter,\n    SUM(unrest) AS horse\n  FROM cte_2\n  GROUP BY\n    hearth,\n    galaxy\n),\ncte_4 AS (\n  SELECT\n    SUM(heaven) AS tonic\n  FROM cte_3\n  GROUP BY\n    hearth,\n    galaxy\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hearth,\n     galaxy,\n     guide,\n     taste,\n     grass\n|> AGGREGATE\n     SUM(grass) AS unrest\n   GROUP BY\n     hearth,\n     galaxy,\n     guide\n|> SELECT\n     hearth,\n     galaxy,\n     unrest\n|> AGGREGATE\n     AVG(unrest) AS nether,\n     MAX(unrest) AS jones,\n     COUNT(unrest) AS archie,\n     MIN(unrest) AS heaven,\n     MAX(clinic) AS litter,\n     SUM(unrest) AS horse\n   GROUP BY\n     hearth,\n     galaxy\n|> AGGREGATE\n     SUM(heaven) AS tonic\n   GROUP BY\n     hearth,\n     galaxy\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"clinic",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    market,\n    heath,\n    import,\n    silver,\n    stool\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    market,\n    heath,\n    import,\n    COUNT(stool) AS trail,\n    COUNT(silver) AS odour,\n    MAX(silver) AS scene,\n    SUM(stool) AS muddle,\n    AVG(silver) AS cheese\n  FROM cte_0\n  GROUP BY\n    market,\n    heath,\n    import\n),\ncte_2 AS (\n  SELECT\n    market,\n    heath,\n    import\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    market,\n    COUNT(chant) AS taboo,\n    AVG(import) AS powder,\n    MAX(heath) AS ballet\n  FROM cte_2\n  GROUP BY\n    market\n),\ncte_4 AS (\n  SELECT\n    market,\n    powder,\n    ballet\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     market,\n     heath,\n     import,\n     silver,\n     stool\n|> AGGREGATE\n     COUNT(stool) AS trail,\n     COUNT(silver) AS odour,\n     MAX(silver) AS scene,\n     SUM(stool) AS muddle,\n     AVG(silver) AS cheese\n   GROUP BY\n     market,\n     heath,\n     import\n|> SELECT\n     market,\n     heath,\n     import\n|> AGGREGATE\n     COUNT(chant) AS taboo,\n     AVG(import) AS powder,\n     MAX(heath) AS ballet\n   GROUP BY\n     market\n|> SELECT\n     market,\n     powder,\n     ballet\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"chant",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    adult,\n    blood,\n    prize,\n    attic,\n    flick\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    adult,\n    SUM(flick) AS shrug\n  FROM cte_0\n  GROUP BY\n    adult,\n    blood,\n    prize\n),\ncte_2 AS (\n  SELECT\n    MAX(shrug) AS mickey,\n    AVG(shrug) AS robot,\n    MIN(shrug) AS reflex,\n    COUNT(shrug) AS trial,\n    SUM(shrug) AS covent\n  FROM cte_1\n  GROUP BY\n    adult\n),\ncte_3 AS (\n  SELECT\n    mickey,\n    robot,\n    reflex,\n    SUM(plane) AS bidder,\n    COUNT(covent) AS delay\n  FROM cte_2\n  GROUP BY\n    mickey,\n    robot,\n    reflex,\n    trial\n),\ncte_4 AS (\n  SELECT\n    MAX(delay) AS bronze\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     adult,\n     blood,\n     prize,\n     attic,\n     flick\n|> AGGREGATE\n     SUM(flick) AS shrug\n   GROUP BY\n     adult,\n     blood,\n     prize\n|> AGGREGATE\n     MAX(shrug) AS mickey,\n     AVG(shrug) AS robot,\n     MIN(shrug) AS reflex,\n     COUNT(shrug) AS trial,\n     SUM(shrug) AS covent\n   GROUP BY\n     adult\n|> AGGREGATE\n     SUM(plane) AS bidder,\n     COUNT(covent) AS delay\n   GROUP BY\n     mickey,\n     robot,\n     reflex,\n     trial\n|> AGGREGATE\n     MAX(delay) AS bronze\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"plane",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    entry,\n    valve,\n    flute,\n    shrug,\n    octave\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    entry,\n    valve,\n    SUM(octave) AS review\n  FROM cte_0\n  GROUP BY\n    entry,\n    valve,\n    flute,\n    shrug\n),\ncte_2 AS (\n  SELECT\n    entry,\n    valve,\n    MIN(review) AS energy,\n    AVG(review) AS buyer,\n    COUNT(review) AS bottom,\n    SUM(review) AS tower,\n    MAX(review) AS model,\n    COUNT(matron) AS whisky\n  FROM cte_1\n  GROUP BY\n    entry,\n    valve\n),\ncte_3 AS (\n  SELECT\n    entry,\n    valve,\n    energy\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    entry,\n    valve,\n    energy\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     entry,\n     valve,\n     flute,\n     shrug,\n     octave\n|> AGGREGATE\n     SUM(octave) AS review\n   GROUP BY\n     entry,\n     valve,\n     flute,\n     shrug\n|> AGGREGATE\n     MIN(review) AS energy,\n     AVG(review) AS buyer,\n     COUNT(review) AS bottom,\n     SUM(review) AS tower,\n     MAX(review) AS model,\n     COUNT(matron) AS whisky\n   GROUP BY\n     entry,\n     valve\n|> SELECT\n     entry,\n     valve,\n     energy\n|> SELECT\n     entry,\n     valve,\n     energy\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"matron",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    gallon,\n    strap,\n    outlet,\n    sexism,\n    beard\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(sexism) AS genre,\n    SUM(sexism) AS birth\n  FROM cte_0\n  GROUP BY\n    gallon\n),\ncte_2 AS (\n  SELECT\n    genre,\n    SUM(birth) AS bench,\n    MAX(birth) AS unease,\n    AVG(birth) AS benny,\n    COUNT(birth) AS puppet\n  FROM cte_1\n  GROUP BY\n    genre\n),\ncte_3 AS (\n  SELECT\n    genre,\n    bench,\n    unease,\n    AVG(puppet) AS apollo,\n    AVG(guilt) AS prose\n  FROM cte_2\n  GROUP BY\n    genre,\n    bench,\n    unease,\n    benny\n),\ncte_4 AS (\n  SELECT\n    genre,\n    bench,\n    unease,\n    apollo\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     gallon,\n     strap,\n     outlet,\n     sexism,\n     beard\n|> AGGREGATE\n     MAX(sexism) AS genre,\n     SUM(sexism) AS birth\n   GROUP BY\n     gallon\n|> AGGREGATE\n     SUM(birth) AS bench,\n     MAX(birth) AS unease,\n     AVG(birth) AS benny,\n     COUNT(birth) AS puppet\n   GROUP BY\n     genre\n|> AGGREGATE\n     AVG(puppet) AS apollo,\n     AVG(guilt) AS prose\n   GROUP BY\n     genre,\n     bench,\n     unease,\n     benny\n|> SELECT\n     genre,\n     bench,\n     unease,\n     apollo\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"guilt",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    drawer,\n    rocket,\n    trench,\n    nanny,\n    liner\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    drawer,\n    rocket,\n    trench,\n    MAX(nanny) AS throne\n  FROM cte_0\n  GROUP BY\n    drawer,\n    rocket,\n    trench\n),\ncte_2 AS (\n  SELECT\n    MAX(throne) AS burrow\n  FROM cte_1\n  GROUP BY\n    drawer,\n    rocket,\n    trench\n),\ncte_3 AS (\n  SELECT\n    AVG(burrow) AS frame,\n    MAX(burrow) AS editor,\n    SUM(burrow) AS custom\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    frame,\n    MIN(paddy) AS tender,\n    COUNT(custom) AS pirate,\n    AVG(custom) AS demise,\n    SUM(custom) AS stream\n  FROM cte_3\n  GROUP BY\n    frame,\n    editor\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     drawer,\n     rocket,\n     trench,\n     nanny,\n     liner\n|> AGGREGATE\n     MAX(nanny) AS throne\n   GROUP BY\n     drawer,\n     rocket,\n     trench\n|> AGGREGATE\n     MAX(throne) AS burrow\n   GROUP BY\n     drawer,\n     rocket,\n     trench\n|> AGGREGATE\n     AVG(burrow) AS frame,\n     MAX(burrow) AS editor,\n     SUM(burrow) AS custom\n|> AGGREGATE\n     MIN(paddy) AS tender,\n     COUNT(custom) AS pirate,\n     AVG(custom) AS demise,\n     SUM(custom) AS stream\n   GROUP BY\n     frame,\n     editor\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"paddy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ordeal,\n    ransom,\n    pilot,\n    summit,\n    tunnel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(tunnel) AS bible,\n    SUM(summit) AS chaos,\n    COUNT(summit) AS warsaw\n  FROM cte_0\n  GROUP BY\n    ordeal,\n    ransom\n),\ncte_2 AS (\n  SELECT\n    bible,\n    chaos,\n    SUM(warsaw) AS livery\n  FROM cte_1\n  GROUP BY\n    bible,\n    chaos\n),\ncte_3 AS (\n  SELECT\n    bible,\n    chaos,\n    MIN(livery) AS flask,\n    SUM(livery) AS harper,\n    AVG(candle) AS strip,\n    MAX(livery) AS enzyme\n  FROM cte_2\n  GROUP BY\n    bible,\n    chaos\n),\ncte_4 AS (\n  SELECT\n    COUNT(flask) AS sultan\n  FROM cte_3\n  GROUP BY\n    bible,\n    chaos\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ordeal,\n     ransom,\n     pilot,\n     summit,\n     tunnel\n|> AGGREGATE\n     AVG(tunnel) AS bible,\n     SUM(summit) AS chaos,\n     COUNT(summit) AS warsaw\n   GROUP BY\n     ordeal,\n     ransom\n|> AGGREGATE\n     SUM(warsaw) AS livery\n   GROUP BY\n     bible,\n     chaos\n|> AGGREGATE\n     MIN(livery) AS flask,\n     SUM(livery) AS harper,\n     AVG(candle) AS strip,\n     MAX(livery) AS enzyme\n   GROUP BY\n     bible,\n     chaos\n|> AGGREGATE\n     COUNT(flask) AS sultan\n   GROUP BY\n     bible,\n     chaos\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"candle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    vogue,\n    weapon,\n    metre,\n    rector,\n    jasper\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    vogue,\n    weapon,\n    metre,\n    rector,\n    COUNT(jasper) AS tower\n  FROM cte_0\n  GROUP BY\n    vogue,\n    weapon,\n    metre,\n    rector\n),\ncte_2 AS (\n  SELECT\n    vogue,\n    weapon,\n    metre,\n    rector\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(metre) AS bloke,\n    MAX(rector) AS arcade,\n    AVG(metre) AS bottom,\n    MIN(offer) AS access\n  FROM cte_2\n  GROUP BY\n    vogue,\n    weapon\n),\ncte_4 AS (\n  SELECT\n    MIN(bottom) AS garage,\n    MAX(bottom) AS square\n  FROM cte_3\n  GROUP BY\n    bloke,\n    arcade\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     vogue,\n     weapon,\n     metre,\n     rector,\n     jasper\n|> AGGREGATE\n     COUNT(jasper) AS tower\n   GROUP BY\n     vogue,\n     weapon,\n     metre,\n     rector\n|> SELECT\n     vogue,\n     weapon,\n     metre,\n     rector\n|> AGGREGATE\n     MAX(metre) AS bloke,\n     MAX(rector) AS arcade,\n     AVG(metre) AS bottom,\n     MIN(offer) AS access\n   GROUP BY\n     vogue,\n     weapon\n|> AGGREGATE\n     MIN(bottom) AS garage,\n     MAX(bottom) AS square\n   GROUP BY\n     bloke,\n     arcade\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"offer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rebate,\n    bruce,\n    token,\n    daniel,\n    donna\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    rebate,\n    bruce,\n    MAX(donna) AS hazard,\n    SUM(donna) AS lough,\n    MIN(donna) AS smile,\n    AVG(donna) AS peach\n  FROM cte_0\n  GROUP BY\n    rebate,\n    bruce,\n    token,\n    daniel\n),\ncte_2 AS (\n  SELECT\n    rebate,\n    bruce,\n    AVG(peach) AS scent\n  FROM cte_1\n  GROUP BY\n    rebate,\n    bruce\n),\ncte_3 AS (\n  SELECT\n    MAX(hedge) AS north,\n    SUM(scent) AS advent,\n    MIN(scent) AS ordeal\n  FROM cte_2\n  GROUP BY\n    rebate,\n    bruce\n),\ncte_4 AS (\n  SELECT\n    advent,\n    ordeal\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rebate,\n     bruce,\n     token,\n     daniel,\n     donna\n|> AGGREGATE\n     MAX(donna) AS hazard,\n     SUM(donna) AS lough,\n     MIN(donna) AS smile,\n     AVG(donna) AS peach\n   GROUP BY\n     rebate,\n     bruce,\n     token,\n     daniel\n|> AGGREGATE\n     AVG(peach) AS scent\n   GROUP BY\n     rebate,\n     bruce\n|> AGGREGATE\n     MAX(hedge) AS north,\n     SUM(scent) AS advent,\n     MIN(scent) AS ordeal\n   GROUP BY\n     rebate,\n     bruce\n|> SELECT\n     advent,\n     ordeal\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"hedge",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    divide,\n    canvas,\n    chess,\n    abbey,\n    laura\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    divide,\n    MIN(abbey) AS heart,\n    MAX(abbey) AS ruler\n  FROM cte_0\n  GROUP BY\n    divide\n),\ncte_2 AS (\n  SELECT\n    SUM(heart) AS wright,\n    SUM(ruler) AS amelia,\n    MAX(ruler) AS porter,\n    MIN(heart) AS berth,\n    AVG(heart) AS palmer\n  FROM cte_1\n  GROUP BY\n    divide\n),\ncte_3 AS (\n  SELECT\n    wright,\n    amelia,\n    porter,\n    MAX(zenith) AS cycle,\n    MIN(palmer) AS pigeon\n  FROM cte_2\n  GROUP BY\n    wright,\n    amelia,\n    porter\n),\ncte_4 AS (\n  SELECT\n    wright,\n    amelia,\n    porter\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     divide,\n     canvas,\n     chess,\n     abbey,\n     laura\n|> AGGREGATE\n     MIN(abbey) AS heart,\n     MAX(abbey) AS ruler\n   GROUP BY\n     divide\n|> AGGREGATE\n     SUM(heart) AS wright,\n     SUM(ruler) AS amelia,\n     MAX(ruler) AS porter,\n     MIN(heart) AS berth,\n     AVG(heart) AS palmer\n   GROUP BY\n     divide\n|> AGGREGATE\n     MAX(zenith) AS cycle,\n     MIN(palmer) AS pigeon\n   GROUP BY\n     wright,\n     amelia,\n     porter\n|> SELECT\n     wright,\n     amelia,\n     porter\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"zenith",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ransom,\n    bowie,\n    arson,\n    tutor,\n    banana\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    ransom,\n    bowie,\n    arson,\n    tutor,\n    MAX(banana) AS altar\n  FROM cte_0\n  GROUP BY\n    ransom,\n    bowie,\n    arson,\n    tutor\n),\ncte_2 AS (\n  SELECT\n    MAX(altar) AS sweat,\n    COUNT(altar) AS course,\n    MIN(altar) AS argyll\n  FROM cte_1\n  GROUP BY\n    ransom,\n    bowie\n),\ncte_3 AS (\n  SELECT\n    AVG(argyll) AS start,\n    MIN(stair) AS bucket\n  FROM cte_2\n  GROUP BY\n    sweat,\n    course\n),\ncte_4 AS (\n  SELECT\n    SUM(start) AS blaze,\n    MAX(start) AS harrow,\n    MIN(start) AS joanna\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ransom,\n     bowie,\n     arson,\n     tutor,\n     banana\n|> AGGREGATE\n     MAX(banana) AS altar\n   GROUP BY\n     ransom,\n     bowie,\n     arson,\n     tutor\n|> AGGREGATE\n     MAX(altar) AS sweat,\n     COUNT(altar) AS course,\n     MIN(altar) AS argyll\n   GROUP BY\n     ransom,\n     bowie\n|> AGGREGATE\n     AVG(argyll) AS start,\n     MIN(stair) AS bucket\n   GROUP BY\n     sweat,\n     course\n|> AGGREGATE\n     SUM(start) AS blaze,\n     MAX(start) AS harrow,\n     MIN(start) AS joanna\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"stair",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    castle,\n    lawson,\n    spoon,\n    median,\n    gamma\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(castle) AS bowel,\n    MIN(lawson) AS baron,\n    MAX(gamma) AS tallis,\n    MAX(castle) AS member,\n    AVG(spoon) AS strain,\n    COUNT(spoon) AS share\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    bowel,\n    baron,\n    COUNT(strain) AS piece\n  FROM cte_1\n  GROUP BY\n    bowel,\n    baron\n),\ncte_3 AS (\n  SELECT\n    bowel,\n    baron,\n    SUM(meadow) AS smile,\n    COUNT(piece) AS pulse\n  FROM cte_2\n  GROUP BY\n    bowel,\n    baron\n),\ncte_4 AS (\n  SELECT\n    bowel,\n    MAX(pulse) AS tarmac\n  FROM cte_3\n  GROUP BY\n    bowel\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     castle,\n     lawson,\n     spoon,\n     median,\n     gamma\n|> AGGREGATE\n     AVG(castle) AS bowel,\n     MIN(lawson) AS baron,\n     MAX(gamma) AS tallis,\n     MAX(castle) AS member,\n     AVG(spoon) AS strain,\n     COUNT(spoon) AS share\n|> AGGREGATE\n     COUNT(strain) AS piece\n   GROUP BY\n     bowel,\n     baron\n|> AGGREGATE\n     SUM(meadow) AS smile,\n     COUNT(piece) AS pulse\n   GROUP BY\n     bowel,\n     baron\n|> AGGREGATE\n     MAX(pulse) AS tarmac\n   GROUP BY\n     bowel\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"meadow",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    mousse,\n    decay,\n    plank,\n    other,\n    mummy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    mousse,\n    COUNT(decay) AS shell,\n    COUNT(plank) AS mosque,\n    MIN(plank) AS priest,\n    MIN(mummy) AS report\n  FROM cte_0\n  GROUP BY\n    mousse\n),\ncte_2 AS (\n  SELECT\n    mousse,\n    shell,\n    SUM(report) AS burgh,\n    MAX(report) AS clerk\n  FROM cte_1\n  GROUP BY\n    mousse,\n    shell,\n    mosque\n),\ncte_3 AS (\n  SELECT\n    mousse,\n    AVG(swift) AS throat,\n    SUM(clerk) AS singer\n  FROM cte_2\n  GROUP BY\n    mousse,\n    shell\n),\ncte_4 AS (\n  SELECT\n    AVG(mousse) AS tiller,\n    SUM(mousse) AS stoke\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     mousse,\n     decay,\n     plank,\n     other,\n     mummy\n|> AGGREGATE\n     COUNT(decay) AS shell,\n     COUNT(plank) AS mosque,\n     MIN(plank) AS priest,\n     MIN(mummy) AS report\n   GROUP BY\n     mousse\n|> AGGREGATE\n     SUM(report) AS burgh,\n     MAX(report) AS clerk\n   GROUP BY\n     mousse,\n     shell,\n     mosque\n|> AGGREGATE\n     AVG(swift) AS throat,\n     SUM(clerk) AS singer\n   GROUP BY\n     mousse,\n     shell\n|> AGGREGATE\n     AVG(mousse) AS tiller,\n     SUM(mousse) AS stoke\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"swift",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    manual,\n    pedal,\n    ditch,\n    access,\n    teapot\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    manual,\n    pedal,\n    MAX(teapot) AS anchor\n  FROM cte_0\n  GROUP BY\n    manual,\n    pedal,\n    ditch\n),\ncte_2 AS (\n  SELECT\n    manual,\n    MAX(anchor) AS report,\n    COUNT(anchor) AS banner,\n    AVG(anchor) AS hooker\n  FROM cte_1\n  GROUP BY\n    manual\n),\ncte_3 AS (\n  SELECT\n    manual,\n    AVG(banner) AS murphy,\n    SUM(banner) AS intent,\n    MIN(spouse) AS darwin,\n    COUNT(hooker) AS storey,\n    SUM(hooker) AS animal\n  FROM cte_2\n  GROUP BY\n    manual,\n    report\n),\ncte_4 AS (\n  SELECT\n    manual,\n    murphy\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     manual,\n     pedal,\n     ditch,\n     access,\n     teapot\n|> AGGREGATE\n     MAX(teapot) AS anchor\n   GROUP BY\n     manual,\n     pedal,\n     ditch\n|> AGGREGATE\n     MAX(anchor) AS report,\n     COUNT(anchor) AS banner,\n     AVG(anchor) AS hooker\n   GROUP BY\n     manual\n|> AGGREGATE\n     AVG(banner) AS murphy,\n     SUM(banner) AS intent,\n     MIN(spouse) AS darwin,\n     COUNT(hooker) AS storey,\n     SUM(hooker) AS animal\n   GROUP BY\n     manual,\n     report\n|> SELECT\n     manual,\n     murphy\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"spouse",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    assent,\n    venom,\n    glory,\n    stern,\n    replay\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    assent,\n    COUNT(glory) AS shaft,\n    COUNT(stern) AS satin,\n    AVG(stern) AS mound,\n    SUM(glory) AS michel,\n    MAX(replay) AS hurdle\n  FROM cte_0\n  GROUP BY\n    assent,\n    venom\n),\ncte_2 AS (\n  SELECT\n    assent,\n    shaft,\n    MIN(satin) AS reply\n  FROM cte_1\n  GROUP BY\n    assent,\n    shaft\n),\ncte_3 AS (\n  SELECT\n    assent,\n    COUNT(campus) AS digit,\n    SUM(reply) AS safari,\n    COUNT(reply) AS rally\n  FROM cte_2\n  GROUP BY\n    assent,\n    shaft\n),\ncte_4 AS (\n  SELECT\n    AVG(rally) AS booth\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     assent,\n     venom,\n     glory,\n     stern,\n     replay\n|> AGGREGATE\n     COUNT(glory) AS shaft,\n     COUNT(stern) AS satin,\n     AVG(stern) AS mound,\n     SUM(glory) AS michel,\n     MAX(replay) AS hurdle\n   GROUP BY\n     assent,\n     venom\n|> AGGREGATE\n     MIN(satin) AS reply\n   GROUP BY\n     assent,\n     shaft\n|> AGGREGATE\n     COUNT(campus) AS digit,\n     SUM(reply) AS safari,\n     COUNT(reply) AS rally\n   GROUP BY\n     assent,\n     shaft\n|> AGGREGATE\n     AVG(rally) AS booth\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"campus",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    novice,\n    stain,\n    ridley,\n    misery,\n    minute\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    novice,\n    stain,\n    ridley,\n    misery,\n    MIN(minute) AS stable\n  FROM cte_0\n  GROUP BY\n    novice,\n    stain,\n    ridley,\n    misery\n),\ncte_2 AS (\n  SELECT\n    AVG(misery) AS crisp\n  FROM cte_1\n  GROUP BY\n    novice,\n    stain\n),\ncte_3 AS (\n  SELECT\n    AVG(crisp) AS patio,\n    MIN(crisp) AS orange,\n    COUNT(crisp) AS gender,\n    AVG(yellow) AS crater,\n    SUM(crisp) AS japan,\n    MAX(crisp) AS cruise\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(cruise) AS jacket,\n    SUM(gender) AS donna\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     novice,\n     stain,\n     ridley,\n     misery,\n     minute\n|> AGGREGATE\n     MIN(minute) AS stable\n   GROUP BY\n     novice,\n     stain,\n     ridley,\n     misery\n|> AGGREGATE\n     AVG(misery) AS crisp\n   GROUP BY\n     novice,\n     stain\n|> AGGREGATE\n     AVG(crisp) AS patio,\n     MIN(crisp) AS orange,\n     COUNT(crisp) AS gender,\n     AVG(yellow) AS crater,\n     SUM(crisp) AS japan,\n     MAX(crisp) AS cruise\n|> AGGREGATE\n     AVG(cruise) AS jacket,\n     SUM(gender) AS donna\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"yellow",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    valley,\n    comma,\n    status,\n    marrow,\n    lough\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    valley,\n    comma,\n    MAX(marrow) AS yellow,\n    AVG(status) AS graph,\n    SUM(marrow) AS glover,\n    SUM(status) AS elite\n  FROM cte_0\n  GROUP BY\n    valley,\n    comma\n),\ncte_2 AS (\n  SELECT\n    valley,\n    comma,\n    yellow,\n    graph\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    valley,\n    comma,\n    COUNT(graph) AS derry,\n    SUM(brute) AS chile,\n    MAX(graph) AS secret\n  FROM cte_2\n  GROUP BY\n    valley,\n    comma\n),\ncte_4 AS (\n  SELECT\n    MAX(derry) AS coral,\n    MIN(derry) AS viking\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     valley,\n     comma,\n     status,\n     marrow,\n     lough\n|> AGGREGATE\n     MAX(marrow) AS yellow,\n     AVG(status) AS graph,\n     SUM(marrow) AS glover,\n     SUM(status) AS elite\n   GROUP BY\n     valley,\n     comma\n|> SELECT\n     valley,\n     comma,\n     yellow,\n     graph\n|> AGGREGATE\n     COUNT(graph) AS derry,\n     SUM(brute) AS chile,\n     MAX(graph) AS secret\n   GROUP BY\n     valley,\n     comma\n|> AGGREGATE\n     MAX(derry) AS coral,\n     MIN(derry) AS viking\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"brute",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    fiasco,\n    metro,\n    ridge,\n    river,\n    voter\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    fiasco,\n    metro,\n    ridge,\n    river,\n    MIN(voter) AS stage,\n    MAX(voter) AS canary\n  FROM cte_0\n  GROUP BY\n    fiasco,\n    metro,\n    ridge,\n    river\n),\ncte_2 AS (\n  SELECT\n    SUM(ridge) AS fringe,\n    COUNT(river) AS slide,\n    AVG(ridge) AS drain\n  FROM cte_1\n  GROUP BY\n    fiasco,\n    metro\n),\ncte_3 AS (\n  SELECT\n    SUM(parody) AS fulham,\n    AVG(drain) AS lapse\n  FROM cte_2\n  GROUP BY\n    fringe,\n    slide\n),\ncte_4 AS (\n  SELECT\n    MIN(lapse) AS point,\n    COUNT(lapse) AS joint\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     fiasco,\n     metro,\n     ridge,\n     river,\n     voter\n|> AGGREGATE\n     MIN(voter) AS stage,\n     MAX(voter) AS canary\n   GROUP BY\n     fiasco,\n     metro,\n     ridge,\n     river\n|> AGGREGATE\n     SUM(ridge) AS fringe,\n     COUNT(river) AS slide,\n     AVG(ridge) AS drain\n   GROUP BY\n     fiasco,\n     metro\n|> AGGREGATE\n     SUM(parody) AS fulham,\n     AVG(drain) AS lapse\n   GROUP BY\n     fringe,\n     slide\n|> AGGREGATE\n     MIN(lapse) AS point,\n     COUNT(lapse) AS joint\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"parody",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    store,\n    sting,\n    nerve,\n    mouse,\n    muddle\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    store,\n    sting,\n    nerve,\n    MAX(mouse) AS fender,\n    SUM(mouse) AS ascent\n  FROM cte_0\n  GROUP BY\n    store,\n    sting,\n    nerve\n),\ncte_2 AS (\n  SELECT\n    store,\n    sting,\n    nerve\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    store,\n    SUM(nerve) AS jewel,\n    MIN(sting) AS wheel,\n    MAX(sting) AS aroma,\n    SUM(kelly) AS phone,\n    COUNT(nerve) AS month\n  FROM cte_2\n  GROUP BY\n    store\n),\ncte_4 AS (\n  SELECT\n    store,\n    MIN(month) AS pollen\n  FROM cte_3\n  GROUP BY\n    store,\n    jewel\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     store,\n     sting,\n     nerve,\n     mouse,\n     muddle\n|> AGGREGATE\n     MAX(mouse) AS fender,\n     SUM(mouse) AS ascent\n   GROUP BY\n     store,\n     sting,\n     nerve\n|> SELECT\n     store,\n     sting,\n     nerve\n|> AGGREGATE\n     SUM(nerve) AS jewel,\n     MIN(sting) AS wheel,\n     MAX(sting) AS aroma,\n     SUM(kelly) AS phone,\n     COUNT(nerve) AS month\n   GROUP BY\n     store\n|> AGGREGATE\n     MIN(month) AS pollen\n   GROUP BY\n     store,\n     jewel\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"kelly",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    plunge,\n    relief,\n    effect,\n    basket,\n    cattle\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    plunge,\n    relief,\n    SUM(basket) AS claret,\n    AVG(cattle) AS credit,\n    AVG(basket) AS norman,\n    MAX(basket) AS midday\n  FROM cte_0\n  GROUP BY\n    plunge,\n    relief,\n    effect\n),\ncte_2 AS (\n  SELECT\n    plunge,\n    relief,\n    claret,\n    credit,\n    norman\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    plunge,\n    relief,\n    COUNT(soccer) AS beast,\n    AVG(norman) AS insect\n  FROM cte_2\n  GROUP BY\n    plunge,\n    relief\n),\ncte_4 AS (\n  SELECT\n    MIN(insect) AS planet\n  FROM cte_3\n  GROUP BY\n    plunge,\n    relief\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     plunge,\n     relief,\n     effect,\n     basket,\n     cattle\n|> AGGREGATE\n     SUM(basket) AS claret,\n     AVG(cattle) AS credit,\n     AVG(basket) AS norman,\n     MAX(basket) AS midday\n   GROUP BY\n     plunge,\n     relief,\n     effect\n|> SELECT\n     plunge,\n     relief,\n     claret,\n     credit,\n     norman\n|> AGGREGATE\n     COUNT(soccer) AS beast,\n     AVG(norman) AS insect\n   GROUP BY\n     plunge,\n     relief\n|> AGGREGATE\n     MIN(insect) AS planet\n   GROUP BY\n     plunge,\n     relief\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"soccer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    crust,\n    press,\n    piety,\n    proxy,\n    minus\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    crust,\n    AVG(proxy) AS empire,\n    AVG(minus) AS north\n  FROM cte_0\n  GROUP BY\n    crust,\n    press,\n    piety\n),\ncte_2 AS (\n  SELECT\n    crust,\n    SUM(north) AS guild,\n    AVG(north) AS chair\n  FROM cte_1\n  GROUP BY\n    crust\n),\ncte_3 AS (\n  SELECT\n    crust,\n    guild,\n    MIN(chair) AS slide\n  FROM cte_2\n  GROUP BY\n    crust,\n    guild\n),\ncte_4 AS (\n  SELECT\n    MIN(strand) AS appeal,\n    COUNT(slide) AS craig,\n    MIN(slide) AS throne,\n    SUM(guild) AS shire,\n    MAX(guild) AS deacon\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     crust,\n     press,\n     piety,\n     proxy,\n     minus\n|> AGGREGATE\n     AVG(proxy) AS empire,\n     AVG(minus) AS north\n   GROUP BY\n     crust,\n     press,\n     piety\n|> AGGREGATE\n     SUM(north) AS guild,\n     AVG(north) AS chair\n   GROUP BY\n     crust\n|> AGGREGATE\n     MIN(chair) AS slide\n   GROUP BY\n     crust,\n     guild\n|> AGGREGATE\n     MIN(strand) AS appeal,\n     COUNT(slide) AS craig,\n     MIN(slide) AS throne,\n     SUM(guild) AS shire,\n     MAX(guild) AS deacon\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"strand",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:43,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rubble,\n    sword,\n    laurel,\n    hopper,\n    steam\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    rubble,\n    sword,\n    laurel,\n    SUM(steam) AS notice\n  FROM cte_0\n  GROUP BY\n    rubble,\n    sword,\n    laurel,\n    hopper\n),\ncte_2 AS (\n  SELECT\n    rubble,\n    MAX(notice) AS slogan\n  FROM cte_1\n  GROUP BY\n    rubble,\n    sword\n),\ncte_3 AS (\n  SELECT\n    COUNT(rubble) AS bully,\n    SUM(rubble) AS proxy,\n    COUNT(slogan) AS shoal,\n    AVG(mantle) AS script,\n    MAX(slogan) AS writer\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(writer) AS breeze,\n    AVG(writer) AS globe\n  FROM cte_3\n  GROUP BY\n    bully,\n    proxy\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rubble,\n     sword,\n     laurel,\n     hopper,\n     steam\n|> AGGREGATE\n     SUM(steam) AS notice\n   GROUP BY\n     rubble,\n     sword,\n     laurel,\n     hopper\n|> AGGREGATE\n     MAX(notice) AS slogan\n   GROUP BY\n     rubble,\n     sword\n|> AGGREGATE\n     COUNT(rubble) AS bully,\n     SUM(rubble) AS proxy,\n     COUNT(slogan) AS shoal,\n     AVG(mantle) AS script,\n     MAX(slogan) AS writer\n|> AGGREGATE\n     MAX(writer) AS breeze,\n     AVG(writer) AS globe\n   GROUP BY\n     bully,\n     proxy\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"mantle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    virus,\n    storey,\n    ballet,\n    lunch,\n    recess\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(ballet) AS fever,\n    COUNT(recess) AS habit,\n    COUNT(lunch) AS vogue,\n    MAX(recess) AS mobile,\n    MAX(lunch) AS notion\n  FROM cte_0\n  GROUP BY\n    virus,\n    storey\n),\ncte_2 AS (\n  SELECT\n    fever,\n    habit,\n    vogue,\n    mobile,\n    MIN(notion) AS stump,\n    MAX(notion) AS stoke,\n    COUNT(haven) AS wally\n  FROM cte_1\n  GROUP BY\n    fever,\n    habit,\n    vogue,\n    mobile\n),\ncte_3 AS (\n  SELECT\n    fever,\n    habit\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(habit) AS wharf\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     virus,\n     storey,\n     ballet,\n     lunch,\n     recess\n|> AGGREGATE\n     MAX(ballet) AS fever,\n     COUNT(recess) AS habit,\n     COUNT(lunch) AS vogue,\n     MAX(recess) AS mobile,\n     MAX(lunch) AS notion\n   GROUP BY\n     virus,\n     storey\n|> AGGREGATE\n     MIN(notion) AS stump,\n     MAX(notion) AS stoke,\n     COUNT(haven) AS wally\n   GROUP BY\n     fever,\n     habit,\n     vogue,\n     mobile\n|> SELECT\n     fever,\n     habit\n|> AGGREGATE\n     MIN(habit) AS wharf\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"haven",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    focus,\n    thumb,\n    newco,\n    whole,\n    gossip\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    focus,\n    thumb,\n    newco,\n    SUM(gossip) AS crook,\n    MAX(gossip) AS arrest\n  FROM cte_0\n  GROUP BY\n    focus,\n    thumb,\n    newco,\n    whole\n),\ncte_2 AS (\n  SELECT\n    MIN(crook) AS witch,\n    COUNT(arrest) AS shade,\n    COUNT(newco) AS fungus\n  FROM cte_1\n  GROUP BY\n    focus,\n    thumb\n),\ncte_3 AS (\n  SELECT\n    witch,\n    MAX(labour) AS height,\n    MIN(fungus) AS strife\n  FROM cte_2\n  GROUP BY\n    witch,\n    shade\n),\ncte_4 AS (\n  SELECT\n    MIN(witch) AS pencil,\n    MAX(strife) AS snail\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     focus,\n     thumb,\n     newco,\n     whole,\n     gossip\n|> AGGREGATE\n     SUM(gossip) AS crook,\n     MAX(gossip) AS arrest\n   GROUP BY\n     focus,\n     thumb,\n     newco,\n     whole\n|> AGGREGATE\n     MIN(crook) AS witch,\n     COUNT(arrest) AS shade,\n     COUNT(newco) AS fungus\n   GROUP BY\n     focus,\n     thumb\n|> AGGREGATE\n     MAX(labour) AS height,\n     MIN(fungus) AS strife\n   GROUP BY\n     witch,\n     shade\n|> AGGREGATE\n     MIN(witch) AS pencil,\n     MAX(strife) AS snail\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"labour",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    school,\n    parcel,\n    bearer,\n    dough,\n    champ\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(school) AS draft,\n    AVG(bearer) AS penny,\n    COUNT(champ) AS nelson,\n    SUM(school) AS cycle,\n    MIN(parcel) AS clinic\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    draft,\n    penny,\n    nelson,\n    cycle,\n    MIN(clinic) AS gossip\n  FROM cte_1\n  GROUP BY\n    draft,\n    penny,\n    nelson,\n    cycle\n),\ncte_3 AS (\n  SELECT\n    COUNT(pepper) AS beast,\n    MAX(gossip) AS stake\n  FROM cte_2\n  GROUP BY\n    draft,\n    penny,\n    nelson,\n    cycle\n),\ncte_4 AS (\n  SELECT\n    SUM(stake) AS witney\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     school,\n     parcel,\n     bearer,\n     dough,\n     champ\n|> AGGREGATE\n     COUNT(school) AS draft,\n     AVG(bearer) AS penny,\n     COUNT(champ) AS nelson,\n     SUM(school) AS cycle,\n     MIN(parcel) AS clinic\n|> AGGREGATE\n     MIN(clinic) AS gossip\n   GROUP BY\n     draft,\n     penny,\n     nelson,\n     cycle\n|> AGGREGATE\n     COUNT(pepper) AS beast,\n     MAX(gossip) AS stake\n   GROUP BY\n     draft,\n     penny,\n     nelson,\n     cycle\n|> AGGREGATE\n     SUM(stake) AS witney\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"pepper",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    backup,\n    change,\n    noise,\n    radio,\n    bother\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    backup,\n    change,\n    MAX(bother) AS parson,\n    COUNT(bother) AS helmet\n  FROM cte_0\n  GROUP BY\n    backup,\n    change,\n    noise,\n    radio\n),\ncte_2 AS (\n  SELECT\n    backup,\n    change,\n    COUNT(helmet) AS pellet\n  FROM cte_1\n  GROUP BY\n    backup,\n    change\n),\ncte_3 AS (\n  SELECT\n    MIN(change) AS petrol,\n    COUNT(pellet) AS benny,\n    SUM(patio) AS period\n  FROM cte_2\n  GROUP BY\n    backup\n),\ncte_4 AS (\n  SELECT\n    COUNT(benny) AS ransom,\n    AVG(benny) AS filter,\n    MAX(benny) AS father\n  FROM cte_3\n  GROUP BY\n    petrol\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     backup,\n     change,\n     noise,\n     radio,\n     bother\n|> AGGREGATE\n     MAX(bother) AS parson,\n     COUNT(bother) AS helmet\n   GROUP BY\n     backup,\n     change,\n     noise,\n     radio\n|> AGGREGATE\n     COUNT(helmet) AS pellet\n   GROUP BY\n     backup,\n     change\n|> AGGREGATE\n     MIN(change) AS petrol,\n     COUNT(pellet) AS benny,\n     SUM(patio) AS period\n   GROUP BY\n     backup\n|> AGGREGATE\n     COUNT(benny) AS ransom,\n     AVG(benny) AS filter,\n     MAX(benny) AS father\n   GROUP BY\n     petrol\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"patio",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    flora,\n    stock,\n    shaikh,\n    waste,\n    branch\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    flora,\n    stock,\n    SUM(branch) AS canopy\n  FROM cte_0\n  GROUP BY\n    flora,\n    stock,\n    shaikh\n),\ncte_2 AS (\n  SELECT\n    flora,\n    stock,\n    canopy\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    flora,\n    stock,\n    COUNT(canopy) AS pistol,\n    MAX(canopy) AS murphy,\n    AVG(canopy) AS period,\n    SUM(canopy) AS guitar,\n    COUNT(libel) AS haven\n  FROM cte_2\n  GROUP BY\n    flora,\n    stock\n),\ncte_4 AS (\n  SELECT\n    flora,\n    AVG(guitar) AS adobe,\n    MIN(murphy) AS jetty\n  FROM cte_3\n  GROUP BY\n    flora\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     flora,\n     stock,\n     shaikh,\n     waste,\n     branch\n|> AGGREGATE\n     SUM(branch) AS canopy\n   GROUP BY\n     flora,\n     stock,\n     shaikh\n|> SELECT\n     flora,\n     stock,\n     canopy\n|> AGGREGATE\n     COUNT(canopy) AS pistol,\n     MAX(canopy) AS murphy,\n     AVG(canopy) AS period,\n     SUM(canopy) AS guitar,\n     COUNT(libel) AS haven\n   GROUP BY\n     flora,\n     stock\n|> AGGREGATE\n     AVG(guitar) AS adobe,\n     MIN(murphy) AS jetty\n   GROUP BY\n     flora\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"libel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ridge,\n    winger,\n    impact,\n    travis,\n    score\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    ridge,\n    winger,\n    impact,\n    MAX(score) AS lotus\n  FROM cte_0\n  GROUP BY\n    ridge,\n    winger,\n    impact\n),\ncte_2 AS (\n  SELECT\n    ridge,\n    COUNT(lotus) AS output,\n    SUM(lotus) AS field\n  FROM cte_1\n  GROUP BY\n    ridge,\n    winger,\n    impact\n),\ncte_3 AS (\n  SELECT\n    SUM(field) AS newton\n  FROM cte_2\n  GROUP BY\n    ridge,\n    output\n),\ncte_4 AS (\n  SELECT\n    COUNT(fender) AS yellow,\n    MIN(newton) AS march,\n    MAX(newton) AS radio,\n    AVG(newton) AS mirror,\n    COUNT(newton) AS crown\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ridge,\n     winger,\n     impact,\n     travis,\n     score\n|> AGGREGATE\n     MAX(score) AS lotus\n   GROUP BY\n     ridge,\n     winger,\n     impact\n|> AGGREGATE\n     COUNT(lotus) AS output,\n     SUM(lotus) AS field\n   GROUP BY\n     ridge,\n     winger,\n     impact\n|> AGGREGATE\n     SUM(field) AS newton\n   GROUP BY\n     ridge,\n     output\n|> AGGREGATE\n     COUNT(fender) AS yellow,\n     MIN(newton) AS march,\n     MAX(newton) AS radio,\n     AVG(newton) AS mirror,\n     COUNT(newton) AS crown\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"fender",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:44,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    weber,\n    feeder,\n    banana,\n    viewer,\n    ribbon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    weber,\n    feeder,\n    banana,\n    MIN(ribbon) AS piazza,\n    MAX(ribbon) AS winger,\n    SUM(ribbon) AS lover\n  FROM cte_0\n  GROUP BY\n    weber,\n    feeder,\n    banana,\n    viewer\n),\ncte_2 AS (\n  SELECT\n    weber,\n    MIN(feeder) AS shelf,\n    COUNT(winger) AS miner,\n    AVG(lover) AS desert,\n    MIN(singer) AS voter\n  FROM cte_1\n  GROUP BY\n    weber\n),\ncte_3 AS (\n  SELECT\n    SUM(weber) AS burke,\n    MAX(shelf) AS public\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(public) AS manual\n  FROM cte_3\n  GROUP BY\n    burke\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     weber,\n     feeder,\n     banana,\n     viewer,\n     ribbon\n|> AGGREGATE\n     MIN(ribbon) AS piazza,\n     MAX(ribbon) AS winger,\n     SUM(ribbon) AS lover\n   GROUP BY\n     weber,\n     feeder,\n     banana,\n     viewer\n|> AGGREGATE\n     MIN(feeder) AS shelf,\n     COUNT(winger) AS miner,\n     AVG(lover) AS desert,\n     MIN(singer) AS voter\n   GROUP BY\n     weber\n|> AGGREGATE\n     SUM(weber) AS burke,\n     MAX(shelf) AS public\n|> AGGREGATE\n     SUM(public) AS manual\n   GROUP BY\n     burke\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"singer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    newco,\n    psyche,\n    nausea,\n    decade,\n    quest\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    newco,\n    psyche,\n    nausea,\n    COUNT(quest) AS spring,\n    SUM(quest) AS smear\n  FROM cte_0\n  GROUP BY\n    newco,\n    psyche,\n    nausea,\n    decade\n),\ncte_2 AS (\n  SELECT\n    newco,\n    psyche,\n    nausea,\n    spring\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    newco,\n    SUM(nausea) AS short,\n    AVG(nausea) AS beech,\n    SUM(telly) AS devon,\n    AVG(spring) AS raffle\n  FROM cte_2\n  GROUP BY\n    newco,\n    psyche\n),\ncte_4 AS (\n  SELECT\n    newco,\n    MAX(short) AS worth\n  FROM cte_3\n  GROUP BY\n    newco\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     newco,\n     psyche,\n     nausea,\n     decade,\n     quest\n|> AGGREGATE\n     COUNT(quest) AS spring,\n     SUM(quest) AS smear\n   GROUP BY\n     newco,\n     psyche,\n     nausea,\n     decade\n|> SELECT\n     newco,\n     psyche,\n     nausea,\n     spring\n|> AGGREGATE\n     SUM(nausea) AS short,\n     AVG(nausea) AS beech,\n     SUM(telly) AS devon,\n     AVG(spring) AS raffle\n   GROUP BY\n     newco,\n     psyche\n|> AGGREGATE\n     MAX(short) AS worth\n   GROUP BY\n     newco\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"telly",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    parry,\n    movie,\n    vinyl,\n    backup,\n    butler\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    parry,\n    movie,\n    MAX(backup) AS favour\n  FROM cte_0\n  GROUP BY\n    parry,\n    movie\n),\ncte_2 AS (\n  SELECT\n    MIN(favour) AS locker\n  FROM cte_1\n  GROUP BY\n    parry,\n    movie\n),\ncte_3 AS (\n  SELECT\n    SUM(locker) AS escort,\n    COUNT(locker) AS moran,\n    MAX(locker) AS abbot\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    escort,\n    moran,\n    MAX(abbot) AS sanity,\n    SUM(dress) AS camel,\n    MIN(abbot) AS beauty,\n    SUM(abbot) AS potato,\n    AVG(abbot) AS vendor\n  FROM cte_3\n  GROUP BY\n    escort,\n    moran\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     parry,\n     movie,\n     vinyl,\n     backup,\n     butler\n|> AGGREGATE\n     MAX(backup) AS favour\n   GROUP BY\n     parry,\n     movie\n|> AGGREGATE\n     MIN(favour) AS locker\n   GROUP BY\n     parry,\n     movie\n|> AGGREGATE\n     SUM(locker) AS escort,\n     COUNT(locker) AS moran,\n     MAX(locker) AS abbot\n|> AGGREGATE\n     MAX(abbot) AS sanity,\n     SUM(dress) AS camel,\n     MIN(abbot) AS beauty,\n     SUM(abbot) AS potato,\n     AVG(abbot) AS vendor\n   GROUP BY\n     escort,\n     moran\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"dress",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    trauma,\n    blood,\n    winner,\n    pledge,\n    chair\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    trauma,\n    blood,\n    winner,\n    pledge,\n    MIN(chair) AS mafia,\n    SUM(chair) AS rigour,\n    AVG(chair) AS comma\n  FROM cte_0\n  GROUP BY\n    trauma,\n    blood,\n    winner,\n    pledge\n),\ncte_2 AS (\n  SELECT\n    MIN(pledge) AS munro,\n    MAX(winner) AS autumn,\n    MIN(blood) AS steven,\n    SUM(future) AS betty\n  FROM cte_1\n  GROUP BY\n    trauma\n),\ncte_3 AS (\n  SELECT\n    munro,\n    AVG(autumn) AS perry\n  FROM cte_2\n  GROUP BY\n    munro\n),\ncte_4 AS (\n  SELECT\n    COUNT(perry) AS hedge\n  FROM cte_3\n  GROUP BY\n    munro\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     trauma,\n     blood,\n     winner,\n     pledge,\n     chair\n|> AGGREGATE\n     MIN(chair) AS mafia,\n     SUM(chair) AS rigour,\n     AVG(chair) AS comma\n   GROUP BY\n     trauma,\n     blood,\n     winner,\n     pledge\n|> AGGREGATE\n     MIN(pledge) AS munro,\n     MAX(winner) AS autumn,\n     MIN(blood) AS steven,\n     SUM(future) AS betty\n   GROUP BY\n     trauma\n|> AGGREGATE\n     AVG(autumn) AS perry\n   GROUP BY\n     munro\n|> AGGREGATE\n     COUNT(perry) AS hedge\n   GROUP BY\n     munro\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"future",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    quartz,\n    office,\n    fright,\n    tissue,\n    tenant\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    quartz,\n    MAX(fright) AS piety\n  FROM cte_0\n  GROUP BY\n    quartz\n),\ncte_2 AS (\n  SELECT\n    quartz,\n    AVG(piety) AS saddle,\n    MIN(piety) AS throat,\n    SUM(piety) AS rumour,\n    COUNT(piety) AS singer,\n    MAX(piety) AS lapse\n  FROM cte_1\n  GROUP BY\n    quartz\n),\ncte_3 AS (\n  SELECT\n    quartz,\n    saddle,\n    COUNT(throat) AS carrot,\n    MAX(motto) AS pepper,\n    MIN(throat) AS wrong\n  FROM cte_2\n  GROUP BY\n    quartz,\n    saddle\n),\ncte_4 AS (\n  SELECT\n    quartz,\n    AVG(wrong) AS climax\n  FROM cte_3\n  GROUP BY\n    quartz\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     quartz,\n     office,\n     fright,\n     tissue,\n     tenant\n|> AGGREGATE\n     MAX(fright) AS piety\n   GROUP BY\n     quartz\n|> AGGREGATE\n     AVG(piety) AS saddle,\n     MIN(piety) AS throat,\n     SUM(piety) AS rumour,\n     COUNT(piety) AS singer,\n     MAX(piety) AS lapse\n   GROUP BY\n     quartz\n|> AGGREGATE\n     COUNT(throat) AS carrot,\n     MAX(motto) AS pepper,\n     MIN(throat) AS wrong\n   GROUP BY\n     quartz,\n     saddle\n|> AGGREGATE\n     AVG(wrong) AS climax\n   GROUP BY\n     quartz\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"motto",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    plough,\n    series,\n    summer,\n    honour,\n    fleece\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    plough,\n    series,\n    summer,\n    MIN(fleece) AS panel,\n    MAX(fleece) AS ozone,\n    COUNT(honour) AS hinge\n  FROM cte_0\n  GROUP BY\n    plough,\n    series,\n    summer\n),\ncte_2 AS (\n  SELECT\n    plough,\n    series,\n    summer,\n    panel,\n    ozone\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    plough,\n    MAX(summer) AS poison,\n    COUNT(dieter) AS creole,\n    MIN(ozone) AS clough\n  FROM cte_2\n  GROUP BY\n    plough,\n    series\n),\ncte_4 AS (\n  SELECT\n    plough,\n    MAX(poison) AS helium\n  FROM cte_3\n  GROUP BY\n    plough\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     plough,\n     series,\n     summer,\n     honour,\n     fleece\n|> AGGREGATE\n     MIN(fleece) AS panel,\n     MAX(fleece) AS ozone,\n     COUNT(honour) AS hinge\n   GROUP BY\n     plough,\n     series,\n     summer\n|> SELECT\n     plough,\n     series,\n     summer,\n     panel,\n     ozone\n|> AGGREGATE\n     MAX(summer) AS poison,\n     COUNT(dieter) AS creole,\n     MIN(ozone) AS clough\n   GROUP BY\n     plough,\n     series\n|> AGGREGATE\n     MAX(poison) AS helium\n   GROUP BY\n     plough\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"dieter",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    miller,\n    fowler,\n    vinyl,\n    millie,\n    fever\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    miller,\n    fowler,\n    vinyl,\n    millie,\n    AVG(fever) AS leigh,\n    MIN(fever) AS domain,\n    COUNT(fever) AS sight\n  FROM cte_0\n  GROUP BY\n    miller,\n    fowler,\n    vinyl,\n    millie\n),\ncte_2 AS (\n  SELECT\n    SUM(miller) AS satin\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(satin) AS member,\n    COUNT(satin) AS tutor,\n    AVG(trench) AS status,\n    MIN(satin) AS bench\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(bench) AS frenzy,\n    COUNT(tutor) AS blame,\n    COUNT(member) AS streak\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     miller,\n     fowler,\n     vinyl,\n     millie,\n     fever\n|> AGGREGATE\n     AVG(fever) AS leigh,\n     MIN(fever) AS domain,\n     COUNT(fever) AS sight\n   GROUP BY\n     miller,\n     fowler,\n     vinyl,\n     millie\n|> AGGREGATE\n     SUM(miller) AS satin\n|> AGGREGATE\n     MAX(satin) AS member,\n     COUNT(satin) AS tutor,\n     AVG(trench) AS status,\n     MIN(satin) AS bench\n|> AGGREGATE\n     MIN(bench) AS frenzy,\n     COUNT(tutor) AS blame,\n     COUNT(member) AS streak\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"trench",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    oscar,\n    dream,\n    savoy,\n    fiddle,\n    sewage\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(sewage) AS answer,\n    COUNT(fiddle) AS mosque,\n    COUNT(sewage) AS phoebe,\n    SUM(sewage) AS mousse\n  FROM cte_0\n  GROUP BY\n    oscar,\n    dream,\n    savoy\n),\ncte_2 AS (\n  SELECT\n    answer,\n    mosque,\n    phoebe,\n    COUNT(mousse) AS cycle\n  FROM cte_1\n  GROUP BY\n    answer,\n    mosque,\n    phoebe\n),\ncte_3 AS (\n  SELECT\n    answer,\n    MIN(quarry) AS stella,\n    COUNT(phoebe) AS breath,\n    SUM(mosque) AS error\n  FROM cte_2\n  GROUP BY\n    answer\n),\ncte_4 AS (\n  SELECT\n    COUNT(breath) AS cider\n  FROM cte_3\n  GROUP BY\n    answer\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     oscar,\n     dream,\n     savoy,\n     fiddle,\n     sewage\n|> AGGREGATE\n     MIN(sewage) AS answer,\n     COUNT(fiddle) AS mosque,\n     COUNT(sewage) AS phoebe,\n     SUM(sewage) AS mousse\n   GROUP BY\n     oscar,\n     dream,\n     savoy\n|> AGGREGATE\n     COUNT(mousse) AS cycle\n   GROUP BY\n     answer,\n     mosque,\n     phoebe\n|> AGGREGATE\n     MIN(quarry) AS stella,\n     COUNT(phoebe) AS breath,\n     SUM(mosque) AS error\n   GROUP BY\n     answer\n|> AGGREGATE\n     COUNT(breath) AS cider\n   GROUP BY\n     answer\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"quarry",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    manila,\n    gentry,\n    stool,\n    bullet,\n    convoy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    manila,\n    gentry,\n    stool,\n    AVG(convoy) AS fever,\n    MIN(convoy) AS cohen\n  FROM cte_0\n  GROUP BY\n    manila,\n    gentry,\n    stool,\n    bullet\n),\ncte_2 AS (\n  SELECT\n    AVG(stool) AS ideal,\n    MIN(fever) AS sally,\n    SUM(stool) AS doctor\n  FROM cte_1\n  GROUP BY\n    manila,\n    gentry\n),\ncte_3 AS (\n  SELECT\n    ideal,\n    COUNT(double) AS peter,\n    AVG(sally) AS charm,\n    COUNT(sally) AS defect\n  FROM cte_2\n  GROUP BY\n    ideal\n),\ncte_4 AS (\n  SELECT\n    ideal,\n    charm,\n    defect\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     manila,\n     gentry,\n     stool,\n     bullet,\n     convoy\n|> AGGREGATE\n     AVG(convoy) AS fever,\n     MIN(convoy) AS cohen\n   GROUP BY\n     manila,\n     gentry,\n     stool,\n     bullet\n|> AGGREGATE\n     AVG(stool) AS ideal,\n     MIN(fever) AS sally,\n     SUM(stool) AS doctor\n   GROUP BY\n     manila,\n     gentry\n|> AGGREGATE\n     COUNT(double) AS peter,\n     AVG(sally) AS charm,\n     COUNT(sally) AS defect\n   GROUP BY\n     ideal\n|> SELECT\n     ideal,\n     charm,\n     defect\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"double",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    pause,\n    simon,\n    guess,\n    trophy,\n    martyr\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    pause,\n    MAX(martyr) AS accent,\n    SUM(guess) AS sweat,\n    MIN(guess) AS rebate,\n    SUM(martyr) AS guilt,\n    MIN(trophy) AS sewer\n  FROM cte_0\n  GROUP BY\n    pause,\n    simon\n),\ncte_2 AS (\n  SELECT\n    pause,\n    accent,\n    sweat,\n    rebate\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    pause,\n    accent,\n    sweat,\n    COUNT(fridge) AS cloth,\n    COUNT(rebate) AS rifle\n  FROM cte_2\n  GROUP BY\n    pause,\n    accent,\n    sweat\n),\ncte_4 AS (\n  SELECT\n    SUM(accent) AS scout\n  FROM cte_3\n  GROUP BY\n    pause\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     pause,\n     simon,\n     guess,\n     trophy,\n     martyr\n|> AGGREGATE\n     MAX(martyr) AS accent,\n     SUM(guess) AS sweat,\n     MIN(guess) AS rebate,\n     SUM(martyr) AS guilt,\n     MIN(trophy) AS sewer\n   GROUP BY\n     pause,\n     simon\n|> SELECT\n     pause,\n     accent,\n     sweat,\n     rebate\n|> AGGREGATE\n     COUNT(fridge) AS cloth,\n     COUNT(rebate) AS rifle\n   GROUP BY\n     pause,\n     accent,\n     sweat\n|> AGGREGATE\n     SUM(accent) AS scout\n   GROUP BY\n     pause\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"fridge",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    crate,\n    fusion,\n    flake,\n    currie,\n    breach\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    crate,\n    fusion,\n    flake,\n    currie,\n    breach\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    crate,\n    fusion,\n    flake,\n    AVG(currie) AS ledger\n  FROM cte_1\n  GROUP BY\n    crate,\n    fusion,\n    flake\n),\ncte_3 AS (\n  SELECT\n    crate,\n    MAX(ledger) AS shade,\n    SUM(ledger) AS jockey,\n    AVG(ledger) AS merger,\n    MAX(topaz) AS batty\n  FROM cte_2\n  GROUP BY\n    crate,\n    fusion,\n    flake\n),\ncte_4 AS (\n  SELECT\n    SUM(merger) AS barton\n  FROM cte_3\n  GROUP BY\n    crate,\n    shade,\n    jockey\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     crate,\n     fusion,\n     flake,\n     currie,\n     breach\n|> SELECT\n     crate,\n     fusion,\n     flake,\n     currie,\n     breach\n|> AGGREGATE\n     AVG(currie) AS ledger\n   GROUP BY\n     crate,\n     fusion,\n     flake\n|> AGGREGATE\n     MAX(ledger) AS shade,\n     SUM(ledger) AS jockey,\n     AVG(ledger) AS merger,\n     MAX(topaz) AS batty\n   GROUP BY\n     crate,\n     fusion,\n     flake\n|> AGGREGATE\n     SUM(merger) AS barton\n   GROUP BY\n     crate,\n     shade,\n     jockey\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"topaz",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    papacy,\n    barber,\n    decade,\n    cloak,\n    effort\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    papacy,\n    barber,\n    decade,\n    MAX(cloak) AS decor,\n    MIN(effort) AS infant,\n    SUM(cloak) AS while,\n    MAX(effort) AS baker\n  FROM cte_0\n  GROUP BY\n    papacy,\n    barber,\n    decade\n),\ncte_2 AS (\n  SELECT\n    papacy,\n    barber,\n    decade\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    papacy,\n    COUNT(decade) AS rifle,\n    MAX(summer) AS wealth,\n    AVG(decade) AS norma,\n    MAX(decade) AS german\n  FROM cte_2\n  GROUP BY\n    papacy,\n    barber\n),\ncte_4 AS (\n  SELECT\n    MAX(norma) AS sonata\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     papacy,\n     barber,\n     decade,\n     cloak,\n     effort\n|> AGGREGATE\n     MAX(cloak) AS decor,\n     MIN(effort) AS infant,\n     SUM(cloak) AS while,\n     MAX(effort) AS baker\n   GROUP BY\n     papacy,\n     barber,\n     decade\n|> SELECT\n     papacy,\n     barber,\n     decade\n|> AGGREGATE\n     COUNT(decade) AS rifle,\n     MAX(summer) AS wealth,\n     AVG(decade) AS norma,\n     MAX(decade) AS german\n   GROUP BY\n     papacy,\n     barber\n|> AGGREGATE\n     MAX(norma) AS sonata\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"summer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    survey,\n    chart,\n    player,\n    fella,\n    dogma\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    survey,\n    chart,\n    SUM(player) AS gamble\n  FROM cte_0\n  GROUP BY\n    survey,\n    chart\n),\ncte_2 AS (\n  SELECT\n    survey,\n    AVG(gamble) AS foyer,\n    SUM(gamble) AS beauty\n  FROM cte_1\n  GROUP BY\n    survey,\n    chart\n),\ncte_3 AS (\n  SELECT\n    survey,\n    MAX(beauty) AS buffet,\n    SUM(beauty) AS video,\n    COUNT(beauty) AS sphere,\n    MAX(threat) AS sauce,\n    AVG(beauty) AS trauma\n  FROM cte_2\n  GROUP BY\n    survey,\n    foyer\n),\ncte_4 AS (\n  SELECT\n    MAX(sphere) AS breach\n  FROM cte_3\n  GROUP BY\n    survey,\n    buffet\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     survey,\n     chart,\n     player,\n     fella,\n     dogma\n|> AGGREGATE\n     SUM(player) AS gamble\n   GROUP BY\n     survey,\n     chart\n|> AGGREGATE\n     AVG(gamble) AS foyer,\n     SUM(gamble) AS beauty\n   GROUP BY\n     survey,\n     chart\n|> AGGREGATE\n     MAX(beauty) AS buffet,\n     SUM(beauty) AS video,\n     COUNT(beauty) AS sphere,\n     MAX(threat) AS sauce,\n     AVG(beauty) AS trauma\n   GROUP BY\n     survey,\n     foyer\n|> AGGREGATE\n     MAX(sphere) AS breach\n   GROUP BY\n     survey,\n     buffet\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"threat",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    schema,\n    xerox,\n    killer,\n    swamp,\n    rhyme\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    schema,\n    xerox,\n    killer,\n    SUM(rhyme) AS becker\n  FROM cte_0\n  GROUP BY\n    schema,\n    xerox,\n    killer,\n    swamp\n),\ncte_2 AS (\n  SELECT\n    schema,\n    SUM(killer) AS tramp\n  FROM cte_1\n  GROUP BY\n    schema,\n    xerox\n),\ncte_3 AS (\n  SELECT\n    MAX(tramp) AS hound,\n    SUM(tramp) AS sharp,\n    MIN(tramp) AS tally,\n    MAX(throne) AS shade\n  FROM cte_2\n  GROUP BY\n    schema\n),\ncte_4 AS (\n  SELECT\n    MAX(sharp) AS spence,\n    COUNT(tally) AS bloke,\n    MIN(sharp) AS cotton\n  FROM cte_3\n  GROUP BY\n    hound\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     schema,\n     xerox,\n     killer,\n     swamp,\n     rhyme\n|> AGGREGATE\n     SUM(rhyme) AS becker\n   GROUP BY\n     schema,\n     xerox,\n     killer,\n     swamp\n|> AGGREGATE\n     SUM(killer) AS tramp\n   GROUP BY\n     schema,\n     xerox\n|> AGGREGATE\n     MAX(tramp) AS hound,\n     SUM(tramp) AS sharp,\n     MIN(tramp) AS tally,\n     MAX(throne) AS shade\n   GROUP BY\n     schema\n|> AGGREGATE\n     MAX(sharp) AS spence,\n     COUNT(tally) AS bloke,\n     MIN(sharp) AS cotton\n   GROUP BY\n     hound\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"throne",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    worth,\n    sultan,\n    custom,\n    center,\n    theme\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    worth,\n    sultan,\n    MIN(custom) AS insert,\n    MIN(center) AS pardon,\n    COUNT(center) AS plank\n  FROM cte_0\n  GROUP BY\n    worth,\n    sultan\n),\ncte_2 AS (\n  SELECT\n    worth,\n    sultan,\n    COUNT(insert) AS coupon,\n    MIN(insert) AS murder,\n    AVG(plank) AS doubt,\n    MAX(insert) AS baton,\n    SUM(common) AS advert\n  FROM cte_1\n  GROUP BY\n    worth,\n    sultan\n),\ncte_3 AS (\n  SELECT\n    worth,\n    MAX(murder) AS sexism\n  FROM cte_2\n  GROUP BY\n    worth\n),\ncte_4 AS (\n  SELECT\n    MIN(worth) AS suburb\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     worth,\n     sultan,\n     custom,\n     center,\n     theme\n|> AGGREGATE\n     MIN(custom) AS insert,\n     MIN(center) AS pardon,\n     COUNT(center) AS plank\n   GROUP BY\n     worth,\n     sultan\n|> AGGREGATE\n     COUNT(insert) AS coupon,\n     MIN(insert) AS murder,\n     AVG(plank) AS doubt,\n     MAX(insert) AS baton,\n     SUM(common) AS advert\n   GROUP BY\n     worth,\n     sultan\n|> AGGREGATE\n     MAX(murder) AS sexism\n   GROUP BY\n     worth\n|> AGGREGATE\n     MIN(worth) AS suburb\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"common",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    laurel,\n    grade,\n    doctor,\n    vowel,\n    manual\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    laurel,\n    grade,\n    SUM(manual) AS croft\n  FROM cte_0\n  GROUP BY\n    laurel,\n    grade,\n    doctor\n),\ncte_2 AS (\n  SELECT\n    laurel,\n    AVG(croft) AS stead,\n    COUNT(croft) AS crypt,\n    MIN(grade) AS skirt\n  FROM cte_1\n  GROUP BY\n    laurel\n),\ncte_3 AS (\n  SELECT\n    laurel,\n    MAX(skirt) AS school,\n    MIN(skirt) AS mousse,\n    AVG(weight) AS oxygen,\n    AVG(skirt) AS cider\n  FROM cte_2\n  GROUP BY\n    laurel,\n    stead,\n    crypt\n),\ncte_4 AS (\n  SELECT\n    AVG(school) AS fever\n  FROM cte_3\n  GROUP BY\n    laurel\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     laurel,\n     grade,\n     doctor,\n     vowel,\n     manual\n|> AGGREGATE\n     SUM(manual) AS croft\n   GROUP BY\n     laurel,\n     grade,\n     doctor\n|> AGGREGATE\n     AVG(croft) AS stead,\n     COUNT(croft) AS crypt,\n     MIN(grade) AS skirt\n   GROUP BY\n     laurel\n|> AGGREGATE\n     MAX(skirt) AS school,\n     MIN(skirt) AS mousse,\n     AVG(weight) AS oxygen,\n     AVG(skirt) AS cider\n   GROUP BY\n     laurel,\n     stead,\n     crypt\n|> AGGREGATE\n     AVG(school) AS fever\n   GROUP BY\n     laurel\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"weight",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    betty,\n    relief,\n    billy,\n    turtle,\n    pilot\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    betty,\n    relief,\n    billy,\n    turtle\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    betty,\n    relief,\n    AVG(turtle) AS gentry,\n    MAX(billy) AS peace\n  FROM cte_1\n  GROUP BY\n    betty,\n    relief\n),\ncte_3 AS (\n  SELECT\n    betty,\n    relief,\n    COUNT(peace) AS widow,\n    SUM(gentry) AS table,\n    MAX(peace) AS quota,\n    COUNT(cassie) AS amazon,\n    MIN(peace) AS trial\n  FROM cte_2\n  GROUP BY\n    betty,\n    relief\n),\ncte_4 AS (\n  SELECT\n    AVG(table) AS barrel\n  FROM cte_3\n  GROUP BY\n    betty,\n    relief\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     betty,\n     relief,\n     billy,\n     turtle,\n     pilot\n|> SELECT\n     betty,\n     relief,\n     billy,\n     turtle\n|> AGGREGATE\n     AVG(turtle) AS gentry,\n     MAX(billy) AS peace\n   GROUP BY\n     betty,\n     relief\n|> AGGREGATE\n     COUNT(peace) AS widow,\n     SUM(gentry) AS table,\n     MAX(peace) AS quota,\n     COUNT(cassie) AS amazon,\n     MIN(peace) AS trial\n   GROUP BY\n     betty,\n     relief\n|> AGGREGATE\n     AVG(table) AS barrel\n   GROUP BY\n     betty,\n     relief\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"cassie",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    outfit,\n    oscar,\n    satan,\n    paste,\n    recall\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    outfit,\n    oscar,\n    satan,\n    paste,\n    SUM(recall) AS lunch,\n    MAX(recall) AS synod,\n    COUNT(recall) AS trust,\n    MIN(recall) AS carpet\n  FROM cte_0\n  GROUP BY\n    outfit,\n    oscar,\n    satan,\n    paste\n),\ncte_2 AS (\n  SELECT\n    MAX(lunch) AS burrow,\n    MIN(carpet) AS chair,\n    SUM(start) AS ulster,\n    AVG(paste) AS press\n  FROM cte_1\n  GROUP BY\n    outfit,\n    oscar\n),\ncte_3 AS (\n  SELECT\n    burrow,\n    chair\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    burrow,\n    chair\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     outfit,\n     oscar,\n     satan,\n     paste,\n     recall\n|> AGGREGATE\n     SUM(recall) AS lunch,\n     MAX(recall) AS synod,\n     COUNT(recall) AS trust,\n     MIN(recall) AS carpet\n   GROUP BY\n     outfit,\n     oscar,\n     satan,\n     paste\n|> AGGREGATE\n     MAX(lunch) AS burrow,\n     MIN(carpet) AS chair,\n     SUM(start) AS ulster,\n     AVG(paste) AS press\n   GROUP BY\n     outfit,\n     oscar\n|> SELECT\n     burrow,\n     chair\n|> SELECT\n     burrow,\n     chair\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"start",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hamlet,\n    mucosa,\n    storm,\n    earth,\n    shore\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    hamlet,\n    mucosa,\n    storm,\n    MAX(shore) AS claim,\n    AVG(shore) AS curry,\n    MIN(shore) AS hammer,\n    SUM(shore) AS camel\n  FROM cte_0\n  GROUP BY\n    hamlet,\n    mucosa,\n    storm,\n    earth\n),\ncte_2 AS (\n  SELECT\n    hamlet,\n    SUM(camel) AS stream,\n    SUM(hammer) AS nation,\n    AVG(chill) AS gauge\n  FROM cte_1\n  GROUP BY\n    hamlet,\n    mucosa,\n    storm\n),\ncte_3 AS (\n  SELECT\n    hamlet,\n    stream\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(stream) AS wagon\n  FROM cte_3\n  GROUP BY\n    hamlet\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hamlet,\n     mucosa,\n     storm,\n     earth,\n     shore\n|> AGGREGATE\n     MAX(shore) AS claim,\n     AVG(shore) AS curry,\n     MIN(shore) AS hammer,\n     SUM(shore) AS camel\n   GROUP BY\n     hamlet,\n     mucosa,\n     storm,\n     earth\n|> AGGREGATE\n     SUM(camel) AS stream,\n     SUM(hammer) AS nation,\n     AVG(chill) AS gauge\n   GROUP BY\n     hamlet,\n     mucosa,\n     storm\n|> SELECT\n     hamlet,\n     stream\n|> AGGREGATE\n     COUNT(stream) AS wagon\n   GROUP BY\n     hamlet\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"chill",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sanity,\n    lender,\n    height,\n    coward,\n    cache\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(lender) AS camera,\n    MIN(coward) AS bottle\n  FROM cte_0\n  GROUP BY\n    sanity\n),\ncte_2 AS (\n  SELECT\n    camera,\n    SUM(bottle) AS chant,\n    MIN(bottle) AS beech,\n    MAX(bottle) AS shire,\n    AVG(bottle) AS fossil\n  FROM cte_1\n  GROUP BY\n    camera\n),\ncte_3 AS (\n  SELECT\n    camera,\n    chant,\n    beech,\n    COUNT(fossil) AS jasper,\n    MAX(creed) AS psalm\n  FROM cte_2\n  GROUP BY\n    camera,\n    chant,\n    beech,\n    shire\n),\ncte_4 AS (\n  SELECT\n    MIN(camera) AS joanna,\n    SUM(chant) AS stroll\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sanity,\n     lender,\n     height,\n     coward,\n     cache\n|> AGGREGATE\n     SUM(lender) AS camera,\n     MIN(coward) AS bottle\n   GROUP BY\n     sanity\n|> AGGREGATE\n     SUM(bottle) AS chant,\n     MIN(bottle) AS beech,\n     MAX(bottle) AS shire,\n     AVG(bottle) AS fossil\n   GROUP BY\n     camera\n|> AGGREGATE\n     COUNT(fossil) AS jasper,\n     MAX(creed) AS psalm\n   GROUP BY\n     camera,\n     chant,\n     beech,\n     shire\n|> AGGREGATE\n     MIN(camera) AS joanna,\n     SUM(chant) AS stroll\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"creed",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    chorus,\n    idiom,\n    hedge,\n    crewe,\n    horror\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    chorus,\n    idiom,\n    AVG(horror) AS currie\n  FROM cte_0\n  GROUP BY\n    chorus,\n    idiom,\n    hedge,\n    crewe\n),\ncte_2 AS (\n  SELECT\n    chorus,\n    idiom,\n    MIN(currie) AS oxide,\n    SUM(currie) AS tangle\n  FROM cte_1\n  GROUP BY\n    chorus,\n    idiom\n),\ncte_3 AS (\n  SELECT\n    MIN(tangle) AS orient,\n    AVG(tangle) AS crane,\n    AVG(reeve) AS daphne,\n    SUM(tangle) AS berth\n  FROM cte_2\n  GROUP BY\n    chorus,\n    idiom,\n    oxide\n),\ncte_4 AS (\n  SELECT\n    orient,\n    crane,\n    berth\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     chorus,\n     idiom,\n     hedge,\n     crewe,\n     horror\n|> AGGREGATE\n     AVG(horror) AS currie\n   GROUP BY\n     chorus,\n     idiom,\n     hedge,\n     crewe\n|> AGGREGATE\n     MIN(currie) AS oxide,\n     SUM(currie) AS tangle\n   GROUP BY\n     chorus,\n     idiom\n|> AGGREGATE\n     MIN(tangle) AS orient,\n     AVG(tangle) AS crane,\n     AVG(reeve) AS daphne,\n     SUM(tangle) AS berth\n   GROUP BY\n     chorus,\n     idiom,\n     oxide\n|> SELECT\n     orient,\n     crane,\n     berth\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"reeve",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    unease,\n    perry,\n    casino,\n    dieter,\n    scorn\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    unease,\n    perry,\n    casino,\n    AVG(scorn) AS album,\n    MIN(scorn) AS thorn\n  FROM cte_0\n  GROUP BY\n    unease,\n    perry,\n    casino,\n    dieter\n),\ncte_2 AS (\n  SELECT\n    unease,\n    perry,\n    casino,\n    album\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    unease,\n    AVG(album) AS comic,\n    SUM(album) AS apollo,\n    COUNT(chase) AS menace,\n    COUNT(album) AS ghetto\n  FROM cte_2\n  GROUP BY\n    unease,\n    perry,\n    casino\n),\ncte_4 AS (\n  SELECT\n    unease,\n    comic,\n    apollo\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     unease,\n     perry,\n     casino,\n     dieter,\n     scorn\n|> AGGREGATE\n     AVG(scorn) AS album,\n     MIN(scorn) AS thorn\n   GROUP BY\n     unease,\n     perry,\n     casino,\n     dieter\n|> SELECT\n     unease,\n     perry,\n     casino,\n     album\n|> AGGREGATE\n     AVG(album) AS comic,\n     SUM(album) AS apollo,\n     COUNT(chase) AS menace,\n     COUNT(album) AS ghetto\n   GROUP BY\n     unease,\n     perry,\n     casino\n|> SELECT\n     unease,\n     comic,\n     apollo\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"chase",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bulge,\n    cable,\n    climax,\n    vinyl,\n    currie\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    bulge,\n    cable,\n    climax,\n    AVG(currie) AS engine\n  FROM cte_0\n  GROUP BY\n    bulge,\n    cable,\n    climax,\n    vinyl\n),\ncte_2 AS (\n  SELECT\n    bulge,\n    MIN(climax) AS rubber,\n    MAX(engine) AS whore,\n    COUNT(climax) AS golfer\n  FROM cte_1\n  GROUP BY\n    bulge,\n    cable\n),\ncte_3 AS (\n  SELECT\n    MAX(whore) AS salad,\n    COUNT(mucosa) AS flick,\n    MAX(bulge) AS powder,\n    AVG(golfer) AS settee\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(powder) AS canopy,\n    COUNT(settee) AS saint\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bulge,\n     cable,\n     climax,\n     vinyl,\n     currie\n|> AGGREGATE\n     AVG(currie) AS engine\n   GROUP BY\n     bulge,\n     cable,\n     climax,\n     vinyl\n|> AGGREGATE\n     MIN(climax) AS rubber,\n     MAX(engine) AS whore,\n     COUNT(climax) AS golfer\n   GROUP BY\n     bulge,\n     cable\n|> AGGREGATE\n     MAX(whore) AS salad,\n     COUNT(mucosa) AS flick,\n     MAX(bulge) AS powder,\n     AVG(golfer) AS settee\n|> AGGREGATE\n     COUNT(powder) AS canopy,\n     COUNT(settee) AS saint\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"mucosa",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    carpet,\n    carter,\n    frame,\n    saloon,\n    horne\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    carpet,\n    carter,\n    MIN(horne) AS slick,\n    COUNT(horne) AS amazon\n  FROM cte_0\n  GROUP BY\n    carpet,\n    carter\n),\ncte_2 AS (\n  SELECT\n    carpet,\n    carter,\n    MIN(amazon) AS glider,\n    SUM(slick) AS small,\n    COUNT(amazon) AS height\n  FROM cte_1\n  GROUP BY\n    carpet,\n    carter\n),\ncte_3 AS (\n  SELECT\n    COUNT(height) AS ticket,\n    AVG(sauce) AS woody,\n    COUNT(small) AS recess\n  FROM cte_2\n  GROUP BY\n    carpet,\n    carter,\n    glider\n),\ncte_4 AS (\n  SELECT\n    SUM(recess) AS sleep\n  FROM cte_3\n  GROUP BY\n    ticket\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     carpet,\n     carter,\n     frame,\n     saloon,\n     horne\n|> AGGREGATE\n     MIN(horne) AS slick,\n     COUNT(horne) AS amazon\n   GROUP BY\n     carpet,\n     carter\n|> AGGREGATE\n     MIN(amazon) AS glider,\n     SUM(slick) AS small,\n     COUNT(amazon) AS height\n   GROUP BY\n     carpet,\n     carter\n|> AGGREGATE\n     COUNT(height) AS ticket,\n     AVG(sauce) AS woody,\n     COUNT(small) AS recess\n   GROUP BY\n     carpet,\n     carter,\n     glider\n|> AGGREGATE\n     SUM(recess) AS sleep\n   GROUP BY\n     ticket\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"sauce",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    store,\n    firth,\n    pastry,\n    exile,\n    range\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    store,\n    firth,\n    pastry,\n    MIN(exile) AS spirit,\n    MAX(exile) AS essay,\n    AVG(exile) AS fright\n  FROM cte_0\n  GROUP BY\n    store,\n    firth,\n    pastry\n),\ncte_2 AS (\n  SELECT\n    store,\n    firth,\n    pastry,\n    spirit,\n    essay\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    store,\n    AVG(pastry) AS digit,\n    COUNT(alien) AS comet\n  FROM cte_2\n  GROUP BY\n    store,\n    firth\n),\ncte_4 AS (\n  SELECT\n    SUM(digit) AS badger,\n    MIN(digit) AS buddy,\n    MAX(digit) AS tyler\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     store,\n     firth,\n     pastry,\n     exile,\n     range\n|> AGGREGATE\n     MIN(exile) AS spirit,\n     MAX(exile) AS essay,\n     AVG(exile) AS fright\n   GROUP BY\n     store,\n     firth,\n     pastry\n|> SELECT\n     store,\n     firth,\n     pastry,\n     spirit,\n     essay\n|> AGGREGATE\n     AVG(pastry) AS digit,\n     COUNT(alien) AS comet\n   GROUP BY\n     store,\n     firth\n|> AGGREGATE\n     SUM(digit) AS badger,\n     MIN(digit) AS buddy,\n     MAX(digit) AS tyler\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"alien",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    squash,\n    crash,\n    ghana,\n    clause,\n    school\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    squash,\n    crash,\n    ghana,\n    MAX(school) AS safari\n  FROM cte_0\n  GROUP BY\n    squash,\n    crash,\n    ghana,\n    clause\n),\ncte_2 AS (\n  SELECT\n    SUM(crash) AS statue,\n    COUNT(crash) AS bearer,\n    MIN(safari) AS granny\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    statue,\n    MIN(granny) AS larry,\n    COUNT(granny) AS arrest,\n    AVG(metal) AS marker,\n    MAX(granny) AS forte,\n    AVG(granny) AS world\n  FROM cte_2\n  GROUP BY\n    statue,\n    bearer\n),\ncte_4 AS (\n  SELECT\n    statue,\n    larry\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     squash,\n     crash,\n     ghana,\n     clause,\n     school\n|> AGGREGATE\n     MAX(school) AS safari\n   GROUP BY\n     squash,\n     crash,\n     ghana,\n     clause\n|> AGGREGATE\n     SUM(crash) AS statue,\n     COUNT(crash) AS bearer,\n     MIN(safari) AS granny\n|> AGGREGATE\n     MIN(granny) AS larry,\n     COUNT(granny) AS arrest,\n     AVG(metal) AS marker,\n     MAX(granny) AS forte,\n     AVG(granny) AS world\n   GROUP BY\n     statue,\n     bearer\n|> SELECT\n     statue,\n     larry\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"metal",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    daniel,\n    stile,\n    speech,\n    hurst,\n    wrong\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    daniel,\n    stile,\n    speech,\n    hurst,\n    SUM(wrong) AS insult,\n    MIN(wrong) AS canoe,\n    AVG(wrong) AS amazon,\n    MAX(wrong) AS turtle\n  FROM cte_0\n  GROUP BY\n    daniel,\n    stile,\n    speech,\n    hurst\n),\ncte_2 AS (\n  SELECT\n    SUM(insult) AS launch\n  FROM cte_1\n  GROUP BY\n    daniel,\n    stile\n),\ncte_3 AS (\n  SELECT\n    MAX(chant) AS peach,\n    MAX(launch) AS organ,\n    SUM(launch) AS robot,\n    AVG(launch) AS millie\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(millie) AS church\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     daniel,\n     stile,\n     speech,\n     hurst,\n     wrong\n|> AGGREGATE\n     SUM(wrong) AS insult,\n     MIN(wrong) AS canoe,\n     AVG(wrong) AS amazon,\n     MAX(wrong) AS turtle\n   GROUP BY\n     daniel,\n     stile,\n     speech,\n     hurst\n|> AGGREGATE\n     SUM(insult) AS launch\n   GROUP BY\n     daniel,\n     stile\n|> AGGREGATE\n     MAX(chant) AS peach,\n     MAX(launch) AS organ,\n     SUM(launch) AS robot,\n     AVG(launch) AS millie\n|> AGGREGATE\n     MAX(millie) AS church\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"chant",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    pirate,\n    lender,\n    gravy,\n    table,\n    upside\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    pirate,\n    lender,\n    MAX(upside) AS alarm,\n    MIN(table) AS thirst\n  FROM cte_0\n  GROUP BY\n    pirate,\n    lender\n),\ncte_2 AS (\n  SELECT\n    pirate,\n    lender,\n    alarm,\n    thirst\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    pirate,\n    lender,\n    alarm,\n    thirst\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    pirate,\n    lender,\n    MAX(alarm) AS waste,\n    SUM(lease) AS assay,\n    MAX(thirst) AS grave,\n    COUNT(alarm) AS biopsy,\n    COUNT(thirst) AS turkey\n  FROM cte_3\n  GROUP BY\n    pirate,\n    lender\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     pirate,\n     lender,\n     gravy,\n     table,\n     upside\n|> AGGREGATE\n     MAX(upside) AS alarm,\n     MIN(table) AS thirst\n   GROUP BY\n     pirate,\n     lender\n|> SELECT\n     pirate,\n     lender,\n     alarm,\n     thirst\n|> SELECT\n     pirate,\n     lender,\n     alarm,\n     thirst\n|> AGGREGATE\n     MAX(alarm) AS waste,\n     SUM(lease) AS assay,\n     MAX(thirst) AS grave,\n     COUNT(alarm) AS biopsy,\n     COUNT(thirst) AS turkey\n   GROUP BY\n     pirate,\n     lender\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"lease",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:43,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    boost,\n    camel,\n    seller,\n    stand,\n    tackle\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    boost,\n    camel,\n    COUNT(stand) AS granny,\n    COUNT(tackle) AS draper\n  FROM cte_0\n  GROUP BY\n    boost,\n    camel\n),\ncte_2 AS (\n  SELECT\n    SUM(draper) AS daisy,\n    MIN(granny) AS brush,\n    MIN(draper) AS argyll,\n    COUNT(granny) AS graham\n  FROM cte_1\n  GROUP BY\n    boost,\n    camel\n),\ncte_3 AS (\n  SELECT\n    daisy,\n    brush,\n    MAX(plain) AS dancer,\n    MIN(argyll) AS liner\n  FROM cte_2\n  GROUP BY\n    daisy,\n    brush\n),\ncte_4 AS (\n  SELECT\n    daisy,\n    SUM(liner) AS arrest\n  FROM cte_3\n  GROUP BY\n    daisy\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     boost,\n     camel,\n     seller,\n     stand,\n     tackle\n|> AGGREGATE\n     COUNT(stand) AS granny,\n     COUNT(tackle) AS draper\n   GROUP BY\n     boost,\n     camel\n|> AGGREGATE\n     SUM(draper) AS daisy,\n     MIN(granny) AS brush,\n     MIN(draper) AS argyll,\n     COUNT(granny) AS graham\n   GROUP BY\n     boost,\n     camel\n|> AGGREGATE\n     MAX(plain) AS dancer,\n     MIN(argyll) AS liner\n   GROUP BY\n     daisy,\n     brush\n|> AGGREGATE\n     SUM(liner) AS arrest\n   GROUP BY\n     daisy\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"plain",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    deputy,\n    nausea,\n    honour,\n    holder,\n    trial\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    deputy,\n    nausea,\n    COUNT(holder) AS billy\n  FROM cte_0\n  GROUP BY\n    deputy,\n    nausea,\n    honour\n),\ncte_2 AS (\n  SELECT\n    deputy,\n    nausea,\n    billy\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    deputy,\n    MAX(billy) AS folder,\n    MIN(billy) AS census\n  FROM cte_2\n  GROUP BY\n    deputy,\n    nausea\n),\ncte_4 AS (\n  SELECT\n    deputy,\n    MAX(census) AS grouse,\n    AVG(tissue) AS mould,\n    SUM(census) AS serial,\n    MIN(census) AS rhythm,\n    COUNT(census) AS malice\n  FROM cte_3\n  GROUP BY\n    deputy,\n    folder\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     deputy,\n     nausea,\n     honour,\n     holder,\n     trial\n|> AGGREGATE\n     COUNT(holder) AS billy\n   GROUP BY\n     deputy,\n     nausea,\n     honour\n|> SELECT\n     deputy,\n     nausea,\n     billy\n|> AGGREGATE\n     MAX(billy) AS folder,\n     MIN(billy) AS census\n   GROUP BY\n     deputy,\n     nausea\n|> AGGREGATE\n     MAX(census) AS grouse,\n     AVG(tissue) AS mould,\n     SUM(census) AS serial,\n     MIN(census) AS rhythm,\n     COUNT(census) AS malice\n   GROUP BY\n     deputy,\n     folder\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"tissue",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:43,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    cocoa,\n    factor,\n    knack,\n    filter,\n    guild\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    cocoa,\n    factor,\n    knack,\n    filter,\n    AVG(guild) AS mould,\n    MIN(guild) AS supply,\n    COUNT(guild) AS brazil,\n    MAX(guild) AS brook,\n    SUM(guild) AS plaque\n  FROM cte_0\n  GROUP BY\n    cocoa,\n    factor,\n    knack,\n    filter\n),\ncte_2 AS (\n  SELECT\n    SUM(filter) AS clone,\n    AVG(stint) AS ribber,\n    MAX(factor) AS remedy\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(remedy) AS common,\n    MIN(remedy) AS trial\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(common) AS ferret\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     cocoa,\n     factor,\n     knack,\n     filter,\n     guild\n|> AGGREGATE\n     AVG(guild) AS mould,\n     MIN(guild) AS supply,\n     COUNT(guild) AS brazil,\n     MAX(guild) AS brook,\n     SUM(guild) AS plaque\n   GROUP BY\n     cocoa,\n     factor,\n     knack,\n     filter\n|> AGGREGATE\n     SUM(filter) AS clone,\n     AVG(stint) AS ribber,\n     MAX(factor) AS remedy\n|> AGGREGATE\n     COUNT(remedy) AS common,\n     MIN(remedy) AS trial\n|> AGGREGATE\n     MAX(common) AS ferret\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"stint",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    clover,\n    jumper,\n    clone,\n    smash,\n    stout\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    clover,\n    jumper,\n    MIN(clone) AS unison\n  FROM cte_0\n  GROUP BY\n    clover,\n    jumper\n),\ncte_2 AS (\n  SELECT\n    clover,\n    AVG(unison) AS psyche,\n    MIN(unison) AS insult,\n    COUNT(unison) AS stench,\n    SUM(unison) AS libel\n  FROM cte_1\n  GROUP BY\n    clover,\n    jumper\n),\ncte_3 AS (\n  SELECT\n    clover,\n    SUM(psyche) AS glider,\n    SUM(thesis) AS prize,\n    COUNT(libel) AS vapour,\n    COUNT(psyche) AS luxury\n  FROM cte_2\n  GROUP BY\n    clover\n),\ncte_4 AS (\n  SELECT\n    MIN(luxury) AS order\n  FROM cte_3\n  GROUP BY\n    clover\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     clover,\n     jumper,\n     clone,\n     smash,\n     stout\n|> AGGREGATE\n     MIN(clone) AS unison\n   GROUP BY\n     clover,\n     jumper\n|> AGGREGATE\n     AVG(unison) AS psyche,\n     MIN(unison) AS insult,\n     COUNT(unison) AS stench,\n     SUM(unison) AS libel\n   GROUP BY\n     clover,\n     jumper\n|> AGGREGATE\n     SUM(psyche) AS glider,\n     SUM(thesis) AS prize,\n     COUNT(libel) AS vapour,\n     COUNT(psyche) AS luxury\n   GROUP BY\n     clover\n|> AGGREGATE\n     MIN(luxury) AS order\n   GROUP BY\n     clover\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"thesis",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    regime,\n    lawson,\n    lionel,\n    forum,\n    scrap\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    regime,\n    COUNT(forum) AS crater,\n    MAX(forum) AS depth,\n    AVG(forum) AS saint\n  FROM cte_0\n  GROUP BY\n    regime,\n    lawson,\n    lionel\n),\ncte_2 AS (\n  SELECT\n    regime,\n    crater,\n    COUNT(saint) AS puppy\n  FROM cte_1\n  GROUP BY\n    regime,\n    crater,\n    depth\n),\ncte_3 AS (\n  SELECT\n    regime,\n    SUM(puppy) AS tundra,\n    AVG(hazard) AS robin,\n    MAX(crater) AS sound\n  FROM cte_2\n  GROUP BY\n    regime\n),\ncte_4 AS (\n  SELECT\n    regime,\n    SUM(sound) AS crash\n  FROM cte_3\n  GROUP BY\n    regime,\n    tundra\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     regime,\n     lawson,\n     lionel,\n     forum,\n     scrap\n|> AGGREGATE\n     COUNT(forum) AS crater,\n     MAX(forum) AS depth,\n     AVG(forum) AS saint\n   GROUP BY\n     regime,\n     lawson,\n     lionel\n|> AGGREGATE\n     COUNT(saint) AS puppy\n   GROUP BY\n     regime,\n     crater,\n     depth\n|> AGGREGATE\n     SUM(puppy) AS tundra,\n     AVG(hazard) AS robin,\n     MAX(crater) AS sound\n   GROUP BY\n     regime\n|> AGGREGATE\n     SUM(sound) AS crash\n   GROUP BY\n     regime,\n     tundra\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"AGG_UNKNOWN",totalInformationDifference:4,unknownName:"hazard",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    crash,\n    brief,\n    parade,\n    police,\n    crunch\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    crash,\n    brief,\n    parade,\n    MIN(crunch) AS summit\n  FROM cte_0\n  GROUP BY\n    crash,\n    brief,\n    parade,\n    police\n),\ncte_2 AS (\n  SELECT\n    crash,\n    brief,\n    COUNT(summit) AS warren,\n    MAX(parade) AS mirror\n  FROM cte_1\n  GROUP BY\n    crash,\n    brief\n),\ncte_3 AS (\n  SELECT\n    crash,\n    brief,\n    warren,\n    COUNT(irony) AS lodge,\n    SUM(mirror) AS diary\n  FROM cte_2\n  GROUP BY\n    crash,\n    brief,\n    warren\n),\ncte_4 AS (\n  SELECT\n    SUM(warren) AS flesh\n  FROM cte_3\n  GROUP BY\n    crash\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     crash,\n     brief,\n     parade,\n     police,\n     crunch\n|> AGGREGATE\n     MIN(crunch) AS summit\n   GROUP BY\n     crash,\n     brief,\n     parade,\n     police\n|> AGGREGATE\n     COUNT(summit) AS warren,\n     MAX(parade) AS mirror\n   GROUP BY\n     crash,\n     brief\n|> AGGREGATE\n     COUNT(irony) AS lodge,\n     SUM(mirror) AS diary\n   GROUP BY\n     crash,\n     brief,\n     warren\n|> AGGREGATE\n     SUM(warren) AS flesh\n   GROUP BY\n     crash\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"irony",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tremor,\n    dinghy,\n    collor,\n    income,\n    excise\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    tremor,\n    dinghy,\n    collor,\n    income,\n    MAX(excise) AS artist,\n    SUM(excise) AS probe,\n    AVG(excise) AS graham\n  FROM cte_0\n  GROUP BY\n    tremor,\n    dinghy,\n    collor,\n    income\n),\ncte_2 AS (\n  SELECT\n    tremor,\n    dinghy,\n    collor,\n    income,\n    COUNT(probe) AS bunch,\n    MAX(knife) AS hatred\n  FROM cte_1\n  GROUP BY\n    tremor,\n    dinghy,\n    collor,\n    income\n),\ncte_3 AS (\n  SELECT\n    tremor,\n    dinghy\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    tremor,\n    dinghy\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tremor,\n     dinghy,\n     collor,\n     income,\n     excise\n|> AGGREGATE\n     MAX(excise) AS artist,\n     SUM(excise) AS probe,\n     AVG(excise) AS graham\n   GROUP BY\n     tremor,\n     dinghy,\n     collor,\n     income\n|> AGGREGATE\n     COUNT(probe) AS bunch,\n     MAX(knife) AS hatred\n   GROUP BY\n     tremor,\n     dinghy,\n     collor,\n     income\n|> SELECT\n     tremor,\n     dinghy\n|> SELECT\n     tremor,\n     dinghy\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"knife",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:19},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    divide,\n    temple,\n    toast,\n    oracle,\n    jerry\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    divide,\n    temple,\n    toast,\n    COUNT(oracle) AS palmer\n  FROM cte_0\n  GROUP BY\n    divide,\n    temple,\n    toast\n),\ncte_2 AS (\n  SELECT\n    divide,\n    temple,\n    toast,\n    MAX(palmer) AS bucket,\n    MIN(palmer) AS stein\n  FROM cte_1\n  GROUP BY\n    divide,\n    temple,\n    toast\n),\ncte_3 AS (\n  SELECT\n    divide,\n    temple,\n    MAX(cortex) AS shield,\n    SUM(bucket) AS joanna\n  FROM cte_2\n  GROUP BY\n    divide,\n    temple\n),\ncte_4 AS (\n  SELECT\n    SUM(divide) AS zenith,\n    SUM(joanna) AS racket\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     divide,\n     temple,\n     toast,\n     oracle,\n     jerry\n|> AGGREGATE\n     COUNT(oracle) AS palmer\n   GROUP BY\n     divide,\n     temple,\n     toast\n|> AGGREGATE\n     MAX(palmer) AS bucket,\n     MIN(palmer) AS stein\n   GROUP BY\n     divide,\n     temple,\n     toast\n|> AGGREGATE\n     MAX(cortex) AS shield,\n     SUM(bucket) AS joanna\n   GROUP BY\n     divide,\n     temple\n|> AGGREGATE\n     SUM(divide) AS zenith,\n     SUM(joanna) AS racket\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"cortex",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    visit,\n    clive,\n    front,\n    cabin,\n    puppy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    visit,\n    clive,\n    front,\n    MIN(puppy) AS derby\n  FROM cte_0\n  GROUP BY\n    visit,\n    clive,\n    front\n),\ncte_2 AS (\n  SELECT\n    visit,\n    clive,\n    front,\n    COUNT(derby) AS parent\n  FROM cte_1\n  GROUP BY\n    visit,\n    clive,\n    front\n),\ncte_3 AS (\n  SELECT\n    visit,\n    clive,\n    MIN(parent) AS track,\n    COUNT(curve) AS lough,\n    MAX(parent) AS kylie\n  FROM cte_2\n  GROUP BY\n    visit,\n    clive,\n    front\n),\ncte_4 AS (\n  SELECT\n    visit,\n    clive,\n    track\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     visit,\n     clive,\n     front,\n     cabin,\n     puppy\n|> AGGREGATE\n     MIN(puppy) AS derby\n   GROUP BY\n     visit,\n     clive,\n     front\n|> AGGREGATE\n     COUNT(derby) AS parent\n   GROUP BY\n     visit,\n     clive,\n     front\n|> AGGREGATE\n     MIN(parent) AS track,\n     COUNT(curve) AS lough,\n     MAX(parent) AS kylie\n   GROUP BY\n     visit,\n     clive,\n     front\n|> SELECT\n     visit,\n     clive,\n     track\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"curve",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    margin,\n    sketch,\n    blood,\n    candy,\n    spruce\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    margin,\n    sketch,\n    blood,\n    COUNT(candy) AS gland\n  FROM cte_0\n  GROUP BY\n    margin,\n    sketch,\n    blood\n),\ncte_2 AS (\n  SELECT\n    margin,\n    sketch,\n    blood,\n    COUNT(gland) AS clown\n  FROM cte_1\n  GROUP BY\n    margin,\n    sketch,\n    blood\n),\ncte_3 AS (\n  SELECT\n    margin,\n    sketch,\n    MIN(clown) AS norma,\n    COUNT(hazard) AS recess,\n    SUM(clown) AS rival\n  FROM cte_2\n  GROUP BY\n    margin,\n    sketch,\n    blood\n),\ncte_4 AS (\n  SELECT\n    margin,\n    sketch,\n    norma\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     margin,\n     sketch,\n     blood,\n     candy,\n     spruce\n|> AGGREGATE\n     COUNT(candy) AS gland\n   GROUP BY\n     margin,\n     sketch,\n     blood\n|> AGGREGATE\n     COUNT(gland) AS clown\n   GROUP BY\n     margin,\n     sketch,\n     blood\n|> AGGREGATE\n     MIN(clown) AS norma,\n     COUNT(hazard) AS recess,\n     SUM(clown) AS rival\n   GROUP BY\n     margin,\n     sketch,\n     blood\n|> SELECT\n     margin,\n     sketch,\n     norma\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"hazard",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    grill,\n    magnet,\n    torch,\n    death,\n    parson\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    grill,\n    magnet,\n    MAX(torch) AS chick\n  FROM cte_0\n  GROUP BY\n    grill,\n    magnet\n),\ncte_2 AS (\n  SELECT\n    grill,\n    magnet,\n    COUNT(chick) AS while\n  FROM cte_1\n  GROUP BY\n    grill,\n    magnet\n),\ncte_3 AS (\n  SELECT\n    grill,\n    magnet,\n    COUNT(while) AS cheek\n  FROM cte_2\n  GROUP BY\n    grill,\n    magnet\n),\ncte_4 AS (\n  SELECT\n    grill,\n    magnet,\n    MIN(dummy) AS outing,\n    MIN(cheek) AS torque,\n    AVG(cheek) AS fiver,\n    COUNT(cheek) AS mummy\n  FROM cte_3\n  GROUP BY\n    grill,\n    magnet\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     grill,\n     magnet,\n     torch,\n     death,\n     parson\n|> AGGREGATE\n     MAX(torch) AS chick\n   GROUP BY\n     grill,\n     magnet\n|> AGGREGATE\n     COUNT(chick) AS while\n   GROUP BY\n     grill,\n     magnet\n|> AGGREGATE\n     COUNT(while) AS cheek\n   GROUP BY\n     grill,\n     magnet\n|> AGGREGATE\n     MIN(dummy) AS outing,\n     MIN(cheek) AS torque,\n     AVG(cheek) AS fiver,\n     COUNT(cheek) AS mummy\n   GROUP BY\n     grill,\n     magnet\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"dummy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:45,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    token,\n    willow,\n    tackle,\n    garage,\n    praise\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    token,\n    willow,\n    tackle,\n    garage,\n    AVG(praise) AS wheat,\n    COUNT(praise) AS effect\n  FROM cte_0\n  GROUP BY\n    token,\n    willow,\n    tackle,\n    garage\n),\ncte_2 AS (\n  SELECT\n    token,\n    willow,\n    AVG(effect) AS collar\n  FROM cte_1\n  GROUP BY\n    token,\n    willow\n),\ncte_3 AS (\n  SELECT\n    token,\n    willow,\n    SUM(mutant) AS foley,\n    MIN(collar) AS uptake\n  FROM cte_2\n  GROUP BY\n    token,\n    willow\n),\ncte_4 AS (\n  SELECT\n    COUNT(uptake) AS ladder\n  FROM cte_3\n  GROUP BY\n    token,\n    willow\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     token,\n     willow,\n     tackle,\n     garage,\n     praise\n|> AGGREGATE\n     AVG(praise) AS wheat,\n     COUNT(praise) AS effect\n   GROUP BY\n     token,\n     willow,\n     tackle,\n     garage\n|> AGGREGATE\n     AVG(effect) AS collar\n   GROUP BY\n     token,\n     willow\n|> AGGREGATE\n     SUM(mutant) AS foley,\n     MIN(collar) AS uptake\n   GROUP BY\n     token,\n     willow\n|> AGGREGATE\n     COUNT(uptake) AS ladder\n   GROUP BY\n     token,\n     willow\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"mutant",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hunter,\n    blouse,\n    deputy,\n    enzyme,\n    check\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    hunter,\n    blouse,\n    deputy,\n    MAX(check) AS saucer\n  FROM cte_0\n  GROUP BY\n    hunter,\n    blouse,\n    deputy\n),\ncte_2 AS (\n  SELECT\n    hunter,\n    blouse,\n    deputy,\n    MAX(saucer) AS mafia\n  FROM cte_1\n  GROUP BY\n    hunter,\n    blouse,\n    deputy\n),\ncte_3 AS (\n  SELECT\n    hunter,\n    blouse,\n    MIN(deputy) AS mount,\n    SUM(agony) AS uproar,\n    MAX(deputy) AS appeal,\n    COUNT(deputy) AS adobe\n  FROM cte_2\n  GROUP BY\n    hunter,\n    blouse\n),\ncte_4 AS (\n  SELECT\n    hunter,\n    blouse\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hunter,\n     blouse,\n     deputy,\n     enzyme,\n     check\n|> AGGREGATE\n     MAX(check) AS saucer\n   GROUP BY\n     hunter,\n     blouse,\n     deputy\n|> AGGREGATE\n     MAX(saucer) AS mafia\n   GROUP BY\n     hunter,\n     blouse,\n     deputy\n|> AGGREGATE\n     MIN(deputy) AS mount,\n     SUM(agony) AS uproar,\n     MAX(deputy) AS appeal,\n     COUNT(deputy) AS adobe\n   GROUP BY\n     hunter,\n     blouse\n|> SELECT\n     hunter,\n     blouse\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"agony",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    roche,\n    kinase,\n    poster,\n    locke,\n    reflux\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    roche,\n    kinase,\n    poster,\n    locke,\n    COUNT(reflux) AS rector,\n    MIN(reflux) AS heron\n  FROM cte_0\n  GROUP BY\n    roche,\n    kinase,\n    poster,\n    locke\n),\ncte_2 AS (\n  SELECT\n    roche,\n    kinase,\n    poster,\n    locke,\n    COUNT(heron) AS think,\n    MAX(heron) AS pulse,\n    SUM(ratio) AS grade\n  FROM cte_1\n  GROUP BY\n    roche,\n    kinase,\n    poster,\n    locke\n),\ncte_3 AS (\n  SELECT\n    roche,\n    kinase\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(kinase) AS miller\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     roche,\n     kinase,\n     poster,\n     locke,\n     reflux\n|> AGGREGATE\n     COUNT(reflux) AS rector,\n     MIN(reflux) AS heron\n   GROUP BY\n     roche,\n     kinase,\n     poster,\n     locke\n|> AGGREGATE\n     COUNT(heron) AS think,\n     MAX(heron) AS pulse,\n     SUM(ratio) AS grade\n   GROUP BY\n     roche,\n     kinase,\n     poster,\n     locke\n|> SELECT\n     roche,\n     kinase\n|> AGGREGATE\n     MAX(kinase) AS miller\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"ratio",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:19},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    brink,\n    craig,\n    rebate,\n    maple,\n    bruce\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    brink,\n    craig,\n    rebate,\n    maple,\n    AVG(bruce) AS window\n  FROM cte_0\n  GROUP BY\n    brink,\n    craig,\n    rebate,\n    maple\n),\ncte_2 AS (\n  SELECT\n    brink,\n    craig,\n    MAX(window) AS parker\n  FROM cte_1\n  GROUP BY\n    brink,\n    craig,\n    rebate,\n    maple\n),\ncte_3 AS (\n  SELECT\n    brink,\n    craig,\n    COUNT(frame) AS widow,\n    AVG(parker) AS lender,\n    COUNT(parker) AS mungo\n  FROM cte_2\n  GROUP BY\n    brink,\n    craig\n),\ncte_4 AS (\n  SELECT\n    MIN(lender) AS editor\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     brink,\n     craig,\n     rebate,\n     maple,\n     bruce\n|> AGGREGATE\n     AVG(bruce) AS window\n   GROUP BY\n     brink,\n     craig,\n     rebate,\n     maple\n|> AGGREGATE\n     MAX(window) AS parker\n   GROUP BY\n     brink,\n     craig,\n     rebate,\n     maple\n|> AGGREGATE\n     COUNT(frame) AS widow,\n     AVG(parker) AS lender,\n     COUNT(parker) AS mungo\n   GROUP BY\n     brink,\n     craig\n|> AGGREGATE\n     MIN(lender) AS editor\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"frame",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    utopia,\n    cache,\n    plague,\n    torque,\n    debut\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    utopia,\n    cache,\n    plague,\n    MIN(torque) AS bobby\n  FROM cte_0\n  GROUP BY\n    utopia,\n    cache,\n    plague\n),\ncte_2 AS (\n  SELECT\n    utopia,\n    cache,\n    plague,\n    MIN(bobby) AS crook\n  FROM cte_1\n  GROUP BY\n    utopia,\n    cache,\n    plague\n),\ncte_3 AS (\n  SELECT\n    utopia,\n    cache,\n    MAX(plague) AS lodge,\n    MAX(fulham) AS output,\n    COUNT(plague) AS leone\n  FROM cte_2\n  GROUP BY\n    utopia,\n    cache\n),\ncte_4 AS (\n  SELECT\n    utopia,\n    cache,\n    lodge,\n    leone\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     utopia,\n     cache,\n     plague,\n     torque,\n     debut\n|> AGGREGATE\n     MIN(torque) AS bobby\n   GROUP BY\n     utopia,\n     cache,\n     plague\n|> AGGREGATE\n     MIN(bobby) AS crook\n   GROUP BY\n     utopia,\n     cache,\n     plague\n|> AGGREGATE\n     MAX(plague) AS lodge,\n     MAX(fulham) AS output,\n     COUNT(plague) AS leone\n   GROUP BY\n     utopia,\n     cache\n|> SELECT\n     utopia,\n     cache,\n     lodge,\n     leone\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"fulham",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    portal,\n    refuge,\n    arrow,\n    carpet,\n    trout\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    portal,\n    refuge,\n    arrow,\n    carpet,\n    COUNT(trout) AS course\n  FROM cte_0\n  GROUP BY\n    portal,\n    refuge,\n    arrow,\n    carpet\n),\ncte_2 AS (\n  SELECT\n    portal,\n    refuge,\n    arrow,\n    MAX(course) AS silver\n  FROM cte_1\n  GROUP BY\n    portal,\n    refuge,\n    arrow,\n    carpet\n),\ncte_3 AS (\n  SELECT\n    portal,\n    MAX(scrub) AS plague,\n    MIN(silver) AS oxygen\n  FROM cte_2\n  GROUP BY\n    portal\n),\ncte_4 AS (\n  SELECT\n    AVG(oxygen) AS crash,\n    MAX(oxygen) AS design\n  FROM cte_3\n  GROUP BY\n    portal\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     portal,\n     refuge,\n     arrow,\n     carpet,\n     trout\n|> AGGREGATE\n     COUNT(trout) AS course\n   GROUP BY\n     portal,\n     refuge,\n     arrow,\n     carpet\n|> AGGREGATE\n     MAX(course) AS silver\n   GROUP BY\n     portal,\n     refuge,\n     arrow,\n     carpet\n|> AGGREGATE\n     MAX(scrub) AS plague,\n     MIN(silver) AS oxygen\n   GROUP BY\n     portal\n|> AGGREGATE\n     AVG(oxygen) AS crash,\n     MAX(oxygen) AS design\n   GROUP BY\n     portal\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"scrub",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    stall,\n    freeze,\n    trench,\n    bleach,\n    bacon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    stall,\n    freeze,\n    trench,\n    SUM(bacon) AS thief\n  FROM cte_0\n  GROUP BY\n    stall,\n    freeze,\n    trench\n),\ncte_2 AS (\n  SELECT\n    stall,\n    freeze,\n    MAX(thief) AS millie,\n    COUNT(thief) AS mirror\n  FROM cte_1\n  GROUP BY\n    stall,\n    freeze,\n    trench\n),\ncte_3 AS (\n  SELECT\n    stall,\n    freeze,\n    millie,\n    SUM(brown) AS wendy,\n    SUM(mirror) AS vicar\n  FROM cte_2\n  GROUP BY\n    stall,\n    freeze,\n    millie\n),\ncte_4 AS (\n  SELECT\n    stall,\n    freeze,\n    millie\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     stall,\n     freeze,\n     trench,\n     bleach,\n     bacon\n|> AGGREGATE\n     SUM(bacon) AS thief\n   GROUP BY\n     stall,\n     freeze,\n     trench\n|> AGGREGATE\n     MAX(thief) AS millie,\n     COUNT(thief) AS mirror\n   GROUP BY\n     stall,\n     freeze,\n     trench\n|> AGGREGATE\n     SUM(brown) AS wendy,\n     SUM(mirror) AS vicar\n   GROUP BY\n     stall,\n     freeze,\n     millie\n|> SELECT\n     stall,\n     freeze,\n     millie\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"brown",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hazard,\n    viewer,\n    fleece,\n    ethos,\n    molla\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    hazard,\n    viewer,\n    fleece,\n    COUNT(ethos) AS barman\n  FROM cte_0\n  GROUP BY\n    hazard,\n    viewer,\n    fleece\n),\ncte_2 AS (\n  SELECT\n    hazard,\n    viewer,\n    fleece,\n    MIN(barman) AS vogue,\n    MAX(barman) AS homage\n  FROM cte_1\n  GROUP BY\n    hazard,\n    viewer,\n    fleece\n),\ncte_3 AS (\n  SELECT\n    hazard,\n    viewer,\n    MAX(basic) AS legacy,\n    AVG(homage) AS lodge,\n    COUNT(homage) AS style\n  FROM cte_2\n  GROUP BY\n    hazard,\n    viewer\n),\ncte_4 AS (\n  SELECT\n    AVG(hazard) AS abuse\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hazard,\n     viewer,\n     fleece,\n     ethos,\n     molla\n|> AGGREGATE\n     COUNT(ethos) AS barman\n   GROUP BY\n     hazard,\n     viewer,\n     fleece\n|> AGGREGATE\n     MIN(barman) AS vogue,\n     MAX(barman) AS homage\n   GROUP BY\n     hazard,\n     viewer,\n     fleece\n|> AGGREGATE\n     MAX(basic) AS legacy,\n     AVG(homage) AS lodge,\n     COUNT(homage) AS style\n   GROUP BY\n     hazard,\n     viewer\n|> AGGREGATE\n     AVG(hazard) AS abuse\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"basic",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    myriad,\n    sword,\n    toast,\n    guest,\n    poetry\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    myriad,\n    sword,\n    toast,\n    COUNT(poetry) AS wharf,\n    SUM(poetry) AS remand\n  FROM cte_0\n  GROUP BY\n    myriad,\n    sword,\n    toast\n),\ncte_2 AS (\n  SELECT\n    myriad,\n    sword,\n    toast,\n    AVG(remand) AS cheer\n  FROM cte_1\n  GROUP BY\n    myriad,\n    sword,\n    toast\n),\ncte_3 AS (\n  SELECT\n    myriad,\n    sword,\n    SUM(fibre) AS battle,\n    SUM(toast) AS henley\n  FROM cte_2\n  GROUP BY\n    myriad,\n    sword\n),\ncte_4 AS (\n  SELECT\n    COUNT(sword) AS thread,\n    MAX(henley) AS jetty\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     myriad,\n     sword,\n     toast,\n     guest,\n     poetry\n|> AGGREGATE\n     COUNT(poetry) AS wharf,\n     SUM(poetry) AS remand\n   GROUP BY\n     myriad,\n     sword,\n     toast\n|> AGGREGATE\n     AVG(remand) AS cheer\n   GROUP BY\n     myriad,\n     sword,\n     toast\n|> AGGREGATE\n     SUM(fibre) AS battle,\n     SUM(toast) AS henley\n   GROUP BY\n     myriad,\n     sword\n|> AGGREGATE\n     COUNT(sword) AS thread,\n     MAX(henley) AS jetty\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"fibre",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    dismay,\n    stake,\n    region,\n    mosaic,\n    throat\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    dismay,\n    stake,\n    region,\n    mosaic,\n    SUM(throat) AS wally\n  FROM cte_0\n  GROUP BY\n    dismay,\n    stake,\n    region,\n    mosaic\n),\ncte_2 AS (\n  SELECT\n    dismay,\n    stake,\n    region,\n    mosaic,\n    AVG(wally) AS glover\n  FROM cte_1\n  GROUP BY\n    dismay,\n    stake,\n    region,\n    mosaic\n),\ncte_3 AS (\n  SELECT\n    AVG(signal) AS honda,\n    COUNT(glover) AS digest,\n    SUM(mosaic) AS condom\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(digest) AS sheet,\n    MAX(digest) AS spouse\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     dismay,\n     stake,\n     region,\n     mosaic,\n     throat\n|> AGGREGATE\n     SUM(throat) AS wally\n   GROUP BY\n     dismay,\n     stake,\n     region,\n     mosaic\n|> AGGREGATE\n     AVG(wally) AS glover\n   GROUP BY\n     dismay,\n     stake,\n     region,\n     mosaic\n|> AGGREGATE\n     AVG(signal) AS honda,\n     COUNT(glover) AS digest,\n     SUM(mosaic) AS condom\n|> AGGREGATE\n     SUM(digest) AS sheet,\n     MAX(digest) AS spouse\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"signal",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    horne,\n    altar,\n    barrow,\n    debit,\n    waist\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    horne,\n    altar,\n    barrow,\n    debit,\n    MAX(waist) AS master\n  FROM cte_0\n  GROUP BY\n    horne,\n    altar,\n    barrow,\n    debit\n),\ncte_2 AS (\n  SELECT\n    horne,\n    altar,\n    AVG(master) AS patrol\n  FROM cte_1\n  GROUP BY\n    horne,\n    altar,\n    barrow,\n    debit\n),\ncte_3 AS (\n  SELECT\n    horne,\n    altar,\n    MAX(spouse) AS peril,\n    SUM(patrol) AS finch\n  FROM cte_2\n  GROUP BY\n    horne,\n    altar\n),\ncte_4 AS (\n  SELECT\n    horne,\n    SUM(finch) AS laser\n  FROM cte_3\n  GROUP BY\n    horne\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     horne,\n     altar,\n     barrow,\n     debit,\n     waist\n|> AGGREGATE\n     MAX(waist) AS master\n   GROUP BY\n     horne,\n     altar,\n     barrow,\n     debit\n|> AGGREGATE\n     AVG(master) AS patrol\n   GROUP BY\n     horne,\n     altar,\n     barrow,\n     debit\n|> AGGREGATE\n     MAX(spouse) AS peril,\n     SUM(patrol) AS finch\n   GROUP BY\n     horne,\n     altar\n|> AGGREGATE\n     SUM(finch) AS laser\n   GROUP BY\n     horne\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"spouse",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sherry,\n    spire,\n    skirt,\n    venus,\n    niece\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    sherry,\n    spire,\n    skirt,\n    venus,\n    AVG(niece) AS demise\n  FROM cte_0\n  GROUP BY\n    sherry,\n    spire,\n    skirt,\n    venus\n),\ncte_2 AS (\n  SELECT\n    sherry,\n    spire,\n    AVG(venus) AS bother\n  FROM cte_1\n  GROUP BY\n    sherry,\n    spire\n),\ncte_3 AS (\n  SELECT\n    sherry,\n    spire,\n    COUNT(bother) AS parade,\n    SUM(gland) AS tackle\n  FROM cte_2\n  GROUP BY\n    sherry,\n    spire\n),\ncte_4 AS (\n  SELECT\n    MAX(spire) AS scream,\n    MAX(parade) AS bowie,\n    AVG(parade) AS reign\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sherry,\n     spire,\n     skirt,\n     venus,\n     niece\n|> AGGREGATE\n     AVG(niece) AS demise\n   GROUP BY\n     sherry,\n     spire,\n     skirt,\n     venus\n|> AGGREGATE\n     AVG(venus) AS bother\n   GROUP BY\n     sherry,\n     spire\n|> AGGREGATE\n     COUNT(bother) AS parade,\n     SUM(gland) AS tackle\n   GROUP BY\n     sherry,\n     spire\n|> AGGREGATE\n     MAX(spire) AS scream,\n     MAX(parade) AS bowie,\n     AVG(parade) AS reign\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"gland",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    closet,\n    realm,\n    union,\n    guest,\n    fleece\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    closet,\n    realm,\n    union,\n    guest,\n    AVG(fleece) AS screen\n  FROM cte_0\n  GROUP BY\n    closet,\n    realm,\n    union,\n    guest\n),\ncte_2 AS (\n  SELECT\n    closet,\n    realm,\n    SUM(guest) AS payne,\n    COUNT(union) AS doubt\n  FROM cte_1\n  GROUP BY\n    closet,\n    realm\n),\ncte_3 AS (\n  SELECT\n    closet,\n    realm,\n    AVG(caddie) AS arson,\n    COUNT(doubt) AS query\n  FROM cte_2\n  GROUP BY\n    closet,\n    realm,\n    payne\n),\ncte_4 AS (\n  SELECT\n    closet,\n    realm,\n    query\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     closet,\n     realm,\n     union,\n     guest,\n     fleece\n|> AGGREGATE\n     AVG(fleece) AS screen\n   GROUP BY\n     closet,\n     realm,\n     union,\n     guest\n|> AGGREGATE\n     SUM(guest) AS payne,\n     COUNT(union) AS doubt\n   GROUP BY\n     closet,\n     realm\n|> AGGREGATE\n     AVG(caddie) AS arson,\n     COUNT(doubt) AS query\n   GROUP BY\n     closet,\n     realm,\n     payne\n|> SELECT\n     closet,\n     realm,\n     query\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"caddie",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    stout,\n    moment,\n    petrol,\n    liner,\n    alien\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    stout,\n    moment,\n    petrol,\n    liner,\n    AVG(alien) AS owner,\n    MIN(alien) AS donkey\n  FROM cte_0\n  GROUP BY\n    stout,\n    moment,\n    petrol,\n    liner\n),\ncte_2 AS (\n  SELECT\n    stout,\n    moment,\n    COUNT(donkey) AS shell\n  FROM cte_1\n  GROUP BY\n    stout,\n    moment\n),\ncte_3 AS (\n  SELECT\n    stout,\n    moment,\n    MIN(image) AS actor,\n    MIN(shell) AS amber\n  FROM cte_2\n  GROUP BY\n    stout,\n    moment\n),\ncte_4 AS (\n  SELECT\n    MIN(amber) AS pillar\n  FROM cte_3\n  GROUP BY\n    stout,\n    moment\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     stout,\n     moment,\n     petrol,\n     liner,\n     alien\n|> AGGREGATE\n     AVG(alien) AS owner,\n     MIN(alien) AS donkey\n   GROUP BY\n     stout,\n     moment,\n     petrol,\n     liner\n|> AGGREGATE\n     COUNT(donkey) AS shell\n   GROUP BY\n     stout,\n     moment\n|> AGGREGATE\n     MIN(image) AS actor,\n     MIN(shell) AS amber\n   GROUP BY\n     stout,\n     moment\n|> AGGREGATE\n     MIN(amber) AS pillar\n   GROUP BY\n     stout,\n     moment\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"image",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bonnet,\n    scare,\n    feeder,\n    bryony,\n    burger\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    bonnet,\n    scare,\n    feeder,\n    MAX(bryony) AS feast\n  FROM cte_0\n  GROUP BY\n    bonnet,\n    scare,\n    feeder\n),\ncte_2 AS (\n  SELECT\n    bonnet,\n    scare,\n    feeder,\n    COUNT(feast) AS fossil,\n    SUM(feast) AS grill\n  FROM cte_1\n  GROUP BY\n    bonnet,\n    scare,\n    feeder\n),\ncte_3 AS (\n  SELECT\n    bonnet,\n    scare,\n    SUM(theft) AS laser,\n    COUNT(grill) AS raffle\n  FROM cte_2\n  GROUP BY\n    bonnet,\n    scare,\n    feeder,\n    fossil\n),\ncte_4 AS (\n  SELECT\n    MAX(scare) AS league\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bonnet,\n     scare,\n     feeder,\n     bryony,\n     burger\n|> AGGREGATE\n     MAX(bryony) AS feast\n   GROUP BY\n     bonnet,\n     scare,\n     feeder\n|> AGGREGATE\n     COUNT(feast) AS fossil,\n     SUM(feast) AS grill\n   GROUP BY\n     bonnet,\n     scare,\n     feeder\n|> AGGREGATE\n     SUM(theft) AS laser,\n     COUNT(grill) AS raffle\n   GROUP BY\n     bonnet,\n     scare,\n     feeder,\n     fossil\n|> AGGREGATE\n     MAX(scare) AS league\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"theft",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    glare,\n    runway,\n    stance,\n    damage,\n    estate\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    glare,\n    runway,\n    stance,\n    AVG(damage) AS sight\n  FROM cte_0\n  GROUP BY\n    glare,\n    runway,\n    stance\n),\ncte_2 AS (\n  SELECT\n    glare,\n    runway,\n    stance,\n    COUNT(sight) AS colony\n  FROM cte_1\n  GROUP BY\n    glare,\n    runway,\n    stance\n),\ncte_3 AS (\n  SELECT\n    glare,\n    runway,\n    MIN(stance) AS ambush,\n    MAX(hopper) AS bully,\n    MIN(colony) AS dismay\n  FROM cte_2\n  GROUP BY\n    glare,\n    runway\n),\ncte_4 AS (\n  SELECT\n    glare,\n    COUNT(dismay) AS crush\n  FROM cte_3\n  GROUP BY\n    glare\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     glare,\n     runway,\n     stance,\n     damage,\n     estate\n|> AGGREGATE\n     AVG(damage) AS sight\n   GROUP BY\n     glare,\n     runway,\n     stance\n|> AGGREGATE\n     COUNT(sight) AS colony\n   GROUP BY\n     glare,\n     runway,\n     stance\n|> AGGREGATE\n     MIN(stance) AS ambush,\n     MAX(hopper) AS bully,\n     MIN(colony) AS dismay\n   GROUP BY\n     glare,\n     runway\n|> AGGREGATE\n     COUNT(dismay) AS crush\n   GROUP BY\n     glare\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"hopper",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    cover,\n    trout,\n    saloon,\n    debut,\n    sample\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    cover,\n    trout,\n    saloon,\n    debut,\n    AVG(sample) AS famine\n  FROM cte_0\n  GROUP BY\n    cover,\n    trout,\n    saloon,\n    debut\n),\ncte_2 AS (\n  SELECT\n    cover,\n    trout,\n    saloon,\n    debut,\n    MAX(famine) AS humour\n  FROM cte_1\n  GROUP BY\n    cover,\n    trout,\n    saloon,\n    debut\n),\ncte_3 AS (\n  SELECT\n    MAX(scream) AS wreck,\n    MIN(saloon) AS canyon,\n    SUM(debut) AS pupil\n  FROM cte_2\n  GROUP BY\n    cover,\n    trout\n),\ncte_4 AS (\n  SELECT\n    SUM(pupil) AS impact\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     cover,\n     trout,\n     saloon,\n     debut,\n     sample\n|> AGGREGATE\n     AVG(sample) AS famine\n   GROUP BY\n     cover,\n     trout,\n     saloon,\n     debut\n|> AGGREGATE\n     MAX(famine) AS humour\n   GROUP BY\n     cover,\n     trout,\n     saloon,\n     debut\n|> AGGREGATE\n     MAX(scream) AS wreck,\n     MIN(saloon) AS canyon,\n     SUM(debut) AS pupil\n   GROUP BY\n     cover,\n     trout\n|> AGGREGATE\n     SUM(pupil) AS impact\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"scream",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    toxin,\n    butler,\n    shoot,\n    secret,\n    reflux\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    toxin,\n    butler,\n    shoot,\n    MAX(secret) AS bacon,\n    AVG(reflux) AS salmon\n  FROM cte_0\n  GROUP BY\n    toxin,\n    butler,\n    shoot\n),\ncte_2 AS (\n  SELECT\n    toxin,\n    butler,\n    shoot,\n    MIN(bacon) AS vacuum\n  FROM cte_1\n  GROUP BY\n    toxin,\n    butler,\n    shoot\n),\ncte_3 AS (\n  SELECT\n    toxin,\n    butler,\n    AVG(depth) AS defect,\n    SUM(shoot) AS worry,\n    AVG(shoot) AS mount\n  FROM cte_2\n  GROUP BY\n    toxin,\n    butler\n),\ncte_4 AS (\n  SELECT\n    COUNT(toxin) AS rebel\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     toxin,\n     butler,\n     shoot,\n     secret,\n     reflux\n|> AGGREGATE\n     MAX(secret) AS bacon,\n     AVG(reflux) AS salmon\n   GROUP BY\n     toxin,\n     butler,\n     shoot\n|> AGGREGATE\n     MIN(bacon) AS vacuum\n   GROUP BY\n     toxin,\n     butler,\n     shoot\n|> AGGREGATE\n     AVG(depth) AS defect,\n     SUM(shoot) AS worry,\n     AVG(shoot) AS mount\n   GROUP BY\n     toxin,\n     butler\n|> AGGREGATE\n     COUNT(toxin) AS rebel\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"depth",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    puppy,\n    heater,\n    fridge,\n    metal,\n    comet\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    puppy,\n    heater,\n    fridge,\n    AVG(comet) AS bridge\n  FROM cte_0\n  GROUP BY\n    puppy,\n    heater,\n    fridge\n),\ncte_2 AS (\n  SELECT\n    puppy,\n    heater,\n    fridge,\n    MAX(bridge) AS sprint,\n    AVG(bridge) AS delta\n  FROM cte_1\n  GROUP BY\n    puppy,\n    heater,\n    fridge\n),\ncte_3 AS (\n  SELECT\n    puppy,\n    heater,\n    AVG(clover) AS rover,\n    COUNT(fridge) AS pastor,\n    MIN(sprint) AS portal\n  FROM cte_2\n  GROUP BY\n    puppy,\n    heater\n),\ncte_4 AS (\n  SELECT\n    MIN(puppy) AS safety\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     puppy,\n     heater,\n     fridge,\n     metal,\n     comet\n|> AGGREGATE\n     AVG(comet) AS bridge\n   GROUP BY\n     puppy,\n     heater,\n     fridge\n|> AGGREGATE\n     MAX(bridge) AS sprint,\n     AVG(bridge) AS delta\n   GROUP BY\n     puppy,\n     heater,\n     fridge\n|> AGGREGATE\n     AVG(clover) AS rover,\n     COUNT(fridge) AS pastor,\n     MIN(sprint) AS portal\n   GROUP BY\n     puppy,\n     heater\n|> AGGREGATE\n     MIN(puppy) AS safety\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"clover",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    pinch,\n    relief,\n    binder,\n    talbot,\n    worry\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    pinch,\n    relief,\n    MIN(talbot) AS daphne\n  FROM cte_0\n  GROUP BY\n    pinch,\n    relief\n),\ncte_2 AS (\n  SELECT\n    pinch,\n    relief,\n    COUNT(daphne) AS jelly,\n    MIN(daphne) AS temple,\n    AVG(daphne) AS trough\n  FROM cte_1\n  GROUP BY\n    pinch,\n    relief\n),\ncte_3 AS (\n  SELECT\n    pinch,\n    relief,\n    jelly,\n    temple,\n    AVG(gossip) AS fruit,\n    MIN(trough) AS excess\n  FROM cte_2\n  GROUP BY\n    pinch,\n    relief,\n    jelly,\n    temple\n),\ncte_4 AS (\n  SELECT\n    COUNT(relief) AS couch\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     pinch,\n     relief,\n     binder,\n     talbot,\n     worry\n|> AGGREGATE\n     MIN(talbot) AS daphne\n   GROUP BY\n     pinch,\n     relief\n|> AGGREGATE\n     COUNT(daphne) AS jelly,\n     MIN(daphne) AS temple,\n     AVG(daphne) AS trough\n   GROUP BY\n     pinch,\n     relief\n|> AGGREGATE\n     AVG(gossip) AS fruit,\n     MIN(trough) AS excess\n   GROUP BY\n     pinch,\n     relief,\n     jelly,\n     temple\n|> AGGREGATE\n     COUNT(relief) AS couch\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"gossip",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    smear,\n    poster,\n    snake,\n    creed,\n    sheet\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    smear,\n    poster,\n    snake,\n    creed,\n    AVG(sheet) AS equity\n  FROM cte_0\n  GROUP BY\n    smear,\n    poster,\n    snake,\n    creed\n),\ncte_2 AS (\n  SELECT\n    smear,\n    poster,\n    snake,\n    AVG(equity) AS bacon\n  FROM cte_1\n  GROUP BY\n    smear,\n    poster,\n    snake,\n    creed\n),\ncte_3 AS (\n  SELECT\n    smear,\n    COUNT(diesel) AS truth,\n    COUNT(bacon) AS pilot\n  FROM cte_2\n  GROUP BY\n    smear\n),\ncte_4 AS (\n  SELECT\n    SUM(pilot) AS glass,\n    MIN(pilot) AS status\n  FROM cte_3\n  GROUP BY\n    smear\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     smear,\n     poster,\n     snake,\n     creed,\n     sheet\n|> AGGREGATE\n     AVG(sheet) AS equity\n   GROUP BY\n     smear,\n     poster,\n     snake,\n     creed\n|> AGGREGATE\n     AVG(equity) AS bacon\n   GROUP BY\n     smear,\n     poster,\n     snake,\n     creed\n|> AGGREGATE\n     COUNT(diesel) AS truth,\n     COUNT(bacon) AS pilot\n   GROUP BY\n     smear\n|> AGGREGATE\n     SUM(pilot) AS glass,\n     MIN(pilot) AS status\n   GROUP BY\n     smear\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"diesel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    morris,\n    booze,\n    miller,\n    piper,\n    union\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    morris,\n    booze,\n    AVG(miller) AS vision\n  FROM cte_0\n  GROUP BY\n    morris,\n    booze\n),\ncte_2 AS (\n  SELECT\n    morris,\n    booze,\n    MAX(vision) AS tally,\n    COUNT(vision) AS depth,\n    AVG(vision) AS gloria\n  FROM cte_1\n  GROUP BY\n    morris,\n    booze\n),\ncte_3 AS (\n  SELECT\n    morris,\n    booze,\n    tally,\n    depth,\n    MAX(cross) AS weight,\n    SUM(gloria) AS grade\n  FROM cte_2\n  GROUP BY\n    morris,\n    booze,\n    tally,\n    depth\n),\ncte_4 AS (\n  SELECT\n    morris,\n    booze\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     morris,\n     booze,\n     miller,\n     piper,\n     union\n|> AGGREGATE\n     AVG(miller) AS vision\n   GROUP BY\n     morris,\n     booze\n|> AGGREGATE\n     MAX(vision) AS tally,\n     COUNT(vision) AS depth,\n     AVG(vision) AS gloria\n   GROUP BY\n     morris,\n     booze\n|> AGGREGATE\n     MAX(cross) AS weight,\n     SUM(gloria) AS grade\n   GROUP BY\n     morris,\n     booze,\n     tally,\n     depth\n|> SELECT\n     morris,\n     booze\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"cross",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    retina,\n    gully,\n    comer,\n    pizza,\n    onion\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    retina,\n    gully,\n    comer,\n    pizza,\n    AVG(onion) AS spite\n  FROM cte_0\n  GROUP BY\n    retina,\n    gully,\n    comer,\n    pizza\n),\ncte_2 AS (\n  SELECT\n    retina,\n    gully,\n    AVG(pizza) AS glider\n  FROM cte_1\n  GROUP BY\n    retina,\n    gully\n),\ncte_3 AS (\n  SELECT\n    retina,\n    gully,\n    MIN(glider) AS level,\n    MAX(palace) AS caste,\n    COUNT(glider) AS robot\n  FROM cte_2\n  GROUP BY\n    retina,\n    gully\n),\ncte_4 AS (\n  SELECT\n    retina,\n    gully,\n    level,\n    robot\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     retina,\n     gully,\n     comer,\n     pizza,\n     onion\n|> AGGREGATE\n     AVG(onion) AS spite\n   GROUP BY\n     retina,\n     gully,\n     comer,\n     pizza\n|> AGGREGATE\n     AVG(pizza) AS glider\n   GROUP BY\n     retina,\n     gully\n|> AGGREGATE\n     MIN(glider) AS level,\n     MAX(palace) AS caste,\n     COUNT(glider) AS robot\n   GROUP BY\n     retina,\n     gully\n|> SELECT\n     retina,\n     gully,\n     level,\n     robot\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"palace",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    collar,\n    crown,\n    other,\n    sequel,\n    donkey\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    collar,\n    crown,\n    COUNT(donkey) AS safety\n  FROM cte_0\n  GROUP BY\n    collar,\n    crown\n),\ncte_2 AS (\n  SELECT\n    collar,\n    crown,\n    AVG(safety) AS thumb,\n    MIN(safety) AS lough,\n    MAX(safety) AS ribber\n  FROM cte_1\n  GROUP BY\n    collar,\n    crown\n),\ncte_3 AS (\n  SELECT\n    collar,\n    crown,\n    thumb,\n    lough,\n    AVG(ariel) AS settee,\n    MIN(ribber) AS party\n  FROM cte_2\n  GROUP BY\n    collar,\n    crown,\n    thumb,\n    lough\n),\ncte_4 AS (\n  SELECT\n    COUNT(lough) AS palmer\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     collar,\n     crown,\n     other,\n     sequel,\n     donkey\n|> AGGREGATE\n     COUNT(donkey) AS safety\n   GROUP BY\n     collar,\n     crown\n|> AGGREGATE\n     AVG(safety) AS thumb,\n     MIN(safety) AS lough,\n     MAX(safety) AS ribber\n   GROUP BY\n     collar,\n     crown\n|> AGGREGATE\n     AVG(ariel) AS settee,\n     MIN(ribber) AS party\n   GROUP BY\n     collar,\n     crown,\n     thumb,\n     lough\n|> AGGREGATE\n     COUNT(lough) AS palmer\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"ariel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    jargon,\n    legion,\n    vector,\n    barker,\n    tyler\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    jargon,\n    legion,\n    vector,\n    barker,\n    MIN(tyler) AS violin\n  FROM cte_0\n  GROUP BY\n    jargon,\n    legion,\n    vector,\n    barker\n),\ncte_2 AS (\n  SELECT\n    jargon,\n    legion,\n    vector,\n    MIN(violin) AS strand\n  FROM cte_1\n  GROUP BY\n    jargon,\n    legion,\n    vector,\n    barker\n),\ncte_3 AS (\n  SELECT\n    jargon,\n    MIN(sprint) AS stand,\n    SUM(strand) AS tommy\n  FROM cte_2\n  GROUP BY\n    jargon,\n    legion,\n    vector\n),\ncte_4 AS (\n  SELECT\n    MAX(tommy) AS gloss\n  FROM cte_3\n  GROUP BY\n    jargon\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     jargon,\n     legion,\n     vector,\n     barker,\n     tyler\n|> AGGREGATE\n     MIN(tyler) AS violin\n   GROUP BY\n     jargon,\n     legion,\n     vector,\n     barker\n|> AGGREGATE\n     MIN(violin) AS strand\n   GROUP BY\n     jargon,\n     legion,\n     vector,\n     barker\n|> AGGREGATE\n     MIN(sprint) AS stand,\n     SUM(strand) AS tommy\n   GROUP BY\n     jargon,\n     legion,\n     vector\n|> AGGREGATE\n     MAX(tommy) AS gloss\n   GROUP BY\n     jargon\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"sprint",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    murder,\n    celery,\n    poster,\n    amber,\n    fringe\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    murder,\n    celery,\n    poster,\n    amber,\n    MAX(fringe) AS carpet\n  FROM cte_0\n  GROUP BY\n    murder,\n    celery,\n    poster,\n    amber\n),\ncte_2 AS (\n  SELECT\n    murder,\n    celery,\n    SUM(amber) AS vector\n  FROM cte_1\n  GROUP BY\n    murder,\n    celery\n),\ncte_3 AS (\n  SELECT\n    murder,\n    celery,\n    MIN(vector) AS trend,\n    AVG(locker) AS forest\n  FROM cte_2\n  GROUP BY\n    murder,\n    celery\n),\ncte_4 AS (\n  SELECT\n    MAX(trend) AS spate,\n    COUNT(trend) AS batty\n  FROM cte_3\n  GROUP BY\n    murder,\n    celery\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     murder,\n     celery,\n     poster,\n     amber,\n     fringe\n|> AGGREGATE\n     MAX(fringe) AS carpet\n   GROUP BY\n     murder,\n     celery,\n     poster,\n     amber\n|> AGGREGATE\n     SUM(amber) AS vector\n   GROUP BY\n     murder,\n     celery\n|> AGGREGATE\n     MIN(vector) AS trend,\n     AVG(locker) AS forest\n   GROUP BY\n     murder,\n     celery\n|> AGGREGATE\n     MAX(trend) AS spate,\n     COUNT(trend) AS batty\n   GROUP BY\n     murder,\n     celery\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"locker",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    crunch,\n    slump,\n    reward,\n    blaze,\n    fairy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    crunch,\n    slump,\n    reward,\n    SUM(fairy) AS memory,\n    COUNT(blaze) AS gossip\n  FROM cte_0\n  GROUP BY\n    crunch,\n    slump,\n    reward\n),\ncte_2 AS (\n  SELECT\n    crunch,\n    slump,\n    reward,\n    SUM(memory) AS knight\n  FROM cte_1\n  GROUP BY\n    crunch,\n    slump,\n    reward\n),\ncte_3 AS (\n  SELECT\n    crunch,\n    slump,\n    SUM(reflex) AS tower,\n    AVG(reward) AS stein\n  FROM cte_2\n  GROUP BY\n    crunch,\n    slump\n),\ncte_4 AS (\n  SELECT\n    crunch,\n    COUNT(slump) AS server\n  FROM cte_3\n  GROUP BY\n    crunch\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     crunch,\n     slump,\n     reward,\n     blaze,\n     fairy\n|> AGGREGATE\n     SUM(fairy) AS memory,\n     COUNT(blaze) AS gossip\n   GROUP BY\n     crunch,\n     slump,\n     reward\n|> AGGREGATE\n     SUM(memory) AS knight\n   GROUP BY\n     crunch,\n     slump,\n     reward\n|> AGGREGATE\n     SUM(reflex) AS tower,\n     AVG(reward) AS stein\n   GROUP BY\n     crunch,\n     slump\n|> AGGREGATE\n     COUNT(slump) AS server\n   GROUP BY\n     crunch\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"reflex",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    utopia,\n    single,\n    motion,\n    devon,\n    climax\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    utopia,\n    single,\n    motion,\n    SUM(climax) AS badger\n  FROM cte_0\n  GROUP BY\n    utopia,\n    single,\n    motion\n),\ncte_2 AS (\n  SELECT\n    utopia,\n    single,\n    motion,\n    MIN(badger) AS packet,\n    AVG(badger) AS spruce\n  FROM cte_1\n  GROUP BY\n    utopia,\n    single,\n    motion\n),\ncte_3 AS (\n  SELECT\n    utopia,\n    single,\n    AVG(dobson) AS blame,\n    SUM(packet) AS spire,\n    SUM(motion) AS sight\n  FROM cte_2\n  GROUP BY\n    utopia,\n    single\n),\ncte_4 AS (\n  SELECT\n    utopia,\n    single\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     utopia,\n     single,\n     motion,\n     devon,\n     climax\n|> AGGREGATE\n     SUM(climax) AS badger\n   GROUP BY\n     utopia,\n     single,\n     motion\n|> AGGREGATE\n     MIN(badger) AS packet,\n     AVG(badger) AS spruce\n   GROUP BY\n     utopia,\n     single,\n     motion\n|> AGGREGATE\n     AVG(dobson) AS blame,\n     SUM(packet) AS spire,\n     SUM(motion) AS sight\n   GROUP BY\n     utopia,\n     single\n|> SELECT\n     utopia,\n     single\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"dobson",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    aspect,\n    strain,\n    chang,\n    yeast,\n    china\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    aspect,\n    strain,\n    chang,\n    MIN(china) AS surge\n  FROM cte_0\n  GROUP BY\n    aspect,\n    strain,\n    chang\n),\ncte_2 AS (\n  SELECT\n    aspect,\n    strain,\n    chang,\n    AVG(surge) AS vendor\n  FROM cte_1\n  GROUP BY\n    aspect,\n    strain,\n    chang\n),\ncte_3 AS (\n  SELECT\n    aspect,\n    strain,\n    AVG(chang) AS effect,\n    MIN(scout) AS fender,\n    AVG(vendor) AS realm\n  FROM cte_2\n  GROUP BY\n    aspect,\n    strain\n),\ncte_4 AS (\n  SELECT\n    MIN(aspect) AS beauty,\n    COUNT(strain) AS remit\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     aspect,\n     strain,\n     chang,\n     yeast,\n     china\n|> AGGREGATE\n     MIN(china) AS surge\n   GROUP BY\n     aspect,\n     strain,\n     chang\n|> AGGREGATE\n     AVG(surge) AS vendor\n   GROUP BY\n     aspect,\n     strain,\n     chang\n|> AGGREGATE\n     AVG(chang) AS effect,\n     MIN(scout) AS fender,\n     AVG(vendor) AS realm\n   GROUP BY\n     aspect,\n     strain\n|> AGGREGATE\n     MIN(aspect) AS beauty,\n     COUNT(strain) AS remit\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"scout",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    basis,\n    tandem,\n    chord,\n    design,\n    craft\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    basis,\n    tandem,\n    chord,\n    design,\n    MAX(craft) AS tempo\n  FROM cte_0\n  GROUP BY\n    basis,\n    tandem,\n    chord,\n    design\n),\ncte_2 AS (\n  SELECT\n    basis,\n    tandem,\n    SUM(tempo) AS credit\n  FROM cte_1\n  GROUP BY\n    basis,\n    tandem\n),\ncte_3 AS (\n  SELECT\n    basis,\n    tandem,\n    SUM(credit) AS nature,\n    AVG(elaine) AS class,\n    AVG(credit) AS serial,\n    MIN(credit) AS parity\n  FROM cte_2\n  GROUP BY\n    basis,\n    tandem\n),\ncte_4 AS (\n  SELECT\n    COUNT(parity) AS sting\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     basis,\n     tandem,\n     chord,\n     design,\n     craft\n|> AGGREGATE\n     MAX(craft) AS tempo\n   GROUP BY\n     basis,\n     tandem,\n     chord,\n     design\n|> AGGREGATE\n     SUM(tempo) AS credit\n   GROUP BY\n     basis,\n     tandem\n|> AGGREGATE\n     SUM(credit) AS nature,\n     AVG(elaine) AS class,\n     AVG(credit) AS serial,\n     MIN(credit) AS parity\n   GROUP BY\n     basis,\n     tandem\n|> AGGREGATE\n     COUNT(parity) AS sting\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"elaine",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    regent,\n    cocoa,\n    single,\n    runner,\n    puppet\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    regent,\n    cocoa,\n    single,\n    runner,\n    COUNT(puppet) AS drake\n  FROM cte_0\n  GROUP BY\n    regent,\n    cocoa,\n    single,\n    runner\n),\ncte_2 AS (\n  SELECT\n    regent,\n    cocoa,\n    single,\n    SUM(drake) AS clerk\n  FROM cte_1\n  GROUP BY\n    regent,\n    cocoa,\n    single,\n    runner\n),\ncte_3 AS (\n  SELECT\n    regent,\n    MAX(index) AS cannon,\n    SUM(clerk) AS hitch,\n    AVG(cocoa) AS degree\n  FROM cte_2\n  GROUP BY\n    regent\n),\ncte_4 AS (\n  SELECT\n    regent,\n    hitch,\n    degree\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     regent,\n     cocoa,\n     single,\n     runner,\n     puppet\n|> AGGREGATE\n     COUNT(puppet) AS drake\n   GROUP BY\n     regent,\n     cocoa,\n     single,\n     runner\n|> AGGREGATE\n     SUM(drake) AS clerk\n   GROUP BY\n     regent,\n     cocoa,\n     single,\n     runner\n|> AGGREGATE\n     MAX(index) AS cannon,\n     SUM(clerk) AS hitch,\n     AVG(cocoa) AS degree\n   GROUP BY\n     regent\n|> SELECT\n     regent,\n     hitch,\n     degree\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"index",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    beauty,\n    finch,\n    lagoon,\n    assent,\n    carrot\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    beauty,\n    finch,\n    lagoon,\n    MIN(carrot) AS canvas,\n    MAX(assent) AS scare\n  FROM cte_0\n  GROUP BY\n    beauty,\n    finch,\n    lagoon\n),\ncte_2 AS (\n  SELECT\n    beauty,\n    finch,\n    lagoon,\n    MIN(scare) AS victim\n  FROM cte_1\n  GROUP BY\n    beauty,\n    finch,\n    lagoon\n),\ncte_3 AS (\n  SELECT\n    beauty,\n    finch,\n    COUNT(depth) AS sweep,\n    SUM(victim) AS fiddle\n  FROM cte_2\n  GROUP BY\n    beauty,\n    finch,\n    lagoon\n),\ncte_4 AS (\n  SELECT\n    beauty,\n    finch,\n    fiddle\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     beauty,\n     finch,\n     lagoon,\n     assent,\n     carrot\n|> AGGREGATE\n     MIN(carrot) AS canvas,\n     MAX(assent) AS scare\n   GROUP BY\n     beauty,\n     finch,\n     lagoon\n|> AGGREGATE\n     MIN(scare) AS victim\n   GROUP BY\n     beauty,\n     finch,\n     lagoon\n|> AGGREGATE\n     COUNT(depth) AS sweep,\n     SUM(victim) AS fiddle\n   GROUP BY\n     beauty,\n     finch,\n     lagoon\n|> SELECT\n     beauty,\n     finch,\n     fiddle\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"depth",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    device,\n    trial,\n    lagoon,\n    bidder,\n    salary\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    device,\n    trial,\n    lagoon,\n    bidder,\n    MAX(salary) AS sprint\n  FROM cte_0\n  GROUP BY\n    device,\n    trial,\n    lagoon,\n    bidder\n),\ncte_2 AS (\n  SELECT\n    device,\n    trial,\n    lagoon,\n    bidder,\n    SUM(sprint) AS cattle\n  FROM cte_1\n  GROUP BY\n    device,\n    trial,\n    lagoon,\n    bidder\n),\ncte_3 AS (\n  SELECT\n    AVG(magic) AS forte,\n    MIN(device) AS pastor,\n    COUNT(bidder) AS voice\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(pastor) AS xerox,\n    SUM(voice) AS forge\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     device,\n     trial,\n     lagoon,\n     bidder,\n     salary\n|> AGGREGATE\n     MAX(salary) AS sprint\n   GROUP BY\n     device,\n     trial,\n     lagoon,\n     bidder\n|> AGGREGATE\n     SUM(sprint) AS cattle\n   GROUP BY\n     device,\n     trial,\n     lagoon,\n     bidder\n|> AGGREGATE\n     AVG(magic) AS forte,\n     MIN(device) AS pastor,\n     COUNT(bidder) AS voice\n|> AGGREGATE\n     SUM(pastor) AS xerox,\n     SUM(voice) AS forge\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"magic",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    upland,\n    brute,\n    crack,\n    artery,\n    flurry\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    upland,\n    brute,\n    MAX(flurry) AS copper,\n    MAX(artery) AS desert\n  FROM cte_0\n  GROUP BY\n    upland,\n    brute,\n    crack\n),\ncte_2 AS (\n  SELECT\n    upland,\n    brute,\n    copper,\n    AVG(desert) AS scare\n  FROM cte_1\n  GROUP BY\n    upland,\n    brute,\n    copper\n),\ncte_3 AS (\n  SELECT\n    upland,\n    brute,\n    copper,\n    SUM(maggot) AS animal,\n    COUNT(scare) AS speed\n  FROM cte_2\n  GROUP BY\n    upland,\n    brute,\n    copper\n),\ncte_4 AS (\n  SELECT\n    upland,\n    brute,\n    copper\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     upland,\n     brute,\n     crack,\n     artery,\n     flurry\n|> AGGREGATE\n     MAX(flurry) AS copper,\n     MAX(artery) AS desert\n   GROUP BY\n     upland,\n     brute,\n     crack\n|> AGGREGATE\n     AVG(desert) AS scare\n   GROUP BY\n     upland,\n     brute,\n     copper\n|> AGGREGATE\n     SUM(maggot) AS animal,\n     COUNT(scare) AS speed\n   GROUP BY\n     upland,\n     brute,\n     copper\n|> SELECT\n     upland,\n     brute,\n     copper\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"maggot",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    chant,\n    baker,\n    resin,\n    tennis,\n    skill\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    chant,\n    baker,\n    resin,\n    MAX(tennis) AS vector\n  FROM cte_0\n  GROUP BY\n    chant,\n    baker,\n    resin\n),\ncte_2 AS (\n  SELECT\n    chant,\n    baker,\n    resin,\n    AVG(vector) AS packet\n  FROM cte_1\n  GROUP BY\n    chant,\n    baker,\n    resin\n),\ncte_3 AS (\n  SELECT\n    chant,\n    baker,\n    MIN(resin) AS cider,\n    SUM(marina) AS tonne,\n    SUM(resin) AS viola\n  FROM cte_2\n  GROUP BY\n    chant,\n    baker\n),\ncte_4 AS (\n  SELECT\n    chant,\n    MAX(cider) AS video\n  FROM cte_3\n  GROUP BY\n    chant\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     chant,\n     baker,\n     resin,\n     tennis,\n     skill\n|> AGGREGATE\n     MAX(tennis) AS vector\n   GROUP BY\n     chant,\n     baker,\n     resin\n|> AGGREGATE\n     AVG(vector) AS packet\n   GROUP BY\n     chant,\n     baker,\n     resin\n|> AGGREGATE\n     MIN(resin) AS cider,\n     SUM(marina) AS tonne,\n     SUM(resin) AS viola\n   GROUP BY\n     chant,\n     baker\n|> AGGREGATE\n     MAX(cider) AS video\n   GROUP BY\n     chant\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"marina",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    locker,\n    bowler,\n    roach,\n    hitch,\n    sponge\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    locker,\n    bowler,\n    roach,\n    hitch,\n    AVG(sponge) AS mungo\n  FROM cte_0\n  GROUP BY\n    locker,\n    bowler,\n    roach,\n    hitch\n),\ncte_2 AS (\n  SELECT\n    locker,\n    bowler,\n    roach,\n    hitch,\n    SUM(mungo) AS market\n  FROM cte_1\n  GROUP BY\n    locker,\n    bowler,\n    roach,\n    hitch\n),\ncte_3 AS (\n  SELECT\n    MAX(bream) AS future,\n    MIN(hitch) AS salon,\n    SUM(market) AS spade\n  FROM cte_2\n  GROUP BY\n    locker,\n    bowler\n),\ncte_4 AS (\n  SELECT\n    MAX(spade) AS mouse\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     locker,\n     bowler,\n     roach,\n     hitch,\n     sponge\n|> AGGREGATE\n     AVG(sponge) AS mungo\n   GROUP BY\n     locker,\n     bowler,\n     roach,\n     hitch\n|> AGGREGATE\n     SUM(mungo) AS market\n   GROUP BY\n     locker,\n     bowler,\n     roach,\n     hitch\n|> AGGREGATE\n     MAX(bream) AS future,\n     MIN(hitch) AS salon,\n     SUM(market) AS spade\n   GROUP BY\n     locker,\n     bowler\n|> AGGREGATE\n     MAX(spade) AS mouse\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"bream",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    cellar,\n    patch,\n    martin,\n    smyth,\n    reform\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    cellar,\n    patch,\n    AVG(smyth) AS purse\n  FROM cte_0\n  GROUP BY\n    cellar,\n    patch\n),\ncte_2 AS (\n  SELECT\n    cellar,\n    patch,\n    AVG(purse) AS lesion,\n    SUM(purse) AS murphy,\n    MIN(purse) AS draper\n  FROM cte_1\n  GROUP BY\n    cellar,\n    patch\n),\ncte_3 AS (\n  SELECT\n    cellar,\n    patch,\n    lesion,\n    murphy,\n    COUNT(wrist) AS depot,\n    SUM(draper) AS ethos\n  FROM cte_2\n  GROUP BY\n    cellar,\n    patch,\n    lesion,\n    murphy\n),\ncte_4 AS (\n  SELECT\n    COUNT(murphy) AS summit\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     cellar,\n     patch,\n     martin,\n     smyth,\n     reform\n|> AGGREGATE\n     AVG(smyth) AS purse\n   GROUP BY\n     cellar,\n     patch\n|> AGGREGATE\n     AVG(purse) AS lesion,\n     SUM(purse) AS murphy,\n     MIN(purse) AS draper\n   GROUP BY\n     cellar,\n     patch\n|> AGGREGATE\n     COUNT(wrist) AS depot,\n     SUM(draper) AS ethos\n   GROUP BY\n     cellar,\n     patch,\n     lesion,\n     murphy\n|> AGGREGATE\n     COUNT(murphy) AS summit\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"wrist",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    weight,\n    warmth,\n    whisky,\n    trial,\n    anthem\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    weight,\n    warmth,\n    whisky,\n    trial,\n    SUM(anthem) AS split\n  FROM cte_0\n  GROUP BY\n    weight,\n    warmth,\n    whisky,\n    trial\n),\ncte_2 AS (\n  SELECT\n    weight,\n    warmth,\n    AVG(whisky) AS accord\n  FROM cte_1\n  GROUP BY\n    weight,\n    warmth\n),\ncte_3 AS (\n  SELECT\n    weight,\n    warmth,\n    SUM(accord) AS comet,\n    AVG(thread) AS nature,\n    MAX(accord) AS yellow\n  FROM cte_2\n  GROUP BY\n    weight,\n    warmth\n),\ncte_4 AS (\n  SELECT\n    weight,\n    warmth,\n    comet,\n    yellow\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     weight,\n     warmth,\n     whisky,\n     trial,\n     anthem\n|> AGGREGATE\n     SUM(anthem) AS split\n   GROUP BY\n     weight,\n     warmth,\n     whisky,\n     trial\n|> AGGREGATE\n     AVG(whisky) AS accord\n   GROUP BY\n     weight,\n     warmth\n|> AGGREGATE\n     SUM(accord) AS comet,\n     AVG(thread) AS nature,\n     MAX(accord) AS yellow\n   GROUP BY\n     weight,\n     warmth\n|> SELECT\n     weight,\n     warmth,\n     comet,\n     yellow\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"thread",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    excess,\n    cycle,\n    thing,\n    shelf,\n    nation\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    excess,\n    cycle,\n    thing,\n    shelf,\n    SUM(nation) AS purse\n  FROM cte_0\n  GROUP BY\n    excess,\n    cycle,\n    thing,\n    shelf\n),\ncte_2 AS (\n  SELECT\n    excess,\n    cycle,\n    MIN(shelf) AS writer\n  FROM cte_1\n  GROUP BY\n    excess,\n    cycle\n),\ncte_3 AS (\n  SELECT\n    excess,\n    cycle,\n    COUNT(writer) AS issue,\n    MAX(reason) AS reader,\n    AVG(writer) AS binder,\n    SUM(writer) AS turner\n  FROM cte_2\n  GROUP BY\n    excess,\n    cycle\n),\ncte_4 AS (\n  SELECT\n    excess,\n    cycle\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     excess,\n     cycle,\n     thing,\n     shelf,\n     nation\n|> AGGREGATE\n     SUM(nation) AS purse\n   GROUP BY\n     excess,\n     cycle,\n     thing,\n     shelf\n|> AGGREGATE\n     MIN(shelf) AS writer\n   GROUP BY\n     excess,\n     cycle\n|> AGGREGATE\n     COUNT(writer) AS issue,\n     MAX(reason) AS reader,\n     AVG(writer) AS binder,\n     SUM(writer) AS turner\n   GROUP BY\n     excess,\n     cycle\n|> SELECT\n     excess,\n     cycle\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"reason",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rugby,\n    memory,\n    stuff,\n    bloom,\n    coward\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    rugby,\n    memory,\n    stuff,\n    COUNT(bloom) AS granny\n  FROM cte_0\n  GROUP BY\n    rugby,\n    memory,\n    stuff\n),\ncte_2 AS (\n  SELECT\n    rugby,\n    memory,\n    stuff,\n    MAX(granny) AS copper,\n    SUM(granny) AS panel\n  FROM cte_1\n  GROUP BY\n    rugby,\n    memory,\n    stuff\n),\ncte_3 AS (\n  SELECT\n    rugby,\n    memory,\n    MIN(doubt) AS paint,\n    COUNT(copper) AS condom\n  FROM cte_2\n  GROUP BY\n    rugby,\n    memory\n),\ncte_4 AS (\n  SELECT\n    MIN(condom) AS salary\n  FROM cte_3\n  GROUP BY\n    rugby,\n    memory\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rugby,\n     memory,\n     stuff,\n     bloom,\n     coward\n|> AGGREGATE\n     COUNT(bloom) AS granny\n   GROUP BY\n     rugby,\n     memory,\n     stuff\n|> AGGREGATE\n     MAX(granny) AS copper,\n     SUM(granny) AS panel\n   GROUP BY\n     rugby,\n     memory,\n     stuff\n|> AGGREGATE\n     MIN(doubt) AS paint,\n     COUNT(copper) AS condom\n   GROUP BY\n     rugby,\n     memory\n|> AGGREGATE\n     MIN(condom) AS salary\n   GROUP BY\n     rugby,\n     memory\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"doubt",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    knack,\n    video,\n    cohort,\n    locker,\n    unison\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    knack,\n    video,\n    cohort,\n    COUNT(unison) AS almond\n  FROM cte_0\n  GROUP BY\n    knack,\n    video,\n    cohort,\n    locker\n),\ncte_2 AS (\n  SELECT\n    knack,\n    video,\n    SUM(cohort) AS groove,\n    MAX(cohort) AS butter\n  FROM cte_1\n  GROUP BY\n    knack,\n    video\n),\ncte_3 AS (\n  SELECT\n    knack,\n    video,\n    groove,\n    SUM(stage) AS bishop,\n    COUNT(butter) AS resale\n  FROM cte_2\n  GROUP BY\n    knack,\n    video,\n    groove\n),\ncte_4 AS (\n  SELECT\n    AVG(resale) AS mouse\n  FROM cte_3\n  GROUP BY\n    knack\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     knack,\n     video,\n     cohort,\n     locker,\n     unison\n|> AGGREGATE\n     COUNT(unison) AS almond\n   GROUP BY\n     knack,\n     video,\n     cohort,\n     locker\n|> AGGREGATE\n     SUM(cohort) AS groove,\n     MAX(cohort) AS butter\n   GROUP BY\n     knack,\n     video\n|> AGGREGATE\n     SUM(stage) AS bishop,\n     COUNT(butter) AS resale\n   GROUP BY\n     knack,\n     video,\n     groove\n|> AGGREGATE\n     AVG(resale) AS mouse\n   GROUP BY\n     knack\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"stage",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    avail,\n    sheep,\n    mutiny,\n    syntax,\n    trail\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    avail,\n    sheep,\n    mutiny,\n    syntax,\n    MIN(trail) AS tunnel\n  FROM cte_0\n  GROUP BY\n    avail,\n    sheep,\n    mutiny,\n    syntax\n),\ncte_2 AS (\n  SELECT\n    avail,\n    sheep,\n    mutiny,\n    syntax,\n    AVG(tunnel) AS gloss\n  FROM cte_1\n  GROUP BY\n    avail,\n    sheep,\n    mutiny,\n    syntax\n),\ncte_3 AS (\n  SELECT\n    SUM(output) AS stride,\n    MAX(gloss) AS breeze\n  FROM cte_2\n  GROUP BY\n    avail,\n    sheep,\n    mutiny,\n    syntax\n),\ncte_4 AS (\n  SELECT\n    SUM(breeze) AS bacon\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     avail,\n     sheep,\n     mutiny,\n     syntax,\n     trail\n|> AGGREGATE\n     MIN(trail) AS tunnel\n   GROUP BY\n     avail,\n     sheep,\n     mutiny,\n     syntax\n|> AGGREGATE\n     AVG(tunnel) AS gloss\n   GROUP BY\n     avail,\n     sheep,\n     mutiny,\n     syntax\n|> AGGREGATE\n     SUM(output) AS stride,\n     MAX(gloss) AS breeze\n   GROUP BY\n     avail,\n     sheep,\n     mutiny,\n     syntax\n|> AGGREGATE\n     SUM(breeze) AS bacon\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"output",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ridley,\n    uptake,\n    flock,\n    grace,\n    method\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    ridley,\n    uptake,\n    flock,\n    grace,\n    MIN(method) AS jenny\n  FROM cte_0\n  GROUP BY\n    ridley,\n    uptake,\n    flock,\n    grace\n),\ncte_2 AS (\n  SELECT\n    ridley,\n    uptake,\n    COUNT(jenny) AS favour,\n    MAX(grace) AS excuse\n  FROM cte_1\n  GROUP BY\n    ridley,\n    uptake\n),\ncte_3 AS (\n  SELECT\n    ridley,\n    uptake,\n    AVG(psalm) AS collor,\n    MAX(excuse) AS harrow,\n    COUNT(excuse) AS ambush\n  FROM cte_2\n  GROUP BY\n    ridley,\n    uptake\n),\ncte_4 AS (\n  SELECT\n    ridley,\n    uptake\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ridley,\n     uptake,\n     flock,\n     grace,\n     method\n|> AGGREGATE\n     MIN(method) AS jenny\n   GROUP BY\n     ridley,\n     uptake,\n     flock,\n     grace\n|> AGGREGATE\n     COUNT(jenny) AS favour,\n     MAX(grace) AS excuse\n   GROUP BY\n     ridley,\n     uptake\n|> AGGREGATE\n     AVG(psalm) AS collor,\n     MAX(excuse) AS harrow,\n     COUNT(excuse) AS ambush\n   GROUP BY\n     ridley,\n     uptake\n|> SELECT\n     ridley,\n     uptake\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"psalm",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    dress,\n    ulster,\n    kitten,\n    score,\n    decay\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    dress,\n    ulster,\n    kitten,\n    score,\n    AVG(decay) AS scrap\n  FROM cte_0\n  GROUP BY\n    dress,\n    ulster,\n    kitten,\n    score\n),\ncte_2 AS (\n  SELECT\n    dress,\n    ulster,\n    kitten,\n    score,\n    COUNT(scrap) AS carver,\n    AVG(scrap) AS daisy,\n    SUM(scrap) AS blood,\n    SUM(throne) AS maiden\n  FROM cte_1\n  GROUP BY\n    dress,\n    ulster,\n    kitten,\n    score\n),\ncte_3 AS (\n  SELECT\n    dress,\n    ulster\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(dress) AS fleece\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     dress,\n     ulster,\n     kitten,\n     score,\n     decay\n|> AGGREGATE\n     AVG(decay) AS scrap\n   GROUP BY\n     dress,\n     ulster,\n     kitten,\n     score\n|> AGGREGATE\n     COUNT(scrap) AS carver,\n     AVG(scrap) AS daisy,\n     SUM(scrap) AS blood,\n     SUM(throne) AS maiden\n   GROUP BY\n     dress,\n     ulster,\n     kitten,\n     score\n|> SELECT\n     dress,\n     ulster\n|> AGGREGATE\n     AVG(dress) AS fleece\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"throne",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:19},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    mixer,\n    action,\n    bishop,\n    ferret,\n    snake\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    mixer,\n    action,\n    bishop,\n    ferret,\n    MAX(snake) AS hooker\n  FROM cte_0\n  GROUP BY\n    mixer,\n    action,\n    bishop,\n    ferret\n),\ncte_2 AS (\n  SELECT\n    mixer,\n    action,\n    COUNT(bishop) AS rider\n  FROM cte_1\n  GROUP BY\n    mixer,\n    action\n),\ncte_3 AS (\n  SELECT\n    mixer,\n    action,\n    MAX(rider) AS burgh,\n    MIN(fight) AS shock\n  FROM cte_2\n  GROUP BY\n    mixer,\n    action\n),\ncte_4 AS (\n  SELECT\n    mixer,\n    action,\n    MAX(burgh) AS beach\n  FROM cte_3\n  GROUP BY\n    mixer,\n    action\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     mixer,\n     action,\n     bishop,\n     ferret,\n     snake\n|> AGGREGATE\n     MAX(snake) AS hooker\n   GROUP BY\n     mixer,\n     action,\n     bishop,\n     ferret\n|> AGGREGATE\n     COUNT(bishop) AS rider\n   GROUP BY\n     mixer,\n     action\n|> AGGREGATE\n     MAX(rider) AS burgh,\n     MIN(fight) AS shock\n   GROUP BY\n     mixer,\n     action\n|> AGGREGATE\n     MAX(burgh) AS beach\n   GROUP BY\n     mixer,\n     action\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"fight",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    guitar,\n    blitz,\n    affect,\n    tennis,\n    utopia\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    guitar,\n    blitz,\n    affect,\n    tennis,\n    AVG(utopia) AS seaman\n  FROM cte_0\n  GROUP BY\n    guitar,\n    blitz,\n    affect,\n    tennis\n),\ncte_2 AS (\n  SELECT\n    guitar,\n    blitz,\n    affect,\n    tennis,\n    AVG(seaman) AS quest\n  FROM cte_1\n  GROUP BY\n    guitar,\n    blitz,\n    affect,\n    tennis\n),\ncte_3 AS (\n  SELECT\n    COUNT(liquid) AS smash,\n    MIN(affect) AS debit,\n    COUNT(affect) AS stage\n  FROM cte_2\n  GROUP BY\n    guitar,\n    blitz\n),\ncte_4 AS (\n  SELECT\n    debit,\n    stage\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     guitar,\n     blitz,\n     affect,\n     tennis,\n     utopia\n|> AGGREGATE\n     AVG(utopia) AS seaman\n   GROUP BY\n     guitar,\n     blitz,\n     affect,\n     tennis\n|> AGGREGATE\n     AVG(seaman) AS quest\n   GROUP BY\n     guitar,\n     blitz,\n     affect,\n     tennis\n|> AGGREGATE\n     COUNT(liquid) AS smash,\n     MIN(affect) AS debit,\n     COUNT(affect) AS stage\n   GROUP BY\n     guitar,\n     blitz\n|> SELECT\n     debit,\n     stage\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"liquid",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    shield,\n    towel,\n    patent,\n    actor,\n    cigar\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    shield,\n    towel,\n    patent,\n    actor,\n    COUNT(cigar) AS chest\n  FROM cte_0\n  GROUP BY\n    shield,\n    towel,\n    patent,\n    actor\n),\ncte_2 AS (\n  SELECT\n    shield,\n    towel,\n    COUNT(chest) AS pride\n  FROM cte_1\n  GROUP BY\n    shield,\n    towel,\n    patent,\n    actor\n),\ncte_3 AS (\n  SELECT\n    shield,\n    towel,\n    SUM(tommy) AS cousin,\n    MIN(pride) AS chase\n  FROM cte_2\n  GROUP BY\n    shield,\n    towel\n),\ncte_4 AS (\n  SELECT\n    MAX(towel) AS scrub,\n    SUM(shield) AS barker\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     shield,\n     towel,\n     patent,\n     actor,\n     cigar\n|> AGGREGATE\n     COUNT(cigar) AS chest\n   GROUP BY\n     shield,\n     towel,\n     patent,\n     actor\n|> AGGREGATE\n     COUNT(chest) AS pride\n   GROUP BY\n     shield,\n     towel,\n     patent,\n     actor\n|> AGGREGATE\n     SUM(tommy) AS cousin,\n     MIN(pride) AS chase\n   GROUP BY\n     shield,\n     towel\n|> AGGREGATE\n     MAX(towel) AS scrub,\n     SUM(shield) AS barker\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"tommy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    desire,\n    noise,\n    stake,\n    gould,\n    dealer\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    desire,\n    noise,\n    MIN(gould) AS wally,\n    SUM(gould) AS system\n  FROM cte_0\n  GROUP BY\n    desire,\n    noise,\n    stake\n),\ncte_2 AS (\n  SELECT\n    desire,\n    noise,\n    wally,\n    SUM(system) AS scope\n  FROM cte_1\n  GROUP BY\n    desire,\n    noise,\n    wally\n),\ncte_3 AS (\n  SELECT\n    desire,\n    noise,\n    wally,\n    SUM(cider) AS timber,\n    AVG(scope) AS galley\n  FROM cte_2\n  GROUP BY\n    desire,\n    noise,\n    wally\n),\ncte_4 AS (\n  SELECT\n    desire,\n    noise,\n    wally\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     desire,\n     noise,\n     stake,\n     gould,\n     dealer\n|> AGGREGATE\n     MIN(gould) AS wally,\n     SUM(gould) AS system\n   GROUP BY\n     desire,\n     noise,\n     stake\n|> AGGREGATE\n     SUM(system) AS scope\n   GROUP BY\n     desire,\n     noise,\n     wally\n|> AGGREGATE\n     SUM(cider) AS timber,\n     AVG(scope) AS galley\n   GROUP BY\n     desire,\n     noise,\n     wally\n|> SELECT\n     desire,\n     noise,\n     wally\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"cider",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hockey,\n    barnet,\n    whore,\n    belief,\n    wallet\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    hockey,\n    barnet,\n    whore,\n    belief,\n    COUNT(wallet) AS mummy\n  FROM cte_0\n  GROUP BY\n    hockey,\n    barnet,\n    whore,\n    belief\n),\ncte_2 AS (\n  SELECT\n    hockey,\n    barnet,\n    whore,\n    COUNT(mummy) AS drake\n  FROM cte_1\n  GROUP BY\n    hockey,\n    barnet,\n    whore,\n    belief\n),\ncte_3 AS (\n  SELECT\n    hockey,\n    MAX(output) AS apollo,\n    MIN(whore) AS decree,\n    AVG(drake) AS trough\n  FROM cte_2\n  GROUP BY\n    hockey,\n    barnet\n),\ncte_4 AS (\n  SELECT\n    COUNT(hockey) AS forum\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hockey,\n     barnet,\n     whore,\n     belief,\n     wallet\n|> AGGREGATE\n     COUNT(wallet) AS mummy\n   GROUP BY\n     hockey,\n     barnet,\n     whore,\n     belief\n|> AGGREGATE\n     COUNT(mummy) AS drake\n   GROUP BY\n     hockey,\n     barnet,\n     whore,\n     belief\n|> AGGREGATE\n     MAX(output) AS apollo,\n     MIN(whore) AS decree,\n     AVG(drake) AS trough\n   GROUP BY\n     hockey,\n     barnet\n|> AGGREGATE\n     COUNT(hockey) AS forum\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"output",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    horror,\n    poker,\n    prayer,\n    length,\n    towel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    horror,\n    poker,\n    prayer,\n    length,\n    MIN(towel) AS strat,\n    MAX(towel) AS option,\n    AVG(towel) AS hollow\n  FROM cte_0\n  GROUP BY\n    horror,\n    poker,\n    prayer,\n    length\n),\ncte_2 AS (\n  SELECT\n    horror,\n    poker,\n    prayer,\n    length,\n    SUM(strat) AS fleece,\n    MAX(diesel) AS jaguar\n  FROM cte_1\n  GROUP BY\n    horror,\n    poker,\n    prayer,\n    length\n),\ncte_3 AS (\n  SELECT\n    horror,\n    poker\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    horror,\n    poker\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     horror,\n     poker,\n     prayer,\n     length,\n     towel\n|> AGGREGATE\n     MIN(towel) AS strat,\n     MAX(towel) AS option,\n     AVG(towel) AS hollow\n   GROUP BY\n     horror,\n     poker,\n     prayer,\n     length\n|> AGGREGATE\n     SUM(strat) AS fleece,\n     MAX(diesel) AS jaguar\n   GROUP BY\n     horror,\n     poker,\n     prayer,\n     length\n|> SELECT\n     horror,\n     poker\n|> SELECT\n     horror,\n     poker\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"diesel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:19},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    vision,\n    flush,\n    sensor,\n    voice,\n    matey\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    vision,\n    flush,\n    sensor,\n    voice,\n    COUNT(matey) AS velvet\n  FROM cte_0\n  GROUP BY\n    vision,\n    flush,\n    sensor,\n    voice\n),\ncte_2 AS (\n  SELECT\n    vision,\n    flush,\n    sensor,\n    COUNT(velvet) AS farce\n  FROM cte_1\n  GROUP BY\n    vision,\n    flush,\n    sensor,\n    voice\n),\ncte_3 AS (\n  SELECT\n    vision,\n    COUNT(flair) AS maggot,\n    MIN(sensor) AS grave,\n    SUM(farce) AS result\n  FROM cte_2\n  GROUP BY\n    vision,\n    flush\n),\ncte_4 AS (\n  SELECT\n    MIN(vision) AS future\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     vision,\n     flush,\n     sensor,\n     voice,\n     matey\n|> AGGREGATE\n     COUNT(matey) AS velvet\n   GROUP BY\n     vision,\n     flush,\n     sensor,\n     voice\n|> AGGREGATE\n     COUNT(velvet) AS farce\n   GROUP BY\n     vision,\n     flush,\n     sensor,\n     voice\n|> AGGREGATE\n     COUNT(flair) AS maggot,\n     MIN(sensor) AS grave,\n     SUM(farce) AS result\n   GROUP BY\n     vision,\n     flush\n|> AGGREGATE\n     MIN(vision) AS future\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"flair",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    satin,\n    gedge,\n    cobalt,\n    county,\n    locke\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    satin,\n    gedge,\n    cobalt,\n    county,\n    AVG(locke) AS bloke\n  FROM cte_0\n  GROUP BY\n    satin,\n    gedge,\n    cobalt,\n    county\n),\ncte_2 AS (\n  SELECT\n    satin,\n    gedge,\n    COUNT(bloke) AS beard\n  FROM cte_1\n  GROUP BY\n    satin,\n    gedge,\n    cobalt,\n    county\n),\ncte_3 AS (\n  SELECT\n    satin,\n    gedge,\n    COUNT(wally) AS angle,\n    SUM(beard) AS famine\n  FROM cte_2\n  GROUP BY\n    satin,\n    gedge\n),\ncte_4 AS (\n  SELECT\n    AVG(famine) AS virtue\n  FROM cte_3\n  GROUP BY\n    satin,\n    gedge\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     satin,\n     gedge,\n     cobalt,\n     county,\n     locke\n|> AGGREGATE\n     AVG(locke) AS bloke\n   GROUP BY\n     satin,\n     gedge,\n     cobalt,\n     county\n|> AGGREGATE\n     COUNT(bloke) AS beard\n   GROUP BY\n     satin,\n     gedge,\n     cobalt,\n     county\n|> AGGREGATE\n     COUNT(wally) AS angle,\n     SUM(beard) AS famine\n   GROUP BY\n     satin,\n     gedge\n|> AGGREGATE\n     AVG(famine) AS virtue\n   GROUP BY\n     satin,\n     gedge\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"wally",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    burton,\n    tarmac,\n    franco,\n    snake,\n    penis\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    burton,\n    tarmac,\n    franco,\n    snake,\n    MAX(penis) AS blitz,\n    COUNT(penis) AS leave\n  FROM cte_0\n  GROUP BY\n    burton,\n    tarmac,\n    franco,\n    snake\n),\ncte_2 AS (\n  SELECT\n    burton,\n    tarmac,\n    COUNT(blitz) AS pellet\n  FROM cte_1\n  GROUP BY\n    burton,\n    tarmac\n),\ncte_3 AS (\n  SELECT\n    burton,\n    tarmac,\n    COUNT(stile) AS plough,\n    MAX(pellet) AS alpha\n  FROM cte_2\n  GROUP BY\n    burton,\n    tarmac\n),\ncte_4 AS (\n  SELECT\n    burton,\n    AVG(tarmac) AS glider\n  FROM cte_3\n  GROUP BY\n    burton\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     burton,\n     tarmac,\n     franco,\n     snake,\n     penis\n|> AGGREGATE\n     MAX(penis) AS blitz,\n     COUNT(penis) AS leave\n   GROUP BY\n     burton,\n     tarmac,\n     franco,\n     snake\n|> AGGREGATE\n     COUNT(blitz) AS pellet\n   GROUP BY\n     burton,\n     tarmac\n|> AGGREGATE\n     COUNT(stile) AS plough,\n     MAX(pellet) AS alpha\n   GROUP BY\n     burton,\n     tarmac\n|> AGGREGATE\n     AVG(tarmac) AS glider\n   GROUP BY\n     burton\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"stile",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    change,\n    miller,\n    second,\n    henry,\n    castle\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    change,\n    miller,\n    second,\n    henry,\n    MAX(castle) AS avenue\n  FROM cte_0\n  GROUP BY\n    change,\n    miller,\n    second,\n    henry\n),\ncte_2 AS (\n  SELECT\n    change,\n    miller,\n    second,\n    COUNT(avenue) AS check\n  FROM cte_1\n  GROUP BY\n    change,\n    miller,\n    second,\n    henry\n),\ncte_3 AS (\n  SELECT\n    change,\n    AVG(glint) AS gender,\n    SUM(second) AS beech\n  FROM cte_2\n  GROUP BY\n    change,\n    miller\n),\ncte_4 AS (\n  SELECT\n    change,\n    MAX(beech) AS slick\n  FROM cte_3\n  GROUP BY\n    change\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     change,\n     miller,\n     second,\n     henry,\n     castle\n|> AGGREGATE\n     MAX(castle) AS avenue\n   GROUP BY\n     change,\n     miller,\n     second,\n     henry\n|> AGGREGATE\n     COUNT(avenue) AS check\n   GROUP BY\n     change,\n     miller,\n     second,\n     henry\n|> AGGREGATE\n     AVG(glint) AS gender,\n     SUM(second) AS beech\n   GROUP BY\n     change,\n     miller\n|> AGGREGATE\n     MAX(beech) AS slick\n   GROUP BY\n     change\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"glint",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    price,\n    grange,\n    remand,\n    smash,\n    caller\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    price,\n    grange,\n    remand,\n    SUM(smash) AS thief\n  FROM cte_0\n  GROUP BY\n    price,\n    grange,\n    remand\n),\ncte_2 AS (\n  SELECT\n    price,\n    grange,\n    remand,\n    MIN(thief) AS speech\n  FROM cte_1\n  GROUP BY\n    price,\n    grange,\n    remand\n),\ncte_3 AS (\n  SELECT\n    price,\n    grange,\n    AVG(speech) AS pocket,\n    MAX(shoot) AS sample\n  FROM cte_2\n  GROUP BY\n    price,\n    grange\n),\ncte_4 AS (\n  SELECT\n    AVG(pocket) AS alien,\n    COUNT(pocket) AS lotus\n  FROM cte_3\n  GROUP BY\n    price,\n    grange\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     price,\n     grange,\n     remand,\n     smash,\n     caller\n|> AGGREGATE\n     SUM(smash) AS thief\n   GROUP BY\n     price,\n     grange,\n     remand\n|> AGGREGATE\n     MIN(thief) AS speech\n   GROUP BY\n     price,\n     grange,\n     remand\n|> AGGREGATE\n     AVG(speech) AS pocket,\n     MAX(shoot) AS sample\n   GROUP BY\n     price,\n     grange\n|> AGGREGATE\n     AVG(pocket) AS alien,\n     COUNT(pocket) AS lotus\n   GROUP BY\n     price,\n     grange\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"shoot",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    torso,\n    hurdle,\n    marker,\n    exodus,\n    tempo\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    torso,\n    hurdle,\n    SUM(exodus) AS layout\n  FROM cte_0\n  GROUP BY\n    torso,\n    hurdle\n),\ncte_2 AS (\n  SELECT\n    torso,\n    hurdle,\n    SUM(layout) AS brett,\n    MAX(layout) AS chest,\n    COUNT(layout) AS blouse\n  FROM cte_1\n  GROUP BY\n    torso,\n    hurdle\n),\ncte_3 AS (\n  SELECT\n    torso,\n    hurdle,\n    brett,\n    chest,\n    AVG(cancer) AS porch,\n    SUM(blouse) AS grasp\n  FROM cte_2\n  GROUP BY\n    torso,\n    hurdle,\n    brett,\n    chest\n),\ncte_4 AS (\n  SELECT\n    COUNT(chest) AS tremor\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     torso,\n     hurdle,\n     marker,\n     exodus,\n     tempo\n|> AGGREGATE\n     SUM(exodus) AS layout\n   GROUP BY\n     torso,\n     hurdle\n|> AGGREGATE\n     SUM(layout) AS brett,\n     MAX(layout) AS chest,\n     COUNT(layout) AS blouse\n   GROUP BY\n     torso,\n     hurdle\n|> AGGREGATE\n     AVG(cancer) AS porch,\n     SUM(blouse) AS grasp\n   GROUP BY\n     torso,\n     hurdle,\n     brett,\n     chest\n|> AGGREGATE\n     COUNT(chest) AS tremor\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"cancer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    wigan,\n    keeper,\n    money,\n    menace,\n    slate\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    wigan,\n    keeper,\n    money,\n    menace,\n    AVG(slate) AS wagon,\n    MIN(slate) AS climax\n  FROM cte_0\n  GROUP BY\n    wigan,\n    keeper,\n    money,\n    menace\n),\ncte_2 AS (\n  SELECT\n    wigan,\n    keeper,\n    AVG(wagon) AS assay\n  FROM cte_1\n  GROUP BY\n    wigan,\n    keeper\n),\ncte_3 AS (\n  SELECT\n    wigan,\n    keeper,\n    AVG(brandy) AS script,\n    MIN(assay) AS supply\n  FROM cte_2\n  GROUP BY\n    wigan,\n    keeper\n),\ncte_4 AS (\n  SELECT\n    SUM(keeper) AS jewel,\n    MAX(supply) AS scotch\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     wigan,\n     keeper,\n     money,\n     menace,\n     slate\n|> AGGREGATE\n     AVG(slate) AS wagon,\n     MIN(slate) AS climax\n   GROUP BY\n     wigan,\n     keeper,\n     money,\n     menace\n|> AGGREGATE\n     AVG(wagon) AS assay\n   GROUP BY\n     wigan,\n     keeper\n|> AGGREGATE\n     AVG(brandy) AS script,\n     MIN(assay) AS supply\n   GROUP BY\n     wigan,\n     keeper\n|> AGGREGATE\n     SUM(keeper) AS jewel,\n     MAX(supply) AS scotch\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"brandy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    fusion,\n    dream,\n    score,\n    drink,\n    method\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    fusion,\n    dream,\n    score,\n    MIN(method) AS grain\n  FROM cte_0\n  GROUP BY\n    fusion,\n    dream,\n    score,\n    drink\n),\ncte_2 AS (\n  SELECT\n    fusion,\n    dream,\n    SUM(grain) AS format,\n    AVG(score) AS helper\n  FROM cte_1\n  GROUP BY\n    fusion,\n    dream\n),\ncte_3 AS (\n  SELECT\n    fusion,\n    dream,\n    format,\n    COUNT(matey) AS alert,\n    MIN(helper) AS recall\n  FROM cte_2\n  GROUP BY\n    fusion,\n    dream,\n    format\n),\ncte_4 AS (\n  SELECT\n    MAX(dream) AS bruce\n  FROM cte_3\n  GROUP BY\n    fusion\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     fusion,\n     dream,\n     score,\n     drink,\n     method\n|> AGGREGATE\n     MIN(method) AS grain\n   GROUP BY\n     fusion,\n     dream,\n     score,\n     drink\n|> AGGREGATE\n     SUM(grain) AS format,\n     AVG(score) AS helper\n   GROUP BY\n     fusion,\n     dream\n|> AGGREGATE\n     COUNT(matey) AS alert,\n     MIN(helper) AS recall\n   GROUP BY\n     fusion,\n     dream,\n     format\n|> AGGREGATE\n     MAX(dream) AS bruce\n   GROUP BY\n     fusion\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"matey",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    wedge,\n    cartel,\n    bowler,\n    demand,\n    brain\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    wedge,\n    cartel,\n    bowler,\n    SUM(brain) AS trout\n  FROM cte_0\n  GROUP BY\n    wedge,\n    cartel,\n    bowler\n),\ncte_2 AS (\n  SELECT\n    wedge,\n    cartel,\n    bowler,\n    MIN(trout) AS atlas,\n    SUM(trout) AS curve\n  FROM cte_1\n  GROUP BY\n    wedge,\n    cartel,\n    bowler\n),\ncte_3 AS (\n  SELECT\n    wedge,\n    cartel,\n    COUNT(brief) AS sauna,\n    SUM(curve) AS marina\n  FROM cte_2\n  GROUP BY\n    wedge,\n    cartel\n),\ncte_4 AS (\n  SELECT\n    MIN(wedge) AS fence,\n    COUNT(wedge) AS mentor\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     wedge,\n     cartel,\n     bowler,\n     demand,\n     brain\n|> AGGREGATE\n     SUM(brain) AS trout\n   GROUP BY\n     wedge,\n     cartel,\n     bowler\n|> AGGREGATE\n     MIN(trout) AS atlas,\n     SUM(trout) AS curve\n   GROUP BY\n     wedge,\n     cartel,\n     bowler\n|> AGGREGATE\n     COUNT(brief) AS sauna,\n     SUM(curve) AS marina\n   GROUP BY\n     wedge,\n     cartel\n|> AGGREGATE\n     MIN(wedge) AS fence,\n     COUNT(wedge) AS mentor\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"brief",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    plague,\n    curve,\n    sphere,\n    flora,\n    berlin\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    plague,\n    curve,\n    sphere,\n    flora,\n    SUM(berlin) AS bucket\n  FROM cte_0\n  GROUP BY\n    plague,\n    curve,\n    sphere,\n    flora\n),\ncte_2 AS (\n  SELECT\n    plague,\n    curve,\n    MAX(bucket) AS gypsy\n  FROM cte_1\n  GROUP BY\n    plague,\n    curve,\n    sphere,\n    flora\n),\ncte_3 AS (\n  SELECT\n    plague,\n    curve,\n    MIN(bloom) AS kinase,\n    AVG(gypsy) AS ghana,\n    MAX(gypsy) AS study\n  FROM cte_2\n  GROUP BY\n    plague,\n    curve\n),\ncte_4 AS (\n  SELECT\n    COUNT(study) AS blast\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     plague,\n     curve,\n     sphere,\n     flora,\n     berlin\n|> AGGREGATE\n     SUM(berlin) AS bucket\n   GROUP BY\n     plague,\n     curve,\n     sphere,\n     flora\n|> AGGREGATE\n     MAX(bucket) AS gypsy\n   GROUP BY\n     plague,\n     curve,\n     sphere,\n     flora\n|> AGGREGATE\n     MIN(bloom) AS kinase,\n     AVG(gypsy) AS ghana,\n     MAX(gypsy) AS study\n   GROUP BY\n     plague,\n     curve\n|> AGGREGATE\n     COUNT(study) AS blast\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"bloom",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    thesis,\n    cartel,\n    excise,\n    plant,\n    river\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    thesis,\n    cartel,\n    excise,\n    plant,\n    MIN(river) AS screen\n  FROM cte_0\n  GROUP BY\n    thesis,\n    cartel,\n    excise,\n    plant\n),\ncte_2 AS (\n  SELECT\n    thesis,\n    cartel,\n    excise,\n    plant,\n    MIN(screen) AS pastry\n  FROM cte_1\n  GROUP BY\n    thesis,\n    cartel,\n    excise,\n    plant\n),\ncte_3 AS (\n  SELECT\n    COUNT(video) AS deputy,\n    COUNT(plant) AS hooker,\n    AVG(thesis) AS ledger\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(ledger) AS maiden,\n    MAX(hooker) AS perry\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     thesis,\n     cartel,\n     excise,\n     plant,\n     river\n|> AGGREGATE\n     MIN(river) AS screen\n   GROUP BY\n     thesis,\n     cartel,\n     excise,\n     plant\n|> AGGREGATE\n     MIN(screen) AS pastry\n   GROUP BY\n     thesis,\n     cartel,\n     excise,\n     plant\n|> AGGREGATE\n     COUNT(video) AS deputy,\n     COUNT(plant) AS hooker,\n     AVG(thesis) AS ledger\n|> AGGREGATE\n     MAX(ledger) AS maiden,\n     MAX(hooker) AS perry\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"video",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    unity,\n    vodka,\n    apron,\n    booth,\n    carrot\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    unity,\n    vodka,\n    apron,\n    booth,\n    MAX(carrot) AS melon\n  FROM cte_0\n  GROUP BY\n    unity,\n    vodka,\n    apron,\n    booth\n),\ncte_2 AS (\n  SELECT\n    unity,\n    vodka,\n    AVG(booth) AS hockey\n  FROM cte_1\n  GROUP BY\n    unity,\n    vodka\n),\ncte_3 AS (\n  SELECT\n    unity,\n    vodka,\n    MAX(hockey) AS strat,\n    COUNT(guide) AS tardis,\n    AVG(hockey) AS patio\n  FROM cte_2\n  GROUP BY\n    unity,\n    vodka\n),\ncte_4 AS (\n  SELECT\n    unity,\n    MAX(patio) AS deacon\n  FROM cte_3\n  GROUP BY\n    unity\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     unity,\n     vodka,\n     apron,\n     booth,\n     carrot\n|> AGGREGATE\n     MAX(carrot) AS melon\n   GROUP BY\n     unity,\n     vodka,\n     apron,\n     booth\n|> AGGREGATE\n     AVG(booth) AS hockey\n   GROUP BY\n     unity,\n     vodka\n|> AGGREGATE\n     MAX(hockey) AS strat,\n     COUNT(guide) AS tardis,\n     AVG(hockey) AS patio\n   GROUP BY\n     unity,\n     vodka\n|> AGGREGATE\n     MAX(patio) AS deacon\n   GROUP BY\n     unity\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"guide",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hostel,\n    noise,\n    title,\n    dance,\n    birth\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    hostel,\n    noise,\n    title,\n    COUNT(dance) AS glider\n  FROM cte_0\n  GROUP BY\n    hostel,\n    noise,\n    title\n),\ncte_2 AS (\n  SELECT\n    hostel,\n    noise,\n    title,\n    AVG(glider) AS nelson,\n    MIN(glider) AS sector\n  FROM cte_1\n  GROUP BY\n    hostel,\n    noise,\n    title\n),\ncte_3 AS (\n  SELECT\n    hostel,\n    noise,\n    SUM(dogma) AS shake,\n    MIN(sector) AS basin\n  FROM cte_2\n  GROUP BY\n    hostel,\n    noise,\n    title\n),\ncte_4 AS (\n  SELECT\n    MIN(basin) AS ambush\n  FROM cte_3\n  GROUP BY\n    hostel\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hostel,\n     noise,\n     title,\n     dance,\n     birth\n|> AGGREGATE\n     COUNT(dance) AS glider\n   GROUP BY\n     hostel,\n     noise,\n     title\n|> AGGREGATE\n     AVG(glider) AS nelson,\n     MIN(glider) AS sector\n   GROUP BY\n     hostel,\n     noise,\n     title\n|> AGGREGATE\n     SUM(dogma) AS shake,\n     MIN(sector) AS basin\n   GROUP BY\n     hostel,\n     noise,\n     title\n|> AGGREGATE\n     MIN(basin) AS ambush\n   GROUP BY\n     hostel\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"dogma",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    permit,\n    phoebe,\n    terry,\n    mouth,\n    treaty\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    permit,\n    phoebe,\n    terry,\n    MIN(mouth) AS thief\n  FROM cte_0\n  GROUP BY\n    permit,\n    phoebe,\n    terry\n),\ncte_2 AS (\n  SELECT\n    permit,\n    phoebe,\n    terry,\n    AVG(thief) AS laura\n  FROM cte_1\n  GROUP BY\n    permit,\n    phoebe,\n    terry\n),\ncte_3 AS (\n  SELECT\n    permit,\n    phoebe,\n    SUM(laura) AS joanna,\n    COUNT(duvet) AS opener\n  FROM cte_2\n  GROUP BY\n    permit,\n    phoebe\n),\ncte_4 AS (\n  SELECT\n    permit,\n    MIN(phoebe) AS yellow,\n    SUM(joanna) AS aggie\n  FROM cte_3\n  GROUP BY\n    permit\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     permit,\n     phoebe,\n     terry,\n     mouth,\n     treaty\n|> AGGREGATE\n     MIN(mouth) AS thief\n   GROUP BY\n     permit,\n     phoebe,\n     terry\n|> AGGREGATE\n     AVG(thief) AS laura\n   GROUP BY\n     permit,\n     phoebe,\n     terry\n|> AGGREGATE\n     SUM(laura) AS joanna,\n     COUNT(duvet) AS opener\n   GROUP BY\n     permit,\n     phoebe\n|> AGGREGATE\n     MIN(phoebe) AS yellow,\n     SUM(joanna) AS aggie\n   GROUP BY\n     permit\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"duvet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    perry,\n    sender,\n    priest,\n    device,\n    appeal\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    perry,\n    sender,\n    priest,\n    device,\n    MAX(appeal) AS rebate\n  FROM cte_0\n  GROUP BY\n    perry,\n    sender,\n    priest,\n    device\n),\ncte_2 AS (\n  SELECT\n    perry,\n    sender,\n    COUNT(device) AS louis\n  FROM cte_1\n  GROUP BY\n    perry,\n    sender\n),\ncte_3 AS (\n  SELECT\n    perry,\n    sender,\n    AVG(louis) AS barrow,\n    COUNT(tiger) AS pigeon,\n    COUNT(louis) AS brunt,\n    SUM(louis) AS talbot\n  FROM cte_2\n  GROUP BY\n    perry,\n    sender\n),\ncte_4 AS (\n  SELECT\n    perry,\n    sender\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     perry,\n     sender,\n     priest,\n     device,\n     appeal\n|> AGGREGATE\n     MAX(appeal) AS rebate\n   GROUP BY\n     perry,\n     sender,\n     priest,\n     device\n|> AGGREGATE\n     COUNT(device) AS louis\n   GROUP BY\n     perry,\n     sender\n|> AGGREGATE\n     AVG(louis) AS barrow,\n     COUNT(tiger) AS pigeon,\n     COUNT(louis) AS brunt,\n     SUM(louis) AS talbot\n   GROUP BY\n     perry,\n     sender\n|> SELECT\n     perry,\n     sender\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"tiger",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hoover,\n    factor,\n    batch,\n    debut,\n    summer\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    hoover,\n    factor,\n    batch,\n    debut,\n    MAX(summer) AS insult\n  FROM cte_0\n  GROUP BY\n    hoover,\n    factor,\n    batch,\n    debut\n),\ncte_2 AS (\n  SELECT\n    hoover,\n    factor,\n    batch,\n    debut,\n    SUM(insult) AS beetle\n  FROM cte_1\n  GROUP BY\n    hoover,\n    factor,\n    batch,\n    debut\n),\ncte_3 AS (\n  SELECT\n    COUNT(manila) AS madam,\n    MIN(factor) AS change,\n    AVG(factor) AS slang\n  FROM cte_2\n  GROUP BY\n    hoover\n),\ncte_4 AS (\n  SELECT\n    SUM(slang) AS pellet\n  FROM cte_3\n  GROUP BY\n    change\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hoover,\n     factor,\n     batch,\n     debut,\n     summer\n|> AGGREGATE\n     MAX(summer) AS insult\n   GROUP BY\n     hoover,\n     factor,\n     batch,\n     debut\n|> AGGREGATE\n     SUM(insult) AS beetle\n   GROUP BY\n     hoover,\n     factor,\n     batch,\n     debut\n|> AGGREGATE\n     COUNT(manila) AS madam,\n     MIN(factor) AS change,\n     AVG(factor) AS slang\n   GROUP BY\n     hoover\n|> AGGREGATE\n     SUM(slang) AS pellet\n   GROUP BY\n     change\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"manila",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    biopsy,\n    dinghy,\n    burton,\n    manual,\n    crate\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    biopsy,\n    dinghy,\n    burton,\n    manual,\n    SUM(crate) AS weight\n  FROM cte_0\n  GROUP BY\n    biopsy,\n    dinghy,\n    burton,\n    manual\n),\ncte_2 AS (\n  SELECT\n    biopsy,\n    dinghy,\n    burton,\n    manual,\n    MAX(weight) AS rhyme,\n    AVG(weight) AS patten,\n    SUM(weight) AS prompt,\n    MAX(marble) AS bliss\n  FROM cte_1\n  GROUP BY\n    biopsy,\n    dinghy,\n    burton,\n    manual\n),\ncte_3 AS (\n  SELECT\n    biopsy,\n    dinghy\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(dinghy) AS carver\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     biopsy,\n     dinghy,\n     burton,\n     manual,\n     crate\n|> AGGREGATE\n     SUM(crate) AS weight\n   GROUP BY\n     biopsy,\n     dinghy,\n     burton,\n     manual\n|> AGGREGATE\n     MAX(weight) AS rhyme,\n     AVG(weight) AS patten,\n     SUM(weight) AS prompt,\n     MAX(marble) AS bliss\n   GROUP BY\n     biopsy,\n     dinghy,\n     burton,\n     manual\n|> SELECT\n     biopsy,\n     dinghy\n|> AGGREGATE\n     SUM(dinghy) AS carver\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"marble",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:19},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    scheme,\n    satin,\n    truce,\n    victor,\n    trait\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    scheme,\n    satin,\n    truce,\n    MIN(victor) AS infant\n  FROM cte_0\n  GROUP BY\n    scheme,\n    satin,\n    truce\n),\ncte_2 AS (\n  SELECT\n    scheme,\n    satin,\n    SUM(infant) AS seaman,\n    COUNT(infant) AS shaft\n  FROM cte_1\n  GROUP BY\n    scheme,\n    satin,\n    truce\n),\ncte_3 AS (\n  SELECT\n    scheme,\n    satin,\n    seaman,\n    AVG(caller) AS branch,\n    AVG(shaft) AS motive\n  FROM cte_2\n  GROUP BY\n    scheme,\n    satin,\n    seaman\n),\ncte_4 AS (\n  SELECT\n    scheme,\n    satin,\n    seaman\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     scheme,\n     satin,\n     truce,\n     victor,\n     trait\n|> AGGREGATE\n     MIN(victor) AS infant\n   GROUP BY\n     scheme,\n     satin,\n     truce\n|> AGGREGATE\n     SUM(infant) AS seaman,\n     COUNT(infant) AS shaft\n   GROUP BY\n     scheme,\n     satin,\n     truce\n|> AGGREGATE\n     AVG(caller) AS branch,\n     AVG(shaft) AS motive\n   GROUP BY\n     scheme,\n     satin,\n     seaman\n|> SELECT\n     scheme,\n     satin,\n     seaman\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"caller",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    penis,\n    hinge,\n    lover,\n    harem,\n    tumour\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    penis,\n    hinge,\n    lover,\n    harem,\n    AVG(tumour) AS batch\n  FROM cte_0\n  GROUP BY\n    penis,\n    hinge,\n    lover,\n    harem\n),\ncte_2 AS (\n  SELECT\n    penis,\n    hinge,\n    lover,\n    harem,\n    COUNT(batch) AS roller,\n    MAX(batch) AS ocean,\n    AVG(batch) AS thief,\n    MIN(screw) AS thirst\n  FROM cte_1\n  GROUP BY\n    penis,\n    hinge,\n    lover,\n    harem\n),\ncte_3 AS (\n  SELECT\n    penis,\n    hinge\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(penis) AS trench\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     penis,\n     hinge,\n     lover,\n     harem,\n     tumour\n|> AGGREGATE\n     AVG(tumour) AS batch\n   GROUP BY\n     penis,\n     hinge,\n     lover,\n     harem\n|> AGGREGATE\n     COUNT(batch) AS roller,\n     MAX(batch) AS ocean,\n     AVG(batch) AS thief,\n     MIN(screw) AS thirst\n   GROUP BY\n     penis,\n     hinge,\n     lover,\n     harem\n|> SELECT\n     penis,\n     hinge\n|> AGGREGATE\n     SUM(penis) AS trench\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"screw",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:19},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    coral,\n    genre,\n    stance,\n    blade,\n    duvet\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    coral,\n    genre,\n    stance,\n    AVG(duvet) AS smear\n  FROM cte_0\n  GROUP BY\n    coral,\n    genre,\n    stance\n),\ncte_2 AS (\n  SELECT\n    coral,\n    genre,\n    COUNT(smear) AS effort,\n    AVG(smear) AS crypt\n  FROM cte_1\n  GROUP BY\n    coral,\n    genre,\n    stance\n),\ncte_3 AS (\n  SELECT\n    coral,\n    genre,\n    effort,\n    MIN(groove) AS court,\n    COUNT(crypt) AS vendor\n  FROM cte_2\n  GROUP BY\n    coral,\n    genre,\n    effort\n),\ncte_4 AS (\n  SELECT\n    coral,\n    genre,\n    effort\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     coral,\n     genre,\n     stance,\n     blade,\n     duvet\n|> AGGREGATE\n     AVG(duvet) AS smear\n   GROUP BY\n     coral,\n     genre,\n     stance\n|> AGGREGATE\n     COUNT(smear) AS effort,\n     AVG(smear) AS crypt\n   GROUP BY\n     coral,\n     genre,\n     stance\n|> AGGREGATE\n     MIN(groove) AS court,\n     COUNT(crypt) AS vendor\n   GROUP BY\n     coral,\n     genre,\n     effort\n|> SELECT\n     coral,\n     genre,\n     effort\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"groove",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    abbot,\n    child,\n    cocoa,\n    defeat,\n    market\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    abbot,\n    child,\n    cocoa,\n    defeat,\n    AVG(market) AS weight\n  FROM cte_0\n  GROUP BY\n    abbot,\n    child,\n    cocoa,\n    defeat\n),\ncte_2 AS (\n  SELECT\n    abbot,\n    child,\n    MIN(weight) AS prior,\n    AVG(cocoa) AS libel\n  FROM cte_1\n  GROUP BY\n    abbot,\n    child\n),\ncte_3 AS (\n  SELECT\n    abbot,\n    child,\n    MAX(woman) AS scope,\n    COUNT(libel) AS venom\n  FROM cte_2\n  GROUP BY\n    abbot,\n    child\n),\ncte_4 AS (\n  SELECT\n    abbot,\n    SUM(child) AS glider\n  FROM cte_3\n  GROUP BY\n    abbot\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     abbot,\n     child,\n     cocoa,\n     defeat,\n     market\n|> AGGREGATE\n     AVG(market) AS weight\n   GROUP BY\n     abbot,\n     child,\n     cocoa,\n     defeat\n|> AGGREGATE\n     MIN(weight) AS prior,\n     AVG(cocoa) AS libel\n   GROUP BY\n     abbot,\n     child\n|> AGGREGATE\n     MAX(woman) AS scope,\n     COUNT(libel) AS venom\n   GROUP BY\n     abbot,\n     child\n|> AGGREGATE\n     SUM(child) AS glider\n   GROUP BY\n     abbot\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"woman",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    scotch,\n    habit,\n    gypsy,\n    issue,\n    madame\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    scotch,\n    habit,\n    gypsy,\n    MAX(madame) AS steen\n  FROM cte_0\n  GROUP BY\n    scotch,\n    habit,\n    gypsy\n),\ncte_2 AS (\n  SELECT\n    scotch,\n    habit,\n    gypsy,\n    AVG(steen) AS sauce,\n    MAX(steen) AS custom\n  FROM cte_1\n  GROUP BY\n    scotch,\n    habit,\n    gypsy\n),\ncte_3 AS (\n  SELECT\n    scotch,\n    habit,\n    MAX(count) AS gentry,\n    MAX(custom) AS japan\n  FROM cte_2\n  GROUP BY\n    scotch,\n    habit,\n    gypsy\n),\ncte_4 AS (\n  SELECT\n    scotch,\n    habit,\n    japan\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     scotch,\n     habit,\n     gypsy,\n     issue,\n     madame\n|> AGGREGATE\n     MAX(madame) AS steen\n   GROUP BY\n     scotch,\n     habit,\n     gypsy\n|> AGGREGATE\n     AVG(steen) AS sauce,\n     MAX(steen) AS custom\n   GROUP BY\n     scotch,\n     habit,\n     gypsy\n|> AGGREGATE\n     MAX(count) AS gentry,\n     MAX(custom) AS japan\n   GROUP BY\n     scotch,\n     habit,\n     gypsy\n|> SELECT\n     scotch,\n     habit,\n     japan\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"count",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    chief,\n    famine,\n    guide,\n    statue,\n    humour\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    chief,\n    famine,\n    guide,\n    statue,\n    MAX(humour) AS stroke\n  FROM cte_0\n  GROUP BY\n    chief,\n    famine,\n    guide,\n    statue\n),\ncte_2 AS (\n  SELECT\n    chief,\n    famine,\n    COUNT(guide) AS tyler,\n    SUM(statue) AS trust\n  FROM cte_1\n  GROUP BY\n    chief,\n    famine\n),\ncte_3 AS (\n  SELECT\n    chief,\n    famine,\n    COUNT(holly) AS farce,\n    SUM(tyler) AS pencil,\n    AVG(trust) AS boyle\n  FROM cte_2\n  GROUP BY\n    chief,\n    famine\n),\ncte_4 AS (\n  SELECT\n    chief,\n    famine\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     chief,\n     famine,\n     guide,\n     statue,\n     humour\n|> AGGREGATE\n     MAX(humour) AS stroke\n   GROUP BY\n     chief,\n     famine,\n     guide,\n     statue\n|> AGGREGATE\n     COUNT(guide) AS tyler,\n     SUM(statue) AS trust\n   GROUP BY\n     chief,\n     famine\n|> AGGREGATE\n     COUNT(holly) AS farce,\n     SUM(tyler) AS pencil,\n     AVG(trust) AS boyle\n   GROUP BY\n     chief,\n     famine\n|> SELECT\n     chief,\n     famine\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"holly",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    poison,\n    faeces,\n    panel,\n    system,\n    winger\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    poison,\n    faeces,\n    panel,\n    system,\n    AVG(winger) AS brown\n  FROM cte_0\n  GROUP BY\n    poison,\n    faeces,\n    panel,\n    system\n),\ncte_2 AS (\n  SELECT\n    poison,\n    faeces,\n    panel,\n    system,\n    MIN(brown) AS scalp,\n    COUNT(brown) AS cradle,\n    SUM(brown) AS troop,\n    COUNT(smyth) AS reason\n  FROM cte_1\n  GROUP BY\n    poison,\n    faeces,\n    panel,\n    system\n),\ncte_3 AS (\n  SELECT\n    poison,\n    faeces\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    poison,\n    faeces\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     poison,\n     faeces,\n     panel,\n     system,\n     winger\n|> AGGREGATE\n     AVG(winger) AS brown\n   GROUP BY\n     poison,\n     faeces,\n     panel,\n     system\n|> AGGREGATE\n     MIN(brown) AS scalp,\n     COUNT(brown) AS cradle,\n     SUM(brown) AS troop,\n     COUNT(smyth) AS reason\n   GROUP BY\n     poison,\n     faeces,\n     panel,\n     system\n|> SELECT\n     poison,\n     faeces\n|> SELECT\n     poison,\n     faeces\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"smyth",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:19},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    chapel,\n    minus,\n    ledger,\n    gorge,\n    pinch\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    chapel,\n    minus,\n    ledger,\n    gorge,\n    AVG(pinch) AS powder\n  FROM cte_0\n  GROUP BY\n    chapel,\n    minus,\n    ledger,\n    gorge\n),\ncte_2 AS (\n  SELECT\n    chapel,\n    minus,\n    ledger,\n    gorge,\n    COUNT(powder) AS venom\n  FROM cte_1\n  GROUP BY\n    chapel,\n    minus,\n    ledger,\n    gorge\n),\ncte_3 AS (\n  SELECT\n    MIN(league) AS chase,\n    MAX(venom) AS blood\n  FROM cte_2\n  GROUP BY\n    chapel,\n    minus,\n    ledger,\n    gorge\n),\ncte_4 AS (\n  SELECT\n    SUM(blood) AS smear\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     chapel,\n     minus,\n     ledger,\n     gorge,\n     pinch\n|> AGGREGATE\n     AVG(pinch) AS powder\n   GROUP BY\n     chapel,\n     minus,\n     ledger,\n     gorge\n|> AGGREGATE\n     COUNT(powder) AS venom\n   GROUP BY\n     chapel,\n     minus,\n     ledger,\n     gorge\n|> AGGREGATE\n     MIN(league) AS chase,\n     MAX(venom) AS blood\n   GROUP BY\n     chapel,\n     minus,\n     ledger,\n     gorge\n|> AGGREGATE\n     SUM(blood) AS smear\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"league",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    radius,\n    rhyme,\n    author,\n    grace,\n    bottle\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    radius,\n    rhyme,\n    author,\n    grace,\n    MAX(bottle) AS flock\n  FROM cte_0\n  GROUP BY\n    radius,\n    rhyme,\n    author,\n    grace\n),\ncte_2 AS (\n  SELECT\n    radius,\n    rhyme,\n    author,\n    grace,\n    COUNT(flock) AS botany\n  FROM cte_1\n  GROUP BY\n    radius,\n    rhyme,\n    author,\n    grace\n),\ncte_3 AS (\n  SELECT\n    SUM(oracle) AS burger,\n    MIN(botany) AS opener\n  FROM cte_2\n  GROUP BY\n    radius,\n    rhyme,\n    author,\n    grace\n),\ncte_4 AS (\n  SELECT\n    SUM(opener) AS signal\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     radius,\n     rhyme,\n     author,\n     grace,\n     bottle\n|> AGGREGATE\n     MAX(bottle) AS flock\n   GROUP BY\n     radius,\n     rhyme,\n     author,\n     grace\n|> AGGREGATE\n     COUNT(flock) AS botany\n   GROUP BY\n     radius,\n     rhyme,\n     author,\n     grace\n|> AGGREGATE\n     SUM(oracle) AS burger,\n     MIN(botany) AS opener\n   GROUP BY\n     radius,\n     rhyme,\n     author,\n     grace\n|> AGGREGATE\n     SUM(opener) AS signal\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"oracle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    shaft,\n    stare,\n    sense,\n    pedal,\n    snake\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    shaft,\n    stare,\n    sense,\n    SUM(snake) AS matron\n  FROM cte_0\n  GROUP BY\n    shaft,\n    stare,\n    sense\n),\ncte_2 AS (\n  SELECT\n    shaft,\n    stare,\n    sense,\n    AVG(matron) AS deputy\n  FROM cte_1\n  GROUP BY\n    shaft,\n    stare,\n    sense\n),\ncte_3 AS (\n  SELECT\n    shaft,\n    stare,\n    COUNT(sense) AS realm,\n    MIN(crust) AS killer\n  FROM cte_2\n  GROUP BY\n    shaft,\n    stare\n),\ncte_4 AS (\n  SELECT\n    MAX(realm) AS basis,\n    AVG(stare) AS jenny,\n    MAX(stare) AS tanker\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     shaft,\n     stare,\n     sense,\n     pedal,\n     snake\n|> AGGREGATE\n     SUM(snake) AS matron\n   GROUP BY\n     shaft,\n     stare,\n     sense\n|> AGGREGATE\n     AVG(matron) AS deputy\n   GROUP BY\n     shaft,\n     stare,\n     sense\n|> AGGREGATE\n     COUNT(sense) AS realm,\n     MIN(crust) AS killer\n   GROUP BY\n     shaft,\n     stare\n|> AGGREGATE\n     MAX(realm) AS basis,\n     AVG(stare) AS jenny,\n     MAX(stare) AS tanker\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"crust",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    cattle,\n    polish,\n    adobe,\n    flock,\n    nylon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    cattle,\n    polish,\n    adobe,\n    MIN(flock) AS payne,\n    SUM(nylon) AS nelson\n  FROM cte_0\n  GROUP BY\n    cattle,\n    polish,\n    adobe\n),\ncte_2 AS (\n  SELECT\n    cattle,\n    polish,\n    adobe,\n    AVG(payne) AS summit\n  FROM cte_1\n  GROUP BY\n    cattle,\n    polish,\n    adobe\n),\ncte_3 AS (\n  SELECT\n    cattle,\n    polish,\n    COUNT(grain) AS phrase,\n    AVG(summit) AS sheila,\n    MAX(summit) AS digest\n  FROM cte_2\n  GROUP BY\n    cattle,\n    polish\n),\ncte_4 AS (\n  SELECT\n    SUM(sheila) AS flurry\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     cattle,\n     polish,\n     adobe,\n     flock,\n     nylon\n|> AGGREGATE\n     MIN(flock) AS payne,\n     SUM(nylon) AS nelson\n   GROUP BY\n     cattle,\n     polish,\n     adobe\n|> AGGREGATE\n     AVG(payne) AS summit\n   GROUP BY\n     cattle,\n     polish,\n     adobe\n|> AGGREGATE\n     COUNT(grain) AS phrase,\n     AVG(summit) AS sheila,\n     MAX(summit) AS digest\n   GROUP BY\n     cattle,\n     polish\n|> AGGREGATE\n     SUM(sheila) AS flurry\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"grain",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    talbot,\n    grief,\n    bacon,\n    relay,\n    kernel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    talbot,\n    grief,\n    bacon,\n    relay,\n    SUM(kernel) AS plague,\n    COUNT(kernel) AS dolly,\n    MAX(kernel) AS belle\n  FROM cte_0\n  GROUP BY\n    talbot,\n    grief,\n    bacon,\n    relay\n),\ncte_2 AS (\n  SELECT\n    talbot,\n    grief,\n    bacon,\n    relay,\n    MIN(dolly) AS phrase,\n    SUM(ulster) AS breeze\n  FROM cte_1\n  GROUP BY\n    talbot,\n    grief,\n    bacon,\n    relay\n),\ncte_3 AS (\n  SELECT\n    MIN(phrase) AS health\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(health) AS think\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     talbot,\n     grief,\n     bacon,\n     relay,\n     kernel\n|> AGGREGATE\n     SUM(kernel) AS plague,\n     COUNT(kernel) AS dolly,\n     MAX(kernel) AS belle\n   GROUP BY\n     talbot,\n     grief,\n     bacon,\n     relay\n|> AGGREGATE\n     MIN(dolly) AS phrase,\n     SUM(ulster) AS breeze\n   GROUP BY\n     talbot,\n     grief,\n     bacon,\n     relay\n|> AGGREGATE\n     MIN(phrase) AS health\n|> AGGREGATE\n     AVG(health) AS think\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"ulster",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:19},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    noble,\n    prison,\n    touch,\n    chalk,\n    swift\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    noble,\n    prison,\n    COUNT(swift) AS bureau\n  FROM cte_0\n  GROUP BY\n    noble,\n    prison\n),\ncte_2 AS (\n  SELECT\n    noble,\n    prison,\n    SUM(bureau) AS front,\n    MIN(bureau) AS sonny,\n    COUNT(bureau) AS ideal\n  FROM cte_1\n  GROUP BY\n    noble,\n    prison\n),\ncte_3 AS (\n  SELECT\n    noble,\n    prison,\n    front,\n    sonny,\n    MAX(opener) AS bronze,\n    MAX(ideal) AS judge\n  FROM cte_2\n  GROUP BY\n    noble,\n    prison,\n    front,\n    sonny\n),\ncte_4 AS (\n  SELECT\n    noble,\n    prison\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     noble,\n     prison,\n     touch,\n     chalk,\n     swift\n|> AGGREGATE\n     COUNT(swift) AS bureau\n   GROUP BY\n     noble,\n     prison\n|> AGGREGATE\n     SUM(bureau) AS front,\n     MIN(bureau) AS sonny,\n     COUNT(bureau) AS ideal\n   GROUP BY\n     noble,\n     prison\n|> AGGREGATE\n     MAX(opener) AS bronze,\n     MAX(ideal) AS judge\n   GROUP BY\n     noble,\n     prison,\n     front,\n     sonny\n|> SELECT\n     noble,\n     prison\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"opener",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    outfit,\n    train,\n    packet,\n    thread,\n    topic\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    outfit,\n    train,\n    packet,\n    thread,\n    MAX(topic) AS reign\n  FROM cte_0\n  GROUP BY\n    outfit,\n    train,\n    packet,\n    thread\n),\ncte_2 AS (\n  SELECT\n    outfit,\n    train,\n    AVG(packet) AS menace\n  FROM cte_1\n  GROUP BY\n    outfit,\n    train\n),\ncte_3 AS (\n  SELECT\n    outfit,\n    train,\n    AVG(menace) AS streak,\n    MIN(update) AS liquid\n  FROM cte_2\n  GROUP BY\n    outfit,\n    train\n),\ncte_4 AS (\n  SELECT\n    AVG(train) AS carter,\n    SUM(streak) AS factor,\n    MAX(train) AS beech\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     outfit,\n     train,\n     packet,\n     thread,\n     topic\n|> AGGREGATE\n     MAX(topic) AS reign\n   GROUP BY\n     outfit,\n     train,\n     packet,\n     thread\n|> AGGREGATE\n     AVG(packet) AS menace\n   GROUP BY\n     outfit,\n     train\n|> AGGREGATE\n     AVG(menace) AS streak,\n     MIN(update) AS liquid\n   GROUP BY\n     outfit,\n     train\n|> AGGREGATE\n     AVG(train) AS carter,\n     SUM(streak) AS factor,\n     MAX(train) AS beech\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"update",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    subset,\n    tackle,\n    powder,\n    smack,\n    queen\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    subset,\n    tackle,\n    MAX(queen) AS effort\n  FROM cte_0\n  GROUP BY\n    subset,\n    tackle\n),\ncte_2 AS (\n  SELECT\n    subset,\n    tackle,\n    SUM(effort) AS heyday,\n    COUNT(effort) AS choice,\n    MIN(effort) AS squad\n  FROM cte_1\n  GROUP BY\n    subset,\n    tackle\n),\ncte_3 AS (\n  SELECT\n    subset,\n    tackle,\n    heyday,\n    choice,\n    AVG(warsaw) AS bundle,\n    SUM(squad) AS poetry\n  FROM cte_2\n  GROUP BY\n    subset,\n    tackle,\n    heyday,\n    choice\n),\ncte_4 AS (\n  SELECT\n    subset,\n    tackle\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     subset,\n     tackle,\n     powder,\n     smack,\n     queen\n|> AGGREGATE\n     MAX(queen) AS effort\n   GROUP BY\n     subset,\n     tackle\n|> AGGREGATE\n     SUM(effort) AS heyday,\n     COUNT(effort) AS choice,\n     MIN(effort) AS squad\n   GROUP BY\n     subset,\n     tackle\n|> AGGREGATE\n     AVG(warsaw) AS bundle,\n     SUM(squad) AS poetry\n   GROUP BY\n     subset,\n     tackle,\n     heyday,\n     choice\n|> SELECT\n     subset,\n     tackle\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"warsaw",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    craven,\n    talent,\n    granny,\n    repair,\n    visit\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    craven,\n    talent,\n    granny,\n    SUM(visit) AS puppet\n  FROM cte_0\n  GROUP BY\n    craven,\n    talent,\n    granny\n),\ncte_2 AS (\n  SELECT\n    craven,\n    talent,\n    granny,\n    AVG(puppet) AS memory\n  FROM cte_1\n  GROUP BY\n    craven,\n    talent,\n    granny\n),\ncte_3 AS (\n  SELECT\n    craven,\n    talent,\n    COUNT(memory) AS dismay,\n    MAX(knight) AS cheque,\n    MIN(memory) AS botany\n  FROM cte_2\n  GROUP BY\n    craven,\n    talent,\n    granny\n),\ncte_4 AS (\n  SELECT\n    craven,\n    talent,\n    dismay\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     craven,\n     talent,\n     granny,\n     repair,\n     visit\n|> AGGREGATE\n     SUM(visit) AS puppet\n   GROUP BY\n     craven,\n     talent,\n     granny\n|> AGGREGATE\n     AVG(puppet) AS memory\n   GROUP BY\n     craven,\n     talent,\n     granny\n|> AGGREGATE\n     COUNT(memory) AS dismay,\n     MAX(knight) AS cheque,\n     MIN(memory) AS botany\n   GROUP BY\n     craven,\n     talent,\n     granny\n|> SELECT\n     craven,\n     talent,\n     dismay\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"knight",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    derby,\n    origin,\n    brandy,\n    troop,\n    olive\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    derby,\n    origin,\n    brandy,\n    SUM(olive) AS access\n  FROM cte_0\n  GROUP BY\n    derby,\n    origin,\n    brandy,\n    troop\n),\ncte_2 AS (\n  SELECT\n    derby,\n    origin,\n    MAX(brandy) AS barman,\n    COUNT(brandy) AS hogan\n  FROM cte_1\n  GROUP BY\n    derby,\n    origin\n),\ncte_3 AS (\n  SELECT\n    derby,\n    origin,\n    barman,\n    AVG(stool) AS safety,\n    SUM(hogan) AS heart\n  FROM cte_2\n  GROUP BY\n    derby,\n    origin,\n    barman\n),\ncte_4 AS (\n  SELECT\n    MAX(origin) AS regime\n  FROM cte_3\n  GROUP BY\n    derby\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     derby,\n     origin,\n     brandy,\n     troop,\n     olive\n|> AGGREGATE\n     SUM(olive) AS access\n   GROUP BY\n     derby,\n     origin,\n     brandy,\n     troop\n|> AGGREGATE\n     MAX(brandy) AS barman,\n     COUNT(brandy) AS hogan\n   GROUP BY\n     derby,\n     origin\n|> AGGREGATE\n     AVG(stool) AS safety,\n     SUM(hogan) AS heart\n   GROUP BY\n     derby,\n     origin,\n     barman\n|> AGGREGATE\n     MAX(origin) AS regime\n   GROUP BY\n     derby\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"stool",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    green,\n    synod,\n    corps,\n    oliver,\n    mould\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    green,\n    synod,\n    corps,\n    SUM(oliver) AS batty\n  FROM cte_0\n  GROUP BY\n    green,\n    synod,\n    corps\n),\ncte_2 AS (\n  SELECT\n    green,\n    synod,\n    corps,\n    MAX(batty) AS celery\n  FROM cte_1\n  GROUP BY\n    green,\n    synod,\n    corps\n),\ncte_3 AS (\n  SELECT\n    green,\n    synod,\n    AVG(celery) AS bishop,\n    AVG(torch) AS octave\n  FROM cte_2\n  GROUP BY\n    green,\n    synod\n),\ncte_4 AS (\n  SELECT\n    SUM(bishop) AS orange,\n    MAX(bishop) AS stein\n  FROM cte_3\n  GROUP BY\n    green,\n    synod\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     green,\n     synod,\n     corps,\n     oliver,\n     mould\n|> AGGREGATE\n     SUM(oliver) AS batty\n   GROUP BY\n     green,\n     synod,\n     corps\n|> AGGREGATE\n     MAX(batty) AS celery\n   GROUP BY\n     green,\n     synod,\n     corps\n|> AGGREGATE\n     AVG(celery) AS bishop,\n     AVG(torch) AS octave\n   GROUP BY\n     green,\n     synod\n|> AGGREGATE\n     SUM(bishop) AS orange,\n     MAX(bishop) AS stein\n   GROUP BY\n     green,\n     synod\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"torch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    branch,\n    mound,\n    finger,\n    curfew,\n    leave\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    branch,\n    mound,\n    finger,\n    curfew,\n    COUNT(leave) AS entry\n  FROM cte_0\n  GROUP BY\n    branch,\n    mound,\n    finger,\n    curfew\n),\ncte_2 AS (\n  SELECT\n    branch,\n    mound,\n    finger,\n    curfew,\n    SUM(entry) AS sauce\n  FROM cte_1\n  GROUP BY\n    branch,\n    mound,\n    finger,\n    curfew\n),\ncte_3 AS (\n  SELECT\n    AVG(burrow) AS pickup,\n    COUNT(finger) AS binder,\n    SUM(sauce) AS syntax\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(binder) AS bryony,\n    AVG(syntax) AS guitar\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     branch,\n     mound,\n     finger,\n     curfew,\n     leave\n|> AGGREGATE\n     COUNT(leave) AS entry\n   GROUP BY\n     branch,\n     mound,\n     finger,\n     curfew\n|> AGGREGATE\n     SUM(entry) AS sauce\n   GROUP BY\n     branch,\n     mound,\n     finger,\n     curfew\n|> AGGREGATE\n     AVG(burrow) AS pickup,\n     COUNT(finger) AS binder,\n     SUM(sauce) AS syntax\n|> AGGREGATE\n     AVG(binder) AS bryony,\n     AVG(syntax) AS guitar\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"burrow",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    wonder,\n    chase,\n    patch,\n    booth,\n    malice\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    wonder,\n    chase,\n    patch,\n    booth,\n    MAX(malice) AS excess\n  FROM cte_0\n  GROUP BY\n    wonder,\n    chase,\n    patch,\n    booth\n),\ncte_2 AS (\n  SELECT\n    wonder,\n    chase,\n    MAX(excess) AS burial\n  FROM cte_1\n  GROUP BY\n    wonder,\n    chase\n),\ncte_3 AS (\n  SELECT\n    wonder,\n    chase,\n    COUNT(burial) AS atlas,\n    AVG(pound) AS patsy,\n    AVG(burial) AS major,\n    MIN(burial) AS bible\n  FROM cte_2\n  GROUP BY\n    wonder,\n    chase\n),\ncte_4 AS (\n  SELECT\n    wonder,\n    chase\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     wonder,\n     chase,\n     patch,\n     booth,\n     malice\n|> AGGREGATE\n     MAX(malice) AS excess\n   GROUP BY\n     wonder,\n     chase,\n     patch,\n     booth\n|> AGGREGATE\n     MAX(excess) AS burial\n   GROUP BY\n     wonder,\n     chase\n|> AGGREGATE\n     COUNT(burial) AS atlas,\n     AVG(pound) AS patsy,\n     AVG(burial) AS major,\n     MIN(burial) AS bible\n   GROUP BY\n     wonder,\n     chase\n|> SELECT\n     wonder,\n     chase\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"pound",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    midday,\n    bowler,\n    thigh,\n    sector,\n    turbo\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    midday,\n    bowler,\n    thigh,\n    AVG(sector) AS tunic\n  FROM cte_0\n  GROUP BY\n    midday,\n    bowler,\n    thigh\n),\ncte_2 AS (\n  SELECT\n    midday,\n    bowler,\n    thigh,\n    MAX(tunic) AS manual,\n    MIN(tunic) AS record\n  FROM cte_1\n  GROUP BY\n    midday,\n    bowler,\n    thigh\n),\ncte_3 AS (\n  SELECT\n    midday,\n    bowler,\n    SUM(henry) AS reggae,\n    COUNT(record) AS liquid\n  FROM cte_2\n  GROUP BY\n    midday,\n    bowler,\n    thigh,\n    manual\n),\ncte_4 AS (\n  SELECT\n    SUM(midday) AS modem\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     midday,\n     bowler,\n     thigh,\n     sector,\n     turbo\n|> AGGREGATE\n     AVG(sector) AS tunic\n   GROUP BY\n     midday,\n     bowler,\n     thigh\n|> AGGREGATE\n     MAX(tunic) AS manual,\n     MIN(tunic) AS record\n   GROUP BY\n     midday,\n     bowler,\n     thigh\n|> AGGREGATE\n     SUM(henry) AS reggae,\n     COUNT(record) AS liquid\n   GROUP BY\n     midday,\n     bowler,\n     thigh,\n     manual\n|> AGGREGATE\n     SUM(midday) AS modem\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"henry",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    scrum,\n    claire,\n    molla,\n    jigsaw,\n    phone\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    scrum,\n    claire,\n    molla,\n    jigsaw,\n    MAX(phone) AS gould\n  FROM cte_0\n  GROUP BY\n    scrum,\n    claire,\n    molla,\n    jigsaw\n),\ncte_2 AS (\n  SELECT\n    scrum,\n    claire,\n    molla,\n    jigsaw,\n    MAX(gould) AS spite\n  FROM cte_1\n  GROUP BY\n    scrum,\n    claire,\n    molla,\n    jigsaw\n),\ncte_3 AS (\n  SELECT\n    AVG(surrey) AS blame,\n    MAX(spite) AS female\n  FROM cte_2\n  GROUP BY\n    scrum,\n    claire,\n    molla,\n    jigsaw\n),\ncte_4 AS (\n  SELECT\n    COUNT(female) AS decor\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     scrum,\n     claire,\n     molla,\n     jigsaw,\n     phone\n|> AGGREGATE\n     MAX(phone) AS gould\n   GROUP BY\n     scrum,\n     claire,\n     molla,\n     jigsaw\n|> AGGREGATE\n     MAX(gould) AS spite\n   GROUP BY\n     scrum,\n     claire,\n     molla,\n     jigsaw\n|> AGGREGATE\n     AVG(surrey) AS blame,\n     MAX(spite) AS female\n   GROUP BY\n     scrum,\n     claire,\n     molla,\n     jigsaw\n|> AGGREGATE\n     COUNT(female) AS decor\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"surrey",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    feast,\n    sheikh,\n    cheese,\n    tycoon,\n    winger\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    feast,\n    sheikh,\n    cheese,\n    SUM(tycoon) AS master\n  FROM cte_0\n  GROUP BY\n    feast,\n    sheikh,\n    cheese\n),\ncte_2 AS (\n  SELECT\n    feast,\n    sheikh,\n    cheese,\n    SUM(master) AS magnet\n  FROM cte_1\n  GROUP BY\n    feast,\n    sheikh,\n    cheese\n),\ncte_3 AS (\n  SELECT\n    feast,\n    sheikh,\n    COUNT(magnet) AS point,\n    COUNT(quarry) AS livery,\n    SUM(cheese) AS letter\n  FROM cte_2\n  GROUP BY\n    feast,\n    sheikh\n),\ncte_4 AS (\n  SELECT\n    feast,\n    AVG(point) AS total\n  FROM cte_3\n  GROUP BY\n    feast\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     feast,\n     sheikh,\n     cheese,\n     tycoon,\n     winger\n|> AGGREGATE\n     SUM(tycoon) AS master\n   GROUP BY\n     feast,\n     sheikh,\n     cheese\n|> AGGREGATE\n     SUM(master) AS magnet\n   GROUP BY\n     feast,\n     sheikh,\n     cheese\n|> AGGREGATE\n     COUNT(magnet) AS point,\n     COUNT(quarry) AS livery,\n     SUM(cheese) AS letter\n   GROUP BY\n     feast,\n     sheikh\n|> AGGREGATE\n     AVG(point) AS total\n   GROUP BY\n     feast\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"quarry",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    finch,\n    slick,\n    empire,\n    mickey,\n    input\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    finch,\n    slick,\n    empire,\n    SUM(input) AS pound,\n    MAX(mickey) AS bugger\n  FROM cte_0\n  GROUP BY\n    finch,\n    slick,\n    empire\n),\ncte_2 AS (\n  SELECT\n    finch,\n    slick,\n    empire,\n    MIN(pound) AS stench\n  FROM cte_1\n  GROUP BY\n    finch,\n    slick,\n    empire\n),\ncte_3 AS (\n  SELECT\n    finch,\n    slick,\n    SUM(match) AS fender,\n    AVG(empire) AS hinge,\n    SUM(empire) AS parity\n  FROM cte_2\n  GROUP BY\n    finch,\n    slick\n),\ncte_4 AS (\n  SELECT\n    SUM(parity) AS start\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     finch,\n     slick,\n     empire,\n     mickey,\n     input\n|> AGGREGATE\n     SUM(input) AS pound,\n     MAX(mickey) AS bugger\n   GROUP BY\n     finch,\n     slick,\n     empire\n|> AGGREGATE\n     MIN(pound) AS stench\n   GROUP BY\n     finch,\n     slick,\n     empire\n|> AGGREGATE\n     SUM(match) AS fender,\n     AVG(empire) AS hinge,\n     SUM(empire) AS parity\n   GROUP BY\n     finch,\n     slick\n|> AGGREGATE\n     SUM(parity) AS start\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"match",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    winger,\n    fritz,\n    travis,\n    mixer,\n    output\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    winger,\n    fritz,\n    travis,\n    mixer,\n    SUM(output) AS handle,\n    COUNT(output) AS intent\n  FROM cte_0\n  GROUP BY\n    winger,\n    fritz,\n    travis,\n    mixer\n),\ncte_2 AS (\n  SELECT\n    winger,\n    fritz,\n    AVG(handle) AS cloak\n  FROM cte_1\n  GROUP BY\n    winger,\n    fritz\n),\ncte_3 AS (\n  SELECT\n    winger,\n    fritz,\n    MIN(stead) AS trifle,\n    AVG(cloak) AS fridge\n  FROM cte_2\n  GROUP BY\n    winger,\n    fritz\n),\ncte_4 AS (\n  SELECT\n    winger,\n    COUNT(fridge) AS stage\n  FROM cte_3\n  GROUP BY\n    winger\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     winger,\n     fritz,\n     travis,\n     mixer,\n     output\n|> AGGREGATE\n     SUM(output) AS handle,\n     COUNT(output) AS intent\n   GROUP BY\n     winger,\n     fritz,\n     travis,\n     mixer\n|> AGGREGATE\n     AVG(handle) AS cloak\n   GROUP BY\n     winger,\n     fritz\n|> AGGREGATE\n     MIN(stead) AS trifle,\n     AVG(cloak) AS fridge\n   GROUP BY\n     winger,\n     fritz\n|> AGGREGATE\n     COUNT(fridge) AS stage\n   GROUP BY\n     winger\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"stead",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rhythm,\n    donna,\n    voter,\n    holder,\n    chase\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    rhythm,\n    donna,\n    voter,\n    holder,\n    MAX(chase) AS satire,\n    AVG(chase) AS expiry\n  FROM cte_0\n  GROUP BY\n    rhythm,\n    donna,\n    voter,\n    holder\n),\ncte_2 AS (\n  SELECT\n    rhythm,\n    donna,\n    voter,\n    holder,\n    COUNT(expiry) AS foyer,\n    AVG(satire) AS barley,\n    MAX(claire) AS texas\n  FROM cte_1\n  GROUP BY\n    rhythm,\n    donna,\n    voter,\n    holder\n),\ncte_3 AS (\n  SELECT\n    rhythm,\n    donna\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(donna) AS comic\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rhythm,\n     donna,\n     voter,\n     holder,\n     chase\n|> AGGREGATE\n     MAX(chase) AS satire,\n     AVG(chase) AS expiry\n   GROUP BY\n     rhythm,\n     donna,\n     voter,\n     holder\n|> AGGREGATE\n     COUNT(expiry) AS foyer,\n     AVG(satire) AS barley,\n     MAX(claire) AS texas\n   GROUP BY\n     rhythm,\n     donna,\n     voter,\n     holder\n|> SELECT\n     rhythm,\n     donna\n|> AGGREGATE\n     MAX(donna) AS comic\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"claire",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:19},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    payne,\n    flare,\n    reflux,\n    estate,\n    ariel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    payne,\n    flare,\n    reflux,\n    AVG(ariel) AS comet\n  FROM cte_0\n  GROUP BY\n    payne,\n    flare,\n    reflux\n),\ncte_2 AS (\n  SELECT\n    payne,\n    flare,\n    reflux,\n    COUNT(comet) AS sound\n  FROM cte_1\n  GROUP BY\n    payne,\n    flare,\n    reflux\n),\ncte_3 AS (\n  SELECT\n    payne,\n    flare,\n    MAX(reflux) AS basket,\n    COUNT(savoy) AS sexism,\n    SUM(sound) AS parcel,\n    MAX(sound) AS crewe\n  FROM cte_2\n  GROUP BY\n    payne,\n    flare\n),\ncte_4 AS (\n  SELECT\n    payne,\n    flare\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     payne,\n     flare,\n     reflux,\n     estate,\n     ariel\n|> AGGREGATE\n     AVG(ariel) AS comet\n   GROUP BY\n     payne,\n     flare,\n     reflux\n|> AGGREGATE\n     COUNT(comet) AS sound\n   GROUP BY\n     payne,\n     flare,\n     reflux\n|> AGGREGATE\n     MAX(reflux) AS basket,\n     COUNT(savoy) AS sexism,\n     SUM(sound) AS parcel,\n     MAX(sound) AS crewe\n   GROUP BY\n     payne,\n     flare\n|> SELECT\n     payne,\n     flare\n|> SELECT *",totalInformationSQL:35,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"AGG_UNKNOWN",totalInformationDifference:8,unknownName:"savoy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    level,\n    cache,\n    shelf,\n    murray,\n    query\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(query) AS motif,\n    AVG(murray) AS teapot,\n    MAX(murray) AS lewis,\n    COUNT(murray) AS sherry,\n    AVG(shelf) AS flour\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(sherry) AS sleeve,\n    MIN(sherry) AS logic,\n    MIN(flour) AS virtue,\n    MAX(lewis) AS tenant\n  FROM cte_1\n  GROUP BY\n    motif,\n    teapot,\n    trust\n),\ncte_3 AS (\n  SELECT\n    MAX(tenant) AS chunk\n  FROM cte_2\n  GROUP BY\n    sleeve,\n    logic\n),\ncte_4 AS (\n  SELECT\n    MAX(chunk) AS texas,\n    SUM(chunk) AS saint\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     level,\n     cache,\n     shelf,\n     murray,\n     query\n|> AGGREGATE\n     MIN(query) AS motif,\n     AVG(murray) AS teapot,\n     MAX(murray) AS lewis,\n     COUNT(murray) AS sherry,\n     AVG(shelf) AS flour\n|> AGGREGATE\n     AVG(sherry) AS sleeve,\n     MIN(sherry) AS logic,\n     MIN(flour) AS virtue,\n     MAX(lewis) AS tenant\n   GROUP BY\n     motif,\n     teapot,\n     trust\n|> AGGREGATE\n     MAX(tenant) AS chunk\n   GROUP BY\n     sleeve,\n     logic\n|> AGGREGATE\n     MAX(chunk) AS texas,\n     SUM(chunk) AS saint\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"trust",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    voice,\n    script,\n    usage,\n    grace,\n    gauge\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    voice,\n    script,\n    usage,\n    grace,\n    gauge\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    voice,\n    script,\n    usage,\n    grace,\n    gauge\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(usage) AS disco,\n    MIN(gauge) AS nether,\n    AVG(gauge) AS circle,\n    MAX(grace) AS brook\n  FROM cte_2\n  GROUP BY\n    voice,\n    script,\n    plate\n),\ncte_4 AS (\n  SELECT\n    disco,\n    nether,\n    circle,\n    AVG(brook) AS holly\n  FROM cte_3\n  GROUP BY\n    disco,\n    nether,\n    circle\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     voice,\n     script,\n     usage,\n     grace,\n     gauge\n|> SELECT\n     voice,\n     script,\n     usage,\n     grace,\n     gauge\n|> SELECT\n     voice,\n     script,\n     usage,\n     grace,\n     gauge\n|> AGGREGATE\n     COUNT(usage) AS disco,\n     MIN(gauge) AS nether,\n     AVG(gauge) AS circle,\n     MAX(grace) AS brook\n   GROUP BY\n     voice,\n     script,\n     plate\n|> AGGREGATE\n     AVG(brook) AS holly\n   GROUP BY\n     disco,\n     nether,\n     circle\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"plate",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:28},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    saloon,\n    warmth,\n    ascent,\n    repeal,\n    lorry\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(ascent) AS donna,\n    MAX(warmth) AS brick,\n    AVG(warmth) AS smear,\n    COUNT(ascent) AS spade,\n    SUM(warmth) AS fossil,\n    COUNT(warmth) AS brass\n  FROM cte_0\n  GROUP BY\n    saloon\n),\ncte_2 AS (\n  SELECT\n    MIN(brass) AS jelly,\n    SUM(brass) AS canon\n  FROM cte_1\n  GROUP BY\n    donna,\n    brick,\n    smear,\n    focus,\n    spade,\n    fossil\n),\ncte_3 AS (\n  SELECT\n    jelly,\n    canon\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    jelly,\n    SUM(canon) AS whisky\n  FROM cte_3\n  GROUP BY\n    jelly\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     saloon,\n     warmth,\n     ascent,\n     repeal,\n     lorry\n|> AGGREGATE\n     MAX(ascent) AS donna,\n     MAX(warmth) AS brick,\n     AVG(warmth) AS smear,\n     COUNT(ascent) AS spade,\n     SUM(warmth) AS fossil,\n     COUNT(warmth) AS brass\n   GROUP BY\n     saloon\n|> AGGREGATE\n     MIN(brass) AS jelly,\n     SUM(brass) AS canon\n   GROUP BY\n     donna,\n     brick,\n     smear,\n     focus,\n     spade,\n     fossil\n|> SELECT\n     jelly,\n     canon\n|> AGGREGATE\n     SUM(canon) AS whisky\n   GROUP BY\n     jelly\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"focus",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    jersey,\n    combat,\n    needle,\n    parent,\n    offer\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(offer) AS fellow,\n    MIN(parent) AS sword,\n    COUNT(parent) AS cargo\n  FROM cte_0\n  GROUP BY\n    jersey,\n    combat\n),\ncte_2 AS (\n  SELECT\n    SUM(cargo) AS decade,\n    MAX(cargo) AS cooker,\n    COUNT(cargo) AS platt,\n    AVG(cargo) AS lesion,\n    MIN(cargo) AS splash\n  FROM cte_1\n  GROUP BY\n    fellow,\n    sword,\n    psalm\n),\ncte_3 AS (\n  SELECT\n    decade,\n    SUM(lesion) AS digest\n  FROM cte_2\n  GROUP BY\n    decade,\n    cooker,\n    platt\n),\ncte_4 AS (\n  SELECT\n    COUNT(decade) AS surety\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     jersey,\n     combat,\n     needle,\n     parent,\n     offer\n|> AGGREGATE\n     SUM(offer) AS fellow,\n     MIN(parent) AS sword,\n     COUNT(parent) AS cargo\n   GROUP BY\n     jersey,\n     combat\n|> AGGREGATE\n     SUM(cargo) AS decade,\n     MAX(cargo) AS cooker,\n     COUNT(cargo) AS platt,\n     AVG(cargo) AS lesion,\n     MIN(cargo) AS splash\n   GROUP BY\n     fellow,\n     sword,\n     psalm\n|> AGGREGATE\n     SUM(lesion) AS digest\n   GROUP BY\n     decade,\n     cooker,\n     platt\n|> AGGREGATE\n     COUNT(decade) AS surety\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"psalm",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    fossil,\n    pinch,\n    common,\n    studio,\n    yellow\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(common) AS fancy,\n    SUM(fossil) AS tomato\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(fancy) AS crater,\n    MAX(tomato) AS laity,\n    MIN(fancy) AS appeal,\n    SUM(fancy) AS plaque,\n    COUNT(tomato) AS honour\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(plaque) AS relief,\n    AVG(plaque) AS actor,\n    MIN(plaque) AS crook\n  FROM cte_2\n  GROUP BY\n    flake,\n    crater,\n    laity\n),\ncte_4 AS (\n  SELECT\n    AVG(relief) AS logic,\n    AVG(actor) AS bomber,\n    COUNT(actor) AS spouse\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     fossil,\n     pinch,\n     common,\n     studio,\n     yellow\n|> AGGREGATE\n     SUM(common) AS fancy,\n     SUM(fossil) AS tomato\n|> AGGREGATE\n     AVG(fancy) AS crater,\n     MAX(tomato) AS laity,\n     MIN(fancy) AS appeal,\n     SUM(fancy) AS plaque,\n     COUNT(tomato) AS honour\n|> AGGREGATE\n     SUM(plaque) AS relief,\n     AVG(plaque) AS actor,\n     MIN(plaque) AS crook\n   GROUP BY\n     flake,\n     crater,\n     laity\n|> AGGREGATE\n     AVG(relief) AS logic,\n     AVG(actor) AS bomber,\n     COUNT(actor) AS spouse\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"flake",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    world,\n    cassie,\n    metal,\n    slope,\n    vanity\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(cassie) AS rubble,\n    MIN(metal) AS ridge\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(ridge) AS derby,\n    MAX(rubble) AS career,\n    MAX(ridge) AS tablet,\n    COUNT(ridge) AS upland,\n    AVG(rubble) AS raffle,\n    COUNT(rubble) AS shire\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(shire) AS ounce,\n    AVG(shire) AS pirate\n  FROM cte_2\n  GROUP BY\n    venom,\n    derby,\n    career,\n    tablet,\n    upland,\n    raffle\n),\ncte_4 AS (\n  SELECT\n    AVG(pirate) AS dragon\n  FROM cte_3\n  GROUP BY\n    ounce\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     world,\n     cassie,\n     metal,\n     slope,\n     vanity\n|> AGGREGATE\n     SUM(cassie) AS rubble,\n     MIN(metal) AS ridge\n|> AGGREGATE\n     SUM(ridge) AS derby,\n     MAX(rubble) AS career,\n     MAX(ridge) AS tablet,\n     COUNT(ridge) AS upland,\n     AVG(rubble) AS raffle,\n     COUNT(rubble) AS shire\n|> AGGREGATE\n     MAX(shire) AS ounce,\n     AVG(shire) AS pirate\n   GROUP BY\n     venom,\n     derby,\n     career,\n     tablet,\n     upland,\n     raffle\n|> AGGREGATE\n     AVG(pirate) AS dragon\n   GROUP BY\n     ounce\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"venom",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    forge,\n    thumb,\n    issue,\n    coffin,\n    backup\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(forge) AS oscar,\n    MIN(issue) AS gloom,\n    MAX(thumb) AS glance,\n    COUNT(thumb) AS cadet,\n    COUNT(issue) AS trough,\n    AVG(forge) AS bunny\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(cadet) AS johnny,\n    SUM(glance) AS spite,\n    MAX(glance) AS future\n  FROM cte_1\n  GROUP BY\n    oscar,\n    gloom,\n    coral\n),\ncte_3 AS (\n  SELECT\n    MAX(spite) AS plough,\n    MIN(spite) AS title\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(title) AS newton,\n    COUNT(plough) AS baxter\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     forge,\n     thumb,\n     issue,\n     coffin,\n     backup\n|> AGGREGATE\n     SUM(forge) AS oscar,\n     MIN(issue) AS gloom,\n     MAX(thumb) AS glance,\n     COUNT(thumb) AS cadet,\n     COUNT(issue) AS trough,\n     AVG(forge) AS bunny\n|> AGGREGATE\n     AVG(cadet) AS johnny,\n     SUM(glance) AS spite,\n     MAX(glance) AS future\n   GROUP BY\n     oscar,\n     gloom,\n     coral\n|> AGGREGATE\n     MAX(spite) AS plough,\n     MIN(spite) AS title\n|> AGGREGATE\n     MIN(title) AS newton,\n     COUNT(plough) AS baxter\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"coral",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    trade,\n    decade,\n    phrase,\n    suede,\n    curse\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    trade,\n    decade,\n    phrase,\n    suede\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(decade) AS scrum,\n    COUNT(suede) AS praise,\n    MIN(phrase) AS hobby,\n    AVG(phrase) AS harris,\n    SUM(decade) AS clinic\n  FROM cte_1\n  GROUP BY\n    trade\n),\ncte_3 AS (\n  SELECT\n    COUNT(clinic) AS vision\n  FROM cte_2\n  GROUP BY\n    scrum,\n    praise,\n    hobby,\n    hardy\n),\ncte_4 AS (\n  SELECT\n    MIN(vision) AS alarm,\n    COUNT(vision) AS stable,\n    MAX(vision) AS tavern,\n    SUM(vision) AS laird\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     trade,\n     decade,\n     phrase,\n     suede,\n     curse\n|> SELECT\n     trade,\n     decade,\n     phrase,\n     suede\n|> AGGREGATE\n     COUNT(decade) AS scrum,\n     COUNT(suede) AS praise,\n     MIN(phrase) AS hobby,\n     AVG(phrase) AS harris,\n     SUM(decade) AS clinic\n   GROUP BY\n     trade\n|> AGGREGATE\n     COUNT(clinic) AS vision\n   GROUP BY\n     scrum,\n     praise,\n     hobby,\n     hardy\n|> AGGREGATE\n     MIN(vision) AS alarm,\n     COUNT(vision) AS stable,\n     MAX(vision) AS tavern,\n     SUM(vision) AS laird\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"hardy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    gunman,\n    tongue,\n    target,\n    pastor,\n    brass\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(tongue) AS polish,\n    AVG(target) AS kidney\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(kidney) AS occult,\n    COUNT(polish) AS stroud,\n    MIN(kidney) AS sally,\n    AVG(polish) AS prose,\n    AVG(kidney) AS shrimp,\n    COUNT(kidney) AS inset\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(shrimp) AS coffin,\n    COUNT(inset) AS buddy\n  FROM cte_2\n  GROUP BY\n    rector,\n    occult,\n    stroud,\n    sally,\n    prose\n),\ncte_4 AS (\n  SELECT\n    coffin,\n    SUM(buddy) AS jelly\n  FROM cte_3\n  GROUP BY\n    coffin\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     gunman,\n     tongue,\n     target,\n     pastor,\n     brass\n|> AGGREGATE\n     AVG(tongue) AS polish,\n     AVG(target) AS kidney\n|> AGGREGATE\n     MAX(kidney) AS occult,\n     COUNT(polish) AS stroud,\n     MIN(kidney) AS sally,\n     AVG(polish) AS prose,\n     AVG(kidney) AS shrimp,\n     COUNT(kidney) AS inset\n|> AGGREGATE\n     COUNT(shrimp) AS coffin,\n     COUNT(inset) AS buddy\n   GROUP BY\n     rector,\n     occult,\n     stroud,\n     sally,\n     prose\n|> AGGREGATE\n     SUM(buddy) AS jelly\n   GROUP BY\n     coffin\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"rector",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    phase,\n    verse,\n    outcry,\n    thrust,\n    surety\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(surety) AS tunic,\n    MAX(outcry) AS greed,\n    MIN(outcry) AS symbol\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(symbol) AS frown,\n    MAX(symbol) AS shine,\n    SUM(symbol) AS jimmy\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(jimmy) AS spruce,\n    MIN(jimmy) AS liquid,\n    MAX(jimmy) AS canon\n  FROM cte_2\n  GROUP BY\n    frown,\n    shine,\n    tallis\n),\ncte_4 AS (\n  SELECT\n    spruce,\n    MAX(canon) AS goblin,\n    COUNT(canon) AS martyr,\n    AVG(liquid) AS china\n  FROM cte_3\n  GROUP BY\n    spruce\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     phase,\n     verse,\n     outcry,\n     thrust,\n     surety\n|> AGGREGATE\n     SUM(surety) AS tunic,\n     MAX(outcry) AS greed,\n     MIN(outcry) AS symbol\n|> AGGREGATE\n     MIN(symbol) AS frown,\n     MAX(symbol) AS shine,\n     SUM(symbol) AS jimmy\n|> AGGREGATE\n     COUNT(jimmy) AS spruce,\n     MIN(jimmy) AS liquid,\n     MAX(jimmy) AS canon\n   GROUP BY\n     frown,\n     shine,\n     tallis\n|> AGGREGATE\n     MAX(canon) AS goblin,\n     COUNT(canon) AS martyr,\n     AVG(liquid) AS china\n   GROUP BY\n     spruce\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"tallis",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    spouse,\n    tarmac,\n    midday,\n    valley,\n    header\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(tarmac) AS ridley,\n    MAX(header) AS bliss,\n    AVG(valley) AS minor,\n    SUM(midday) AS surge,\n    COUNT(header) AS stuff,\n    AVG(midday) AS whole\n  FROM cte_0\n  GROUP BY\n    spouse\n),\ncte_2 AS (\n  SELECT\n    AVG(whole) AS perry,\n    AVG(bliss) AS accord,\n    MAX(bliss) AS night\n  FROM cte_1\n  GROUP BY\n    ridley,\n    major\n),\ncte_3 AS (\n  SELECT\n    MIN(night) AS cheque,\n    MAX(accord) AS shadow\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    cheque,\n    MIN(shadow) AS feast\n  FROM cte_3\n  GROUP BY\n    cheque\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     spouse,\n     tarmac,\n     midday,\n     valley,\n     header\n|> AGGREGATE\n     MAX(tarmac) AS ridley,\n     MAX(header) AS bliss,\n     AVG(valley) AS minor,\n     SUM(midday) AS surge,\n     COUNT(header) AS stuff,\n     AVG(midday) AS whole\n   GROUP BY\n     spouse\n|> AGGREGATE\n     AVG(whole) AS perry,\n     AVG(bliss) AS accord,\n     MAX(bliss) AS night\n   GROUP BY\n     ridley,\n     major\n|> AGGREGATE\n     MIN(night) AS cheque,\n     MAX(accord) AS shadow\n|> AGGREGATE\n     MIN(shadow) AS feast\n   GROUP BY\n     cheque\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"major",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    berth,\n    twist,\n    server,\n    cowboy,\n    betty\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(betty) AS campus,\n    COUNT(betty) AS taste\n  FROM cte_0\n  GROUP BY\n    berth,\n    twist,\n    server,\n    cowboy\n),\ncte_2 AS (\n  SELECT\n    SUM(campus) AS blade,\n    MIN(campus) AS lynch\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(lynch) AS toilet,\n    COUNT(lynch) AS treat,\n    MAX(lynch) AS palace,\n    MIN(lynch) AS answer\n  FROM cte_2\n  GROUP BY\n    pupil\n),\ncte_4 AS (\n  SELECT\n    toilet,\n    treat,\n    MAX(answer) AS oliver,\n    MIN(palace) AS range\n  FROM cte_3\n  GROUP BY\n    toilet,\n    treat\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     berth,\n     twist,\n     server,\n     cowboy,\n     betty\n|> AGGREGATE\n     MIN(betty) AS campus,\n     COUNT(betty) AS taste\n   GROUP BY\n     berth,\n     twist,\n     server,\n     cowboy\n|> AGGREGATE\n     SUM(campus) AS blade,\n     MIN(campus) AS lynch\n|> AGGREGATE\n     AVG(lynch) AS toilet,\n     COUNT(lynch) AS treat,\n     MAX(lynch) AS palace,\n     MIN(lynch) AS answer\n   GROUP BY\n     pupil\n|> AGGREGATE\n     MAX(answer) AS oliver,\n     MIN(palace) AS range\n   GROUP BY\n     toilet,\n     treat\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"pupil",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hector,\n    tensor,\n    brick,\n    claire,\n    bride\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(claire) AS morgan,\n    SUM(bride) AS unison,\n    MIN(bride) AS close,\n    MAX(brick) AS betty\n  FROM cte_0\n  GROUP BY\n    hector,\n    tensor\n),\ncte_2 AS (\n  SELECT\n    SUM(betty) AS trace,\n    SUM(unison) AS nausea,\n    MAX(betty) AS bloom,\n    AVG(betty) AS screen\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(bloom) AS china\n  FROM cte_2\n  GROUP BY\n    hostel\n),\ncte_4 AS (\n  SELECT\n    MIN(china) AS graham,\n    MAX(china) AS signal,\n    COUNT(china) AS drain,\n    AVG(china) AS gypsy\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hector,\n     tensor,\n     brick,\n     claire,\n     bride\n|> AGGREGATE\n     COUNT(claire) AS morgan,\n     SUM(bride) AS unison,\n     MIN(bride) AS close,\n     MAX(brick) AS betty\n   GROUP BY\n     hector,\n     tensor\n|> AGGREGATE\n     SUM(betty) AS trace,\n     SUM(unison) AS nausea,\n     MAX(betty) AS bloom,\n     AVG(betty) AS screen\n|> AGGREGATE\n     SUM(bloom) AS china\n   GROUP BY\n     hostel\n|> AGGREGATE\n     MIN(china) AS graham,\n     MAX(china) AS signal,\n     COUNT(china) AS drain,\n     AVG(china) AS gypsy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"hostel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    utopia,\n    coffee,\n    martyr,\n    vigil,\n    ransom\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    utopia,\n    coffee\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(coffee) AS mouse,\n    COUNT(utopia) AS stump,\n    MAX(coffee) AS while,\n    MIN(coffee) AS vicar,\n    SUM(coffee) AS dummy,\n    AVG(coffee) AS wrath,\n    MIN(utopia) AS folly,\n    SUM(utopia) AS widow,\n    AVG(utopia) AS bunker\n  FROM cte_1\n  GROUP BY\n    saddle\n),\ncte_3 AS (\n  SELECT\n    mouse,\n    stump,\n    while,\n    vicar\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(mouse) AS pilot,\n    AVG(while) AS unease\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     utopia,\n     coffee,\n     martyr,\n     vigil,\n     ransom\n|> SELECT\n     utopia,\n     coffee\n|> AGGREGATE\n     COUNT(coffee) AS mouse,\n     COUNT(utopia) AS stump,\n     MAX(coffee) AS while,\n     MIN(coffee) AS vicar,\n     SUM(coffee) AS dummy,\n     AVG(coffee) AS wrath,\n     MIN(utopia) AS folly,\n     SUM(utopia) AS widow,\n     AVG(utopia) AS bunker\n   GROUP BY\n     saddle\n|> SELECT\n     mouse,\n     stump,\n     while,\n     vicar\n|> AGGREGATE\n     MIN(mouse) AS pilot,\n     AVG(while) AS unease\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"saddle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ethic,\n    bloke,\n    ellen,\n    ticket,\n    unison\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(unison) AS digest,\n    COUNT(ethic) AS baker,\n    AVG(bloke) AS smith,\n    AVG(ethic) AS mount\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(mount) AS crest,\n    SUM(smith) AS lever,\n    COUNT(digest) AS jargon\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(lever) AS camel,\n    MIN(lever) AS season,\n    AVG(lever) AS manila\n  FROM cte_2\n  GROUP BY\n    elite,\n    crest\n),\ncte_4 AS (\n  SELECT\n    MAX(manila) AS praise,\n    SUM(manila) AS mason,\n    MAX(season) AS rhyme\n  FROM cte_3\n  GROUP BY\n    camel\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ethic,\n     bloke,\n     ellen,\n     ticket,\n     unison\n|> AGGREGATE\n     MAX(unison) AS digest,\n     COUNT(ethic) AS baker,\n     AVG(bloke) AS smith,\n     AVG(ethic) AS mount\n|> AGGREGATE\n     MIN(mount) AS crest,\n     SUM(smith) AS lever,\n     COUNT(digest) AS jargon\n|> AGGREGATE\n     COUNT(lever) AS camel,\n     MIN(lever) AS season,\n     AVG(lever) AS manila\n   GROUP BY\n     elite,\n     crest\n|> AGGREGATE\n     MAX(manila) AS praise,\n     SUM(manila) AS mason,\n     MAX(season) AS rhyme\n   GROUP BY\n     camel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"elite",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sample,\n    potato,\n    mercy,\n    crane,\n    rebate\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(crane) AS uproar,\n    MAX(rebate) AS relic,\n    AVG(rebate) AS avail,\n    SUM(mercy) AS barth\n  FROM cte_0\n  GROUP BY\n    sample,\n    potato\n),\ncte_2 AS (\n  SELECT\n    MIN(avail) AS ruler,\n    AVG(uproar) AS relief,\n    SUM(avail) AS mentor\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(mentor) AS march\n  FROM cte_2\n  GROUP BY\n    ruler,\n    relief,\n    winter\n),\ncte_4 AS (\n  SELECT\n    SUM(march) AS amino,\n    COUNT(march) AS labour,\n    MAX(march) AS sting,\n    MIN(march) AS needle\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sample,\n     potato,\n     mercy,\n     crane,\n     rebate\n|> AGGREGATE\n     MIN(crane) AS uproar,\n     MAX(rebate) AS relic,\n     AVG(rebate) AS avail,\n     SUM(mercy) AS barth\n   GROUP BY\n     sample,\n     potato\n|> AGGREGATE\n     MIN(avail) AS ruler,\n     AVG(uproar) AS relief,\n     SUM(avail) AS mentor\n|> AGGREGATE\n     AVG(mentor) AS march\n   GROUP BY\n     ruler,\n     relief,\n     winter\n|> AGGREGATE\n     SUM(march) AS amino,\n     COUNT(march) AS labour,\n     MAX(march) AS sting,\n     MIN(march) AS needle\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"winter",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    stout,\n    norma,\n    tonic,\n    armour,\n    sharp\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(norma) AS caste,\n    SUM(norma) AS brink,\n    AVG(sharp) AS beggar,\n    MIN(tonic) AS guide\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    caste,\n    brink,\n    beggar\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(brink) AS artist,\n    MAX(brink) AS anchor,\n    MIN(beggar) AS cutter,\n    SUM(beggar) AS gable\n  FROM cte_2\n  GROUP BY\n    caste,\n    nelson\n),\ncte_4 AS (\n  SELECT\n    AVG(cutter) AS trail,\n    AVG(anchor) AS tally,\n    AVG(gable) AS crane,\n    MIN(gable) AS slick\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     stout,\n     norma,\n     tonic,\n     armour,\n     sharp\n|> AGGREGATE\n     AVG(norma) AS caste,\n     SUM(norma) AS brink,\n     AVG(sharp) AS beggar,\n     MIN(tonic) AS guide\n|> SELECT\n     caste,\n     brink,\n     beggar\n|> AGGREGATE\n     AVG(brink) AS artist,\n     MAX(brink) AS anchor,\n     MIN(beggar) AS cutter,\n     SUM(beggar) AS gable\n   GROUP BY\n     caste,\n     nelson\n|> AGGREGATE\n     AVG(cutter) AS trail,\n     AVG(anchor) AS tally,\n     AVG(gable) AS crane,\n     MIN(gable) AS slick\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"nelson",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    lodge,\n    dobson,\n    cattle,\n    marsh,\n    facade\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(marsh) AS trench,\n    COUNT(facade) AS favour,\n    SUM(facade) AS brunt\n  FROM cte_0\n  GROUP BY\n    lodge,\n    dobson\n),\ncte_2 AS (\n  SELECT\n    SUM(favour) AS carpet,\n    SUM(brunt) AS thirst,\n    AVG(trench) AS house,\n    COUNT(trench) AS ransom,\n    MAX(trench) AS devil,\n    MIN(favour) AS status\n  FROM cte_1\n  GROUP BY\n    hooper\n),\ncte_3 AS (\n  SELECT\n    carpet,\n    thirst,\n    house\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(house) AS cargo,\n    AVG(thirst) AS angel\n  FROM cte_3\n  GROUP BY\n    carpet\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     lodge,\n     dobson,\n     cattle,\n     marsh,\n     facade\n|> AGGREGATE\n     MIN(marsh) AS trench,\n     COUNT(facade) AS favour,\n     SUM(facade) AS brunt\n   GROUP BY\n     lodge,\n     dobson\n|> AGGREGATE\n     SUM(favour) AS carpet,\n     SUM(brunt) AS thirst,\n     AVG(trench) AS house,\n     COUNT(trench) AS ransom,\n     MAX(trench) AS devil,\n     MIN(favour) AS status\n   GROUP BY\n     hooper\n|> SELECT\n     carpet,\n     thirst,\n     house\n|> AGGREGATE\n     MIN(house) AS cargo,\n     AVG(thirst) AS angel\n   GROUP BY\n     carpet\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"hooper",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tenant,\n    suburb,\n    rumour,\n    goblin,\n    pistol\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(pistol) AS thorpe,\n    AVG(suburb) AS stout,\n    AVG(rumour) AS convoy,\n    MIN(tenant) AS table\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(table) AS pollen,\n    COUNT(convoy) AS walnut,\n    MIN(table) AS state,\n    SUM(table) AS drink,\n    AVG(stout) AS enemy,\n    SUM(convoy) AS lorry\n  FROM cte_1\n  GROUP BY\n    basis,\n    thorpe\n),\ncte_3 AS (\n  SELECT\n    pollen,\n    walnut,\n    state,\n    drink,\n    enemy\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    pollen,\n    walnut\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tenant,\n     suburb,\n     rumour,\n     goblin,\n     pistol\n|> AGGREGATE\n     COUNT(pistol) AS thorpe,\n     AVG(suburb) AS stout,\n     AVG(rumour) AS convoy,\n     MIN(tenant) AS table\n|> AGGREGATE\n     COUNT(table) AS pollen,\n     COUNT(convoy) AS walnut,\n     MIN(table) AS state,\n     SUM(table) AS drink,\n     AVG(stout) AS enemy,\n     SUM(convoy) AS lorry\n   GROUP BY\n     basis,\n     thorpe\n|> SELECT\n     pollen,\n     walnut,\n     state,\n     drink,\n     enemy\n|> SELECT\n     pollen,\n     walnut\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"basis",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    witney,\n    slough,\n    derby,\n    cowboy,\n    brewer\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(brewer) AS ferry,\n    MAX(brewer) AS seaman,\n    SUM(brewer) AS banana\n  FROM cte_0\n  GROUP BY\n    witney,\n    slough,\n    derby,\n    cowboy\n),\ncte_2 AS (\n  SELECT\n    ferry,\n    seaman,\n    banana\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(banana) AS hector,\n    MAX(banana) AS vacuum,\n    SUM(banana) AS period\n  FROM cte_2\n  GROUP BY\n    ferry,\n    rumour,\n    seaman\n),\ncte_4 AS (\n  SELECT\n    MAX(period) AS medal,\n    AVG(period) AS broad,\n    AVG(vacuum) AS tiller\n  FROM cte_3\n  GROUP BY\n    hector\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     witney,\n     slough,\n     derby,\n     cowboy,\n     brewer\n|> AGGREGATE\n     MIN(brewer) AS ferry,\n     MAX(brewer) AS seaman,\n     SUM(brewer) AS banana\n   GROUP BY\n     witney,\n     slough,\n     derby,\n     cowboy\n|> SELECT\n     ferry,\n     seaman,\n     banana\n|> AGGREGATE\n     AVG(banana) AS hector,\n     MAX(banana) AS vacuum,\n     SUM(banana) AS period\n   GROUP BY\n     ferry,\n     rumour,\n     seaman\n|> AGGREGATE\n     MAX(period) AS medal,\n     AVG(period) AS broad,\n     AVG(vacuum) AS tiller\n   GROUP BY\n     hector\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"rumour",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    inset,\n    tailor,\n    robot,\n    shelf,\n    level\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(shelf) AS storey,\n    MAX(level) AS dobson,\n    SUM(shelf) AS honour,\n    SUM(level) AS woody,\n    AVG(level) AS carter\n  FROM cte_0\n  GROUP BY\n    inset,\n    tailor,\n    robot\n),\ncte_2 AS (\n  SELECT\n    MAX(carter) AS spate,\n    MIN(carter) AS utopia,\n    MIN(honour) AS stain\n  FROM cte_1\n  GROUP BY\n    storey,\n    manual\n),\ncte_3 AS (\n  SELECT\n    spate,\n    utopia,\n    SUM(stain) AS baxter\n  FROM cte_2\n  GROUP BY\n    spate,\n    utopia\n),\ncte_4 AS (\n  SELECT\n    SUM(utopia) AS glint\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     inset,\n     tailor,\n     robot,\n     shelf,\n     level\n|> AGGREGATE\n     AVG(shelf) AS storey,\n     MAX(level) AS dobson,\n     SUM(shelf) AS honour,\n     SUM(level) AS woody,\n     AVG(level) AS carter\n   GROUP BY\n     inset,\n     tailor,\n     robot\n|> AGGREGATE\n     MAX(carter) AS spate,\n     MIN(carter) AS utopia,\n     MIN(honour) AS stain\n   GROUP BY\n     storey,\n     manual\n|> AGGREGATE\n     SUM(stain) AS baxter\n   GROUP BY\n     spate,\n     utopia\n|> AGGREGATE\n     SUM(utopia) AS glint\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"manual",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    stead,\n    maiden,\n    texas,\n    kitty,\n    serum\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(serum) AS batch,\n    AVG(serum) AS chant,\n    MAX(maiden) AS crime\n  FROM cte_0\n  GROUP BY\n    stead\n),\ncte_2 AS (\n  SELECT\n    AVG(batch) AS spirit,\n    MAX(chant) AS belle,\n    AVG(chant) AS grape\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MIN(grape) AS creole,\n    MAX(grape) AS jones,\n    AVG(grape) AS henley\n  FROM cte_2\n  GROUP BY\n    spirit,\n    dogma,\n    belle\n),\ncte_4 AS (\n  SELECT\n    creole,\n    COUNT(henley) AS device,\n    MAX(henley) AS report\n  FROM cte_3\n  GROUP BY\n    creole,\n    jones\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     stead,\n     maiden,\n     texas,\n     kitty,\n     serum\n|> AGGREGATE\n     SUM(serum) AS batch,\n     AVG(serum) AS chant,\n     MAX(maiden) AS crime\n   GROUP BY\n     stead\n|> AGGREGATE\n     AVG(batch) AS spirit,\n     MAX(chant) AS belle,\n     AVG(chant) AS grape\n|> AGGREGATE\n     MIN(grape) AS creole,\n     MAX(grape) AS jones,\n     AVG(grape) AS henley\n   GROUP BY\n     spirit,\n     dogma,\n     belle\n|> AGGREGATE\n     COUNT(henley) AS device,\n     MAX(henley) AS report\n   GROUP BY\n     creole,\n     jones\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"dogma",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tyler,\n    leave,\n    cloak,\n    option,\n    desert\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(desert) AS wallet,\n    COUNT(cloak) AS legion,\n    SUM(desert) AS quest\n  FROM cte_0\n  GROUP BY\n    tyler,\n    leave\n),\ncte_2 AS (\n  SELECT\n    AVG(quest) AS pedal,\n    COUNT(legion) AS alaska,\n    MAX(legion) AS murphy\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MIN(murphy) AS matey,\n    COUNT(murphy) AS kidney,\n    MAX(murphy) AS basic\n  FROM cte_2\n  GROUP BY\n    wonder,\n    pedal,\n    alaska\n),\ncte_4 AS (\n  SELECT\n    matey,\n    AVG(basic) AS flora,\n    SUM(kidney) AS sensor\n  FROM cte_3\n  GROUP BY\n    matey\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tyler,\n     leave,\n     cloak,\n     option,\n     desert\n|> AGGREGATE\n     AVG(desert) AS wallet,\n     COUNT(cloak) AS legion,\n     SUM(desert) AS quest\n   GROUP BY\n     tyler,\n     leave\n|> AGGREGATE\n     AVG(quest) AS pedal,\n     COUNT(legion) AS alaska,\n     MAX(legion) AS murphy\n|> AGGREGATE\n     MIN(murphy) AS matey,\n     COUNT(murphy) AS kidney,\n     MAX(murphy) AS basic\n   GROUP BY\n     wonder,\n     pedal,\n     alaska\n|> AGGREGATE\n     AVG(basic) AS flora,\n     SUM(kidney) AS sensor\n   GROUP BY\n     matey\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"wonder",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    combo,\n    jersey,\n    style,\n    advent,\n    lizard\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(lizard) AS colin,\n    MAX(advent) AS wallet\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(colin) AS person,\n    SUM(colin) AS cargo,\n    COUNT(wallet) AS pillar,\n    SUM(wallet) AS sequel,\n    MIN(wallet) AS booze,\n    COUNT(colin) AS strand,\n    AVG(wallet) AS heresy,\n    MAX(wallet) AS helix\n  FROM cte_1\n  GROUP BY\n    stoke\n),\ncte_3 AS (\n  SELECT\n    person,\n    cargo,\n    pillar,\n    sequel\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(cargo) AS shaikh,\n    SUM(sequel) AS panic\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     combo,\n     jersey,\n     style,\n     advent,\n     lizard\n|> AGGREGATE\n     COUNT(lizard) AS colin,\n     MAX(advent) AS wallet\n|> AGGREGATE\n     MIN(colin) AS person,\n     SUM(colin) AS cargo,\n     COUNT(wallet) AS pillar,\n     SUM(wallet) AS sequel,\n     MIN(wallet) AS booze,\n     COUNT(colin) AS strand,\n     AVG(wallet) AS heresy,\n     MAX(wallet) AS helix\n   GROUP BY\n     stoke\n|> SELECT\n     person,\n     cargo,\n     pillar,\n     sequel\n|> AGGREGATE\n     AVG(cargo) AS shaikh,\n     SUM(sequel) AS panic\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"stoke",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hoover,\n    bloke,\n    banana,\n    guest,\n    hammer\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(hammer) AS fiddle\n  FROM cte_0\n  GROUP BY\n    hoover,\n    bloke,\n    banana,\n    guest\n),\ncte_2 AS (\n  SELECT\n    AVG(fiddle) AS ballot,\n    SUM(fiddle) AS porter,\n    MIN(fiddle) AS chick\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(chick) AS chapel,\n    AVG(chick) AS olive,\n    COUNT(chick) AS person,\n    MIN(chick) AS daphne\n  FROM cte_2\n  GROUP BY\n    slave,\n    ballot,\n    porter\n),\ncte_4 AS (\n  SELECT\n    MIN(daphne) AS crowd,\n    SUM(person) AS bowie,\n    AVG(daphne) AS powder\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hoover,\n     bloke,\n     banana,\n     guest,\n     hammer\n|> AGGREGATE\n     SUM(hammer) AS fiddle\n   GROUP BY\n     hoover,\n     bloke,\n     banana,\n     guest\n|> AGGREGATE\n     AVG(fiddle) AS ballot,\n     SUM(fiddle) AS porter,\n     MIN(fiddle) AS chick\n|> AGGREGATE\n     SUM(chick) AS chapel,\n     AVG(chick) AS olive,\n     COUNT(chick) AS person,\n     MIN(chick) AS daphne\n   GROUP BY\n     slave,\n     ballot,\n     porter\n|> AGGREGATE\n     MIN(daphne) AS crowd,\n     SUM(person) AS bowie,\n     AVG(daphne) AS powder\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"slave",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    daily,\n    reply,\n    kettle,\n    paddy,\n    roche\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(roche) AS flank,\n    AVG(kettle) AS bacon,\n    AVG(daily) AS thorpe,\n    SUM(kettle) AS barry,\n    SUM(reply) AS brace,\n    AVG(reply) AS lagoon\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    flank,\n    bacon\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MIN(flank) AS betty,\n    SUM(flank) AS tomato,\n    AVG(flank) AS trade\n  FROM cte_2\n  GROUP BY\n    tangle\n),\ncte_4 AS (\n  SELECT\n    betty,\n    tomato,\n    COUNT(trade) AS filth,\n    MIN(trade) AS lodge\n  FROM cte_3\n  GROUP BY\n    betty,\n    tomato\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     daily,\n     reply,\n     kettle,\n     paddy,\n     roche\n|> AGGREGATE\n     MIN(roche) AS flank,\n     AVG(kettle) AS bacon,\n     AVG(daily) AS thorpe,\n     SUM(kettle) AS barry,\n     SUM(reply) AS brace,\n     AVG(reply) AS lagoon\n|> SELECT\n     flank,\n     bacon\n|> AGGREGATE\n     MIN(flank) AS betty,\n     SUM(flank) AS tomato,\n     AVG(flank) AS trade\n   GROUP BY\n     tangle\n|> AGGREGATE\n     COUNT(trade) AS filth,\n     MIN(trade) AS lodge\n   GROUP BY\n     betty,\n     tomato\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"tangle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    gypsy,\n    scrap,\n    brazil,\n    array,\n    fowler\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(fowler) AS hazard,\n    COUNT(fowler) AS elaine,\n    MAX(fowler) AS roller,\n    SUM(fowler) AS maxim\n  FROM cte_0\n  GROUP BY\n    gypsy,\n    scrap,\n    brazil,\n    array\n),\ncte_2 AS (\n  SELECT\n    MAX(roller) AS hector,\n    SUM(roller) AS blitz,\n    AVG(elaine) AS torch,\n    COUNT(maxim) AS sphere\n  FROM cte_1\n  GROUP BY\n    member,\n    hazard\n),\ncte_3 AS (\n  SELECT\n    hector,\n    MIN(torch) AS crisp\n  FROM cte_2\n  GROUP BY\n    hector,\n    blitz\n),\ncte_4 AS (\n  SELECT\n    SUM(hector) AS denial\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     gypsy,\n     scrap,\n     brazil,\n     array,\n     fowler\n|> AGGREGATE\n     AVG(fowler) AS hazard,\n     COUNT(fowler) AS elaine,\n     MAX(fowler) AS roller,\n     SUM(fowler) AS maxim\n   GROUP BY\n     gypsy,\n     scrap,\n     brazil,\n     array\n|> AGGREGATE\n     MAX(roller) AS hector,\n     SUM(roller) AS blitz,\n     AVG(elaine) AS torch,\n     COUNT(maxim) AS sphere\n   GROUP BY\n     member,\n     hazard\n|> AGGREGATE\n     MIN(torch) AS crisp\n   GROUP BY\n     hector,\n     blitz\n|> AGGREGATE\n     SUM(hector) AS denial\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"member",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    gutter,\n    toxin,\n    radius,\n    henry,\n    swift\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(toxin) AS planet,\n    COUNT(radius) AS hurst,\n    MIN(henry) AS reflex,\n    AVG(radius) AS label\n  FROM cte_0\n  GROUP BY\n    gutter\n),\ncte_2 AS (\n  SELECT\n    MAX(label) AS muddle,\n    AVG(label) AS shaikh,\n    MIN(label) AS clutch,\n    COUNT(label) AS chord\n  FROM cte_1\n  GROUP BY\n    planet,\n    hurst,\n    reflex,\n    wright\n),\ncte_3 AS (\n  SELECT\n    muddle,\n    shaikh,\n    clutch,\n    chord\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(shaikh) AS effort,\n    MAX(clutch) AS board\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     gutter,\n     toxin,\n     radius,\n     henry,\n     swift\n|> AGGREGATE\n     MIN(toxin) AS planet,\n     COUNT(radius) AS hurst,\n     MIN(henry) AS reflex,\n     AVG(radius) AS label\n   GROUP BY\n     gutter\n|> AGGREGATE\n     MAX(label) AS muddle,\n     AVG(label) AS shaikh,\n     MIN(label) AS clutch,\n     COUNT(label) AS chord\n   GROUP BY\n     planet,\n     hurst,\n     reflex,\n     wright\n|> SELECT\n     muddle,\n     shaikh,\n     clutch,\n     chord\n|> AGGREGATE\n     SUM(shaikh) AS effort,\n     MAX(clutch) AS board\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"wright",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    notion,\n    debit,\n    matron,\n    harem,\n    unease\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(unease) AS aspect,\n    SUM(matron) AS nephew,\n    SUM(harem) AS caller,\n    MIN(matron) AS curse\n  FROM cte_0\n  GROUP BY\n    notion,\n    debit\n),\ncte_2 AS (\n  SELECT\n    aspect,\n    nephew,\n    caller,\n    curse\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(curse) AS blend,\n    MIN(curse) AS devil,\n    MAX(curse) AS facade\n  FROM cte_2\n  GROUP BY\n    hollow,\n    aspect,\n    nephew,\n    caller\n),\ncte_4 AS (\n  SELECT\n    blend,\n    COUNT(facade) AS leader\n  FROM cte_3\n  GROUP BY\n    blend,\n    devil\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     notion,\n     debit,\n     matron,\n     harem,\n     unease\n|> AGGREGATE\n     MAX(unease) AS aspect,\n     SUM(matron) AS nephew,\n     SUM(harem) AS caller,\n     MIN(matron) AS curse\n   GROUP BY\n     notion,\n     debit\n|> SELECT\n     aspect,\n     nephew,\n     caller,\n     curse\n|> AGGREGATE\n     AVG(curse) AS blend,\n     MIN(curse) AS devil,\n     MAX(curse) AS facade\n   GROUP BY\n     hollow,\n     aspect,\n     nephew,\n     caller\n|> AGGREGATE\n     COUNT(facade) AS leader\n   GROUP BY\n     blend,\n     devil\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"hollow",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    mayor,\n    chief,\n    block,\n    farce,\n    battle\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(mayor) AS bonus,\n    MAX(battle) AS travel,\n    COUNT(chief) AS mobile\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    bonus,\n    travel\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(travel) AS sleeve,\n    COUNT(bonus) AS gunman,\n    AVG(bonus) AS tennis,\n    MIN(travel) AS linen,\n    MAX(bonus) AS stare,\n    SUM(bonus) AS flick\n  FROM cte_2\n  GROUP BY\n    treat\n),\ncte_4 AS (\n  SELECT\n    sleeve,\n    MAX(tennis) AS strip,\n    SUM(flick) AS betty,\n    MIN(linen) AS havoc\n  FROM cte_3\n  GROUP BY\n    sleeve\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     mayor,\n     chief,\n     block,\n     farce,\n     battle\n|> AGGREGATE\n     SUM(mayor) AS bonus,\n     MAX(battle) AS travel,\n     COUNT(chief) AS mobile\n|> SELECT\n     bonus,\n     travel\n|> AGGREGATE\n     COUNT(travel) AS sleeve,\n     COUNT(bonus) AS gunman,\n     AVG(bonus) AS tennis,\n     MIN(travel) AS linen,\n     MAX(bonus) AS stare,\n     SUM(bonus) AS flick\n   GROUP BY\n     treat\n|> AGGREGATE\n     MAX(tennis) AS strip,\n     SUM(flick) AS betty,\n     MIN(linen) AS havoc\n   GROUP BY\n     sleeve\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"treat",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    prose,\n    terry,\n    saddle,\n    turkey,\n    crypt\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(terry) AS pearl,\n    COUNT(crypt) AS rumour\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(rumour) AS nature,\n    SUM(rumour) AS candle,\n    MAX(rumour) AS rogue,\n    AVG(rumour) AS spine\n  FROM cte_1\n  GROUP BY\n    pearl\n),\ncte_3 AS (\n  SELECT\n    MAX(spine) AS murphy,\n    AVG(spine) AS ozone\n  FROM cte_2\n  GROUP BY\n    nature,\n    candle,\n    rogue,\n    resale\n),\ncte_4 AS (\n  SELECT\n    AVG(murphy) AS smith,\n    SUM(murphy) AS blitz,\n    AVG(ozone) AS dinghy,\n    SUM(ozone) AS heresy\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     prose,\n     terry,\n     saddle,\n     turkey,\n     crypt\n|> AGGREGATE\n     COUNT(terry) AS pearl,\n     COUNT(crypt) AS rumour\n|> AGGREGATE\n     MIN(rumour) AS nature,\n     SUM(rumour) AS candle,\n     MAX(rumour) AS rogue,\n     AVG(rumour) AS spine\n   GROUP BY\n     pearl\n|> AGGREGATE\n     MAX(spine) AS murphy,\n     AVG(spine) AS ozone\n   GROUP BY\n     nature,\n     candle,\n     rogue,\n     resale\n|> AGGREGATE\n     AVG(murphy) AS smith,\n     SUM(murphy) AS blitz,\n     AVG(ozone) AS dinghy,\n     SUM(ozone) AS heresy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"resale",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    coffin,\n    daniel,\n    marker,\n    brewer,\n    groom\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(coffin) AS summit,\n    MIN(groom) AS nancy,\n    MAX(brewer) AS death,\n    MIN(daniel) AS perry,\n    AVG(coffin) AS strap\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(death) AS fancy,\n    COUNT(strap) AS foster,\n    SUM(perry) AS sauna,\n    MIN(death) AS space,\n    AVG(perry) AS zebra\n  FROM cte_1\n  GROUP BY\n    photo,\n    summit,\n    nancy\n),\ncte_3 AS (\n  SELECT\n    fancy,\n    foster\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    fancy,\n    COUNT(foster) AS poetry\n  FROM cte_3\n  GROUP BY\n    fancy\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     coffin,\n     daniel,\n     marker,\n     brewer,\n     groom\n|> AGGREGATE\n     MIN(coffin) AS summit,\n     MIN(groom) AS nancy,\n     MAX(brewer) AS death,\n     MIN(daniel) AS perry,\n     AVG(coffin) AS strap\n|> AGGREGATE\n     SUM(death) AS fancy,\n     COUNT(strap) AS foster,\n     SUM(perry) AS sauna,\n     MIN(death) AS space,\n     AVG(perry) AS zebra\n   GROUP BY\n     photo,\n     summit,\n     nancy\n|> SELECT\n     fancy,\n     foster\n|> AGGREGATE\n     COUNT(foster) AS poetry\n   GROUP BY\n     fancy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"photo",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ivory,\n    rarity,\n    bunny,\n    crush,\n    divide\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    ivory,\n    rarity\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(ivory) AS buyer,\n    MIN(rarity) AS groove,\n    SUM(rarity) AS viking,\n    COUNT(ivory) AS plague,\n    MIN(ivory) AS graham,\n    MAX(ivory) AS breach,\n    AVG(rarity) AS rhine,\n    COUNT(rarity) AS sally,\n    AVG(ivory) AS cuckoo\n  FROM cte_1\n  GROUP BY\n    agency\n),\ncte_3 AS (\n  SELECT\n    buyer,\n    groove\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    buyer,\n    MIN(groove) AS circus,\n    SUM(groove) AS swift\n  FROM cte_3\n  GROUP BY\n    buyer\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ivory,\n     rarity,\n     bunny,\n     crush,\n     divide\n|> SELECT\n     ivory,\n     rarity\n|> AGGREGATE\n     SUM(ivory) AS buyer,\n     MIN(rarity) AS groove,\n     SUM(rarity) AS viking,\n     COUNT(ivory) AS plague,\n     MIN(ivory) AS graham,\n     MAX(ivory) AS breach,\n     AVG(rarity) AS rhine,\n     COUNT(rarity) AS sally,\n     AVG(ivory) AS cuckoo\n   GROUP BY\n     agency\n|> SELECT\n     buyer,\n     groove\n|> AGGREGATE\n     MIN(groove) AS circus,\n     SUM(groove) AS swift\n   GROUP BY\n     buyer\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"agency",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    wealth,\n    draft,\n    queue,\n    kylie,\n    carbon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    wealth,\n    draft,\n    queue\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(queue) AS spate,\n    AVG(draft) AS cotton,\n    AVG(queue) AS puppet\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(cotton) AS trophy,\n    SUM(cotton) AS centre,\n    MIN(puppet) AS medal,\n    MIN(cotton) AS piano,\n    COUNT(puppet) AS clinic\n  FROM cte_2\n  GROUP BY\n    spate,\n    night\n),\ncte_4 AS (\n  SELECT\n    trophy,\n    centre,\n    medal,\n    SUM(piano) AS crush\n  FROM cte_3\n  GROUP BY\n    trophy,\n    centre,\n    medal\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     wealth,\n     draft,\n     queue,\n     kylie,\n     carbon\n|> SELECT\n     wealth,\n     draft,\n     queue\n|> AGGREGATE\n     MAX(queue) AS spate,\n     AVG(draft) AS cotton,\n     AVG(queue) AS puppet\n|> AGGREGATE\n     COUNT(cotton) AS trophy,\n     SUM(cotton) AS centre,\n     MIN(puppet) AS medal,\n     MIN(cotton) AS piano,\n     COUNT(puppet) AS clinic\n   GROUP BY\n     spate,\n     night\n|> AGGREGATE\n     SUM(piano) AS crush\n   GROUP BY\n     trophy,\n     centre,\n     medal\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"night",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    flame,\n    jasper,\n    radar,\n    orchid,\n    incest\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(incest) AS saloon,\n    MAX(incest) AS hedge,\n    SUM(incest) AS utopia,\n    MIN(incest) AS joanna,\n    COUNT(incest) AS menace\n  FROM cte_0\n  GROUP BY\n    flame,\n    jasper,\n    radar,\n    orchid\n),\ncte_2 AS (\n  SELECT\n    SUM(menace) AS noise,\n    SUM(utopia) AS folly,\n    MAX(menace) AS dwarf\n  FROM cte_1\n  GROUP BY\n    claire,\n    saloon\n),\ncte_3 AS (\n  SELECT\n    noise,\n    SUM(dwarf) AS bugger\n  FROM cte_2\n  GROUP BY\n    noise\n),\ncte_4 AS (\n  SELECT\n    SUM(bugger) AS uptake\n  FROM cte_3\n  GROUP BY\n    noise\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     flame,\n     jasper,\n     radar,\n     orchid,\n     incest\n|> AGGREGATE\n     AVG(incest) AS saloon,\n     MAX(incest) AS hedge,\n     SUM(incest) AS utopia,\n     MIN(incest) AS joanna,\n     COUNT(incest) AS menace\n   GROUP BY\n     flame,\n     jasper,\n     radar,\n     orchid\n|> AGGREGATE\n     SUM(menace) AS noise,\n     SUM(utopia) AS folly,\n     MAX(menace) AS dwarf\n   GROUP BY\n     claire,\n     saloon\n|> AGGREGATE\n     SUM(dwarf) AS bugger\n   GROUP BY\n     noise\n|> AGGREGATE\n     SUM(bugger) AS uptake\n   GROUP BY\n     noise\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"claire",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    doctor,\n    stella,\n    spine,\n    gender,\n    stereo\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(stella) AS daniel,\n    COUNT(gender) AS liquid,\n    COUNT(doctor) AS abuse,\n    MIN(stella) AS grant,\n    COUNT(stereo) AS final,\n    SUM(gender) AS willy,\n    MIN(stereo) AS drama\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(abuse) AS supper,\n    AVG(grant) AS smash,\n    MAX(drama) AS maple\n  FROM cte_1\n  GROUP BY\n    cereal\n),\ncte_3 AS (\n  SELECT\n    COUNT(smash) AS battle\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(battle) AS bottom,\n    MAX(battle) AS cover,\n    AVG(battle) AS centre\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     doctor,\n     stella,\n     spine,\n     gender,\n     stereo\n|> AGGREGATE\n     AVG(stella) AS daniel,\n     COUNT(gender) AS liquid,\n     COUNT(doctor) AS abuse,\n     MIN(stella) AS grant,\n     COUNT(stereo) AS final,\n     SUM(gender) AS willy,\n     MIN(stereo) AS drama\n|> AGGREGATE\n     SUM(abuse) AS supper,\n     AVG(grant) AS smash,\n     MAX(drama) AS maple\n   GROUP BY\n     cereal\n|> AGGREGATE\n     COUNT(smash) AS battle\n|> AGGREGATE\n     MIN(battle) AS bottom,\n     MAX(battle) AS cover,\n     AVG(battle) AS centre\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"cereal",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    finger,\n    fluid,\n    decade,\n    scale,\n    harem\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(finger) AS ditch,\n    COUNT(finger) AS valve,\n    AVG(scale) AS buddy,\n    SUM(scale) AS bridge,\n    AVG(finger) AS mosque\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    ditch,\n    valve,\n    buddy,\n    bridge\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(valve) AS chant,\n    MIN(buddy) AS watch,\n    MAX(valve) AS lager\n  FROM cte_2\n  GROUP BY\n    rouge\n),\ncte_4 AS (\n  SELECT\n    COUNT(lager) AS orange,\n    AVG(lager) AS hitch,\n    SUM(lager) AS wight\n  FROM cte_3\n  GROUP BY\n    chant,\n    watch\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     finger,\n     fluid,\n     decade,\n     scale,\n     harem\n|> AGGREGATE\n     MIN(finger) AS ditch,\n     COUNT(finger) AS valve,\n     AVG(scale) AS buddy,\n     SUM(scale) AS bridge,\n     AVG(finger) AS mosque\n|> SELECT\n     ditch,\n     valve,\n     buddy,\n     bridge\n|> AGGREGATE\n     AVG(valve) AS chant,\n     MIN(buddy) AS watch,\n     MAX(valve) AS lager\n   GROUP BY\n     rouge\n|> AGGREGATE\n     COUNT(lager) AS orange,\n     AVG(lager) AS hitch,\n     SUM(lager) AS wight\n   GROUP BY\n     chant,\n     watch\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"rouge",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    butler,\n    street,\n    fault,\n    ideal,\n    plain\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(ideal) AS torque,\n    AVG(street) AS range,\n    MIN(plain) AS siren,\n    AVG(butler) AS weight,\n    COUNT(butler) AS sierra,\n    AVG(fault) AS remedy,\n    MIN(street) AS couple,\n    COUNT(ideal) AS warden,\n    SUM(butler) AS review,\n    AVG(plain) AS valve\n  FROM cte_0\n  GROUP BY\n    weaver\n),\ncte_2 AS (\n  SELECT\n    torque,\n    range\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    torque,\n    range\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    torque,\n    SUM(range) AS strand\n  FROM cte_3\n  GROUP BY\n    torque\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     butler,\n     street,\n     fault,\n     ideal,\n     plain\n|> AGGREGATE\n     MAX(ideal) AS torque,\n     AVG(street) AS range,\n     MIN(plain) AS siren,\n     AVG(butler) AS weight,\n     COUNT(butler) AS sierra,\n     AVG(fault) AS remedy,\n     MIN(street) AS couple,\n     COUNT(ideal) AS warden,\n     SUM(butler) AS review,\n     AVG(plain) AS valve\n   GROUP BY\n     weaver\n|> SELECT\n     torque,\n     range\n|> SELECT\n     torque,\n     range\n|> AGGREGATE\n     SUM(range) AS strand\n   GROUP BY\n     torque\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"weaver",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:25,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    danger,\n    price,\n    mister,\n    niece,\n    pastry\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(mister) AS dummy,\n    MAX(niece) AS format,\n    COUNT(danger) AS candy,\n    SUM(pastry) AS gospel,\n    MIN(danger) AS adobe,\n    MAX(pastry) AS bridle\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(adobe) AS barley,\n    COUNT(bridle) AS croft,\n    SUM(gospel) AS closet,\n    SUM(adobe) AS tumour\n  FROM cte_1\n  GROUP BY\n    outing,\n    dummy,\n    format,\n    candy\n),\ncte_3 AS (\n  SELECT\n    barley,\n    croft,\n    closet\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    barley,\n    croft\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     danger,\n     price,\n     mister,\n     niece,\n     pastry\n|> AGGREGATE\n     SUM(mister) AS dummy,\n     MAX(niece) AS format,\n     COUNT(danger) AS candy,\n     SUM(pastry) AS gospel,\n     MIN(danger) AS adobe,\n     MAX(pastry) AS bridle\n|> AGGREGATE\n     MIN(adobe) AS barley,\n     COUNT(bridle) AS croft,\n     SUM(gospel) AS closet,\n     SUM(adobe) AS tumour\n   GROUP BY\n     outing,\n     dummy,\n     format,\n     candy\n|> SELECT\n     barley,\n     croft,\n     closet\n|> SELECT\n     barley,\n     croft\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"outing",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bidder,\n    serial,\n    climax,\n    lawson,\n    exodus\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(lawson) AS dealer,\n    COUNT(exodus) AS henry,\n    SUM(exodus) AS final\n  FROM cte_0\n  GROUP BY\n    bidder,\n    serial,\n    climax\n),\ncte_2 AS (\n  SELECT\n    AVG(henry) AS farce,\n    AVG(final) AS sherry,\n    SUM(final) AS split,\n    COUNT(final) AS river,\n    MAX(henry) AS trauma\n  FROM cte_1\n  GROUP BY\n    dealer,\n    kernel\n),\ncte_3 AS (\n  SELECT\n    farce,\n    MAX(sherry) AS fella,\n    MIN(trauma) AS millie\n  FROM cte_2\n  GROUP BY\n    farce\n),\ncte_4 AS (\n  SELECT\n    AVG(fella) AS jacket\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bidder,\n     serial,\n     climax,\n     lawson,\n     exodus\n|> AGGREGATE\n     SUM(lawson) AS dealer,\n     COUNT(exodus) AS henry,\n     SUM(exodus) AS final\n   GROUP BY\n     bidder,\n     serial,\n     climax\n|> AGGREGATE\n     AVG(henry) AS farce,\n     AVG(final) AS sherry,\n     SUM(final) AS split,\n     COUNT(final) AS river,\n     MAX(henry) AS trauma\n   GROUP BY\n     dealer,\n     kernel\n|> AGGREGATE\n     MAX(sherry) AS fella,\n     MIN(trauma) AS millie\n   GROUP BY\n     farce\n|> AGGREGATE\n     AVG(fella) AS jacket\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"kernel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    oxide,\n    shame,\n    white,\n    chaos,\n    olive\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(chaos) AS beryl\n  FROM cte_0\n  GROUP BY\n    oxide,\n    shame,\n    white\n),\ncte_2 AS (\n  SELECT\n    AVG(beryl) AS track,\n    MAX(beryl) AS print,\n    SUM(beryl) AS remit,\n    MIN(beryl) AS retina,\n    COUNT(beryl) AS plank\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MIN(retina) AS luxury,\n    SUM(plank) AS paddy\n  FROM cte_2\n  GROUP BY\n    track,\n    fella,\n    print\n),\ncte_4 AS (\n  SELECT\n    MIN(paddy) AS scope,\n    MAX(paddy) AS toilet,\n    AVG(paddy) AS crack\n  FROM cte_3\n  GROUP BY\n    luxury\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     oxide,\n     shame,\n     white,\n     chaos,\n     olive\n|> AGGREGATE\n     SUM(chaos) AS beryl\n   GROUP BY\n     oxide,\n     shame,\n     white\n|> AGGREGATE\n     AVG(beryl) AS track,\n     MAX(beryl) AS print,\n     SUM(beryl) AS remit,\n     MIN(beryl) AS retina,\n     COUNT(beryl) AS plank\n|> AGGREGATE\n     MIN(retina) AS luxury,\n     SUM(plank) AS paddy\n   GROUP BY\n     track,\n     fella,\n     print\n|> AGGREGATE\n     MIN(paddy) AS scope,\n     MAX(paddy) AS toilet,\n     AVG(paddy) AS crack\n   GROUP BY\n     luxury\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"fella",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    crunch,\n    nurse,\n    locke,\n    chile,\n    shout\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(crunch) AS filter,\n    COUNT(crunch) AS gloss,\n    AVG(shout) AS debate,\n    SUM(nurse) AS reply\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(gloss) AS boxer,\n    MAX(reply) AS argyll,\n    AVG(reply) AS spirit,\n    MAX(gloss) AS mirror,\n    MAX(debate) AS arrest,\n    MIN(debate) AS villa\n  FROM cte_1\n  GROUP BY\n    pencil,\n    filter\n),\ncte_3 AS (\n  SELECT\n    boxer,\n    argyll,\n    spirit\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(spirit) AS barker\n  FROM cte_3\n  GROUP BY\n    boxer,\n    argyll\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     crunch,\n     nurse,\n     locke,\n     chile,\n     shout\n|> AGGREGATE\n     MAX(crunch) AS filter,\n     COUNT(crunch) AS gloss,\n     AVG(shout) AS debate,\n     SUM(nurse) AS reply\n|> AGGREGATE\n     SUM(gloss) AS boxer,\n     MAX(reply) AS argyll,\n     AVG(reply) AS spirit,\n     MAX(gloss) AS mirror,\n     MAX(debate) AS arrest,\n     MIN(debate) AS villa\n   GROUP BY\n     pencil,\n     filter\n|> SELECT\n     boxer,\n     argyll,\n     spirit\n|> AGGREGATE\n     AVG(spirit) AS barker\n   GROUP BY\n     boxer,\n     argyll\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"pencil",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    creed,\n    enamel,\n    proton,\n    flare,\n    weight\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(weight) AS throne,\n    COUNT(weight) AS stroll,\n    SUM(flare) AS demand\n  FROM cte_0\n  GROUP BY\n    creed,\n    enamel,\n    proton\n),\ncte_2 AS (\n  SELECT\n    SUM(demand) AS morse,\n    SUM(stroll) AS thrust\n  FROM cte_1\n  GROUP BY\n    throne\n),\ncte_3 AS (\n  SELECT\n    SUM(thrust) AS maker,\n    MAX(thrust) AS level,\n    MIN(thrust) AS palmer\n  FROM cte_2\n  GROUP BY\n    craven,\n    morse\n),\ncte_4 AS (\n  SELECT\n    AVG(level) AS donor,\n    AVG(palmer) AS aroma,\n    COUNT(level) AS tablet\n  FROM cte_3\n  GROUP BY\n    maker\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     creed,\n     enamel,\n     proton,\n     flare,\n     weight\n|> AGGREGATE\n     AVG(weight) AS throne,\n     COUNT(weight) AS stroll,\n     SUM(flare) AS demand\n   GROUP BY\n     creed,\n     enamel,\n     proton\n|> AGGREGATE\n     SUM(demand) AS morse,\n     SUM(stroll) AS thrust\n   GROUP BY\n     throne\n|> AGGREGATE\n     SUM(thrust) AS maker,\n     MAX(thrust) AS level,\n     MIN(thrust) AS palmer\n   GROUP BY\n     craven,\n     morse\n|> AGGREGATE\n     AVG(level) AS donor,\n     AVG(palmer) AS aroma,\n     COUNT(level) AS tablet\n   GROUP BY\n     maker\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"craven",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    target,\n    claim,\n    double,\n    forte,\n    salon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(target) AS layout,\n    MAX(double) AS glider,\n    MAX(forte) AS winter\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(winter) AS cocoa,\n    MIN(winter) AS rebel,\n    COUNT(winter) AS cuckoo\n  FROM cte_1\n  GROUP BY\n    layout,\n    glider\n),\ncte_3 AS (\n  SELECT\n    COUNT(cuckoo) AS panic,\n    MAX(cuckoo) AS pledge,\n    AVG(cuckoo) AS remedy\n  FROM cte_2\n  GROUP BY\n    mister,\n    cocoa,\n    rebel\n),\ncte_4 AS (\n  SELECT\n    SUM(pledge) AS shock,\n    COUNT(remedy) AS carpet,\n    MAX(pledge) AS hooper\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     target,\n     claim,\n     double,\n     forte,\n     salon\n|> AGGREGATE\n     AVG(target) AS layout,\n     MAX(double) AS glider,\n     MAX(forte) AS winter\n|> AGGREGATE\n     SUM(winter) AS cocoa,\n     MIN(winter) AS rebel,\n     COUNT(winter) AS cuckoo\n   GROUP BY\n     layout,\n     glider\n|> AGGREGATE\n     COUNT(cuckoo) AS panic,\n     MAX(cuckoo) AS pledge,\n     AVG(cuckoo) AS remedy\n   GROUP BY\n     mister,\n     cocoa,\n     rebel\n|> AGGREGATE\n     SUM(pledge) AS shock,\n     COUNT(remedy) AS carpet,\n     MAX(pledge) AS hooper\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"mister",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    union,\n    human,\n    weber,\n    bobby,\n    burger\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    union,\n    human,\n    weber\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(human) AS venue,\n    MAX(human) AS result,\n    MIN(human) AS walnut,\n    COUNT(human) AS centre,\n    AVG(weber) AS moray,\n    AVG(human) AS music,\n    SUM(weber) AS sweet,\n    MAX(weber) AS orbit\n  FROM cte_1\n  GROUP BY\n    union,\n    mosaic\n),\ncte_3 AS (\n  SELECT\n    COUNT(sweet) AS danger,\n    MIN(music) AS decree\n  FROM cte_2\n  GROUP BY\n    venue,\n    result\n),\ncte_4 AS (\n  SELECT\n    AVG(danger) AS thumb\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     union,\n     human,\n     weber,\n     bobby,\n     burger\n|> SELECT\n     union,\n     human,\n     weber\n|> AGGREGATE\n     SUM(human) AS venue,\n     MAX(human) AS result,\n     MIN(human) AS walnut,\n     COUNT(human) AS centre,\n     AVG(weber) AS moray,\n     AVG(human) AS music,\n     SUM(weber) AS sweet,\n     MAX(weber) AS orbit\n   GROUP BY\n     union,\n     mosaic\n|> AGGREGATE\n     COUNT(sweet) AS danger,\n     MIN(music) AS decree\n   GROUP BY\n     venue,\n     result\n|> AGGREGATE\n     AVG(danger) AS thumb\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"mosaic",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    museum,\n    ordeal,\n    lobby,\n    claret,\n    paper\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(paper) AS metro,\n    COUNT(paper) AS synod,\n    MIN(paper) AS worry,\n    SUM(paper) AS homage\n  FROM cte_0\n  GROUP BY\n    museum,\n    ordeal,\n    lobby,\n    claret\n),\ncte_2 AS (\n  SELECT\n    SUM(synod) AS relic\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(relic) AS friend,\n    SUM(relic) AS watch,\n    AVG(relic) AS rider\n  FROM cte_2\n  GROUP BY\n    libel\n),\ncte_4 AS (\n  SELECT\n    MIN(friend) AS range,\n    MIN(watch) AS falcon,\n    AVG(friend) AS torque,\n    SUM(friend) AS elite\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     museum,\n     ordeal,\n     lobby,\n     claret,\n     paper\n|> AGGREGATE\n     AVG(paper) AS metro,\n     COUNT(paper) AS synod,\n     MIN(paper) AS worry,\n     SUM(paper) AS homage\n   GROUP BY\n     museum,\n     ordeal,\n     lobby,\n     claret\n|> AGGREGATE\n     SUM(synod) AS relic\n|> AGGREGATE\n     COUNT(relic) AS friend,\n     SUM(relic) AS watch,\n     AVG(relic) AS rider\n   GROUP BY\n     libel\n|> AGGREGATE\n     MIN(friend) AS range,\n     MIN(watch) AS falcon,\n     AVG(friend) AS torque,\n     SUM(friend) AS elite\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"libel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    thrust,\n    backup,\n    amount,\n    toxin,\n    daisy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    thrust,\n    backup\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(backup) AS heath,\n    SUM(backup) AS grease,\n    MIN(backup) AS garvey,\n    MAX(backup) AS module\n  FROM cte_1\n  GROUP BY\n    thrust\n),\ncte_3 AS (\n  SELECT\n    SUM(module) AS match,\n    MAX(module) AS filth,\n    AVG(module) AS assent\n  FROM cte_2\n  GROUP BY\n    heath,\n    grease,\n    garvey,\n    parrot\n),\ncte_4 AS (\n  SELECT\n    match,\n    SUM(filth) AS phrase,\n    MAX(filth) AS ounce,\n    MAX(assent) AS famine\n  FROM cte_3\n  GROUP BY\n    match\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     thrust,\n     backup,\n     amount,\n     toxin,\n     daisy\n|> SELECT\n     thrust,\n     backup\n|> AGGREGATE\n     COUNT(backup) AS heath,\n     SUM(backup) AS grease,\n     MIN(backup) AS garvey,\n     MAX(backup) AS module\n   GROUP BY\n     thrust\n|> AGGREGATE\n     SUM(module) AS match,\n     MAX(module) AS filth,\n     AVG(module) AS assent\n   GROUP BY\n     heath,\n     grease,\n     garvey,\n     parrot\n|> AGGREGATE\n     SUM(filth) AS phrase,\n     MAX(filth) AS ounce,\n     MAX(assent) AS famine\n   GROUP BY\n     match\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"parrot",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    width,\n    spite,\n    powder,\n    reform,\n    prayer\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(prayer) AS clive,\n    MIN(prayer) AS remand,\n    AVG(prayer) AS burial\n  FROM cte_0\n  GROUP BY\n    width,\n    spite\n),\ncte_2 AS (\n  SELECT\n    SUM(remand) AS blaze,\n    COUNT(remand) AS ration\n  FROM cte_1\n  GROUP BY\n    clive\n),\ncte_3 AS (\n  SELECT\n    MAX(ration) AS myriad,\n    SUM(ration) AS warren,\n    COUNT(ration) AS count\n  FROM cte_2\n  GROUP BY\n    blaze,\n    dance\n),\ncte_4 AS (\n  SELECT\n    myriad,\n    warren,\n    MIN(count) AS hammer,\n    MAX(count) AS potter\n  FROM cte_3\n  GROUP BY\n    myriad,\n    warren\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     width,\n     spite,\n     powder,\n     reform,\n     prayer\n|> AGGREGATE\n     SUM(prayer) AS clive,\n     MIN(prayer) AS remand,\n     AVG(prayer) AS burial\n   GROUP BY\n     width,\n     spite\n|> AGGREGATE\n     SUM(remand) AS blaze,\n     COUNT(remand) AS ration\n   GROUP BY\n     clive\n|> AGGREGATE\n     MAX(ration) AS myriad,\n     SUM(ration) AS warren,\n     COUNT(ration) AS count\n   GROUP BY\n     blaze,\n     dance\n|> AGGREGATE\n     MIN(count) AS hammer,\n     MAX(count) AS potter\n   GROUP BY\n     myriad,\n     warren\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"dance",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    queue,\n    rector,\n    excise,\n    polish,\n    blanc\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(blanc) AS daphne,\n    MIN(queue) AS humour,\n    MAX(excise) AS stick,\n    COUNT(rector) AS camera\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(stick) AS flora,\n    MAX(camera) AS prison,\n    SUM(camera) AS rover,\n    AVG(stick) AS spoon,\n    COUNT(stick) AS salmon\n  FROM cte_1\n  GROUP BY\n    daphne,\n    humour,\n    degree\n),\ncte_3 AS (\n  SELECT\n    flora,\n    prison,\n    rover,\n    spoon,\n    salmon\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    flora,\n    prison,\n    rover\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     queue,\n     rector,\n     excise,\n     polish,\n     blanc\n|> AGGREGATE\n     MIN(blanc) AS daphne,\n     MIN(queue) AS humour,\n     MAX(excise) AS stick,\n     COUNT(rector) AS camera\n|> AGGREGATE\n     SUM(stick) AS flora,\n     MAX(camera) AS prison,\n     SUM(camera) AS rover,\n     AVG(stick) AS spoon,\n     COUNT(stick) AS salmon\n   GROUP BY\n     daphne,\n     humour,\n     degree\n|> SELECT\n     flora,\n     prison,\n     rover,\n     spoon,\n     salmon\n|> SELECT\n     flora,\n     prison,\n     rover\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"degree",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    prior,\n    crook,\n    slate,\n    bloom,\n    alert\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(slate) AS spirit,\n    MIN(slate) AS platt\n  FROM cte_0\n  GROUP BY\n    prior\n),\ncte_2 AS (\n  SELECT\n    COUNT(platt) AS gamble,\n    MAX(platt) AS sodium,\n    MIN(platt) AS chuck\n  FROM cte_1\n  GROUP BY\n    spirit\n),\ncte_3 AS (\n  SELECT\n    MIN(sodium) AS bullet,\n    COUNT(chuck) AS plague,\n    SUM(chuck) AS blame,\n    MIN(chuck) AS charm\n  FROM cte_2\n  GROUP BY\n    thrust,\n    gamble\n),\ncte_4 AS (\n  SELECT\n    bullet,\n    SUM(charm) AS hamlet,\n    MAX(charm) AS vendor\n  FROM cte_3\n  GROUP BY\n    bullet,\n    plague\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     prior,\n     crook,\n     slate,\n     bloom,\n     alert\n|> AGGREGATE\n     AVG(slate) AS spirit,\n     MIN(slate) AS platt\n   GROUP BY\n     prior\n|> AGGREGATE\n     COUNT(platt) AS gamble,\n     MAX(platt) AS sodium,\n     MIN(platt) AS chuck\n   GROUP BY\n     spirit\n|> AGGREGATE\n     MIN(sodium) AS bullet,\n     COUNT(chuck) AS plague,\n     SUM(chuck) AS blame,\n     MIN(chuck) AS charm\n   GROUP BY\n     thrust,\n     gamble\n|> AGGREGATE\n     SUM(charm) AS hamlet,\n     MAX(charm) AS vendor\n   GROUP BY\n     bullet,\n     plague\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"thrust",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    black,\n    umpire,\n    middle,\n    single,\n    remand\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(black) AS income,\n    AVG(black) AS keeper,\n    MIN(remand) AS daddy,\n    AVG(remand) AS scale,\n    COUNT(single) AS parent,\n    COUNT(remand) AS trader,\n    MIN(single) AS resin\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(trader) AS reflex,\n    MIN(parent) AS circus\n  FROM cte_1\n  GROUP BY\n    income,\n    keeper,\n    phase\n),\ncte_3 AS (\n  SELECT\n    reflex,\n    circus\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    reflex,\n    SUM(circus) AS ruler,\n    AVG(circus) AS regret\n  FROM cte_3\n  GROUP BY\n    reflex\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     black,\n     umpire,\n     middle,\n     single,\n     remand\n|> AGGREGATE\n     MIN(black) AS income,\n     AVG(black) AS keeper,\n     MIN(remand) AS daddy,\n     AVG(remand) AS scale,\n     COUNT(single) AS parent,\n     COUNT(remand) AS trader,\n     MIN(single) AS resin\n|> AGGREGATE\n     MAX(trader) AS reflex,\n     MIN(parent) AS circus\n   GROUP BY\n     income,\n     keeper,\n     phase\n|> SELECT\n     reflex,\n     circus\n|> AGGREGATE\n     SUM(circus) AS ruler,\n     AVG(circus) AS regret\n   GROUP BY\n     reflex\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"phase",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    batty,\n    chord,\n    ascot,\n    robin,\n    pellet\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(chord) AS rubber,\n    COUNT(ascot) AS shower\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(shower) AS sharp,\n    MAX(shower) AS laurel,\n    COUNT(rubber) AS plight,\n    SUM(shower) AS family,\n    AVG(shower) AS breath,\n    SUM(rubber) AS matey,\n    MAX(rubber) AS accord,\n    MIN(rubber) AS seaman\n  FROM cte_1\n  GROUP BY\n    chick\n),\ncte_3 AS (\n  SELECT\n    sharp,\n    laurel,\n    plight,\n    family,\n    breath\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(family) AS ethel\n  FROM cte_3\n  GROUP BY\n    sharp\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     batty,\n     chord,\n     ascot,\n     robin,\n     pellet\n|> AGGREGATE\n     SUM(chord) AS rubber,\n     COUNT(ascot) AS shower\n|> AGGREGATE\n     MIN(shower) AS sharp,\n     MAX(shower) AS laurel,\n     COUNT(rubber) AS plight,\n     SUM(shower) AS family,\n     AVG(shower) AS breath,\n     SUM(rubber) AS matey,\n     MAX(rubber) AS accord,\n     MIN(rubber) AS seaman\n   GROUP BY\n     chick\n|> SELECT\n     sharp,\n     laurel,\n     plight,\n     family,\n     breath\n|> AGGREGATE\n     SUM(family) AS ethel\n   GROUP BY\n     sharp\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"chick",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    dollar,\n    stool,\n    ethel,\n    alert,\n    rhino\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(alert) AS crisis\n  FROM cte_0\n  GROUP BY\n    dollar,\n    stool\n),\ncte_2 AS (\n  SELECT\n    SUM(crisis) AS hogan,\n    MAX(crisis) AS timer,\n    MIN(crisis) AS jetty,\n    AVG(crisis) AS worry,\n    COUNT(crisis) AS excise\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(worry) AS height,\n    MAX(jetty) AS market\n  FROM cte_2\n  GROUP BY\n    hogan,\n    timer,\n    report\n),\ncte_4 AS (\n  SELECT\n    height,\n    MAX(market) AS hatred,\n    SUM(market) AS thumb,\n    AVG(market) AS winner\n  FROM cte_3\n  GROUP BY\n    height\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     dollar,\n     stool,\n     ethel,\n     alert,\n     rhino\n|> AGGREGATE\n     MIN(alert) AS crisis\n   GROUP BY\n     dollar,\n     stool\n|> AGGREGATE\n     SUM(crisis) AS hogan,\n     MAX(crisis) AS timer,\n     MIN(crisis) AS jetty,\n     AVG(crisis) AS worry,\n     COUNT(crisis) AS excise\n|> AGGREGATE\n     AVG(worry) AS height,\n     MAX(jetty) AS market\n   GROUP BY\n     hogan,\n     timer,\n     report\n|> AGGREGATE\n     MAX(market) AS hatred,\n     SUM(market) AS thumb,\n     AVG(market) AS winner\n   GROUP BY\n     height\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"report",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    kelly,\n    lunch,\n    rigour,\n    smith,\n    bonus\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(smith) AS badger,\n    MIN(bonus) AS ration,\n    COUNT(smith) AS hostel,\n    SUM(smith) AS handle,\n    COUNT(bonus) AS spell\n  FROM cte_0\n  GROUP BY\n    kelly,\n    lunch,\n    rigour\n),\ncte_2 AS (\n  SELECT\n    COUNT(handle) AS search,\n    COUNT(spell) AS blitz,\n    AVG(spell) AS stench\n  FROM cte_1\n  GROUP BY\n    badger,\n    sailor,\n    ration,\n    hostel\n),\ncte_3 AS (\n  SELECT\n    search,\n    MAX(stench) AS murphy\n  FROM cte_2\n  GROUP BY\n    search\n),\ncte_4 AS (\n  SELECT\n    COUNT(murphy) AS esteem\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     kelly,\n     lunch,\n     rigour,\n     smith,\n     bonus\n|> AGGREGATE\n     MIN(smith) AS badger,\n     MIN(bonus) AS ration,\n     COUNT(smith) AS hostel,\n     SUM(smith) AS handle,\n     COUNT(bonus) AS spell\n   GROUP BY\n     kelly,\n     lunch,\n     rigour\n|> AGGREGATE\n     COUNT(handle) AS search,\n     COUNT(spell) AS blitz,\n     AVG(spell) AS stench\n   GROUP BY\n     badger,\n     sailor,\n     ration,\n     hostel\n|> AGGREGATE\n     MAX(stench) AS murphy\n   GROUP BY\n     search\n|> AGGREGATE\n     COUNT(murphy) AS esteem\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"sailor",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    cocoa,\n    depot,\n    shine,\n    tally,\n    spire\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(tally) AS chain,\n    SUM(depot) AS beech\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(beech) AS dance,\n    COUNT(chain) AS sudden,\n    AVG(chain) AS temper,\n    MAX(chain) AS heroin,\n    MIN(chain) AS villa,\n    AVG(beech) AS track,\n    COUNT(beech) AS carol,\n    MAX(beech) AS ballet\n  FROM cte_1\n  GROUP BY\n    bullet\n),\ncte_3 AS (\n  SELECT\n    dance,\n    sudden\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(dance) AS summit,\n    SUM(sudden) AS laura,\n    MIN(dance) AS batty\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     cocoa,\n     depot,\n     shine,\n     tally,\n     spire\n|> AGGREGATE\n     MIN(tally) AS chain,\n     SUM(depot) AS beech\n|> AGGREGATE\n     SUM(beech) AS dance,\n     COUNT(chain) AS sudden,\n     AVG(chain) AS temper,\n     MAX(chain) AS heroin,\n     MIN(chain) AS villa,\n     AVG(beech) AS track,\n     COUNT(beech) AS carol,\n     MAX(beech) AS ballet\n   GROUP BY\n     bullet\n|> SELECT\n     dance,\n     sudden\n|> AGGREGATE\n     SUM(dance) AS summit,\n     SUM(sudden) AS laura,\n     MIN(dance) AS batty\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"bullet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    spread,\n    castle,\n    bullet,\n    plasma,\n    giant\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    spread,\n    castle,\n    bullet,\n    plasma\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    spread,\n    castle\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(castle) AS waste,\n    MAX(spread) AS robot,\n    MAX(castle) AS joseph,\n    SUM(castle) AS basil,\n    MIN(castle) AS slater,\n    COUNT(castle) AS plank,\n    MIN(spread) AS server\n  FROM cte_2\n  GROUP BY\n    median\n),\ncte_4 AS (\n  SELECT\n    waste,\n    COUNT(server) AS trifle,\n    COUNT(slater) AS hurdle\n  FROM cte_3\n  GROUP BY\n    waste,\n    robot,\n    joseph\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     spread,\n     castle,\n     bullet,\n     plasma,\n     giant\n|> SELECT\n     spread,\n     castle,\n     bullet,\n     plasma\n|> SELECT\n     spread,\n     castle\n|> AGGREGATE\n     AVG(castle) AS waste,\n     MAX(spread) AS robot,\n     MAX(castle) AS joseph,\n     SUM(castle) AS basil,\n     MIN(castle) AS slater,\n     COUNT(castle) AS plank,\n     MIN(spread) AS server\n   GROUP BY\n     median\n|> AGGREGATE\n     COUNT(server) AS trifle,\n     COUNT(slater) AS hurdle\n   GROUP BY\n     waste,\n     robot,\n     joseph\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"median",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    martin,\n    steel,\n    serial,\n    state,\n    nanny\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    martin,\n    steel,\n    serial,\n    state\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(martin) AS wreck,\n    AVG(steel) AS schema,\n    MAX(state) AS bypass,\n    SUM(steel) AS metal,\n    MAX(steel) AS bronze,\n    COUNT(serial) AS barge\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(bronze) AS uproar,\n    SUM(metal) AS oscar\n  FROM cte_2\n  GROUP BY\n    velvet,\n    wreck,\n    schema,\n    bypass\n),\ncte_4 AS (\n  SELECT\n    SUM(oscar) AS tally,\n    COUNT(oscar) AS prince\n  FROM cte_3\n  GROUP BY\n    uproar\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     martin,\n     steel,\n     serial,\n     state,\n     nanny\n|> SELECT\n     martin,\n     steel,\n     serial,\n     state\n|> AGGREGATE\n     MAX(martin) AS wreck,\n     AVG(steel) AS schema,\n     MAX(state) AS bypass,\n     SUM(steel) AS metal,\n     MAX(steel) AS bronze,\n     COUNT(serial) AS barge\n|> AGGREGATE\n     MAX(bronze) AS uproar,\n     SUM(metal) AS oscar\n   GROUP BY\n     velvet,\n     wreck,\n     schema,\n     bypass\n|> AGGREGATE\n     SUM(oscar) AS tally,\n     COUNT(oscar) AS prince\n   GROUP BY\n     uproar\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"velvet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    frost,\n    growth,\n    canoe,\n    bosom,\n    pencil\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    frost,\n    growth,\n    canoe,\n    bosom,\n    pencil\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(pencil) AS sigma,\n    SUM(bosom) AS grief,\n    MAX(bosom) AS jacket,\n    MIN(canoe) AS sheikh\n  FROM cte_1\n  GROUP BY\n    frost,\n    growth\n),\ncte_3 AS (\n  SELECT\n    AVG(sheikh) AS malice\n  FROM cte_2\n  GROUP BY\n    sigma,\n    grief,\n    jacket,\n    remand\n),\ncte_4 AS (\n  SELECT\n    MAX(malice) AS vessel,\n    AVG(malice) AS strat,\n    COUNT(malice) AS cooper,\n    SUM(malice) AS smear\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     frost,\n     growth,\n     canoe,\n     bosom,\n     pencil\n|> SELECT\n     frost,\n     growth,\n     canoe,\n     bosom,\n     pencil\n|> AGGREGATE\n     SUM(pencil) AS sigma,\n     SUM(bosom) AS grief,\n     MAX(bosom) AS jacket,\n     MIN(canoe) AS sheikh\n   GROUP BY\n     frost,\n     growth\n|> AGGREGATE\n     AVG(sheikh) AS malice\n   GROUP BY\n     sigma,\n     grief,\n     jacket,\n     remand\n|> AGGREGATE\n     MAX(malice) AS vessel,\n     AVG(malice) AS strat,\n     COUNT(malice) AS cooper,\n     SUM(malice) AS smear\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"remand",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:28},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    idiot,\n    lorry,\n    attack,\n    tommy,\n    jones\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(jones) AS expert,\n    COUNT(attack) AS madame,\n    SUM(attack) AS radio,\n    AVG(attack) AS regime,\n    COUNT(tommy) AS saddle,\n    MAX(attack) AS guide\n  FROM cte_0\n  GROUP BY\n    idiot,\n    lorry\n),\ncte_2 AS (\n  SELECT\n    MAX(regime) AS stile,\n    SUM(regime) AS mason\n  FROM cte_1\n  GROUP BY\n    expert,\n    madame,\n    volume,\n    radio\n),\ncte_3 AS (\n  SELECT\n    stile,\n    mason\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(mason) AS tomato,\n    COUNT(mason) AS straw\n  FROM cte_3\n  GROUP BY\n    stile\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     idiot,\n     lorry,\n     attack,\n     tommy,\n     jones\n|> AGGREGATE\n     MAX(jones) AS expert,\n     COUNT(attack) AS madame,\n     SUM(attack) AS radio,\n     AVG(attack) AS regime,\n     COUNT(tommy) AS saddle,\n     MAX(attack) AS guide\n   GROUP BY\n     idiot,\n     lorry\n|> AGGREGATE\n     MAX(regime) AS stile,\n     SUM(regime) AS mason\n   GROUP BY\n     expert,\n     madame,\n     volume,\n     radio\n|> SELECT\n     stile,\n     mason\n|> AGGREGATE\n     SUM(mason) AS tomato,\n     COUNT(mason) AS straw\n   GROUP BY\n     stile\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"volume",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    clinic,\n    deity,\n    strike,\n    chant,\n    jessie\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(chant) AS farce,\n    MAX(chant) AS killer,\n    AVG(chant) AS cheek\n  FROM cte_0\n  GROUP BY\n    clinic,\n    deity\n),\ncte_2 AS (\n  SELECT\n    AVG(cheek) AS blast,\n    MAX(killer) AS cavity,\n    MIN(killer) AS holly,\n    AVG(killer) AS credit,\n    MAX(cheek) AS morgan,\n    MAX(farce) AS spell\n  FROM cte_1\n  GROUP BY\n    guido\n),\ncte_3 AS (\n  SELECT\n    blast,\n    cavity,\n    holly,\n    credit\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(blast) AS keeper,\n    MIN(holly) AS victor\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     clinic,\n     deity,\n     strike,\n     chant,\n     jessie\n|> AGGREGATE\n     COUNT(chant) AS farce,\n     MAX(chant) AS killer,\n     AVG(chant) AS cheek\n   GROUP BY\n     clinic,\n     deity\n|> AGGREGATE\n     AVG(cheek) AS blast,\n     MAX(killer) AS cavity,\n     MIN(killer) AS holly,\n     AVG(killer) AS credit,\n     MAX(cheek) AS morgan,\n     MAX(farce) AS spell\n   GROUP BY\n     guido\n|> SELECT\n     blast,\n     cavity,\n     holly,\n     credit\n|> AGGREGATE\n     MIN(blast) AS keeper,\n     MIN(holly) AS victor\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"guido",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    wallet,\n    county,\n    lunch,\n    clint,\n    father\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(county) AS trust,\n    MAX(father) AS cider,\n    AVG(clint) AS lagoon,\n    MIN(lunch) AS thief\n  FROM cte_0\n  GROUP BY\n    wallet\n),\ncte_2 AS (\n  SELECT\n    SUM(thief) AS strike,\n    MAX(cider) AS nerve,\n    MIN(cider) AS demon,\n    SUM(lagoon) AS airbus,\n    COUNT(cider) AS convoy\n  FROM cte_1\n  GROUP BY\n    trust,\n    beech\n),\ncte_3 AS (\n  SELECT\n    MAX(demon) AS tramp,\n    COUNT(convoy) AS sphere,\n    COUNT(nerve) AS bowie\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    tramp,\n    sphere\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     wallet,\n     county,\n     lunch,\n     clint,\n     father\n|> AGGREGATE\n     AVG(county) AS trust,\n     MAX(father) AS cider,\n     AVG(clint) AS lagoon,\n     MIN(lunch) AS thief\n   GROUP BY\n     wallet\n|> AGGREGATE\n     SUM(thief) AS strike,\n     MAX(cider) AS nerve,\n     MIN(cider) AS demon,\n     SUM(lagoon) AS airbus,\n     COUNT(cider) AS convoy\n   GROUP BY\n     trust,\n     beech\n|> AGGREGATE\n     MAX(demon) AS tramp,\n     COUNT(convoy) AS sphere,\n     COUNT(nerve) AS bowie\n|> SELECT\n     tramp,\n     sphere\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"beech",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    viewer,\n    guitar,\n    habit,\n    angel,\n    winner\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    viewer,\n    guitar,\n    habit,\n    angel\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(habit) AS talent,\n    MAX(angel) AS train,\n    SUM(angel) AS esteem\n  FROM cte_1\n  GROUP BY\n    viewer,\n    guitar\n),\ncte_3 AS (\n  SELECT\n    SUM(esteem) AS singer,\n    MAX(esteem) AS hogan,\n    COUNT(esteem) AS plaque\n  FROM cte_2\n  GROUP BY\n    talent,\n    train,\n    clone\n),\ncte_4 AS (\n  SELECT\n    singer,\n    AVG(hogan) AS refuge,\n    SUM(plaque) AS outing,\n    MAX(plaque) AS collor\n  FROM cte_3\n  GROUP BY\n    singer\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     viewer,\n     guitar,\n     habit,\n     angel,\n     winner\n|> SELECT\n     viewer,\n     guitar,\n     habit,\n     angel\n|> AGGREGATE\n     COUNT(habit) AS talent,\n     MAX(angel) AS train,\n     SUM(angel) AS esteem\n   GROUP BY\n     viewer,\n     guitar\n|> AGGREGATE\n     SUM(esteem) AS singer,\n     MAX(esteem) AS hogan,\n     COUNT(esteem) AS plaque\n   GROUP BY\n     talent,\n     train,\n     clone\n|> AGGREGATE\n     AVG(hogan) AS refuge,\n     SUM(plaque) AS outing,\n     MAX(plaque) AS collor\n   GROUP BY\n     singer\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"clone",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:27},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    dieter,\n    theory,\n    rebate,\n    choir,\n    archie\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(archie) AS daily,\n    SUM(archie) AS gender,\n    AVG(choir) AS forge,\n    COUNT(choir) AS regard\n  FROM cte_0\n  GROUP BY\n    dieter,\n    theory,\n    rebate\n),\ncte_2 AS (\n  SELECT\n    COUNT(regard) AS notice,\n    MAX(regard) AS bertha,\n    SUM(regard) AS shrug\n  FROM cte_1\n  GROUP BY\n    daily,\n    gender,\n    forge,\n    sheikh\n),\ncte_3 AS (\n  SELECT\n    COUNT(shrug) AS hooper\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(hooper) AS abuse,\n    AVG(hooper) AS delta,\n    MIN(hooper) AS summer\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     dieter,\n     theory,\n     rebate,\n     choir,\n     archie\n|> AGGREGATE\n     AVG(archie) AS daily,\n     SUM(archie) AS gender,\n     AVG(choir) AS forge,\n     COUNT(choir) AS regard\n   GROUP BY\n     dieter,\n     theory,\n     rebate\n|> AGGREGATE\n     COUNT(regard) AS notice,\n     MAX(regard) AS bertha,\n     SUM(regard) AS shrug\n   GROUP BY\n     daily,\n     gender,\n     forge,\n     sheikh\n|> AGGREGATE\n     COUNT(shrug) AS hooper\n|> AGGREGATE\n     MAX(hooper) AS abuse,\n     AVG(hooper) AS delta,\n     MIN(hooper) AS summer\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"sheikh",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    groove,\n    glare,\n    shield,\n    parry,\n    crack\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(glare) AS poster,\n    COUNT(shield) AS niche,\n    SUM(glare) AS mandy,\n    COUNT(crack) AS winner,\n    COUNT(parry) AS alpha\n  FROM cte_0\n  GROUP BY\n    groove\n),\ncte_2 AS (\n  SELECT\n    SUM(winner) AS empire,\n    COUNT(winner) AS debit\n  FROM cte_1\n  GROUP BY\n    poster,\n    niche,\n    mandy\n),\ncte_3 AS (\n  SELECT\n    AVG(debit) AS slogan\n  FROM cte_2\n  GROUP BY\n    spiral\n),\ncte_4 AS (\n  SELECT\n    AVG(slogan) AS pillar,\n    SUM(slogan) AS laser,\n    COUNT(slogan) AS liquid,\n    MIN(slogan) AS clinic\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     groove,\n     glare,\n     shield,\n     parry,\n     crack\n|> AGGREGATE\n     COUNT(glare) AS poster,\n     COUNT(shield) AS niche,\n     SUM(glare) AS mandy,\n     COUNT(crack) AS winner,\n     COUNT(parry) AS alpha\n   GROUP BY\n     groove\n|> AGGREGATE\n     SUM(winner) AS empire,\n     COUNT(winner) AS debit\n   GROUP BY\n     poster,\n     niche,\n     mandy\n|> AGGREGATE\n     AVG(debit) AS slogan\n   GROUP BY\n     spiral\n|> AGGREGATE\n     AVG(slogan) AS pillar,\n     SUM(slogan) AS laser,\n     COUNT(slogan) AS liquid,\n     MIN(slogan) AS clinic\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"spiral",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    pasta,\n    dread,\n    pulpit,\n    remand,\n    syrup\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    pasta,\n    dread,\n    pulpit\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(pasta) AS tyler,\n    SUM(dread) AS mutiny,\n    MIN(pasta) AS misery,\n    COUNT(pulpit) AS candy,\n    MIN(dread) AS essay,\n    SUM(pasta) AS umpire\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MIN(candy) AS appeal,\n    SUM(umpire) AS breed\n  FROM cte_2\n  GROUP BY\n    tyler,\n    hinge,\n    mutiny\n),\ncte_4 AS (\n  SELECT\n    MAX(breed) AS almond,\n    COUNT(breed) AS screen,\n    AVG(breed) AS smack\n  FROM cte_3\n  GROUP BY\n    appeal\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     pasta,\n     dread,\n     pulpit,\n     remand,\n     syrup\n|> SELECT\n     pasta,\n     dread,\n     pulpit\n|> AGGREGATE\n     MAX(pasta) AS tyler,\n     SUM(dread) AS mutiny,\n     MIN(pasta) AS misery,\n     COUNT(pulpit) AS candy,\n     MIN(dread) AS essay,\n     SUM(pasta) AS umpire\n|> AGGREGATE\n     MIN(candy) AS appeal,\n     SUM(umpire) AS breed\n   GROUP BY\n     tyler,\n     hinge,\n     mutiny\n|> AGGREGATE\n     MAX(breed) AS almond,\n     COUNT(breed) AS screen,\n     AVG(breed) AS smack\n   GROUP BY\n     appeal\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"hinge",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    client,\n    press,\n    future,\n    friar,\n    jersey\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    client,\n    press,\n    future\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(client) AS lewis,\n    SUM(client) AS summit,\n    AVG(future) AS condom\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MIN(condom) AS paste,\n    AVG(condom) AS breed,\n    COUNT(condom) AS torch,\n    SUM(condom) AS mutant,\n    MAX(condom) AS grand\n  FROM cte_2\n  GROUP BY\n    lewis,\n    prison,\n    summit\n),\ncte_4 AS (\n  SELECT\n    paste,\n    breed,\n    COUNT(mutant) AS modem\n  FROM cte_3\n  GROUP BY\n    paste,\n    breed,\n    torch\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     client,\n     press,\n     future,\n     friar,\n     jersey\n|> SELECT\n     client,\n     press,\n     future\n|> AGGREGATE\n     COUNT(client) AS lewis,\n     SUM(client) AS summit,\n     AVG(future) AS condom\n|> AGGREGATE\n     MIN(condom) AS paste,\n     AVG(condom) AS breed,\n     COUNT(condom) AS torch,\n     SUM(condom) AS mutant,\n     MAX(condom) AS grand\n   GROUP BY\n     lewis,\n     prison,\n     summit\n|> AGGREGATE\n     COUNT(mutant) AS modem\n   GROUP BY\n     paste,\n     breed,\n     torch\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"prison",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    artist,\n    splash,\n    staple,\n    heroin,\n    bobby\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(heroin) AS shrug,\n    MIN(bobby) AS carpet,\n    MAX(artist) AS cough\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    shrug,\n    carpet,\n    cough\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(cough) AS glance,\n    MIN(cough) AS season,\n    COUNT(cough) AS voice,\n    MAX(cough) AS lunch,\n    AVG(cough) AS siren\n  FROM cte_2\n  GROUP BY\n    shrug,\n    ferry,\n    carpet\n),\ncte_4 AS (\n  SELECT\n    glance,\n    season,\n    MIN(lunch) AS topic\n  FROM cte_3\n  GROUP BY\n    glance,\n    season,\n    voice\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     artist,\n     splash,\n     staple,\n     heroin,\n     bobby\n|> AGGREGATE\n     COUNT(heroin) AS shrug,\n     MIN(bobby) AS carpet,\n     MAX(artist) AS cough\n|> SELECT\n     shrug,\n     carpet,\n     cough\n|> AGGREGATE\n     SUM(cough) AS glance,\n     MIN(cough) AS season,\n     COUNT(cough) AS voice,\n     MAX(cough) AS lunch,\n     AVG(cough) AS siren\n   GROUP BY\n     shrug,\n     ferry,\n     carpet\n|> AGGREGATE\n     MIN(lunch) AS topic\n   GROUP BY\n     glance,\n     season,\n     voice\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"ferry",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    alien,\n    remit,\n    creek,\n    apron,\n    effort\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(effort) AS stint,\n    SUM(apron) AS harem\n  FROM cte_0\n  GROUP BY\n    alien,\n    remit\n),\ncte_2 AS (\n  SELECT\n    AVG(harem) AS prayer,\n    SUM(harem) AS smell,\n    AVG(stint) AS hobby,\n    COUNT(harem) AS stack\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(stack) AS debate,\n    MAX(stack) AS travel,\n    SUM(stack) AS pepper\n  FROM cte_2\n  GROUP BY\n    trout,\n    prayer,\n    smell,\n    hobby\n),\ncte_4 AS (\n  SELECT\n    SUM(travel) AS castle,\n    SUM(pepper) AS sheikh\n  FROM cte_3\n  GROUP BY\n    debate\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     alien,\n     remit,\n     creek,\n     apron,\n     effort\n|> AGGREGATE\n     AVG(effort) AS stint,\n     SUM(apron) AS harem\n   GROUP BY\n     alien,\n     remit\n|> AGGREGATE\n     AVG(harem) AS prayer,\n     SUM(harem) AS smell,\n     AVG(stint) AS hobby,\n     COUNT(harem) AS stack\n|> AGGREGATE\n     AVG(stack) AS debate,\n     MAX(stack) AS travel,\n     SUM(stack) AS pepper\n   GROUP BY\n     trout,\n     prayer,\n     smell,\n     hobby\n|> AGGREGATE\n     SUM(travel) AS castle,\n     SUM(pepper) AS sheikh\n   GROUP BY\n     debate\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"trout",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    trick,\n    christ,\n    shower,\n    catch,\n    beach\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    trick,\n    christ,\n    shower\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(shower) AS laugh,\n    MIN(shower) AS stand,\n    MIN(christ) AS swift,\n    SUM(shower) AS groin,\n    MAX(shower) AS psyche,\n    COUNT(christ) AS caller\n  FROM cte_1\n  GROUP BY\n    trick\n),\ncte_3 AS (\n  SELECT\n    COUNT(caller) AS inside,\n    MIN(psyche) AS flour\n  FROM cte_2\n  GROUP BY\n    brute,\n    laugh,\n    stand\n),\ncte_4 AS (\n  SELECT\n    COUNT(inside) AS texas,\n    MIN(flour) AS idiot,\n    MAX(inside) AS dogma\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     trick,\n     christ,\n     shower,\n     catch,\n     beach\n|> SELECT\n     trick,\n     christ,\n     shower\n|> AGGREGATE\n     AVG(shower) AS laugh,\n     MIN(shower) AS stand,\n     MIN(christ) AS swift,\n     SUM(shower) AS groin,\n     MAX(shower) AS psyche,\n     COUNT(christ) AS caller\n   GROUP BY\n     trick\n|> AGGREGATE\n     COUNT(caller) AS inside,\n     MIN(psyche) AS flour\n   GROUP BY\n     brute,\n     laugh,\n     stand\n|> AGGREGATE\n     COUNT(inside) AS texas,\n     MIN(flour) AS idiot,\n     MAX(inside) AS dogma\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"brute",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    alley,\n    smith,\n    canvas,\n    timber,\n    debut\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(smith) AS disco,\n    AVG(timber) AS basket\n  FROM cte_0\n  GROUP BY\n    alley\n),\ncte_2 AS (\n  SELECT\n    MIN(disco) AS lotion,\n    SUM(basket) AS casino\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(casino) AS groom,\n    SUM(casino) AS johnny,\n    MIN(casino) AS jacket,\n    COUNT(casino) AS vogue,\n    AVG(casino) AS manor\n  FROM cte_2\n  GROUP BY\n    lotion,\n    barrel\n),\ncte_4 AS (\n  SELECT\n    groom,\n    johnny,\n    jacket,\n    MIN(manor) AS burke\n  FROM cte_3\n  GROUP BY\n    groom,\n    johnny,\n    jacket\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     alley,\n     smith,\n     canvas,\n     timber,\n     debut\n|> AGGREGATE\n     MIN(smith) AS disco,\n     AVG(timber) AS basket\n   GROUP BY\n     alley\n|> AGGREGATE\n     MIN(disco) AS lotion,\n     SUM(basket) AS casino\n|> AGGREGATE\n     MAX(casino) AS groom,\n     SUM(casino) AS johnny,\n     MIN(casino) AS jacket,\n     COUNT(casino) AS vogue,\n     AVG(casino) AS manor\n   GROUP BY\n     lotion,\n     barrel\n|> AGGREGATE\n     MIN(manor) AS burke\n   GROUP BY\n     groom,\n     johnny,\n     jacket\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"barrel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tucker,\n    nylon,\n    sample,\n    atlas,\n    amazon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(atlas) AS owner,\n    MIN(nylon) AS coward,\n    MIN(tucker) AS embryo,\n    AVG(tucker) AS biopsy\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    owner,\n    coward,\n    embryo,\n    biopsy\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(biopsy) AS proxy,\n    AVG(biopsy) AS ambush,\n    MIN(biopsy) AS idiom,\n    COUNT(biopsy) AS marsh\n  FROM cte_2\n  GROUP BY\n    bullet,\n    owner,\n    coward,\n    embryo\n),\ncte_4 AS (\n  SELECT\n    COUNT(marsh) AS outing\n  FROM cte_3\n  GROUP BY\n    proxy,\n    ambush,\n    idiom\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tucker,\n     nylon,\n     sample,\n     atlas,\n     amazon\n|> AGGREGATE\n     MIN(atlas) AS owner,\n     MIN(nylon) AS coward,\n     MIN(tucker) AS embryo,\n     AVG(tucker) AS biopsy\n|> SELECT\n     owner,\n     coward,\n     embryo,\n     biopsy\n|> AGGREGATE\n     SUM(biopsy) AS proxy,\n     AVG(biopsy) AS ambush,\n     MIN(biopsy) AS idiom,\n     COUNT(biopsy) AS marsh\n   GROUP BY\n     bullet,\n     owner,\n     coward,\n     embryo\n|> AGGREGATE\n     COUNT(marsh) AS outing\n   GROUP BY\n     proxy,\n     ambush,\n     idiom\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"bullet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    player,\n    mince,\n    trauma,\n    guide,\n    wharf\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(wharf) AS filth\n  FROM cte_0\n  GROUP BY\n    player,\n    mince\n),\ncte_2 AS (\n  SELECT\n    MAX(filth) AS trade,\n    AVG(filth) AS origin,\n    SUM(filth) AS cheese,\n    COUNT(filth) AS danger\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(danger) AS poker,\n    MIN(danger) AS airbus,\n    MIN(cheese) AS umpire,\n    AVG(danger) AS sector\n  FROM cte_2\n  GROUP BY\n    cousin,\n    trade,\n    origin\n),\ncte_4 AS (\n  SELECT\n    poker,\n    airbus,\n    MAX(sector) AS comer\n  FROM cte_3\n  GROUP BY\n    poker,\n    airbus\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     player,\n     mince,\n     trauma,\n     guide,\n     wharf\n|> AGGREGATE\n     AVG(wharf) AS filth\n   GROUP BY\n     player,\n     mince\n|> AGGREGATE\n     MAX(filth) AS trade,\n     AVG(filth) AS origin,\n     SUM(filth) AS cheese,\n     COUNT(filth) AS danger\n|> AGGREGATE\n     SUM(danger) AS poker,\n     MIN(danger) AS airbus,\n     MIN(cheese) AS umpire,\n     AVG(danger) AS sector\n   GROUP BY\n     cousin,\n     trade,\n     origin\n|> AGGREGATE\n     MAX(sector) AS comer\n   GROUP BY\n     poker,\n     airbus\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"cousin",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    chase,\n    ryder,\n    incest,\n    shift,\n    knight\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    chase,\n    ryder,\n    incest\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    chase,\n    ryder,\n    incest\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(ryder) AS malice,\n    MAX(incest) AS insert,\n    MAX(ryder) AS patch,\n    MIN(incest) AS pouch,\n    SUM(ryder) AS delta,\n    AVG(ryder) AS reader,\n    COUNT(incest) AS turkey\n  FROM cte_2\n  GROUP BY\n    smart,\n    chase\n),\ncte_4 AS (\n  SELECT\n    MAX(reader) AS piety,\n    AVG(reader) AS reason\n  FROM cte_3\n  GROUP BY\n    malice,\n    insert,\n    patch\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     chase,\n     ryder,\n     incest,\n     shift,\n     knight\n|> SELECT\n     chase,\n     ryder,\n     incest\n|> SELECT\n     chase,\n     ryder,\n     incest\n|> AGGREGATE\n     COUNT(ryder) AS malice,\n     MAX(incest) AS insert,\n     MAX(ryder) AS patch,\n     MIN(incest) AS pouch,\n     SUM(ryder) AS delta,\n     AVG(ryder) AS reader,\n     COUNT(incest) AS turkey\n   GROUP BY\n     smart,\n     chase\n|> AGGREGATE\n     MAX(reader) AS piety,\n     AVG(reader) AS reason\n   GROUP BY\n     malice,\n     insert,\n     patch\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"smart",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    stump,\n    dexter,\n    jumper,\n    sophie,\n    issue\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(issue) AS video\n  FROM cte_0\n  GROUP BY\n    stump,\n    dexter,\n    jumper,\n    sophie\n),\ncte_2 AS (\n  SELECT\n    COUNT(video) AS buyer,\n    SUM(video) AS panel\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(panel) AS clutch,\n    COUNT(panel) AS packet,\n    MIN(panel) AS bamboo,\n    AVG(panel) AS clump,\n    SUM(panel) AS yellow\n  FROM cte_2\n  GROUP BY\n    thumb,\n    buyer\n),\ncte_4 AS (\n  SELECT\n    clutch,\n    MAX(bamboo) AS series,\n    SUM(bamboo) AS custom\n  FROM cte_3\n  GROUP BY\n    clutch,\n    packet\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     stump,\n     dexter,\n     jumper,\n     sophie,\n     issue\n|> AGGREGATE\n     SUM(issue) AS video\n   GROUP BY\n     stump,\n     dexter,\n     jumper,\n     sophie\n|> AGGREGATE\n     COUNT(video) AS buyer,\n     SUM(video) AS panel\n|> AGGREGATE\n     MAX(panel) AS clutch,\n     COUNT(panel) AS packet,\n     MIN(panel) AS bamboo,\n     AVG(panel) AS clump,\n     SUM(panel) AS yellow\n   GROUP BY\n     thumb,\n     buyer\n|> AGGREGATE\n     MAX(bamboo) AS series,\n     SUM(bamboo) AS custom\n   GROUP BY\n     clutch,\n     packet\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"thumb",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    width,\n    rival,\n    deacon,\n    misuse,\n    amino\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(deacon) AS sudden,\n    SUM(rival) AS county,\n    COUNT(amino) AS chance,\n    SUM(deacon) AS bosom,\n    MIN(amino) AS affect\n  FROM cte_0\n  GROUP BY\n    width\n),\ncte_2 AS (\n  SELECT\n    MAX(bosom) AS merit,\n    COUNT(bosom) AS donor,\n    COUNT(chance) AS poster,\n    COUNT(affect) AS bumper\n  FROM cte_1\n  GROUP BY\n    sudden,\n    spruce,\n    county\n),\ncte_3 AS (\n  SELECT\n    MAX(bumper) AS savage,\n    AVG(donor) AS liner\n  FROM cte_2\n  GROUP BY\n    merit\n),\ncte_4 AS (\n  SELECT\n    savage,\n    liner\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     width,\n     rival,\n     deacon,\n     misuse,\n     amino\n|> AGGREGATE\n     MIN(deacon) AS sudden,\n     SUM(rival) AS county,\n     COUNT(amino) AS chance,\n     SUM(deacon) AS bosom,\n     MIN(amino) AS affect\n   GROUP BY\n     width\n|> AGGREGATE\n     MAX(bosom) AS merit,\n     COUNT(bosom) AS donor,\n     COUNT(chance) AS poster,\n     COUNT(affect) AS bumper\n   GROUP BY\n     sudden,\n     spruce,\n     county\n|> AGGREGATE\n     MAX(bumper) AS savage,\n     AVG(donor) AS liner\n   GROUP BY\n     merit\n|> SELECT\n     savage,\n     liner\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"spruce",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    person,\n    laird,\n    spray,\n    object,\n    desert\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(desert) AS defect,\n    MIN(object) AS group,\n    SUM(laird) AS facade,\n    AVG(object) AS plague\n  FROM cte_0\n  GROUP BY\n    person\n),\ncte_2 AS (\n  SELECT\n    COUNT(plague) AS scrub,\n    SUM(plague) AS torch,\n    MAX(plague) AS asthma,\n    MIN(plague) AS price\n  FROM cte_1\n  GROUP BY\n    defect,\n    group,\n    facade,\n    chick\n),\ncte_3 AS (\n  SELECT\n    MAX(torch) AS volley\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(volley) AS grand,\n    MIN(volley) AS energy,\n    SUM(volley) AS patron\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     person,\n     laird,\n     spray,\n     object,\n     desert\n|> AGGREGATE\n     SUM(desert) AS defect,\n     MIN(object) AS group,\n     SUM(laird) AS facade,\n     AVG(object) AS plague\n   GROUP BY\n     person\n|> AGGREGATE\n     COUNT(plague) AS scrub,\n     SUM(plague) AS torch,\n     MAX(plague) AS asthma,\n     MIN(plague) AS price\n   GROUP BY\n     defect,\n     group,\n     facade,\n     chick\n|> AGGREGATE\n     MAX(torch) AS volley\n|> AGGREGATE\n     COUNT(volley) AS grand,\n     MIN(volley) AS energy,\n     SUM(volley) AS patron\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"chick",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    jersey,\n    secret,\n    policy,\n    unease,\n    piety\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(secret) AS avail,\n    AVG(policy) AS german,\n    MAX(policy) AS coast,\n    AVG(jersey) AS melody\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(melody) AS unrest,\n    MAX(melody) AS ulcer,\n    MIN(melody) AS kettle,\n    COUNT(melody) AS money,\n    AVG(melody) AS quarry\n  FROM cte_1\n  GROUP BY\n    avail,\n    german,\n    penis,\n    coast\n),\ncte_3 AS (\n  SELECT\n    MAX(kettle) AS influx\n  FROM cte_2\n  GROUP BY\n    unrest\n),\ncte_4 AS (\n  SELECT\n    MIN(influx) AS series,\n    AVG(influx) AS shine\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     jersey,\n     secret,\n     policy,\n     unease,\n     piety\n|> AGGREGATE\n     MIN(secret) AS avail,\n     AVG(policy) AS german,\n     MAX(policy) AS coast,\n     AVG(jersey) AS melody\n|> AGGREGATE\n     SUM(melody) AS unrest,\n     MAX(melody) AS ulcer,\n     MIN(melody) AS kettle,\n     COUNT(melody) AS money,\n     AVG(melody) AS quarry\n   GROUP BY\n     avail,\n     german,\n     penis,\n     coast\n|> AGGREGATE\n     MAX(kettle) AS influx\n   GROUP BY\n     unrest\n|> AGGREGATE\n     MIN(influx) AS series,\n     AVG(influx) AS shine\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"penis",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    abbot,\n    touch,\n    career,\n    carbon,\n    artist\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    abbot,\n    touch\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(touch) AS mother,\n    AVG(touch) AS tandem,\n    SUM(touch) AS custom\n  FROM cte_1\n  GROUP BY\n    abbot\n),\ncte_3 AS (\n  SELECT\n    MIN(custom) AS henry,\n    MAX(custom) AS growth,\n    AVG(custom) AS spence,\n    COUNT(custom) AS fabric,\n    SUM(custom) AS price\n  FROM cte_2\n  GROUP BY\n    mother,\n    litter,\n    tandem\n),\ncte_4 AS (\n  SELECT\n    henry,\n    SUM(price) AS dolly,\n    AVG(spence) AS mutant\n  FROM cte_3\n  GROUP BY\n    henry,\n    growth\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     abbot,\n     touch,\n     career,\n     carbon,\n     artist\n|> SELECT\n     abbot,\n     touch\n|> AGGREGATE\n     MIN(touch) AS mother,\n     AVG(touch) AS tandem,\n     SUM(touch) AS custom\n   GROUP BY\n     abbot\n|> AGGREGATE\n     MIN(custom) AS henry,\n     MAX(custom) AS growth,\n     AVG(custom) AS spence,\n     COUNT(custom) AS fabric,\n     SUM(custom) AS price\n   GROUP BY\n     mother,\n     litter,\n     tandem\n|> AGGREGATE\n     SUM(price) AS dolly,\n     AVG(spence) AS mutant\n   GROUP BY\n     henry,\n     growth\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"litter",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    maggot,\n    armour,\n    tardis,\n    denim,\n    spray\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    maggot,\n    armour,\n    tardis,\n    denim,\n    spray\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(armour) AS bottom,\n    COUNT(maggot) AS ascot,\n    AVG(armour) AS copper\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(ascot) AS roach,\n    AVG(ascot) AS income,\n    SUM(copper) AS johnny,\n    COUNT(ascot) AS myriad\n  FROM cte_2\n  GROUP BY\n    bottom,\n    price\n),\ncte_4 AS (\n  SELECT\n    roach,\n    AVG(myriad) AS crypt,\n    SUM(myriad) AS glider\n  FROM cte_3\n  GROUP BY\n    roach,\n    income,\n    johnny\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     maggot,\n     armour,\n     tardis,\n     denim,\n     spray\n|> SELECT\n     maggot,\n     armour,\n     tardis,\n     denim,\n     spray\n|> AGGREGATE\n     COUNT(armour) AS bottom,\n     COUNT(maggot) AS ascot,\n     AVG(armour) AS copper\n|> AGGREGATE\n     MAX(ascot) AS roach,\n     AVG(ascot) AS income,\n     SUM(copper) AS johnny,\n     COUNT(ascot) AS myriad\n   GROUP BY\n     bottom,\n     price\n|> AGGREGATE\n     AVG(myriad) AS crypt,\n     SUM(myriad) AS glider\n   GROUP BY\n     roach,\n     income,\n     johnny\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"price",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    martyr,\n    herald,\n    equity,\n    vacuum,\n    castle\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(equity) AS effect,\n    MIN(herald) AS course,\n    MAX(castle) AS label\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    AVG(label) AS irony,\n    SUM(label) AS origin,\n    COUNT(label) AS manure\n  FROM cte_1\n  GROUP BY\n    effect,\n    course\n),\ncte_3 AS (\n  SELECT\n    MAX(manure) AS cavity,\n    AVG(manure) AS henry\n  FROM cte_2\n  GROUP BY\n    irony,\n    origin,\n    comet\n),\ncte_4 AS (\n  SELECT\n    cavity,\n    MAX(henry) AS millie,\n    COUNT(henry) AS alaska,\n    MIN(henry) AS pasta\n  FROM cte_3\n  GROUP BY\n    cavity\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     martyr,\n     herald,\n     equity,\n     vacuum,\n     castle\n|> AGGREGATE\n     SUM(equity) AS effect,\n     MIN(herald) AS course,\n     MAX(castle) AS label\n|> AGGREGATE\n     AVG(label) AS irony,\n     SUM(label) AS origin,\n     COUNT(label) AS manure\n   GROUP BY\n     effect,\n     course\n|> AGGREGATE\n     MAX(manure) AS cavity,\n     AVG(manure) AS henry\n   GROUP BY\n     irony,\n     origin,\n     comet\n|> AGGREGATE\n     MAX(henry) AS millie,\n     COUNT(henry) AS alaska,\n     MIN(henry) AS pasta\n   GROUP BY\n     cavity\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"comet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    remark,\n    maple,\n    pigeon,\n    misuse,\n    colon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(colon) AS ankle,\n    AVG(maple) AS amazon,\n    SUM(misuse) AS thorpe\n  FROM cte_0\n  GROUP BY\n    remark\n),\ncte_2 AS (\n  SELECT\n    MAX(thorpe) AS bishop,\n    MIN(amazon) AS apathy,\n    COUNT(thorpe) AS priory,\n    SUM(amazon) AS clerk,\n    MAX(amazon) AS poster,\n    AVG(thorpe) AS alaska\n  FROM cte_1\n  GROUP BY\n    ankle,\n    litter\n),\ncte_3 AS (\n  SELECT\n    bishop,\n    apathy,\n    SUM(alaska) AS satin\n  FROM cte_2\n  GROUP BY\n    bishop,\n    apathy\n),\ncte_4 AS (\n  SELECT\n    SUM(bishop) AS start\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     remark,\n     maple,\n     pigeon,\n     misuse,\n     colon\n|> AGGREGATE\n     MIN(colon) AS ankle,\n     AVG(maple) AS amazon,\n     SUM(misuse) AS thorpe\n   GROUP BY\n     remark\n|> AGGREGATE\n     MAX(thorpe) AS bishop,\n     MIN(amazon) AS apathy,\n     COUNT(thorpe) AS priory,\n     SUM(amazon) AS clerk,\n     MAX(amazon) AS poster,\n     AVG(thorpe) AS alaska\n   GROUP BY\n     ankle,\n     litter\n|> AGGREGATE\n     SUM(alaska) AS satin\n   GROUP BY\n     bishop,\n     apathy\n|> AGGREGATE\n     SUM(bishop) AS start\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"litter",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ariel,\n    stall,\n    spoon,\n    tooth,\n    liner\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(stall) AS refuse\n  FROM cte_0\n  GROUP BY\n    ariel\n),\ncte_2 AS (\n  SELECT\n    SUM(refuse) AS scrub,\n    MIN(refuse) AS frank,\n    MAX(refuse) AS hockey\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(hockey) AS brunt,\n    AVG(hockey) AS album,\n    MIN(hockey) AS repeal,\n    MAX(hockey) AS import,\n    SUM(hockey) AS horror\n  FROM cte_2\n  GROUP BY\n    scrub,\n    cradle,\n    frank\n),\ncte_4 AS (\n  SELECT\n    brunt,\n    album,\n    COUNT(horror) AS diesel\n  FROM cte_3\n  GROUP BY\n    brunt,\n    album,\n    repeal\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ariel,\n     stall,\n     spoon,\n     tooth,\n     liner\n|> AGGREGATE\n     MIN(stall) AS refuse\n   GROUP BY\n     ariel\n|> AGGREGATE\n     SUM(refuse) AS scrub,\n     MIN(refuse) AS frank,\n     MAX(refuse) AS hockey\n|> AGGREGATE\n     COUNT(hockey) AS brunt,\n     AVG(hockey) AS album,\n     MIN(hockey) AS repeal,\n     MAX(hockey) AS import,\n     SUM(hockey) AS horror\n   GROUP BY\n     scrub,\n     cradle,\n     frank\n|> AGGREGATE\n     COUNT(horror) AS diesel\n   GROUP BY\n     brunt,\n     album,\n     repeal\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"cradle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ascent,\n    elbow,\n    power,\n    japan,\n    detail\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(japan) AS shake,\n    COUNT(detail) AS holder,\n    COUNT(power) AS answer\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    shake,\n    holder,\n    answer\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(answer) AS talbot,\n    AVG(holder) AS limit,\n    COUNT(answer) AS escort,\n    SUM(holder) AS jelly,\n    COUNT(holder) AS truth\n  FROM cte_2\n  GROUP BY\n    shake,\n    pitch\n),\ncte_4 AS (\n  SELECT\n    talbot,\n    MAX(truth) AS stile,\n    COUNT(escort) AS sherry,\n    SUM(limit) AS cavity\n  FROM cte_3\n  GROUP BY\n    talbot\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ascent,\n     elbow,\n     power,\n     japan,\n     detail\n|> AGGREGATE\n     MAX(japan) AS shake,\n     COUNT(detail) AS holder,\n     COUNT(power) AS answer\n|> SELECT\n     shake,\n     holder,\n     answer\n|> AGGREGATE\n     MAX(answer) AS talbot,\n     AVG(holder) AS limit,\n     COUNT(answer) AS escort,\n     SUM(holder) AS jelly,\n     COUNT(holder) AS truth\n   GROUP BY\n     shake,\n     pitch\n|> AGGREGATE\n     MAX(truth) AS stile,\n     COUNT(escort) AS sherry,\n     SUM(limit) AS cavity\n   GROUP BY\n     talbot\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"pitch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sensor,\n    leave,\n    elite,\n    lapse,\n    piazza\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(lapse) AS canon,\n    MAX(lapse) AS bomber,\n    MAX(sensor) AS charge,\n    SUM(elite) AS stream\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    canon,\n    bomber,\n    charge,\n    stream\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MIN(canon) AS temple,\n    MAX(bomber) AS favour,\n    AVG(canon) AS camel,\n    AVG(charge) AS tempo\n  FROM cte_2\n  GROUP BY\n    tunnel\n),\ncte_4 AS (\n  SELECT\n    temple,\n    favour,\n    camel,\n    SUM(tempo) AS satan\n  FROM cte_3\n  GROUP BY\n    temple,\n    favour,\n    camel\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sensor,\n     leave,\n     elite,\n     lapse,\n     piazza\n|> AGGREGATE\n     MIN(lapse) AS canon,\n     MAX(lapse) AS bomber,\n     MAX(sensor) AS charge,\n     SUM(elite) AS stream\n|> SELECT\n     canon,\n     bomber,\n     charge,\n     stream\n|> AGGREGATE\n     MIN(canon) AS temple,\n     MAX(bomber) AS favour,\n     AVG(canon) AS camel,\n     AVG(charge) AS tempo\n   GROUP BY\n     tunnel\n|> AGGREGATE\n     SUM(tempo) AS satan\n   GROUP BY\n     temple,\n     favour,\n     camel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"tunnel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    parade,\n    theft,\n    critic,\n    ellen,\n    squire\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(squire) AS oyster,\n    MIN(ellen) AS brook,\n    AVG(squire) AS joanna\n  FROM cte_0\n  GROUP BY\n    parade,\n    theft,\n    critic\n),\ncte_2 AS (\n  SELECT\n    COUNT(joanna) AS policy,\n    SUM(joanna) AS arcade,\n    MIN(joanna) AS height,\n    MAX(joanna) AS upside,\n    AVG(joanna) AS query\n  FROM cte_1\n  GROUP BY\n    oyster,\n    flint,\n    brook\n),\ncte_3 AS (\n  SELECT\n    policy,\n    arcade,\n    height\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(height) AS petrol\n  FROM cte_3\n  GROUP BY\n    policy,\n    arcade\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     parade,\n     theft,\n     critic,\n     ellen,\n     squire\n|> AGGREGATE\n     SUM(squire) AS oyster,\n     MIN(ellen) AS brook,\n     AVG(squire) AS joanna\n   GROUP BY\n     parade,\n     theft,\n     critic\n|> AGGREGATE\n     COUNT(joanna) AS policy,\n     SUM(joanna) AS arcade,\n     MIN(joanna) AS height,\n     MAX(joanna) AS upside,\n     AVG(joanna) AS query\n   GROUP BY\n     oyster,\n     flint,\n     brook\n|> SELECT\n     policy,\n     arcade,\n     height\n|> AGGREGATE\n     MIN(height) AS petrol\n   GROUP BY\n     policy,\n     arcade\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"flint",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    cowboy,\n    taylor,\n    ellen,\n    harris,\n    member\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    cowboy,\n    taylor,\n    ellen,\n    harris,\n    member\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(taylor) AS burst,\n    MIN(harris) AS alley,\n    AVG(ellen) AS sword,\n    SUM(ellen) AS female,\n    COUNT(taylor) AS heath,\n    AVG(taylor) AS sprint,\n    MIN(ellen) AS lounge\n  FROM cte_1\n  GROUP BY\n    cowboy,\n    lesson\n),\ncte_3 AS (\n  SELECT\n    burst,\n    alley,\n    sword,\n    female,\n    heath,\n    sprint\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(sword) AS cadet\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     cowboy,\n     taylor,\n     ellen,\n     harris,\n     member\n|> SELECT\n     cowboy,\n     taylor,\n     ellen,\n     harris,\n     member\n|> AGGREGATE\n     MAX(taylor) AS burst,\n     MIN(harris) AS alley,\n     AVG(ellen) AS sword,\n     SUM(ellen) AS female,\n     COUNT(taylor) AS heath,\n     AVG(taylor) AS sprint,\n     MIN(ellen) AS lounge\n   GROUP BY\n     cowboy,\n     lesson\n|> SELECT\n     burst,\n     alley,\n     sword,\n     female,\n     heath,\n     sprint\n|> AGGREGATE\n     MAX(sword) AS cadet\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"lesson",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    moray,\n    bakery,\n    bloom,\n    reflux,\n    booze\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(booze) AS lease,\n    MIN(reflux) AS intake,\n    AVG(reflux) AS idiom,\n    AVG(bloom) AS inside,\n    MAX(bakery) AS bench\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    lease,\n    intake\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(intake) AS guide,\n    MIN(intake) AS cable,\n    COUNT(intake) AS spoon,\n    SUM(intake) AS friend\n  FROM cte_2\n  GROUP BY\n    jasper,\n    lease\n),\ncte_4 AS (\n  SELECT\n    guide,\n    cable,\n    COUNT(friend) AS jordan\n  FROM cte_3\n  GROUP BY\n    guide,\n    cable,\n    spoon\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     moray,\n     bakery,\n     bloom,\n     reflux,\n     booze\n|> AGGREGATE\n     SUM(booze) AS lease,\n     MIN(reflux) AS intake,\n     AVG(reflux) AS idiom,\n     AVG(bloom) AS inside,\n     MAX(bakery) AS bench\n|> SELECT\n     lease,\n     intake\n|> AGGREGATE\n     MAX(intake) AS guide,\n     MIN(intake) AS cable,\n     COUNT(intake) AS spoon,\n     SUM(intake) AS friend\n   GROUP BY\n     jasper,\n     lease\n|> AGGREGATE\n     COUNT(friend) AS jordan\n   GROUP BY\n     guide,\n     cable,\n     spoon\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:32,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"jasper",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    cruise,\n    tribe,\n    outcry,\n    broom,\n    amber\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(amber) AS convoy,\n    MIN(amber) AS credit,\n    MAX(broom) AS expert,\n    AVG(amber) AS plight,\n    SUM(broom) AS paint,\n    SUM(amber) AS violet\n  FROM cte_0\n  GROUP BY\n    cruise,\n    tribe,\n    outcry\n),\ncte_2 AS (\n  SELECT\n    MAX(plight) AS fight,\n    AVG(violet) AS artist\n  FROM cte_1\n  GROUP BY\n    convoy,\n    midday,\n    credit,\n    expert\n),\ncte_3 AS (\n  SELECT\n    fight,\n    artist\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(fight) AS travel,\n    MIN(artist) AS burgh\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     cruise,\n     tribe,\n     outcry,\n     broom,\n     amber\n|> AGGREGATE\n     COUNT(amber) AS convoy,\n     MIN(amber) AS credit,\n     MAX(broom) AS expert,\n     AVG(amber) AS plight,\n     SUM(broom) AS paint,\n     SUM(amber) AS violet\n   GROUP BY\n     cruise,\n     tribe,\n     outcry\n|> AGGREGATE\n     MAX(plight) AS fight,\n     AVG(violet) AS artist\n   GROUP BY\n     convoy,\n     midday,\n     credit,\n     expert\n|> SELECT\n     fight,\n     artist\n|> AGGREGATE\n     COUNT(fight) AS travel,\n     MIN(artist) AS burgh\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"midday",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    nurse,\n    medal,\n    corner,\n    video,\n    murmur\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(nurse) AS zenith,\n    COUNT(video) AS fungus,\n    MIN(nurse) AS dough,\n    MAX(video) AS pocket,\n    COUNT(murmur) AS repeal,\n    MIN(video) AS fraud,\n    AVG(nurse) AS libel\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(libel) AS curry\n  FROM cte_1\n  GROUP BY\n    zenith,\n    fungus,\n    dough,\n    pocket,\n    spider,\n    repeal,\n    fraud\n),\ncte_3 AS (\n  SELECT\n    MIN(curry) AS white,\n    SUM(curry) AS slough\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    white,\n    slough\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     nurse,\n     medal,\n     corner,\n     video,\n     murmur\n|> AGGREGATE\n     COUNT(nurse) AS zenith,\n     COUNT(video) AS fungus,\n     MIN(nurse) AS dough,\n     MAX(video) AS pocket,\n     COUNT(murmur) AS repeal,\n     MIN(video) AS fraud,\n     AVG(nurse) AS libel\n|> AGGREGATE\n     MIN(libel) AS curry\n   GROUP BY\n     zenith,\n     fungus,\n     dough,\n     pocket,\n     spider,\n     repeal,\n     fraud\n|> AGGREGATE\n     MIN(curry) AS white,\n     SUM(curry) AS slough\n|> SELECT\n     white,\n     slough\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"spider",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hamlet,\n    south,\n    warden,\n    pillar,\n    button\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(pillar) AS spade,\n    SUM(button) AS price,\n    AVG(pillar) AS stress,\n    SUM(pillar) AS hobby\n  FROM cte_0\n  GROUP BY\n    hamlet,\n    south,\n    warden\n),\ncte_2 AS (\n  SELECT\n    MAX(hobby) AS payne,\n    AVG(hobby) AS bazaar,\n    SUM(stress) AS bucket,\n    AVG(stress) AS shore\n  FROM cte_1\n  GROUP BY\n    spade,\n    hurst,\n    price\n),\ncte_3 AS (\n  SELECT\n    MAX(shore) AS pencil,\n    MIN(shore) AS flare\n  FROM cte_2\n  GROUP BY\n    payne\n),\ncte_4 AS (\n  SELECT\n    pencil,\n    flare\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hamlet,\n     south,\n     warden,\n     pillar,\n     button\n|> AGGREGATE\n     MIN(pillar) AS spade,\n     SUM(button) AS price,\n     AVG(pillar) AS stress,\n     SUM(pillar) AS hobby\n   GROUP BY\n     hamlet,\n     south,\n     warden\n|> AGGREGATE\n     MAX(hobby) AS payne,\n     AVG(hobby) AS bazaar,\n     SUM(stress) AS bucket,\n     AVG(stress) AS shore\n   GROUP BY\n     spade,\n     hurst,\n     price\n|> AGGREGATE\n     MAX(shore) AS pencil,\n     MIN(shore) AS flare\n   GROUP BY\n     payne\n|> SELECT\n     pencil,\n     flare\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"hurst",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    berth,\n    critic,\n    shrine,\n    riddle,\n    sketch\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(sketch) AS duvet,\n    SUM(sketch) AS hector,\n    MIN(riddle) AS future,\n    SUM(critic) AS cider,\n    MAX(critic) AS reform\n  FROM cte_0\n  GROUP BY\n    berth\n),\ncte_2 AS (\n  SELECT\n    MIN(cider) AS treat,\n    SUM(cider) AS lagoon,\n    COUNT(cider) AS idiot\n  FROM cte_1\n  GROUP BY\n    duvet,\n    hector,\n    future,\n    radar\n),\ncte_3 AS (\n  SELECT\n    COUNT(lagoon) AS tempo\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(tempo) AS tenor,\n    COUNT(tempo) AS health,\n    AVG(tempo) AS peter\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     berth,\n     critic,\n     shrine,\n     riddle,\n     sketch\n|> AGGREGATE\n     AVG(sketch) AS duvet,\n     SUM(sketch) AS hector,\n     MIN(riddle) AS future,\n     SUM(critic) AS cider,\n     MAX(critic) AS reform\n   GROUP BY\n     berth\n|> AGGREGATE\n     MIN(cider) AS treat,\n     SUM(cider) AS lagoon,\n     COUNT(cider) AS idiot\n   GROUP BY\n     duvet,\n     hector,\n     future,\n     radar\n|> AGGREGATE\n     COUNT(lagoon) AS tempo\n|> AGGREGATE\n     MIN(tempo) AS tenor,\n     COUNT(tempo) AS health,\n     AVG(tempo) AS peter\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"radar",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ethos,\n    shine,\n    almond,\n    saucer,\n    strap\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(shine) AS voyage,\n    MAX(shine) AS debut,\n    AVG(almond) AS trance,\n    MAX(almond) AS alaska,\n    COUNT(almond) AS feeder\n  FROM cte_0\n  GROUP BY\n    ethos\n),\ncte_2 AS (\n  SELECT\n    voyage,\n    debut,\n    trance,\n    alaska,\n    feeder\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    AVG(trance) AS maggie,\n    COUNT(alaska) AS status\n  FROM cte_2\n  GROUP BY\n    atlas,\n    voyage,\n    debut\n),\ncte_4 AS (\n  SELECT\n    MIN(maggie) AS hassle,\n    COUNT(maggie) AS ellen,\n    AVG(status) AS laity\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ethos,\n     shine,\n     almond,\n     saucer,\n     strap\n|> AGGREGATE\n     COUNT(shine) AS voyage,\n     MAX(shine) AS debut,\n     AVG(almond) AS trance,\n     MAX(almond) AS alaska,\n     COUNT(almond) AS feeder\n   GROUP BY\n     ethos\n|> SELECT\n     voyage,\n     debut,\n     trance,\n     alaska,\n     feeder\n|> AGGREGATE\n     AVG(trance) AS maggie,\n     COUNT(alaska) AS status\n   GROUP BY\n     atlas,\n     voyage,\n     debut\n|> AGGREGATE\n     MIN(maggie) AS hassle,\n     COUNT(maggie) AS ellen,\n     AVG(status) AS laity\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"atlas",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    alley,\n    nation,\n    panic,\n    rotor,\n    sting\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(sting) AS fiddle,\n    MIN(alley) AS graham\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(fiddle) AS mosaic,\n    AVG(fiddle) AS barman,\n    SUM(fiddle) AS vendor,\n    MIN(fiddle) AS roller,\n    SUM(graham) AS blank,\n    MIN(graham) AS sierra,\n    MAX(fiddle) AS tundra,\n    MAX(graham) AS critic\n  FROM cte_1\n  GROUP BY\n    ryder\n),\ncte_3 AS (\n  SELECT\n    mosaic,\n    MAX(blank) AS blind\n  FROM cte_2\n  GROUP BY\n    mosaic,\n    barman,\n    vendor\n),\ncte_4 AS (\n  SELECT\n    mosaic,\n    blind\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     alley,\n     nation,\n     panic,\n     rotor,\n     sting\n|> AGGREGATE\n     AVG(sting) AS fiddle,\n     MIN(alley) AS graham\n|> AGGREGATE\n     COUNT(fiddle) AS mosaic,\n     AVG(fiddle) AS barman,\n     SUM(fiddle) AS vendor,\n     MIN(fiddle) AS roller,\n     SUM(graham) AS blank,\n     MIN(graham) AS sierra,\n     MAX(fiddle) AS tundra,\n     MAX(graham) AS critic\n   GROUP BY\n     ryder\n|> AGGREGATE\n     MAX(blank) AS blind\n   GROUP BY\n     mosaic,\n     barman,\n     vendor\n|> SELECT\n     mosaic,\n     blind\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"ryder",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:29,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    excise,\n    trunk,\n    jenny,\n    haven,\n    guitar\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(guitar) AS claire\n  FROM cte_0\n  GROUP BY\n    excise,\n    trunk,\n    jenny,\n    haven\n),\ncte_2 AS (\n  SELECT\n    MIN(claire) AS essay,\n    AVG(claire) AS stench\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(essay) AS nation,\n    COUNT(essay) AS voter,\n    MIN(essay) AS outlay,\n    MAX(stench) AS chant,\n    MIN(stench) AS slater\n  FROM cte_2\n  GROUP BY\n    frost\n),\ncte_4 AS (\n  SELECT\n    nation,\n    voter,\n    outlay,\n    AVG(slater) AS lynch\n  FROM cte_3\n  GROUP BY\n    nation,\n    voter,\n    outlay\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     excise,\n     trunk,\n     jenny,\n     haven,\n     guitar\n|> AGGREGATE\n     COUNT(guitar) AS claire\n   GROUP BY\n     excise,\n     trunk,\n     jenny,\n     haven\n|> AGGREGATE\n     MIN(claire) AS essay,\n     AVG(claire) AS stench\n|> AGGREGATE\n     MAX(essay) AS nation,\n     COUNT(essay) AS voter,\n     MIN(essay) AS outlay,\n     MAX(stench) AS chant,\n     MIN(stench) AS slater\n   GROUP BY\n     frost\n|> AGGREGATE\n     AVG(slater) AS lynch\n   GROUP BY\n     nation,\n     voter,\n     outlay\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:31,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"frost",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    avenue,\n    boiler,\n    creek,\n    graham,\n    burden\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(boiler) AS kernel,\n    MAX(avenue) AS prose,\n    MIN(boiler) AS scorn,\n    AVG(burden) AS record,\n    MAX(graham) AS genome,\n    COUNT(graham) AS crate\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(record) AS burger,\n    MIN(record) AS galley,\n    AVG(record) AS rover\n  FROM cte_1\n  GROUP BY\n    kernel,\n    prose,\n    carrot,\n    scorn\n),\ncte_3 AS (\n  SELECT\n    burger,\n    galley,\n    rover\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(rover) AS bread\n  FROM cte_3\n  GROUP BY\n    burger,\n    galley\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     avenue,\n     boiler,\n     creek,\n     graham,\n     burden\n|> AGGREGATE\n     SUM(boiler) AS kernel,\n     MAX(avenue) AS prose,\n     MIN(boiler) AS scorn,\n     AVG(burden) AS record,\n     MAX(graham) AS genome,\n     COUNT(graham) AS crate\n|> AGGREGATE\n     COUNT(record) AS burger,\n     MIN(record) AS galley,\n     AVG(record) AS rover\n   GROUP BY\n     kernel,\n     prose,\n     carrot,\n     scorn\n|> SELECT\n     burger,\n     galley,\n     rover\n|> AGGREGATE\n     SUM(rover) AS bread\n   GROUP BY\n     burger,\n     galley\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"carrot",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:30,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    mason,\n    handle,\n    prince,\n    notion,\n    safety\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    mason,\n    handle\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    SUM(handle) AS organ,\n    AVG(handle) AS unison,\n    MIN(mason) AS frenzy,\n    MAX(handle) AS empire,\n    MIN(handle) AS flame\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    MAX(empire) AS convoy,\n    COUNT(flame) AS basis,\n    SUM(flame) AS beech\n  FROM cte_2\n  GROUP BY\n    organ,\n    unison,\n    cloud,\n    frenzy\n),\ncte_4 AS (\n  SELECT\n    MIN(basis) AS vodka,\n    MIN(beech) AS teddy,\n    SUM(beech) AS wagon\n  FROM cte_3\n  GROUP BY\n    convoy\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     mason,\n     handle,\n     prince,\n     notion,\n     safety\n|> SELECT\n     mason,\n     handle\n|> AGGREGATE\n     SUM(handle) AS organ,\n     AVG(handle) AS unison,\n     MIN(mason) AS frenzy,\n     MAX(handle) AS empire,\n     MIN(handle) AS flame\n|> AGGREGATE\n     MAX(empire) AS convoy,\n     COUNT(flame) AS basis,\n     SUM(flame) AS beech\n   GROUP BY\n     organ,\n     unison,\n     cloud,\n     frenzy\n|> AGGREGATE\n     MIN(basis) AS vodka,\n     MIN(beech) AS teddy,\n     SUM(beech) AS wagon\n   GROUP BY\n     convoy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"cloud",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    throne,\n    basin,\n    dragon,\n    troop,\n    mafia\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    AVG(troop) AS lance,\n    MAX(dragon) AS paper,\n    COUNT(mafia) AS aspect\n  FROM cte_0\n  GROUP BY\n    throne,\n    basin\n),\ncte_2 AS (\n  SELECT\n    SUM(aspect) AS taste,\n    MAX(paper) AS noise,\n    AVG(lance) AS flute,\n    MIN(aspect) AS candy,\n    AVG(aspect) AS advice,\n    SUM(paper) AS grasp\n  FROM cte_1\n  GROUP BY\n    scalp\n),\ncte_3 AS (\n  SELECT\n    taste,\n    noise,\n    flute,\n    candy\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(flute) AS curve\n  FROM cte_3\n  GROUP BY\n    taste,\n    noise\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     throne,\n     basin,\n     dragon,\n     troop,\n     mafia\n|> AGGREGATE\n     AVG(troop) AS lance,\n     MAX(dragon) AS paper,\n     COUNT(mafia) AS aspect\n   GROUP BY\n     throne,\n     basin\n|> AGGREGATE\n     SUM(aspect) AS taste,\n     MAX(paper) AS noise,\n     AVG(lance) AS flute,\n     MIN(aspect) AS candy,\n     AVG(aspect) AS advice,\n     SUM(paper) AS grasp\n   GROUP BY\n     scalp\n|> SELECT\n     taste,\n     noise,\n     flute,\n     candy\n|> AGGREGATE\n     MIN(flute) AS curve\n   GROUP BY\n     taste,\n     noise\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"scalp",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:31,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    clive,\n    sensor,\n    planet,\n    jargon,\n    score\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    clive,\n    sensor,\n    planet,\n    jargon\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MIN(jargon) AS regime,\n    COUNT(planet) AS shade\n  FROM cte_1\n  GROUP BY\n    clive\n),\ncte_3 AS (\n  SELECT\n    AVG(shade) AS bunker,\n    COUNT(shade) AS chord,\n    SUM(shade) AS favour,\n    MIN(shade) AS bread,\n    MAX(shade) AS yellow\n  FROM cte_2\n  GROUP BY\n    regime,\n    louis\n),\ncte_4 AS (\n  SELECT\n    AVG(yellow) AS colour,\n    MIN(favour) AS gully,\n    SUM(bread) AS advent\n  FROM cte_3\n  GROUP BY\n    bunker,\n    chord\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     clive,\n     sensor,\n     planet,\n     jargon,\n     score\n|> SELECT\n     clive,\n     sensor,\n     planet,\n     jargon\n|> AGGREGATE\n     MIN(jargon) AS regime,\n     COUNT(planet) AS shade\n   GROUP BY\n     clive\n|> AGGREGATE\n     AVG(shade) AS bunker,\n     COUNT(shade) AS chord,\n     SUM(shade) AS favour,\n     MIN(shade) AS bread,\n     MAX(shade) AS yellow\n   GROUP BY\n     regime,\n     louis\n|> AGGREGATE\n     AVG(yellow) AS colour,\n     MIN(favour) AS gully,\n     SUM(bread) AS advent\n   GROUP BY\n     bunker,\n     chord\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"louis",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    owner,\n    policy,\n    pixel,\n    bearer,\n    bruce\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    owner,\n    policy,\n    pixel,\n    bearer\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(bearer) AS torso,\n    AVG(pixel) AS tavern,\n    MAX(pixel) AS cuckoo,\n    SUM(bearer) AS comic\n  FROM cte_1\n  GROUP BY\n    owner\n),\ncte_3 AS (\n  SELECT\n    AVG(tavern) AS seller,\n    SUM(tavern) AS metre,\n    COUNT(cuckoo) AS ethel\n  FROM cte_2\n  GROUP BY\n    torso,\n    offer\n),\ncte_4 AS (\n  SELECT\n    seller,\n    SUM(ethel) AS pellet,\n    SUM(metre) AS bailey,\n    AVG(metre) AS aggie\n  FROM cte_3\n  GROUP BY\n    seller\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     owner,\n     policy,\n     pixel,\n     bearer,\n     bruce\n|> SELECT\n     owner,\n     policy,\n     pixel,\n     bearer\n|> AGGREGATE\n     COUNT(bearer) AS torso,\n     AVG(pixel) AS tavern,\n     MAX(pixel) AS cuckoo,\n     SUM(bearer) AS comic\n   GROUP BY\n     owner\n|> AGGREGATE\n     AVG(tavern) AS seller,\n     SUM(tavern) AS metre,\n     COUNT(cuckoo) AS ethel\n   GROUP BY\n     torso,\n     offer\n|> AGGREGATE\n     SUM(ethel) AS pellet,\n     SUM(metre) AS bailey,\n     AVG(metre) AS aggie\n   GROUP BY\n     seller\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:33,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"offer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    house,\n    pitch,\n    sorrow,\n    strife,\n    carver\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    house,\n    pitch,\n    sorrow,\n    strife,\n    carver\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    COUNT(strife) AS realm,\n    SUM(pitch) AS buyer,\n    SUM(carver) AS blind,\n    COUNT(carver) AS blitz,\n    COUNT(sorrow) AS shore,\n    AVG(strife) AS singer,\n    MIN(sorrow) AS breed\n  FROM cte_1\n  GROUP BY\n    house,\n    click\n),\ncte_3 AS (\n  SELECT\n    realm,\n    buyer,\n    blind,\n    blitz,\n    shore\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    realm,\n    buyer,\n    blind\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     house,\n     pitch,\n     sorrow,\n     strife,\n     carver\n|> SELECT\n     house,\n     pitch,\n     sorrow,\n     strife,\n     carver\n|> AGGREGATE\n     COUNT(strife) AS realm,\n     SUM(pitch) AS buyer,\n     SUM(carver) AS blind,\n     COUNT(carver) AS blitz,\n     COUNT(sorrow) AS shore,\n     AVG(strife) AS singer,\n     MIN(sorrow) AS breed\n   GROUP BY\n     house,\n     click\n|> SELECT\n     realm,\n     buyer,\n     blind,\n     blitz,\n     shore\n|> SELECT\n     realm,\n     buyer,\n     blind\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:34,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:26,errorType:"GROUP_UNKNOWN",totalInformationDifference:0,unknownName:"click",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:32,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    shrug,\n    blanc,\n    cinema,\n    vowel,\n    title\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    shrug,\n    blanc,\n    MAX(title) AS trust,\n    AVG(cinema) AS comma\n  FROM cte_0\n  GROUP BY\n    shrug,\n    blanc\n),\ncte_2 AS (\n  SELECT\n    shrug,\n    COUNT(comma) AS repeat,\n    MIN(comma) AS corps\n  FROM cte_1\n  GROUP BY\n    shrug,\n    blanc,\n    trust\n),\ncte_3 AS (\n  SELECT\n    shrug,\n    MAX(repeat) AS lesson\n  FROM cte_2\n  GROUP BY\n    shrug,\n    tangle\n),\ncte_4 AS (\n  SELECT\n    MIN(lesson) AS canvas,\n    AVG(shrug) AS layout,\n    AVG(lesson) AS willy,\n    COUNT(lesson) AS elbow\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     shrug,\n     blanc,\n     cinema,\n     vowel,\n     title\n|> AGGREGATE\n     MAX(title) AS trust,\n     AVG(cinema) AS comma\n   GROUP BY\n     shrug,\n     blanc\n|> AGGREGATE\n     COUNT(comma) AS repeat,\n     MIN(comma) AS corps\n   GROUP BY\n     shrug,\n     blanc,\n     trust\n|> AGGREGATE\n     MAX(repeat) AS lesson\n   GROUP BY\n     shrug,\n     tangle\n|> AGGREGATE\n     MIN(lesson) AS canvas,\n     AVG(shrug) AS layout,\n     AVG(lesson) AS willy,\n     COUNT(lesson) AS elbow\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"tangle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    turkey,\n    dogma,\n    pause,\n    start,\n    heyday\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    turkey,\n    MIN(heyday) AS marker,\n    SUM(pause) AS cowboy,\n    COUNT(start) AS walnut,\n    MAX(dogma) AS barney,\n    MAX(start) AS lobby\n  FROM cte_0\n  GROUP BY\n    turkey\n),\ncte_2 AS (\n  SELECT\n    turkey,\n    marker,\n    cowboy,\n    SUM(barney) AS morse\n  FROM cte_1\n  GROUP BY\n    turkey,\n    marker,\n    cowboy,\n    state\n),\ncte_3 AS (\n  SELECT\n    turkey,\n    MIN(cowboy) AS finale\n  FROM cte_2\n  GROUP BY\n    turkey\n),\ncte_4 AS (\n  SELECT\n    SUM(finale) AS chance,\n    COUNT(turkey) AS reign\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     turkey,\n     dogma,\n     pause,\n     start,\n     heyday\n|> AGGREGATE\n     MIN(heyday) AS marker,\n     SUM(pause) AS cowboy,\n     COUNT(start) AS walnut,\n     MAX(dogma) AS barney,\n     MAX(start) AS lobby\n   GROUP BY\n     turkey\n|> AGGREGATE\n     SUM(barney) AS morse\n   GROUP BY\n     turkey,\n     marker,\n     cowboy,\n     state\n|> AGGREGATE\n     MIN(cowboy) AS finale\n   GROUP BY\n     turkey\n|> AGGREGATE\n     SUM(finale) AS chance,\n     COUNT(turkey) AS reign\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"state",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    piece,\n    linen,\n    insert,\n    cache,\n    opium\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    piece,\n    linen,\n    insert,\n    cache,\n    opium\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    piece,\n    AVG(cache) AS basket,\n    COUNT(cache) AS barrel,\n    AVG(opium) AS repair\n  FROM cte_1\n  GROUP BY\n    piece,\n    linen\n),\ncte_3 AS (\n  SELECT\n    piece,\n    basket,\n    barrel,\n    MIN(repair) AS crown\n  FROM cte_2\n  GROUP BY\n    piece,\n    hearer,\n    basket,\n    barrel\n),\ncte_4 AS (\n  SELECT\n    MAX(crown) AS mucus,\n    COUNT(crown) AS dinghy,\n    MIN(barrel) AS claim\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     piece,\n     linen,\n     insert,\n     cache,\n     opium\n|> SELECT\n     piece,\n     linen,\n     insert,\n     cache,\n     opium\n|> AGGREGATE\n     AVG(cache) AS basket,\n     COUNT(cache) AS barrel,\n     AVG(opium) AS repair\n   GROUP BY\n     piece,\n     linen\n|> AGGREGATE\n     MIN(repair) AS crown\n   GROUP BY\n     piece,\n     hearer,\n     basket,\n     barrel\n|> AGGREGATE\n     MAX(crown) AS mucus,\n     COUNT(crown) AS dinghy,\n     MIN(barrel) AS claim\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"hearer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    grove,\n    couch,\n    denial,\n    bowie,\n    sprint\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    grove,\n    couch,\n    COUNT(bowie) AS waste,\n    COUNT(denial) AS luxury,\n    MAX(bowie) AS fungus,\n    MIN(bowie) AS class\n  FROM cte_0\n  GROUP BY\n    grove,\n    couch\n),\ncte_2 AS (\n  SELECT\n    grove,\n    couch,\n    MIN(class) AS burgh\n  FROM cte_1\n  GROUP BY\n    grove,\n    couch,\n    waste,\n    luxury,\n    growth,\n    fungus\n),\ncte_3 AS (\n  SELECT\n    grove,\n    MAX(burgh) AS combat\n  FROM cte_2\n  GROUP BY\n    grove,\n    couch\n),\ncte_4 AS (\n  SELECT\n    MIN(grove) AS venom\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     grove,\n     couch,\n     denial,\n     bowie,\n     sprint\n|> AGGREGATE\n     COUNT(bowie) AS waste,\n     COUNT(denial) AS luxury,\n     MAX(bowie) AS fungus,\n     MIN(bowie) AS class\n   GROUP BY\n     grove,\n     couch\n|> AGGREGATE\n     MIN(class) AS burgh\n   GROUP BY\n     grove,\n     couch,\n     waste,\n     luxury,\n     growth,\n     fungus\n|> AGGREGATE\n     MAX(burgh) AS combat\n   GROUP BY\n     grove,\n     couch\n|> AGGREGATE\n     MIN(grove) AS venom\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"growth",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    legend,\n    vendor,\n    wendy,\n    niece,\n    monkey\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    legend,\n    vendor,\n    MAX(niece) AS water\n  FROM cte_0\n  GROUP BY\n    legend,\n    vendor\n),\ncte_2 AS (\n  SELECT\n    MAX(legend) AS occult,\n    COUNT(water) AS banker,\n    SUM(vendor) AS luxury,\n    AVG(vendor) AS artery,\n    SUM(water) AS wrong\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    occult,\n    banker,\n    MIN(wrong) AS chorus\n  FROM cte_2\n  GROUP BY\n    regard,\n    occult,\n    banker,\n    luxury,\n    artery\n),\ncte_4 AS (\n  SELECT\n    occult,\n    SUM(banker) AS pound\n  FROM cte_3\n  GROUP BY\n    occult\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     legend,\n     vendor,\n     wendy,\n     niece,\n     monkey\n|> AGGREGATE\n     MAX(niece) AS water\n   GROUP BY\n     legend,\n     vendor\n|> AGGREGATE\n     MAX(legend) AS occult,\n     COUNT(water) AS banker,\n     SUM(vendor) AS luxury,\n     AVG(vendor) AS artery,\n     SUM(water) AS wrong\n|> AGGREGATE\n     MIN(wrong) AS chorus\n   GROUP BY\n     regard,\n     occult,\n     banker,\n     luxury,\n     artery\n|> AGGREGATE\n     SUM(banker) AS pound\n   GROUP BY\n     occult\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"regard",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hedge,\n    crane,\n    chile,\n    fritz,\n    chorus\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    hedge,\n    crane,\n    chile,\n    fritz,\n    SUM(chorus) AS arena,\n    COUNT(chorus) AS thesis\n  FROM cte_0\n  GROUP BY\n    hedge,\n    crane,\n    chile,\n    fritz\n),\ncte_2 AS (\n  SELECT\n    COUNT(arena) AS brewer,\n    MAX(arena) AS image\n  FROM cte_1\n  GROUP BY\n    hedge,\n    crane,\n    chile,\n    fritz,\n    senna\n),\ncte_3 AS (\n  SELECT\n    brewer,\n    MAX(image) AS spence\n  FROM cte_2\n  GROUP BY\n    brewer\n),\ncte_4 AS (\n  SELECT\n    MIN(brewer) AS month,\n    AVG(spence) AS claret\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hedge,\n     crane,\n     chile,\n     fritz,\n     chorus\n|> AGGREGATE\n     SUM(chorus) AS arena,\n     COUNT(chorus) AS thesis\n   GROUP BY\n     hedge,\n     crane,\n     chile,\n     fritz\n|> AGGREGATE\n     COUNT(arena) AS brewer,\n     MAX(arena) AS image\n   GROUP BY\n     hedge,\n     crane,\n     chile,\n     fritz,\n     senna\n|> AGGREGATE\n     MAX(image) AS spence\n   GROUP BY\n     brewer\n|> AGGREGATE\n     MIN(brewer) AS month,\n     AVG(spence) AS claret\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"senna",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    barney,\n    schema,\n    mandy,\n    plane,\n    thumb\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    barney,\n    schema\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    barney,\n    AVG(schema) AS mouse,\n    SUM(schema) AS irene,\n    MIN(schema) AS watch,\n    COUNT(schema) AS grain\n  FROM cte_1\n  GROUP BY\n    barney\n),\ncte_3 AS (\n  SELECT\n    barney,\n    mouse,\n    irene,\n    MAX(grain) AS mould\n  FROM cte_2\n  GROUP BY\n    barney,\n    mouse,\n    irene,\n    tarmac\n),\ncte_4 AS (\n  SELECT\n    barney,\n    mouse,\n    irene,\n    MAX(mould) AS court\n  FROM cte_3\n  GROUP BY\n    barney,\n    mouse,\n    irene\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     barney,\n     schema,\n     mandy,\n     plane,\n     thumb\n|> SELECT\n     barney,\n     schema\n|> AGGREGATE\n     AVG(schema) AS mouse,\n     SUM(schema) AS irene,\n     MIN(schema) AS watch,\n     COUNT(schema) AS grain\n   GROUP BY\n     barney\n|> AGGREGATE\n     MAX(grain) AS mould\n   GROUP BY\n     barney,\n     mouse,\n     irene,\n     tarmac\n|> AGGREGATE\n     MAX(mould) AS court\n   GROUP BY\n     barney,\n     mouse,\n     irene\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"tarmac",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    other,\n    havoc,\n    cheek,\n    reign,\n    scalp\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    other,\n    COUNT(havoc) AS fauna,\n    MIN(reign) AS hardy\n  FROM cte_0\n  GROUP BY\n    other\n),\ncte_2 AS (\n  SELECT\n    other,\n    AVG(hardy) AS permit,\n    MIN(hardy) AS tower,\n    COUNT(hardy) AS cloth\n  FROM cte_1\n  GROUP BY\n    other,\n    fauna\n),\ncte_3 AS (\n  SELECT\n    other,\n    permit,\n    MAX(cloth) AS spell\n  FROM cte_2\n  GROUP BY\n    other,\n    yogurt,\n    permit\n),\ncte_4 AS (\n  SELECT\n    other,\n    SUM(spell) AS ghetto,\n    AVG(spell) AS origin\n  FROM cte_3\n  GROUP BY\n    other,\n    permit\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     other,\n     havoc,\n     cheek,\n     reign,\n     scalp\n|> AGGREGATE\n     COUNT(havoc) AS fauna,\n     MIN(reign) AS hardy\n   GROUP BY\n     other\n|> AGGREGATE\n     AVG(hardy) AS permit,\n     MIN(hardy) AS tower,\n     COUNT(hardy) AS cloth\n   GROUP BY\n     other,\n     fauna\n|> AGGREGATE\n     MAX(cloth) AS spell\n   GROUP BY\n     other,\n     yogurt,\n     permit\n|> AGGREGATE\n     SUM(spell) AS ghetto,\n     AVG(spell) AS origin\n   GROUP BY\n     other,\n     permit\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"yogurt",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    booze,\n    wallet,\n    chair,\n    tiger,\n    advert\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    booze,\n    wallet,\n    chair,\n    tiger,\n    MIN(advert) AS letter,\n    SUM(advert) AS quote,\n    MAX(advert) AS array\n  FROM cte_0\n  GROUP BY\n    booze,\n    wallet,\n    chair,\n    tiger\n),\ncte_2 AS (\n  SELECT\n    AVG(array) AS miner\n  FROM cte_1\n  GROUP BY\n    booze,\n    wallet,\n    chair,\n    tiger,\n    focus,\n    letter,\n    quote\n),\ncte_3 AS (\n  SELECT\n    SUM(miner) AS model,\n    AVG(miner) AS close\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    model,\n    close\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     booze,\n     wallet,\n     chair,\n     tiger,\n     advert\n|> AGGREGATE\n     MIN(advert) AS letter,\n     SUM(advert) AS quote,\n     MAX(advert) AS array\n   GROUP BY\n     booze,\n     wallet,\n     chair,\n     tiger\n|> AGGREGATE\n     AVG(array) AS miner\n   GROUP BY\n     booze,\n     wallet,\n     chair,\n     tiger,\n     focus,\n     letter,\n     quote\n|> AGGREGATE\n     SUM(miner) AS model,\n     AVG(miner) AS close\n|> SELECT\n     model,\n     close\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"focus",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    japan,\n    spirit,\n    mouse,\n    greek,\n    finch\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    japan,\n    spirit\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    japan,\n    MAX(spirit) AS panel,\n    SUM(spirit) AS brooke,\n    AVG(spirit) AS update,\n    COUNT(spirit) AS plank,\n    MIN(spirit) AS pillow\n  FROM cte_1\n  GROUP BY\n    japan\n),\ncte_3 AS (\n  SELECT\n    japan,\n    panel,\n    brooke,\n    COUNT(plank) AS needle\n  FROM cte_2\n  GROUP BY\n    japan,\n    billy,\n    panel,\n    brooke\n),\ncte_4 AS (\n  SELECT\n    japan,\n    panel,\n    SUM(needle) AS mouth\n  FROM cte_3\n  GROUP BY\n    japan,\n    panel\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     japan,\n     spirit,\n     mouse,\n     greek,\n     finch\n|> SELECT\n     japan,\n     spirit\n|> AGGREGATE\n     MAX(spirit) AS panel,\n     SUM(spirit) AS brooke,\n     AVG(spirit) AS update,\n     COUNT(spirit) AS plank,\n     MIN(spirit) AS pillow\n   GROUP BY\n     japan\n|> AGGREGATE\n     COUNT(plank) AS needle\n   GROUP BY\n     japan,\n     billy,\n     panel,\n     brooke\n|> AGGREGATE\n     SUM(needle) AS mouth\n   GROUP BY\n     japan,\n     panel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:28,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"billy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    victor,\n    burger,\n    burrow,\n    stall,\n    toxin\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    victor,\n    burger,\n    burrow,\n    stall\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    victor,\n    AVG(stall) AS summit,\n    COUNT(burrow) AS diary,\n    MIN(burrow) AS coward,\n    MIN(stall) AS ethel\n  FROM cte_1\n  GROUP BY\n    victor\n),\ncte_3 AS (\n  SELECT\n    victor,\n    summit,\n    diary,\n    MIN(ethel) AS input\n  FROM cte_2\n  GROUP BY\n    victor,\n    willy,\n    summit,\n    diary,\n    coward\n),\ncte_4 AS (\n  SELECT\n    MAX(input) AS gauge\n  FROM cte_3\n  GROUP BY\n    victor,\n    summit,\n    diary\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     victor,\n     burger,\n     burrow,\n     stall,\n     toxin\n|> SELECT\n     victor,\n     burger,\n     burrow,\n     stall\n|> AGGREGATE\n     AVG(stall) AS summit,\n     COUNT(burrow) AS diary,\n     MIN(burrow) AS coward,\n     MIN(stall) AS ethel\n   GROUP BY\n     victor\n|> AGGREGATE\n     MIN(ethel) AS input\n   GROUP BY\n     victor,\n     willy,\n     summit,\n     diary,\n     coward\n|> AGGREGATE\n     MAX(input) AS gauge\n   GROUP BY\n     victor,\n     summit,\n     diary\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"willy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    domain,\n    foetus,\n    visit,\n    terry,\n    query\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    domain,\n    foetus,\n    visit,\n    terry,\n    SUM(query) AS spread\n  FROM cte_0\n  GROUP BY\n    domain,\n    foetus,\n    visit,\n    terry\n),\ncte_2 AS (\n  SELECT\n    MAX(terry) AS genome,\n    SUM(spread) AS photo,\n    MAX(spread) AS upside,\n    AVG(terry) AS vicar,\n    AVG(spread) AS adobe\n  FROM cte_1\n  GROUP BY\n    muscle,\n    domain,\n    foetus,\n    visit\n),\ncte_3 AS (\n  SELECT\n    AVG(vicar) AS comedy\n  FROM cte_2\n  GROUP BY\n    genome\n),\ncte_4 AS (\n  SELECT\n    SUM(comedy) AS pigeon\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     domain,\n     foetus,\n     visit,\n     terry,\n     query\n|> AGGREGATE\n     SUM(query) AS spread\n   GROUP BY\n     domain,\n     foetus,\n     visit,\n     terry\n|> AGGREGATE\n     MAX(terry) AS genome,\n     SUM(spread) AS photo,\n     MAX(spread) AS upside,\n     AVG(terry) AS vicar,\n     AVG(spread) AS adobe\n   GROUP BY\n     muscle,\n     domain,\n     foetus,\n     visit\n|> AGGREGATE\n     AVG(vicar) AS comedy\n   GROUP BY\n     genome\n|> AGGREGATE\n     SUM(comedy) AS pigeon\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"muscle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    check,\n    mosaic,\n    tucker,\n    sweat,\n    texas\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    check,\n    mosaic,\n    MAX(sweat) AS praise,\n    AVG(texas) AS spell\n  FROM cte_0\n  GROUP BY\n    check,\n    mosaic\n),\ncte_2 AS (\n  SELECT\n    check,\n    MIN(spell) AS format,\n    SUM(spell) AS vapour\n  FROM cte_1\n  GROUP BY\n    check,\n    mosaic,\n    praise\n),\ncte_3 AS (\n  SELECT\n    check,\n    COUNT(vapour) AS slater\n  FROM cte_2\n  GROUP BY\n    check,\n    corner\n),\ncte_4 AS (\n  SELECT\n    AVG(slater) AS common,\n    SUM(slater) AS towel,\n    COUNT(slater) AS stain,\n    AVG(check) AS point\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     check,\n     mosaic,\n     tucker,\n     sweat,\n     texas\n|> AGGREGATE\n     MAX(sweat) AS praise,\n     AVG(texas) AS spell\n   GROUP BY\n     check,\n     mosaic\n|> AGGREGATE\n     MIN(spell) AS format,\n     SUM(spell) AS vapour\n   GROUP BY\n     check,\n     mosaic,\n     praise\n|> AGGREGATE\n     COUNT(vapour) AS slater\n   GROUP BY\n     check,\n     corner\n|> AGGREGATE\n     AVG(slater) AS common,\n     SUM(slater) AS towel,\n     COUNT(slater) AS stain,\n     AVG(check) AS point\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"corner",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    handle,\n    barth,\n    loser,\n    trout,\n    bottom\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    handle,\n    barth,\n    loser,\n    SUM(bottom) AS plague\n  FROM cte_0\n  GROUP BY\n    handle,\n    barth,\n    loser,\n    trout\n),\ncte_2 AS (\n  SELECT\n    handle,\n    MAX(barth) AS carrot,\n    COUNT(loser) AS bucket,\n    AVG(loser) AS couple,\n    AVG(barth) AS trauma,\n    SUM(loser) AS litre\n  FROM cte_1\n  GROUP BY\n    orange,\n    handle\n),\ncte_3 AS (\n  SELECT\n    handle,\n    MAX(bucket) AS offer\n  FROM cte_2\n  GROUP BY\n    handle,\n    carrot\n),\ncte_4 AS (\n  SELECT\n    SUM(offer) AS johnny\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     handle,\n     barth,\n     loser,\n     trout,\n     bottom\n|> AGGREGATE\n     SUM(bottom) AS plague\n   GROUP BY\n     handle,\n     barth,\n     loser,\n     trout\n|> AGGREGATE\n     MAX(barth) AS carrot,\n     COUNT(loser) AS bucket,\n     AVG(loser) AS couple,\n     AVG(barth) AS trauma,\n     SUM(loser) AS litre\n   GROUP BY\n     orange,\n     handle\n|> AGGREGATE\n     MAX(bucket) AS offer\n   GROUP BY\n     handle,\n     carrot\n|> AGGREGATE\n     SUM(offer) AS johnny\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"orange",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    cooker,\n    grave,\n    bunch,\n    mercer,\n    juice\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    cooker,\n    grave,\n    bunch,\n    mercer\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    cooker,\n    grave,\n    MAX(bunch) AS resale,\n    AVG(bunch) AS finch,\n    SUM(bunch) AS condom,\n    AVG(mercer) AS cowboy\n  FROM cte_1\n  GROUP BY\n    cooker,\n    grave\n),\ncte_3 AS (\n  SELECT\n    cooker,\n    grave,\n    SUM(condom) AS grain\n  FROM cte_2\n  GROUP BY\n    aunty,\n    cooker,\n    grave\n),\ncte_4 AS (\n  SELECT\n    cooker,\n    grave,\n    MAX(grain) AS maize\n  FROM cte_3\n  GROUP BY\n    cooker,\n    grave\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     cooker,\n     grave,\n     bunch,\n     mercer,\n     juice\n|> SELECT\n     cooker,\n     grave,\n     bunch,\n     mercer\n|> AGGREGATE\n     MAX(bunch) AS resale,\n     AVG(bunch) AS finch,\n     SUM(bunch) AS condom,\n     AVG(mercer) AS cowboy\n   GROUP BY\n     cooker,\n     grave\n|> AGGREGATE\n     SUM(condom) AS grain\n   GROUP BY\n     aunty,\n     cooker,\n     grave\n|> AGGREGATE\n     MAX(grain) AS maize\n   GROUP BY\n     cooker,\n     grave\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:28,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"aunty",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    alkali,\n    cellar,\n    henley,\n    uproar,\n    scotch\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    alkali,\n    COUNT(henley) AS singer,\n    MAX(henley) AS output,\n    AVG(henley) AS health,\n    MIN(henley) AS trail\n  FROM cte_0\n  GROUP BY\n    alkali,\n    cellar\n),\ncte_2 AS (\n  SELECT\n    alkali,\n    singer,\n    output,\n    MAX(trail) AS rhine\n  FROM cte_1\n  GROUP BY\n    alkali,\n    singer,\n    output,\n    health,\n    rogue\n),\ncte_3 AS (\n  SELECT\n    alkali,\n    COUNT(output) AS prior,\n    MAX(singer) AS gould\n  FROM cte_2\n  GROUP BY\n    alkali\n),\ncte_4 AS (\n  SELECT\n    MIN(prior) AS police\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     alkali,\n     cellar,\n     henley,\n     uproar,\n     scotch\n|> AGGREGATE\n     COUNT(henley) AS singer,\n     MAX(henley) AS output,\n     AVG(henley) AS health,\n     MIN(henley) AS trail\n   GROUP BY\n     alkali,\n     cellar\n|> AGGREGATE\n     MAX(trail) AS rhine\n   GROUP BY\n     alkali,\n     singer,\n     output,\n     health,\n     rogue\n|> AGGREGATE\n     COUNT(output) AS prior,\n     MAX(singer) AS gould\n   GROUP BY\n     alkali\n|> AGGREGATE\n     MIN(prior) AS police\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"rogue",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    salon,\n    helper,\n    anger,\n    error,\n    opener\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    salon,\n    helper,\n    AVG(error) AS arrow\n  FROM cte_0\n  GROUP BY\n    salon,\n    helper\n),\ncte_2 AS (\n  SELECT\n    salon,\n    COUNT(arrow) AS actor,\n    MIN(arrow) AS torch,\n    SUM(arrow) AS trend\n  FROM cte_1\n  GROUP BY\n    salon,\n    helper\n),\ncte_3 AS (\n  SELECT\n    salon,\n    AVG(trend) AS stable\n  FROM cte_2\n  GROUP BY\n    salon,\n    actor,\n    ankle,\n    torch\n),\ncte_4 AS (\n  SELECT\n    SUM(stable) AS tissue,\n    MAX(stable) AS zenith,\n    COUNT(stable) AS stake\n  FROM cte_3\n  GROUP BY\n    salon\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     salon,\n     helper,\n     anger,\n     error,\n     opener\n|> AGGREGATE\n     AVG(error) AS arrow\n   GROUP BY\n     salon,\n     helper\n|> AGGREGATE\n     COUNT(arrow) AS actor,\n     MIN(arrow) AS torch,\n     SUM(arrow) AS trend\n   GROUP BY\n     salon,\n     helper\n|> AGGREGATE\n     AVG(trend) AS stable\n   GROUP BY\n     salon,\n     actor,\n     ankle,\n     torch\n|> AGGREGATE\n     SUM(stable) AS tissue,\n     MAX(stable) AS zenith,\n     COUNT(stable) AS stake\n   GROUP BY\n     salon\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"ankle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    travis,\n    suite,\n    bubble,\n    molla,\n    beech\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    travis,\n    suite,\n    bubble\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    travis,\n    suite,\n    COUNT(bubble) AS barney,\n    MIN(bubble) AS grass\n  FROM cte_1\n  GROUP BY\n    travis,\n    suite\n),\ncte_3 AS (\n  SELECT\n    travis,\n    suite,\n    COUNT(grass) AS favour,\n    MIN(grass) AS brandy\n  FROM cte_2\n  GROUP BY\n    travis,\n    suite,\n    barney,\n    purse\n),\ncte_4 AS (\n  SELECT\n    AVG(travis) AS chase,\n    MIN(travis) AS spirit,\n    SUM(travis) AS punch,\n    AVG(suite) AS woman\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     travis,\n     suite,\n     bubble,\n     molla,\n     beech\n|> SELECT\n     travis,\n     suite,\n     bubble\n|> AGGREGATE\n     COUNT(bubble) AS barney,\n     MIN(bubble) AS grass\n   GROUP BY\n     travis,\n     suite\n|> AGGREGATE\n     COUNT(grass) AS favour,\n     MIN(grass) AS brandy\n   GROUP BY\n     travis,\n     suite,\n     barney,\n     purse\n|> AGGREGATE\n     AVG(travis) AS chase,\n     MIN(travis) AS spirit,\n     SUM(travis) AS punch,\n     AVG(suite) AS woman\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"purse",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    figure,\n    course,\n    badge,\n    torch,\n    sheila\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    figure,\n    course,\n    badge,\n    COUNT(sheila) AS period,\n    MIN(sheila) AS snail,\n    MAX(sheila) AS behalf\n  FROM cte_0\n  GROUP BY\n    figure,\n    course,\n    badge,\n    torch\n),\ncte_2 AS (\n  SELECT\n    figure,\n    MIN(behalf) AS orchid\n  FROM cte_1\n  GROUP BY\n    figure,\n    course,\n    badge,\n    period,\n    score\n),\ncte_3 AS (\n  SELECT\n    figure,\n    AVG(orchid) AS jerry\n  FROM cte_2\n  GROUP BY\n    figure\n),\ncte_4 AS (\n  SELECT\n    COUNT(jerry) AS circus,\n    COUNT(figure) AS reply\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     figure,\n     course,\n     badge,\n     torch,\n     sheila\n|> AGGREGATE\n     COUNT(sheila) AS period,\n     MIN(sheila) AS snail,\n     MAX(sheila) AS behalf\n   GROUP BY\n     figure,\n     course,\n     badge,\n     torch\n|> AGGREGATE\n     MIN(behalf) AS orchid\n   GROUP BY\n     figure,\n     course,\n     badge,\n     period,\n     score\n|> AGGREGATE\n     AVG(orchid) AS jerry\n   GROUP BY\n     figure\n|> AGGREGATE\n     COUNT(jerry) AS circus,\n     COUNT(figure) AS reply\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"score",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    franc,\n    black,\n    blouse,\n    genius,\n    daisy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(daisy) AS sunset,\n    MIN(genius) AS cradle,\n    COUNT(genius) AS fulham\n  FROM cte_0\n  GROUP BY\n    franc,\n    black,\n    blouse\n),\ncte_2 AS (\n  SELECT\n    sunset,\n    cradle,\n    SUM(fulham) AS arrow\n  FROM cte_1\n  GROUP BY\n    sunset,\n    cradle\n),\ncte_3 AS (\n  SELECT\n    sunset,\n    cradle,\n    AVG(arrow) AS yield\n  FROM cte_2\n  GROUP BY\n    sunset,\n    rival,\n    cradle\n),\ncte_4 AS (\n  SELECT\n    AVG(yield) AS cavity,\n    MIN(yield) AS depot,\n    AVG(cradle) AS little\n  FROM cte_3\n  GROUP BY\n    sunset\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     franc,\n     black,\n     blouse,\n     genius,\n     daisy\n|> AGGREGATE\n     SUM(daisy) AS sunset,\n     MIN(genius) AS cradle,\n     COUNT(genius) AS fulham\n   GROUP BY\n     franc,\n     black,\n     blouse\n|> AGGREGATE\n     SUM(fulham) AS arrow\n   GROUP BY\n     sunset,\n     cradle\n|> AGGREGATE\n     AVG(arrow) AS yield\n   GROUP BY\n     sunset,\n     rival,\n     cradle\n|> AGGREGATE\n     AVG(yield) AS cavity,\n     MIN(yield) AS depot,\n     AVG(cradle) AS little\n   GROUP BY\n     sunset\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"rival",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tonne,\n    colour,\n    answer,\n    japan,\n    marrow\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    tonne,\n    colour,\n    answer,\n    COUNT(marrow) AS rattle,\n    MAX(marrow) AS stain,\n    AVG(marrow) AS fodder,\n    MIN(marrow) AS brook\n  FROM cte_0\n  GROUP BY\n    tonne,\n    colour,\n    answer,\n    japan\n),\ncte_2 AS (\n  SELECT\n    tonne,\n    SUM(fodder) AS clutch,\n    MAX(brook) AS dealer\n  FROM cte_1\n  GROUP BY\n    shaft,\n    tonne,\n    colour\n),\ncte_3 AS (\n  SELECT\n    tonne,\n    clutch\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    tonne,\n    SUM(clutch) AS madam\n  FROM cte_3\n  GROUP BY\n    tonne\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tonne,\n     colour,\n     answer,\n     japan,\n     marrow\n|> AGGREGATE\n     COUNT(marrow) AS rattle,\n     MAX(marrow) AS stain,\n     AVG(marrow) AS fodder,\n     MIN(marrow) AS brook\n   GROUP BY\n     tonne,\n     colour,\n     answer,\n     japan\n|> AGGREGATE\n     SUM(fodder) AS clutch,\n     MAX(brook) AS dealer\n   GROUP BY\n     shaft,\n     tonne,\n     colour\n|> SELECT\n     tonne,\n     clutch\n|> AGGREGATE\n     SUM(clutch) AS madam\n   GROUP BY\n     tonne\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"shaft",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    goblin,\n    power,\n    brewer,\n    cabin,\n    canvas\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    goblin,\n    AVG(canvas) AS wizard\n  FROM cte_0\n  GROUP BY\n    goblin,\n    power,\n    brewer\n),\ncte_2 AS (\n  SELECT\n    goblin,\n    MIN(wizard) AS ghana,\n    MAX(wizard) AS cover,\n    AVG(wizard) AS movie,\n    SUM(wizard) AS trick\n  FROM cte_1\n  GROUP BY\n    goblin\n),\ncte_3 AS (\n  SELECT\n    goblin,\n    ghana,\n    COUNT(trick) AS terror\n  FROM cte_2\n  GROUP BY\n    timer,\n    goblin,\n    ghana,\n    cover,\n    movie\n),\ncte_4 AS (\n  SELECT\n    AVG(terror) AS berth,\n    COUNT(goblin) AS boxer\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     goblin,\n     power,\n     brewer,\n     cabin,\n     canvas\n|> AGGREGATE\n     AVG(canvas) AS wizard\n   GROUP BY\n     goblin,\n     power,\n     brewer\n|> AGGREGATE\n     MIN(wizard) AS ghana,\n     MAX(wizard) AS cover,\n     AVG(wizard) AS movie,\n     SUM(wizard) AS trick\n   GROUP BY\n     goblin\n|> AGGREGATE\n     COUNT(trick) AS terror\n   GROUP BY\n     timer,\n     goblin,\n     ghana,\n     cover,\n     movie\n|> AGGREGATE\n     AVG(terror) AS berth,\n     COUNT(goblin) AS boxer\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"timer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    south,\n    valve,\n    wealth,\n    pupil,\n    tycoon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    south,\n    valve,\n    wealth,\n    pupil,\n    tycoon\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    south,\n    valve,\n    MIN(pupil) AS diesel\n  FROM cte_1\n  GROUP BY\n    south,\n    valve,\n    wealth\n),\ncte_3 AS (\n  SELECT\n    south,\n    valve,\n    MAX(diesel) AS pencil,\n    SUM(diesel) AS safari\n  FROM cte_2\n  GROUP BY\n    south,\n    valve,\n    creole\n),\ncte_4 AS (\n  SELECT\n    south,\n    valve,\n    pencil,\n    SUM(safari) AS board\n  FROM cte_3\n  GROUP BY\n    south,\n    valve,\n    pencil\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     south,\n     valve,\n     wealth,\n     pupil,\n     tycoon\n|> SELECT\n     south,\n     valve,\n     wealth,\n     pupil,\n     tycoon\n|> AGGREGATE\n     MIN(pupil) AS diesel\n   GROUP BY\n     south,\n     valve,\n     wealth\n|> AGGREGATE\n     MAX(diesel) AS pencil,\n     SUM(diesel) AS safari\n   GROUP BY\n     south,\n     valve,\n     creole\n|> AGGREGATE\n     SUM(safari) AS board\n   GROUP BY\n     south,\n     valve,\n     pencil\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:27,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"creole",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    kitten,\n    irony,\n    chant,\n    yellow,\n    rarity\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    kitten,\n    COUNT(yellow) AS tallis\n  FROM cte_0\n  GROUP BY\n    kitten,\n    irony\n),\ncte_2 AS (\n  SELECT\n    COUNT(tallis) AS scare,\n    AVG(tallis) AS advice,\n    MAX(tallis) AS ozone,\n    MIN(tallis) AS tablet\n  FROM cte_1\n  GROUP BY\n    kitten\n),\ncte_3 AS (\n  SELECT\n    scare,\n    advice,\n    ozone,\n    SUM(tablet) AS hostel\n  FROM cte_2\n  GROUP BY\n    scare,\n    signal,\n    advice,\n    ozone\n),\ncte_4 AS (\n  SELECT\n    scare,\n    MIN(hostel) AS bingo\n  FROM cte_3\n  GROUP BY\n    scare,\n    advice,\n    ozone\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     kitten,\n     irony,\n     chant,\n     yellow,\n     rarity\n|> AGGREGATE\n     COUNT(yellow) AS tallis\n   GROUP BY\n     kitten,\n     irony\n|> AGGREGATE\n     COUNT(tallis) AS scare,\n     AVG(tallis) AS advice,\n     MAX(tallis) AS ozone,\n     MIN(tallis) AS tablet\n   GROUP BY\n     kitten\n|> AGGREGATE\n     SUM(tablet) AS hostel\n   GROUP BY\n     scare,\n     signal,\n     advice,\n     ozone\n|> AGGREGATE\n     MIN(hostel) AS bingo\n   GROUP BY\n     scare,\n     advice,\n     ozone\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"signal",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    advert,\n    singer,\n    corpse,\n    swamp,\n    warmth\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(advert) AS peach,\n    MAX(warmth) AS runner\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    MAX(runner) AS scope,\n    MIN(peach) AS oxygen,\n    COUNT(runner) AS burrow,\n    SUM(runner) AS shire,\n    AVG(peach) AS entity\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    scope,\n    oxygen,\n    burrow,\n    shire,\n    SUM(entity) AS salmon\n  FROM cte_2\n  GROUP BY\n    author,\n    scope,\n    oxygen,\n    burrow,\n    shire\n),\ncte_4 AS (\n  SELECT\n    AVG(burrow) AS norma,\n    AVG(shire) AS donor\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     advert,\n     singer,\n     corpse,\n     swamp,\n     warmth\n|> AGGREGATE\n     COUNT(advert) AS peach,\n     MAX(warmth) AS runner\n|> AGGREGATE\n     MAX(runner) AS scope,\n     MIN(peach) AS oxygen,\n     COUNT(runner) AS burrow,\n     SUM(runner) AS shire,\n     AVG(peach) AS entity\n|> AGGREGATE\n     SUM(entity) AS salmon\n   GROUP BY\n     author,\n     scope,\n     oxygen,\n     burrow,\n     shire\n|> AGGREGATE\n     AVG(burrow) AS norma,\n     AVG(shire) AS donor\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"author",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    array,\n    haste,\n    gentry,\n    smell,\n    treaty\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    array,\n    haste,\n    gentry,\n    smell,\n    MAX(treaty) AS meadow\n  FROM cte_0\n  GROUP BY\n    array,\n    haste,\n    gentry,\n    smell\n),\ncte_2 AS (\n  SELECT\n    array,\n    haste,\n    gentry,\n    smell\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    COUNT(smell) AS resale,\n    AVG(smell) AS relish,\n    MAX(gentry) AS border\n  FROM cte_2\n  GROUP BY\n    cigar,\n    array,\n    haste\n),\ncte_4 AS (\n  SELECT\n    SUM(border) AS fibre,\n    COUNT(border) AS intent\n  FROM cte_3\n  GROUP BY\n    resale,\n    relish\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     array,\n     haste,\n     gentry,\n     smell,\n     treaty\n|> AGGREGATE\n     MAX(treaty) AS meadow\n   GROUP BY\n     array,\n     haste,\n     gentry,\n     smell\n|> SELECT\n     array,\n     haste,\n     gentry,\n     smell\n|> AGGREGATE\n     COUNT(smell) AS resale,\n     AVG(smell) AS relish,\n     MAX(gentry) AS border\n   GROUP BY\n     cigar,\n     array,\n     haste\n|> AGGREGATE\n     SUM(border) AS fibre,\n     COUNT(border) AS intent\n   GROUP BY\n     resale,\n     relish\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"cigar",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    copper,\n    farce,\n    dragon,\n    sense,\n    crane\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    copper,\n    farce,\n    dragon,\n    COUNT(crane) AS young,\n    AVG(crane) AS misery,\n    MAX(sense) AS spine,\n    MIN(sense) AS garage\n  FROM cte_0\n  GROUP BY\n    copper,\n    farce,\n    dragon\n),\ncte_2 AS (\n  SELECT\n    copper,\n    MIN(young) AS mercy,\n    COUNT(spine) AS plasma\n  FROM cte_1\n  GROUP BY\n    copper,\n    leader,\n    farce,\n    dragon\n),\ncte_3 AS (\n  SELECT\n    SUM(mercy) AS deity\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(deity) AS switch,\n    MIN(deity) AS remedy\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     copper,\n     farce,\n     dragon,\n     sense,\n     crane\n|> AGGREGATE\n     COUNT(crane) AS young,\n     AVG(crane) AS misery,\n     MAX(sense) AS spine,\n     MIN(sense) AS garage\n   GROUP BY\n     copper,\n     farce,\n     dragon\n|> AGGREGATE\n     MIN(young) AS mercy,\n     COUNT(spine) AS plasma\n   GROUP BY\n     copper,\n     leader,\n     farce,\n     dragon\n|> AGGREGATE\n     SUM(mercy) AS deity\n|> AGGREGATE\n     MAX(deity) AS switch,\n     MIN(deity) AS remedy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"leader",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    brown,\n    ordeal,\n    savoy,\n    henley,\n    idiom\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    brown,\n    COUNT(henley) AS widow\n  FROM cte_0\n  GROUP BY\n    brown,\n    ordeal\n),\ncte_2 AS (\n  SELECT\n    brown,\n    MAX(widow) AS right,\n    COUNT(widow) AS trauma,\n    SUM(widow) AS devil\n  FROM cte_1\n  GROUP BY\n    brown\n),\ncte_3 AS (\n  SELECT\n    brown,\n    right,\n    COUNT(devil) AS merger,\n    MAX(devil) AS bruce\n  FROM cte_2\n  GROUP BY\n    brown,\n    slang,\n    right,\n    trauma\n),\ncte_4 AS (\n  SELECT\n    brown,\n    right,\n    SUM(bruce) AS tally\n  FROM cte_3\n  GROUP BY\n    brown,\n    right\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     brown,\n     ordeal,\n     savoy,\n     henley,\n     idiom\n|> AGGREGATE\n     COUNT(henley) AS widow\n   GROUP BY\n     brown,\n     ordeal\n|> AGGREGATE\n     MAX(widow) AS right,\n     COUNT(widow) AS trauma,\n     SUM(widow) AS devil\n   GROUP BY\n     brown\n|> AGGREGATE\n     COUNT(devil) AS merger,\n     MAX(devil) AS bruce\n   GROUP BY\n     brown,\n     slang,\n     right,\n     trauma\n|> AGGREGATE\n     SUM(bruce) AS tally\n   GROUP BY\n     brown,\n     right\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:28,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"slang",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    stress,\n    thing,\n    blaze,\n    idiom,\n    clause\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(thing) AS elaine,\n    COUNT(thing) AS attack,\n    MIN(idiom) AS loser,\n    SUM(thing) AS splash,\n    MIN(thing) AS picket,\n    MAX(idiom) AS volley\n  FROM cte_0\n  GROUP BY\n    stress\n),\ncte_2 AS (\n  SELECT\n    elaine,\n    attack,\n    loser,\n    splash,\n    COUNT(volley) AS botany\n  FROM cte_1\n  GROUP BY\n    elaine,\n    review,\n    attack,\n    loser,\n    splash,\n    picket\n),\ncte_3 AS (\n  SELECT\n    elaine,\n    attack\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    elaine,\n    attack\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     stress,\n     thing,\n     blaze,\n     idiom,\n     clause\n|> AGGREGATE\n     MAX(thing) AS elaine,\n     COUNT(thing) AS attack,\n     MIN(idiom) AS loser,\n     SUM(thing) AS splash,\n     MIN(thing) AS picket,\n     MAX(idiom) AS volley\n   GROUP BY\n     stress\n|> AGGREGATE\n     COUNT(volley) AS botany\n   GROUP BY\n     elaine,\n     review,\n     attack,\n     loser,\n     splash,\n     picket\n|> SELECT\n     elaine,\n     attack\n|> SELECT\n     elaine,\n     attack\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"review",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    clover,\n    occult,\n    acorn,\n    hopper,\n    matrix\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    clover,\n    occult,\n    acorn,\n    hopper\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    clover,\n    occult,\n    acorn,\n    SUM(hopper) AS theory\n  FROM cte_1\n  GROUP BY\n    clover,\n    occult,\n    acorn\n),\ncte_3 AS (\n  SELECT\n    clover,\n    MIN(theory) AS tavern,\n    COUNT(theory) AS break,\n    SUM(theory) AS farmer\n  FROM cte_2\n  GROUP BY\n    clover,\n    tennis,\n    occult,\n    acorn\n),\ncte_4 AS (\n  SELECT\n    AVG(farmer) AS scout,\n    SUM(tavern) AS window,\n    COUNT(tavern) AS leone\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     clover,\n     occult,\n     acorn,\n     hopper,\n     matrix\n|> SELECT\n     clover,\n     occult,\n     acorn,\n     hopper\n|> AGGREGATE\n     SUM(hopper) AS theory\n   GROUP BY\n     clover,\n     occult,\n     acorn\n|> AGGREGATE\n     MIN(theory) AS tavern,\n     COUNT(theory) AS break,\n     SUM(theory) AS farmer\n   GROUP BY\n     clover,\n     tennis,\n     occult,\n     acorn\n|> AGGREGATE\n     AVG(farmer) AS scout,\n     SUM(tavern) AS window,\n     COUNT(tavern) AS leone\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"tennis",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    beetle,\n    rival,\n    urine,\n    baxter,\n    marina\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    beetle,\n    rival,\n    urine,\n    AVG(marina) AS caesar\n  FROM cte_0\n  GROUP BY\n    beetle,\n    rival,\n    urine,\n    baxter\n),\ncte_2 AS (\n  SELECT\n    MIN(rival) AS chest,\n    MIN(caesar) AS thesis,\n    SUM(caesar) AS yeast\n  FROM cte_1\n  GROUP BY\n    beetle\n),\ncte_3 AS (\n  SELECT\n    chest,\n    AVG(yeast) AS strike\n  FROM cte_2\n  GROUP BY\n    chest,\n    bamboo,\n    thesis\n),\ncte_4 AS (\n  SELECT\n    MAX(strike) AS stone,\n    COUNT(strike) AS stitch,\n    MIN(strike) AS sleeve\n  FROM cte_3\n  GROUP BY\n    chest\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     beetle,\n     rival,\n     urine,\n     baxter,\n     marina\n|> AGGREGATE\n     AVG(marina) AS caesar\n   GROUP BY\n     beetle,\n     rival,\n     urine,\n     baxter\n|> AGGREGATE\n     MIN(rival) AS chest,\n     MIN(caesar) AS thesis,\n     SUM(caesar) AS yeast\n   GROUP BY\n     beetle\n|> AGGREGATE\n     AVG(yeast) AS strike\n   GROUP BY\n     chest,\n     bamboo,\n     thesis\n|> AGGREGATE\n     MAX(strike) AS stone,\n     COUNT(strike) AS stitch,\n     MIN(strike) AS sleeve\n   GROUP BY\n     chest\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"bamboo",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    banana,\n    remark,\n    salute,\n    pellet,\n    oyster\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MAX(pellet) AS jasper,\n    MIN(salute) AS shadow,\n    MAX(salute) AS stuff\n  FROM cte_0\n  GROUP BY\n    banana,\n    remark\n),\ncte_2 AS (\n  SELECT\n    jasper,\n    shadow,\n    MAX(stuff) AS heater\n  FROM cte_1\n  GROUP BY\n    jasper,\n    shadow\n),\ncte_3 AS (\n  SELECT\n    jasper,\n    shadow,\n    MIN(heater) AS fence\n  FROM cte_2\n  GROUP BY\n    jasper,\n    shadow,\n    celery\n),\ncte_4 AS (\n  SELECT\n    jasper,\n    AVG(shadow) AS outlet,\n    MIN(shadow) AS smoke,\n    COUNT(shadow) AS unity\n  FROM cte_3\n  GROUP BY\n    jasper\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     banana,\n     remark,\n     salute,\n     pellet,\n     oyster\n|> AGGREGATE\n     MAX(pellet) AS jasper,\n     MIN(salute) AS shadow,\n     MAX(salute) AS stuff\n   GROUP BY\n     banana,\n     remark\n|> AGGREGATE\n     MAX(stuff) AS heater\n   GROUP BY\n     jasper,\n     shadow\n|> AGGREGATE\n     MIN(heater) AS fence\n   GROUP BY\n     jasper,\n     shadow,\n     celery\n|> AGGREGATE\n     AVG(shadow) AS outlet,\n     MIN(shadow) AS smoke,\n     COUNT(shadow) AS unity\n   GROUP BY\n     jasper\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"celery",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    dolly,\n    paper,\n    shrug,\n    tycoon,\n    livery\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    dolly,\n    paper,\n    AVG(livery) AS apple,\n    MAX(livery) AS census,\n    MIN(livery) AS organ\n  FROM cte_0\n  GROUP BY\n    dolly,\n    paper,\n    shrug,\n    tycoon\n),\ncte_2 AS (\n  SELECT\n    dolly,\n    paper,\n    COUNT(organ) AS ellen,\n    SUM(organ) AS savoy\n  FROM cte_1\n  GROUP BY\n    dolly,\n    paper,\n    school,\n    apple\n),\ncte_3 AS (\n  SELECT\n    dolly,\n    paper,\n    ellen\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(ellen) AS artist,\n    SUM(dolly) AS tailor\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     dolly,\n     paper,\n     shrug,\n     tycoon,\n     livery\n|> AGGREGATE\n     AVG(livery) AS apple,\n     MAX(livery) AS census,\n     MIN(livery) AS organ\n   GROUP BY\n     dolly,\n     paper,\n     shrug,\n     tycoon\n|> AGGREGATE\n     COUNT(organ) AS ellen,\n     SUM(organ) AS savoy\n   GROUP BY\n     dolly,\n     paper,\n     school,\n     apple\n|> SELECT\n     dolly,\n     paper,\n     ellen\n|> AGGREGATE\n     COUNT(ellen) AS artist,\n     SUM(dolly) AS tailor\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"school",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    touch,\n    fleet,\n    steen,\n    cocoa,\n    trauma\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    touch,\n    fleet,\n    steen,\n    COUNT(cocoa) AS build,\n    COUNT(trauma) AS sherry,\n    MIN(cocoa) AS gender\n  FROM cte_0\n  GROUP BY\n    touch,\n    fleet,\n    steen\n),\ncte_2 AS (\n  SELECT\n    SUM(steen) AS wreck,\n    MAX(fleet) AS theory\n  FROM cte_1\n  GROUP BY\n    touch\n),\ncte_3 AS (\n  SELECT\n    wreck,\n    SUM(theory) AS prize\n  FROM cte_2\n  GROUP BY\n    madge,\n    wreck\n),\ncte_4 AS (\n  SELECT\n    SUM(prize) AS laura,\n    COUNT(prize) AS puppy,\n    AVG(prize) AS goblin\n  FROM cte_3\n  GROUP BY\n    wreck\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     touch,\n     fleet,\n     steen,\n     cocoa,\n     trauma\n|> AGGREGATE\n     COUNT(cocoa) AS build,\n     COUNT(trauma) AS sherry,\n     MIN(cocoa) AS gender\n   GROUP BY\n     touch,\n     fleet,\n     steen\n|> AGGREGATE\n     SUM(steen) AS wreck,\n     MAX(fleet) AS theory\n   GROUP BY\n     touch\n|> AGGREGATE\n     SUM(theory) AS prize\n   GROUP BY\n     madge,\n     wreck\n|> AGGREGATE\n     SUM(prize) AS laura,\n     COUNT(prize) AS puppy,\n     AVG(prize) AS goblin\n   GROUP BY\n     wreck\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"madge",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    final,\n    player,\n    vanity,\n    craven,\n    picket\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    final,\n    player,\n    COUNT(vanity) AS stream,\n    COUNT(craven) AS labour\n  FROM cte_0\n  GROUP BY\n    final,\n    player\n),\ncte_2 AS (\n  SELECT\n    MAX(final) AS mortar,\n    SUM(final) AS fulham,\n    COUNT(final) AS thorn\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    mortar,\n    fulham,\n    SUM(thorn) AS sonny,\n    MIN(thorn) AS insert\n  FROM cte_2\n  GROUP BY\n    ideal,\n    mortar,\n    fulham\n),\ncte_4 AS (\n  SELECT\n    mortar,\n    SUM(insert) AS finale,\n    MAX(fulham) AS module\n  FROM cte_3\n  GROUP BY\n    mortar\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     final,\n     player,\n     vanity,\n     craven,\n     picket\n|> AGGREGATE\n     COUNT(vanity) AS stream,\n     COUNT(craven) AS labour\n   GROUP BY\n     final,\n     player\n|> AGGREGATE\n     MAX(final) AS mortar,\n     SUM(final) AS fulham,\n     COUNT(final) AS thorn\n|> AGGREGATE\n     SUM(thorn) AS sonny,\n     MIN(thorn) AS insert\n   GROUP BY\n     ideal,\n     mortar,\n     fulham\n|> AGGREGATE\n     SUM(insert) AS finale,\n     MAX(fulham) AS module\n   GROUP BY\n     mortar\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"ideal",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    drawer,\n    pollen,\n    midday,\n    suite,\n    jargon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    drawer,\n    pollen,\n    midday,\n    MAX(jargon) AS pasta\n  FROM cte_0\n  GROUP BY\n    drawer,\n    pollen,\n    midday\n),\ncte_2 AS (\n  SELECT\n    drawer,\n    MIN(pasta) AS staple,\n    COUNT(pasta) AS night,\n    AVG(pasta) AS amino,\n    SUM(pasta) AS charm,\n    MAX(pasta) AS nerve\n  FROM cte_1\n  GROUP BY\n    drawer,\n    cheer,\n    pollen,\n    midday\n),\ncte_3 AS (\n  SELECT\n    drawer,\n    staple\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(drawer) AS colour,\n    AVG(drawer) AS choice\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     drawer,\n     pollen,\n     midday,\n     suite,\n     jargon\n|> AGGREGATE\n     MAX(jargon) AS pasta\n   GROUP BY\n     drawer,\n     pollen,\n     midday\n|> AGGREGATE\n     MIN(pasta) AS staple,\n     COUNT(pasta) AS night,\n     AVG(pasta) AS amino,\n     SUM(pasta) AS charm,\n     MAX(pasta) AS nerve\n   GROUP BY\n     drawer,\n     cheer,\n     pollen,\n     midday\n|> SELECT\n     drawer,\n     staple\n|> AGGREGATE\n     COUNT(drawer) AS colour,\n     AVG(drawer) AS choice\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"cheer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    crunch,\n    shoot,\n    caesar,\n    moment,\n    vessel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    crunch,\n    shoot\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    crunch,\n    MAX(shoot) AS sermon,\n    AVG(shoot) AS tardis,\n    SUM(shoot) AS viking\n  FROM cte_1\n  GROUP BY\n    crunch\n),\ncte_3 AS (\n  SELECT\n    crunch,\n    sermon,\n    tardis,\n    COUNT(viking) AS guilt,\n    MIN(viking) AS speech\n  FROM cte_2\n  GROUP BY\n    crunch,\n    sermon,\n    tardis,\n    wicket\n),\ncte_4 AS (\n  SELECT\n    crunch,\n    MIN(sermon) AS tower,\n    SUM(speech) AS groove,\n    COUNT(guilt) AS grass\n  FROM cte_3\n  GROUP BY\n    crunch\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     crunch,\n     shoot,\n     caesar,\n     moment,\n     vessel\n|> SELECT\n     crunch,\n     shoot\n|> AGGREGATE\n     MAX(shoot) AS sermon,\n     AVG(shoot) AS tardis,\n     SUM(shoot) AS viking\n   GROUP BY\n     crunch\n|> AGGREGATE\n     COUNT(viking) AS guilt,\n     MIN(viking) AS speech\n   GROUP BY\n     crunch,\n     sermon,\n     tardis,\n     wicket\n|> AGGREGATE\n     MIN(sermon) AS tower,\n     SUM(speech) AS groove,\n     COUNT(guilt) AS grass\n   GROUP BY\n     crunch\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"wicket",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sperm,\n    subset,\n    mafia,\n    report,\n    fauna\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    sperm,\n    subset,\n    AVG(report) AS brass\n  FROM cte_0\n  GROUP BY\n    sperm,\n    subset,\n    mafia\n),\ncte_2 AS (\n  SELECT\n    sperm,\n    COUNT(subset) AS remand,\n    MAX(subset) AS statue,\n    MIN(brass) AS carver\n  FROM cte_1\n  GROUP BY\n    sperm\n),\ncte_3 AS (\n  SELECT\n    sperm,\n    MIN(carver) AS token,\n    COUNT(carver) AS auntie\n  FROM cte_2\n  GROUP BY\n    usage,\n    sperm,\n    remand,\n    statue\n),\ncte_4 AS (\n  SELECT\n    SUM(token) AS viola,\n    MAX(auntie) AS radar\n  FROM cte_3\n  GROUP BY\n    sperm\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sperm,\n     subset,\n     mafia,\n     report,\n     fauna\n|> AGGREGATE\n     AVG(report) AS brass\n   GROUP BY\n     sperm,\n     subset,\n     mafia\n|> AGGREGATE\n     COUNT(subset) AS remand,\n     MAX(subset) AS statue,\n     MIN(brass) AS carver\n   GROUP BY\n     sperm\n|> AGGREGATE\n     MIN(carver) AS token,\n     COUNT(carver) AS auntie\n   GROUP BY\n     usage,\n     sperm,\n     remand,\n     statue\n|> AGGREGATE\n     SUM(token) AS viola,\n     MAX(auntie) AS radar\n   GROUP BY\n     sperm\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"usage",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rhino,\n    guide,\n    melon,\n    bible,\n    cellar\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    rhino,\n    MIN(cellar) AS phoebe,\n    SUM(cellar) AS virus\n  FROM cte_0\n  GROUP BY\n    rhino,\n    guide,\n    melon,\n    bible\n),\ncte_2 AS (\n  SELECT\n    rhino,\n    phoebe,\n    AVG(virus) AS quarry\n  FROM cte_1\n  GROUP BY\n    rhino,\n    phoebe\n),\ncte_3 AS (\n  SELECT\n    rhino,\n    COUNT(quarry) AS league,\n    MAX(quarry) AS onion\n  FROM cte_2\n  GROUP BY\n    trend,\n    rhino,\n    phoebe\n),\ncte_4 AS (\n  SELECT\n    COUNT(onion) AS glint,\n    AVG(onion) AS writer\n  FROM cte_3\n  GROUP BY\n    rhino,\n    league\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rhino,\n     guide,\n     melon,\n     bible,\n     cellar\n|> AGGREGATE\n     MIN(cellar) AS phoebe,\n     SUM(cellar) AS virus\n   GROUP BY\n     rhino,\n     guide,\n     melon,\n     bible\n|> AGGREGATE\n     AVG(virus) AS quarry\n   GROUP BY\n     rhino,\n     phoebe\n|> AGGREGATE\n     COUNT(quarry) AS league,\n     MAX(quarry) AS onion\n   GROUP BY\n     trend,\n     rhino,\n     phoebe\n|> AGGREGATE\n     COUNT(onion) AS glint,\n     AVG(onion) AS writer\n   GROUP BY\n     rhino,\n     league\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"trend",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    draper,\n    sailor,\n    penis,\n    texas,\n    sanity\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    draper,\n    sailor,\n    SUM(sanity) AS piazza,\n    COUNT(penis) AS score,\n    COUNT(sanity) AS reform,\n    MAX(texas) AS garden\n  FROM cte_0\n  GROUP BY\n    draper,\n    sailor\n),\ncte_2 AS (\n  SELECT\n    draper,\n    sailor,\n    SUM(score) AS rhino,\n    AVG(score) AS satire\n  FROM cte_1\n  GROUP BY\n    draper,\n    sailor,\n    strain,\n    piazza\n),\ncte_3 AS (\n  SELECT\n    draper,\n    sailor,\n    rhino\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(draper) AS reply,\n    SUM(draper) AS hostel\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     draper,\n     sailor,\n     penis,\n     texas,\n     sanity\n|> AGGREGATE\n     SUM(sanity) AS piazza,\n     COUNT(penis) AS score,\n     COUNT(sanity) AS reform,\n     MAX(texas) AS garden\n   GROUP BY\n     draper,\n     sailor\n|> AGGREGATE\n     SUM(score) AS rhino,\n     AVG(score) AS satire\n   GROUP BY\n     draper,\n     sailor,\n     strain,\n     piazza\n|> SELECT\n     draper,\n     sailor,\n     rhino\n|> AGGREGATE\n     COUNT(draper) AS reply,\n     SUM(draper) AS hostel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"strain",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    stance,\n    sound,\n    medal,\n    moment,\n    beacon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    stance,\n    sound,\n    medal,\n    MAX(beacon) AS rugby,\n    MIN(beacon) AS meadow\n  FROM cte_0\n  GROUP BY\n    stance,\n    sound,\n    medal,\n    moment\n),\ncte_2 AS (\n  SELECT\n    stance,\n    MIN(meadow) AS supper,\n    COUNT(meadow) AS quartz\n  FROM cte_1\n  GROUP BY\n    stance,\n    sound,\n    medal,\n    rugby,\n    breed\n),\ncte_3 AS (\n  SELECT\n    stance,\n    COUNT(quartz) AS derry\n  FROM cte_2\n  GROUP BY\n    stance,\n    supper\n),\ncte_4 AS (\n  SELECT\n    SUM(derry) AS light\n  FROM cte_3\n  GROUP BY\n    stance\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     stance,\n     sound,\n     medal,\n     moment,\n     beacon\n|> AGGREGATE\n     MAX(beacon) AS rugby,\n     MIN(beacon) AS meadow\n   GROUP BY\n     stance,\n     sound,\n     medal,\n     moment\n|> AGGREGATE\n     MIN(meadow) AS supper,\n     COUNT(meadow) AS quartz\n   GROUP BY\n     stance,\n     sound,\n     medal,\n     rugby,\n     breed\n|> AGGREGATE\n     COUNT(quartz) AS derry\n   GROUP BY\n     stance,\n     supper\n|> AGGREGATE\n     SUM(derry) AS light\n   GROUP BY\n     stance\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"breed",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    whisky,\n    south,\n    cutter,\n    curfew,\n    pasta\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    whisky,\n    south,\n    cutter,\n    MAX(pasta) AS cradle,\n    MIN(pasta) AS chunk\n  FROM cte_0\n  GROUP BY\n    whisky,\n    south,\n    cutter,\n    curfew\n),\ncte_2 AS (\n  SELECT\n    whisky,\n    MIN(cradle) AS flora,\n    AVG(cradle) AS equity,\n    SUM(cradle) AS reform,\n    COUNT(cradle) AS venue\n  FROM cte_1\n  GROUP BY\n    clerk,\n    whisky,\n    south\n),\ncte_3 AS (\n  SELECT\n    whisky,\n    MAX(equity) AS smile\n  FROM cte_2\n  GROUP BY\n    whisky\n),\ncte_4 AS (\n  SELECT\n    whisky,\n    smile\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     whisky,\n     south,\n     cutter,\n     curfew,\n     pasta\n|> AGGREGATE\n     MAX(pasta) AS cradle,\n     MIN(pasta) AS chunk\n   GROUP BY\n     whisky,\n     south,\n     cutter,\n     curfew\n|> AGGREGATE\n     MIN(cradle) AS flora,\n     AVG(cradle) AS equity,\n     SUM(cradle) AS reform,\n     COUNT(cradle) AS venue\n   GROUP BY\n     clerk,\n     whisky,\n     south\n|> AGGREGATE\n     MAX(equity) AS smile\n   GROUP BY\n     whisky\n|> SELECT\n     whisky,\n     smile\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"clerk",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    manor,\n    merger,\n    export,\n    chaos,\n    tennis\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    manor,\n    SUM(tennis) AS yield,\n    COUNT(export) AS bumper\n  FROM cte_0\n  GROUP BY\n    manor,\n    merger\n),\ncte_2 AS (\n  SELECT\n    manor,\n    yield,\n    MIN(bumper) AS falcon,\n    AVG(bumper) AS mungo\n  FROM cte_1\n  GROUP BY\n    manor,\n    yield\n),\ncte_3 AS (\n  SELECT\n    manor,\n    MIN(mungo) AS strat,\n    SUM(mungo) AS thrill\n  FROM cte_2\n  GROUP BY\n    stern,\n    manor,\n    yield,\n    falcon\n),\ncte_4 AS (\n  SELECT\n    COUNT(thrill) AS forte,\n    AVG(thrill) AS bucket\n  FROM cte_3\n  GROUP BY\n    manor\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     manor,\n     merger,\n     export,\n     chaos,\n     tennis\n|> AGGREGATE\n     SUM(tennis) AS yield,\n     COUNT(export) AS bumper\n   GROUP BY\n     manor,\n     merger\n|> AGGREGATE\n     MIN(bumper) AS falcon,\n     AVG(bumper) AS mungo\n   GROUP BY\n     manor,\n     yield\n|> AGGREGATE\n     MIN(mungo) AS strat,\n     SUM(mungo) AS thrill\n   GROUP BY\n     stern,\n     manor,\n     yield,\n     falcon\n|> AGGREGATE\n     COUNT(thrill) AS forte,\n     AVG(thrill) AS bucket\n   GROUP BY\n     manor\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"stern",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sound,\n    claire,\n    orange,\n    killer,\n    price\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    sound,\n    SUM(killer) AS shore,\n    AVG(killer) AS novel,\n    SUM(price) AS rally,\n    AVG(price) AS brick\n  FROM cte_0\n  GROUP BY\n    sound\n),\ncte_2 AS (\n  SELECT\n    sound,\n    shore,\n    novel,\n    MAX(brick) AS tomato,\n    MIN(rally) AS foetus\n  FROM cte_1\n  GROUP BY\n    sound,\n    shore,\n    novel,\n    tunnel\n),\ncte_3 AS (\n  SELECT\n    sound,\n    shore,\n    novel\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    sound,\n    COUNT(novel) AS winner\n  FROM cte_3\n  GROUP BY\n    sound,\n    shore\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sound,\n     claire,\n     orange,\n     killer,\n     price\n|> AGGREGATE\n     SUM(killer) AS shore,\n     AVG(killer) AS novel,\n     SUM(price) AS rally,\n     AVG(price) AS brick\n   GROUP BY\n     sound\n|> AGGREGATE\n     MAX(brick) AS tomato,\n     MIN(rally) AS foetus\n   GROUP BY\n     sound,\n     shore,\n     novel,\n     tunnel\n|> SELECT\n     sound,\n     shore,\n     novel\n|> AGGREGATE\n     COUNT(novel) AS winner\n   GROUP BY\n     sound,\n     shore\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"tunnel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    county,\n    knock,\n    thigh,\n    idiom,\n    folly\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    county,\n    knock,\n    thigh,\n    SUM(folly) AS incest\n  FROM cte_0\n  GROUP BY\n    county,\n    knock,\n    thigh\n),\ncte_2 AS (\n  SELECT\n    county,\n    COUNT(knock) AS stone,\n    MAX(knock) AS point,\n    MIN(knock) AS bottle\n  FROM cte_1\n  GROUP BY\n    county\n),\ncte_3 AS (\n  SELECT\n    SUM(point) AS surge,\n    MAX(point) AS index\n  FROM cte_2\n  GROUP BY\n    lotion,\n    county,\n    stone\n),\ncte_4 AS (\n  SELECT\n    COUNT(surge) AS stamp,\n    MAX(index) AS vodka,\n    SUM(surge) AS plough\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     county,\n     knock,\n     thigh,\n     idiom,\n     folly\n|> AGGREGATE\n     SUM(folly) AS incest\n   GROUP BY\n     county,\n     knock,\n     thigh\n|> AGGREGATE\n     COUNT(knock) AS stone,\n     MAX(knock) AS point,\n     MIN(knock) AS bottle\n   GROUP BY\n     county\n|> AGGREGATE\n     SUM(point) AS surge,\n     MAX(point) AS index\n   GROUP BY\n     lotion,\n     county,\n     stone\n|> AGGREGATE\n     COUNT(surge) AS stamp,\n     MAX(index) AS vodka,\n     SUM(surge) AS plough\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"lotion",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tender,\n    pitch,\n    medium,\n    colour,\n    helmet\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    tender,\n    pitch,\n    medium,\n    colour\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    tender,\n    pitch,\n    medium,\n    AVG(colour) AS deputy\n  FROM cte_1\n  GROUP BY\n    tender,\n    pitch,\n    medium\n),\ncte_3 AS (\n  SELECT\n    tender,\n    SUM(deputy) AS menace,\n    MIN(deputy) AS agent,\n    AVG(deputy) AS motion\n  FROM cte_2\n  GROUP BY\n    tender,\n    bridle,\n    pitch,\n    medium\n),\ncte_4 AS (\n  SELECT\n    tender,\n    MAX(agent) AS bowler,\n    MAX(menace) AS lotus\n  FROM cte_3\n  GROUP BY\n    tender\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tender,\n     pitch,\n     medium,\n     colour,\n     helmet\n|> SELECT\n     tender,\n     pitch,\n     medium,\n     colour\n|> AGGREGATE\n     AVG(colour) AS deputy\n   GROUP BY\n     tender,\n     pitch,\n     medium\n|> AGGREGATE\n     SUM(deputy) AS menace,\n     MIN(deputy) AS agent,\n     AVG(deputy) AS motion\n   GROUP BY\n     tender,\n     bridle,\n     pitch,\n     medium\n|> AGGREGATE\n     MAX(agent) AS bowler,\n     MAX(menace) AS lotus\n   GROUP BY\n     tender\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"bridle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    anthem,\n    coward,\n    tomato,\n    female,\n    label\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    anthem,\n    coward,\n    tomato,\n    AVG(female) AS toilet,\n    SUM(label) AS ribbon,\n    SUM(female) AS spark,\n    MIN(label) AS meter\n  FROM cte_0\n  GROUP BY\n    anthem,\n    coward,\n    tomato\n),\ncte_2 AS (\n  SELECT\n    anthem,\n    MAX(meter) AS museum\n  FROM cte_1\n  GROUP BY\n    anthem,\n    coward,\n    tomato,\n    flock\n),\ncte_3 AS (\n  SELECT\n    SUM(museum) AS cooker,\n    MAX(museum) AS trough,\n    COUNT(museum) AS march\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    cooker,\n    trough\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     anthem,\n     coward,\n     tomato,\n     female,\n     label\n|> AGGREGATE\n     AVG(female) AS toilet,\n     SUM(label) AS ribbon,\n     SUM(female) AS spark,\n     MIN(label) AS meter\n   GROUP BY\n     anthem,\n     coward,\n     tomato\n|> AGGREGATE\n     MAX(meter) AS museum\n   GROUP BY\n     anthem,\n     coward,\n     tomato,\n     flock\n|> AGGREGATE\n     SUM(museum) AS cooker,\n     MAX(museum) AS trough,\n     COUNT(museum) AS march\n|> SELECT\n     cooker,\n     trough\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"flock",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    black,\n    connie,\n    lounge,\n    bloom,\n    cereal\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    black,\n    connie,\n    COUNT(cereal) AS single\n  FROM cte_0\n  GROUP BY\n    black,\n    connie,\n    lounge,\n    bloom\n),\ncte_2 AS (\n  SELECT\n    black,\n    COUNT(single) AS bidder\n  FROM cte_1\n  GROUP BY\n    black\n),\ncte_3 AS (\n  SELECT\n    black,\n    AVG(bidder) AS trade,\n    COUNT(bidder) AS stone,\n    MIN(bidder) AS incest\n  FROM cte_2\n  GROUP BY\n    black,\n    debut\n),\ncte_4 AS (\n  SELECT\n    MAX(incest) AS canopy,\n    SUM(black) AS filth,\n    COUNT(black) AS gloss,\n    MAX(stone) AS birch\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     black,\n     connie,\n     lounge,\n     bloom,\n     cereal\n|> AGGREGATE\n     COUNT(cereal) AS single\n   GROUP BY\n     black,\n     connie,\n     lounge,\n     bloom\n|> AGGREGATE\n     COUNT(single) AS bidder\n   GROUP BY\n     black\n|> AGGREGATE\n     AVG(bidder) AS trade,\n     COUNT(bidder) AS stone,\n     MIN(bidder) AS incest\n   GROUP BY\n     black,\n     debut\n|> AGGREGATE\n     MAX(incest) AS canopy,\n     SUM(black) AS filth,\n     COUNT(black) AS gloss,\n     MAX(stone) AS birch\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"debut",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rally,\n    slice,\n    mister,\n    texas,\n    comet\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    rally,\n    MIN(slice) AS sigma\n  FROM cte_0\n  GROUP BY\n    rally\n),\ncte_2 AS (\n  SELECT\n    rally,\n    MAX(sigma) AS union,\n    AVG(sigma) AS slope,\n    SUM(sigma) AS sanity,\n    COUNT(sigma) AS slide,\n    MIN(sigma) AS spire\n  FROM cte_1\n  GROUP BY\n    rally\n),\ncte_3 AS (\n  SELECT\n    rally,\n    union,\n    AVG(spire) AS census\n  FROM cte_2\n  GROUP BY\n    tunnel,\n    rally,\n    union\n),\ncte_4 AS (\n  SELECT\n    COUNT(census) AS guide,\n    SUM(union) AS porter,\n    MAX(rally) AS peter\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rally,\n     slice,\n     mister,\n     texas,\n     comet\n|> AGGREGATE\n     MIN(slice) AS sigma\n   GROUP BY\n     rally\n|> AGGREGATE\n     MAX(sigma) AS union,\n     AVG(sigma) AS slope,\n     SUM(sigma) AS sanity,\n     COUNT(sigma) AS slide,\n     MIN(sigma) AS spire\n   GROUP BY\n     rally\n|> AGGREGATE\n     AVG(spire) AS census\n   GROUP BY\n     tunnel,\n     rally,\n     union\n|> AGGREGATE\n     COUNT(census) AS guide,\n     SUM(union) AS porter,\n     MAX(rally) AS peter\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"tunnel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    angel,\n    tennis,\n    alley,\n    bruce,\n    pasta\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    angel,\n    MIN(bruce) AS estate,\n    SUM(bruce) AS thorpe,\n    AVG(bruce) AS warsaw,\n    AVG(pasta) AS newco\n  FROM cte_0\n  GROUP BY\n    angel,\n    tennis,\n    alley\n),\ncte_2 AS (\n  SELECT\n    angel,\n    estate,\n    thorpe,\n    MAX(warsaw) AS canada,\n    COUNT(warsaw) AS stable\n  FROM cte_1\n  GROUP BY\n    angel,\n    buyer,\n    estate,\n    thorpe\n),\ncte_3 AS (\n  SELECT\n    angel,\n    estate\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    angel,\n    COUNT(estate) AS taste\n  FROM cte_3\n  GROUP BY\n    angel\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     angel,\n     tennis,\n     alley,\n     bruce,\n     pasta\n|> AGGREGATE\n     MIN(bruce) AS estate,\n     SUM(bruce) AS thorpe,\n     AVG(bruce) AS warsaw,\n     AVG(pasta) AS newco\n   GROUP BY\n     angel,\n     tennis,\n     alley\n|> AGGREGATE\n     MAX(warsaw) AS canada,\n     COUNT(warsaw) AS stable\n   GROUP BY\n     angel,\n     buyer,\n     estate,\n     thorpe\n|> SELECT\n     angel,\n     estate\n|> AGGREGATE\n     COUNT(estate) AS taste\n   GROUP BY\n     angel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"buyer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    jerry,\n    spate,\n    scare,\n    xerox,\n    chick\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    jerry,\n    spate,\n    SUM(chick) AS marrow,\n    AVG(xerox) AS taste,\n    MIN(chick) AS willy\n  FROM cte_0\n  GROUP BY\n    jerry,\n    spate\n),\ncte_2 AS (\n  SELECT\n    jerry,\n    spate,\n    MIN(taste) AS factor,\n    COUNT(willy) AS kidney,\n    AVG(willy) AS sister\n  FROM cte_1\n  GROUP BY\n    jerry,\n    spate,\n    system,\n    marrow\n),\ncte_3 AS (\n  SELECT\n    MIN(spate) AS device\n  FROM cte_2\n  GROUP BY\n    jerry\n),\ncte_4 AS (\n  SELECT\n    SUM(device) AS guess,\n    AVG(device) AS shape\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     jerry,\n     spate,\n     scare,\n     xerox,\n     chick\n|> AGGREGATE\n     SUM(chick) AS marrow,\n     AVG(xerox) AS taste,\n     MIN(chick) AS willy\n   GROUP BY\n     jerry,\n     spate\n|> AGGREGATE\n     MIN(taste) AS factor,\n     COUNT(willy) AS kidney,\n     AVG(willy) AS sister\n   GROUP BY\n     jerry,\n     spate,\n     system,\n     marrow\n|> AGGREGATE\n     MIN(spate) AS device\n   GROUP BY\n     jerry\n|> AGGREGATE\n     SUM(device) AS guess,\n     AVG(device) AS shape\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"system",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    squire,\n    clown,\n    inset,\n    animal,\n    simon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    squire,\n    clown,\n    MIN(simon) AS maker,\n    MIN(animal) AS thrush,\n    SUM(animal) AS lever,\n    MAX(simon) AS offer\n  FROM cte_0\n  GROUP BY\n    squire,\n    clown,\n    inset\n),\ncte_2 AS (\n  SELECT\n    squire,\n    clown,\n    MAX(offer) AS lodge,\n    MIN(lever) AS summit\n  FROM cte_1\n  GROUP BY\n    squire,\n    galaxy,\n    clown,\n    maker,\n    thrush\n),\ncte_3 AS (\n  SELECT\n    squire,\n    clown\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(clown) AS forge\n  FROM cte_3\n  GROUP BY\n    squire\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     squire,\n     clown,\n     inset,\n     animal,\n     simon\n|> AGGREGATE\n     MIN(simon) AS maker,\n     MIN(animal) AS thrush,\n     SUM(animal) AS lever,\n     MAX(simon) AS offer\n   GROUP BY\n     squire,\n     clown,\n     inset\n|> AGGREGATE\n     MAX(offer) AS lodge,\n     MIN(lever) AS summit\n   GROUP BY\n     squire,\n     galaxy,\n     clown,\n     maker,\n     thrush\n|> SELECT\n     squire,\n     clown\n|> AGGREGATE\n     COUNT(clown) AS forge\n   GROUP BY\n     squire\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"galaxy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    greek,\n    cadet,\n    drama,\n    zenith,\n    teapot\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    greek,\n    cadet,\n    SUM(teapot) AS dream\n  FROM cte_0\n  GROUP BY\n    greek,\n    cadet,\n    drama,\n    zenith\n),\ncte_2 AS (\n  SELECT\n    greek,\n    MAX(dream) AS crush,\n    COUNT(dream) AS thrill\n  FROM cte_1\n  GROUP BY\n    greek\n),\ncte_3 AS (\n  SELECT\n    greek,\n    SUM(thrill) AS donkey,\n    COUNT(crush) AS lough\n  FROM cte_2\n  GROUP BY\n    greek,\n    filth\n),\ncte_4 AS (\n  SELECT\n    MAX(donkey) AS offer,\n    MAX(greek) AS hotel,\n    SUM(lough) AS seller,\n    MIN(greek) AS choice\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     greek,\n     cadet,\n     drama,\n     zenith,\n     teapot\n|> AGGREGATE\n     SUM(teapot) AS dream\n   GROUP BY\n     greek,\n     cadet,\n     drama,\n     zenith\n|> AGGREGATE\n     MAX(dream) AS crush,\n     COUNT(dream) AS thrill\n   GROUP BY\n     greek\n|> AGGREGATE\n     SUM(thrill) AS donkey,\n     COUNT(crush) AS lough\n   GROUP BY\n     greek,\n     filth\n|> AGGREGATE\n     MAX(donkey) AS offer,\n     MAX(greek) AS hotel,\n     SUM(lough) AS seller,\n     MIN(greek) AS choice\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"filth",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bread,\n    forth,\n    silver,\n    hearth,\n    bowel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    MIN(forth) AS novel,\n    MAX(bowel) AS assay\n  FROM cte_0\n  GROUP BY\n    bread\n),\ncte_2 AS (\n  SELECT\n    novel,\n    MIN(assay) AS reader,\n    MAX(assay) AS abbot,\n    COUNT(assay) AS relic\n  FROM cte_1\n  GROUP BY\n    novel\n),\ncte_3 AS (\n  SELECT\n    novel,\n    reader,\n    abbot,\n    COUNT(relic) AS smash,\n    MAX(relic) AS dieter\n  FROM cte_2\n  GROUP BY\n    fancy,\n    novel,\n    reader,\n    abbot\n),\ncte_4 AS (\n  SELECT\n    MIN(smash) AS piety\n  FROM cte_3\n  GROUP BY\n    novel,\n    reader,\n    abbot\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bread,\n     forth,\n     silver,\n     hearth,\n     bowel\n|> AGGREGATE\n     MIN(forth) AS novel,\n     MAX(bowel) AS assay\n   GROUP BY\n     bread\n|> AGGREGATE\n     MIN(assay) AS reader,\n     MAX(assay) AS abbot,\n     COUNT(assay) AS relic\n   GROUP BY\n     novel\n|> AGGREGATE\n     COUNT(relic) AS smash,\n     MAX(relic) AS dieter\n   GROUP BY\n     fancy,\n     novel,\n     reader,\n     abbot\n|> AGGREGATE\n     MIN(smash) AS piety\n   GROUP BY\n     novel,\n     reader,\n     abbot\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"fancy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    colin,\n    viking,\n    amazon,\n    lagoon,\n    jetty\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    colin,\n    viking,\n    amazon,\n    lagoon,\n    jetty\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    colin,\n    viking,\n    amazon,\n    AVG(lagoon) AS duchy\n  FROM cte_1\n  GROUP BY\n    colin,\n    viking,\n    amazon\n),\ncte_3 AS (\n  SELECT\n    colin,\n    SUM(duchy) AS blank,\n    MIN(amazon) AS walker,\n    COUNT(amazon) AS recipe\n  FROM cte_2\n  GROUP BY\n    tavern,\n    colin,\n    viking\n),\ncte_4 AS (\n  SELECT\n    MAX(blank) AS asthma,\n    SUM(walker) AS opium,\n    MIN(walker) AS tommy\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     colin,\n     viking,\n     amazon,\n     lagoon,\n     jetty\n|> SELECT\n     colin,\n     viking,\n     amazon,\n     lagoon,\n     jetty\n|> AGGREGATE\n     AVG(lagoon) AS duchy\n   GROUP BY\n     colin,\n     viking,\n     amazon\n|> AGGREGATE\n     SUM(duchy) AS blank,\n     MIN(amazon) AS walker,\n     COUNT(amazon) AS recipe\n   GROUP BY\n     tavern,\n     colin,\n     viking\n|> AGGREGATE\n     MAX(blank) AS asthma,\n     SUM(walker) AS opium,\n     MIN(walker) AS tommy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"tavern",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    penis,\n    novice,\n    tandem,\n    breach,\n    tender\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    penis,\n    novice,\n    tandem,\n    COUNT(tender) AS chuck\n  FROM cte_0\n  GROUP BY\n    penis,\n    novice,\n    tandem\n),\ncte_2 AS (\n  SELECT\n    penis,\n    AVG(chuck) AS sleep,\n    SUM(chuck) AS travis,\n    MAX(chuck) AS flick,\n    MIN(chuck) AS tongue,\n    COUNT(chuck) AS cartel\n  FROM cte_1\n  GROUP BY\n    penis,\n    replay,\n    novice,\n    tandem\n),\ncte_3 AS (\n  SELECT\n    penis,\n    MAX(flick) AS willy\n  FROM cte_2\n  GROUP BY\n    penis\n),\ncte_4 AS (\n  SELECT\n    penis,\n    willy\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     penis,\n     novice,\n     tandem,\n     breach,\n     tender\n|> AGGREGATE\n     COUNT(tender) AS chuck\n   GROUP BY\n     penis,\n     novice,\n     tandem\n|> AGGREGATE\n     AVG(chuck) AS sleep,\n     SUM(chuck) AS travis,\n     MAX(chuck) AS flick,\n     MIN(chuck) AS tongue,\n     COUNT(chuck) AS cartel\n   GROUP BY\n     penis,\n     replay,\n     novice,\n     tandem\n|> AGGREGATE\n     MAX(flick) AS willy\n   GROUP BY\n     penis\n|> SELECT\n     penis,\n     willy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"replay",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sonnet,\n    arrow,\n    baxter,\n    stain,\n    dummy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    sonnet,\n    arrow,\n    SUM(dummy) AS bloke,\n    AVG(dummy) AS voyage,\n    MAX(stain) AS rally\n  FROM cte_0\n  GROUP BY\n    sonnet,\n    arrow,\n    baxter\n),\ncte_2 AS (\n  SELECT\n    sonnet,\n    arrow,\n    MAX(rally) AS norman,\n    COUNT(voyage) AS pillar,\n    AVG(voyage) AS hogan\n  FROM cte_1\n  GROUP BY\n    sonnet,\n    sweep,\n    arrow\n),\ncte_3 AS (\n  SELECT\n    sonnet,\n    SUM(hogan) AS vessel\n  FROM cte_2\n  GROUP BY\n    sonnet,\n    arrow\n),\ncte_4 AS (\n  SELECT\n    sonnet,\n    vessel\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sonnet,\n     arrow,\n     baxter,\n     stain,\n     dummy\n|> AGGREGATE\n     SUM(dummy) AS bloke,\n     AVG(dummy) AS voyage,\n     MAX(stain) AS rally\n   GROUP BY\n     sonnet,\n     arrow,\n     baxter\n|> AGGREGATE\n     MAX(rally) AS norman,\n     COUNT(voyage) AS pillar,\n     AVG(voyage) AS hogan\n   GROUP BY\n     sonnet,\n     sweep,\n     arrow\n|> AGGREGATE\n     SUM(hogan) AS vessel\n   GROUP BY\n     sonnet,\n     arrow\n|> SELECT\n     sonnet,\n     vessel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"sweep",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    goose,\n    metre,\n    cheque,\n    tablet,\n    moran\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    goose,\n    metre,\n    MAX(moran) AS violet,\n    MIN(moran) AS trauma,\n    SUM(moran) AS melon,\n    AVG(moran) AS hooker\n  FROM cte_0\n  GROUP BY\n    goose,\n    metre,\n    cheque,\n    tablet\n),\ncte_2 AS (\n  SELECT\n    goose,\n    metre,\n    MAX(violet) AS sender,\n    AVG(hooker) AS parson\n  FROM cte_1\n  GROUP BY\n    model,\n    goose,\n    metre\n),\ncte_3 AS (\n  SELECT\n    MIN(parson) AS walker\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(walker) AS ridge,\n    SUM(walker) AS knife\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     goose,\n     metre,\n     cheque,\n     tablet,\n     moran\n|> AGGREGATE\n     MAX(moran) AS violet,\n     MIN(moran) AS trauma,\n     SUM(moran) AS melon,\n     AVG(moran) AS hooker\n   GROUP BY\n     goose,\n     metre,\n     cheque,\n     tablet\n|> AGGREGATE\n     MAX(violet) AS sender,\n     AVG(hooker) AS parson\n   GROUP BY\n     model,\n     goose,\n     metre\n|> AGGREGATE\n     MIN(parson) AS walker\n|> AGGREGATE\n     MAX(walker) AS ridge,\n     SUM(walker) AS knife\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"model",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    mickey,\n    bloke,\n    flake,\n    burton,\n    stump\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    mickey,\n    AVG(bloke) AS bloom,\n    MIN(bloke) AS murray,\n    COUNT(bloke) AS smart\n  FROM cte_0\n  GROUP BY\n    mickey\n),\ncte_2 AS (\n  SELECT\n    mickey,\n    SUM(smart) AS shrub,\n    MAX(bloom) AS rouge,\n    MAX(smart) AS costa\n  FROM cte_1\n  GROUP BY\n    mickey\n),\ncte_3 AS (\n  SELECT\n    mickey,\n    shrub,\n    AVG(costa) AS cellar\n  FROM cte_2\n  GROUP BY\n    patch,\n    mickey,\n    shrub,\n    rouge\n),\ncte_4 AS (\n  SELECT\n    mickey,\n    AVG(cellar) AS winner\n  FROM cte_3\n  GROUP BY\n    mickey,\n    shrub\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     mickey,\n     bloke,\n     flake,\n     burton,\n     stump\n|> AGGREGATE\n     AVG(bloke) AS bloom,\n     MIN(bloke) AS murray,\n     COUNT(bloke) AS smart\n   GROUP BY\n     mickey\n|> AGGREGATE\n     SUM(smart) AS shrub,\n     MAX(bloom) AS rouge,\n     MAX(smart) AS costa\n   GROUP BY\n     mickey\n|> AGGREGATE\n     AVG(costa) AS cellar\n   GROUP BY\n     patch,\n     mickey,\n     shrub,\n     rouge\n|> AGGREGATE\n     AVG(cellar) AS winner\n   GROUP BY\n     mickey,\n     shrub\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"patch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    slump,\n    chapel,\n    mince,\n    merger,\n    crust\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    slump,\n    SUM(crust) AS blade,\n    MIN(crust) AS ethic\n  FROM cte_0\n  GROUP BY\n    slump,\n    chapel,\n    mince,\n    merger\n),\ncte_2 AS (\n  SELECT\n    slump,\n    blade,\n    MAX(ethic) AS parade\n  FROM cte_1\n  GROUP BY\n    slump,\n    blade\n),\ncte_3 AS (\n  SELECT\n    slump,\n    AVG(parade) AS liquid,\n    MAX(parade) AS caste\n  FROM cte_2\n  GROUP BY\n    sense,\n    slump,\n    blade\n),\ncte_4 AS (\n  SELECT\n    slump,\n    AVG(caste) AS market,\n    COUNT(caste) AS ledge\n  FROM cte_3\n  GROUP BY\n    slump\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     slump,\n     chapel,\n     mince,\n     merger,\n     crust\n|> AGGREGATE\n     SUM(crust) AS blade,\n     MIN(crust) AS ethic\n   GROUP BY\n     slump,\n     chapel,\n     mince,\n     merger\n|> AGGREGATE\n     MAX(ethic) AS parade\n   GROUP BY\n     slump,\n     blade\n|> AGGREGATE\n     AVG(parade) AS liquid,\n     MAX(parade) AS caste\n   GROUP BY\n     sense,\n     slump,\n     blade\n|> AGGREGATE\n     AVG(caste) AS market,\n     COUNT(caste) AS ledge\n   GROUP BY\n     slump\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"sense",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sewer,\n    miller,\n    lunch,\n    flash,\n    rogue\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    sewer,\n    miller,\n    lunch,\n    AVG(rogue) AS remit\n  FROM cte_0\n  GROUP BY\n    sewer,\n    miller,\n    lunch,\n    flash\n),\ncte_2 AS (\n  SELECT\n    sewer,\n    miller,\n    lunch,\n    remit\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    sewer,\n    SUM(lunch) AS gossip,\n    MAX(lunch) AS appeal\n  FROM cte_2\n  GROUP BY\n    sewer,\n    miller,\n    motif\n),\ncte_4 AS (\n  SELECT\n    AVG(appeal) AS canary,\n    COUNT(appeal) AS belle,\n    MAX(appeal) AS period\n  FROM cte_3\n  GROUP BY\n    sewer,\n    gossip\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sewer,\n     miller,\n     lunch,\n     flash,\n     rogue\n|> AGGREGATE\n     AVG(rogue) AS remit\n   GROUP BY\n     sewer,\n     miller,\n     lunch,\n     flash\n|> SELECT\n     sewer,\n     miller,\n     lunch,\n     remit\n|> AGGREGATE\n     SUM(lunch) AS gossip,\n     MAX(lunch) AS appeal\n   GROUP BY\n     sewer,\n     miller,\n     motif\n|> AGGREGATE\n     AVG(appeal) AS canary,\n     COUNT(appeal) AS belle,\n     MAX(appeal) AS period\n   GROUP BY\n     sewer,\n     gossip\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"motif",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    debate,\n    stump,\n    phase,\n    walker,\n    pastry\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    debate,\n    COUNT(walker) AS pouch\n  FROM cte_0\n  GROUP BY\n    debate\n),\ncte_2 AS (\n  SELECT\n    AVG(pouch) AS shire,\n    SUM(pouch) AS factor,\n    SUM(debate) AS verge,\n    MAX(pouch) AS elbow,\n    COUNT(debate) AS venus\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    shire,\n    factor,\n    verge,\n    SUM(venus) AS crater\n  FROM cte_2\n  GROUP BY\n    shire,\n    chant,\n    factor,\n    verge\n),\ncte_4 AS (\n  SELECT\n    COUNT(shire) AS trough,\n    COUNT(crater) AS golfer,\n    MAX(shire) AS cobalt\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     debate,\n     stump,\n     phase,\n     walker,\n     pastry\n|> AGGREGATE\n     COUNT(walker) AS pouch\n   GROUP BY\n     debate\n|> AGGREGATE\n     AVG(pouch) AS shire,\n     SUM(pouch) AS factor,\n     SUM(debate) AS verge,\n     MAX(pouch) AS elbow,\n     COUNT(debate) AS venus\n|> AGGREGATE\n     SUM(venus) AS crater\n   GROUP BY\n     shire,\n     chant,\n     factor,\n     verge\n|> AGGREGATE\n     COUNT(shire) AS trough,\n     COUNT(crater) AS golfer,\n     MAX(shire) AS cobalt\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"chant",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    speech,\n    effort,\n    volume,\n    driver,\n    salute\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    speech,\n    effort,\n    MAX(driver) AS slough,\n    SUM(salute) AS attack,\n    COUNT(driver) AS salad\n  FROM cte_0\n  GROUP BY\n    speech,\n    effort,\n    volume\n),\ncte_2 AS (\n  SELECT\n    speech,\n    effort,\n    SUM(salad) AS queue,\n    AVG(salad) AS breach\n  FROM cte_1\n  GROUP BY\n    speech,\n    effort,\n    slough,\n    public\n),\ncte_3 AS (\n  SELECT\n    speech,\n    effort,\n    queue,\n    breach\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(effort) AS patent,\n    AVG(queue) AS unison\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     speech,\n     effort,\n     volume,\n     driver,\n     salute\n|> AGGREGATE\n     MAX(driver) AS slough,\n     SUM(salute) AS attack,\n     COUNT(driver) AS salad\n   GROUP BY\n     speech,\n     effort,\n     volume\n|> AGGREGATE\n     SUM(salad) AS queue,\n     AVG(salad) AS breach\n   GROUP BY\n     speech,\n     effort,\n     slough,\n     public\n|> SELECT\n     speech,\n     effort,\n     queue,\n     breach\n|> AGGREGATE\n     MAX(effort) AS patent,\n     AVG(queue) AS unison\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"public",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    decor,\n    monte,\n    knack,\n    hunter,\n    pilot\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    decor,\n    MAX(pilot) AS murmur,\n    AVG(pilot) AS woman,\n    MAX(hunter) AS demon,\n    MIN(knack) AS hound,\n    AVG(hunter) AS brazil\n  FROM cte_0\n  GROUP BY\n    decor,\n    monte\n),\ncte_2 AS (\n  SELECT\n    decor,\n    murmur,\n    woman,\n    MIN(hound) AS friend\n  FROM cte_1\n  GROUP BY\n    decor,\n    murmur,\n    peril,\n    woman\n),\ncte_3 AS (\n  SELECT\n    decor,\n    murmur,\n    woman\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(decor) AS merit,\n    MIN(woman) AS stare\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     decor,\n     monte,\n     knack,\n     hunter,\n     pilot\n|> AGGREGATE\n     MAX(pilot) AS murmur,\n     AVG(pilot) AS woman,\n     MAX(hunter) AS demon,\n     MIN(knack) AS hound,\n     AVG(hunter) AS brazil\n   GROUP BY\n     decor,\n     monte\n|> AGGREGATE\n     MIN(hound) AS friend\n   GROUP BY\n     decor,\n     murmur,\n     peril,\n     woman\n|> SELECT\n     decor,\n     murmur,\n     woman\n|> AGGREGATE\n     MIN(decor) AS merit,\n     MIN(woman) AS stare\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"peril",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    survey,\n    locus,\n    energy,\n    siege,\n    detail\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    survey,\n    locus,\n    energy,\n    siege,\n    detail\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    survey,\n    locus,\n    energy,\n    COUNT(siege) AS doubt\n  FROM cte_1\n  GROUP BY\n    survey,\n    locus,\n    energy\n),\ncte_3 AS (\n  SELECT\n    survey,\n    AVG(doubt) AS spine,\n    SUM(doubt) AS shark\n  FROM cte_2\n  GROUP BY\n    survey,\n    locus,\n    client,\n    energy\n),\ncte_4 AS (\n  SELECT\n    survey,\n    MIN(shark) AS collor,\n    MAX(shark) AS vanity\n  FROM cte_3\n  GROUP BY\n    survey,\n    spine\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     survey,\n     locus,\n     energy,\n     siege,\n     detail\n|> SELECT\n     survey,\n     locus,\n     energy,\n     siege,\n     detail\n|> AGGREGATE\n     COUNT(siege) AS doubt\n   GROUP BY\n     survey,\n     locus,\n     energy\n|> AGGREGATE\n     AVG(doubt) AS spine,\n     SUM(doubt) AS shark\n   GROUP BY\n     survey,\n     locus,\n     client,\n     energy\n|> AGGREGATE\n     MIN(shark) AS collor,\n     MAX(shark) AS vanity\n   GROUP BY\n     survey,\n     spine\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"client",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bliss,\n    puppy,\n    magnet,\n    grove,\n    alloy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    bliss,\n    puppy,\n    magnet,\n    MAX(grove) AS virgin,\n    MIN(alloy) AS picnic,\n    MAX(alloy) AS irony\n  FROM cte_0\n  GROUP BY\n    bliss,\n    puppy,\n    magnet\n),\ncte_2 AS (\n  SELECT\n    bliss,\n    COUNT(irony) AS review,\n    MAX(picnic) AS waste,\n    COUNT(virgin) AS grange\n  FROM cte_1\n  GROUP BY\n    bliss,\n    cotton,\n    puppy,\n    magnet\n),\ncte_3 AS (\n  SELECT\n    MIN(bliss) AS collar\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(collar) AS carrot,\n    MIN(collar) AS toilet\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bliss,\n     puppy,\n     magnet,\n     grove,\n     alloy\n|> AGGREGATE\n     MAX(grove) AS virgin,\n     MIN(alloy) AS picnic,\n     MAX(alloy) AS irony\n   GROUP BY\n     bliss,\n     puppy,\n     magnet\n|> AGGREGATE\n     COUNT(irony) AS review,\n     MAX(picnic) AS waste,\n     COUNT(virgin) AS grange\n   GROUP BY\n     bliss,\n     cotton,\n     puppy,\n     magnet\n|> AGGREGATE\n     MIN(bliss) AS collar\n|> AGGREGATE\n     MAX(collar) AS carrot,\n     MIN(collar) AS toilet\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"cotton",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    letter,\n    month,\n    moray,\n    tarmac,\n    shout\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    letter,\n    MAX(month) AS drain,\n    MIN(moray) AS dance,\n    SUM(tarmac) AS canon\n  FROM cte_0\n  GROUP BY\n    letter\n),\ncte_2 AS (\n  SELECT\n    letter,\n    MAX(canon) AS jumble,\n    SUM(drain) AS roach\n  FROM cte_1\n  GROUP BY\n    letter\n),\ncte_3 AS (\n  SELECT\n    letter,\n    jumble,\n    MAX(roach) AS volley,\n    COUNT(roach) AS wight\n  FROM cte_2\n  GROUP BY\n    michel,\n    letter,\n    jumble\n),\ncte_4 AS (\n  SELECT\n    MAX(volley) AS genre,\n    SUM(volley) AS thumb\n  FROM cte_3\n  GROUP BY\n    letter,\n    jumble\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     letter,\n     month,\n     moray,\n     tarmac,\n     shout\n|> AGGREGATE\n     MAX(month) AS drain,\n     MIN(moray) AS dance,\n     SUM(tarmac) AS canon\n   GROUP BY\n     letter\n|> AGGREGATE\n     MAX(canon) AS jumble,\n     SUM(drain) AS roach\n   GROUP BY\n     letter\n|> AGGREGATE\n     MAX(roach) AS volley,\n     COUNT(roach) AS wight\n   GROUP BY\n     michel,\n     letter,\n     jumble\n|> AGGREGATE\n     MAX(volley) AS genre,\n     SUM(volley) AS thumb\n   GROUP BY\n     letter,\n     jumble\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"michel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rifle,\n    limit,\n    train,\n    nature,\n    folder\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    rifle,\n    limit,\n    train,\n    nature,\n    COUNT(folder) AS gloss\n  FROM cte_0\n  GROUP BY\n    rifle,\n    limit,\n    train,\n    nature\n),\ncte_2 AS (\n  SELECT\n    AVG(gloss) AS ferry,\n    MIN(gloss) AS belle,\n    SUM(gloss) AS gorge\n  FROM cte_1\n  GROUP BY\n    rifle,\n    limit,\n    train,\n    nature,\n    chain\n),\ncte_3 AS (\n  SELECT\n    ferry,\n    SUM(gorge) AS throne,\n    MIN(belle) AS apollo\n  FROM cte_2\n  GROUP BY\n    ferry\n),\ncte_4 AS (\n  SELECT\n    COUNT(throne) AS madam\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rifle,\n     limit,\n     train,\n     nature,\n     folder\n|> AGGREGATE\n     COUNT(folder) AS gloss\n   GROUP BY\n     rifle,\n     limit,\n     train,\n     nature\n|> AGGREGATE\n     AVG(gloss) AS ferry,\n     MIN(gloss) AS belle,\n     SUM(gloss) AS gorge\n   GROUP BY\n     rifle,\n     limit,\n     train,\n     nature,\n     chain\n|> AGGREGATE\n     SUM(gorge) AS throne,\n     MIN(belle) AS apollo\n   GROUP BY\n     ferry\n|> AGGREGATE\n     COUNT(throne) AS madam\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"chain",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    death,\n    sonnet,\n    dalton,\n    affect,\n    accord\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    death,\n    sonnet,\n    dalton,\n    COUNT(accord) AS alarm,\n    COUNT(affect) AS flare\n  FROM cte_0\n  GROUP BY\n    death,\n    sonnet,\n    dalton\n),\ncte_2 AS (\n  SELECT\n    death,\n    COUNT(alarm) AS float,\n    SUM(alarm) AS pastor,\n    MAX(alarm) AS stress,\n    COUNT(dalton) AS hunger\n  FROM cte_1\n  GROUP BY\n    death,\n    barrel,\n    sonnet\n),\ncte_3 AS (\n  SELECT\n    death,\n    float,\n    pastor\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    death,\n    AVG(pastor) AS arcade\n  FROM cte_3\n  GROUP BY\n    death\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     death,\n     sonnet,\n     dalton,\n     affect,\n     accord\n|> AGGREGATE\n     COUNT(accord) AS alarm,\n     COUNT(affect) AS flare\n   GROUP BY\n     death,\n     sonnet,\n     dalton\n|> AGGREGATE\n     COUNT(alarm) AS float,\n     SUM(alarm) AS pastor,\n     MAX(alarm) AS stress,\n     COUNT(dalton) AS hunger\n   GROUP BY\n     death,\n     barrel,\n     sonnet\n|> SELECT\n     death,\n     float,\n     pastor\n|> AGGREGATE\n     AVG(pastor) AS arcade\n   GROUP BY\n     death\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"barrel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    praise,\n    paddy,\n    sword,\n    vinyl,\n    carer\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    praise,\n    paddy,\n    SUM(carer) AS delay,\n    AVG(sword) AS sewer,\n    SUM(sword) AS drill,\n    COUNT(sword) AS crime,\n    MAX(carer) AS adobe,\n    MIN(carer) AS scorn\n  FROM cte_0\n  GROUP BY\n    praise,\n    paddy\n),\ncte_2 AS (\n  SELECT\n    praise,\n    paddy,\n    COUNT(crime) AS butter\n  FROM cte_1\n  GROUP BY\n    quartz,\n    praise,\n    paddy\n),\ncte_3 AS (\n  SELECT\n    MAX(butter) AS replay,\n    COUNT(butter) AS stream\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    replay,\n    stream\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     praise,\n     paddy,\n     sword,\n     vinyl,\n     carer\n|> AGGREGATE\n     SUM(carer) AS delay,\n     AVG(sword) AS sewer,\n     SUM(sword) AS drill,\n     COUNT(sword) AS crime,\n     MAX(carer) AS adobe,\n     MIN(carer) AS scorn\n   GROUP BY\n     praise,\n     paddy\n|> AGGREGATE\n     COUNT(crime) AS butter\n   GROUP BY\n     quartz,\n     praise,\n     paddy\n|> AGGREGATE\n     MAX(butter) AS replay,\n     COUNT(butter) AS stream\n|> SELECT\n     replay,\n     stream\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"quartz",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    victor,\n    spread,\n    torch,\n    guard,\n    monkey\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    victor,\n    spread,\n    MAX(monkey) AS topaz,\n    COUNT(monkey) AS rarity,\n    MIN(monkey) AS pitch,\n    SUM(monkey) AS clutch\n  FROM cte_0\n  GROUP BY\n    victor,\n    spread,\n    torch,\n    guard\n),\ncte_2 AS (\n  SELECT\n    victor,\n    spread,\n    MIN(pitch) AS ozone\n  FROM cte_1\n  GROUP BY\n    victor,\n    spread,\n    width\n),\ncte_3 AS (\n  SELECT\n    COUNT(ozone) AS patron,\n    COUNT(victor) AS morse\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    patron,\n    MAX(morse) AS mutant\n  FROM cte_3\n  GROUP BY\n    patron\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     victor,\n     spread,\n     torch,\n     guard,\n     monkey\n|> AGGREGATE\n     MAX(monkey) AS topaz,\n     COUNT(monkey) AS rarity,\n     MIN(monkey) AS pitch,\n     SUM(monkey) AS clutch\n   GROUP BY\n     victor,\n     spread,\n     torch,\n     guard\n|> AGGREGATE\n     MIN(pitch) AS ozone\n   GROUP BY\n     victor,\n     spread,\n     width\n|> AGGREGATE\n     COUNT(ozone) AS patron,\n     COUNT(victor) AS morse\n|> AGGREGATE\n     MAX(morse) AS mutant\n   GROUP BY\n     patron\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"width",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    pulse,\n    piety,\n    master,\n    snail,\n    sting\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    pulse,\n    piety,\n    master,\n    MAX(sting) AS behalf\n  FROM cte_0\n  GROUP BY\n    pulse,\n    piety,\n    master\n),\ncte_2 AS (\n  SELECT\n    pulse,\n    MIN(behalf) AS octave,\n    MAX(behalf) AS billy,\n    COUNT(master) AS coffee,\n    MAX(master) AS donkey,\n    SUM(behalf) AS rifle\n  FROM cte_1\n  GROUP BY\n    pulse,\n    dollar,\n    piety\n),\ncte_3 AS (\n  SELECT\n    SUM(octave) AS clutch,\n    AVG(rifle) AS sunset\n  FROM cte_2\n  GROUP BY\n    pulse\n),\ncte_4 AS (\n  SELECT\n    clutch,\n    sunset\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     pulse,\n     piety,\n     master,\n     snail,\n     sting\n|> AGGREGATE\n     MAX(sting) AS behalf\n   GROUP BY\n     pulse,\n     piety,\n     master\n|> AGGREGATE\n     MIN(behalf) AS octave,\n     MAX(behalf) AS billy,\n     COUNT(master) AS coffee,\n     MAX(master) AS donkey,\n     SUM(behalf) AS rifle\n   GROUP BY\n     pulse,\n     dollar,\n     piety\n|> AGGREGATE\n     SUM(octave) AS clutch,\n     AVG(rifle) AS sunset\n   GROUP BY\n     pulse\n|> SELECT\n     clutch,\n     sunset\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"dollar",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tunnel,\n    metre,\n    sister,\n    fridge,\n    heyday\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    tunnel,\n    MIN(heyday) AS mount,\n    MIN(sister) AS square,\n    SUM(heyday) AS helper\n  FROM cte_0\n  GROUP BY\n    tunnel\n),\ncte_2 AS (\n  SELECT\n    tunnel,\n    AVG(square) AS closet,\n    MIN(helper) AS gleam\n  FROM cte_1\n  GROUP BY\n    tunnel,\n    mount\n),\ncte_3 AS (\n  SELECT\n    tunnel,\n    closet,\n    SUM(gleam) AS twist\n  FROM cte_2\n  GROUP BY\n    tunnel,\n    liquid,\n    closet\n),\ncte_4 AS (\n  SELECT\n    AVG(closet) AS tarmac,\n    SUM(closet) AS elaine,\n    COUNT(closet) AS kelly\n  FROM cte_3\n  GROUP BY\n    tunnel\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tunnel,\n     metre,\n     sister,\n     fridge,\n     heyday\n|> AGGREGATE\n     MIN(heyday) AS mount,\n     MIN(sister) AS square,\n     SUM(heyday) AS helper\n   GROUP BY\n     tunnel\n|> AGGREGATE\n     AVG(square) AS closet,\n     MIN(helper) AS gleam\n   GROUP BY\n     tunnel,\n     mount\n|> AGGREGATE\n     SUM(gleam) AS twist\n   GROUP BY\n     tunnel,\n     liquid,\n     closet\n|> AGGREGATE\n     AVG(closet) AS tarmac,\n     SUM(closet) AS elaine,\n     COUNT(closet) AS kelly\n   GROUP BY\n     tunnel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"liquid",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    resin,\n    yogurt,\n    arena,\n    glass,\n    magic\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    resin,\n    yogurt,\n    AVG(magic) AS combo\n  FROM cte_0\n  GROUP BY\n    resin,\n    yogurt,\n    arena\n),\ncte_2 AS (\n  SELECT\n    resin,\n    yogurt,\n    combo\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    resin,\n    yogurt,\n    AVG(combo) AS header,\n    MAX(combo) AS brown,\n    SUM(combo) AS burgh,\n    COUNT(combo) AS heroin\n  FROM cte_2\n  GROUP BY\n    diary,\n    resin,\n    yogurt\n),\ncte_4 AS (\n  SELECT\n    MAX(heroin) AS slump\n  FROM cte_3\n  GROUP BY\n    resin,\n    yogurt,\n    header,\n    brown\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     resin,\n     yogurt,\n     arena,\n     glass,\n     magic\n|> AGGREGATE\n     AVG(magic) AS combo\n   GROUP BY\n     resin,\n     yogurt,\n     arena\n|> SELECT\n     resin,\n     yogurt,\n     combo\n|> AGGREGATE\n     AVG(combo) AS header,\n     MAX(combo) AS brown,\n     SUM(combo) AS burgh,\n     COUNT(combo) AS heroin\n   GROUP BY\n     diary,\n     resin,\n     yogurt\n|> AGGREGATE\n     MAX(heroin) AS slump\n   GROUP BY\n     resin,\n     yogurt,\n     header,\n     brown\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"diary",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    organ,\n    ankle,\n    rifle,\n    juice,\n    blade\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    organ,\n    ankle,\n    rifle,\n    juice,\n    blade\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    organ,\n    ankle,\n    rifle,\n    juice,\n    AVG(blade) AS oliver\n  FROM cte_1\n  GROUP BY\n    organ,\n    ankle,\n    rifle,\n    juice\n),\ncte_3 AS (\n  SELECT\n    SUM(oliver) AS stake,\n    MIN(rifle) AS brooke\n  FROM cte_2\n  GROUP BY\n    organ,\n    anthem\n),\ncte_4 AS (\n  SELECT\n    stake,\n    COUNT(brooke) AS cuckoo,\n    AVG(brooke) AS groin,\n    SUM(brooke) AS trunk\n  FROM cte_3\n  GROUP BY\n    stake\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     organ,\n     ankle,\n     rifle,\n     juice,\n     blade\n|> SELECT\n     organ,\n     ankle,\n     rifle,\n     juice,\n     blade\n|> AGGREGATE\n     AVG(blade) AS oliver\n   GROUP BY\n     organ,\n     ankle,\n     rifle,\n     juice\n|> AGGREGATE\n     SUM(oliver) AS stake,\n     MIN(rifle) AS brooke\n   GROUP BY\n     organ,\n     anthem\n|> AGGREGATE\n     COUNT(brooke) AS cuckoo,\n     AVG(brooke) AS groin,\n     SUM(brooke) AS trunk\n   GROUP BY\n     stake\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"anthem",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    breeze,\n    whisky,\n    search,\n    sermon,\n    sherry\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    breeze,\n    whisky,\n    MIN(sherry) AS jungle\n  FROM cte_0\n  GROUP BY\n    breeze,\n    whisky,\n    search,\n    sermon\n),\ncte_2 AS (\n  SELECT\n    breeze,\n    whisky,\n    jungle\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    breeze,\n    whisky,\n    COUNT(jungle) AS steen,\n    SUM(jungle) AS stroke,\n    MAX(jungle) AS vodka\n  FROM cte_2\n  GROUP BY\n    breeze,\n    satin,\n    whisky\n),\ncte_4 AS (\n  SELECT\n    breeze,\n    whisky,\n    SUM(stroke) AS daisy\n  FROM cte_3\n  GROUP BY\n    breeze,\n    whisky,\n    steen\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     breeze,\n     whisky,\n     search,\n     sermon,\n     sherry\n|> AGGREGATE\n     MIN(sherry) AS jungle\n   GROUP BY\n     breeze,\n     whisky,\n     search,\n     sermon\n|> SELECT\n     breeze,\n     whisky,\n     jungle\n|> AGGREGATE\n     COUNT(jungle) AS steen,\n     SUM(jungle) AS stroke,\n     MAX(jungle) AS vodka\n   GROUP BY\n     breeze,\n     satin,\n     whisky\n|> AGGREGATE\n     SUM(stroke) AS daisy\n   GROUP BY\n     breeze,\n     whisky,\n     steen\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:28,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"satin",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bleach,\n    lorry,\n    manor,\n    scrap,\n    vault\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    bleach,\n    lorry,\n    manor,\n    MIN(vault) AS actor\n  FROM cte_0\n  GROUP BY\n    bleach,\n    lorry,\n    manor\n),\ncte_2 AS (\n  SELECT\n    AVG(manor) AS groan,\n    COUNT(manor) AS melon\n  FROM cte_1\n  GROUP BY\n    bleach,\n    lorry\n),\ncte_3 AS (\n  SELECT\n    groan,\n    SUM(melon) AS tyler,\n    COUNT(melon) AS bubble\n  FROM cte_2\n  GROUP BY\n    groan,\n    outlet\n),\ncte_4 AS (\n  SELECT\n    COUNT(bubble) AS uptake,\n    COUNT(groan) AS fellow,\n    AVG(groan) AS coral,\n    MIN(tyler) AS whore\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bleach,\n     lorry,\n     manor,\n     scrap,\n     vault\n|> AGGREGATE\n     MIN(vault) AS actor\n   GROUP BY\n     bleach,\n     lorry,\n     manor\n|> AGGREGATE\n     AVG(manor) AS groan,\n     COUNT(manor) AS melon\n   GROUP BY\n     bleach,\n     lorry\n|> AGGREGATE\n     SUM(melon) AS tyler,\n     COUNT(melon) AS bubble\n   GROUP BY\n     groan,\n     outlet\n|> AGGREGATE\n     COUNT(bubble) AS uptake,\n     COUNT(groan) AS fellow,\n     AVG(groan) AS coral,\n     MIN(tyler) AS whore\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"outlet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bishop,\n    right,\n    grand,\n    actor,\n    baton\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    bishop,\n    right,\n    grand,\n    MAX(baton) AS noble\n  FROM cte_0\n  GROUP BY\n    bishop,\n    right,\n    grand,\n    actor\n),\ncte_2 AS (\n  SELECT\n    bishop,\n    COUNT(noble) AS horne,\n    MIN(noble) AS mafia,\n    MAX(noble) AS organ,\n    AVG(noble) AS frame\n  FROM cte_1\n  GROUP BY\n    bishop,\n    right,\n    female,\n    grand\n),\ncte_3 AS (\n  SELECT\n    bishop,\n    horne,\n    mafia,\n    organ,\n    frame\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    bishop,\n    horne\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bishop,\n     right,\n     grand,\n     actor,\n     baton\n|> AGGREGATE\n     MAX(baton) AS noble\n   GROUP BY\n     bishop,\n     right,\n     grand,\n     actor\n|> AGGREGATE\n     COUNT(noble) AS horne,\n     MIN(noble) AS mafia,\n     MAX(noble) AS organ,\n     AVG(noble) AS frame\n   GROUP BY\n     bishop,\n     right,\n     female,\n     grand\n|> SELECT\n     bishop,\n     horne,\n     mafia,\n     organ,\n     frame\n|> SELECT\n     bishop,\n     horne\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"female",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    image,\n    roller,\n    buddy,\n    timber,\n    cabin\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    image,\n    roller,\n    MAX(cabin) AS court,\n    MIN(cabin) AS ladder,\n    AVG(cabin) AS luxury\n  FROM cte_0\n  GROUP BY\n    image,\n    roller,\n    buddy,\n    timber\n),\ncte_2 AS (\n  SELECT\n    image,\n    roller,\n    MAX(luxury) AS demise\n  FROM cte_1\n  GROUP BY\n    image,\n    roller,\n    court,\n    ladder,\n    animal\n),\ncte_3 AS (\n  SELECT\n    MIN(roller) AS grape,\n    MIN(demise) AS laity\n  FROM cte_2\n  GROUP BY\n    image\n),\ncte_4 AS (\n  SELECT\n    SUM(laity) AS peace\n  FROM cte_3\n  GROUP BY\n    grape\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     image,\n     roller,\n     buddy,\n     timber,\n     cabin\n|> AGGREGATE\n     MAX(cabin) AS court,\n     MIN(cabin) AS ladder,\n     AVG(cabin) AS luxury\n   GROUP BY\n     image,\n     roller,\n     buddy,\n     timber\n|> AGGREGATE\n     MAX(luxury) AS demise\n   GROUP BY\n     image,\n     roller,\n     court,\n     ladder,\n     animal\n|> AGGREGATE\n     MIN(roller) AS grape,\n     MIN(demise) AS laity\n   GROUP BY\n     image\n|> AGGREGATE\n     SUM(laity) AS peace\n   GROUP BY\n     grape\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"animal",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    report,\n    picnic,\n    craig,\n    expert,\n    mercy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    report,\n    picnic,\n    craig,\n    COUNT(mercy) AS rattle,\n    MIN(mercy) AS night,\n    SUM(mercy) AS gully,\n    AVG(mercy) AS timber\n  FROM cte_0\n  GROUP BY\n    report,\n    picnic,\n    craig,\n    expert\n),\ncte_2 AS (\n  SELECT\n    report,\n    MAX(gully) AS puzzle,\n    SUM(night) AS money\n  FROM cte_1\n  GROUP BY\n    orchid,\n    report\n),\ncte_3 AS (\n  SELECT\n    report,\n    MIN(money) AS melon\n  FROM cte_2\n  GROUP BY\n    report\n),\ncte_4 AS (\n  SELECT\n    MAX(melon) AS synod\n  FROM cte_3\n  GROUP BY\n    report\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     report,\n     picnic,\n     craig,\n     expert,\n     mercy\n|> AGGREGATE\n     COUNT(mercy) AS rattle,\n     MIN(mercy) AS night,\n     SUM(mercy) AS gully,\n     AVG(mercy) AS timber\n   GROUP BY\n     report,\n     picnic,\n     craig,\n     expert\n|> AGGREGATE\n     MAX(gully) AS puzzle,\n     SUM(night) AS money\n   GROUP BY\n     orchid,\n     report\n|> AGGREGATE\n     MIN(money) AS melon\n   GROUP BY\n     report\n|> AGGREGATE\n     MAX(melon) AS synod\n   GROUP BY\n     report\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"orchid",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    region,\n    floor,\n    berlin,\n    statue,\n    soccer\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    region,\n    floor,\n    berlin,\n    MIN(soccer) AS pasta,\n    AVG(statue) AS bamboo\n  FROM cte_0\n  GROUP BY\n    region,\n    floor,\n    berlin\n),\ncte_2 AS (\n  SELECT\n    region,\n    MAX(bamboo) AS plough,\n    MIN(bamboo) AS burrow,\n    AVG(bamboo) AS piano\n  FROM cte_1\n  GROUP BY\n    region,\n    floor,\n    berlin,\n    ghetto,\n    pasta\n),\ncte_3 AS (\n  SELECT\n    COUNT(burrow) AS grasp\n  FROM cte_2\n  GROUP BY\n    region\n),\ncte_4 AS (\n  SELECT\n    MIN(grasp) AS revolt,\n    AVG(grasp) AS rouge\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     region,\n     floor,\n     berlin,\n     statue,\n     soccer\n|> AGGREGATE\n     MIN(soccer) AS pasta,\n     AVG(statue) AS bamboo\n   GROUP BY\n     region,\n     floor,\n     berlin\n|> AGGREGATE\n     MAX(bamboo) AS plough,\n     MIN(bamboo) AS burrow,\n     AVG(bamboo) AS piano\n   GROUP BY\n     region,\n     floor,\n     berlin,\n     ghetto,\n     pasta\n|> AGGREGATE\n     COUNT(burrow) AS grasp\n   GROUP BY\n     region\n|> AGGREGATE\n     MIN(grasp) AS revolt,\n     AVG(grasp) AS rouge\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"ghetto",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    peach,\n    stage,\n    shoot,\n    sting,\n    stigma\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    peach,\n    stage,\n    shoot,\n    MIN(stigma) AS guide,\n    COUNT(sting) AS maxim,\n    AVG(sting) AS tarmac\n  FROM cte_0\n  GROUP BY\n    peach,\n    stage,\n    shoot\n),\ncte_2 AS (\n  SELECT\n    peach,\n    AVG(tarmac) AS clergy,\n    SUM(tarmac) AS bundle\n  FROM cte_1\n  GROUP BY\n    peach,\n    stage,\n    shoot,\n    disco,\n    guide,\n    maxim\n),\ncte_3 AS (\n  SELECT\n    SUM(clergy) AS pledge,\n    SUM(peach) AS strat\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(pledge) AS island\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     peach,\n     stage,\n     shoot,\n     sting,\n     stigma\n|> AGGREGATE\n     MIN(stigma) AS guide,\n     COUNT(sting) AS maxim,\n     AVG(sting) AS tarmac\n   GROUP BY\n     peach,\n     stage,\n     shoot\n|> AGGREGATE\n     AVG(tarmac) AS clergy,\n     SUM(tarmac) AS bundle\n   GROUP BY\n     peach,\n     stage,\n     shoot,\n     disco,\n     guide,\n     maxim\n|> AGGREGATE\n     SUM(clergy) AS pledge,\n     SUM(peach) AS strat\n|> AGGREGATE\n     MAX(pledge) AS island\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"disco",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    coral,\n    palmer,\n    author,\n    farce,\n    canvas\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    coral,\n    palmer,\n    author,\n    farce,\n    COUNT(canvas) AS rudder,\n    MIN(canvas) AS draft\n  FROM cte_0\n  GROUP BY\n    coral,\n    palmer,\n    author,\n    farce\n),\ncte_2 AS (\n  SELECT\n    coral,\n    palmer,\n    author,\n    farce\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    SUM(coral) AS advent,\n    MAX(author) AS hogan\n  FROM cte_2\n  GROUP BY\n    donna\n),\ncte_4 AS (\n  SELECT\n    AVG(hogan) AS shirt,\n    MIN(advent) AS finger,\n    AVG(advent) AS phase,\n    SUM(hogan) AS verse\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     coral,\n     palmer,\n     author,\n     farce,\n     canvas\n|> AGGREGATE\n     COUNT(canvas) AS rudder,\n     MIN(canvas) AS draft\n   GROUP BY\n     coral,\n     palmer,\n     author,\n     farce\n|> SELECT\n     coral,\n     palmer,\n     author,\n     farce\n|> AGGREGATE\n     SUM(coral) AS advent,\n     MAX(author) AS hogan\n   GROUP BY\n     donna\n|> AGGREGATE\n     AVG(hogan) AS shirt,\n     MIN(advent) AS finger,\n     AVG(advent) AS phase,\n     SUM(hogan) AS verse\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"donna",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    daddy,\n    fauna,\n    crown,\n    clown,\n    garvey\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    daddy,\n    fauna,\n    crown,\n    AVG(garvey) AS plant,\n    SUM(garvey) AS cheek,\n    COUNT(garvey) AS glass\n  FROM cte_0\n  GROUP BY\n    daddy,\n    fauna,\n    crown,\n    clown\n),\ncte_2 AS (\n  SELECT\n    daddy,\n    AVG(glass) AS spread,\n    SUM(plant) AS court,\n    SUM(fauna) AS toast\n  FROM cte_1\n  GROUP BY\n    thread,\n    daddy\n),\ncte_3 AS (\n  SELECT\n    daddy,\n    spread,\n    court,\n    toast\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    daddy,\n    spread,\n    court\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     daddy,\n     fauna,\n     crown,\n     clown,\n     garvey\n|> AGGREGATE\n     AVG(garvey) AS plant,\n     SUM(garvey) AS cheek,\n     COUNT(garvey) AS glass\n   GROUP BY\n     daddy,\n     fauna,\n     crown,\n     clown\n|> AGGREGATE\n     AVG(glass) AS spread,\n     SUM(plant) AS court,\n     SUM(fauna) AS toast\n   GROUP BY\n     thread,\n     daddy\n|> SELECT\n     daddy,\n     spread,\n     court,\n     toast\n|> SELECT\n     daddy,\n     spread,\n     court\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"thread",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bloke,\n    hassle,\n    cabin,\n    morale,\n    strap\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    bloke,\n    hassle,\n    MIN(strap) AS glare,\n    COUNT(morale) AS queue,\n    SUM(morale) AS sleep,\n    SUM(cabin) AS sheet,\n    COUNT(strap) AS cellar,\n    MIN(morale) AS caesar\n  FROM cte_0\n  GROUP BY\n    bloke,\n    hassle\n),\ncte_2 AS (\n  SELECT\n    bloke,\n    hassle,\n    MAX(sleep) AS curve\n  FROM cte_1\n  GROUP BY\n    cowboy,\n    bloke,\n    hassle,\n    glare\n),\ncte_3 AS (\n  SELECT\n    bloke,\n    hassle,\n    curve\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(bloke) AS beach\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bloke,\n     hassle,\n     cabin,\n     morale,\n     strap\n|> AGGREGATE\n     MIN(strap) AS glare,\n     COUNT(morale) AS queue,\n     SUM(morale) AS sleep,\n     SUM(cabin) AS sheet,\n     COUNT(strap) AS cellar,\n     MIN(morale) AS caesar\n   GROUP BY\n     bloke,\n     hassle\n|> AGGREGATE\n     MAX(sleep) AS curve\n   GROUP BY\n     cowboy,\n     bloke,\n     hassle,\n     glare\n|> SELECT\n     bloke,\n     hassle,\n     curve\n|> AGGREGATE\n     COUNT(bloke) AS beach\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"cowboy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:33,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    manner,\n    treaty,\n    pixel,\n    helium,\n    elaine\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    manner,\n    treaty,\n    pixel,\n    COUNT(helium) AS drink,\n    COUNT(elaine) AS field,\n    SUM(elaine) AS drawer\n  FROM cte_0\n  GROUP BY\n    manner,\n    treaty,\n    pixel\n),\ncte_2 AS (\n  SELECT\n    AVG(treaty) AS blouse,\n    COUNT(drink) AS greek\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    blouse,\n    COUNT(greek) AS chuck\n  FROM cte_2\n  GROUP BY\n    blouse,\n    stand\n),\ncte_4 AS (\n  SELECT\n    COUNT(chuck) AS lawson,\n    COUNT(blouse) AS night,\n    AVG(chuck) AS shoot,\n    MIN(chuck) AS drill\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     manner,\n     treaty,\n     pixel,\n     helium,\n     elaine\n|> AGGREGATE\n     COUNT(helium) AS drink,\n     COUNT(elaine) AS field,\n     SUM(elaine) AS drawer\n   GROUP BY\n     manner,\n     treaty,\n     pixel\n|> AGGREGATE\n     AVG(treaty) AS blouse,\n     COUNT(drink) AS greek\n|> AGGREGATE\n     COUNT(greek) AS chuck\n   GROUP BY\n     blouse,\n     stand\n|> AGGREGATE\n     COUNT(chuck) AS lawson,\n     COUNT(blouse) AS night,\n     AVG(chuck) AS shoot,\n     MIN(chuck) AS drill\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"stand",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    blanc,\n    model,\n    patch,\n    relief,\n    build\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    blanc,\n    model,\n    patch,\n    AVG(relief) AS dummy,\n    AVG(build) AS street,\n    SUM(build) AS rally\n  FROM cte_0\n  GROUP BY\n    blanc,\n    model,\n    patch\n),\ncte_2 AS (\n  SELECT\n    blanc,\n    COUNT(rally) AS chile,\n    MAX(rally) AS writer\n  FROM cte_1\n  GROUP BY\n    blanc,\n    model,\n    patch,\n    owner,\n    dummy,\n    street\n),\ncte_3 AS (\n  SELECT\n    blanc,\n    chile\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    blanc,\n    AVG(chile) AS remand\n  FROM cte_3\n  GROUP BY\n    blanc\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     blanc,\n     model,\n     patch,\n     relief,\n     build\n|> AGGREGATE\n     AVG(relief) AS dummy,\n     AVG(build) AS street,\n     SUM(build) AS rally\n   GROUP BY\n     blanc,\n     model,\n     patch\n|> AGGREGATE\n     COUNT(rally) AS chile,\n     MAX(rally) AS writer\n   GROUP BY\n     blanc,\n     model,\n     patch,\n     owner,\n     dummy,\n     street\n|> SELECT\n     blanc,\n     chile\n|> AGGREGATE\n     AVG(chile) AS remand\n   GROUP BY\n     blanc\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"owner",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:35,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    salute,\n    goblin,\n    golfer,\n    stench,\n    drawer\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    salute,\n    MAX(goblin) AS henry,\n    COUNT(stench) AS genus,\n    MIN(golfer) AS movie,\n    SUM(golfer) AS dismay,\n    COUNT(drawer) AS creed\n  FROM cte_0\n  GROUP BY\n    salute\n),\ncte_2 AS (\n  SELECT\n    salute,\n    henry,\n    genus,\n    MIN(creed) AS pride\n  FROM cte_1\n  GROUP BY\n    salute,\n    henry,\n    genus,\n    crime,\n    movie\n),\ncte_3 AS (\n  SELECT\n    AVG(henry) AS client,\n    SUM(pride) AS darwin\n  FROM cte_2\n  GROUP BY\n    salute\n),\ncte_4 AS (\n  SELECT\n    client,\n    darwin\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     salute,\n     goblin,\n     golfer,\n     stench,\n     drawer\n|> AGGREGATE\n     MAX(goblin) AS henry,\n     COUNT(stench) AS genus,\n     MIN(golfer) AS movie,\n     SUM(golfer) AS dismay,\n     COUNT(drawer) AS creed\n   GROUP BY\n     salute\n|> AGGREGATE\n     MIN(creed) AS pride\n   GROUP BY\n     salute,\n     henry,\n     genus,\n     crime,\n     movie\n|> AGGREGATE\n     AVG(henry) AS client,\n     SUM(pride) AS darwin\n   GROUP BY\n     salute\n|> SELECT\n     client,\n     darwin\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"crime",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    roach,\n    jacket,\n    notion,\n    chart,\n    ethel\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    roach,\n    jacket,\n    notion,\n    MIN(chart) AS forum\n  FROM cte_0\n  GROUP BY\n    roach,\n    jacket,\n    notion\n),\ncte_2 AS (\n  SELECT\n    roach,\n    COUNT(notion) AS tract,\n    MIN(jacket) AS grave,\n    SUM(jacket) AS focus,\n    COUNT(forum) AS crash,\n    COUNT(jacket) AS costa\n  FROM cte_1\n  GROUP BY\n    roach,\n    share\n),\ncte_3 AS (\n  SELECT\n    roach,\n    COUNT(grave) AS nether,\n    SUM(crash) AS strain\n  FROM cte_2\n  GROUP BY\n    roach\n),\ncte_4 AS (\n  SELECT\n    roach,\n    nether\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     roach,\n     jacket,\n     notion,\n     chart,\n     ethel\n|> AGGREGATE\n     MIN(chart) AS forum\n   GROUP BY\n     roach,\n     jacket,\n     notion\n|> AGGREGATE\n     COUNT(notion) AS tract,\n     MIN(jacket) AS grave,\n     SUM(jacket) AS focus,\n     COUNT(forum) AS crash,\n     COUNT(jacket) AS costa\n   GROUP BY\n     roach,\n     share\n|> AGGREGATE\n     COUNT(grave) AS nether,\n     SUM(crash) AS strain\n   GROUP BY\n     roach\n|> SELECT\n     roach,\n     nether\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"share",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    moment,\n    lender,\n    tallis,\n    brick,\n    finale\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    moment,\n    lender,\n    tallis,\n    brick,\n    SUM(finale) AS cutter,\n    MIN(finale) AS brute\n  FROM cte_0\n  GROUP BY\n    moment,\n    lender,\n    tallis,\n    brick\n),\ncte_2 AS (\n  SELECT\n    AVG(brute) AS little,\n    AVG(cutter) AS guild\n  FROM cte_1\n  GROUP BY\n    moment,\n    lender,\n    tallis,\n    brick,\n    piano\n),\ncte_3 AS (\n  SELECT\n    AVG(guild) AS sender,\n    COUNT(little) AS tally\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(tally) AS strand,\n    MIN(tally) AS patch\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     moment,\n     lender,\n     tallis,\n     brick,\n     finale\n|> AGGREGATE\n     SUM(finale) AS cutter,\n     MIN(finale) AS brute\n   GROUP BY\n     moment,\n     lender,\n     tallis,\n     brick\n|> AGGREGATE\n     AVG(brute) AS little,\n     AVG(cutter) AS guild\n   GROUP BY\n     moment,\n     lender,\n     tallis,\n     brick,\n     piano\n|> AGGREGATE\n     AVG(guild) AS sender,\n     COUNT(little) AS tally\n|> AGGREGATE\n     SUM(tally) AS strand,\n     MIN(tally) AS patch\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"piano",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    aroma,\n    seller,\n    voice,\n    idiot,\n    gutter\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    aroma,\n    seller,\n    SUM(gutter) AS class,\n    MAX(idiot) AS crewe\n  FROM cte_0\n  GROUP BY\n    aroma,\n    seller,\n    voice\n),\ncte_2 AS (\n  SELECT\n    aroma,\n    seller,\n    MIN(crewe) AS midst,\n    SUM(class) AS swamp,\n    COUNT(class) AS strat,\n    SUM(crewe) AS rocket\n  FROM cte_1\n  GROUP BY\n    aroma,\n    clutch,\n    seller\n),\ncte_3 AS (\n  SELECT\n    MIN(midst) AS synod,\n    MIN(seller) AS chess\n  FROM cte_2\n  GROUP BY\n    aroma\n),\ncte_4 AS (\n  SELECT\n    synod,\n    chess\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     aroma,\n     seller,\n     voice,\n     idiot,\n     gutter\n|> AGGREGATE\n     SUM(gutter) AS class,\n     MAX(idiot) AS crewe\n   GROUP BY\n     aroma,\n     seller,\n     voice\n|> AGGREGATE\n     MIN(crewe) AS midst,\n     SUM(class) AS swamp,\n     COUNT(class) AS strat,\n     SUM(crewe) AS rocket\n   GROUP BY\n     aroma,\n     clutch,\n     seller\n|> AGGREGATE\n     MIN(midst) AS synod,\n     MIN(seller) AS chess\n   GROUP BY\n     aroma\n|> SELECT\n     synod,\n     chess\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"clutch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    scout,\n    flask,\n    runner,\n    volume,\n    ethos\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    scout,\n    flask,\n    runner,\n    volume\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    scout,\n    flask,\n    AVG(volume) AS oxygen,\n    MIN(volume) AS bakery\n  FROM cte_1\n  GROUP BY\n    scout,\n    flask,\n    runner\n),\ncte_3 AS (\n  SELECT\n    scout,\n    flask,\n    AVG(bakery) AS couch\n  FROM cte_2\n  GROUP BY\n    scout,\n    flask,\n    oxygen,\n    catch\n),\ncte_4 AS (\n  SELECT\n    scout,\n    MAX(flask) AS caesar,\n    AVG(couch) AS whisky,\n    MAX(couch) AS woman\n  FROM cte_3\n  GROUP BY\n    scout\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     scout,\n     flask,\n     runner,\n     volume,\n     ethos\n|> SELECT\n     scout,\n     flask,\n     runner,\n     volume\n|> AGGREGATE\n     AVG(volume) AS oxygen,\n     MIN(volume) AS bakery\n   GROUP BY\n     scout,\n     flask,\n     runner\n|> AGGREGATE\n     AVG(bakery) AS couch\n   GROUP BY\n     scout,\n     flask,\n     oxygen,\n     catch\n|> AGGREGATE\n     MAX(flask) AS caesar,\n     AVG(couch) AS whisky,\n     MAX(couch) AS woman\n   GROUP BY\n     scout\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"catch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:41,errorLinePipe:26},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    belief,\n    terry,\n    arson,\n    market,\n    ruler\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    belief,\n    SUM(arson) AS bliss,\n    MIN(ruler) AS server,\n    COUNT(market) AS stake\n  FROM cte_0\n  GROUP BY\n    belief\n),\ncte_2 AS (\n  SELECT\n    belief,\n    MAX(server) AS barker,\n    SUM(server) AS decade\n  FROM cte_1\n  GROUP BY\n    belief\n),\ncte_3 AS (\n  SELECT\n    belief,\n    barker,\n    AVG(decade) AS metro\n  FROM cte_2\n  GROUP BY\n    belief,\n    barker,\n    oyster\n),\ncte_4 AS (\n  SELECT\n    belief,\n    SUM(barker) AS insect,\n    AVG(barker) AS entity,\n    SUM(metro) AS tooth\n  FROM cte_3\n  GROUP BY\n    belief\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     belief,\n     terry,\n     arson,\n     market,\n     ruler\n|> AGGREGATE\n     SUM(arson) AS bliss,\n     MIN(ruler) AS server,\n     COUNT(market) AS stake\n   GROUP BY\n     belief\n|> AGGREGATE\n     MAX(server) AS barker,\n     SUM(server) AS decade\n   GROUP BY\n     belief\n|> AGGREGATE\n     AVG(decade) AS metro\n   GROUP BY\n     belief,\n     barker,\n     oyster\n|> AGGREGATE\n     SUM(barker) AS insect,\n     AVG(barker) AS entity,\n     SUM(metro) AS tooth\n   GROUP BY\n     belief\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"oyster",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    argyll,\n    rogue,\n    reach,\n    galley,\n    falcon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    SUM(falcon) AS purse,\n    MIN(falcon) AS pardon,\n    MIN(galley) AS maxim,\n    AVG(galley) AS dinghy,\n    COUNT(galley) AS uproar\n  FROM cte_0\n  GROUP BY\n    argyll,\n    rogue,\n    reach\n),\ncte_2 AS (\n  SELECT\n    purse,\n    pardon,\n    maxim,\n    dinghy,\n    SUM(uproar) AS reason\n  FROM cte_1\n  GROUP BY\n    purse,\n    strike,\n    pardon,\n    maxim,\n    dinghy\n),\ncte_3 AS (\n  SELECT\n    MAX(pardon) AS cheek\n  FROM cte_2\n  GROUP BY\n    purse\n),\ncte_4 AS (\n  SELECT\n    MIN(cheek) AS public\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     argyll,\n     rogue,\n     reach,\n     galley,\n     falcon\n|> AGGREGATE\n     SUM(falcon) AS purse,\n     MIN(falcon) AS pardon,\n     MIN(galley) AS maxim,\n     AVG(galley) AS dinghy,\n     COUNT(galley) AS uproar\n   GROUP BY\n     argyll,\n     rogue,\n     reach\n|> AGGREGATE\n     SUM(uproar) AS reason\n   GROUP BY\n     purse,\n     strike,\n     pardon,\n     maxim,\n     dinghy\n|> AGGREGATE\n     MAX(pardon) AS cheek\n   GROUP BY\n     purse\n|> AGGREGATE\n     MIN(cheek) AS public\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"strike",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    planet,\n    baron,\n    aisle,\n    connie,\n    crown\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    COUNT(baron) AS crust,\n    SUM(baron) AS eagle,\n    COUNT(connie) AS artist,\n    SUM(crown) AS strip,\n    COUNT(aisle) AS bridle\n  FROM cte_0\n),\ncte_2 AS (\n  SELECT\n    crust,\n    eagle,\n    MIN(strip) AS upland\n  FROM cte_1\n  GROUP BY\n    crust,\n    eagle\n),\ncte_3 AS (\n  SELECT\n    crust,\n    eagle,\n    MAX(upland) AS cradle\n  FROM cte_2\n  GROUP BY\n    green,\n    crust,\n    eagle\n),\ncte_4 AS (\n  SELECT\n    crust,\n    eagle,\n    COUNT(cradle) AS tempo\n  FROM cte_3\n  GROUP BY\n    crust,\n    eagle\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     planet,\n     baron,\n     aisle,\n     connie,\n     crown\n|> AGGREGATE\n     COUNT(baron) AS crust,\n     SUM(baron) AS eagle,\n     COUNT(connie) AS artist,\n     SUM(crown) AS strip,\n     COUNT(aisle) AS bridle\n|> AGGREGATE\n     MIN(strip) AS upland\n   GROUP BY\n     crust,\n     eagle\n|> AGGREGATE\n     MAX(upland) AS cradle\n   GROUP BY\n     green,\n     crust,\n     eagle\n|> AGGREGATE\n     COUNT(cradle) AS tempo\n   GROUP BY\n     crust,\n     eagle\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:28,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"green",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sonata,\n    frown,\n    supply,\n    tommy,\n    heath\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    sonata,\n    frown,\n    AVG(supply) AS grange\n  FROM cte_0\n  GROUP BY\n    sonata,\n    frown\n),\ncte_2 AS (\n  SELECT\n    sonata,\n    AVG(grange) AS proxy\n  FROM cte_1\n  GROUP BY\n    sonata,\n    frown\n),\ncte_3 AS (\n  SELECT\n    sonata,\n    SUM(proxy) AS hedge,\n    AVG(proxy) AS member,\n    COUNT(proxy) AS chant,\n    MIN(proxy) AS pepper\n  FROM cte_2\n  GROUP BY\n    press,\n    sonata\n),\ncte_4 AS (\n  SELECT\n    MIN(hedge) AS rebel,\n    MAX(member) AS drill,\n    MIN(member) AS baron\n  FROM cte_3\n  GROUP BY\n    sonata\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sonata,\n     frown,\n     supply,\n     tommy,\n     heath\n|> AGGREGATE\n     AVG(supply) AS grange\n   GROUP BY\n     sonata,\n     frown\n|> AGGREGATE\n     AVG(grange) AS proxy\n   GROUP BY\n     sonata,\n     frown\n|> AGGREGATE\n     SUM(proxy) AS hedge,\n     AVG(proxy) AS member,\n     COUNT(proxy) AS chant,\n     MIN(proxy) AS pepper\n   GROUP BY\n     press,\n     sonata\n|> AGGREGATE\n     MIN(hedge) AS rebel,\n     MAX(member) AS drill,\n     MIN(member) AS baron\n   GROUP BY\n     sonata\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"press",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:39,errorLinePipe:24},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tenant,\n    proxy,\n    modem,\n    helix,\n    sonny\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    tenant,\n    proxy,\n    modem,\n    helix,\n    MAX(sonny) AS common\n  FROM cte_0\n  GROUP BY\n    tenant,\n    proxy,\n    modem,\n    helix\n),\ncte_2 AS (\n  SELECT\n    MAX(proxy) AS roller,\n    MAX(common) AS detail,\n    COUNT(common) AS gentry,\n    COUNT(proxy) AS stern,\n    COUNT(helix) AS appeal\n  FROM cte_1\n  GROUP BY\n    stigma\n),\ncte_3 AS (\n  SELECT\n    roller,\n    detail,\n    gentry,\n    stern\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    roller,\n    MIN(gentry) AS bertha\n  FROM cte_3\n  GROUP BY\n    roller\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tenant,\n     proxy,\n     modem,\n     helix,\n     sonny\n|> AGGREGATE\n     MAX(sonny) AS common\n   GROUP BY\n     tenant,\n     proxy,\n     modem,\n     helix\n|> AGGREGATE\n     MAX(proxy) AS roller,\n     MAX(common) AS detail,\n     COUNT(common) AS gentry,\n     COUNT(proxy) AS stern,\n     COUNT(helix) AS appeal\n   GROUP BY\n     stigma\n|> SELECT\n     roller,\n     detail,\n     gentry,\n     stern\n|> AGGREGATE\n     MIN(gentry) AS bertha\n   GROUP BY\n     roller\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:29,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"stigma",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    wendy,\n    blade,\n    ration,\n    gloom,\n    rhythm\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    wendy,\n    blade,\n    MAX(gloom) AS utopia\n  FROM cte_0\n  GROUP BY\n    wendy,\n    blade,\n    ration\n),\ncte_2 AS (\n  SELECT\n    wendy,\n    blade,\n    utopia\n  FROM cte_1\n),\ncte_3 AS (\n  SELECT\n    wendy,\n    blade,\n    COUNT(utopia) AS slang,\n    AVG(utopia) AS marsh,\n    MIN(utopia) AS shirt\n  FROM cte_2\n  GROUP BY\n    wendy,\n    blade,\n    delay\n),\ncte_4 AS (\n  SELECT\n    COUNT(shirt) AS advert,\n    MAX(marsh) AS timber,\n    MIN(shirt) AS cobalt\n  FROM cte_3\n  GROUP BY\n    wendy,\n    blade\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     wendy,\n     blade,\n     ration,\n     gloom,\n     rhythm\n|> AGGREGATE\n     MAX(gloom) AS utopia\n   GROUP BY\n     wendy,\n     blade,\n     ration\n|> SELECT\n     wendy,\n     blade,\n     utopia\n|> AGGREGATE\n     COUNT(utopia) AS slang,\n     AVG(utopia) AS marsh,\n     MIN(utopia) AS shirt\n   GROUP BY\n     wendy,\n     blade,\n     delay\n|> AGGREGATE\n     COUNT(shirt) AS advert,\n     MAX(marsh) AS timber,\n     MIN(shirt) AS cobalt\n   GROUP BY\n     wendy,\n     blade\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"delay",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ritual,\n    victor,\n    flask,\n    glove,\n    stick\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    ritual,\n    victor,\n    MIN(stick) AS metro\n  FROM cte_0\n  GROUP BY\n    ritual,\n    victor,\n    flask,\n    glove\n),\ncte_2 AS (\n  SELECT\n    ritual,\n    victor,\n    MAX(metro) AS strap\n  FROM cte_1\n  GROUP BY\n    ritual,\n    victor\n),\ncte_3 AS (\n  SELECT\n    SUM(strap) AS divide,\n    AVG(victor) AS jerry,\n    COUNT(strap) AS teddy\n  FROM cte_2\n  GROUP BY\n    tommy\n),\ncte_4 AS (\n  SELECT\n    SUM(divide) AS sigma,\n    MIN(jerry) AS temple,\n    AVG(divide) AS ocean,\n    SUM(jerry) AS blitz\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ritual,\n     victor,\n     flask,\n     glove,\n     stick\n|> AGGREGATE\n     MIN(stick) AS metro\n   GROUP BY\n     ritual,\n     victor,\n     flask,\n     glove\n|> AGGREGATE\n     MAX(metro) AS strap\n   GROUP BY\n     ritual,\n     victor\n|> AGGREGATE\n     SUM(strap) AS divide,\n     AVG(victor) AS jerry,\n     COUNT(strap) AS teddy\n   GROUP BY\n     tommy\n|> AGGREGATE\n     SUM(divide) AS sigma,\n     MIN(jerry) AS temple,\n     AVG(divide) AS ocean,\n     SUM(jerry) AS blitz\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"tommy",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:40,errorLinePipe:25},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    petrol,\n    rugby,\n    motto,\n    butler,\n    scarf\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    petrol,\n    rugby,\n    motto,\n    butler,\n    COUNT(scarf) AS prose,\n    MIN(scarf) AS piper\n  FROM cte_0\n  GROUP BY\n    petrol,\n    rugby,\n    motto,\n    butler\n),\ncte_2 AS (\n  SELECT\n    MIN(motto) AS shape,\n    COUNT(motto) AS sweat,\n    AVG(prose) AS genome,\n    COUNT(prose) AS parole\n  FROM cte_1\n  GROUP BY\n    wallet,\n    petrol\n),\ncte_3 AS (\n  SELECT\n    shape,\n    sweat,\n    genome\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(sweat) AS bailey,\n    SUM(genome) AS crypt\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     petrol,\n     rugby,\n     motto,\n     butler,\n     scarf\n|> AGGREGATE\n     COUNT(scarf) AS prose,\n     MIN(scarf) AS piper\n   GROUP BY\n     petrol,\n     rugby,\n     motto,\n     butler\n|> AGGREGATE\n     MIN(motto) AS shape,\n     COUNT(motto) AS sweat,\n     AVG(prose) AS genome,\n     COUNT(prose) AS parole\n   GROUP BY\n     wallet,\n     petrol\n|> SELECT\n     shape,\n     sweat,\n     genome\n|> AGGREGATE\n     SUM(sweat) AS bailey,\n     SUM(genome) AS crypt\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:30,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:22,errorType:"GROUP_UNKNOWN",totalInformationDifference:4,unknownName:"wallet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:34,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    stream,\n    genome,\n    lough,\n    flush,\n    review\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    stream,\n    genome,\n    lough,\n    flush,\n    COUNT(review) AS trench\n  FROM cte_0\n  GROUP BY\n    stream,\n    genome,\n    lough,\n    flush\n),\ncte_2 AS (\n  SELECT\n    stream,\n    genome,\n    lough,\n    flush,\n    AVG(trench) AS madge\n  FROM cte_1\n  GROUP BY\n    stream,\n    genome,\n    lough,\n    flush,\n    jewel\n),\ncte_3 AS (\n  SELECT\n    AVG(flush) AS trauma,\n    SUM(madge) AS divide\n  FROM cte_2\n  GROUP BY\n    stream,\n    genome\n),\ncte_4 AS (\n  SELECT\n    MAX(trauma) AS spirit\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     stream,\n     genome,\n     lough,\n     flush,\n     review\n|> AGGREGATE\n     COUNT(review) AS trench\n   GROUP BY\n     stream,\n     genome,\n     lough,\n     flush\n|> AGGREGATE\n     AVG(trench) AS madge\n   GROUP BY\n     stream,\n     genome,\n     lough,\n     flush,\n     jewel\n|> AGGREGATE\n     AVG(flush) AS trauma,\n     SUM(madge) AS divide\n   GROUP BY\n     stream,\n     genome\n|> AGGREGATE\n     MAX(trauma) AS spirit\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"jewel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rhine,\n    harrow,\n    pillow,\n    chaos,\n    jelly\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    rhine,\n    harrow,\n    pillow,\n    chaos,\n    SUM(jelly) AS magnet\n  FROM cte_0\n  GROUP BY\n    rhine,\n    harrow,\n    pillow,\n    chaos\n),\ncte_2 AS (\n  SELECT\n    rhine,\n    harrow,\n    pillow,\n    chaos,\n    COUNT(magnet) AS crowd,\n    AVG(magnet) AS minus\n  FROM cte_1\n  GROUP BY\n    rhine,\n    harrow,\n    manual,\n    pillow,\n    chaos\n),\ncte_3 AS (\n  SELECT\n    rhine,\n    AVG(harrow) AS altar\n  FROM cte_2\n  GROUP BY\n    rhine\n),\ncte_4 AS (\n  SELECT\n    COUNT(rhine) AS spark\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rhine,\n     harrow,\n     pillow,\n     chaos,\n     jelly\n|> AGGREGATE\n     SUM(jelly) AS magnet\n   GROUP BY\n     rhine,\n     harrow,\n     pillow,\n     chaos\n|> AGGREGATE\n     COUNT(magnet) AS crowd,\n     AVG(magnet) AS minus\n   GROUP BY\n     rhine,\n     harrow,\n     manual,\n     pillow,\n     chaos\n|> AGGREGATE\n     AVG(harrow) AS altar\n   GROUP BY\n     rhine\n|> AGGREGATE\n     COUNT(rhine) AS spark\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"manual",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    pencil,\n    safety,\n    common,\n    trout,\n    novice\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    pencil,\n    safety,\n    common,\n    MAX(trout) AS suite,\n    AVG(novice) AS virgin,\n    AVG(trout) AS jumble\n  FROM cte_0\n  GROUP BY\n    pencil,\n    safety,\n    common\n),\ncte_2 AS (\n  SELECT\n    pencil,\n    safety,\n    common,\n    suite,\n    virgin,\n    COUNT(jumble) AS major\n  FROM cte_1\n  GROUP BY\n    pencil,\n    matey,\n    safety,\n    common,\n    suite,\n    virgin\n),\ncte_3 AS (\n  SELECT\n    MIN(major) AS limit\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(limit) AS abbey\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     pencil,\n     safety,\n     common,\n     trout,\n     novice\n|> AGGREGATE\n     MAX(trout) AS suite,\n     AVG(novice) AS virgin,\n     AVG(trout) AS jumble\n   GROUP BY\n     pencil,\n     safety,\n     common\n|> AGGREGATE\n     COUNT(jumble) AS major\n   GROUP BY\n     pencil,\n     matey,\n     safety,\n     common,\n     suite,\n     virgin\n|> AGGREGATE\n     MIN(major) AS limit\n|> AGGREGATE\n     SUM(limit) AS abbey\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"matey",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    idiot,\n    crime,\n    growth,\n    owner,\n    clergy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    idiot,\n    crime,\n    growth,\n    owner,\n    MIN(clergy) AS glass,\n    MAX(clergy) AS bowler\n  FROM cte_0\n  GROUP BY\n    idiot,\n    crime,\n    growth,\n    owner\n),\ncte_2 AS (\n  SELECT\n    idiot,\n    crime,\n    growth,\n    owner,\n    MIN(bowler) AS almond\n  FROM cte_1\n  GROUP BY\n    idiot,\n    crime,\n    cobalt,\n    growth,\n    owner\n),\ncte_3 AS (\n  SELECT\n    idiot,\n    MIN(almond) AS prior\n  FROM cte_2\n  GROUP BY\n    idiot\n),\ncte_4 AS (\n  SELECT\n    SUM(idiot) AS umpire\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     idiot,\n     crime,\n     growth,\n     owner,\n     clergy\n|> AGGREGATE\n     MIN(clergy) AS glass,\n     MAX(clergy) AS bowler\n   GROUP BY\n     idiot,\n     crime,\n     growth,\n     owner\n|> AGGREGATE\n     MIN(bowler) AS almond\n   GROUP BY\n     idiot,\n     crime,\n     cobalt,\n     growth,\n     owner\n|> AGGREGATE\n     MIN(almond) AS prior\n   GROUP BY\n     idiot\n|> AGGREGATE\n     SUM(idiot) AS umpire\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"cobalt",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    parody,\n    mould,\n    state,\n    roach,\n    stage\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    parody,\n    mould,\n    state,\n    COUNT(roach) AS camera,\n    AVG(roach) AS tweed,\n    MIN(roach) AS altar\n  FROM cte_0\n  GROUP BY\n    parody,\n    mould,\n    state\n),\ncte_2 AS (\n  SELECT\n    parody,\n    mould,\n    state,\n    camera,\n    tweed,\n    SUM(altar) AS inset\n  FROM cte_1\n  GROUP BY\n    parody,\n    child,\n    mould,\n    state,\n    camera,\n    tweed\n),\ncte_3 AS (\n  SELECT\n    parody,\n    mould\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    parody,\n    mould\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     parody,\n     mould,\n     state,\n     roach,\n     stage\n|> AGGREGATE\n     COUNT(roach) AS camera,\n     AVG(roach) AS tweed,\n     MIN(roach) AS altar\n   GROUP BY\n     parody,\n     mould,\n     state\n|> AGGREGATE\n     SUM(altar) AS inset\n   GROUP BY\n     parody,\n     child,\n     mould,\n     state,\n     camera,\n     tweed\n|> SELECT\n     parody,\n     mould\n|> SELECT\n     parody,\n     mould\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"child",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    asset,\n    light,\n    jacket,\n    comer,\n    gamble\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    asset,\n    light,\n    jacket,\n    comer,\n    AVG(gamble) AS probe\n  FROM cte_0\n  GROUP BY\n    asset,\n    light,\n    jacket,\n    comer\n),\ncte_2 AS (\n  SELECT\n    asset,\n    light,\n    jacket,\n    comer,\n    AVG(probe) AS muscle\n  FROM cte_1\n  GROUP BY\n    asset,\n    light,\n    jacket,\n    comer,\n    debris\n),\ncte_3 AS (\n  SELECT\n    asset,\n    MAX(jacket) AS primer,\n    COUNT(jacket) AS beard\n  FROM cte_2\n  GROUP BY\n    asset\n),\ncte_4 AS (\n  SELECT\n    asset,\n    primer\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     asset,\n     light,\n     jacket,\n     comer,\n     gamble\n|> AGGREGATE\n     AVG(gamble) AS probe\n   GROUP BY\n     asset,\n     light,\n     jacket,\n     comer\n|> AGGREGATE\n     AVG(probe) AS muscle\n   GROUP BY\n     asset,\n     light,\n     jacket,\n     comer,\n     debris\n|> AGGREGATE\n     MAX(jacket) AS primer,\n     COUNT(jacket) AS beard\n   GROUP BY\n     asset\n|> SELECT\n     asset,\n     primer\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"debris",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    blank,\n    bureau,\n    flora,\n    guitar,\n    brent\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    blank,\n    bureau,\n    flora,\n    SUM(brent) AS hunter\n  FROM cte_0\n  GROUP BY\n    blank,\n    bureau,\n    flora\n),\ncte_2 AS (\n  SELECT\n    blank,\n    bureau,\n    flora,\n    SUM(hunter) AS pollen\n  FROM cte_1\n  GROUP BY\n    blank,\n    bureau,\n    flora\n),\ncte_3 AS (\n  SELECT\n    blank,\n    bureau,\n    AVG(pollen) AS nausea\n  FROM cte_2\n  GROUP BY\n    pickup,\n    blank,\n    bureau,\n    flora\n),\ncte_4 AS (\n  SELECT\n    blank,\n    MIN(nausea) AS regard\n  FROM cte_3\n  GROUP BY\n    blank,\n    bureau\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     blank,\n     bureau,\n     flora,\n     guitar,\n     brent\n|> AGGREGATE\n     SUM(brent) AS hunter\n   GROUP BY\n     blank,\n     bureau,\n     flora\n|> AGGREGATE\n     SUM(hunter) AS pollen\n   GROUP BY\n     blank,\n     bureau,\n     flora\n|> AGGREGATE\n     AVG(pollen) AS nausea\n   GROUP BY\n     pickup,\n     blank,\n     bureau,\n     flora\n|> AGGREGATE\n     MIN(nausea) AS regard\n   GROUP BY\n     blank,\n     bureau\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"pickup",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    mosque,\n    parcel,\n    spoon,\n    sequel,\n    blank\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    mosque,\n    parcel,\n    spoon,\n    sequel,\n    MIN(blank) AS sperm\n  FROM cte_0\n  GROUP BY\n    mosque,\n    parcel,\n    spoon,\n    sequel\n),\ncte_2 AS (\n  SELECT\n    mosque,\n    parcel,\n    spoon,\n    sequel,\n    MAX(sperm) AS hatch\n  FROM cte_1\n  GROUP BY\n    mosque,\n    parcel,\n    spoon,\n    sequel,\n    saucer\n),\ncte_3 AS (\n  SELECT\n    AVG(sequel) AS nylon,\n    MIN(hatch) AS staff\n  FROM cte_2\n  GROUP BY\n    mosque,\n    parcel\n),\ncte_4 AS (\n  SELECT\n    nylon,\n    staff\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     mosque,\n     parcel,\n     spoon,\n     sequel,\n     blank\n|> AGGREGATE\n     MIN(blank) AS sperm\n   GROUP BY\n     mosque,\n     parcel,\n     spoon,\n     sequel\n|> AGGREGATE\n     MAX(sperm) AS hatch\n   GROUP BY\n     mosque,\n     parcel,\n     spoon,\n     sequel,\n     saucer\n|> AGGREGATE\n     AVG(sequel) AS nylon,\n     MIN(hatch) AS staff\n   GROUP BY\n     mosque,\n     parcel\n|> SELECT\n     nylon,\n     staff\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"saucer",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    jimmy,\n    clough,\n    goblin,\n    cobalt,\n    murray\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    jimmy,\n    clough,\n    goblin,\n    cobalt,\n    AVG(murray) AS critic\n  FROM cte_0\n  GROUP BY\n    jimmy,\n    clough,\n    goblin,\n    cobalt\n),\ncte_2 AS (\n  SELECT\n    jimmy,\n    clough,\n    AVG(cobalt) AS hearer\n  FROM cte_1\n  GROUP BY\n    jimmy,\n    clough\n),\ncte_3 AS (\n  SELECT\n    jimmy,\n    clough,\n    COUNT(hearer) AS check\n  FROM cte_2\n  GROUP BY\n    sprint,\n    jimmy,\n    clough\n),\ncte_4 AS (\n  SELECT\n    jimmy,\n    clough,\n    MAX(check) AS sodium\n  FROM cte_3\n  GROUP BY\n    jimmy,\n    clough\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     jimmy,\n     clough,\n     goblin,\n     cobalt,\n     murray\n|> AGGREGATE\n     AVG(murray) AS critic\n   GROUP BY\n     jimmy,\n     clough,\n     goblin,\n     cobalt\n|> AGGREGATE\n     AVG(cobalt) AS hearer\n   GROUP BY\n     jimmy,\n     clough\n|> AGGREGATE\n     COUNT(hearer) AS check\n   GROUP BY\n     sprint,\n     jimmy,\n     clough\n|> AGGREGATE\n     MAX(check) AS sodium\n   GROUP BY\n     jimmy,\n     clough\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:24,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"sprint",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    plate,\n    diesel,\n    pledge,\n    basket,\n    thread\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    plate,\n    diesel,\n    pledge,\n    AVG(thread) AS stack\n  FROM cte_0\n  GROUP BY\n    plate,\n    diesel,\n    pledge\n),\ncte_2 AS (\n  SELECT\n    plate,\n    diesel,\n    pledge,\n    MIN(stack) AS tardis\n  FROM cte_1\n  GROUP BY\n    plate,\n    diesel,\n    pledge\n),\ncte_3 AS (\n  SELECT\n    plate,\n    diesel,\n    SUM(pledge) AS flight\n  FROM cte_2\n  GROUP BY\n    minus,\n    plate,\n    diesel\n),\ncte_4 AS (\n  SELECT\n    plate,\n    diesel,\n    SUM(flight) AS daddy\n  FROM cte_3\n  GROUP BY\n    plate,\n    diesel\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     plate,\n     diesel,\n     pledge,\n     basket,\n     thread\n|> AGGREGATE\n     AVG(thread) AS stack\n   GROUP BY\n     plate,\n     diesel,\n     pledge\n|> AGGREGATE\n     MIN(stack) AS tardis\n   GROUP BY\n     plate,\n     diesel,\n     pledge\n|> AGGREGATE\n     SUM(pledge) AS flight\n   GROUP BY\n     minus,\n     plate,\n     diesel\n|> AGGREGATE\n     SUM(flight) AS daddy\n   GROUP BY\n     plate,\n     diesel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:24,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"minus",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    amelia,\n    slang,\n    flame,\n    break,\n    salary\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    amelia,\n    slang,\n    flame,\n    break,\n    MAX(salary) AS memory\n  FROM cte_0\n  GROUP BY\n    amelia,\n    slang,\n    flame,\n    break\n),\ncte_2 AS (\n  SELECT\n    amelia,\n    slang,\n    flame,\n    break,\n    SUM(memory) AS check\n  FROM cte_1\n  GROUP BY\n    amelia,\n    slang,\n    flame,\n    break,\n    tariff\n),\ncte_3 AS (\n  SELECT\n    amelia,\n    slang,\n    flame,\n    break,\n    check\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    amelia,\n    slang,\n    flame\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     amelia,\n     slang,\n     flame,\n     break,\n     salary\n|> AGGREGATE\n     MAX(salary) AS memory\n   GROUP BY\n     amelia,\n     slang,\n     flame,\n     break\n|> AGGREGATE\n     SUM(memory) AS check\n   GROUP BY\n     amelia,\n     slang,\n     flame,\n     break,\n     tariff\n|> SELECT\n     amelia,\n     slang,\n     flame,\n     break,\n     check\n|> SELECT\n     amelia,\n     slang,\n     flame\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"tariff",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    series,\n    brewer,\n    venus,\n    scrap,\n    cursor\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    series,\n    brewer,\n    venus,\n    scrap,\n    MAX(cursor) AS booze,\n    MIN(cursor) AS ransom\n  FROM cte_0\n  GROUP BY\n    series,\n    brewer,\n    venus,\n    scrap\n),\ncte_2 AS (\n  SELECT\n    series,\n    brewer,\n    venus,\n    scrap,\n    COUNT(booze) AS lapse\n  FROM cte_1\n  GROUP BY\n    series,\n    brewer,\n    stoke,\n    venus,\n    scrap\n),\ncte_3 AS (\n  SELECT\n    series,\n    AVG(scrap) AS ghetto\n  FROM cte_2\n  GROUP BY\n    series\n),\ncte_4 AS (\n  SELECT\n    SUM(series) AS weber\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     series,\n     brewer,\n     venus,\n     scrap,\n     cursor\n|> AGGREGATE\n     MAX(cursor) AS booze,\n     MIN(cursor) AS ransom\n   GROUP BY\n     series,\n     brewer,\n     venus,\n     scrap\n|> AGGREGATE\n     COUNT(booze) AS lapse\n   GROUP BY\n     series,\n     brewer,\n     stoke,\n     venus,\n     scrap\n|> AGGREGATE\n     AVG(scrap) AS ghetto\n   GROUP BY\n     series\n|> AGGREGATE\n     SUM(series) AS weber\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"stoke",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    breast,\n    level,\n    cocoa,\n    canon,\n    potter\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    breast,\n    level,\n    cocoa,\n    canon,\n    SUM(potter) AS travel\n  FROM cte_0\n  GROUP BY\n    breast,\n    level,\n    cocoa,\n    canon\n),\ncte_2 AS (\n  SELECT\n    breast,\n    level,\n    cocoa,\n    canon,\n    COUNT(travel) AS parity\n  FROM cte_1\n  GROUP BY\n    breast,\n    level,\n    cocoa,\n    canon,\n    boost\n),\ncte_3 AS (\n  SELECT\n    SUM(cocoa) AS taylor,\n    MAX(canon) AS racism,\n    AVG(breast) AS excise\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(racism) AS touch\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     breast,\n     level,\n     cocoa,\n     canon,\n     potter\n|> AGGREGATE\n     SUM(potter) AS travel\n   GROUP BY\n     breast,\n     level,\n     cocoa,\n     canon\n|> AGGREGATE\n     COUNT(travel) AS parity\n   GROUP BY\n     breast,\n     level,\n     cocoa,\n     canon,\n     boost\n|> AGGREGATE\n     SUM(cocoa) AS taylor,\n     MAX(canon) AS racism,\n     AVG(breast) AS excise\n|> AGGREGATE\n     COUNT(racism) AS touch\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"boost",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    juice,\n    widow,\n    chalk,\n    sister,\n    sheen\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    juice,\n    widow,\n    chalk,\n    sister,\n    MIN(sheen) AS peach\n  FROM cte_0\n  GROUP BY\n    juice,\n    widow,\n    chalk,\n    sister\n),\ncte_2 AS (\n  SELECT\n    juice,\n    widow,\n    chalk,\n    sister,\n    AVG(peach) AS study,\n    SUM(peach) AS target\n  FROM cte_1\n  GROUP BY\n    juice,\n    widow,\n    motto,\n    chalk,\n    sister\n),\ncte_3 AS (\n  SELECT\n    juice,\n    widow,\n    chalk,\n    sister\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(widow) AS tenant\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     juice,\n     widow,\n     chalk,\n     sister,\n     sheen\n|> AGGREGATE\n     MIN(sheen) AS peach\n   GROUP BY\n     juice,\n     widow,\n     chalk,\n     sister\n|> AGGREGATE\n     AVG(peach) AS study,\n     SUM(peach) AS target\n   GROUP BY\n     juice,\n     widow,\n     motto,\n     chalk,\n     sister\n|> SELECT\n     juice,\n     widow,\n     chalk,\n     sister\n|> AGGREGATE\n     MIN(widow) AS tenant\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"motto",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    camel,\n    thrust,\n    canopy,\n    wendy,\n    drink\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    camel,\n    thrust,\n    canopy,\n    wendy,\n    MIN(drink) AS throw,\n    COUNT(drink) AS church\n  FROM cte_0\n  GROUP BY\n    camel,\n    thrust,\n    canopy,\n    wendy\n),\ncte_2 AS (\n  SELECT\n    camel,\n    thrust,\n    canopy,\n    wendy,\n    SUM(church) AS movie\n  FROM cte_1\n  GROUP BY\n    camel,\n    thrust,\n    ribber,\n    canopy,\n    wendy,\n    throw\n),\ncte_3 AS (\n  SELECT\n    camel,\n    thrust\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(thrust) AS crown\n  FROM cte_3\n  GROUP BY\n    camel\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     camel,\n     thrust,\n     canopy,\n     wendy,\n     drink\n|> AGGREGATE\n     MIN(drink) AS throw,\n     COUNT(drink) AS church\n   GROUP BY\n     camel,\n     thrust,\n     canopy,\n     wendy\n|> AGGREGATE\n     SUM(church) AS movie\n   GROUP BY\n     camel,\n     thrust,\n     ribber,\n     canopy,\n     wendy,\n     throw\n|> SELECT\n     camel,\n     thrust\n|> AGGREGATE\n     COUNT(thrust) AS crown\n   GROUP BY\n     camel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"ribber",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    south,\n    squash,\n    scotch,\n    puppet,\n    split\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    south,\n    squash,\n    scotch,\n    puppet,\n    AVG(split) AS pencil,\n    MIN(split) AS glove,\n    MAX(split) AS meter\n  FROM cte_0\n  GROUP BY\n    south,\n    squash,\n    scotch,\n    puppet\n),\ncte_2 AS (\n  SELECT\n    south,\n    squash,\n    scotch,\n    puppet,\n    MIN(glove) AS lough\n  FROM cte_1\n  GROUP BY\n    wight,\n    south,\n    squash,\n    scotch,\n    puppet\n),\ncte_3 AS (\n  SELECT\n    south,\n    squash\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(south) AS danger\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     south,\n     squash,\n     scotch,\n     puppet,\n     split\n|> AGGREGATE\n     AVG(split) AS pencil,\n     MIN(split) AS glove,\n     MAX(split) AS meter\n   GROUP BY\n     south,\n     squash,\n     scotch,\n     puppet\n|> AGGREGATE\n     MIN(glove) AS lough\n   GROUP BY\n     wight,\n     south,\n     squash,\n     scotch,\n     puppet\n|> SELECT\n     south,\n     squash\n|> AGGREGATE\n     MAX(south) AS danger\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"wight",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    mortar,\n    death,\n    stern,\n    pixel,\n    stance\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    mortar,\n    death,\n    stern,\n    pixel,\n    MAX(stance) AS shoal\n  FROM cte_0\n  GROUP BY\n    mortar,\n    death,\n    stern,\n    pixel\n),\ncte_2 AS (\n  SELECT\n    mortar,\n    death,\n    stern,\n    pixel,\n    COUNT(shoal) AS horse,\n    MAX(shoal) AS gemma\n  FROM cte_1\n  GROUP BY\n    mortar,\n    death,\n    ethos,\n    stern,\n    pixel\n),\ncte_3 AS (\n  SELECT\n    COUNT(death) AS slice,\n    SUM(gemma) AS frenzy\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    slice,\n    frenzy\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     mortar,\n     death,\n     stern,\n     pixel,\n     stance\n|> AGGREGATE\n     MAX(stance) AS shoal\n   GROUP BY\n     mortar,\n     death,\n     stern,\n     pixel\n|> AGGREGATE\n     COUNT(shoal) AS horse,\n     MAX(shoal) AS gemma\n   GROUP BY\n     mortar,\n     death,\n     ethos,\n     stern,\n     pixel\n|> AGGREGATE\n     COUNT(death) AS slice,\n     SUM(gemma) AS frenzy\n|> SELECT\n     slice,\n     frenzy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"ethos",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    stack,\n    shoal,\n    supply,\n    centre,\n    degree\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    stack,\n    shoal,\n    supply,\n    centre,\n    MAX(degree) AS poppy\n  FROM cte_0\n  GROUP BY\n    stack,\n    shoal,\n    supply,\n    centre\n),\ncte_2 AS (\n  SELECT\n    stack,\n    shoal,\n    supply,\n    centre,\n    MAX(poppy) AS influx\n  FROM cte_1\n  GROUP BY\n    stack,\n    shoal,\n    supply,\n    centre,\n    steam\n),\ncte_3 AS (\n  SELECT\n    MAX(influx) AS rubble\n  FROM cte_2\n  GROUP BY\n    stack,\n    shoal\n),\ncte_4 AS (\n  SELECT\n    MIN(rubble) AS amino,\n    COUNT(rubble) AS cable\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     stack,\n     shoal,\n     supply,\n     centre,\n     degree\n|> AGGREGATE\n     MAX(degree) AS poppy\n   GROUP BY\n     stack,\n     shoal,\n     supply,\n     centre\n|> AGGREGATE\n     MAX(poppy) AS influx\n   GROUP BY\n     stack,\n     shoal,\n     supply,\n     centre,\n     steam\n|> AGGREGATE\n     MAX(influx) AS rubble\n   GROUP BY\n     stack,\n     shoal\n|> AGGREGATE\n     MIN(rubble) AS amino,\n     COUNT(rubble) AS cable\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"steam",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    corpse,\n    reeve,\n    haven,\n    worth,\n    hatred\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    corpse,\n    reeve,\n    haven,\n    worth,\n    COUNT(hatred) AS draft\n  FROM cte_0\n  GROUP BY\n    corpse,\n    reeve,\n    haven,\n    worth\n),\ncte_2 AS (\n  SELECT\n    corpse,\n    reeve,\n    haven,\n    worth,\n    MIN(draft) AS voyage\n  FROM cte_1\n  GROUP BY\n    corpse,\n    reeve,\n    haven,\n    worth,\n    globe\n),\ncte_3 AS (\n  SELECT\n    corpse,\n    MAX(haven) AS quote,\n    AVG(worth) AS parson\n  FROM cte_2\n  GROUP BY\n    corpse\n),\ncte_4 AS (\n  SELECT\n    MAX(parson) AS animal\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     corpse,\n     reeve,\n     haven,\n     worth,\n     hatred\n|> AGGREGATE\n     COUNT(hatred) AS draft\n   GROUP BY\n     corpse,\n     reeve,\n     haven,\n     worth\n|> AGGREGATE\n     MIN(draft) AS voyage\n   GROUP BY\n     corpse,\n     reeve,\n     haven,\n     worth,\n     globe\n|> AGGREGATE\n     MAX(haven) AS quote,\n     AVG(worth) AS parson\n   GROUP BY\n     corpse\n|> AGGREGATE\n     MAX(parson) AS animal\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"globe",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    mound,\n    fodder,\n    safari,\n    reggae,\n    mount\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    mound,\n    fodder,\n    safari,\n    SUM(reggae) AS german,\n    COUNT(mount) AS spark,\n    AVG(reggae) AS impact\n  FROM cte_0\n  GROUP BY\n    mound,\n    fodder,\n    safari\n),\ncte_2 AS (\n  SELECT\n    mound,\n    fodder,\n    safari,\n    german,\n    spark,\n    AVG(impact) AS hatch\n  FROM cte_1\n  GROUP BY\n    mound,\n    tunnel,\n    fodder,\n    safari,\n    german,\n    spark\n),\ncte_3 AS (\n  SELECT\n    mound,\n    fodder\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    mound,\n    fodder\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     mound,\n     fodder,\n     safari,\n     reggae,\n     mount\n|> AGGREGATE\n     SUM(reggae) AS german,\n     COUNT(mount) AS spark,\n     AVG(reggae) AS impact\n   GROUP BY\n     mound,\n     fodder,\n     safari\n|> AGGREGATE\n     AVG(impact) AS hatch\n   GROUP BY\n     mound,\n     tunnel,\n     fodder,\n     safari,\n     german,\n     spark\n|> SELECT\n     mound,\n     fodder\n|> SELECT\n     mound,\n     fodder\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"tunnel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    fowler,\n    corps,\n    kettle,\n    cuckoo,\n    gallon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    fowler,\n    corps,\n    kettle,\n    cuckoo,\n    AVG(gallon) AS fence,\n    MAX(gallon) AS tissue\n  FROM cte_0\n  GROUP BY\n    fowler,\n    corps,\n    kettle,\n    cuckoo\n),\ncte_2 AS (\n  SELECT\n    fowler,\n    corps,\n    kettle,\n    cuckoo,\n    COUNT(fence) AS boiler,\n    AVG(tissue) AS liquid\n  FROM cte_1\n  GROUP BY\n    taboo,\n    fowler,\n    corps,\n    kettle,\n    cuckoo\n),\ncte_3 AS (\n  SELECT\n    fowler,\n    corps\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(corps) AS tennis\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     fowler,\n     corps,\n     kettle,\n     cuckoo,\n     gallon\n|> AGGREGATE\n     AVG(gallon) AS fence,\n     MAX(gallon) AS tissue\n   GROUP BY\n     fowler,\n     corps,\n     kettle,\n     cuckoo\n|> AGGREGATE\n     COUNT(fence) AS boiler,\n     AVG(tissue) AS liquid\n   GROUP BY\n     taboo,\n     fowler,\n     corps,\n     kettle,\n     cuckoo\n|> SELECT\n     fowler,\n     corps\n|> AGGREGATE\n     SUM(corps) AS tennis\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"taboo",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    wheat,\n    yield,\n    block,\n    stride,\n    pepper\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    wheat,\n    yield,\n    block,\n    stride,\n    COUNT(pepper) AS coffee\n  FROM cte_0\n  GROUP BY\n    wheat,\n    yield,\n    block,\n    stride\n),\ncte_2 AS (\n  SELECT\n    wheat,\n    yield,\n    block,\n    stride,\n    MIN(coffee) AS demand\n  FROM cte_1\n  GROUP BY\n    wheat,\n    yield,\n    block,\n    stride,\n    coach\n),\ncte_3 AS (\n  SELECT\n    wheat,\n    yield,\n    block\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(yield) AS hybrid,\n    AVG(yield) AS salary\n  FROM cte_3\n  GROUP BY\n    wheat\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     wheat,\n     yield,\n     block,\n     stride,\n     pepper\n|> AGGREGATE\n     COUNT(pepper) AS coffee\n   GROUP BY\n     wheat,\n     yield,\n     block,\n     stride\n|> AGGREGATE\n     MIN(coffee) AS demand\n   GROUP BY\n     wheat,\n     yield,\n     block,\n     stride,\n     coach\n|> SELECT\n     wheat,\n     yield,\n     block\n|> AGGREGATE\n     SUM(yield) AS hybrid,\n     AVG(yield) AS salary\n   GROUP BY\n     wheat\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"coach",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    larry,\n    stint,\n    delay,\n    whisky,\n    brain\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    larry,\n    stint,\n    delay,\n    whisky,\n    MAX(brain) AS queue\n  FROM cte_0\n  GROUP BY\n    larry,\n    stint,\n    delay,\n    whisky\n),\ncte_2 AS (\n  SELECT\n    larry,\n    stint,\n    delay,\n    whisky,\n    SUM(queue) AS comma,\n    MIN(queue) AS sketch\n  FROM cte_1\n  GROUP BY\n    larry,\n    stint,\n    parker,\n    delay,\n    whisky\n),\ncte_3 AS (\n  SELECT\n    larry,\n    stint,\n    delay,\n    whisky\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(larry) AS flash\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     larry,\n     stint,\n     delay,\n     whisky,\n     brain\n|> AGGREGATE\n     MAX(brain) AS queue\n   GROUP BY\n     larry,\n     stint,\n     delay,\n     whisky\n|> AGGREGATE\n     SUM(queue) AS comma,\n     MIN(queue) AS sketch\n   GROUP BY\n     larry,\n     stint,\n     parker,\n     delay,\n     whisky\n|> SELECT\n     larry,\n     stint,\n     delay,\n     whisky\n|> AGGREGATE\n     SUM(larry) AS flash\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"parker",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    level,\n    sigma,\n    touch,\n    shade,\n    noble\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    level,\n    sigma,\n    touch,\n    shade,\n    AVG(noble) AS portal\n  FROM cte_0\n  GROUP BY\n    level,\n    sigma,\n    touch,\n    shade\n),\ncte_2 AS (\n  SELECT\n    level,\n    sigma,\n    touch,\n    shade,\n    MAX(portal) AS kelly,\n    COUNT(portal) AS hearth\n  FROM cte_1\n  GROUP BY\n    level,\n    sigma,\n    gorge,\n    touch,\n    shade\n),\ncte_3 AS (\n  SELECT\n    level,\n    sigma,\n    touch,\n    shade\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    level,\n    sigma\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     level,\n     sigma,\n     touch,\n     shade,\n     noble\n|> AGGREGATE\n     AVG(noble) AS portal\n   GROUP BY\n     level,\n     sigma,\n     touch,\n     shade\n|> AGGREGATE\n     MAX(portal) AS kelly,\n     COUNT(portal) AS hearth\n   GROUP BY\n     level,\n     sigma,\n     gorge,\n     touch,\n     shade\n|> SELECT\n     level,\n     sigma,\n     touch,\n     shade\n|> SELECT\n     level,\n     sigma\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"gorge",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    shelf,\n    choir,\n    enamel,\n    screw,\n    sketch\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    shelf,\n    choir,\n    enamel,\n    screw,\n    MIN(sketch) AS berlin\n  FROM cte_0\n  GROUP BY\n    shelf,\n    choir,\n    enamel,\n    screw\n),\ncte_2 AS (\n  SELECT\n    shelf,\n    choir,\n    enamel,\n    screw,\n    AVG(berlin) AS cable,\n    COUNT(berlin) AS intake\n  FROM cte_1\n  GROUP BY\n    shelf,\n    choir,\n    quest,\n    enamel,\n    screw\n),\ncte_3 AS (\n  SELECT\n    shelf,\n    choir\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(choir) AS expiry,\n    COUNT(shelf) AS tiller\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     shelf,\n     choir,\n     enamel,\n     screw,\n     sketch\n|> AGGREGATE\n     MIN(sketch) AS berlin\n   GROUP BY\n     shelf,\n     choir,\n     enamel,\n     screw\n|> AGGREGATE\n     AVG(berlin) AS cable,\n     COUNT(berlin) AS intake\n   GROUP BY\n     shelf,\n     choir,\n     quest,\n     enamel,\n     screw\n|> SELECT\n     shelf,\n     choir\n|> AGGREGATE\n     MAX(choir) AS expiry,\n     COUNT(shelf) AS tiller\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"quest",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    thread,\n    golfer,\n    parkin,\n    appeal,\n    snake\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    thread,\n    golfer,\n    parkin,\n    MAX(snake) AS martin\n  FROM cte_0\n  GROUP BY\n    thread,\n    golfer,\n    parkin\n),\ncte_2 AS (\n  SELECT\n    thread,\n    golfer,\n    parkin,\n    SUM(martin) AS drive\n  FROM cte_1\n  GROUP BY\n    thread,\n    golfer,\n    parkin\n),\ncte_3 AS (\n  SELECT\n    thread,\n    golfer,\n    MAX(parkin) AS collar\n  FROM cte_2\n  GROUP BY\n    yeast,\n    thread,\n    golfer\n),\ncte_4 AS (\n  SELECT\n    thread,\n    MAX(golfer) AS biopsy,\n    COUNT(golfer) AS rudder\n  FROM cte_3\n  GROUP BY\n    thread\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     thread,\n     golfer,\n     parkin,\n     appeal,\n     snake\n|> AGGREGATE\n     MAX(snake) AS martin\n   GROUP BY\n     thread,\n     golfer,\n     parkin\n|> AGGREGATE\n     SUM(martin) AS drive\n   GROUP BY\n     thread,\n     golfer,\n     parkin\n|> AGGREGATE\n     MAX(parkin) AS collar\n   GROUP BY\n     yeast,\n     thread,\n     golfer\n|> AGGREGATE\n     MAX(golfer) AS biopsy,\n     COUNT(golfer) AS rudder\n   GROUP BY\n     thread\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"yeast",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    belief,\n    widow,\n    burst,\n    irony,\n    vicar\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    belief,\n    widow,\n    burst,\n    AVG(vicar) AS gummer\n  FROM cte_0\n  GROUP BY\n    belief,\n    widow,\n    burst\n),\ncte_2 AS (\n  SELECT\n    belief,\n    widow,\n    burst,\n    AVG(gummer) AS verge\n  FROM cte_1\n  GROUP BY\n    belief,\n    widow,\n    burst\n),\ncte_3 AS (\n  SELECT\n    belief,\n    widow,\n    AVG(burst) AS flesh\n  FROM cte_2\n  GROUP BY\n    stamp,\n    belief,\n    widow\n),\ncte_4 AS (\n  SELECT\n    MAX(flesh) AS firth,\n    AVG(flesh) AS heath\n  FROM cte_3\n  GROUP BY\n    belief,\n    widow\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     belief,\n     widow,\n     burst,\n     irony,\n     vicar\n|> AGGREGATE\n     AVG(vicar) AS gummer\n   GROUP BY\n     belief,\n     widow,\n     burst\n|> AGGREGATE\n     AVG(gummer) AS verge\n   GROUP BY\n     belief,\n     widow,\n     burst\n|> AGGREGATE\n     AVG(burst) AS flesh\n   GROUP BY\n     stamp,\n     belief,\n     widow\n|> AGGREGATE\n     MAX(flesh) AS firth,\n     AVG(flesh) AS heath\n   GROUP BY\n     belief,\n     widow\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"stamp",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bother,\n    revolt,\n    lounge,\n    gable,\n    plain\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    bother,\n    revolt,\n    lounge,\n    gable,\n    SUM(plain) AS genre\n  FROM cte_0\n  GROUP BY\n    bother,\n    revolt,\n    lounge,\n    gable\n),\ncte_2 AS (\n  SELECT\n    bother,\n    revolt,\n    lounge,\n    gable,\n    AVG(genre) AS wisdom,\n    COUNT(genre) AS insert\n  FROM cte_1\n  GROUP BY\n    bother,\n    revolt,\n    warsaw,\n    lounge,\n    gable\n),\ncte_3 AS (\n  SELECT\n    bother,\n    revolt,\n    lounge,\n    gable\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    bother,\n    revolt\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bother,\n     revolt,\n     lounge,\n     gable,\n     plain\n|> AGGREGATE\n     SUM(plain) AS genre\n   GROUP BY\n     bother,\n     revolt,\n     lounge,\n     gable\n|> AGGREGATE\n     AVG(genre) AS wisdom,\n     COUNT(genre) AS insert\n   GROUP BY\n     bother,\n     revolt,\n     warsaw,\n     lounge,\n     gable\n|> SELECT\n     bother,\n     revolt,\n     lounge,\n     gable\n|> SELECT\n     bother,\n     revolt\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"warsaw",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    circus,\n    monte,\n    barnet,\n    fibre,\n    dinghy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    circus,\n    monte,\n    barnet,\n    fibre,\n    AVG(dinghy) AS office,\n    COUNT(dinghy) AS animal\n  FROM cte_0\n  GROUP BY\n    circus,\n    monte,\n    barnet,\n    fibre\n),\ncte_2 AS (\n  SELECT\n    circus,\n    monte,\n    barnet,\n    fibre,\n    AVG(office) AS feeder\n  FROM cte_1\n  GROUP BY\n    circus,\n    monte,\n    horror,\n    barnet,\n    fibre\n),\ncte_3 AS (\n  SELECT\n    circus,\n    SUM(barnet) AS arson\n  FROM cte_2\n  GROUP BY\n    circus\n),\ncte_4 AS (\n  SELECT\n    AVG(arson) AS pistol\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     circus,\n     monte,\n     barnet,\n     fibre,\n     dinghy\n|> AGGREGATE\n     AVG(dinghy) AS office,\n     COUNT(dinghy) AS animal\n   GROUP BY\n     circus,\n     monte,\n     barnet,\n     fibre\n|> AGGREGATE\n     AVG(office) AS feeder\n   GROUP BY\n     circus,\n     monte,\n     horror,\n     barnet,\n     fibre\n|> AGGREGATE\n     SUM(barnet) AS arson\n   GROUP BY\n     circus\n|> AGGREGATE\n     AVG(arson) AS pistol\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"horror",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    lorry,\n    spence,\n    wonder,\n    cliff,\n    music\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    lorry,\n    spence,\n    wonder,\n    cliff,\n    MIN(music) AS front\n  FROM cte_0\n  GROUP BY\n    lorry,\n    spence,\n    wonder,\n    cliff\n),\ncte_2 AS (\n  SELECT\n    lorry,\n    spence,\n    wonder,\n    cliff,\n    MIN(front) AS apollo\n  FROM cte_1\n  GROUP BY\n    lorry,\n    spence,\n    wonder,\n    cliff,\n    priest\n),\ncte_3 AS (\n  SELECT\n    lorry,\n    spence,\n    wonder\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(wonder) AS pizza,\n    MAX(spence) AS bottom\n  FROM cte_3\n  GROUP BY\n    lorry\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     lorry,\n     spence,\n     wonder,\n     cliff,\n     music\n|> AGGREGATE\n     MIN(music) AS front\n   GROUP BY\n     lorry,\n     spence,\n     wonder,\n     cliff\n|> AGGREGATE\n     MIN(front) AS apollo\n   GROUP BY\n     lorry,\n     spence,\n     wonder,\n     cliff,\n     priest\n|> SELECT\n     lorry,\n     spence,\n     wonder\n|> AGGREGATE\n     MIN(wonder) AS pizza,\n     MAX(spence) AS bottom\n   GROUP BY\n     lorry\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"priest",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    holder,\n    tensor,\n    sheila,\n    client,\n    surety\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    holder,\n    tensor,\n    sheila,\n    client,\n    MIN(surety) AS piazza\n  FROM cte_0\n  GROUP BY\n    holder,\n    tensor,\n    sheila,\n    client\n),\ncte_2 AS (\n  SELECT\n    holder,\n    tensor,\n    sheila,\n    client,\n    COUNT(piazza) AS groove,\n    SUM(piazza) AS lough\n  FROM cte_1\n  GROUP BY\n    holder,\n    tensor,\n    carrot,\n    sheila,\n    client\n),\ncte_3 AS (\n  SELECT\n    SUM(holder) AS snake,\n    COUNT(client) AS harrow\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    snake,\n    harrow\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     holder,\n     tensor,\n     sheila,\n     client,\n     surety\n|> AGGREGATE\n     MIN(surety) AS piazza\n   GROUP BY\n     holder,\n     tensor,\n     sheila,\n     client\n|> AGGREGATE\n     COUNT(piazza) AS groove,\n     SUM(piazza) AS lough\n   GROUP BY\n     holder,\n     tensor,\n     carrot,\n     sheila,\n     client\n|> AGGREGATE\n     SUM(holder) AS snake,\n     COUNT(client) AS harrow\n|> SELECT\n     snake,\n     harrow\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"carrot",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    moran,\n    sailor,\n    sheila,\n    fiddle,\n    manure\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    moran,\n    sailor,\n    sheila,\n    fiddle,\n    MIN(manure) AS breed,\n    SUM(manure) AS chaos\n  FROM cte_0\n  GROUP BY\n    moran,\n    sailor,\n    sheila,\n    fiddle\n),\ncte_2 AS (\n  SELECT\n    moran,\n    sailor,\n    sheila,\n    fiddle,\n    AVG(chaos) AS lesson\n  FROM cte_1\n  GROUP BY\n    moran,\n    sailor,\n    kernel,\n    sheila,\n    fiddle\n),\ncte_3 AS (\n  SELECT\n    moran,\n    sailor\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    moran,\n    AVG(sailor) AS intake\n  FROM cte_3\n  GROUP BY\n    moran\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     moran,\n     sailor,\n     sheila,\n     fiddle,\n     manure\n|> AGGREGATE\n     MIN(manure) AS breed,\n     SUM(manure) AS chaos\n   GROUP BY\n     moran,\n     sailor,\n     sheila,\n     fiddle\n|> AGGREGATE\n     AVG(chaos) AS lesson\n   GROUP BY\n     moran,\n     sailor,\n     kernel,\n     sheila,\n     fiddle\n|> SELECT\n     moran,\n     sailor\n|> AGGREGATE\n     AVG(sailor) AS intake\n   GROUP BY\n     moran\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"kernel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    youth,\n    heater,\n    alkali,\n    irene,\n    parole\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    youth,\n    heater,\n    alkali,\n    irene,\n    COUNT(parole) AS smear\n  FROM cte_0\n  GROUP BY\n    youth,\n    heater,\n    alkali,\n    irene\n),\ncte_2 AS (\n  SELECT\n    youth,\n    heater,\n    AVG(irene) AS swift\n  FROM cte_1\n  GROUP BY\n    youth,\n    heater\n),\ncte_3 AS (\n  SELECT\n    youth,\n    heater,\n    COUNT(swift) AS frost\n  FROM cte_2\n  GROUP BY\n    turkey,\n    youth,\n    heater\n),\ncte_4 AS (\n  SELECT\n    MAX(frost) AS lizard,\n    MIN(frost) AS entry\n  FROM cte_3\n  GROUP BY\n    youth,\n    heater\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     youth,\n     heater,\n     alkali,\n     irene,\n     parole\n|> AGGREGATE\n     COUNT(parole) AS smear\n   GROUP BY\n     youth,\n     heater,\n     alkali,\n     irene\n|> AGGREGATE\n     AVG(irene) AS swift\n   GROUP BY\n     youth,\n     heater\n|> AGGREGATE\n     COUNT(swift) AS frost\n   GROUP BY\n     turkey,\n     youth,\n     heater\n|> AGGREGATE\n     MAX(frost) AS lizard,\n     MIN(frost) AS entry\n   GROUP BY\n     youth,\n     heater\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"turkey",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    loser,\n    mound,\n    grill,\n    shock,\n    beacon\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    loser,\n    mound,\n    grill,\n    shock,\n    MIN(beacon) AS moran,\n    SUM(beacon) AS frenzy\n  FROM cte_0\n  GROUP BY\n    loser,\n    mound,\n    grill,\n    shock\n),\ncte_2 AS (\n  SELECT\n    loser,\n    mound,\n    grill,\n    shock,\n    AVG(frenzy) AS server,\n    AVG(moran) AS bowel\n  FROM cte_1\n  GROUP BY\n    straw,\n    loser,\n    mound,\n    grill,\n    shock\n),\ncte_3 AS (\n  SELECT\n    loser,\n    mound\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    loser,\n    mound\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     loser,\n     mound,\n     grill,\n     shock,\n     beacon\n|> AGGREGATE\n     MIN(beacon) AS moran,\n     SUM(beacon) AS frenzy\n   GROUP BY\n     loser,\n     mound,\n     grill,\n     shock\n|> AGGREGATE\n     AVG(frenzy) AS server,\n     AVG(moran) AS bowel\n   GROUP BY\n     straw,\n     loser,\n     mound,\n     grill,\n     shock\n|> SELECT\n     loser,\n     mound\n|> SELECT\n     loser,\n     mound\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"straw",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    kelly,\n    course,\n    update,\n    poppy,\n    blast\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    kelly,\n    course,\n    update,\n    MAX(blast) AS bunny\n  FROM cte_0\n  GROUP BY\n    kelly,\n    course,\n    update\n),\ncte_2 AS (\n  SELECT\n    kelly,\n    course,\n    update,\n    MIN(bunny) AS baker\n  FROM cte_1\n  GROUP BY\n    kelly,\n    course,\n    update\n),\ncte_3 AS (\n  SELECT\n    kelly,\n    course,\n    MIN(baker) AS hinge\n  FROM cte_2\n  GROUP BY\n    henry,\n    kelly,\n    course\n),\ncte_4 AS (\n  SELECT\n    MAX(hinge) AS ruler,\n    COUNT(hinge) AS build\n  FROM cte_3\n  GROUP BY\n    kelly,\n    course\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     kelly,\n     course,\n     update,\n     poppy,\n     blast\n|> AGGREGATE\n     MAX(blast) AS bunny\n   GROUP BY\n     kelly,\n     course,\n     update\n|> AGGREGATE\n     MIN(bunny) AS baker\n   GROUP BY\n     kelly,\n     course,\n     update\n|> AGGREGATE\n     MIN(baker) AS hinge\n   GROUP BY\n     henry,\n     kelly,\n     course\n|> AGGREGATE\n     MAX(hinge) AS ruler,\n     COUNT(hinge) AS build\n   GROUP BY\n     kelly,\n     course\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"henry",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    prize,\n    remark,\n    petrol,\n    finish,\n    moment\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    prize,\n    remark,\n    petrol,\n    finish,\n    MIN(moment) AS grief\n  FROM cte_0\n  GROUP BY\n    prize,\n    remark,\n    petrol,\n    finish\n),\ncte_2 AS (\n  SELECT\n    prize,\n    remark,\n    petrol,\n    finish,\n    COUNT(grief) AS stone\n  FROM cte_1\n  GROUP BY\n    prize,\n    remark,\n    petrol,\n    finish,\n    fauna\n),\ncte_3 AS (\n  SELECT\n    prize,\n    remark,\n    petrol,\n    finish\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    prize,\n    SUM(petrol) AS unison\n  FROM cte_3\n  GROUP BY\n    prize\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     prize,\n     remark,\n     petrol,\n     finish,\n     moment\n|> AGGREGATE\n     MIN(moment) AS grief\n   GROUP BY\n     prize,\n     remark,\n     petrol,\n     finish\n|> AGGREGATE\n     COUNT(grief) AS stone\n   GROUP BY\n     prize,\n     remark,\n     petrol,\n     finish,\n     fauna\n|> SELECT\n     prize,\n     remark,\n     petrol,\n     finish\n|> AGGREGATE\n     SUM(petrol) AS unison\n   GROUP BY\n     prize\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"fauna",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    jersey,\n    square,\n    hangar,\n    weber,\n    kylie\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    jersey,\n    square,\n    hangar,\n    weber,\n    MAX(kylie) AS torch\n  FROM cte_0\n  GROUP BY\n    jersey,\n    square,\n    hangar,\n    weber\n),\ncte_2 AS (\n  SELECT\n    jersey,\n    square,\n    hangar,\n    weber,\n    COUNT(torch) AS affect\n  FROM cte_1\n  GROUP BY\n    jersey,\n    square,\n    hangar,\n    weber,\n    optic\n),\ncte_3 AS (\n  SELECT\n    SUM(jersey) AS midst,\n    SUM(hangar) AS motto\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    midst,\n    AVG(motto) AS series\n  FROM cte_3\n  GROUP BY\n    midst\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     jersey,\n     square,\n     hangar,\n     weber,\n     kylie\n|> AGGREGATE\n     MAX(kylie) AS torch\n   GROUP BY\n     jersey,\n     square,\n     hangar,\n     weber\n|> AGGREGATE\n     COUNT(torch) AS affect\n   GROUP BY\n     jersey,\n     square,\n     hangar,\n     weber,\n     optic\n|> AGGREGATE\n     SUM(jersey) AS midst,\n     SUM(hangar) AS motto\n|> AGGREGATE\n     AVG(motto) AS series\n   GROUP BY\n     midst\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"optic",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    trace,\n    digest,\n    valve,\n    mount,\n    reggae\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    trace,\n    digest,\n    valve,\n    mount,\n    SUM(reggae) AS comer\n  FROM cte_0\n  GROUP BY\n    trace,\n    digest,\n    valve,\n    mount\n),\ncte_2 AS (\n  SELECT\n    trace,\n    digest,\n    valve,\n    mount,\n    COUNT(comer) AS rhino,\n    AVG(comer) AS member\n  FROM cte_1\n  GROUP BY\n    trace,\n    digest,\n    click,\n    valve,\n    mount\n),\ncte_3 AS (\n  SELECT\n    trace,\n    digest\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(trace) AS pickup,\n    AVG(trace) AS steen\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     trace,\n     digest,\n     valve,\n     mount,\n     reggae\n|> AGGREGATE\n     SUM(reggae) AS comer\n   GROUP BY\n     trace,\n     digest,\n     valve,\n     mount\n|> AGGREGATE\n     COUNT(comer) AS rhino,\n     AVG(comer) AS member\n   GROUP BY\n     trace,\n     digest,\n     click,\n     valve,\n     mount\n|> SELECT\n     trace,\n     digest\n|> AGGREGATE\n     SUM(trace) AS pickup,\n     AVG(trace) AS steen\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"click",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    equity,\n    beach,\n    german,\n    folder,\n    battle\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    equity,\n    beach,\n    german,\n    folder,\n    SUM(battle) AS goblin,\n    MAX(battle) AS roach\n  FROM cte_0\n  GROUP BY\n    equity,\n    beach,\n    german,\n    folder\n),\ncte_2 AS (\n  SELECT\n    equity,\n    beach,\n    german,\n    folder,\n    MAX(roach) AS access\n  FROM cte_1\n  GROUP BY\n    equity,\n    beach,\n    pouch,\n    german,\n    folder\n),\ncte_3 AS (\n  SELECT\n    equity,\n    beach,\n    german\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    equity,\n    beach,\n    german\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     equity,\n     beach,\n     german,\n     folder,\n     battle\n|> AGGREGATE\n     SUM(battle) AS goblin,\n     MAX(battle) AS roach\n   GROUP BY\n     equity,\n     beach,\n     german,\n     folder\n|> AGGREGATE\n     MAX(roach) AS access\n   GROUP BY\n     equity,\n     beach,\n     pouch,\n     german,\n     folder\n|> SELECT\n     equity,\n     beach,\n     german\n|> SELECT\n     equity,\n     beach,\n     german\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"pouch",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    patch,\n    chile,\n    jersey,\n    favour,\n    bunker\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    patch,\n    chile,\n    jersey,\n    favour,\n    MAX(bunker) AS drink\n  FROM cte_0\n  GROUP BY\n    patch,\n    chile,\n    jersey,\n    favour\n),\ncte_2 AS (\n  SELECT\n    patch,\n    chile,\n    jersey,\n    favour,\n    AVG(drink) AS proof\n  FROM cte_1\n  GROUP BY\n    patch,\n    chile,\n    jersey,\n    favour,\n    stein\n),\ncte_3 AS (\n  SELECT\n    patch,\n    AVG(chile) AS fulham\n  FROM cte_2\n  GROUP BY\n    patch\n),\ncte_4 AS (\n  SELECT\n    MAX(patch) AS lesson,\n    MAX(fulham) AS studio\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     patch,\n     chile,\n     jersey,\n     favour,\n     bunker\n|> AGGREGATE\n     MAX(bunker) AS drink\n   GROUP BY\n     patch,\n     chile,\n     jersey,\n     favour\n|> AGGREGATE\n     AVG(drink) AS proof\n   GROUP BY\n     patch,\n     chile,\n     jersey,\n     favour,\n     stein\n|> AGGREGATE\n     AVG(chile) AS fulham\n   GROUP BY\n     patch\n|> AGGREGATE\n     MAX(patch) AS lesson,\n     MAX(fulham) AS studio\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"stein",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    prison,\n    bobby,\n    fiasco,\n    signal,\n    roche\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    prison,\n    bobby,\n    fiasco,\n    signal,\n    SUM(roche) AS onion\n  FROM cte_0\n  GROUP BY\n    prison,\n    bobby,\n    fiasco,\n    signal\n),\ncte_2 AS (\n  SELECT\n    prison,\n    bobby,\n    fiasco,\n    signal,\n    COUNT(onion) AS liquid,\n    SUM(onion) AS issue\n  FROM cte_1\n  GROUP BY\n    prison,\n    bobby,\n    mosaic,\n    fiasco,\n    signal\n),\ncte_3 AS (\n  SELECT\n    prison,\n    COUNT(signal) AS perry\n  FROM cte_2\n  GROUP BY\n    prison\n),\ncte_4 AS (\n  SELECT\n    prison,\n    perry\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     prison,\n     bobby,\n     fiasco,\n     signal,\n     roche\n|> AGGREGATE\n     SUM(roche) AS onion\n   GROUP BY\n     prison,\n     bobby,\n     fiasco,\n     signal\n|> AGGREGATE\n     COUNT(onion) AS liquid,\n     SUM(onion) AS issue\n   GROUP BY\n     prison,\n     bobby,\n     mosaic,\n     fiasco,\n     signal\n|> AGGREGATE\n     COUNT(signal) AS perry\n   GROUP BY\n     prison\n|> SELECT\n     prison,\n     perry\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"mosaic",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    slide,\n    genome,\n    piper,\n    manor,\n    marrow\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    slide,\n    genome,\n    piper,\n    manor,\n    COUNT(marrow) AS finale,\n    MAX(marrow) AS kylie\n  FROM cte_0\n  GROUP BY\n    slide,\n    genome,\n    piper,\n    manor\n),\ncte_2 AS (\n  SELECT\n    slide,\n    genome,\n    piper,\n    manor,\n    SUM(kylie) AS reason,\n    COUNT(finale) AS leave\n  FROM cte_1\n  GROUP BY\n    sheila,\n    slide,\n    genome,\n    piper,\n    manor\n),\ncte_3 AS (\n  SELECT\n    slide,\n    genome\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    slide,\n    genome\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     slide,\n     genome,\n     piper,\n     manor,\n     marrow\n|> AGGREGATE\n     COUNT(marrow) AS finale,\n     MAX(marrow) AS kylie\n   GROUP BY\n     slide,\n     genome,\n     piper,\n     manor\n|> AGGREGATE\n     SUM(kylie) AS reason,\n     COUNT(finale) AS leave\n   GROUP BY\n     sheila,\n     slide,\n     genome,\n     piper,\n     manor\n|> SELECT\n     slide,\n     genome\n|> SELECT\n     slide,\n     genome\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"sheila",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    willie,\n    vowel,\n    rival,\n    bomber,\n    cloth\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    willie,\n    vowel,\n    rival,\n    bomber,\n    COUNT(cloth) AS maxim\n  FROM cte_0\n  GROUP BY\n    willie,\n    vowel,\n    rival,\n    bomber\n),\ncte_2 AS (\n  SELECT\n    willie,\n    vowel,\n    rival,\n    bomber,\n    AVG(maxim) AS sound\n  FROM cte_1\n  GROUP BY\n    willie,\n    vowel,\n    rival,\n    bomber,\n    design\n),\ncte_3 AS (\n  SELECT\n    willie,\n    vowel,\n    SUM(rival) AS denim\n  FROM cte_2\n  GROUP BY\n    willie,\n    vowel\n),\ncte_4 AS (\n  SELECT\n    willie,\n    vowel\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     willie,\n     vowel,\n     rival,\n     bomber,\n     cloth\n|> AGGREGATE\n     COUNT(cloth) AS maxim\n   GROUP BY\n     willie,\n     vowel,\n     rival,\n     bomber\n|> AGGREGATE\n     AVG(maxim) AS sound\n   GROUP BY\n     willie,\n     vowel,\n     rival,\n     bomber,\n     design\n|> AGGREGATE\n     SUM(rival) AS denim\n   GROUP BY\n     willie,\n     vowel\n|> SELECT\n     willie,\n     vowel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:24,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"design",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    score,\n    sophie,\n    pollen,\n    speed,\n    patrol\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    score,\n    sophie,\n    pollen,\n    speed,\n    AVG(patrol) AS waiter\n  FROM cte_0\n  GROUP BY\n    score,\n    sophie,\n    pollen,\n    speed\n),\ncte_2 AS (\n  SELECT\n    score,\n    sophie,\n    pollen,\n    speed,\n    AVG(waiter) AS cinema,\n    MAX(waiter) AS ryder\n  FROM cte_1\n  GROUP BY\n    score,\n    sophie,\n    craft,\n    pollen,\n    speed\n),\ncte_3 AS (\n  SELECT\n    MAX(score) AS onset,\n    SUM(score) AS bamboo\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    onset,\n    bamboo\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     score,\n     sophie,\n     pollen,\n     speed,\n     patrol\n|> AGGREGATE\n     AVG(patrol) AS waiter\n   GROUP BY\n     score,\n     sophie,\n     pollen,\n     speed\n|> AGGREGATE\n     AVG(waiter) AS cinema,\n     MAX(waiter) AS ryder\n   GROUP BY\n     score,\n     sophie,\n     craft,\n     pollen,\n     speed\n|> AGGREGATE\n     MAX(score) AS onset,\n     SUM(score) AS bamboo\n|> SELECT\n     onset,\n     bamboo\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"craft",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    grange,\n    willy,\n    juice,\n    louis,\n    pellet\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    grange,\n    willy,\n    juice,\n    louis,\n    MIN(pellet) AS savage\n  FROM cte_0\n  GROUP BY\n    grange,\n    willy,\n    juice,\n    louis\n),\ncte_2 AS (\n  SELECT\n    grange,\n    willy,\n    MIN(juice) AS choir\n  FROM cte_1\n  GROUP BY\n    grange,\n    willy\n),\ncte_3 AS (\n  SELECT\n    grange,\n    willy,\n    MAX(choir) AS button\n  FROM cte_2\n  GROUP BY\n    gleam,\n    grange,\n    willy\n),\ncte_4 AS (\n  SELECT\n    grange,\n    willy,\n    COUNT(button) AS rubble\n  FROM cte_3\n  GROUP BY\n    grange,\n    willy\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     grange,\n     willy,\n     juice,\n     louis,\n     pellet\n|> AGGREGATE\n     MIN(pellet) AS savage\n   GROUP BY\n     grange,\n     willy,\n     juice,\n     louis\n|> AGGREGATE\n     MIN(juice) AS choir\n   GROUP BY\n     grange,\n     willy\n|> AGGREGATE\n     MAX(choir) AS button\n   GROUP BY\n     gleam,\n     grange,\n     willy\n|> AGGREGATE\n     COUNT(button) AS rubble\n   GROUP BY\n     grange,\n     willy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:24,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"gleam",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    widow,\n    paste,\n    chair,\n    knack,\n    stein\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    widow,\n    paste,\n    chair,\n    knack,\n    MIN(stein) AS month,\n    COUNT(stein) AS ernest,\n    AVG(stein) AS recess\n  FROM cte_0\n  GROUP BY\n    widow,\n    paste,\n    chair,\n    knack\n),\ncte_2 AS (\n  SELECT\n    widow,\n    paste,\n    chair,\n    knack,\n    MAX(recess) AS apron\n  FROM cte_1\n  GROUP BY\n    sperm,\n    widow,\n    paste,\n    chair,\n    knack\n),\ncte_3 AS (\n  SELECT\n    widow,\n    paste\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    widow,\n    paste\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     widow,\n     paste,\n     chair,\n     knack,\n     stein\n|> AGGREGATE\n     MIN(stein) AS month,\n     COUNT(stein) AS ernest,\n     AVG(stein) AS recess\n   GROUP BY\n     widow,\n     paste,\n     chair,\n     knack\n|> AGGREGATE\n     MAX(recess) AS apron\n   GROUP BY\n     sperm,\n     widow,\n     paste,\n     chair,\n     knack\n|> SELECT\n     widow,\n     paste\n|> SELECT\n     widow,\n     paste\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"sperm",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    breast,\n    senna,\n    style,\n    lunch,\n    matey\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    breast,\n    senna,\n    style,\n    lunch,\n    MAX(matey) AS start\n  FROM cte_0\n  GROUP BY\n    breast,\n    senna,\n    style,\n    lunch\n),\ncte_2 AS (\n  SELECT\n    breast,\n    senna,\n    style,\n    lunch,\n    SUM(start) AS grass\n  FROM cte_1\n  GROUP BY\n    breast,\n    senna,\n    style,\n    lunch,\n    float\n),\ncte_3 AS (\n  SELECT\n    COUNT(senna) AS thirst,\n    COUNT(grass) AS cough\n  FROM cte_2\n  GROUP BY\n    breast\n),\ncte_4 AS (\n  SELECT\n    MAX(cough) AS spark\n  FROM cte_3\n  GROUP BY\n    thirst\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     breast,\n     senna,\n     style,\n     lunch,\n     matey\n|> AGGREGATE\n     MAX(matey) AS start\n   GROUP BY\n     breast,\n     senna,\n     style,\n     lunch\n|> AGGREGATE\n     SUM(start) AS grass\n   GROUP BY\n     breast,\n     senna,\n     style,\n     lunch,\n     float\n|> AGGREGATE\n     COUNT(senna) AS thirst,\n     COUNT(grass) AS cough\n   GROUP BY\n     breast\n|> AGGREGATE\n     MAX(cough) AS spark\n   GROUP BY\n     thirst\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"float",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hurry,\n    sierra,\n    stella,\n    knock,\n    crest\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    hurry,\n    sierra,\n    stella,\n    AVG(knock) AS tucker\n  FROM cte_0\n  GROUP BY\n    hurry,\n    sierra,\n    stella\n),\ncte_2 AS (\n  SELECT\n    hurry,\n    sierra,\n    stella,\n    MIN(tucker) AS aggie\n  FROM cte_1\n  GROUP BY\n    hurry,\n    sierra,\n    stella\n),\ncte_3 AS (\n  SELECT\n    hurry,\n    sierra,\n    COUNT(aggie) AS cider\n  FROM cte_2\n  GROUP BY\n    outlet,\n    hurry,\n    sierra,\n    stella\n),\ncte_4 AS (\n  SELECT\n    MAX(cider) AS psalm,\n    MIN(cider) AS tenor\n  FROM cte_3\n  GROUP BY\n    hurry\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hurry,\n     sierra,\n     stella,\n     knock,\n     crest\n|> AGGREGATE\n     AVG(knock) AS tucker\n   GROUP BY\n     hurry,\n     sierra,\n     stella\n|> AGGREGATE\n     MIN(tucker) AS aggie\n   GROUP BY\n     hurry,\n     sierra,\n     stella\n|> AGGREGATE\n     COUNT(aggie) AS cider\n   GROUP BY\n     outlet,\n     hurry,\n     sierra,\n     stella\n|> AGGREGATE\n     MAX(cider) AS psalm,\n     MIN(cider) AS tenor\n   GROUP BY\n     hurry\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"outlet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    space,\n    shake,\n    sphere,\n    widow,\n    sigma\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    space,\n    shake,\n    sphere,\n    widow,\n    AVG(sigma) AS norman,\n    COUNT(sigma) AS build\n  FROM cte_0\n  GROUP BY\n    space,\n    shake,\n    sphere,\n    widow\n),\ncte_2 AS (\n  SELECT\n    space,\n    shake,\n    sphere,\n    widow,\n    COUNT(norman) AS denim\n  FROM cte_1\n  GROUP BY\n    space,\n    shake,\n    volume,\n    sphere,\n    widow\n),\ncte_3 AS (\n  SELECT\n    MAX(sphere) AS mister,\n    COUNT(shake) AS merit\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(mister) AS swing\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     space,\n     shake,\n     sphere,\n     widow,\n     sigma\n|> AGGREGATE\n     AVG(sigma) AS norman,\n     COUNT(sigma) AS build\n   GROUP BY\n     space,\n     shake,\n     sphere,\n     widow\n|> AGGREGATE\n     COUNT(norman) AS denim\n   GROUP BY\n     space,\n     shake,\n     volume,\n     sphere,\n     widow\n|> AGGREGATE\n     MAX(sphere) AS mister,\n     COUNT(shake) AS merit\n|> AGGREGATE\n     AVG(mister) AS swing\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"volume",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    guido,\n    bypass,\n    wicket,\n    costa,\n    repeal\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    guido,\n    bypass,\n    wicket,\n    costa,\n    COUNT(repeal) AS flora\n  FROM cte_0\n  GROUP BY\n    guido,\n    bypass,\n    wicket,\n    costa\n),\ncte_2 AS (\n  SELECT\n    guido,\n    bypass,\n    wicket,\n    costa,\n    SUM(flora) AS ratio\n  FROM cte_1\n  GROUP BY\n    guido,\n    bypass,\n    wicket,\n    costa,\n    chill\n),\ncte_3 AS (\n  SELECT\n    COUNT(wicket) AS secret\n  FROM cte_2\n  GROUP BY\n    guido,\n    bypass\n),\ncte_4 AS (\n  SELECT\n    SUM(secret) AS break,\n    COUNT(secret) AS layout\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     guido,\n     bypass,\n     wicket,\n     costa,\n     repeal\n|> AGGREGATE\n     COUNT(repeal) AS flora\n   GROUP BY\n     guido,\n     bypass,\n     wicket,\n     costa\n|> AGGREGATE\n     SUM(flora) AS ratio\n   GROUP BY\n     guido,\n     bypass,\n     wicket,\n     costa,\n     chill\n|> AGGREGATE\n     COUNT(wicket) AS secret\n   GROUP BY\n     guido,\n     bypass\n|> AGGREGATE\n     SUM(secret) AS break,\n     COUNT(secret) AS layout\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"chill",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    exile,\n    ulcer,\n    anthem,\n    belly,\n    stint\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    exile,\n    ulcer,\n    anthem,\n    belly,\n    MIN(stint) AS origin\n  FROM cte_0\n  GROUP BY\n    exile,\n    ulcer,\n    anthem,\n    belly\n),\ncte_2 AS (\n  SELECT\n    exile,\n    ulcer,\n    anthem,\n    belly,\n    COUNT(origin) AS dough\n  FROM cte_1\n  GROUP BY\n    exile,\n    ulcer,\n    anthem,\n    belly,\n    frame\n),\ncte_3 AS (\n  SELECT\n    exile,\n    MIN(dough) AS almond,\n    COUNT(belly) AS condom\n  FROM cte_2\n  GROUP BY\n    exile\n),\ncte_4 AS (\n  SELECT\n    MIN(exile) AS laura\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     exile,\n     ulcer,\n     anthem,\n     belly,\n     stint\n|> AGGREGATE\n     MIN(stint) AS origin\n   GROUP BY\n     exile,\n     ulcer,\n     anthem,\n     belly\n|> AGGREGATE\n     COUNT(origin) AS dough\n   GROUP BY\n     exile,\n     ulcer,\n     anthem,\n     belly,\n     frame\n|> AGGREGATE\n     MIN(dough) AS almond,\n     COUNT(belly) AS condom\n   GROUP BY\n     exile\n|> AGGREGATE\n     MIN(exile) AS laura\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"frame",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    esteem,\n    mixer,\n    metre,\n    update,\n    result\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    esteem,\n    mixer,\n    metre,\n    update,\n    SUM(result) AS joseph\n  FROM cte_0\n  GROUP BY\n    esteem,\n    mixer,\n    metre,\n    update\n),\ncte_2 AS (\n  SELECT\n    esteem,\n    mixer,\n    MAX(joseph) AS barton\n  FROM cte_1\n  GROUP BY\n    esteem,\n    mixer\n),\ncte_3 AS (\n  SELECT\n    esteem,\n    mixer,\n    MIN(barton) AS stump\n  FROM cte_2\n  GROUP BY\n    demand,\n    esteem,\n    mixer\n),\ncte_4 AS (\n  SELECT\n    AVG(stump) AS cinema,\n    MAX(esteem) AS resale,\n    MAX(mixer) AS arcade\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     esteem,\n     mixer,\n     metre,\n     update,\n     result\n|> AGGREGATE\n     SUM(result) AS joseph\n   GROUP BY\n     esteem,\n     mixer,\n     metre,\n     update\n|> AGGREGATE\n     MAX(joseph) AS barton\n   GROUP BY\n     esteem,\n     mixer\n|> AGGREGATE\n     MIN(barton) AS stump\n   GROUP BY\n     demand,\n     esteem,\n     mixer\n|> AGGREGATE\n     AVG(stump) AS cinema,\n     MAX(esteem) AS resale,\n     MAX(mixer) AS arcade\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"demand",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    dream,\n    basic,\n    cattle,\n    kelly,\n    uplift\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    dream,\n    basic,\n    cattle,\n    kelly,\n    MIN(uplift) AS summer\n  FROM cte_0\n  GROUP BY\n    dream,\n    basic,\n    cattle,\n    kelly\n),\ncte_2 AS (\n  SELECT\n    dream,\n    basic,\n    MIN(summer) AS laugh\n  FROM cte_1\n  GROUP BY\n    dream,\n    basic\n),\ncte_3 AS (\n  SELECT\n    dream,\n    basic,\n    COUNT(laugh) AS morgan\n  FROM cte_2\n  GROUP BY\n    blade,\n    dream,\n    basic\n),\ncte_4 AS (\n  SELECT\n    dream,\n    basic,\n    COUNT(morgan) AS maiden\n  FROM cte_3\n  GROUP BY\n    dream,\n    basic\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     dream,\n     basic,\n     cattle,\n     kelly,\n     uplift\n|> AGGREGATE\n     MIN(uplift) AS summer\n   GROUP BY\n     dream,\n     basic,\n     cattle,\n     kelly\n|> AGGREGATE\n     MIN(summer) AS laugh\n   GROUP BY\n     dream,\n     basic\n|> AGGREGATE\n     COUNT(laugh) AS morgan\n   GROUP BY\n     blade,\n     dream,\n     basic\n|> AGGREGATE\n     COUNT(morgan) AS maiden\n   GROUP BY\n     dream,\n     basic\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:24,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"blade",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    intel,\n    violin,\n    flint,\n    police,\n    terry\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    intel,\n    violin,\n    flint,\n    police,\n    COUNT(terry) AS whale\n  FROM cte_0\n  GROUP BY\n    intel,\n    violin,\n    flint,\n    police\n),\ncte_2 AS (\n  SELECT\n    intel,\n    violin,\n    flint,\n    police,\n    COUNT(whale) AS saucer\n  FROM cte_1\n  GROUP BY\n    intel,\n    violin,\n    flint,\n    police,\n    stable\n),\ncte_3 AS (\n  SELECT\n    intel,\n    MAX(saucer) AS oxide\n  FROM cte_2\n  GROUP BY\n    intel\n),\ncte_4 AS (\n  SELECT\n    COUNT(intel) AS title,\n    SUM(intel) AS dolly\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     intel,\n     violin,\n     flint,\n     police,\n     terry\n|> AGGREGATE\n     COUNT(terry) AS whale\n   GROUP BY\n     intel,\n     violin,\n     flint,\n     police\n|> AGGREGATE\n     COUNT(whale) AS saucer\n   GROUP BY\n     intel,\n     violin,\n     flint,\n     police,\n     stable\n|> AGGREGATE\n     MAX(saucer) AS oxide\n   GROUP BY\n     intel\n|> AGGREGATE\n     COUNT(intel) AS title,\n     SUM(intel) AS dolly\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"stable",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    whole,\n    parish,\n    brown,\n    dobson,\n    glory\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    whole,\n    parish,\n    brown,\n    dobson,\n    COUNT(glory) AS letter\n  FROM cte_0\n  GROUP BY\n    whole,\n    parish,\n    brown,\n    dobson\n),\ncte_2 AS (\n  SELECT\n    whole,\n    parish,\n    brown,\n    dobson,\n    SUM(letter) AS cavity\n  FROM cte_1\n  GROUP BY\n    whole,\n    parish,\n    brown,\n    dobson,\n    phrase\n),\ncte_3 AS (\n  SELECT\n    whole,\n    parish,\n    SUM(dobson) AS bonnet\n  FROM cte_2\n  GROUP BY\n    whole,\n    parish\n),\ncte_4 AS (\n  SELECT\n    COUNT(bonnet) AS covent\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     whole,\n     parish,\n     brown,\n     dobson,\n     glory\n|> AGGREGATE\n     COUNT(glory) AS letter\n   GROUP BY\n     whole,\n     parish,\n     brown,\n     dobson\n|> AGGREGATE\n     SUM(letter) AS cavity\n   GROUP BY\n     whole,\n     parish,\n     brown,\n     dobson,\n     phrase\n|> AGGREGATE\n     SUM(dobson) AS bonnet\n   GROUP BY\n     whole,\n     parish\n|> AGGREGATE\n     COUNT(bonnet) AS covent\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:24,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"phrase",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    radius,\n    suede,\n    damage,\n    shoot,\n    dwarf\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    radius,\n    suede,\n    damage,\n    shoot,\n    AVG(dwarf) AS wheel\n  FROM cte_0\n  GROUP BY\n    radius,\n    suede,\n    damage,\n    shoot\n),\ncte_2 AS (\n  SELECT\n    radius,\n    suede,\n    damage,\n    shoot,\n    SUM(wheel) AS plight,\n    COUNT(wheel) AS remedy\n  FROM cte_1\n  GROUP BY\n    radius,\n    suede,\n    tongue,\n    damage,\n    shoot\n),\ncte_3 AS (\n  SELECT\n    radius,\n    suede,\n    damage,\n    shoot\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    radius,\n    suede\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     radius,\n     suede,\n     damage,\n     shoot,\n     dwarf\n|> AGGREGATE\n     AVG(dwarf) AS wheel\n   GROUP BY\n     radius,\n     suede,\n     damage,\n     shoot\n|> AGGREGATE\n     SUM(wheel) AS plight,\n     COUNT(wheel) AS remedy\n   GROUP BY\n     radius,\n     suede,\n     tongue,\n     damage,\n     shoot\n|> SELECT\n     radius,\n     suede,\n     damage,\n     shoot\n|> SELECT\n     radius,\n     suede\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"tongue",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    treaty,\n    spate,\n    murmur,\n    blast,\n    shirt\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    treaty,\n    spate,\n    murmur,\n    blast,\n    AVG(shirt) AS wally\n  FROM cte_0\n  GROUP BY\n    treaty,\n    spate,\n    murmur,\n    blast\n),\ncte_2 AS (\n  SELECT\n    treaty,\n    spate,\n    murmur,\n    blast,\n    COUNT(wally) AS enamel,\n    AVG(wally) AS hitch\n  FROM cte_1\n  GROUP BY\n    treaty,\n    spate,\n    madge,\n    murmur,\n    blast\n),\ncte_3 AS (\n  SELECT\n    treaty,\n    spate,\n    murmur\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    treaty,\n    spate,\n    murmur\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     treaty,\n     spate,\n     murmur,\n     blast,\n     shirt\n|> AGGREGATE\n     AVG(shirt) AS wally\n   GROUP BY\n     treaty,\n     spate,\n     murmur,\n     blast\n|> AGGREGATE\n     COUNT(wally) AS enamel,\n     AVG(wally) AS hitch\n   GROUP BY\n     treaty,\n     spate,\n     madge,\n     murmur,\n     blast\n|> SELECT\n     treaty,\n     spate,\n     murmur\n|> SELECT\n     treaty,\n     spate,\n     murmur\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"madge",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    jelly,\n    lobby,\n    jewel,\n    cigar,\n    anchor\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    jelly,\n    lobby,\n    jewel,\n    cigar,\n    SUM(anchor) AS altar,\n    AVG(anchor) AS buffet\n  FROM cte_0\n  GROUP BY\n    jelly,\n    lobby,\n    jewel,\n    cigar\n),\ncte_2 AS (\n  SELECT\n    jelly,\n    lobby,\n    jewel,\n    cigar,\n    AVG(buffet) AS stead\n  FROM cte_1\n  GROUP BY\n    jelly,\n    lobby,\n    closet,\n    jewel,\n    cigar\n),\ncte_3 AS (\n  SELECT\n    jelly,\n    lobby,\n    jewel\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    jelly,\n    lobby,\n    jewel\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     jelly,\n     lobby,\n     jewel,\n     cigar,\n     anchor\n|> AGGREGATE\n     SUM(anchor) AS altar,\n     AVG(anchor) AS buffet\n   GROUP BY\n     jelly,\n     lobby,\n     jewel,\n     cigar\n|> AGGREGATE\n     AVG(buffet) AS stead\n   GROUP BY\n     jelly,\n     lobby,\n     closet,\n     jewel,\n     cigar\n|> SELECT\n     jelly,\n     lobby,\n     jewel\n|> SELECT\n     jelly,\n     lobby,\n     jewel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"closet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    zebra,\n    opium,\n    miller,\n    little,\n    brief\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    zebra,\n    opium,\n    miller,\n    little,\n    SUM(brief) AS guise\n  FROM cte_0\n  GROUP BY\n    zebra,\n    opium,\n    miller,\n    little\n),\ncte_2 AS (\n  SELECT\n    zebra,\n    opium,\n    miller,\n    little,\n    MIN(guise) AS weaver\n  FROM cte_1\n  GROUP BY\n    zebra,\n    opium,\n    miller,\n    little,\n    darwin\n),\ncte_3 AS (\n  SELECT\n    zebra,\n    opium,\n    miller,\n    little\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    zebra,\n    MAX(little) AS nylon\n  FROM cte_3\n  GROUP BY\n    zebra\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     zebra,\n     opium,\n     miller,\n     little,\n     brief\n|> AGGREGATE\n     SUM(brief) AS guise\n   GROUP BY\n     zebra,\n     opium,\n     miller,\n     little\n|> AGGREGATE\n     MIN(guise) AS weaver\n   GROUP BY\n     zebra,\n     opium,\n     miller,\n     little,\n     darwin\n|> SELECT\n     zebra,\n     opium,\n     miller,\n     little\n|> AGGREGATE\n     MAX(little) AS nylon\n   GROUP BY\n     zebra\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"darwin",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    manner,\n    attic,\n    cliff,\n    colour,\n    lesion\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    manner,\n    attic,\n    cliff,\n    colour,\n    MAX(lesion) AS bugger\n  FROM cte_0\n  GROUP BY\n    manner,\n    attic,\n    cliff,\n    colour\n),\ncte_2 AS (\n  SELECT\n    manner,\n    attic,\n    MIN(cliff) AS novel\n  FROM cte_1\n  GROUP BY\n    manner,\n    attic\n),\ncte_3 AS (\n  SELECT\n    manner,\n    attic,\n    MIN(novel) AS burger\n  FROM cte_2\n  GROUP BY\n    groove,\n    manner,\n    attic\n),\ncte_4 AS (\n  SELECT\n    AVG(burger) AS tycoon,\n    MIN(burger) AS bingo\n  FROM cte_3\n  GROUP BY\n    manner,\n    attic\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     manner,\n     attic,\n     cliff,\n     colour,\n     lesion\n|> AGGREGATE\n     MAX(lesion) AS bugger\n   GROUP BY\n     manner,\n     attic,\n     cliff,\n     colour\n|> AGGREGATE\n     MIN(cliff) AS novel\n   GROUP BY\n     manner,\n     attic\n|> AGGREGATE\n     MIN(novel) AS burger\n   GROUP BY\n     groove,\n     manner,\n     attic\n|> AGGREGATE\n     AVG(burger) AS tycoon,\n     MIN(burger) AS bingo\n   GROUP BY\n     manner,\n     attic\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"groove",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    merger,\n    sleeve,\n    badge,\n    shape,\n    dance\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    merger,\n    sleeve,\n    badge,\n    MAX(dance) AS hotel\n  FROM cte_0\n  GROUP BY\n    merger,\n    sleeve,\n    badge\n),\ncte_2 AS (\n  SELECT\n    merger,\n    sleeve,\n    badge,\n    COUNT(hotel) AS peril\n  FROM cte_1\n  GROUP BY\n    merger,\n    sleeve,\n    badge\n),\ncte_3 AS (\n  SELECT\n    merger,\n    sleeve,\n    COUNT(peril) AS stench\n  FROM cte_2\n  GROUP BY\n    gland,\n    merger,\n    sleeve\n),\ncte_4 AS (\n  SELECT\n    MIN(stench) AS cannon,\n    COUNT(stench) AS advent\n  FROM cte_3\n  GROUP BY\n    merger,\n    sleeve\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     merger,\n     sleeve,\n     badge,\n     shape,\n     dance\n|> AGGREGATE\n     MAX(dance) AS hotel\n   GROUP BY\n     merger,\n     sleeve,\n     badge\n|> AGGREGATE\n     COUNT(hotel) AS peril\n   GROUP BY\n     merger,\n     sleeve,\n     badge\n|> AGGREGATE\n     COUNT(peril) AS stench\n   GROUP BY\n     gland,\n     merger,\n     sleeve\n|> AGGREGATE\n     MIN(stench) AS cannon,\n     COUNT(stench) AS advent\n   GROUP BY\n     merger,\n     sleeve\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"gland",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    spread,\n    bureau,\n    sprint,\n    smack,\n    prize\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    spread,\n    bureau,\n    sprint,\n    COUNT(prize) AS crook\n  FROM cte_0\n  GROUP BY\n    spread,\n    bureau,\n    sprint\n),\ncte_2 AS (\n  SELECT\n    spread,\n    bureau,\n    sprint,\n    MAX(crook) AS fancy\n  FROM cte_1\n  GROUP BY\n    spread,\n    bureau,\n    sprint\n),\ncte_3 AS (\n  SELECT\n    spread,\n    bureau,\n    AVG(fancy) AS sweat\n  FROM cte_2\n  GROUP BY\n    barker,\n    spread,\n    bureau\n),\ncte_4 AS (\n  SELECT\n    COUNT(sweat) AS feast,\n    AVG(sweat) AS dealer\n  FROM cte_3\n  GROUP BY\n    spread,\n    bureau\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     spread,\n     bureau,\n     sprint,\n     smack,\n     prize\n|> AGGREGATE\n     COUNT(prize) AS crook\n   GROUP BY\n     spread,\n     bureau,\n     sprint\n|> AGGREGATE\n     MAX(crook) AS fancy\n   GROUP BY\n     spread,\n     bureau,\n     sprint\n|> AGGREGATE\n     AVG(fancy) AS sweat\n   GROUP BY\n     barker,\n     spread,\n     bureau\n|> AGGREGATE\n     COUNT(sweat) AS feast,\n     AVG(sweat) AS dealer\n   GROUP BY\n     spread,\n     bureau\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"barker",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    slide,\n    crush,\n    tremor,\n    dummy,\n    cotton\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    slide,\n    crush,\n    tremor,\n    dummy,\n    SUM(cotton) AS nether\n  FROM cte_0\n  GROUP BY\n    slide,\n    crush,\n    tremor,\n    dummy\n),\ncte_2 AS (\n  SELECT\n    slide,\n    crush,\n    tremor,\n    dummy,\n    MIN(nether) AS centre\n  FROM cte_1\n  GROUP BY\n    slide,\n    crush,\n    tremor,\n    dummy,\n    flute\n),\ncte_3 AS (\n  SELECT\n    COUNT(tremor) AS bobby\n  FROM cte_2\n  GROUP BY\n    slide,\n    crush\n),\ncte_4 AS (\n  SELECT\n    AVG(bobby) AS shrimp,\n    MAX(bobby) AS cobalt\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     slide,\n     crush,\n     tremor,\n     dummy,\n     cotton\n|> AGGREGATE\n     SUM(cotton) AS nether\n   GROUP BY\n     slide,\n     crush,\n     tremor,\n     dummy\n|> AGGREGATE\n     MIN(nether) AS centre\n   GROUP BY\n     slide,\n     crush,\n     tremor,\n     dummy,\n     flute\n|> AGGREGATE\n     COUNT(tremor) AS bobby\n   GROUP BY\n     slide,\n     crush\n|> AGGREGATE\n     AVG(bobby) AS shrimp,\n     MAX(bobby) AS cobalt\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"flute",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    alarm,\n    knack,\n    smile,\n    excuse,\n    front\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    alarm,\n    knack,\n    smile,\n    MIN(excuse) AS morris\n  FROM cte_0\n  GROUP BY\n    alarm,\n    knack,\n    smile\n),\ncte_2 AS (\n  SELECT\n    alarm,\n    knack,\n    smile,\n    SUM(morris) AS copper\n  FROM cte_1\n  GROUP BY\n    alarm,\n    knack,\n    smile\n),\ncte_3 AS (\n  SELECT\n    alarm,\n    knack,\n    COUNT(copper) AS curry\n  FROM cte_2\n  GROUP BY\n    kitty,\n    alarm,\n    knack,\n    smile\n),\ncte_4 AS (\n  SELECT\n    alarm,\n    COUNT(curry) AS arson\n  FROM cte_3\n  GROUP BY\n    alarm,\n    knack\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     alarm,\n     knack,\n     smile,\n     excuse,\n     front\n|> AGGREGATE\n     MIN(excuse) AS morris\n   GROUP BY\n     alarm,\n     knack,\n     smile\n|> AGGREGATE\n     SUM(morris) AS copper\n   GROUP BY\n     alarm,\n     knack,\n     smile\n|> AGGREGATE\n     COUNT(copper) AS curry\n   GROUP BY\n     kitty,\n     alarm,\n     knack,\n     smile\n|> AGGREGATE\n     COUNT(curry) AS arson\n   GROUP BY\n     alarm,\n     knack\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"kitty",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    bluff,\n    video,\n    walnut,\n    butter,\n    branch\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    bluff,\n    video,\n    walnut,\n    butter,\n    MIN(branch) AS cycle\n  FROM cte_0\n  GROUP BY\n    bluff,\n    video,\n    walnut,\n    butter\n),\ncte_2 AS (\n  SELECT\n    bluff,\n    video,\n    walnut,\n    butter,\n    COUNT(cycle) AS cursor,\n    MIN(cycle) AS window,\n    MAX(cycle) AS forth\n  FROM cte_1\n  GROUP BY\n    impact,\n    bluff,\n    video,\n    walnut,\n    butter\n),\ncte_3 AS (\n  SELECT\n    SUM(bluff) AS ladder\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(ladder) AS sheila\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     bluff,\n     video,\n     walnut,\n     butter,\n     branch\n|> AGGREGATE\n     MIN(branch) AS cycle\n   GROUP BY\n     bluff,\n     video,\n     walnut,\n     butter\n|> AGGREGATE\n     COUNT(cycle) AS cursor,\n     MIN(cycle) AS window,\n     MAX(cycle) AS forth\n   GROUP BY\n     impact,\n     bluff,\n     video,\n     walnut,\n     butter\n|> AGGREGATE\n     SUM(bluff) AS ladder\n|> AGGREGATE\n     MIN(ladder) AS sheila\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"impact",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    count,\n    growth,\n    crater,\n    martin,\n    budget\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    count,\n    growth,\n    crater,\n    martin,\n    SUM(budget) AS cable\n  FROM cte_0\n  GROUP BY\n    count,\n    growth,\n    crater,\n    martin\n),\ncte_2 AS (\n  SELECT\n    count,\n    growth,\n    crater,\n    martin,\n    COUNT(cable) AS grade\n  FROM cte_1\n  GROUP BY\n    count,\n    growth,\n    crater,\n    martin,\n    cannon\n),\ncte_3 AS (\n  SELECT\n    count,\n    growth,\n    crater,\n    martin\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(count) AS spence,\n    AVG(count) AS rebel\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     count,\n     growth,\n     crater,\n     martin,\n     budget\n|> AGGREGATE\n     SUM(budget) AS cable\n   GROUP BY\n     count,\n     growth,\n     crater,\n     martin\n|> AGGREGATE\n     COUNT(cable) AS grade\n   GROUP BY\n     count,\n     growth,\n     crater,\n     martin,\n     cannon\n|> SELECT\n     count,\n     growth,\n     crater,\n     martin\n|> AGGREGATE\n     MAX(count) AS spence,\n     AVG(count) AS rebel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"cannon",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    irony,\n    alarm,\n    boiler,\n    series,\n    volume\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    irony,\n    alarm,\n    boiler,\n    series,\n    COUNT(volume) AS parker\n  FROM cte_0\n  GROUP BY\n    irony,\n    alarm,\n    boiler,\n    series\n),\ncte_2 AS (\n  SELECT\n    irony,\n    alarm,\n    boiler,\n    series,\n    COUNT(parker) AS maiden\n  FROM cte_1\n  GROUP BY\n    irony,\n    alarm,\n    boiler,\n    series,\n    foley\n),\ncte_3 AS (\n  SELECT\n    irony,\n    alarm,\n    boiler,\n    series\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    irony,\n    AVG(series) AS stable\n  FROM cte_3\n  GROUP BY\n    irony\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     irony,\n     alarm,\n     boiler,\n     series,\n     volume\n|> AGGREGATE\n     COUNT(volume) AS parker\n   GROUP BY\n     irony,\n     alarm,\n     boiler,\n     series\n|> AGGREGATE\n     COUNT(parker) AS maiden\n   GROUP BY\n     irony,\n     alarm,\n     boiler,\n     series,\n     foley\n|> SELECT\n     irony,\n     alarm,\n     boiler,\n     series\n|> AGGREGATE\n     AVG(series) AS stable\n   GROUP BY\n     irony\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"foley",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    blame,\n    elbow,\n    person,\n    fungus,\n    bother\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    blame,\n    elbow,\n    person,\n    fungus,\n    MAX(bother) AS creek,\n    MIN(bother) AS comet\n  FROM cte_0\n  GROUP BY\n    blame,\n    elbow,\n    person,\n    fungus\n),\ncte_2 AS (\n  SELECT\n    blame,\n    elbow,\n    person,\n    fungus,\n    SUM(comet) AS debtor\n  FROM cte_1\n  GROUP BY\n    blame,\n    elbow,\n    relish,\n    person,\n    fungus\n),\ncte_3 AS (\n  SELECT\n    blame,\n    elbow,\n    person\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(person) AS carrot\n  FROM cte_3\n  GROUP BY\n    blame\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     blame,\n     elbow,\n     person,\n     fungus,\n     bother\n|> AGGREGATE\n     MAX(bother) AS creek,\n     MIN(bother) AS comet\n   GROUP BY\n     blame,\n     elbow,\n     person,\n     fungus\n|> AGGREGATE\n     SUM(comet) AS debtor\n   GROUP BY\n     blame,\n     elbow,\n     relish,\n     person,\n     fungus\n|> SELECT\n     blame,\n     elbow,\n     person\n|> AGGREGATE\n     AVG(person) AS carrot\n   GROUP BY\n     blame\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"relish",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    piety,\n    curve,\n    libel,\n    henry,\n    lance\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    piety,\n    curve,\n    libel,\n    henry,\n    AVG(lance) AS cement,\n    MIN(lance) AS behalf\n  FROM cte_0\n  GROUP BY\n    piety,\n    curve,\n    libel,\n    henry\n),\ncte_2 AS (\n  SELECT\n    piety,\n    curve,\n    libel,\n    henry,\n    COUNT(behalf) AS boiler\n  FROM cte_1\n  GROUP BY\n    piety,\n    curve,\n    price,\n    libel,\n    henry\n),\ncte_3 AS (\n  SELECT\n    piety,\n    curve\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MAX(piety) AS cellar,\n    COUNT(curve) AS bowie\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     piety,\n     curve,\n     libel,\n     henry,\n     lance\n|> AGGREGATE\n     AVG(lance) AS cement,\n     MIN(lance) AS behalf\n   GROUP BY\n     piety,\n     curve,\n     libel,\n     henry\n|> AGGREGATE\n     COUNT(behalf) AS boiler\n   GROUP BY\n     piety,\n     curve,\n     price,\n     libel,\n     henry\n|> SELECT\n     piety,\n     curve\n|> AGGREGATE\n     MAX(piety) AS cellar,\n     COUNT(curve) AS bowie\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"price",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    slide,\n    puppet,\n    mortar,\n    hooper,\n    satan\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    slide,\n    puppet,\n    mortar,\n    hooper,\n    MIN(satan) AS grape,\n    SUM(satan) AS vogue\n  FROM cte_0\n  GROUP BY\n    slide,\n    puppet,\n    mortar,\n    hooper\n),\ncte_2 AS (\n  SELECT\n    slide,\n    puppet,\n    mortar,\n    hooper,\n    MAX(grape) AS decade\n  FROM cte_1\n  GROUP BY\n    slide,\n    puppet,\n    choir,\n    mortar,\n    hooper\n),\ncte_3 AS (\n  SELECT\n    slide,\n    MIN(hooper) AS threat\n  FROM cte_2\n  GROUP BY\n    slide\n),\ncte_4 AS (\n  SELECT\n    slide,\n    threat\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     slide,\n     puppet,\n     mortar,\n     hooper,\n     satan\n|> AGGREGATE\n     MIN(satan) AS grape,\n     SUM(satan) AS vogue\n   GROUP BY\n     slide,\n     puppet,\n     mortar,\n     hooper\n|> AGGREGATE\n     MAX(grape) AS decade\n   GROUP BY\n     slide,\n     puppet,\n     choir,\n     mortar,\n     hooper\n|> AGGREGATE\n     MIN(hooper) AS threat\n   GROUP BY\n     slide\n|> SELECT\n     slide,\n     threat\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"choir",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    hearer,\n    guinea,\n    drama,\n    drake,\n    perry\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    hearer,\n    guinea,\n    drama,\n    drake,\n    MAX(perry) AS guest,\n    AVG(perry) AS family\n  FROM cte_0\n  GROUP BY\n    hearer,\n    guinea,\n    drama,\n    drake\n),\ncte_2 AS (\n  SELECT\n    hearer,\n    guinea,\n    drama,\n    drake,\n    MAX(family) AS reach\n  FROM cte_1\n  GROUP BY\n    hearer,\n    guinea,\n    status,\n    drama,\n    drake\n),\ncte_3 AS (\n  SELECT\n    AVG(hearer) AS graph,\n    AVG(reach) AS visit\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(visit) AS motor\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     hearer,\n     guinea,\n     drama,\n     drake,\n     perry\n|> AGGREGATE\n     MAX(perry) AS guest,\n     AVG(perry) AS family\n   GROUP BY\n     hearer,\n     guinea,\n     drama,\n     drake\n|> AGGREGATE\n     MAX(family) AS reach\n   GROUP BY\n     hearer,\n     guinea,\n     status,\n     drama,\n     drake\n|> AGGREGATE\n     AVG(hearer) AS graph,\n     AVG(reach) AS visit\n|> AGGREGATE\n     MIN(visit) AS motor\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"status",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ivory,\n    enigma,\n    colin,\n    spence,\n    clive\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    ivory,\n    enigma,\n    colin,\n    spence,\n    MAX(clive) AS mortar,\n    MIN(clive) AS newco\n  FROM cte_0\n  GROUP BY\n    ivory,\n    enigma,\n    colin,\n    spence\n),\ncte_2 AS (\n  SELECT\n    ivory,\n    enigma,\n    colin,\n    spence,\n    MAX(mortar) AS tallis\n  FROM cte_1\n  GROUP BY\n    ivory,\n    enigma,\n    flour,\n    colin,\n    spence\n),\ncte_3 AS (\n  SELECT\n    ivory,\n    COUNT(tallis) AS throat\n  FROM cte_2\n  GROUP BY\n    ivory\n),\ncte_4 AS (\n  SELECT\n    ivory,\n    throat\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ivory,\n     enigma,\n     colin,\n     spence,\n     clive\n|> AGGREGATE\n     MAX(clive) AS mortar,\n     MIN(clive) AS newco\n   GROUP BY\n     ivory,\n     enigma,\n     colin,\n     spence\n|> AGGREGATE\n     MAX(mortar) AS tallis\n   GROUP BY\n     ivory,\n     enigma,\n     flour,\n     colin,\n     spence\n|> AGGREGATE\n     COUNT(tallis) AS throat\n   GROUP BY\n     ivory\n|> SELECT\n     ivory,\n     throat\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"flour",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    dairy,\n    grave,\n    meter,\n    phone,\n    hogan\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    dairy,\n    grave,\n    meter,\n    phone,\n    MAX(hogan) AS dolly\n  FROM cte_0\n  GROUP BY\n    dairy,\n    grave,\n    meter,\n    phone\n),\ncte_2 AS (\n  SELECT\n    dairy,\n    grave,\n    meter,\n    phone,\n    SUM(dolly) AS chain\n  FROM cte_1\n  GROUP BY\n    dairy,\n    grave,\n    meter,\n    phone,\n    league\n),\ncte_3 AS (\n  SELECT\n    dairy,\n    grave,\n    meter,\n    phone,\n    chain\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    dairy,\n    grave,\n    meter\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     dairy,\n     grave,\n     meter,\n     phone,\n     hogan\n|> AGGREGATE\n     MAX(hogan) AS dolly\n   GROUP BY\n     dairy,\n     grave,\n     meter,\n     phone\n|> AGGREGATE\n     SUM(dolly) AS chain\n   GROUP BY\n     dairy,\n     grave,\n     meter,\n     phone,\n     league\n|> SELECT\n     dairy,\n     grave,\n     meter,\n     phone,\n     chain\n|> SELECT\n     dairy,\n     grave,\n     meter\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"league",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    filth,\n    party,\n    second,\n    berry,\n    motive\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    filth,\n    party,\n    second,\n    berry,\n    AVG(motive) AS clutch\n  FROM cte_0\n  GROUP BY\n    filth,\n    party,\n    second,\n    berry\n),\ncte_2 AS (\n  SELECT\n    filth,\n    party,\n    second,\n    berry,\n    MIN(clutch) AS resale,\n    MAX(clutch) AS flank,\n    AVG(clutch) AS flask\n  FROM cte_1\n  GROUP BY\n    brake,\n    filth,\n    party,\n    second,\n    berry\n),\ncte_3 AS (\n  SELECT\n    MAX(flask) AS crack\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(crack) AS warsaw\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     filth,\n     party,\n     second,\n     berry,\n     motive\n|> AGGREGATE\n     AVG(motive) AS clutch\n   GROUP BY\n     filth,\n     party,\n     second,\n     berry\n|> AGGREGATE\n     MIN(clutch) AS resale,\n     MAX(clutch) AS flank,\n     AVG(clutch) AS flask\n   GROUP BY\n     brake,\n     filth,\n     party,\n     second,\n     berry\n|> AGGREGATE\n     MAX(flask) AS crack\n|> AGGREGATE\n     MIN(crack) AS warsaw\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"brake",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    family,\n    offer,\n    taboo,\n    penny,\n    debtor\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    family,\n    offer,\n    taboo,\n    penny,\n    SUM(debtor) AS rover,\n    AVG(debtor) AS middle,\n    COUNT(debtor) AS marker\n  FROM cte_0\n  GROUP BY\n    family,\n    offer,\n    taboo,\n    penny\n),\ncte_2 AS (\n  SELECT\n    family,\n    offer,\n    taboo,\n    penny,\n    AVG(rover) AS violin\n  FROM cte_1\n  GROUP BY\n    small,\n    family,\n    offer,\n    taboo,\n    penny\n),\ncte_3 AS (\n  SELECT\n    family,\n    offer\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(family) AS female\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     family,\n     offer,\n     taboo,\n     penny,\n     debtor\n|> AGGREGATE\n     SUM(debtor) AS rover,\n     AVG(debtor) AS middle,\n     COUNT(debtor) AS marker\n   GROUP BY\n     family,\n     offer,\n     taboo,\n     penny\n|> AGGREGATE\n     AVG(rover) AS violin\n   GROUP BY\n     small,\n     family,\n     offer,\n     taboo,\n     penny\n|> SELECT\n     family,\n     offer\n|> AGGREGATE\n     COUNT(family) AS female\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"small",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    stint,\n    heart,\n    cuckoo,\n    scent,\n    hobby\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    stint,\n    heart,\n    cuckoo,\n    scent,\n    AVG(hobby) AS tennis\n  FROM cte_0\n  GROUP BY\n    stint,\n    heart,\n    cuckoo,\n    scent\n),\ncte_2 AS (\n  SELECT\n    stint,\n    heart,\n    cuckoo,\n    scent,\n    COUNT(tennis) AS lounge\n  FROM cte_1\n  GROUP BY\n    stint,\n    heart,\n    cuckoo,\n    scent,\n    motor\n),\ncte_3 AS (\n  SELECT\n    stint,\n    heart,\n    cuckoo,\n    scent\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    stint,\n    SUM(heart) AS sophie\n  FROM cte_3\n  GROUP BY\n    stint\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     stint,\n     heart,\n     cuckoo,\n     scent,\n     hobby\n|> AGGREGATE\n     AVG(hobby) AS tennis\n   GROUP BY\n     stint,\n     heart,\n     cuckoo,\n     scent\n|> AGGREGATE\n     COUNT(tennis) AS lounge\n   GROUP BY\n     stint,\n     heart,\n     cuckoo,\n     scent,\n     motor\n|> SELECT\n     stint,\n     heart,\n     cuckoo,\n     scent\n|> AGGREGATE\n     SUM(heart) AS sophie\n   GROUP BY\n     stint\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"motor",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    input,\n    branch,\n    casino,\n    realm,\n    ripple\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    input,\n    branch,\n    casino,\n    realm,\n    COUNT(ripple) AS parole\n  FROM cte_0\n  GROUP BY\n    input,\n    branch,\n    casino,\n    realm\n),\ncte_2 AS (\n  SELECT\n    input,\n    branch,\n    casino,\n    realm,\n    MAX(parole) AS entity\n  FROM cte_1\n  GROUP BY\n    input,\n    branch,\n    casino,\n    realm,\n    planet\n),\ncte_3 AS (\n  SELECT\n    input,\n    MIN(realm) AS revolt\n  FROM cte_2\n  GROUP BY\n    input,\n    branch,\n    casino\n),\ncte_4 AS (\n  SELECT\n    MIN(revolt) AS garlic\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     input,\n     branch,\n     casino,\n     realm,\n     ripple\n|> AGGREGATE\n     COUNT(ripple) AS parole\n   GROUP BY\n     input,\n     branch,\n     casino,\n     realm\n|> AGGREGATE\n     MAX(parole) AS entity\n   GROUP BY\n     input,\n     branch,\n     casino,\n     realm,\n     planet\n|> AGGREGATE\n     MIN(realm) AS revolt\n   GROUP BY\n     input,\n     branch,\n     casino\n|> AGGREGATE\n     MIN(revolt) AS garlic\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"planet",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    priest,\n    import,\n    grant,\n    pinch,\n    chest\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    priest,\n    import,\n    grant,\n    pinch,\n    MAX(chest) AS prince,\n    MIN(chest) AS regime\n  FROM cte_0\n  GROUP BY\n    priest,\n    import,\n    grant,\n    pinch\n),\ncte_2 AS (\n  SELECT\n    priest,\n    import,\n    grant,\n    pinch,\n    SUM(regime) AS frost\n  FROM cte_1\n  GROUP BY\n    priest,\n    import,\n    phase,\n    grant,\n    pinch\n),\ncte_3 AS (\n  SELECT\n    priest,\n    import\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    priest,\n    COUNT(import) AS habit\n  FROM cte_3\n  GROUP BY\n    priest\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     priest,\n     import,\n     grant,\n     pinch,\n     chest\n|> AGGREGATE\n     MAX(chest) AS prince,\n     MIN(chest) AS regime\n   GROUP BY\n     priest,\n     import,\n     grant,\n     pinch\n|> AGGREGATE\n     SUM(regime) AS frost\n   GROUP BY\n     priest,\n     import,\n     phase,\n     grant,\n     pinch\n|> SELECT\n     priest,\n     import\n|> AGGREGATE\n     COUNT(import) AS habit\n   GROUP BY\n     priest\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"phase",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    blaze,\n    straw,\n    cough,\n    tutor,\n    basket\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    blaze,\n    straw,\n    cough,\n    tutor,\n    MAX(basket) AS comic\n  FROM cte_0\n  GROUP BY\n    blaze,\n    straw,\n    cough,\n    tutor\n),\ncte_2 AS (\n  SELECT\n    blaze,\n    straw,\n    cough,\n    tutor,\n    MIN(comic) AS gallon\n  FROM cte_1\n  GROUP BY\n    blaze,\n    straw,\n    cough,\n    tutor,\n    parish\n),\ncte_3 AS (\n  SELECT\n    blaze,\n    straw,\n    cough,\n    tutor\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(cough) AS barnet\n  FROM cte_3\n  GROUP BY\n    blaze,\n    straw\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     blaze,\n     straw,\n     cough,\n     tutor,\n     basket\n|> AGGREGATE\n     MAX(basket) AS comic\n   GROUP BY\n     blaze,\n     straw,\n     cough,\n     tutor\n|> AGGREGATE\n     MIN(comic) AS gallon\n   GROUP BY\n     blaze,\n     straw,\n     cough,\n     tutor,\n     parish\n|> SELECT\n     blaze,\n     straw,\n     cough,\n     tutor\n|> AGGREGATE\n     MIN(cough) AS barnet\n   GROUP BY\n     blaze,\n     straw\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"parish",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    cohort,\n    minor,\n    tarmac,\n    oracle,\n    pulpit\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    cohort,\n    minor,\n    tarmac,\n    oracle,\n    MAX(pulpit) AS tenor\n  FROM cte_0\n  GROUP BY\n    cohort,\n    minor,\n    tarmac,\n    oracle\n),\ncte_2 AS (\n  SELECT\n    cohort,\n    minor,\n    tarmac,\n    oracle,\n    MAX(tenor) AS drive,\n    AVG(tenor) AS sanity\n  FROM cte_1\n  GROUP BY\n    cohort,\n    minor,\n    matron,\n    tarmac,\n    oracle\n),\ncte_3 AS (\n  SELECT\n    cohort,\n    MAX(minor) AS honour\n  FROM cte_2\n  GROUP BY\n    cohort\n),\ncte_4 AS (\n  SELECT\n    cohort,\n    honour\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     cohort,\n     minor,\n     tarmac,\n     oracle,\n     pulpit\n|> AGGREGATE\n     MAX(pulpit) AS tenor\n   GROUP BY\n     cohort,\n     minor,\n     tarmac,\n     oracle\n|> AGGREGATE\n     MAX(tenor) AS drive,\n     AVG(tenor) AS sanity\n   GROUP BY\n     cohort,\n     minor,\n     matron,\n     tarmac,\n     oracle\n|> AGGREGATE\n     MAX(minor) AS honour\n   GROUP BY\n     cohort\n|> SELECT\n     cohort,\n     honour\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"matron",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    month,\n    hangar,\n    winter,\n    access,\n    teddy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    month,\n    hangar,\n    winter,\n    access,\n    SUM(teddy) AS carbon\n  FROM cte_0\n  GROUP BY\n    month,\n    hangar,\n    winter,\n    access\n),\ncte_2 AS (\n  SELECT\n    month,\n    hangar,\n    winter,\n    access,\n    MAX(carbon) AS powder,\n    SUM(carbon) AS saint,\n    MIN(carbon) AS octave\n  FROM cte_1\n  GROUP BY\n    queen,\n    month,\n    hangar,\n    winter,\n    access\n),\ncte_3 AS (\n  SELECT\n    month,\n    hangar\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    month,\n    hangar\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     month,\n     hangar,\n     winter,\n     access,\n     teddy\n|> AGGREGATE\n     SUM(teddy) AS carbon\n   GROUP BY\n     month,\n     hangar,\n     winter,\n     access\n|> AGGREGATE\n     MAX(carbon) AS powder,\n     SUM(carbon) AS saint,\n     MIN(carbon) AS octave\n   GROUP BY\n     queen,\n     month,\n     hangar,\n     winter,\n     access\n|> SELECT\n     month,\n     hangar\n|> SELECT\n     month,\n     hangar\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"queen",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    heyday,\n    import,\n    cigar,\n    count,\n    jaguar\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    heyday,\n    import,\n    cigar,\n    count,\n    MAX(jaguar) AS flame,\n    AVG(jaguar) AS sauna\n  FROM cte_0\n  GROUP BY\n    heyday,\n    import,\n    cigar,\n    count\n),\ncte_2 AS (\n  SELECT\n    heyday,\n    import,\n    cigar,\n    count,\n    SUM(sauna) AS helix\n  FROM cte_1\n  GROUP BY\n    heyday,\n    import,\n    career,\n    cigar,\n    count\n),\ncte_3 AS (\n  SELECT\n    SUM(cigar) AS horror\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    COUNT(horror) AS crypt,\n    MIN(horror) AS dolly\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     heyday,\n     import,\n     cigar,\n     count,\n     jaguar\n|> AGGREGATE\n     MAX(jaguar) AS flame,\n     AVG(jaguar) AS sauna\n   GROUP BY\n     heyday,\n     import,\n     cigar,\n     count\n|> AGGREGATE\n     SUM(sauna) AS helix\n   GROUP BY\n     heyday,\n     import,\n     career,\n     cigar,\n     count\n|> AGGREGATE\n     SUM(cigar) AS horror\n|> AGGREGATE\n     COUNT(horror) AS crypt,\n     MIN(horror) AS dolly\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"career",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    quota,\n    siege,\n    bertha,\n    noise,\n    expert\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    quota,\n    siege,\n    bertha,\n    noise,\n    SUM(expert) AS steak,\n    COUNT(expert) AS croft\n  FROM cte_0\n  GROUP BY\n    quota,\n    siege,\n    bertha,\n    noise\n),\ncte_2 AS (\n  SELECT\n    quota,\n    siege,\n    bertha,\n    noise,\n    AVG(croft) AS brunt\n  FROM cte_1\n  GROUP BY\n    quota,\n    siege,\n    screen,\n    bertha,\n    noise\n),\ncte_3 AS (\n  SELECT\n    COUNT(bertha) AS settee,\n    MIN(siege) AS taylor\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(taylor) AS clown\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     quota,\n     siege,\n     bertha,\n     noise,\n     expert\n|> AGGREGATE\n     SUM(expert) AS steak,\n     COUNT(expert) AS croft\n   GROUP BY\n     quota,\n     siege,\n     bertha,\n     noise\n|> AGGREGATE\n     AVG(croft) AS brunt\n   GROUP BY\n     quota,\n     siege,\n     screen,\n     bertha,\n     noise\n|> AGGREGATE\n     COUNT(bertha) AS settee,\n     MIN(siege) AS taylor\n|> AGGREGATE\n     SUM(taylor) AS clown\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"screen",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    tiller,\n    income,\n    joseph,\n    hitch,\n    rescue\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    tiller,\n    income,\n    joseph,\n    hitch,\n    MAX(rescue) AS spire\n  FROM cte_0\n  GROUP BY\n    tiller,\n    income,\n    joseph,\n    hitch\n),\ncte_2 AS (\n  SELECT\n    tiller,\n    income,\n    joseph,\n    hitch,\n    SUM(spire) AS digit\n  FROM cte_1\n  GROUP BY\n    tiller,\n    income,\n    joseph,\n    hitch,\n    collor\n),\ncte_3 AS (\n  SELECT\n    tiller,\n    income,\n    joseph,\n    hitch,\n    digit\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    tiller,\n    income,\n    joseph\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     tiller,\n     income,\n     joseph,\n     hitch,\n     rescue\n|> AGGREGATE\n     MAX(rescue) AS spire\n   GROUP BY\n     tiller,\n     income,\n     joseph,\n     hitch\n|> AGGREGATE\n     SUM(spire) AS digit\n   GROUP BY\n     tiller,\n     income,\n     joseph,\n     hitch,\n     collor\n|> SELECT\n     tiller,\n     income,\n     joseph,\n     hitch,\n     digit\n|> SELECT\n     tiller,\n     income,\n     joseph\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"collor",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    poppy,\n    gooch,\n    mutant,\n    butter,\n    fender\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    poppy,\n    gooch,\n    mutant,\n    butter,\n    MIN(fender) AS pellet\n  FROM cte_0\n  GROUP BY\n    poppy,\n    gooch,\n    mutant,\n    butter\n),\ncte_2 AS (\n  SELECT\n    poppy,\n    gooch,\n    mutant,\n    butter,\n    COUNT(pellet) AS whore,\n    AVG(pellet) AS shaft\n  FROM cte_1\n  GROUP BY\n    poppy,\n    gooch,\n    mafia,\n    mutant,\n    butter\n),\ncte_3 AS (\n  SELECT\n    poppy,\n    gooch,\n    mutant\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(mutant) AS reggae\n  FROM cte_3\n  GROUP BY\n    poppy\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     poppy,\n     gooch,\n     mutant,\n     butter,\n     fender\n|> AGGREGATE\n     MIN(fender) AS pellet\n   GROUP BY\n     poppy,\n     gooch,\n     mutant,\n     butter\n|> AGGREGATE\n     COUNT(pellet) AS whore,\n     AVG(pellet) AS shaft\n   GROUP BY\n     poppy,\n     gooch,\n     mafia,\n     mutant,\n     butter\n|> SELECT\n     poppy,\n     gooch,\n     mutant\n|> AGGREGATE\n     AVG(mutant) AS reggae\n   GROUP BY\n     poppy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"mafia",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    decor,\n    mutiny,\n    bench,\n    blade,\n    snake\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    decor,\n    mutiny,\n    bench,\n    blade,\n    SUM(snake) AS factor\n  FROM cte_0\n  GROUP BY\n    decor,\n    mutiny,\n    bench,\n    blade\n),\ncte_2 AS (\n  SELECT\n    decor,\n    mutiny,\n    bench,\n    blade,\n    AVG(factor) AS check\n  FROM cte_1\n  GROUP BY\n    decor,\n    mutiny,\n    bench,\n    blade,\n    stance\n),\ncte_3 AS (\n  SELECT\n    COUNT(check) AS update,\n    AVG(check) AS kinase\n  FROM cte_2\n  GROUP BY\n    decor,\n    mutiny\n),\ncte_4 AS (\n  SELECT\n    update,\n    kinase\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     decor,\n     mutiny,\n     bench,\n     blade,\n     snake\n|> AGGREGATE\n     SUM(snake) AS factor\n   GROUP BY\n     decor,\n     mutiny,\n     bench,\n     blade\n|> AGGREGATE\n     AVG(factor) AS check\n   GROUP BY\n     decor,\n     mutiny,\n     bench,\n     blade,\n     stance\n|> AGGREGATE\n     COUNT(check) AS update,\n     AVG(check) AS kinase\n   GROUP BY\n     decor,\n     mutiny\n|> SELECT\n     update,\n     kinase\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"stance",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    ferry,\n    tiger,\n    flair,\n    spiral,\n    barman\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    ferry,\n    tiger,\n    flair,\n    COUNT(spiral) AS arrest\n  FROM cte_0\n  GROUP BY\n    ferry,\n    tiger,\n    flair\n),\ncte_2 AS (\n  SELECT\n    ferry,\n    tiger,\n    flair,\n    SUM(arrest) AS glance\n  FROM cte_1\n  GROUP BY\n    ferry,\n    tiger,\n    flair\n),\ncte_3 AS (\n  SELECT\n    ferry,\n    tiger,\n    MIN(flair) AS satan\n  FROM cte_2\n  GROUP BY\n    ankle,\n    ferry,\n    tiger\n),\ncte_4 AS (\n  SELECT\n    ferry,\n    AVG(satan) AS shout,\n    COUNT(satan) AS draft\n  FROM cte_3\n  GROUP BY\n    ferry\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     ferry,\n     tiger,\n     flair,\n     spiral,\n     barman\n|> AGGREGATE\n     COUNT(spiral) AS arrest\n   GROUP BY\n     ferry,\n     tiger,\n     flair\n|> AGGREGATE\n     SUM(arrest) AS glance\n   GROUP BY\n     ferry,\n     tiger,\n     flair\n|> AGGREGATE\n     MIN(flair) AS satan\n   GROUP BY\n     ankle,\n     ferry,\n     tiger\n|> AGGREGATE\n     AVG(satan) AS shout,\n     COUNT(satan) AS draft\n   GROUP BY\n     ferry\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"ankle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    market,\n    ballot,\n    logan,\n    widow,\n    escape\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    market,\n    ballot,\n    logan,\n    widow,\n    SUM(escape) AS summer\n  FROM cte_0\n  GROUP BY\n    market,\n    ballot,\n    logan,\n    widow\n),\ncte_2 AS (\n  SELECT\n    market,\n    ballot,\n    logan,\n    widow,\n    MIN(summer) AS comic,\n    SUM(summer) AS demon\n  FROM cte_1\n  GROUP BY\n    market,\n    ballot,\n    module,\n    logan,\n    widow\n),\ncte_3 AS (\n  SELECT\n    MIN(market) AS pause,\n    COUNT(comic) AS pistol\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(pause) AS union\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     market,\n     ballot,\n     logan,\n     widow,\n     escape\n|> AGGREGATE\n     SUM(escape) AS summer\n   GROUP BY\n     market,\n     ballot,\n     logan,\n     widow\n|> AGGREGATE\n     MIN(summer) AS comic,\n     SUM(summer) AS demon\n   GROUP BY\n     market,\n     ballot,\n     module,\n     logan,\n     widow\n|> AGGREGATE\n     MIN(market) AS pause,\n     COUNT(comic) AS pistol\n|> AGGREGATE\n     SUM(pause) AS union\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"module",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    sperm,\n    stage,\n    prison,\n    backup,\n    booth\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    sperm,\n    stage,\n    prison,\n    backup,\n    COUNT(booth) AS monte\n  FROM cte_0\n  GROUP BY\n    sperm,\n    stage,\n    prison,\n    backup\n),\ncte_2 AS (\n  SELECT\n    sperm,\n    stage,\n    prison,\n    backup,\n    MAX(monte) AS jumper,\n    MIN(monte) AS clough\n  FROM cte_1\n  GROUP BY\n    sperm,\n    stage,\n    fluid,\n    prison,\n    backup\n),\ncte_3 AS (\n  SELECT\n    sperm,\n    stage,\n    prison\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    sperm,\n    stage,\n    prison\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     sperm,\n     stage,\n     prison,\n     backup,\n     booth\n|> AGGREGATE\n     COUNT(booth) AS monte\n   GROUP BY\n     sperm,\n     stage,\n     prison,\n     backup\n|> AGGREGATE\n     MAX(monte) AS jumper,\n     MIN(monte) AS clough\n   GROUP BY\n     sperm,\n     stage,\n     fluid,\n     prison,\n     backup\n|> SELECT\n     sperm,\n     stage,\n     prison\n|> SELECT\n     sperm,\n     stage,\n     prison\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"fluid",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    wheat,\n    splash,\n    array,\n    resin,\n    gamble\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    wheat,\n    splash,\n    array,\n    resin,\n    AVG(gamble) AS clutch,\n    COUNT(gamble) AS beauty\n  FROM cte_0\n  GROUP BY\n    wheat,\n    splash,\n    array,\n    resin\n),\ncte_2 AS (\n  SELECT\n    wheat,\n    splash,\n    array,\n    resin,\n    SUM(clutch) AS bucket\n  FROM cte_1\n  GROUP BY\n    wheat,\n    splash,\n    madame,\n    array,\n    resin\n),\ncte_3 AS (\n  SELECT\n    wheat,\n    AVG(array) AS trend\n  FROM cte_2\n  GROUP BY\n    wheat\n),\ncte_4 AS (\n  SELECT\n    wheat,\n    trend\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     wheat,\n     splash,\n     array,\n     resin,\n     gamble\n|> AGGREGATE\n     AVG(gamble) AS clutch,\n     COUNT(gamble) AS beauty\n   GROUP BY\n     wheat,\n     splash,\n     array,\n     resin\n|> AGGREGATE\n     SUM(clutch) AS bucket\n   GROUP BY\n     wheat,\n     splash,\n     madame,\n     array,\n     resin\n|> AGGREGATE\n     AVG(array) AS trend\n   GROUP BY\n     wheat\n|> SELECT\n     wheat,\n     trend\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:25,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"madame",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    slice,\n    upland,\n    church,\n    stance,\n    trophy\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    slice,\n    upland,\n    church,\n    stance,\n    MAX(trophy) AS brace,\n    MIN(trophy) AS racism\n  FROM cte_0\n  GROUP BY\n    slice,\n    upland,\n    church,\n    stance\n),\ncte_2 AS (\n  SELECT\n    slice,\n    upland,\n    church,\n    stance,\n    SUM(brace) AS throne,\n    MIN(racism) AS bugger\n  FROM cte_1\n  GROUP BY\n    binder,\n    slice,\n    upland,\n    church,\n    stance\n),\ncte_3 AS (\n  SELECT\n    slice,\n    upland\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    slice,\n    upland\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     slice,\n     upland,\n     church,\n     stance,\n     trophy\n|> AGGREGATE\n     MAX(trophy) AS brace,\n     MIN(trophy) AS racism\n   GROUP BY\n     slice,\n     upland,\n     church,\n     stance\n|> AGGREGATE\n     SUM(brace) AS throne,\n     MIN(racism) AS bugger\n   GROUP BY\n     binder,\n     slice,\n     upland,\n     church,\n     stance\n|> SELECT\n     slice,\n     upland\n|> SELECT\n     slice,\n     upland\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"binder",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:36,errorLinePipe:20},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    misery,\n    volume,\n    cause,\n    legion,\n    merit\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    misery,\n    volume,\n    cause,\n    legion,\n    MAX(merit) AS smith\n  FROM cte_0\n  GROUP BY\n    misery,\n    volume,\n    cause,\n    legion\n),\ncte_2 AS (\n  SELECT\n    misery,\n    volume,\n    cause,\n    legion,\n    COUNT(smith) AS pirate,\n    MIN(smith) AS affect\n  FROM cte_1\n  GROUP BY\n    misery,\n    volume,\n    rhythm,\n    cause,\n    legion\n),\ncte_3 AS (\n  SELECT\n    misery,\n    volume,\n    cause,\n    legion\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    SUM(volume) AS helmet\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     misery,\n     volume,\n     cause,\n     legion,\n     merit\n|> AGGREGATE\n     MAX(merit) AS smith\n   GROUP BY\n     misery,\n     volume,\n     cause,\n     legion\n|> AGGREGATE\n     COUNT(smith) AS pirate,\n     MIN(smith) AS affect\n   GROUP BY\n     misery,\n     volume,\n     rhythm,\n     cause,\n     legion\n|> SELECT\n     misery,\n     volume,\n     cause,\n     legion\n|> AGGREGATE\n     SUM(volume) AS helmet\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"rhythm",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    cheer,\n    candy,\n    flash,\n    vision,\n    doubt\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    cheer,\n    candy,\n    flash,\n    vision,\n    MAX(doubt) AS mandy\n  FROM cte_0\n  GROUP BY\n    cheer,\n    candy,\n    flash,\n    vision\n),\ncte_2 AS (\n  SELECT\n    cheer,\n    candy,\n    MAX(flash) AS garlic\n  FROM cte_1\n  GROUP BY\n    cheer,\n    candy\n),\ncte_3 AS (\n  SELECT\n    cheer,\n    candy,\n    MAX(garlic) AS organ\n  FROM cte_2\n  GROUP BY\n    eagle,\n    cheer,\n    candy\n),\ncte_4 AS (\n  SELECT\n    cheer,\n    candy,\n    MAX(organ) AS fella\n  FROM cte_3\n  GROUP BY\n    cheer,\n    candy\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     cheer,\n     candy,\n     flash,\n     vision,\n     doubt\n|> AGGREGATE\n     MAX(doubt) AS mandy\n   GROUP BY\n     cheer,\n     candy,\n     flash,\n     vision\n|> AGGREGATE\n     MAX(flash) AS garlic\n   GROUP BY\n     cheer,\n     candy\n|> AGGREGATE\n     MAX(garlic) AS organ\n   GROUP BY\n     eagle,\n     cheer,\n     candy\n|> AGGREGATE\n     MAX(organ) AS fella\n   GROUP BY\n     cheer,\n     candy\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:24,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"eagle",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    queue,\n    morris,\n    optic,\n    violin,\n    cache\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    queue,\n    morris,\n    optic,\n    violin,\n    COUNT(cache) AS combat,\n    MIN(cache) AS motor\n  FROM cte_0\n  GROUP BY\n    queue,\n    morris,\n    optic,\n    violin\n),\ncte_2 AS (\n  SELECT\n    queue,\n    morris,\n    optic,\n    violin,\n    SUM(motor) AS shock\n  FROM cte_1\n  GROUP BY\n    queue,\n    morris,\n    fraud,\n    optic,\n    violin,\n    combat\n),\ncte_3 AS (\n  SELECT\n    queue,\n    morris,\n    optic\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    AVG(queue) AS wheel\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     queue,\n     morris,\n     optic,\n     violin,\n     cache\n|> AGGREGATE\n     COUNT(cache) AS combat,\n     MIN(cache) AS motor\n   GROUP BY\n     queue,\n     morris,\n     optic,\n     violin\n|> AGGREGATE\n     SUM(motor) AS shock\n   GROUP BY\n     queue,\n     morris,\n     fraud,\n     optic,\n     violin,\n     combat\n|> SELECT\n     queue,\n     morris,\n     optic\n|> AGGREGATE\n     AVG(queue) AS wheel\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"fraud",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    proxy,\n    esteem,\n    picnic,\n    rarity,\n    couple\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    proxy,\n    esteem,\n    picnic,\n    rarity,\n    COUNT(couple) AS libel\n  FROM cte_0\n  GROUP BY\n    proxy,\n    esteem,\n    picnic,\n    rarity\n),\ncte_2 AS (\n  SELECT\n    proxy,\n    esteem,\n    picnic,\n    rarity,\n    AVG(libel) AS stance\n  FROM cte_1\n  GROUP BY\n    proxy,\n    esteem,\n    picnic,\n    rarity,\n    voter\n),\ncte_3 AS (\n  SELECT\n    SUM(picnic) AS brick,\n    MAX(stance) AS orchid\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(brick) AS cursor,\n    COUNT(brick) AS deity\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     proxy,\n     esteem,\n     picnic,\n     rarity,\n     couple\n|> AGGREGATE\n     COUNT(couple) AS libel\n   GROUP BY\n     proxy,\n     esteem,\n     picnic,\n     rarity\n|> AGGREGATE\n     AVG(libel) AS stance\n   GROUP BY\n     proxy,\n     esteem,\n     picnic,\n     rarity,\n     voter\n|> AGGREGATE\n     SUM(picnic) AS brick,\n     MAX(stance) AS orchid\n|> AGGREGATE\n     MIN(brick) AS cursor,\n     COUNT(brick) AS deity\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"voter",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    rector,\n    frenzy,\n    gland,\n    metro,\n    jumper\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    rector,\n    frenzy,\n    gland,\n    metro,\n    SUM(jumper) AS ocean\n  FROM cte_0\n  GROUP BY\n    rector,\n    frenzy,\n    gland,\n    metro\n),\ncte_2 AS (\n  SELECT\n    rector,\n    frenzy,\n    gland,\n    metro,\n    MIN(ocean) AS cartel\n  FROM cte_1\n  GROUP BY\n    rector,\n    frenzy,\n    gland,\n    metro,\n    daily\n),\ncte_3 AS (\n  SELECT\n    rector,\n    SUM(metro) AS barker\n  FROM cte_2\n  GROUP BY\n    rector\n),\ncte_4 AS (\n  SELECT\n    rector,\n    MIN(barker) AS sherry\n  FROM cte_3\n  GROUP BY\n    rector\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     rector,\n     frenzy,\n     gland,\n     metro,\n     jumper\n|> AGGREGATE\n     SUM(jumper) AS ocean\n   GROUP BY\n     rector,\n     frenzy,\n     gland,\n     metro\n|> AGGREGATE\n     MIN(ocean) AS cartel\n   GROUP BY\n     rector,\n     frenzy,\n     gland,\n     metro,\n     daily\n|> AGGREGATE\n     SUM(metro) AS barker\n   GROUP BY\n     rector\n|> AGGREGATE\n     MIN(barker) AS sherry\n   GROUP BY\n     rector\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:24,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"daily",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:38,errorLinePipe:22},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    access,\n    cereal,\n    stock,\n    zenith,\n    radio\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    access,\n    cereal,\n    stock,\n    zenith,\n    MIN(radio) AS paddy\n  FROM cte_0\n  GROUP BY\n    access,\n    cereal,\n    stock,\n    zenith\n),\ncte_2 AS (\n  SELECT\n    access,\n    cereal,\n    SUM(zenith) AS expiry\n  FROM cte_1\n  GROUP BY\n    access,\n    cereal\n),\ncte_3 AS (\n  SELECT\n    access,\n    cereal,\n    COUNT(expiry) AS indie\n  FROM cte_2\n  GROUP BY\n    streak,\n    access,\n    cereal\n),\ncte_4 AS (\n  SELECT\n    MIN(indie) AS start,\n    MAX(access) AS vigil,\n    AVG(indie) AS uptake\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     access,\n     cereal,\n     stock,\n     zenith,\n     radio\n|> AGGREGATE\n     MIN(radio) AS paddy\n   GROUP BY\n     access,\n     cereal,\n     stock,\n     zenith\n|> AGGREGATE\n     SUM(zenith) AS expiry\n   GROUP BY\n     access,\n     cereal\n|> AGGREGATE\n     COUNT(expiry) AS indie\n   GROUP BY\n     streak,\n     access,\n     cereal\n|> AGGREGATE\n     MIN(indie) AS start,\n     MAX(access) AS vigil,\n     AVG(indie) AS uptake\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"streak",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:42,errorLinePipe:23},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    actor,\n    parkin,\n    median,\n    rhythm,\n    prayer\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    actor,\n    parkin,\n    median,\n    rhythm,\n    MIN(prayer) AS pillow\n  FROM cte_0\n  GROUP BY\n    actor,\n    parkin,\n    median,\n    rhythm\n),\ncte_2 AS (\n  SELECT\n    actor,\n    parkin,\n    median,\n    rhythm,\n    MIN(pillow) AS bucket,\n    COUNT(pillow) AS chief\n  FROM cte_1\n  GROUP BY\n    actor,\n    parkin,\n    dough,\n    median,\n    rhythm\n),\ncte_3 AS (\n  SELECT\n    actor,\n    parkin,\n    median,\n    rhythm\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    MIN(rhythm) AS kylie\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     actor,\n     parkin,\n     median,\n     rhythm,\n     prayer\n|> AGGREGATE\n     MIN(prayer) AS pillow\n   GROUP BY\n     actor,\n     parkin,\n     median,\n     rhythm\n|> AGGREGATE\n     MIN(pillow) AS bucket,\n     COUNT(pillow) AS chief\n   GROUP BY\n     actor,\n     parkin,\n     dough,\n     median,\n     rhythm\n|> SELECT\n     actor,\n     parkin,\n     median,\n     rhythm\n|> AGGREGATE\n     MIN(rhythm) AS kylie\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"dough",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    scorer,\n    archie,\n    foetus,\n    shrub,\n    outfit\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    scorer,\n    archie,\n    foetus,\n    shrub,\n    COUNT(outfit) AS uplift\n  FROM cte_0\n  GROUP BY\n    scorer,\n    archie,\n    foetus,\n    shrub\n),\ncte_2 AS (\n  SELECT\n    scorer,\n    archie,\n    foetus,\n    shrub,\n    MAX(uplift) AS filter,\n    COUNT(uplift) AS dobson\n  FROM cte_1\n  GROUP BY\n    scorer,\n    archie,\n    caesar,\n    foetus,\n    shrub\n),\ncte_3 AS (\n  SELECT\n    scorer,\n    archie,\n    foetus\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    scorer,\n    archie,\n    foetus\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     scorer,\n     archie,\n     foetus,\n     shrub,\n     outfit\n|> AGGREGATE\n     COUNT(outfit) AS uplift\n   GROUP BY\n     scorer,\n     archie,\n     foetus,\n     shrub\n|> AGGREGATE\n     MAX(uplift) AS filter,\n     COUNT(uplift) AS dobson\n   GROUP BY\n     scorer,\n     archie,\n     caesar,\n     foetus,\n     shrub\n|> SELECT\n     scorer,\n     archie,\n     foetus\n|> SELECT\n     scorer,\n     archie,\n     foetus\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"caesar",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21},{sqlQuery:"WITH\ncte_0 AS (\n  SELECT\n    month,\n    stern,\n    pause,\n    tribe,\n    groan\n  FROM base\n),\ncte_1 AS (\n  SELECT\n    month,\n    stern,\n    pause,\n    tribe,\n    AVG(groan) AS crowd,\n    MIN(groan) AS theme\n  FROM cte_0\n  GROUP BY\n    month,\n    stern,\n    pause,\n    tribe\n),\ncte_2 AS (\n  SELECT\n    month,\n    stern,\n    pause,\n    tribe,\n    MIN(theme) AS claret\n  FROM cte_1\n  GROUP BY\n    month,\n    stern,\n    travel,\n    pause,\n    tribe,\n    crowd\n),\ncte_3 AS (\n  SELECT\n    month,\n    stern,\n    pause\n  FROM cte_2\n),\ncte_4 AS (\n  SELECT\n    month,\n    stern\n  FROM cte_3\n)\n SELECT * \n FROM cte_4;",pipeQuery:"FROM base\n|> SELECT\n     month,\n     stern,\n     pause,\n     tribe,\n     groan\n|> AGGREGATE\n     AVG(groan) AS crowd,\n     MIN(groan) AS theme\n   GROUP BY\n     month,\n     stern,\n     pause,\n     tribe\n|> AGGREGATE\n     MIN(theme) AS claret\n   GROUP BY\n     month,\n     stern,\n     travel,\n     pause,\n     tribe,\n     crowd\n|> SELECT\n     month,\n     stern,\n     pause\n|> SELECT\n     month,\n     stern\n|> SELECT *",totalInformationSQL:34,totalInformationPipe:26,totalInformationUntilErrorSQL:26,totalInformationUntilErrorPipe:18,errorType:"GROUP_UNKNOWN",totalInformationDifference:8,unknownName:"travel",numberQueries_excludingBase:4,columnsBaseQuery:5,errorLineSQL:37,errorLinePipe:21}].map(n=>Object.assign(Object.assign({},n),{sqlQuery:X(n.sqlQuery),pipeQuery:X(n.pipeQuery)}));L("42");let W=x;!function(){let n=new V,e={experiment_name:"CTE vs. Pipe",seed:"42",introduction_pages:[()=>{r.print_string_on_stage('\n          <section class="instructions" lang="en">\n            <h1>Experiment Instructions</h1>\n\n            <p>\n              Please only start the experiment if you have sufficient time\n              (<strong>approximately 20–35 minutes</strong>), are able to focus, and feel motivated to complete it in one go.\n            </p>\n\n            <h2>Technical Setup</h2>\n            <ul>\n              <li>Use a device with a screen resolution of at least <strong>1920×1080</strong>.</li>\n<li>\n  Once the experiment opens, switch your browser to <strong>full-screen mode</strong>\n  (on many systems this is <span class="sourcecode">F11</span>).\n <li>Zoom out (Ctrl/⌘ + –) until the entire query is visible <strong>without scrolling</strong>.</li>\n\n</li>\n            </ul>\n\n<h2>Your Task</h2>\n<p>\n  In each trial, you will be shown <strong>one SQL query</strong> written in one of two syntaxes:\n</p>\n<ul>\n  <li><strong>Standard SQL syntax</strong> (using CTEs), or</li>\n  <li><strong>Pipe-based SQL syntax</strong></li>\n</ul>\n\n\n            <p>\n              For <strong>each</strong> query, decide whether it contains a <strong>syntactic error</strong>.\n              If it does, enter the <strong>line number</strong> where the error occurs (or where you first notice it).\n              If there is <strong>no error</strong>, enter <strong>0</strong> and press <strong>Enter</strong>.\n            </p>\n\n            <p>\n              Each query contains <strong>at most one</strong> error (or none).\n            </p>\n\n            <h2>How to Respond</h2>\n            <ul>\n              <li>If there is an error: enter the <strong>line number</strong> where it occurs.</li>\n              <li>If there is no error: enter <strong>0</strong> and press <strong>Enter</strong>.</li>\n            </ul>\n\n            <h2>Explanation of Google\'s Pipe Syntax and Revision of Common SQL Syntax</h2>\n            <p>\n              Please review the next pages (press the right arrow → to navigate) and <strong>read through</strong> explanation for google\'s pipe syntax and revision of common SQL syntax.\n            </p>\n\n\n            <h2>Error Types</h2>\n            <p>\n              Please review the next pages (press the right arrow → to navigate) and <strong>read through</strong> the error types.\n              Then, in the training phase, you can practice for the experiment and confirm whether you’ve understood the error types.\n            </p>\n\n            <h4>Thank you for your careful participation.</h4>\n\n          </section>\n        ')},()=>{r.print_string_on_stage('<h2>Google Pipe Syntax</h2>\n\n<p>\n  Google <strong>Pipe Syntax</strong> expresses a query as a <strong>linear data flow</strong>:\n  you start with a source (<span class="sourcecode">FROM</span>) and then pass the intermediate\n  result through transformation stages using <span class="sourcecode">|&gt;</span>.\n</p>\n\n<p>\n  Each stage consumes the table produced by the previous stage and outputs a new table.\n</p>\n\n<p>\n  <strong>Important for this experiment:</strong> the special property of Pipe Syntax is this\n  <strong>explicit linear data flow</strong> (a sequence of stages). The experiment deliberately\n  relies on this idea and compares Pipe Syntax to <strong>CTE-based SQL</strong>, because\n  <strong>both</strong> can be written to enforce the same step-by-step linear flow.\n</p>\n\n<p>\n  The most common stages are:\n  <span class="sourcecode">|&gt; SELECT</span> (shape the data by choosing/renaming columns and deriving new fields),\n  <span class="sourcecode">GROUP BY</span> (bucket rows into groups),\n  and <span class="sourcecode">|&gt; AGGREGATE</span> (compute summaries like\n  <span class="sourcecode">COUNT</span>, <span class="sourcecode">SUM</span>, and <span class="sourcecode">AVG</span>\n  for each group).\n  The key principle: every stage does one clear step, and the output flows forward to the next stage.\n</p>\n<p>\n  When combining projection with aggregation, the <span class="sourcecode">|&gt; SELECT</span> stage is omitted entirely.\n  The <span class="sourcecode">GROUP BY</span> clause handles both grouping and projection: columns listed in\n  <span class="sourcecode">GROUP BY</span> automatically appear in the output alongside the aggregated values.\n</p>\n\n<h3>Pipe example 1: <span class="sourcecode">|&gt; SELECT</span> (projection)</h3>\n\n<pre class="sourcecode"><code>-- Start: read the base table\n-- Visible columns: all columns from base\nFROM base\n\n-- Project fields (this defines the new "shape" of the table)\n-- Visible columns after this step: customer_id, order_id, order_total\n|&gt; SELECT\n     customer_id,\n     order_id,\n     order_total\n-- Re-project to keep only what you need\n-- Visible columns after this step: customer_id, order_total\n|&gt; SELECT\n     customer_id,\n     order_total;</code></pre>\n\n<h3>Pipe example 2: <span class="sourcecode">GROUP BY</span> + <span class="sourcecode">|&gt; AGGREGATE</span></h3>\n\n<pre class="sourcecode"><code>-- Start: read the base table\n-- Visible columns: all columns from base\nFROM base\n\n-- Prepare the fields needed for grouping + aggregations\n-- Visible columns after this step: customer_id, order_total, order_date\n|&gt; SELECT\n     customer_id,\n     order_total,\n     order_date\n\n-- Aggregate collapses rows into one row per group\n-- GROUP BY keys remain visible; aggregate outputs become new columns\n-- Visible columns after this step: revenue, avg_order_value, customer_id, order_date\n|&gt; AGGREGATE\n     SUM(order_total) AS revenue,\n     AVG(order_total) AS avg_order_value\n   GROUP BY \n     customer_id, \n     order_date\n\n-- Final projection, keeps the result tidy\n-- Visible columns after this step: customer_id, order_date, revenue, avg_order_value\n|&gt; SELECT\n     customer_id,\n     order_date,\n     revenue,\n     avg_order_value;</code></pre>\n')},()=>{r.print_string_on_stage('<h2>Pipe example 3: Aggregation (and GROUP BY) with sample table</h2>\n<p>\n  This example uses aggregation: we group by <span class="sourcecode">city</span>,\n  count people, and compute the average age.\n</p>\n<h3>Sample table: <span class="sourcecode">people</span></h3>\n<p>Columns:</p>\n<ul>\n  <li><span class="sourcecode">id</span> (INT)</li>\n  <li><span class="sourcecode">name</span> (STRING)</li>\n  <li><span class="sourcecode">city</span> (STRING)</li>\n  <li><span class="sourcecode">age</span> (INT)</li>\n</ul>\n<p>Sample data:</p>\n<pre class="sourcecode"><code>-- people\n-- id | name  | city    | age\n1     | Anna  | Berlin  | 28\n2     | Ben   | Berlin  | 17\n3     | Carla | Munich  | 34\n4     | Diego | Berlin  | 42\n5     | Emma  | Hamburg | 22</code></pre>\n<h3>Pipe query: people per city</h3>\n<pre class="sourcecode"><code>-- Start: read the base table\n-- Visible columns: all columns from people\nFROM people\n\n-- Aggregate into one row per city\n-- No |&gt; SELECT needed: GROUP BY keys + aggregate results are projected automatically\n-- Visible columns after this step: city, residents, avg_age\n|&gt; AGGREGATE\n     COUNT(*) AS residents,\n     AVG(age) AS avg_age\n   GROUP BY \n     city;</code></pre>\n\n<h3>Expected result</h3>\n<pre class="sourcecode"><code> city    | residents | avg_age\nBerlin     | 3         | 29.0\nMunich     | 1         | 34.0\nHamburg    | 1         | 22.0</code></pre>\n')},()=>{r.print_string_on_stage('<h2>Standard SQL with CTEs</h2>\n\n<p>\n  In <strong>standard SQL</strong>, the same step-by-step “pipeline” is typically written using\n  <strong>CTEs</strong> (<span class="sourcecode">WITH ... AS (...)</span>).\n  Each CTE represents one stage: it produces a temporary result table, and the next CTEs can read from it.\n  This can create the same <strong>linear data flow</strong> as Pipe Syntax.\n</p>\n\n<p>\n  Important mental model: a CTE defines the <strong>visible/accessible columns</strong> for later steps.\n  If a column is not selected into a CTE (or produced by aggregation), it cannot be referenced downstream.\n</p>\n\n<h3>SQL example 1: Projection + Aggregation</h3>\n\n<pre class="sourcecode"><code>-- Start: read the base table\n-- Visible columns: all columns from base\nWITH step_0 AS (\n  SELECT\n     customer_id,\n     order_id,\n     order_total,\n     SUM(order_ts) AS yearly_orders_ts\n   FROM base\n   GROUP BY\n     customer_id, \n     order_id, \n     order_total\n  -- Visible columns after step_0: customer_id, order_id, order_total, yearly_orders_ts\n),\nstep_1 AS (\n  SELECT\n    customer_id,\n    yearly_orders_ts,\n    order_total\n  FROM step_0\n  -- Visible columns after step_1: customer_id, yearly_orders_ts, order_total\n)\nSELECT *\nFROM step_1;</code></pre>\n\n<h3>SQL example 2: GROUP BY + aggregates</h3>\n\n<pre class="sourcecode"><code>-- Start: read the base table\n-- Visible columns: all columns from base\nWITH step_0 AS (\n  SELECT\n    customer_id,\n    order_total,\n    order_date\n  FROM base\n  -- Visible columns after step_0: customer_id, order_total, order_date\n),\nstep_1 AS (\n  SELECT\n    customer_id,\n    order_date,\n    SUM(order_total) AS revenue,\n    AVG(order_total) AS avg_order_value\n  FROM step_0\n  GROUP BY \n    customer_id, \n    order_date\n  -- Visible columns after step_1: customer_id, order_date, revenue, avg_order_value\n),\nstep_2 AS (\n  SELECT\n    customer_id,\n    order_date,\n    revenue,\n    avg_order_value\n  FROM step_1\n  -- Visible columns after step_2: customer_id, order_date, revenue, avg_order_value\n)\nSELECT *\nFROM step_2;</code></pre>\n')},()=>{r.print_string_on_stage('<h2>SQL example 3: Aggregation + GROUP BY</h2>\n\n<p>\n  Same logic as the pipe version, but written as plain SQL using CTEs:\n  group by <span class="sourcecode">city</span>, count people, compute average age.\n</p>\n\n<h3>Sample table: <span class="sourcecode">people</span></h3>\n\n<p>Columns:</p>\n<ul>\n  <li><span class="sourcecode">id</span> (INT)</li>\n  <li><span class="sourcecode">name</span> (STRING)</li>\n  <li><span class="sourcecode">city</span> (STRING)</li>\n  <li><span class="sourcecode">age</span> (INT)</li>\n</ul>\n\n<p>Sample data:</p>\n<pre class="sourcecode"><code>-- people\n-- id | name  | city    | age\n1     | Anna  | Berlin  | 28\n2     | Ben   | Berlin  | 17\n3     | Carla | Munich  | 34\n4     | Diego | Berlin  | 42\n5     | Emma  | Hamburg | 22</code></pre>\n\n<h3>SQL query (CTEs)</h3>\n\n<pre class="sourcecode"><code>WITH step_0 AS (\n  -- Start: read the base table\n  SELECT *\n  FROM people\n),\nstep_1 AS (\n  -- Aggregate into one row per city\n  SELECT\n    city,\n    COUNT(*) AS residents,\n    AVG(age) AS avg_age\n  FROM step_0\n  GROUP BY \n    city\n)\n-- Final projection\nSELECT\n  city,\n  residents,\n  avg_age\nFROM step_1;</code></pre>\n\n<h3>Expected result</h3>\n<pre class="sourcecode"><code>-- city    | residents | avg_age\nBerlin     | 3         | 29.0\nMunich     | 1         | 34.0\nHamburg    | 1         | 22.0</code></pre>\n')},()=>{r.print_string_on_stage('\n<h3>Mini example: Multi-stage aggregation</h3>\n\n<div style="display:flex; gap:14px; align-items:flex-start; width:100%;">\n  <div style="flex:1 1 50%; min-width:0;">\n    <h4>Pipe Syntax</h4>\n    <pre class="sourcecode"><code>FROM Base\n|&gt; AGGREGATE\n     SUM(amount) AS month_amount\n   GROUP BY\n     customer_id\n|&gt; AGGREGATE\n     SUM(month_amount) AS ytd_amount,\n     MAX(month_amount) AS best_month_amount\n   GROUP BY\n     customer_id;</code></pre>\n  </div>\n\n  <div style="flex:1 1 50%; min-width:0;">\n    <h4>Standard SQL with CTEs</h4>\n    <pre class="sourcecode"><code>WITH step_0 AS (\n  SELECT *\n  FROM Base\n),\nstep_1 AS (\n  SELECT\n    customer_id,\n    SUM(amount) AS month_amount\n  FROM step_0\n  GROUP BY\n    customer_id\n),\nstep_2 AS (\n  SELECT\n    customer_id,\n    SUM(month_amount) AS ytd_amount,\n    MAX(month_amount) AS best_month_amount\n  FROM step_1\n  GROUP BY \n    customer_id\n)\nSELECT *\nFROM step_2;\n</code></pre>\n  </div>\n</div>\n\n\n<p>\n  <strong>What you should watch for in the experiment:</strong> if a column is not introduced (projected/aggregated)\n  in an earlier step, it cannot be referenced later, this is exactly what many of the syntactic error types test.\n</p>\n')},()=>{r.print_string_on_stage('\n<h3>Mini example 2</h3>\n<div style="display:flex; gap:14px; align-items:flex-start; width:100%;">\n  <div style="flex:1 1 50%; min-width:0;">\n    <h4>Pipe Syntax</h4>\n    <pre class="sourcecode"><code>FROM transactions\n|&gt; AGGREGATE\n     SUM(revenue) AS revenue_daily,\n     COUNT(*) AS num_transactions\n   GROUP BY\n     store_id,\n     tx_date\n|&gt; SELECT\n     store_id,\n     tx_date,\n     revenue_daily\n|&gt; AGGREGATE\n     SUM(revenue_daily) AS revenue_monthly,\n     AVG(revenue_daily) AS avg_daily_revenue\n   GROUP BY\n     store_id\n|&gt; SELECT\n     store_id,\n     revenue_monthly\n|&gt; AGGREGATE\n     SUM(revenue_monthly) AS revenue_yearly\n   GROUP BY\n     store_id;</code></pre>\n  </div>\n  <div style="flex:1 1 50%; min-width:0;">\n    <h4>Standard SQL with CTEs</h4>\n    <pre class="sourcecode"><code>WITH step_0 AS (\n  SELECT\n    store_id,\n    tx_date,\n    SUM(revenue) AS revenue_daily,\n    COUNT(*) AS num_transactions\n  FROM base\n  GROUP BY \n    store_id, \n    tx_date\n),\nstep_1 AS (\n  SELECT\n    store_id,\n    tx_date,\n    revenue_daily\n  FROM step_0\n),\nstep_2 AS (\n  SELECT\n    store_id,\n    SUM(revenue_daily) AS revenue_monthly,\n    AVG(revenue_daily) AS avg_daily_revenue\n  FROM step_1\n  GROUP BY \n    store_id\n),\nstep_3 AS (\n  SELECT\n    store_id,\n    revenue_monthly\n  FROM step_2\n),\nstep_4 AS (\n  SELECT\n    store_id,\n    SUM(revenue_monthly) AS revenue_yearly\n  FROM step_3\n  GROUP BY store_id\n)\nSELECT *\nFROM step_4;</code></pre>\n  </div>\n</div>\n<p>\n  <strong>Notice:</strong> The intermediate <span class="sourcecode">|&gt; SELECT</span> steps drop columns \n  (<span class="sourcecode">num_transactions</span>, <span class="sourcecode">avg_daily_revenue</span>) \n  before the next aggregation. Referencing a dropped column in later steps would cause a syntax error.\n</p>\n')},()=>{r.print_string_on_stage('<h2>Possible Error Types</h2>\n<h3>Error Type 1: New column appears in SELECT</h3>\n<p>\n  A column is projected in the <strong>SELECT</strong> clause even though it has not been projected, aggregated or grouped in an earlier step.\n  This means the column appears for the first time in <strong>SELECT</strong>.\n</p>\n<p>\n  <strong>Only occurs in:</strong>\n  <span class="sourcecode">SELECT</span> / <span class="sourcecode">|&gt; SELECT</span>.\n</p>\n<h4>Pipe example</h4>\n<pre class="sourcecode"><code>FROM base\n|&gt; SELECT\n     sponge,\n     trees\n|&gt; SELECT\n     sponge,\n     trees,\n     crown   -- ❌ Error Type 1: "crown" was not projected in the previous step\n|&gt; SELECT *;</code></pre>\n\n<h4>Standard SQL example</h4>\n<pre class="sourcecode"><code>WITH CTE_0 AS (\n  SELECT\n    sponge,\n    trees\n  FROM base\n),\nCTE_1 AS (\n  SELECT\n    sponge,\n    trees,\n    crown   -- ❌ Error Type 1: "crown" was not projected in the previous step\n  FROM CTE_0\n)\nSELECT *\nFROM CTE_1;</code></pre>\n')},()=>{r.print_string_on_stage('<h2>Possible Error Types</h2>\n<h3>Error Type 2: Invalid column in GROUP BY</h3>\n<p>\n  A column appears in <span class="sourcecode">GROUP BY</span> even though it has not been \n  projected or grouped in an earlier step.\n</p>\n<p>\n  <strong>Only occurs in:</strong> <span class="sourcecode">GROUP BY</span>.\n</p>\n\n<h4>Pipe example</h4>\n<pre class="sourcecode"><code>FROM base\n|&gt; SELECT\n     sponge,\n     expert\n|&gt; AGGREGATE\n     AVG(expert) AS avg_expert\n   GROUP BY\n     sponge,\n     crown   -- ❌ Error Type 2: "crown" was not projected in the previous step\n|&gt; SELECT *;</code></pre>\n\n<h4>Standard SQL example</h4>\n<pre class="sourcecode"><code>WITH step_0 AS (\n  SELECT\n    sponge,\n    expert\n  FROM base\n),\nstep_1 AS (\n  SELECT\n    sponge,\n    AVG(expert) AS avg_expert\n  FROM step_0\n  GROUP BY\n    sponge,\n    crown   -- ❌ Error Type 2: "crown" was not projected in the previous step\n)\nSELECT *\nFROM step_1;</code></pre>')},()=>{r.print_string_on_stage('<h2>Possible Error Types</h2>\n<h3>Error Type 3: Aggregate uses an unknown column</h3>\n<p>\n  An aggregate function references a column that has not been projected or grouped in an earlier step.\n</p>\n<p>\n  <strong>Only occurs in:</strong>\n  <span class="sourcecode">COUNT(COL), AVG(COL), MIN(COL), MAX(COL), SUM(COL)</span>.\n</p>\n\n<h4>Pipe example</h4>\n<pre class="sourcecode"><code>FROM base\n|&gt; SELECT\n     sponge,\n     expert\n|&gt; AGGREGATE\n     AVG(expert) AS avg_expert,\n     SUM(crown)  AS sum_crown   -- ❌ Error Type 3: "crown" is unknown and can not be aggregated\n   GROUP BY\n     sponge\n|&gt; SELECT *;</code></pre>\n\n<h4>Standard SQL example</h4>\n<pre class="sourcecode"><code>WITH step_0 AS (\n  SELECT\n    sponge,\n    expert\n  FROM base\n),\nstep_1 AS (\n  SELECT\n    sponge,\n    AVG(expert) AS avg_expert,\n    SUM(crown)  AS sum_crown   -- ❌ Error Type 3: "crown" is unknown in step_1 and can not be aggregated\n  FROM step_0\n  GROUP BY \n    sponge\n)\nSELECT *\nFROM step_1;</code></pre>')}],pre_run_training_instructions:(r=n).string_page_command(r.convert_string_to_html_string("You have entered the training phase. It consists of 9 example queries per syntax.\n\nFor each query, decide whether it contains a syntactic error:\n- If an error is present, enter the line number where the error occurs (or where you first notice it).\n- If there is no error, press 0 and [ENTER] to continue.\n\nYou will advance to the next task after you enter your answer.")),pre_run_experiment_instructions:r.string_page_command(r.convert_string_to_html_string("You entered the experiment phase. It should take probably 15-30 minutes until the end of experiment.")),post_questionnaire:[G("Age","What's your age??",["younger than 18","between 18 and (excluding) 25","between 25 and (excluding) 30","between 30 and (excluding) 35","between 35 and (excluding) 40","between 40 and (excluding) 45","45 or older"]),G("Status","What is your current working status?",["Undergraduate student (BSc not yet finished)","Graduate student (at least BSc finished)","PhD student","Employed in software/data industry (Software developer, Data Scientist, Data Engineer, ...)","Other"]),G("Studies","In case you study, what's your subject?",["I do not study","Computer science","computer science related (such as information systems, aka WiInf)","something else in natural sciences","something else"]),G("LOCExperienceProgramming","What describes your programming background best?",["I never program","I rarely program","I write some LOC from time to time","I frequently write code","I write code almost every day"]),G("LOCExperienceSQL","What describes your SQL background best?",["I never write SQL queries","I rarely SQL queries","I write some SQL queries from time to time","I frequently SQL queries","I write SQL queries almost every day"]),G("YearsOfExperienceSoftware","How many years of experience do you have in software industry?",["none","less than or equal 1 year","more than 1 year, but less than or equal 3 years","more than 3 years, but less than or equal 5 year","more than 5 years"]),G("YearsOfExperienceData","How many years of experience do you have in Data industry?",["none","less than or equal 1 year","more than 1 year, but less than or equal 3 years","more than 3 years, but less than or equal 5 year","more than 5 years"]),G("impression","Which statement best describes your overall impression of the experiment?",["I did not notice a difference between standard SQL syntax and pipe-based SQL syntax.","Pipe-based SQL syntax made it slightly easier for me than standard SQL syntax.","Pipe-based SQL syntax made it much easier for me than standard SQL syntax.","Standard SQL syntax made it slightly easier for me than pipe-based SQL syntax.","Standard SQL syntax made it much easier for me than pipe-based SQL syntax."]),G("preference","Which SQL syntax did you find more appealing?",["Slightly prefer pipe-based SQL syntax over standard SQL syntax.","Slightly prefer standard SQL syntax over pipe-based SQL syntax.","Strongly prefer pipe-based SQL syntax over standard SQL syntax.","Strongly prefer standard SQL syntax over pipe-based SQL syntax.","No preference / I don't mind which SQL syntax is used."])],finish_pages:[r.string_page_command("<p>Almost done. Next, the experiment data will be downloaded (after pressing [Enter]).<br><br>Please, send the downloaded file to the experimenter: <a href='mailto:denis.artjuch@stud.uni-due.de'>denis.artjuch@stud.uni-due.de</a></p><p>By sending that mail, you agree that your (anonymized) data will be used for scientific analyses where your data (together with others in an anonymized way) will be published.<br><br>I.e., you agree with the information sheet, see <a href='https://github.com/denisartjuch/readability_sql_queries_vs_pipe_queries/blob/main/Agreement.pdf' target='_blank'>here</a>. Note, that it it no longer necessary to send a signed version of the agreement to the experimenter.<br><br>After sending your email, you can close this window.</p><p>Many thanks for your participation.<br>-Denis Artjuch</p>")],layout:[{variable:"queryKind",treatments:["sql","pipe"]},{variable:"totalInformationDifference",treatments:["0","4","8"]}],repetitions:12,measurement:(t=m,n=>new O(t(n))),training_configuration:{fixed_treatments:[["sql","0"],["sql","4"],["sql","8"],["pipe","0"],["pipe","4"],["pipe","8"],["sql","0"],["sql","4"],["sql","8"],["pipe","0"],["pipe","4"],["pipe","8"],["sql","0"],["sql","4"],["sql","8"],["pipe","0"],["pipe","4"],["pipe","8"]],can_be_cancelled:!1,can_be_repeated:!1},task_configuration:n=>{const e=n.treatment_value("queryKind"),t=Number(n.treatment_value("totalInformationDifference")),o=(A=W.filter(n=>n.totalInformationDifference===t))[f(A.length)],a=("pipe"===e?o.pipeQuery:o.sqlQuery).split("\n").map((n,e)=>`${(e+1).toString().padStart(3," ")} | ${n}`).join("\n"),i="<pre><code>"+r.convert_string_to_html_string(a)+"</code></pre>",S=(n,e)=>`\n        <div style="\n          display:flex;\n          gap:14px;\n          align-items:flex-start;\n          width:100%;\n        ">\n          <div style="flex:1 1 70%; min-width:0;">\n            ${n}\n          </div>\n\n          <aside style="\n            flex:0 0 30%;\n            max-width:30%;\n            min-width:260px;\n            border-left:1px solid rgba(0,0,0,0.12);\n            padding-left:10px;\n            padding-right:6px;\n            text-align:left;\n          ">\n            <div style="text-align:left;">\n              ${e}\n            </div>\n          </aside>\n        </div>\n      `;var A;n.do_print_task=()=>{r.clear_stage(),r.print_html_on_stage(S(i,'\n          <div style="\n            opacity:0.65;\n            font-size:0.95em;\n            line-height:1.35;\n          ">\n            Feedback will appear here after you answer.\n          </div>\n        '))},n.expected_answer=JSON.stringify("pipe"===e?o.errorLinePipe:o.errorLineSQL),n.do_print_after_task_information=()=>{var n;r.clear_stage(),null===(n=r.clear_error)||void 0===n||n.call(r);const t="pipe"===e?o.errorLinePipe:o.errorLineSQL,a=("pipe"===e?o.totalInformationUntilErrorPipe:o.totalInformationUntilErrorSQL,null===t?"There was no Error":String(t)),A=null===o.unknownName?"There was no Error":o.unknownName,l=`\n          <div style="font-size:0.98em; line-height:1.45;">\n            <p style="margin:0 0 10px 0;"><strong>The correct answer was:</strong> ${r.convert_string_to_html_string(a)}</p>\n\n            <p style="margin:0 0 16px 0;"><strong>The wrong attribute was:</strong> ${r.convert_string_to_html_string(A)}.</p>\n\n            <p style="margin:0 0 8px 0;"><strong>Hint</strong></p>\n            <p style="margin:0 0 16px 0; white-space:pre-wrap;">${r.convert_string_to_html_string("In case your answer was wrong:\n- Refer to the prior CTE/stage where the column should have been introduced.\n- Validate that the column has been projected or aggregated before it is referenced.")}</p>\n\n            <p style="margin:0 0 16px 0;">In case you feel not concentrated enough, make a short break.</p>\n\n            <p style="margin:0;"><strong>Press [Enter]</strong> to go on.</p>\n          </div>\n        `;r.print_html_on_stage(S(i,l))}}};var r,t;L(e.seed);let o=e.measurement(n),a=function(n){let e=v.from_layout(n.layout),r=new d(e,n.repetitions),t=new I(n.experiment_name,!1,r,e,n.repetitions,n.measurement,n.task_configuration),o=new B(n.training_configuration);return t.create_code_all_experiment_automatas({seed:n.seed,introduction_texts:n.introduction_pages,post_questionnaire:n.post_questionnaire,pre_run_training_output:n.pre_run_training_output,training_configuration:o,pre_run_experiment_output:n.pre_run_experiment_output,finish_texts:n.finish_pages,measurement:n.measurement,finish_function:n.finish_function})}({experiment_name:e.experiment_name,seed:e.seed,introduction_pages:e.introduction_pages,post_questionnaire:e.post_questionnaire,pre_run_training_output:e.pre_run_training_instructions,training_configuration:e.training_configuration,pre_run_experiment_output:e.pre_run_experiment_instructions,finish_pages:e.finish_pages,layout:e.layout,repetitions:e.repetitions,task_configuration:e.task_configuration,measurement:o,finish_function:n=>{document.removeEventListener("keydown",i),function(n,e){const r=new Blob(e,{type:"application/ssc"}),t=window.document.createElement("a");t.href=window.URL.createObjectURL(r),t.download=n,document.body.appendChild(t),t.click(),document.body.removeChild(t)}(e.experiment_name+".csv",n.generate_csv_data())}}),i=n=>{let e=(t="","Alt"==(r=n).key&&r.ctrlKey?"Alt+Ctrl":"Control"==r.key&&r.altKey?"Ctrl+Alt":(t+=r.altKey?"+Alt":"",t+=r.ctrlKey?"+Control":"","Alt"==r.key?"Alt":""+r.key+t));var r,t;a.input(e)};document.addEventListener("keydown",i,!1),null!=e.pre_activation_function&&e.pre_activation_function(a),a.set_active()}()})();
//# sourceMappingURL=experiment_configuration.js.map